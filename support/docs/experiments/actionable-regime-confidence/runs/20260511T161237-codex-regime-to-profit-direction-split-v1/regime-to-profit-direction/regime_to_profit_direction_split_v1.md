# Regime To Profit Direction Split v1

Run ID: `20260511T161237+0800-codex-regime-to-profit-direction-split-v1`

## Decision

Board B is no longer waiting for a usable regime input. It can proceed from the accepted scoped regime consumer map:

- `docs/experiments/actionable-regime-confidence/runs/20260511T153637-codex-regime-factor-consumer-map-v1/regime-factor-map/regime_factor_consumer_map_v1.csv`

This does not complete the older strict full Board A objective, but that older full-market/full-timeframe/full-cycle/full-species wording is now an elastic quality target. It is not a hard Board B blocker.

## Board A Parallel Work

Board A agents should now work on these lanes opportunistically without blocking Board B:

- Main regime confidence hardening: improve `Bull`, `Bear`, `Sideways`, and `Crisis` coverage beyond scoped context into broader exact-source/full-matrix evidence.
- Sub-regime diversity: add and validate child evidence under the roots, not beside them.
- Direct `Manipulation` species expansion: acquire source-owned positive rows, matched negatives, and provenance for spoofing/layering, quote stuffing, pinging, bear raid/painting tape, and social/text pump-dump variants.
- Root consumption audit: verify that BBN, CatBoost/path-ranker, and execution tree consume parent roots before child/sub-regime context.

## Board B Direction

Every profitability factor or Auto-Quant recipe must start from a parent regime root:

- `Bull`
- `Bear`
- `Sideways`
- `Crisis`
- optional scoped direct `Manipulation` overlay

Only after the root is declared may the recipe add sub-regime tags such as `TrendExpansion`, `RangeConsolidation`, `ExtremeStress`, `ReversalBrewing`, `SessionLiquidityCoreViable`, or `ThinLiquidity`.

## Non-Completion Guardrail

- `update_goal=false`
- Older strict full objective achieved: `false`
- Full-matrix work is elastic: `true`
- Trade usable: `false`
- Thresholds relaxed: `false`
- Runtime code changed: `false`
- Raw data committed: `false`

## Gate Result

`regime_to_profit_direction_split_v1=board_b_unblocked_board_a_parallel_hardening`
