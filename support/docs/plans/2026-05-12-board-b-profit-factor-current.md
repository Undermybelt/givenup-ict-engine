# Board B Profit Factor Current - ARCHIVE / REDIRECT

Current Board B entrypoint:
`support/docs/plans/2026-05-23-board-b-current.md`.

Current terminal decisions:
`support/docs/plans/2026-05-17-board-b-factor-refinement-small-cycle-current.md`.

This file is retained for historical compact contract text and older terminal
rows only. Do not append new Board B rows here; start from the current entrypoint
above.

Updated: `2026-05-17 20:15:00 +0800`

Purpose: clean current authority for Board B. Live profitability-factor
behavior belongs in `ict-engine` runtime code, state artifacts, provider configs,
candidate packs, admission targets, and compact evidence packets; old
append-only prose is archival only. Historical append-only prose is not a
planning source, status source, next-step source, or completion source.

Factor ingestion instruction surface: `support/docs/plans/2026-05-12-factor-candidate-ingestion-instructions.md`.

## Reader Contract

## Gate 1 Negative: IBKR MGC 5m Turtle Soup False Breakout 10D

Append-only terminal row at `2026-05-20 09:12:37 +0800` after observing active AQ completion and duplicate-searching the Board for `MGC 5m Turtle Soup`, `mgc5m_turtle_soup`, `090342`, `090505`, and `Turtle Soup`. Duplicate run `20260520T090505+0800-codex-ibkr-mgc5m-turtle-soup-false-breakout-10d-gate1-v1` produced identical metrics and is suppressed; use `090342` as terminal authority.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T090342+0800-codex-ibkr-mgc5m-turtle-soup-false-breakout-10d-gate1-v1/`.
- Factor id: `ibkr_mgc5m_turtle_soup_false_breakout_10d_gate1_v1`.
- Branch path: `FalseBreakoutReversal -> TurtleSoupReversal -> ibkr_mgc5m_turtle_soup_false_breakout_10d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 5m`.
- Diversity/source note: this gives Turtle Soup / false-breakout reversal a real `MGC/5m` precious-metals Gate 1 attempt, separate from MGC `1m` oscillator/reclaim packets and SI `5m` tight-range fade survivors.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `5m 10 D` rows; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=4`; `rank_total_trade_count=25`.
- Cost/density evidence: `MGC/soup_quality/5m` had only `1` trade, raw `+0.13%`, `1bps=+0.11%`, `2bps=+0.09%`, and `5bps=+0.03%`; the denser rows were negative (`MGC/soup_balanced/5m` `4` trades raw `-0.04%`, `5bps=-0.44%`; `MGC/soup_capitulation/5m` `8` trades raw `-0.46%`, `5bps=-1.26%`; `MGC/soup_dense/5m` `12` trades raw `-1.67%`, `5bps=-2.87%`). `exact_5m_survivors_2bps=[]`; `exact_5m_survivors_5bps=[]` because the only positive row was a one-trade sample and failed practical density.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc5m_turtle_soup_false_breakout_10d_gate1_v1.py`.

Interpretation:

- Turtle Soup improved source/family diversity for precious metals, but it did not produce a practical cost-stressed branch. A single positive trade at `5bps/side` is not density. Do not downstream or clone this shape until the hypothesis materially increases sample count and per-trade excursion.

## Gate 1 Negative: IBKR SI 1m VPT Divergence Reclaim 7D

Append-only terminal row at `2026-05-20 09:12:37 +0800` after observing active AQ completion and duplicate-searching the Board for `SI 1m VPT`, `si1m_vpt`, `090527`, and `VPTDivergence`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T090527+0800-codex-ibkr-si1m-vpt-divergence-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_si1m_vpt_divergence_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> VPTDivergenceReclaim -> ibkr_si1m_vpt_divergence_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Diversity/source note: this gives Volume Price Trend divergence/reclaim a real `SI/1m` Gate 1 attempt, separate from SI `1m` volatility/aroon variants and SI `5m` tight-range/ATR survivors.
- Provider/Gate 1 evidence: retained real IBKR `SI 202607` `1m 7 D` rows; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=51`.
- Cost/density evidence: VPT was dense enough to judge but structurally negative. `SI/vpt_dense/1m` had `27` trades, raw `-1.02%`, `1bps=-1.56%`, `2bps=-2.10%`, and `5bps=-3.72%`; `SI/vpt_balanced/1m` had `23` trades, raw `-0.64%`, `1bps=-1.10%`, `2bps=-1.56%`, and `5bps=-2.94%`; `SI/vpt_quality/1m` had `1` losing trade; `SI/vpt_washout/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si1m_vpt_divergence_reclaim_7d_gate1_v1.py`.

Interpretation:

- VPT divergence/reclaim adds a fair volume-price family sample, but it fails before downstream: dense rows are negative raw and deteriorate under tiny cost stress. Do not use this as an SI practical branch. Keep practical work on the existing SI `5m` tight-range fade survivor or M2K `1m` liquidity-sweep survivor, where the blocker is execution admission rather than Gate 1 economics.

This file is the default Board B entrypoint. Do not enter historical logs during
normal iteration.

Use runtime/state surfaces first. Board B status updates must be terminal
decisions only: `keep`, `drop`, `incubate`, `blocked`, or `handoff`, with compact
evidence paths. Routine coordination and start claims must not be appended here.

Historical material may be opened only by exact artifact id, exact heading, or
exact evidence path when this file names a gap. It may never supply a new task,
claim, acceptance gate, route, or Board A/B boundary.

Useful factor findings must enter through the repo-local candidate-pack and
admission commands named in the factor ingestion instruction surface. This file
only records compact decisions and pointers to evidence packets.

## Role

Board B owns profitability-factor discovery, training, and downstream admission. It must root every candidate in:

`main_regime -> sub_regime -> sub_sub_regime_or_profit_factor -> profit_factor`

Board B must not take over Board A regime work, and must not touch non-owner roots such as `141000`.

## Parallel Boundary

- Expected concurrency is high: assume about 20 agents may work Board B while
  about 20 agents work Board A. Agents must not use this board as a shared lock
  table or scratchpad.
- Start claims are ephemeral: use `/tmp/ict-engine-agent-claims/board-b/` or an
  equivalent process-local lock outside the repo. Do not create claim docs,
  claim rows, TODO sprawl, or sibling plan files in the repo for starting work.
- Repo writes are allowed only for durable terminal evidence: compact run-root
  packets and one current-board decision row after the idea has evidence.
- Board B work can run in parallel with Board A only when it stays on
  profitability-factor discovery, candidate packs, provider/AQ provenance,
  branch-keyed profitability statistics, admission targets, and downstream
  handoff evidence for an already-frozen Board A context label.
- Board B must not claim, repair, promote, reject, relabel, or rerun Board A
  regime-confidence roots, posterior state, market-state labels, provider
  authority preflights owned by Board A, or recovered regime-confidence assets.
- Board A context fields inside Board B artifacts are attribution keys only.
  They are not Board B deliverables and do not authorize regime work.

## Idea Collision Rule

- If the chosen Board B idea is already claimed, active, done, or blocked, do not
  continue, repair, re-run, summarize, or "help" that idea.
- Write only a compact `duplicate_suppressed` or no-takeover note if useful,
  then pick a new unclaimed Board B idea from `Factor Gate Model` or
  `Live Gates`.
- A new idea must be different by at least one real ownership axis: factor,
  root regime, symbol/instrument set, provider/window, artifact root, or gate
  being tested.
- Do not create a sibling readback just to stay busy. If no unclaimed Board B
  idea exists, hand off the blocker in this file and stop.

## Code/Docs Isolation

- This file is not a runtime input. No Rust, Python, shell, provider, Auto-Quant,
  training, or workflow code may import, parse, grep, or depend on this markdown.
- Runtime state must live in code, configs, explicit state directories, provider
  manifests, candidate packs, admission targets, JSON/CSV/JSONL artifacts, or
  CLI output.
- Markdown may cite artifact paths and commands for humans/agents, but it must
  not become a hidden dependency, router, data source, fixture, or model input.
- If code needs a rule currently written only here, promote that rule into a
  typed config, command flag, schema, or test fixture first; do not read the doc.

## Current Contract

- Discovery is `profit_seed_first`: find a concrete profitable seed inside one clearly identified regime branch before running broad promotion audits.
- Factor training is isolated by default. Do not make every factor prove the full
  provider -> Auto-Quant -> Pre-Bayes -> BBN -> CatBoost -> execution-tree chain
  before it is useful.
- The normal Board B unit is one factor plus one gate, optionally conditioned on
  one root regime. The question is: "does this factor help in this root regime?"
- Promotion is stricter than seed discovery, but promotion should still be
  staged: first factor gate, then optional root-regime fit, then portability or
  downstream handoff only if the first two are worth keeping.
- Full-market / full-instrument closure is an elastic quality target, not a
  hard Board B blocker. Board B should prefer "each market or instrument family
  has at least one strong branch-local factor" over "every factor must pass
  everywhere."
- Branch path must survive as first-class data and must be regime-rooted:
  `main_regime -> sub_regime -> ... -> first_profit_factor -> overlay_profit_factor`.
  Market, product, provider, symbol, contract, base timeframe, ladder timeframe,
  and provider window are labels/provenance fields only. They must not become
  branch roots.
- The current allowed main-regime roots for normalized artifacts are `Crisis`,
  `ExtremeStress`, `MomentumContinuation`, `Range`, `RangeConsolidation`,
  `RangeReversion`, `ReversalBrewing`, `SessionLiquidityCoreViable`,
  `ThinLiquidity`, `Transition`, `TrendExpansion`, and `WideRange`.
  Historical rows whose first branch segment is a factor family, market family,
  symbol group, or strategy family are legacy evidence and need a regime-root
  remap before any future downstream/promotion claim.
- Previous rows that used paths such as
  `FUTURES -> precious_metals -> SI -> 5m -> RangeConsolidation -> ...` or
  `CryptoLinearPerp -> RangeReversion -> ...` are legacy notation. Read them as
  labels plus canonical branches like `RangeConsolidation -> ...` or
  `RangeReversion -> ...`. New artifacts should write both the canonical
  `branch_path` and separate labels.
- Sidecar CSV branch paths are not enough when a packet claims root-regime fit
  or downstream handoff.
- `exit=0` is not success unless the artifact covers the actual gate.

## Regime Transition Laws

Board B should treat the regime map with these three laws unless a later
verified packet replaces them:

1. All regimes are intraday. Board B factor routing should not assume a
   multi-day regime root by default.
2. Trend must end into range, and range must resolve into expansion. The
   admissible regime path family is therefore limited to:
   - `up -> range -> up`
   - `up -> range -> down`
   - `down -> range -> down`
   - `down -> range -> up`
3. Reversal is not a standalone regime root. It is the final segment before a
   range resolves and the first segment after an expansion exhausts.

Board B implication:

- factor branching should treat reversal evidence as a boundary-state signal
  inside `range -> expansion` handoff logic, not as a separate long-lived root
  regime

## Factor Gate Model

Use the simplest gate that answers the current isolated factor question.

- Gate 1, factor viability: enough observations, positive expectancy or useful
  negative classification, cost/slippage sanity, and no obvious leakage.
- Gate 2, root-regime fit: compare the factor inside one frozen Board A root
  regime versus outside it; keep the factor only if it improves that root or is
  clearly marked as root-agnostic.
- Gate 3, portability check: provider/window/instrument reproduction only after
  Gate 1 or Gate 2 says the factor is worth carrying.
- Gate 3 pass does not require every provider, every market, or every
  instrument to agree. A single branch can be accepted when it has a credible
  champion factor on its own usable provider/window, while broader portability
  remains follow-up evidence.
- Gate 4, downstream handoff: Pre-Bayes, BBN, CatBoost/path-ranker, execution
  tree, and feedback/update are optional consumer-admission checks, not the
  default requirement for every isolated factor training run.

Default answer format for each Board B idea:

`factor_id`, `claimed_gate`, `root_regime` or `root_agnostic`,
`evidence_path`, `decision=keep/drop/incubate/blocked`,
`next_unclaimed_idea`.

## Current Status

- Handoff `2026-05-18 / hermes-yf-etf-tod-slot-alpha-15m-cost-gate-v2`: continued source-backed TOD slot-alpha factor training under branch `Range -> IntradaySeasonality -> same_time_slot_alpha -> yf_etf_tod_slot_alpha_15m_cost_gate_v2` without mutating other agents' packets. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260518T125346+0800-hermes-yf-etf-tod-slot-alpha-15m-cost-gate-v2/summaries/terminal_decision_summary.md`; downstream readback: `support/docs/experiments/actionable-regime-confidence/runs/20260518T125346+0800-hermes-yf-etf-tod-slot-alpha-15m-cost-gate-v2/downstream-20260518T130702+0800/summaries/terminal_decision_summary.md`; Auto-Quant autoresearch workspace: `support/docs/experiments/actionable-regime-confidence/runs/20260518T125346+0800-hermes-yf-etf-tod-slot-alpha-15m-cost-gate-v2/autoresearch-20260518T130900/state/.deps/auto-quant`. Real work executed: YF 15m SPY/QQQ/IWM Gate 1 AQ rows were imported, BBN prior initialized, analyze/workflow/Pre-Bayes/export/policy ran, CatBoost/path-ranker trained/applied/registered/enabled, and post-ranker analyze/workflow/Pre-Bayes/policy readbacks all exited `0`; then `factor-autoresearch --backend auto-quant` prepared Auto-Quant synthetic OHLCV data and `run_tomac.py` executed three seed strategies. Result: Gate 1 has `3/3` positive AQ rows and downstream exact branch survived with path-ranker visible/used, but execution remains `fail_closed` / `execution_observe_only` / `transition_guardrail`, `ranker_validation_ready=false`, `promotion_allowed=false`, `trade_usable=false`. Auto-Quant autonomous run is positive mechanically (`3` strategies succeeded, each `4` trades, `15.63%`, Sharpe `6.2451` on synthetic `ETF/USD` 1h), but this is seed evidence only because the synthetic profile collapsed provider symbols/timeframe and is not live parity.

- Handoff `2026-05-18 / codex-yf-leveraged-etf-trendday-late-momentum-5m-aq-v1`: followed the operator correction to hunt source-backed practical factors instead of continuing low-quality local sweeps, then fed the strongest immediately codeable candidate into Auto-Quant. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260518T124306+0800-codex-yf-leveraged-etf-trendday-late-momentum-5m-aq-v1/summaries/terminal_decision_summary.md`; structured summary: `support/docs/experiments/actionable-regime-confidence/runs/20260518T124306+0800-codex-yf-leveraged-etf-trendday-late-momentum-5m-aq-v1/summaries/yf_leveraged_etf_trendday_late_momentum_etf_5m_v1.json`; provider matrix: `support/docs/experiments/actionable-regime-confidence/runs/20260518T124306+0800-codex-yf-leveraged-etf-trendday-late-momentum-5m-aq-v1/summaries/provider_provenance_matrix.csv`. Source basis: Ernie Chan / QuantRocket leveraged ETF trend-day late-session momentum, with Ivanov-Lenkey LETF counterevidence retained in material notes. Real work executed: a first YF run hit the known intraday-window `HTTP 422` blocker and was preserved as provider-window evidence; the corrected run narrowed the timerange to `20260323-20260516`, fetched yfinance/YF `UPRO/SPXL/SSO` `5m` rows (`3043` each), compiled three strategy materials, and ran Auto-Quant batch, dispatch, and rank; all useful corrected commands exited `0`. Result: Gate 1 is a keep candidate, not live promotion: `rank_rows=3`, `rank_total_trade_count=65`, `branch_fields_preserved=true`, `SPXL=24 trades / 75.0% / +0.69%`, `SSO=17 / 64.7059% / +0.88%`, `UPRO=24 / 75.0% / -0.29%`; `downstream_allowed=true` for follow-up cost/execution verification, but `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false` until Pre-Bayes/BBN/CatBoost/execution-tree and cost gates pass.

- Handoff `2026-05-17 / codex-board-b-smallcycle-sweep-nq4h-confirmation-v1`: followed the operator correction that existing market-state factors are sufficient and Board B should use the corresponding profit factor inside that state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T221500+0800-codex-board-b-smallcycle-sweep-nq4h-confirmation-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260517T221500+0800-codex-board-b-smallcycle-sweep-nq4h-confirmation-v1/checks/prompt_to_artifact_checklist.csv`; final execution trace: `support/docs/experiments/actionable-regime-confidence/runs/20260517T221500+0800-codex-board-b-smallcycle-sweep-nq4h-confirmation-v1/state/SMALLCYCLE_SWEEP_NQ4H_CONFIRMATION/execution_tree_trace.json`; final analyze readback: `support/docs/experiments/actionable-regime-confidence/runs/20260517T221500+0800-codex-board-b-smallcycle-sweep-nq4h-confirmation-v1/checks/36_analyze_nq_bounded_after_rescore.json`. Real work executed: opened only a 4h confirmation/veto slice for `Transition -> LiquiditySweep -> sweep_reclaim_small_cycle -> liquidity_sweep_reclaim_15m_wide_v1`; rejected a direct `4h` override because the parent strategy has a faster `1h` informative feed; ran a run-root-local 15m strategy variant requiring 4h trend confirmation through Auto-Quant; imported the result, initialized BBN prior, converted `62` AQ trades to branch-preserving real-trade feedback, trained/applied CatBoost, registered/enabled the path-ranker, refreshed Pre-Bayes/workflow/policy readbacks, created bounded `15m/1h/4h` analyze inputs, ran fresh bounded analyze, then exported/refreshed the post-analyze target and reran CatBoost score application/runtime enablement so the current execution tree consumed the intended branch score. Result: AQ confirmation is positive (`62` trades, `30/32/0` win/loss/draw, `8.67%` total profit, PF `1.7157`), BBN prior passed, structural validation is ready (`raw_scored_mature=63/30`, `production_validation=62/30`, `observation_validation=62/30`), and final execution-tree readback shows the exact branch using CatBoost from `candidate_set` with `path_ranker_raw_score=0.2732645693067194`, `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, and `ranker_validation_ready=true`. Decision: `handoff`, not promotion; the fresh analyze snapshot is still `pre_bayes_gate_status=observe_only`, execution gates at `gate_status=observe` / `branch=transition_guardrail` / `execution_bias=guarded`, latest candidate is `actionable=false` / `candidate_status=no_trade` / `review_status=discard`, and the terminal decision hint is `execution_guarded_due_to_high_transition_hazard`.

- Handoff `2026-05-17 / codex-ob-runtime-branch-binding-v1`: repaired and verified owner-symbol `order_block_variant_classifier` branch-field binding without mutating the prior owner packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T073100+0800-codex-ob-runtime-branch-binding-v1/summaries/terminal_decision_summary.md`; branch readback: `support/docs/experiments/actionable-regime-confidence/runs/20260517T073100+0800-codex-ob-runtime-branch-binding-v1/checks/branch_binding_readback.json`; final chain readback: `support/docs/experiments/actionable-regime-confidence/runs/20260517T073100+0800-codex-ob-runtime-branch-binding-v1/checks/final_chain_readback.json`. Real work executed: patched `src/application/orchestration/structural_playbook.rs`, ran `cargo test order_block -- --nocapture`, copied the prior OB state into an isolated run root, then reran `export-structural-path-ranking-target`, `policy-training-status`, `workflow-status --refresh`, `analyze`, and post-analyze `workflow-status --refresh`; all useful commands exited `0` after one rejected export probe with unsupported `--output-format` exited `2`. Result: exact rooted branch `Transition -> OrderBlockVariant -> ob_mitigation_breaker_rejection -> order_block_variant_classifier_v1` now appears as a current structural target row with populated `regime_profit_branch_path`, `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`, `profit_factor`, and OB context fields; execution tree now reports `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, `path_ranker_runtime_source=candidate_set`, and `path_ranker_raw_score=0.7586923138550259`. Decision: `handoff`, not promotion; the branch still fails closed with `closed_loop_branch_admission.status=fail_closed`, `ready=false`, `actionable=false`, `execution_tree_gate_status=observe`, `ranker_validation_ready=false`, and `blocking_truth.status=bridge_needs_confirmation` because bridge gap remains `0.037`.

- Handoff `2026-05-17 / codex-provider-quartet-ibkr-realtrade-replay-v1`: replayed the existing provider-quartet branch's `124` real IBKR Auto-Quant trades into structural feedback on an isolated copied state, without taking over other Board B lanes or mutating the source run. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T072820+0800-codex-provider-quartet-ibkr-realtrade-replay-v1/summaries/terminal_decision_summary.md`; final readback: `support/docs/experiments/actionable-regime-confidence/runs/20260517T072820+0800-codex-provider-quartet-ibkr-realtrade-replay-v1/checks/chain_readback_after_feedback.json`. Real work executed: converted `49` wins, `47` losses, and `28` breakevens into `structural-feedback-v1`, ran `update --feedback-file --ensemble` `124/124` times with exit `0`, then reran Pre-Bayes, policy training, structural target export, CatBoost apply, score apply, runtime enable, analyze, workflow, and execution-tree readback. Result: the exact branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1` now has `feedback_rows_with_structural_feedback=124`, `raw_scored_mature=7999/30`, `production_validation=7999/30`, `observation_validation=124/30`, and final execution-tree score consumption is `visible=true` / `used=true` with `ranker_validation_ready=true`. Decision: `handoff`, not promotion; execution still fails closed at `execution_observe_only` / `transition_guardrail` / `guarded`, and both entry models remain `matched_rows=0` with `consumed_analyze_run_missing_from_state:124`.

- Handoff `2026-05-17 / codex-provider-quartet-post-realtrades-readback-v1`: cloned the existing provider-quartet rooted AQ chain state after the IBKR real-trade export and reran downstream readbacks without mutating the shared run state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T072903+0800-codex-provider-quartet-post-realtrades-readback-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260517T072903+0800-codex-provider-quartet-post-realtrades-readback-v1/checks/prompt_to_artifact_checklist.csv`; combined readback: `support/docs/experiments/actionable-regime-confidence/runs/20260517T072903+0800-codex-provider-quartet-post-realtrades-readback-v1/checks/post_realtrades_readback_summary.json`. Real work executed: copied `20260516T234500+0800-hermes-board-b-regime-rooted-provider-quartet-aq-chain-v1/state` to `/tmp/ict-engine-provider-quartet-post-realtrades-readback-v1/state`, ran `policy-training-status`, `workflow-status --refresh`, and `pre-bayes-status --refresh`, then attempted non-dry-run `auto-quant-ingest-real-trades` on the copied state and reran the same readbacks. Result: the copied state has the IBKR real-trade export (`124` rows, `49` wins, `47` losses, `28` breakevens, branch path preserved) and an existing `auto_quant_real_trades_ingested` ledger entry, so re-ingest correctly failed on the same-content hash guard. Downstream still fails closed: Pre-Bayes is `pass_hard`, CatBoost runtime is `enabled_candidate_set_ready` with `runtime_matches=4`, but policy remains `update_runs=0`, `mature_rows=0`, `rows_with_training_weight=0`, `observation_validation=0/30`, both entry models have `matched_rows=0`, and workflow remains `execution_observe_only` with `closed_loop_branch_admission.status=fail_closed`. Decision: `handoff`, not promotion; next useful work is the bridge from `auto_quant_real_trades_ingested` ledger entries into policy/entry-model training observations, not provider ingress, AQ rerun, or forced re-ingest without rollback.

- Done `2026-05-17 / codex-board-b-objective-completion-audit-v13`: completion audit for the operator-requested regime-rooted provider-quartet AQ/downstream chain. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T201500+0800-codex-board-b-objective-completion-audit-v13/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260517T201500+0800-codex-board-b-objective-completion-audit-v13/checks/prompt_to_artifact_checklist.csv`. Real evidence inspected: current board, active `/tmp/ict-engine-agent-claims/board-b` roots, and `support/docs/experiments/actionable-regime-confidence/runs/20260516T234500+0800-hermes-board-b-regime-rooted-provider-quartet-aq-chain-v1/checks/chain_readback.json`. Result: the requested shape is now complete as an evidence handoff: yfinance, TradingViewRemix/local-stdio, IBKR, and Kraken all entered one branch-rooted AQ chain; AQ rank preserved `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`, `profit_factor`, and `regime_profit_branch_path`; BBN prior init, Pre-Bayes readback, CatBoost/path-ranker, and execution-tree readback all ran; execution tree consumed the CatBoost ranker score. Decision: `done_for_requested_full_chain_shape`, not promotion; `promotion_allowed=false` and `trade_usable=false` because provider profitability is mixed, structural target has `mature_rows=0` / `rows_with_training_weight=0`, and execution remains observe/transition-guardrail.

- Handoff `2026-05-17 / codex-liquidity-texture-registered-model-match-diagnosis-v1`: diagnosed the owner-symbol `liquidity_pool_texture` registered-model mismatch from the prior `IBKR TLT` bridge without mutating the source owner state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T200000+0800-codex-liquidity-texture-registered-model-match-diagnosis-v1/summaries/terminal_decision_summary.md`; final execution readback: `support/docs/experiments/actionable-regime-confidence/runs/20260517T200000+0800-codex-liquidity-texture-registered-model-match-diagnosis-v1/checks/final_execution_tree_trace_readback.json`. Real work executed: copied the prior owner state, proved the registered direct-model artifact URI was repo-relative while the runtime loader resolves relative URIs under `<state>/<symbol>/policy_training/`, patched only the copied artifact URI to `path_ranker_model/path_ranker_direct_model.json`, then reran `policy-training-status`, `workflow-status`, full-frame `analyze`, and final workflow/policy readbacks. Result: the copied state flips from `enabled_registered_model_invalid/runtime_matches=0` to `enabled_registered_model_ready/runtime_matches=3`; workflow uses `registered_model_artifact`; fresh execution tree sees and uses the registered model (`path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, `path_ranker_model_family=weighted_feature_sum_v1`). Decision: `handoff`, not promotion; target maturity remains `mature_rows=0`, `rows_with_training_weight=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and execution stays observe/transition-guardrail.

- Handoff `2026-05-16 / hermes-board-b-regime-rooted-provider-quartet-aq-chain-v1`: executed one isolated regime-rooted provider-quartet profitability chain for `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1`, without taking over existing Board B lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T234500+0800-hermes-board-b-regime-rooted-provider-quartet-aq-chain-v1/summaries/terminal_decision_summary.md`; readback: `support/docs/experiments/actionable-regime-confidence/runs/20260516T234500+0800-hermes-board-b-regime-rooted-provider-quartet-aq-chain-v1/checks/chain_readback.json`. Real work executed: `provider-status` plus provider fetches for yfinance `QQQ 1h` (`366` rows), TradingViewRemix/local-stdio `NASDAQ:QQQ 1h` (`152` rows), IBKR `QQQ 1h` (`480` rows), and Kraken `XBTUSD 1h` (`721` rows); then `auto-quant-agent-material-batch`, `dispatch`, `rank`, `auto-quant-results-import`, `auto-quant-prior-init`, `analyze`, `pre-bayes-status`, `workflow-status`, `export-structural-path-ranking-target`, CatBoost train/apply, `apply-structural-path-ranking-external-scores`, `register-structural-path-ranking-trainer-artifact --model-family catboost`, `enable-structural-path-ranking-runtime`, and post-ranker analyze/workflow/policy readback all exited `0`. Result: AQ rank preserved `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`, `profit_factor`, and full `regime_profit_branch_path` for all four provider rows; execution tree consumed CatBoost (`path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, `path_ranker_model_family=catboost`). Decision: `handoff`, not promotion; provider portability is mixed (IBKR positive, TVR weak positive, yfinance/Kraken negative), structural target remains immature (`mature_rows=0`, `rows_with_training_weight=0`), and execution stays observe/transition-guardrail.

- Blocked `2026-05-16 / codex-board-b-no-unclaimed-after-tvr-recovery-v1`: read-only no-takeover/collision audit after TradingViewRemix local stdio OHLCV recovery. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T221100+0800-codex-board-b-no-unclaimed-after-tvr-recovery-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T221100+0800-codex-board-b-no-unclaimed-after-tvr-recovery-v1/checks/collision_audit.json`. Real work executed: refreshed active Board B claims and current board top only; no provider fetch, Auto-Quant, feedback update, source edit, Pre-Bayes/BBN, CatBoost/path-ranker, or execution-tree promotion was run. Result: the TVR QQQ entry-density owner terminalized while this audit was in flight, bridging `154` TradingViewRemix `NASDAQ:QQQ` `1h` rows into the rooted direct-limit chain; it still fails closed at `runtime_selection=enabled_no_matching_scores`, insufficient validation rows, `ready=false`, and `execution_gate_status=execution_observe_only`. A tentative order-block ranker-consumption continuation was suppressed because the source packet already proves post-analyze execution-tree ranker visibility/usage; remaining order-block blockers are maturity/training rows, entry-model matches, and observe-only execution. Decision: `blocked`, not promotion; `update_goal=false`.

- Handoff `2026-05-16 / codex-market-structure-tvr-qqq-entry-density-v1`: fed current TradingViewRemix/`tradingview_mcp` local-stdio OHLCV into the same rooted direct-limit branch as the yfinance/Kraken/IBKR entry-density packets, using isolated copied state and no source edits. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220325+0800-codex-market-structure-tvr-qqq-entry-density-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220325+0800-codex-market-structure-tvr-qqq-entry-density-v1/checks/artifact_validation.json`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220325+0800-codex-market-structure-tvr-qqq-entry-density-v1/checks/prompt_to_artifact_checklist.csv`. Real work executed: after two shell-wrapper misses (`timeout` unavailable on macOS and one quoted command issue, both before useful engine work), `market-data-harness` fetched `154` `NASDAQ:QQQ` `1h` rows from `tradingview_mcp`, then direct-binary `analyze`, branch-aware `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate` all exited `0`. Result: analyze run `analyze:MARKET_STRUCTURE_DIRECT_LIMIT:20260516T140755.258Z` and feedback preserved `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1`; Pre-Bayes gate is `pass_neutralized`; exported target has `rows=4`, `mature_rows=2`, `rows_with_training_weight=2`; workflow selects the exact rooted path with `execution_readiness=0.57`, but `path_ranker_runtime.status=enabled_no_matching_scores`, `ready=false`, `actionable=false`, and `execution_gate_status=execution_observe_only`. Decision: `handoff`, not promotion; the old TVR OHLCV provider gap is now bridged into the rooted chain, but path-ranker scores still do not match the current candidate set and execution remains observe-only.

- Handoff `2026-05-16 / codex-order-block-owner-rank-bridge-v1`: carried the exact rooted order-block branch into the fresh yfinance owner-symbol state through the existing `auto_quant_agent_material_rank` artifact contract, then reran structural export, CatBoost/path-ranker integration, score apply, policy readback, workflow readback, and a same-state analyze rerun on an isolated `/tmp` state copy. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215000+0800-codex-order-block-owner-rank-bridge-v1/summaries/terminal_decision_summary.md`; readback: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215000+0800-codex-order-block-owner-rank-bridge-v1/checks/bridge_readback.json`. Real work executed: created one isolated rank artifact/ledger entry for `Transition -> OrderBlockVariant -> ob_mitigation_breaker_rejection -> order_block_variant_classifier_v1`, ran `export-structural-path-ranking-target`, `path_ranker_integration.py --allow-direct-fallback --register-runtime-artifact`, `apply-structural-path-ranking-external-scores`, `policy-training-status`, `workflow-status --refresh`, `analyze`, and another `workflow-status --refresh`; all exited `0`. Result: structural target now includes the exact branch row with first-class branch fields, CatBoost scores all `4` target rows, policy sees `runtime_selection_status=enabled_registered_artifact_ready`, `runtime_source_kind=registered_artifact`, `runtime_active_match_count=3`, workflow sees `path_ranker_raw_score=0.7782076731306604` from `registered_artifact`, and post-rerun `execution_tree_trace.json` now has `path_ranker_score_visible_to_execution_tree=true` / `path_ranker_score_used_by_execution_tree=true` / `path_ranker_runtime_source=registered_artifact`. Decision: `handoff`, not promotion; maturity/training remain zero (`mature_rows=0`, `rows_with_training_weight=0`), entry models remain `matched_rows=[0,0]`, workflow remains `execution_observe_only`, and execution tree still gates at `observe`.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v10`: prompt-to-artifact completion audit refreshed after exact-IBKR AQ evidence, the failed robust AQ iteration, and the newer TradingViewRemix local-stdio parity update. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215526+0800-codex-board-b-objective-completion-audit-v10/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215526+0800-codex-board-b-objective-completion-audit-v10/checks/prompt_to_artifact_checklist.csv`. Real work executed: no provider fetch, source edit, update-feedback mutation, Pre-Bayes mutation, BBN, CatBoost/path-ranker mutation, or execution-tree promotion was started by this audit; it inspected current terminal evidence and wrote durable audit artifacts only. Result: objective remains incomplete. Exact branch paths and real AQ execution exist, and TVR OHLCV local stdio is now usable, but no packet has carried `ibkr + tradingview_mcp + yfinance + kraken` material through one rooted profitability-factor chain into non-observe execution; exact IBKR AQ robust seeds failed `profit_floor`, exact entry models remain `matched_rows=0`, ranker validation is below floor, and workflow remains observe-only. Decision: `handoff`, not promotion; `update_goal=false`.

- Blocked `2026-05-16 / codex-board-b-no-unclaimed-after-research-v1`: read-only collision audit after the external research packet and SMT threshold split. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215942+0800-codex-board-b-no-unclaimed-after-research-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215942+0800-codex-board-b-no-unclaimed-after-research-v1/checks/collision_audit.json`. Real work executed: refreshed current board top, active `/tmp/ict-engine-agent-claims/board-b` owners, and live processes only; no provider fetch, Auto-Quant, feedback update, source edit, Pre-Bayes/BBN, CatBoost/path-ranker, or execution-tree promotion was run. Result: the natural next runtime lanes are actively owned or just terminalized: market-structure CatBoost execution-context sweep, market-structure factor-research followup, order-block owner-rank bridge, objective completion audit v10, and SMT threshold split. Decision: `blocked`, not promotion; next safe action is an active-owner terminal handoff, TradingViewRemix rate-limit recovery, or a genuinely distinct provider-backed factor/window/symbol branch.

- Handoff `2026-05-16 / codex-market-structure-catboost-execution-context-sweep-v1`: bounded retained-yfinance execution-context sweep after the exact direct-limit CatBoost refresh, without source edits, provider fetches, or TVR retries. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-market-structure-catboost-execution-context-sweep-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-market-structure-catboost-execution-context-sweep-v1/checks/artifact_validation.json`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-market-structure-catboost-execution-context-sweep-v1/checks/prompt_to_artifact_checklist.csv`. Real work executed: copied the completed CatBoost state from `codex-market-structure-catboost-artifact-refresh-v1`, ran direct-binary `analyze` across `22` retained yfinance `1h` clean JSON contexts, then reran final ACWI `analyze`, `workflow-status --phase execution-candidate`, and `policy-training-status`; all commands exited `0`. Result: CatBoost/entry-model evidence stays strong (`score_model_family=catboost`, `runtime_matches=3`, `raw_scored_mature=1560/30`, `production_validation=1560/30`, `observation_validation=55/30`, `cisd_rb_long_v1 matched_rows=53`, `breaker_rb_long_v1 matched_rows=50`), but no retained context reached closed-loop readiness (`closed_loop_ready_count=0`). Sweep branches were `transition_guardrail=12`, `block_crowded=8`, and `fill_viable=2`; ranker was visible/used in `6/22` contexts, but every visible/used case still failed closed. Best final ACWI readback remained `execution_observe_only`, `ready=false`, execution tree `observe/transition_guardrail`, and `closed_loop_status=fail_closed`. Decision: `handoff`, not promotion; the blocker is broader than the original XME context, and next useful work is feeding the now-ready TradingViewRemix OHLCV material into the same rooted branch or repairing the execution admission/ranker-visibility contract.

- Handoff `2026-05-16 / codex-board-b-tvr-local-stdio-parity-v1`: refreshed the objective-required TradingViewRemix/`tradingview_mcp` provider blocker after older HTTP `429` evidence, without taking over active factor lanes or mutating runtime state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215312+0800-codex-board-b-tvr-local-stdio-parity-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215312+0800-codex-board-b-tvr-local-stdio-parity-v1/checks/prompt_to_artifact_checklist.csv`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215312+0800-codex-board-b-tvr-local-stdio-parity-v1/checks/artifact_validation.json`. Real work executed: direct-binary `provider-status` showed `tradingview_mcp`, `yfinance`, provider-venv `ibkr`, and provider-venv `kraken_public` all ready for their checked market-data surfaces, and `market-data-harness` fetched `21` `NASDAQ:QQQ` daily rows from `tradingview_mcp` local stdio OHLCV (`2026-04-17T13:30:00Z` to `2026-05-15T13:30:00Z`) with no missing roles or warnings. Decision: `handoff`, not promotion; the old blanket TVR-unavailable blocker is stale for OHLCV, but Board B completion still requires feeding fresh TVR material into one unclaimed rooted factor chain through Auto-Quant/native ingest, Pre-Bayes/filter, BBN/entry models, CatBoost/path-ranker, and execution-tree non-observe admission.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v11`: read-only prompt-to-artifact completion audit after the fresh `tradingview_mcp` local stdio OHLCV parity packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220119+0800-codex-board-b-objective-completion-audit-v11/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220119+0800-codex-board-b-objective-completion-audit-v11/checks/prompt_to_artifact_checklist.csv`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220119+0800-codex-board-b-objective-completion-audit-v11/checks/artifact_validation.json`. Result: completion remains not achieved. Positive delta: the old blanket TradingViewRemix-unavailable blocker is stale for OHLCV because current `tradingview_mcp` status is `ready_degraded` and the harness fetched `21` `NASDAQ:QQQ` daily rows. Missing evidence remains decisive: no single rooted profitability-factor chain has consumed those fresh TVR rows through Auto-Quant/native ingest, update learning, Pre-Bayes/filter, BBN/entry models, CatBoost/path-ranker, and execution-tree non-observe admission; market-structure remains `execution_blocked`, Brooks remains `execution_observe_only`, and no inspected packet has `promotion_allowed=true` or `trade_usable=true`. Decision: `handoff`, not promotion; `update_goal=false`.

- Handoff `2026-05-16 / codex-board-b-smt-threshold-split-v1`: research-derived SMT threshold split on the existing provider-backed historical strict SMT rows; no provider fetch, Auto-Quant, feedback update, source edit, Pre-Bayes/BBN, CatBoost/path-ranker, or execution-tree promotion was performed. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215421+0800-codex-board-b-smt-threshold-split-v1/summaries/terminal_decision_summary.md`; packet: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215421+0800-codex-board-b-smt-threshold-split-v1/materials/smt_threshold_split_packet.json`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215421+0800-codex-board-b-smt-threshold-split-v1/checks/artifact_validation.json`. Real work executed: threshold grid over `max_event_delta_bars`, `relationship_confidence`, MSS/CISD, displacement, and PDA requirements using existing historical strict SMT rows. Best rule `delta1_conf0.85_mss1_disp0_pda1` selected `16` rows with win rate `0.875`, expectancy `0.0036009715714354585`, `3` inverse rows, `0` missing level rows, and `0` actionable rows; per-regime stats were trend `3` rows / `1.0` win rate, range `9` / `1.0`, transition `4` / `0.5`, stress/other `0`. Decision: `handoff`, not promotion; it is a cleaner candidate input, but it drops required-pair coverage for `NQ/YM` and `XAUUSD/DXY`, remains thin by regime, and SMT is still confirmation-only.

- Drop `2026-05-16 / codex-market-structure-exact-ibkr-aq-robust-seed-iteration-v1`: isolated AQ robustness iteration from the exact IBKR handoff state for `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1`, with no provider retry, source edit, update feedback, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree promotion. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214607+0800-codex-market-structure-exact-ibkr-aq-robust-seed-iteration-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214607+0800-codex-market-structure-exact-ibkr-aq-robust-seed-iteration-v1/checks/prompt_to_artifact_checklist.csv`. Real work executed: cloned the prior exact IBKR run-root state into a new run root, kept AQ `dependency_ready_data_ready`, compiled three active strategies, and ran AQ `run.py`; all commands exited `0`, with `12` backtests succeeded and `0` failed. Result: all robustness variants failed the AQ gate: revised `ExpansionBreakoutSeed` had `robust_sharpe=-0.4153` / `worst_profit_pct=-7.8700`, `SolBnbBearBounceSeed` had `robust_sharpe=-0.2070` / `worst_profit_pct=-16.5100`, and `BnbRecoveryReclaimSeed` had `robust_sharpe=-2.1403` / `worst_profit_pct=-44.7300`; all had `profit_floor=FAIL`. Decision: `drop`, not promotion; do not carry this AQ iteration into Pre-Bayes/BBN/CatBoost/execution-tree. The prior exact IBKR handoff remains the better evidence packet, and the remaining objective blocker is still same-state realized update rows / entry-model readiness plus TradingViewRemix provider recovery.

- Handoff `2026-05-16 / codex-board-b-external-factor-research-v1`: user-directed external research slice for open-source scripts, strategies, papers, algorithms, and formulas that can improve Board B factor training. Evidence packet: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214640+0800-codex-board-b-external-factor-research-v1/summaries/external_factor_research_packet.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214640+0800-codex-board-b-external-factor-research-v1/checks/artifact_validation.json`. Real work executed: searched public TradingView SMT scripts, Python SMC/FVG/OB/liquidity repositories, 0DTE/GEX tooling, and lead-lag market-structure papers; distilled measurable formulas for SMT time-matched/min-diff pivots, FVG/IFVG lifecycle fill/inversion, OB breaker/rejection validation, liquidity texture/sweep quality, market-structure confirmation, and options gamma-wall context. Decision: `handoff`, not promotion; sources are design inputs only, no external code was copied, and no provider fetch/Auto-Quant/Pre-Bayes/BBN/CatBoost/execution-tree promotion was run from this research packet.

- Handoff `2026-05-16 / codex-board-b-smt-inverse-normalization-parity-v1`: bounded read-only parity audit for the existing `smt_relationship_resolver` historical strict packet; no provider fetch, Auto-Quant, feedback update, source edit, Pre-Bayes/BBN, CatBoost/path-ranker, execution-tree promotion, or source-code mutation was performed. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214640+0800-codex-board-b-smt-inverse-normalization-parity-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214640+0800-codex-board-b-smt-inverse-normalization-parity-v1/checks/artifact_validation.json`. Real work executed: SMT observation packet unit test, historical strict SMT assertion, native `factor-candidate-packs`, `factor-candidate-admission-targets`, and `policy-training-status`; all exited `0` against isolated state `/tmp/ict-engine-board-b-smt-inverse-normalization-parity-v1-state`. Result: source rows contain `165` SMT events, `45/45` inverse-normalized rows preserve raw comparison swing and raw comparison level, `0` rows miss `base_level` or `comparison_level`, `0` rows have non-`confirmation_only` role, `0` strict-pass rows are actionable, and required pairs `NQ/ES`, `NQ/YM`, `EURUSD/GBPUSD`, `EURUSD/DXY`, `XAUUSD/XAGUSD`, `XAUUSD/DXY`, and `BTC/ETH` are present. Decision: `handoff`, not promotion; inverse-normalization parity is verified for current evidence, but SMT remains confirmation-only and pair/regime density is still below promotion floor.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v9`: read-only prompt-to-artifact completion audit after the newest exact-IBKR AQ probe, TradingViewRemix rate-limit refresh, VRP feasibility closeout, and order-block yfinance owner-state readback. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214500+0800-codex-board-b-objective-completion-audit-v9/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214500+0800-codex-board-b-objective-completion-audit-v9/checks/prompt_to_artifact_checklist.csv`. Real work executed: no source edits, provider fetch, Auto-Quant run, update-feedback mutation, Pre-Bayes mutation, CatBoost/path-ranker mutation, or execution-tree promotion was started by this audit; it inspected current board rows, active claims, exact-IBKR AQ evidence, TVR 429 evidence, VRP IV/HV readback, and order-block yfinance shared-analyze readback. Result: completion remains not achieved. Positive evidence is real: exact IBKR AQ now reaches data-ready seeded execution with `12` successful backtests, yfinance order-block replay can materialize durable analyze/workflow state, and VRP has dated real IV/HV observations. Missing evidence remains decisive: TradingViewRemix still returns HTTP `429`, exact IBKR AQ has no robust passing exported candidate, entry models remain `matched_rows=0`, order-block owner structural target rows have empty `regime_profit_branch_path`, ranker maturity/training rows remain zero, execution tree still does not consume a path-ranker score, and VRP remains observation-only below the row floor. Decision: `handoff`, not promotion; `update_goal=false`.

- Handoff `2026-05-16 / codex-order-block-yf-shared-analyze-sample-v1`: refined the rooted order-block yfinance blocker using retained cleaned yfinance candles from the prior non-live parity packet, without source edits or new provider fetches. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213811+0800-codex-order-block-yf-shared-analyze-sample-v1/summaries/terminal_decision_summary.md`; readback: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213811+0800-codex-order-block-yf-shared-analyze-sample-v1/checks/packet_readback.json`. Real work executed: first bounded `analyze` replay was alive after `35s` with no state files, but stack sampling failed because `/opt/homebrew/bin/sample` is a broken Python shim; after intentional termination, the same replay shape completed on rerun before `/usr/bin/sample` was needed, writing `analyze_runs.json`, `workflow_snapshot.json`, `execution_candidate.json`, `execution_tree_trace.json`, and Pre-Bayes/update artifacts under `/tmp/ict-engine-order-block-yf-shared-analyze-sample-v1-rerun`. Downstream commands `workflow-status --refresh`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, and `policy-training-status` all exited `0`. Result: the broad shared-analyze-stall blocker is no longer valid for this replay shape, but promotion is still blocked because structural target rows are generic scenario paths with empty `regime_profit_branch_path`/branch fields, `mature_rows=0`, `rows_with_training_weight=0`, entry models `matched_rows=[0,0]`, workflow remains `execution_observe_only`, and execution tree still has `path_ranker_score_visible_to_execution_tree=false` / `path_ranker_score_used_by_execution_tree=false`. Decision: `handoff`, not promotion; next work should bind the rooted `order_block_variant_classifier_v1` candidate branch into this fresh owner-symbol state or repair owner-symbol structural target export so explicit branch fields survive.

- Handoff `2026-05-16 / codex-market-structure-exact-ibkr-entrymodel-probe-v1`: completed the previously active exact IBKR entry-model probe through real AQ prepare, seeded AQ execution, ict-engine handoff refresh, adoption review, and downstream policy/workflow readbacks. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212200+0800-codex-market-structure-exact-ibkr-entrymodel-probe-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212200+0800-codex-market-structure-exact-ibkr-entrymodel-probe-v1/checks/prompt_to_artifact_checklist.csv`. Real work executed: `auto-quant-prepare`, `auto-quant-status`, three run-root-local AQ seed strategies, `python3 -m py_compile`, AQ `run.py`, `factor-research --backend auto-quant`, `auto-quant-adoption-review`, `policy-training-status`, and `workflow-status`; all exited `0`. Result: the AQ blocker moved from `dependency_ready_data_missing` to `dependency_ready_data_ready`, and AQ measured `12` backtests with `0` failures. Best first-round seed was `ExpansionBreakoutSeed` with full-period profit `47.97%` / `406` trades, but it still failed robustness (`robust_sharpe=-0.0560`, `profit_floor=FAIL`); the other seeds also failed profit/robustness gates. Downstream state still has `cisd_rb_long_v1 matched_rows=0`, `breaker_rb_long_v1 matched_rows=0`, ranker validation `raw_scored_mature=5/30 production_validation=5/30 observation_validation=2/30`, and workflow `execution_gate_status=execution_observe_only` / `review_status=observe` / `ready=false`. Decision: `handoff`, not promotion; next useful work is a stricter AQ iteration with exported stable candidate package or same-state realized update rows for exact `MARKET_STRUCTURE_EVENT_145000` analyze ids.

- Blocked `2026-05-16 / codex-board-b-tvr-rate-limit-refresh-v1`: bounded TradingViewRemix health/rate-limit refresh for the provider-quartet blocker, run without source edits, Cargo, factor replay, Auto-Quant, update feedback, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree promotion. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213916+0800-codex-board-b-tvr-rate-limit-refresh-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213916+0800-codex-board-b-tvr-rate-limit-refresh-v1/checks/artifact_validation.json`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213916+0800-codex-board-b-tvr-rate-limit-refresh-v1/checks/prompt_to_artifact_checklist.md`. Real work executed: `.local-artifacts/cargo-target/debug/ict-engine provider-status --provider tradingview_mcp --agent` exited `0` but still reported `market_data:0/1 ready`, `status=configured_runtime_unhealthy`, `reason=tradingview_mcp_connectivity_probe_failed`; a redacted direct `tools/list` probe against the configured MCP endpoint returned HTTP `429` with `Rate limit exceeded. Retry after 37128s.` Decision: `blocked`, not promotion; the full `ibkr + tradingview_mcp + yfinance + kraken` provider-quartet path remains unavailable until TradingViewRemix rate limit/connectivity clears, so no full-chain Board B completion claim is allowed from current evidence.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v8`: read-only prompt-to-artifact completion audit after the SPY loss-density terminalization and active-claim refresh. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213100+0800-codex-board-b-objective-completion-audit-v8/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213100+0800-codex-board-b-objective-completion-audit-v8/checks/prompt_to_artifact_checklist.csv`. Real work executed: no provider fetch, Auto-Quant run, source edit, update-feedback mutation, Pre-Bayes refresh, CatBoost/path-ranker command, or execution-tree mutation was started by this audit; it inspected current terminal evidence and active claims only. Result: completion remains not achieved. Positive evidence is real: the direct-limit branch path is repeatedly preserved, yfinance/Kraken/IBKR have local provider-backed packets, and the new SPY loss packet adds a real contrasting loss observation. Missing evidence remains decisive: TradingViewRemix is still unhealthy/rate-limited, entry models remain not ready (`cisd`/`breaker` below threshold even after sector/SPY-loss density), CatBoost/path-ranker validation is still not promotion-grade, and workflow/execution-tree remains observe/blocked. Active direct-limit cross-asset work is claimed separately and was not touched. Decision: `handoff`, not promotion; `update_goal=false`.

- Blocked `2026-05-16 / codex-board-b-vrp-ivhv-outcome-feasibility-v1`: closed the post-repair VRP IV/HV outcome feasibility scan for exact branch `TrendExpansion -> VolatilityCompression -> iv_hv_compression_regime -> family_f_vrp_real_ivhv_qqq_observation_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212736+0800-codex-board-b-vrp-ivhv-outcome-feasibility-v1/summaries/terminal_decision_summary.md`; readback: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212736+0800-codex-board-b-vrp-ivhv-outcome-feasibility-v1/checks/vrp_ivhv_outcome_summary.json`. Real work executed: reused existing real IBKR delayed `QQQ` IV/HV daily series plus yfinance `QQQ` 6-month daily closes, joined dated IV/HV to underlying closes, computed 20-trading-day forward realized volatility, and wrote `materials/vrp_ivhv_forward_rv20_outcome_rows.csv`. Result: `overlap_days=124`, `mature_forward_rv20_rows=85`, `selected_branch_rows=24`, `selected_success_rows=13`, `selected_failure_rows=11`, `selected_win_rate=0.541667`, but `trade_usable=false`, `downstream_allowed=false`, and `promotion_allowed=false` because this is observation-only forward vol-spread evidence, not options fill/trade PnL, and selected exact-branch rows remain below the `30` row floor. Decision: `blocked`, not promotion; do not push these rows through Pre-Bayes/BBN/CatBoost/execution-tree as fills without either more dated IV/HV history or a first-class non-trade volatility-spread observation schema.

- Blocked `2026-05-16 / codex-board-b-no-unclaimed-after-provider-gate-v1`: read-only no-takeover collision audit after the provider-quartet current-health gate. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215200+0800-codex-board-b-no-unclaimed-after-provider-gate-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215200+0800-codex-board-b-no-unclaimed-after-provider-gate-v1/checks/collision_audit.json`. Real work executed: no Cargo, provider fetch, Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, execution-tree, update-feedback, or source-edit commands were started; this only refreshed current board state, active claims, and active processes. Result: the full provider-quartet path remains blocked by TradingViewRemix HTTP `429`, while the natural non-TVR continuation lanes are already owned or terminalizing: market-structure yfinance cross-asset basket density, exact IBKR entry-model probe, VRP IV/HV outcome feasibility, Brooks balanced-threshold candidate pack, proxy feedback hard-reject test, options-dealer repeated snapshot, and market-structure sector basket density. Decision: `blocked`, not promotion; next safe condition is an active-owner terminal handoff or TradingViewRemix rate-limit recovery before a true provider-quartet replay.

- Handoff `2026-05-16 / codex-brooks-balanced-threshold-candidate-v1`: materialized the prior `balanced_high_winrate` Brooks threshold support surface into a native fail-closed candidate-pack/readback without taking over active proxy-feedback, market-structure, options, VRP, SMT, order-block, liquidity, or Kraken owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212553+0800-codex-brooks-balanced-threshold-candidate-v1/summaries/terminal_decision_summary.md`. Real work executed: added candidate pack `support/examples/factor_candidate_packs/board-b-brooks-balanced-threshold-v1/brooks_second_entry_reversal_balanced_threshold_v1/`, copied the source threshold scan summary, then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only`, `policy-training-status`, `pre-bayes-status --refresh`, and `workflow-status --refresh --agent` on isolated state `/tmp/ict-engine-brooks-balanced-threshold-candidate-v1-state`; all commands exited `0`. Result: the source surface has `120` unique realized rows, `5` providers, `3` regimes, win rate `0.700000`, expectancy `1.177698R`, and bounded second-entry failed-breakout thresholds; native inventory sees `brooks_second_entry_reversal_balanced_threshold_v1` with `aggregate_trade_count=120`, admission/runtime writes `rows=3`, ranker runtime reaches `enabled_registered_model_ready` with `runtime_matches=3`, and branch fields preserve `TrendExpansion -> BrooksPriceAction -> second_entry_failed_breakout -> brooks_second_entry_reversal_balanced_threshold_v1`. Decision: `handoff`, not promotion; this is summary-level candidate evidence only, not a same-state structural-feedback replay, so Pre-Bayes remains null, both entry models have `matched_rows=0`, validation is `raw_scored_mature=0/30 production_validation=0/30 observation_validation=0/30`, and workflow remains bootstrap/no-workflow-state. Next useful work is replaying the underlying realized rows as exact branch structural feedback before any BBN/CatBoost/execution-tree promotion claim.

- Handoff `2026-05-16 / codex-brooks-threshold-v1-real-trade-replay`: replayed the current Brooks threshold candidate as exact-branch real-trade feedback without taking over active market-structure, VRP, liquidity, or 220646 owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213744+0800-codex-brooks-threshold-v1-real-trade-replay/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213744+0800-codex-brooks-threshold-v1-real-trade-replay/summaries/prompt_to_artifact_checklist.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213744+0800-codex-brooks-threshold-v1-real-trade-replay/checks/artifact_validation.json`. Real work executed: retargeted the existing `120` realized Brooks rows to `TrendExpansion -> BrooksPriceAction -> second_entry_failed_breakout -> brooks_second_entry_reversal_balanced_threshold_v1`, then ran direct-binary `auto-quant-ingest-real-trades`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, `analyze --demo`, `workflow-status --phase execution-candidate`, fallback path-ranker score apply/register, and post-registration analyze/workflow/Pre-Bayes on isolated `BROOKS_THRESHOLD_V1_213744` state; all direct-binary commands exited `0`. Result: real-trade ingest inserted `120/120` feedback records with exact structural branch path preserved, provider rows include `IBKR=20`, `yfinance/YF=11`, `Kraken=2`, `Binance=83`, `Bybit=4`, post-analyze Pre-Bayes reports `pass_hard` with trend posterior `0.8265627340766648` and confidence `0.780340949880645`, path-ranker runtime is `enabled_registered_model_ready` with `runtime_matches=2`, workflow selects the exact threshold v1 branch with `path_ranker_runtime_source=registered_model_artifact` and raw score `0.5677478008126375`, and the post-registration execution tree reports `path_ranker_score_visible_to_execution_tree=true` plus `path_ranker_score_used_by_execution_tree=true`. Decision: `handoff`, not promotion; CatBoost training failed closed because features were constant/ignored and fell back to `weighted_feature_sum_v1`, validation remains `raw_scored_mature=1/30 production_validation=0/30 observation_validation=120/30`, both entry models remain `matched_rows=0`, TradingViewRemix is absent from this retained row set, and execution remains `observe / transition_guardrail / execution_guarded_due_to_high_transition_hazard`.

- Handoff `2026-05-16 / codex-market-structure-yf-spy-loss-density-v1`: resumed and terminalized the unfinished real Yahoo/yfinance `SPY` `1h` loss-density replay for the exact direct-limit branch without source edits or takeover of active SMT/VRP/order-block/liquidity/provider owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211800+0800-codex-market-structure-yf-spy-loss-density-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211800+0800-codex-market-structure-yf-spy-loss-density-v1/checks/artifact_validation.json`. Real work executed: reused the fetched `99` row SPY loss window around the real seed row `2026-03-09 18:30:00+00:00`, then ran direct-binary `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only`, `policy-training-status`, and `workflow-status --phase execution-candidate` on the run-root-local `state_clone`; all commands exited `0`. Result: Pre-Bayes and workflow preserved `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1`, entry-model density now reads `cisd_rb_long_v1 matched_rows=8` and `breaker_rb_long_v1 matched_rows=7`, but both remain `ready=false`; ranker validation is still insufficient (`raw_scored_mature=1/30`, `production_validation=0/30`, `observation_validation=1/30`), runtime is `enabled_no_matching_scores`, and workflow remains `review_status=observe` / `execution_gate_status=execution_blocked`. Decision: `handoff`, not promotion; this adds a real loss observation and branch-path readback, but Board B completion remains blocked by entry-model/readiness thresholds, missing matching runtime scores, and non-actionable execution-tree state.

- Blocked `2026-05-16 / codex-board-b-provider-quartet-current-health-v1`: bounded current provider-quartet health gate for the objective-required `ibkr`, `tradingview_mcp`, `yfinance`, and `kraken` legs before any new full-chain replay. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214500+0800-codex-board-b-provider-quartet-current-health-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214500+0800-codex-board-b-provider-quartet-current-health-v1/checks/artifact_validation.json`. Real work executed: `.local-artifacts/cargo-target/debug/ict-engine provider-status --provider {ibkr,tradingview_mcp,yfinance,kraken_cli} --agent`, `provider-status --compact`, provider-venv parity probes for `ibkr`, `kraken_public`, and `tradingview_mcp`, plus a redacted direct `tools/list` HTTP probe against the configured TradingViewRemix MCP endpoint; all commands exited `0`. Result: default runtime has `yfinance` and `kraken_cli` ready, while `ibkr` is unhealthy in the default shell; injecting `/Users/thrill3r/.venvs/ict-engine-provider-py313/bin` makes `ibkr` and `kraken_public` ready, but `tradingview_mcp` still fails. Direct MCP evidence shows local config exists and has an API key, but the endpoint returns HTTP `429` / `Rate limit exceeded. Retry after 37937s.` Decision: `blocked`, not promotion; full provider-quartet closure is still not achieved, and new full-chain claims must either wait for TradingViewRemix rate-limit recovery or explicitly label coverage as `yf+kraken+ibkr only`.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v7`: read-only prompt-to-artifact completion audit after the exact-feedback patched workflow/execution-tree bridge and the latest active-claim refresh. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211659+0800-codex-board-b-objective-completion-audit-v7/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211659+0800-codex-board-b-objective-completion-audit-v7/checks/prompt_to_artifact_checklist.csv`. Real work executed: no Cargo, provider fetch, Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, execution-tree, update-feedback, or source-edit commands were started by this audit; it inspected current terminal evidence only. Result: completion remains not achieved, but the live blocker changed. The `MARKET_STRUCTURE_EVENT_145000` exact branch now has workflow selection and execution-tree score consumption (`path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, `runtime_source=candidate_set`, score `0.8002395367207108`). Missing evidence remains decisive: closed-loop branch admission is still `fail_closed`, execution tree gate is still `observe`, validation remains `raw_scored_mature=5/30 production_validation=5/30 observation_validation=2/30`, both entry models remain `matched_rows=0`, and provider parity is incomplete because the named `yf/kraken/ibkr` density evidence is on the sibling direct-limit branch while TradingViewRemix is still unhealthy in the current health gate. Decision: `handoff`, not promotion; next work should target exact-branch entry-model/validation density or TVR provider recovery, not repeat CatBoost/workflow score-consumption repair. `update_goal=false`.

- Handoff `2026-05-16 / codex-board-b-vrp-real-ivhv-branch-metadata-repair-v1`: repaired the native factor-candidate admission branch metadata path on a run-root-local copy of the real yfinance `QQQ` IV/HV VRP candidate pack. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T205800+0800-codex-board-b-vrp-real-ivhv-branch-metadata-repair-v1/summaries/terminal_decision_summary.md`. Real work executed: added a focused TDD regression in `src/main.rs` proving `branch_path_contract` must override market-derived branch fields, observed RED (`sub_regime=qqq` instead of `VolatilityCompression`), implemented the minimal resolver, observed GREEN, then reran native `factor-candidate-admission-targets`, `enable-structural-path-ranking-runtime`, `policy-training-status`, `pre-bayes-status --refresh`, and `workflow-status --refresh --agent` on isolated `state_contract_repair`. Result: admission now emits both `AGGREGATE` and `QQQ` rows with exact branch `TrendExpansion -> VolatilityCompression -> iv_hv_compression_regime -> family_f_vrp_real_ivhv_qqq_observation_v1`, and runtime reaches `enabled_registered_model_ready` with `runtime_active_match_count=1`. Decision: `handoff`, not promotion; the pack remains observation-only (`mature_rows=0`, `rows_with_training_weight=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`), Pre-Bayes is null, workflow remains `no_workflow_state`, and entry models remain `matched_rows=0`. Next useful work is real mature structural feedback or trade/outcome rows for this exact VRP branch before rerunning Pre-Bayes, BBN, CatBoost/path-ranker, and execution-tree promotion.

- Handoff `2026-05-16 / codex-board-b-options-dealer-etf-breadth-v1`: expanded the new `options_dealer_context` yfinance option-chain evidence from single-symbol `QQQ` into a same-shape ETF breadth packet for `SPY`, `DIA`, and `IWM`, without taking over market-structure, SMT, VRP, FVG/IFVG, liquidity-texture, order-block, or Kraken owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211000+0800-codex-board-b-options-dealer-etf-breadth-v1/summaries/terminal_decision_summary.md`. Real work executed: reused `support/scripts/research/options_dealer_context_observation_packet.py` to fetch yfinance option chains for two near expirations per ETF, produced `1052` source rows / `982` normalized rows, and built observation fields for `SPY` (`gamma_wall=730.0`, `put_wall=730.0`, `call_wall=755.0`, `dealer_gamma_regime=positive_gamma_pin_risk`, `skew_stress=0.069237`), `DIA` (`gamma_wall=525.0`, `put_wall=450.0`, `call_wall=525.0`, `dealer_gamma_regime=positive_gamma_pin_risk`, `skew_stress=0.106038`), and `IWM` (`gamma_wall=295.0`, `put_wall=268.0`, `call_wall=295.0`, `dealer_gamma_regime=put_dominant_acceleration_risk`, `skew_stress=0.117447`). Native candidate-pack readback under `support/examples/factor_candidate_packs/board-b-options-dealer-context-etf-breadth-v1/` sees `candidate_pack_count=1`, admission `rows=5`, `mature_rows=0`, `rows_with_training_weight=0`, policy `runtime_selection=disabled`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and entry-model `matched_rows=0`. Decision: `handoff`, not promotion; breadth improved, but these are still same-day snapshots with no realized outcomes, repeated windows, or IBKR/TradingViewRemix parity, so no `update --feedback-file`, Pre-Bayes/BBN, CatBoost/path-ranker, or execution-tree carry is allowed yet.

- Handoff `2026-05-16 / codex-board-b-sweep-quality-proxy-feedback-guard-v1`: ran an isolated negative update guard on the existing sweep-quality proxy labels without taking over active VRP, market-structure, liquidity, order-block, FVG, SMT, or 220646 lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213000+0800-codex-board-b-sweep-quality-proxy-feedback-guard-v1/summaries/terminal_decision_summary.md`; parsed readback: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213000+0800-codex-board-b-sweep-quality-proxy-feedback-guard-v1/checks/proxy_feedback_guard_readback.json`. Real work executed: built a feedback fixture from one `sweep_quality_proxy_outcome_observation_v1` row with `label_kind=proxy_continuation_vs_invalidation`, `trade_usable=false`, and `exit_reason=proxy_label_not_real_trade`, then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `policy-training-status`, and `workflow-status --refresh` on isolated state `/tmp/ict-engine-board-b-sweep-quality-proxy-feedback-guard-v1-state`; all commands exited `0`. Result: native `update` accepts the proxy feedback into structural feedback/readback surfaces (`update_runs=1`, Pre-Bayes branch path preserved, `latest_gate_status=proxy_feedback_should_fail_closed`) but assigns `learning_semantics:class=no_credit_not_followed success_credit=0.000 observation_weight=0.000`; policy stays unready with observation validation `1/30`, both entry models `matched_rows=0`, and workflow remains `execution_observe_only`. Decision: `handoff`, not promotion; proxy sweep rows still must not be learned as fills, and next useful work is either a real same-state analyze/update bridge with actual fill/outcome fields or a source guard that rejects proxy feedback before `update` persists it.

- Handoff `2026-05-16 / codex-board-b-proxy-feedback-source-guard-v1`: implemented the source guard requested by the prior proxy-feedback negative slice without taking over active provider/factor lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212819+0800-codex-board-b-proxy-feedback-source-guard-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212819+0800-codex-board-b-proxy-feedback-source-guard-v1/checks/artifact_validation.json`. Real work executed: added a failing regression in `src/update_command.rs` proving `update --feedback-file` accepted a `structural-feedback-v1` payload with `label_kind=proxy_continuation_vs_invalidation`, `trade_usable=false`, and `exit_reason=proxy_label_not_real_trade`; then added a raw JSON guard immediately after `std::fs::read_to_string(path)?` so explicit proxy/non-trade structural feedback is rejected before `FeedbackRecord`, `StructuralFeedbackSubmission`, or `PendingUpdateArtifact` deserialization/persistence. Verification: RED observed via `cargo test update_feedback_file_rejects_proxy_non_trade_structural_feedback --quiet` (`feedback_records_applied=1`, `structural_learning_credit_class=no_credit_not_followed`), GREEN passed after the guard, `cargo test update_command::tests --quiet` passed `3` tests, `cargo fmt --check` passed, direct binary rebuild with `CARGO_TARGET_DIR=.local-artifacts/cargo-target cargo build --bin ict-engine --quiet` passed, and rebuilt `.local-artifacts/cargo-target/debug/ict-engine update --feedback-file` rejected the proxy fixture with exit `1` before stdout. Decision: `handoff`, not promotion; this closes the proxy-persistence safety blocker only, while the full Board B rooted profit-factor chain still needs real trade/outcome rows through provider/Auto-Quant -> Pre-Bayes/filter -> BBN/update -> CatBoost/path-ranker -> execution tree.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v6`: read-only prompt-to-artifact completion audit after fresh direct-limit yfinance/Kraken/IBKR density rows and the TVR health gate. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211500+0800-codex-board-b-objective-completion-audit-v6/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211500+0800-codex-board-b-objective-completion-audit-v6/checks/prompt_to_artifact_checklist.csv`. Real work executed: no Cargo, provider fetch, Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, execution-tree, update-feedback, or source-edit commands were started by this audit; it inspected current terminal evidence only. Result: completion remains not achieved. Positive evidence is real: the direct-limit branch now has fresh yfinance, Kraken, and IBKR same-state entry-density evidence with branch identity preserved through update/Pre-Bayes/workflow surfaces. Missing evidence remains decisive: TradingViewRemix is provider-blocked (`configured_runtime_unhealthy` / `tradingview_mcp_connectivity_probe_failed`), IBKR remains weak (`matched_rows=1`, `runtime_matches=0`, path-ranker visible/used=false), entry-model readiness is still below thresholds, and workflow/execution-tree remains observe/blocked. Decision: `handoff`, not promotion; `update_goal=false`.

- Blocked `2026-05-16 / codex-market-structure-tvr-health-gate-v1`: bounded TradingViewRemix provider-health gate for the exact `market_structure_event_classifier_direct_limit_v1` direct-limit entry bridge, run without taking over the active IBKR owner. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211200+0800-codex-market-structure-tvr-health-gate-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211200+0800-codex-market-structure-tvr-health-gate-v1/checks/artifact_validation.json`. Real work executed: direct-binary `provider-status --provider tradingview_mcp --agent`, exit `0`. Result: command succeeded but provider gate is unhealthy (`summary_line=market_data:0/1 ready`, `ready=false`, `status=configured_runtime_unhealthy`, `reason=tradingview_mcp_connectivity_probe_failed`). Decision: `blocked`, not promotion; no TVR `analyze`, `update`, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree command was run because there is no healthy TradingViewRemix candle source for this exact branch in this slice. Current direct-limit bridge has fresh yfinance, Kraken, and IBKR density evidence, while TVR remains provider-blocked here.

- Handoff `2026-05-16 / codex-market-structure-ibkr-qqq-entry-density-v1`: repeated the same-state market-structure entry-density bridge on a distinct IBKR provider axis after yfinance SPY/DIA and Kraken XBT, without source edits or takeover of active exact-feedback/SMT/VRP/options lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T210300+0800-codex-market-structure-ibkr-qqq-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched IBKR `QQQ` `1 hour` TRADES through the provider Python environment (`441` data rows), normalized the material, ran direct-binary `analyze`, branch-aware `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: IBKR is now directly covered for this bridge, the same-state analyze id `analyze:MARKET_STRUCTURE_DIRECT_LIMIT:20260516T130549.036Z` was consumed, and the rooted branch `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1` survives through update/Pre-Bayes/workflow. Decision: `handoff`, not promotion; entry-model density is only `matched_rows=1` for both `cisd_rb_long_v1` and `breaker_rb_long_v1`, labels still lack win/loss diversity, path-ranker runtime is `enabled_no_matching_scores` with `runtime_matches=0`, validation remains `raw_scored_mature=1/30 production_validation=0/30 observation_validation=1/30`, workflow is still `execution_observe_only`, and execution tree remains blocked (`branch=block_crowded`, path-ranker visible/used=false). Provider-quartet coverage for this exact bridge is now `yf+kraken+ibkr`; TradingViewRemix remains the uncovered/unhealthy leg.

- Handoff `2026-05-16 / codex-board-b-options-dealer-yf-chain-packet-v1`: converted `options_dealer_context` from the earlier harness-level blocker into a direct yfinance option-chain observation packet without taking over active SMT, liquidity-texture, FVG/IFVG, order-block, Kraken, or market-structure owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T205658+0800-codex-board-b-options-dealer-yf-chain-packet-v1/summaries/terminal_decision_summary.md`. Real work executed: added `support/scripts/research/options_dealer_context_observation_packet.py` plus unit coverage, fetched current `QQQ` yfinance option chains for two near expirations, derived approximate gamma exposure from `522` source rows / `490` normalized rows, and emitted first-class fields: `gamma_wall=730.0`, `put_wall=730.0`, `call_wall=715.0`, `dealer_gamma_regime=positive_gamma_pin_risk`, `zero_dte_pressure=none`, `skew_stress=0.273875`, and `expected_pin_or_acceleration=downside_acceleration_below_put_wall`. Native candidate-pack readback under `support/examples/factor_candidate_packs/board-b-options-dealer-context-yf-v1/` sees `candidate_pack_count=1`, admission `rows=4`, `mature_rows=0`, `rows_with_training_weight=0`, policy `runtime_selection=disabled`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and entry-model `matched_rows=0`. Decision: `handoff`, not promotion; this is a useful structured runtime-evidence surface, but it is single-symbol/single-snapshot observation only and must not be learned as feedback or carried into Pre-Bayes/BBN/CatBoost/execution-tree until repeated option-chain snapshots have realized outcomes.

- Handoff `2026-05-16 / codex-board-b-smt-outcome-label-bridge-v1`: cleared the narrow SMT schema blocker from the prior strict-provider refresh without taking over active VRP, market-structure, liquidity, order-block, or 220646 claims. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T204916+0800-codex-board-b-smt-outcome-label-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: labeled retained provider-backed SMT observation rows with bounded 4-bar forward outcomes from the preserved pair OHLCV files, reran the strict SMT trainer/assertion, materialized a run-root-local observation candidate pack, then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, and `policy-training-status` on isolated state `/tmp/ict-engine-board-b-smt-outcome-label-bridge-v1-state`. Result: script/native exits are `0`, source rows `15`, bounded forward labels `1`, strict training rows `0`, native candidate packs `1`, admission rows `8`, branch fields present on all native target rows, but `mature_rows=0`, `rows_with_training_weight=0`, ranker runtime `disabled`, validation remains `raw_scored_mature=0/30 production_validation=0/30 observation_validation=0/30`, and both entry models still have `matched_rows=0`. Decision: `handoff`, not promotion; the previous `forward_return/outcome_hit` schema absence is no longer the blocker, but this packet must not enter Pre-Bayes/BBN/CatBoost/execution-tree promotion until historical provider-backed SMT outcome rows create strict same-event density across required pair lanes and regimes.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v5`: read-only prompt-to-artifact completion audit after refreshing the current board, requested path, active claims, and v4 evidence. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T210058+0800-codex-board-b-objective-completion-audit-v5/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T210058+0800-codex-board-b-objective-completion-audit-v5/checks/prompt_to_artifact_checklist.csv`. Real work executed: no Cargo, provider fetch, Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, execution-tree, update-feedback, or source-edit commands were started; this is an evidence audit only. Result: completion is still not achieved. Positive evidence now includes yfinance DIA entry-density improvement (`matched_rows=2` on both entry models), exact IBKR IV/HV feasibility clearing the data-file blocker but producing zero trades, and rooted CatBoost/path-ranker training evidence on market-structure/Kraken paths. Missing evidence remains decisive: no single rooted branch has complete provider/AQ -> filter -> BBN/update -> CatBoost -> workflow/execution-candidate -> execution-tree -> entry-model linkage with comparable `ibkr`/`tradingviewremix`/`yf`/`kraken` coverage. Active owners currently occupy Kraken entry-density, VRP metadata repair, SMT outcome-label bridge, options-dealer yfinance chain packet, and market-structure exact-feedback. Decision: `handoff`, not promotion; `update_goal=false`.

- Blocked `2026-05-16 / codex-board-b-no-unclaimed-lane-collision-audit-v1`: read-only no-takeover collision audit after refreshing the current board, dirty worktree, active process list, and `/tmp/ict-engine-agent-claims/board-b` before selecting new work. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T205736+0800-codex-board-b-no-unclaimed-lane-collision-audit-v1/summaries/terminal_decision_summary.md`. Real work executed: no Cargo, provider fetch, Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, execution-tree, update-feedback, or source-edit commands were started; the audit only read current claims and board status. Result: the natural next lanes are already active or recently terminalized: options-dealer yfinance chain packet, VRP branch-metadata repair, SMT outcome-label bridge, market-structure exact-feedback bridge, sweep-quality proxy admission, FVG/IFVG density, order-block, liquidity-texture, Kraken exact-branch diagnostics, and provider quartet parity. Decision: `blocked`, not promotion; starting a new runtime slice now would collide with an active owner or duplicate a recent readback. Next safe action is to wait for an active owner to terminalize, then choose a genuinely different axis by factor, root regime, symbol/provider/window, artifact root, or gate.

- Handoff `2026-05-16 / codex-market-structure-kraken-xbt-entry-density-v1`: repeated the exact `market_structure_event_classifier_direct_limit_v1` same-state entry-model bridge on a non-yfinance provider axis using Kraken spot `XBTUSD 1h`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T205300+0800-codex-market-structure-kraken-xbt-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched Kraken spot `XBTUSD 1h` with the provider venv (`721` rows), normalized the CSV, ran `analyze`, attached branch-aware structural feedback to `analyze:MARKET_STRUCTURE_DIRECT_LIMIT:20260516T130053.279Z`, then ran `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: Kraken analyze emitted both `breaker_rb_long_v1` and `cisd_rb_long_v1` packets; update/Pre-Bayes preserved `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1`; both entry models improved to `matched_rows=3`; ranker validation improved to `raw_scored_mature=15/30`, `production_validation=15/30`, `observation_validation=5/30`; workflow still selects the exact rooted branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.32885873518963377`, and `execution_readiness=0.57`; execution-tree trace references the exact path. Decision: `handoff`, not promotion; entry rows remain below thresholds, labels are still all-win/no win-loss diversity, inherited linkage warnings remain, workflow is still `execution_observe_only`, and IBKR/TradingViewRemix have not yet been repeated for this entry-model bridge. Next useful work is a comparable IBKR bridge if the provider venv can fetch candles, or a bounded TradingViewRemix health/readback if MCP rate limits permit.

- Handoff `2026-05-16 / codex-market-structure-yf-dia-entry-density-v1`: added a fresh same-state yfinance `DIA 1h` entry-density bridge for `market_structure_event_classifier_direct_limit_v1` without taking over the active exact-feedback claim. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T204103+0800-codex-market-structure-yf-dia-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: cloned the completed SPY entry-packet state, fetched real DIA 1h candles through `market-data-harness`, normalized `369` rows, ran `analyze`, attached branch-aware structural feedback to `analyze:MARKET_STRUCTURE_DIRECT_LIMIT:20260516T124943.768Z`, then ran `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: DIA analyze emitted both `breaker_rb_long_v1` and `cisd_rb_long_v1` packets; update/Pre-Bayes preserved `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1`; both entry models improved to `matched_rows=2`; ranker validation improved to `raw_scored_mature=10/30`, `production_validation=10/30`, `observation_validation=4/30`; workflow still selects the exact rooted branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.3264204833628647`, and `execution_readiness=0.57`. Decision: `handoff`, not promotion; entry rows remain below thresholds, outcome labels are still all-win/no win-loss diversity, inherited linkage warnings remain, workflow is still `execution_observe_only`, and IBKR/Kraken/TradingViewRemix have not yet been repeated for this entry-model bridge. Next useful work is a comparable non-yfinance provider bridge or a real contrasting outcome row for the same exact branch.

- Handoff `2026-05-16 / codex-board-b-vrp-real-ivhv-native-carry-v1`: carried the existing real yfinance QQQ IV/HV VRP candidate pack through native inventory, admission, runtime enablement, policy, Pre-Bayes, and workflow readback without taking over active market-structure/Kraken/order-block/SMT/FVG/liquidity-texture lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T205000+0800-codex-board-b-vrp-real-ivhv-native-carry-v1/summaries/terminal_decision_summary.md`. Real work executed: used `support/examples/factor_candidate_packs/board-b-family-f-vrp-real-ivhv-yf-v1`, ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, `enable-structural-path-ranking-runtime`, `policy-training-status`, `pre-bayes-status --refresh`, and `workflow-status --refresh --agent` on isolated state `/tmp/ict-engine-vrp-real-ivhv-native-carry-v1`; all commands exited `0`. Result: inventory sees `candidate_pack_count=1`, admission writes `rows=2`, runtime reaches `enabled_registered_model_ready` with `runtime_matches=2`, and target rows preserve first-class branch fields. Decision: `handoff`, not promotion; the pack remains observation-only (`mature_rows=0`, `training_weight_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`), Pre-Bayes remains null, workflow is `no_workflow_state`, entry models are empty, and native target rows emit `TrendExpansion -> aggregate/qqq -> family_f_vrp:real_iv_hv_vrp_observation:1d -> family_f_vrp_real_ivhv_qqq_observation_v1@...` rather than the intended exact branch `TrendExpansion -> VolatilityCompression -> iv_hv_compression_regime -> vrp_compression_long_v1`. Next useful work is branch-metadata repair before downstream promotion claims.

- Handoff `2026-05-16 / codex-board-b-family-f-vrp-real-ivhv-yf-pack-v1`: converted the new real `QQQ` / `yfinance` IV/HV material into a distinct observation-only native candidate-pack/admission surface without taking over the active IBKR IV/HV feasibility claim or any Kraken, market-structure, order-block, SMT, FVG, or liquidity-texture lane. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T204139+0800-codex-board-b-family-f-vrp-real-ivhv-yf-pack-v1/summaries/terminal_decision_summary.md`. Real work executed: added candidate pack `support/examples/factor_candidate_packs/board-b-family-f-vrp-real-ivhv-yf-v1/family_f_vrp_real_ivhv_qqq_observation_v1/`, sourced from the real IV/HV probe, then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, `policy-training-status`, `pre-bayes-status --refresh`, and `workflow-status --refresh` on isolated state `/tmp/ict-engine-family-f-vrp-real-ivhv-yf-pack-v1`. Result: native admission writes `rows=2` for `FAMILY_F_VRP_REAL_IVHV_YF`; both target rows preserve rooted branch fields under `TrendExpansion -> ... -> family_f_vrp_real_ivhv_qqq_observation_v1`, including a QQQ row `TrendExpansion -> qqq -> family_f_vrp:real_iv_hv_vrp_observation:1d -> family_f_vrp_real_ivhv_qqq_observation_v1@QQQ`. Decision: `handoff`, not promotion; it remains fail-closed with `mature_rows=0`, `rows_with_training_weight=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, `runtime_selection=disabled`, both entry models `matched_rows=0`, no Pre-Bayes gate, and no workflow execution candidate. Next useful work is a real Auto-Quant replay using dated `qqq.iv.1d` and `qqq.hv.1d` inputs, not promotion of this observation-only pack.

- Blocked `2026-05-16 / codex-board-b-vrp-ibkr-ivhv-feasibility-v1`: exact `family_f_vrp_compression_15m_v1` VRP data blocker was retested with real IBKR IV/HV series, then carried through Auto-Quant and ict-engine fail-closed readback without taking over active dense-kline/Kraken, SMT, FVG/IFVG, order-block, liquidity-texture, market-structure, or sweep-quality owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T203200+0800-codex-board-b-vrp-ibkr-ivhv-feasibility-v1/summaries/terminal_decision_summary.md`. Real work executed: `uv run ... fetch_external.py ibkr-historical` fetched `QQQ` `OPTION_IMPLIED_VOLATILITY` and `HISTORICAL_VOLATILITY` daily rows through IBKR delayed mode (`3M=63/63` rows, `1Y=251/251` rows), copied the fresh exact IV/HV files into the retained VRP Auto-Quant workspace, reran `uv run python run_tomac.py`, then created a zero-trade `strategy_library` and ran direct-binary `auto-quant-results-import`, `auto-quant-prior-init --dry-run`, `auto-quant-prior-init --force`, `pre-bayes-status --refresh`, `policy-training-status`, and `workflow-status --refresh` on isolated state `/tmp/ict-engine-vrp-ibkr-ivhv-feasibility-v1`. Result: the previous `FileNotFoundError` blocker is gone and the exact AQ strategy executes, but it produces `trade_count=0`, `sharpe=0`, `win_rate_pct=0`, and `profit_factor=0`; ict-engine import accepts the manifest with `n_ok=1`, prior-init skips the strategy with `trade_count=0` and `evidence_value_gate_passed=false`, Pre-Bayes remains null, policy entry models stay `matched_rows=0`, and workflow remains `insufficient_state / no workflow phase snapshots available`. Decision: `blocked`, not promotion; this exact VRP branch should not proceed to CatBoost/execution-tree promotion on current evidence, and any OHLCV/VIX proxy compression attempt needs a distinct factor id and branch path rather than relabeling `iv_hv_compression_regime`.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v4`: fresh non-takeover completion audit after the market-structure branch-aware workflow bridge, yfinance SPY entry-packet bridge, and exact-feedback CatBoost bridge. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T204000+0800-codex-board-b-objective-completion-audit-v4/summaries/terminal_decision_summary.md`. Real evidence inspected: current board, active `/tmp` claims, `codex-market-structure-yf-spy-entry-packet-probe-v1`, `codex-market-structure-exact-feedback-bridge-v1`, and `codex-market-structure-branch-aware-workflow-bridge-v1`. Positive evidence sharpened: yfinance SPY 1h now emits real entry-model packets with `cisd_rb_long_v1 matched_rows=1` and `breaker_rb_long_v1 matched_rows=1`; the exact-feedback bridge can mature exact branch rows and train CatBoost after adding positive/negative comparison evidence; the exact branch CatBoost score `0.8002395367207108` beats generic `0.32715998375361466`; Pre-Bayes/update target rows preserve rooted branch fields. Decision: `handoff`, not promotion; completion remains blocked because workflow still selects generic `trend_follow_through`, execution tree still does not consume the exact branch score in the exact-feedback state, entry-model density is below readiness and lacks label diversity, and IBKR/Kraken/TradingViewRemix do not yet have comparable entry-packet density for this branch. `update_goal=false`.

- Handoff `2026-05-16 / codex-board-b-family-f-vrp-real-ivhv-gate1-v1`: cleared the narrow real-data blocker for the exact `family_f_vrp_compression_15m_v1` branch without taking over active Kraken, market-structure, order-block, SMT, FVG, or liquidity-texture lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T203228+0800-codex-board-b-family-f-vrp-real-ivhv-gate1-v1/summaries/terminal_decision_summary.md`. Real work executed: used a run-root-local yfinance probe to fetch QQQ 6-month daily history plus four near expirations of QQQ option-chain calls/puts, then computed HV20/HV60 and near-ATM IV/OI/volume coverage for rooted branch `TrendExpansion -> VolatilityCompression -> iv_hv_compression_regime -> vrp_compression_long_v1`. Result: `gate1_real_iv_hv_fields_acquired=true`, `history_rows=124`, `near_atm_sample_rows=80`, `near_atm_iv_rows=80`, `near_atm_nonzero_open_interest_rows=80`, `near_atm_nonzero_volume_rows=80`, all required option columns present (`impliedVolatility`, `lastPrice`, `openInterest`, `strike`, `volume`), `historical_vol_20d=0.17156024793787022`, `historical_vol_60d=0.19526389920978163`, `near_atm_implied_vol_mean=0.20190770024108887`, and IV-HV spread is positive versus both windows. Decision: `handoff`, not promotion; no Auto-Quant, Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution-tree promotion was run from this new material, and the next useful work is to convert the real IV/HV material into dated VRP feature/trade material for the same rooted branch.

- Handoff `2026-05-17 / codex-board-b-sweep-quality-proxy-admission-v1`: converted the retained `sweep_quality_provider_observation_v1` proxy outcome rows into a dedicated observation-only native candidate-pack/admission surface without treating proxy labels as realized trades. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T193500+0800-codex-board-b-sweep-quality-proxy-admission-v1/summaries/terminal_decision_summary.md`. Real work executed: built `support/examples/factor_candidate_packs/board-b-sweep-quality-proxy-admission-v1/sweep_quality_proxy_outcome_observation_v1/` from the prior `2088` proxy rows, preserving the explicit branch contract `Transition -> LiquiditySweep -> sweep_quality_confirmation_only -> sweep_quality_proxy_outcome_observation_v1`, then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, and `policy-training-status` on isolated state `/tmp/ict-engine-board-b-sweep-quality-proxy-admission-v1-state`. Native readback sees `candidate_pack_count=1`, `aggregate_trade_count=0`, `transfer_status=proxy_observation_only_candidate`, admission `rows=11`, all `candidate_pack_admission_pending`, `mature_rows=0`, `rows_with_training_weight=0`, ranker runtime `runtime_selection=disabled`, validation `raw_scored_mature=0/30 production_validation=0/30 observation_validation=0/30`, and both entry models `matched_rows=0`. The packet assertion passed with `proxy_rows=2088`, `admission_rows=11`, `runtime_selection=disabled`, and `entry_model_matched_rows=[0,0]`. Decision: `handoff`, not promotion; no Auto-Quant, Pre-Bayes/filter, BBN, CatBoost/runtime enablement, execution-tree promotion, or `update --feedback-file` ingestion was run because the labels remain proxy-only and must not be learned as fills.

- Drop `2026-05-17 / codex-board-b-sweep-quality-provider-analyze-carry-v1`: tested the next sweep-quality gate with real retained provider candles instead of proxy labels, then stopped at the first real profitability blocker. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T200500+0800-codex-board-b-sweep-quality-provider-analyze-carry-v1/summaries/terminal_decision_summary.md`; compact AQ summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T200500+0800-codex-board-b-sweep-quality-provider-analyze-carry-v1/checks/32_auto_quant_summary_compact.json`. Real work executed: normalized retained `BTC_yahoo_1h` provider candles (`1429` rows), ran direct-binary `analyze`, `workflow-status --refresh`, `pre-bayes-status --refresh`, `policy-training-status`, `factor-research`, `auto-quant-prepare`, seeded three isolated AQ strategies under `/tmp/.../.deps/auto-quant/user_data/strategies`, and ran the real AQ backend (`7` backtests succeeded, `0` failed). Result: analyze/Pre-Bayes reached `pass_neutralized` with transition posterior, but live workflow carried the generic `transition_confirmation` path rather than the intended `Transition -> LiquiditySweep -> sweep_quality_confirmation_only -> sweep_quality_provider_observation:v1`; entry models stayed `matched_rows=0` and path-ranker target rows stayed `0`. AQ profitability was negative: `CryptoSweepExhaustionReversal1h robust_sharpe=-1.6248 worst_profit_pct=-47.87`, `CryptoSweepReclaim1h robust_sharpe=-1.1884 worst_profit_pct=-35.86`, and `CryptoSweepTrendContinuation1h robust_sharpe=-0.0059 worst_profit_pct=-0.20`; all three had `profit_floor=FAIL`. Decision: `drop`, not promotion; no BBN/CatBoost/path-ranker/execution-tree promotion is allowed from this slice because Gate 1 failed before a profitable seed existed, and future sweep-quality work needs a first-class provider-backed factor/entry schema that emits exact branch fields.

- Handoff `2026-05-16 / codex-market-structure-branch-aware-workflow-bridge-v1`: tested the next non-takeover bridge for `market_structure_event_classifier_atr_cisd_direct_limit_v1` on a copied state from the current market-structure replay. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T201633+0800-codex-market-structure-branch-aware-workflow-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: registered a branch-aware `weighted_feature_sum_v1` direct-model artifact for `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1`, enabled structural path-ranking runtime in `candidate_set_only` mode, reran `policy-training-status`, `workflow-status --phase execution-candidate`, `analyze`, post-analyze workflow/policy readbacks, and a post-analyze re-enable readback. Verified improvement: before fresh `analyze`, workflow selects the exact rooted branch as rank 1 with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.705535918331943`, `candidate_status=ready`, `ready=true`, and `review_status=promote_latest`; after fresh `analyze`, `execution_tree_trace.json` reports `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, `path_ranker_model_family=weighted_feature_sum_v1`, and `path_ranker_runtime_source=registered_model_artifact`. Decision: `handoff`, not promotion; fresh `analyze` still rebuilds the live candidate set around the generic `trend_follow_through` scenario path, post-analyze runtime applies the registered model to only one live candidate, persisted `execution_candidate.json` remains `candidate_status=no_trade`, the execution tree stays `execution_guarded_due_to_high_transition_hazard`, and both entry models still have `matched_rows=0`. Next useful unclaimed work is analyze-time structural recommended-path candidate assembly repair for first-class rooted profit branches, plus comparable update/analyze rows for nonzero entry-model matches.

- Blocked `2026-05-16 / codex-board-b-options-dealer-context-gate1-v1`: tested the unclaimed broader-backlog `options_dealer_context` / `vrp_pressure_qqq_v1` Gate 1 provider surface without taking over active SMT, liquidity-texture, FVG, order-block, or Kraken lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T200524+0800-codex-board-b-options-dealer-context-gate1-v1/summaries/terminal_decision_summary.md`. Real work executed: created a `/tmp` claim, captured yfinance provider status, then ran direct-binary `market-data-harness --action plan` and `--action fetch` for QQQ `options_greeks`, `options_oi`, and `options_iv` roles on the rooted probe path `RangeConsolidation -> OptionsDealerContext -> qqq_options_summary_surface -> vrp_pressure_qqq_v1`. Plan/fetch both exited `0`, but all three successful rows came from `yfinance:volatility_proxy:^VIX`: `near_atm_implied_volatility_rows=3`, while `put_call_oi_ratio_rows=0`, `put_call_volume_ratio_rows=0`, `near_atm_gamma_rows=0`, `gamma_skew_rows=0`, and nonzero OI rows are `0`. Provider summary also shows IBKR ready in the provider venv but with no options-summary requirements supported in this harness path, TradingViewRemix connectivity-unhealthy, and Hubble unconfigured. Decision: `blocked`, not promotion; this is not a real options-dealer factor seed and must not enter Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree promotion until a real option-chain provider supplies OI/volume/IV/gamma fields.

- Handoff `2026-05-16 / codex-board-b-order-block-variant-packet-v1`: converted the requested `order_block_variant_classifier` into an isolated fail-closed runtime-evidence packet and candidate-pack surface without taking over active SMT, FVG/IFVG, liquidity-texture, market-structure, or Kraken exact-branch owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T200350+0800-codex-board-b-order-block-variant-packet-v1/summaries/terminal_decision_summary.md`. Real work executed: added `support/scripts/research/order_block_variant_observation_packet.py`, its unittest, and candidate pack `support/examples/factor_candidate_packs/board-b-order-block-variant-v1/order_block_variant_observation_v1/`; then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, and `policy-training-status` on isolated state. Result: inventory sees `candidate_pack_count=1`, admission writes `rows=6`, packet tests pass `14/14`, and the candidate exposes exact `high/low/midpoint`, `variant`, `validation_state`, `mitigation_count`, `breaker_confirmed`, and `rejection_confirmed` fields for Structure/Technicals/Regime/execution-tree/feedback mapping. Decision: `handoff`, not promotion; it is intentionally `aggregate_label=invalid` and `transfer_status=fail_closed_required_markets_not_yet_provider_backed`, with `mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, `runtime_selection=disabled`, and entry-model `matched_rows=0`. Next useful work is provider-backed retained-window OB variant rows across `NQ/ES/YM/RTY`, `NAS100/US500/US30`, `DXY`, `XAUUSD`, and `BTC/ETH`, not promoting this packet.

- Handoff `2026-05-16 / codex-board-b-family-f-vrp-pack-readback-v1`: isolated native readback for `family_f_vrp_compression_15m_v1` without taking over active SMT, FVG/IFVG, order-block, liquidity-texture, Kraken exact-branch, market-structure, or Family F trend-pullback owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T201939+0800-codex-board-b-family-f-vrp-pack-readback-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the existing repo-local VRP candidate pack into a run-root-local candidate-pack root, then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only`, `policy-training-status`, `pre-bayes-status --refresh`, and `workflow-status --refresh` on isolated state `/tmp/ict-engine-family-f-vrp-pack-readback-v1`; all commands exited `0`, and `checks/artifact_validation.json` has no errors. Result: inventory sees `candidate_pack_count=1`, `preferred_density=1`, `cross_market=1`; admission writes `rows=6`, `mature_rows=6`, `training_weight_rows=6`; all `6/6` target rows preserve first-class branch fields; ranker runtime reaches `enabled_registered_model_ready` with `runtime_matches=6`. Decision: `handoff`, not promotion; validation remains insufficient (`raw_scored_mature=6/30`, `production_validation=6/30`, `observation_validation=0/30`), entry models still have `matched_rows=0`, Pre-Bayes/workflow do not create a live policy/bridge/execution candidate, and the earlier exact-VRP data blocker still stands because `iv_hv_compression_regime` requires real IV/HV inputs rather than OHLCV/VIX proxy relabeling.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v3`: fresh non-takeover completion audit says the current Board B objective is still not achieved. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T201543+0800-codex-board-b-objective-completion-audit-v3/summaries/terminal_decision_summary.md`. Real evidence inspected: current board, external claims, git status, provider quartet readback, Auto-Quant/import replay summary, Kraken exact-branch workflow/policy/pre-Bayes readbacks, Kraken entry-model export summaries, Kraken analyze-bundle parity summary, and OB CatBoost propagation summary. Positive evidence remains real: yfinance/Kraken/IBKR/TradingViewRemix probes exist, Auto-Quant/import and prior init returned `0` on a replay, CatBoost/path-ranker runtime is ready on the strongest Kraken exact branch with `raw_scored_mature=12124/30`, `production_validation=12124/30`, `observation_validation=498/30`, and workflow phase readback can select `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1`. Decision: `handoff`, not promotion; completion remains blocked because BBN/entry-model exports still have `matched_rows=0`, update rows consume analyze ids missing from the copied owner state, and execution remains `execution_observe_only` / `ready=false` while fresh analyze can still rebuild a generic scenario-led live bundle. Next useful work is a comparable-data owner-symbol analyze/update bridge for `DENSE_KLINE_BRANCH`, not another provider probe or ranker-forcing slice.

- Handoff `2026-05-16 / codex-board-b-smt-historical-strict-scan-v1`: expanded `smt_relationship_resolver` from the latest-event outcome bridge into a historical scan over retained provider-backed 15m candles without mutating shared runtime code or taking over other SMT roots. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T195905+0800-codex-board-b-smt-historical-strict-scan-v1/summaries/terminal_decision_summary.md`. Real work executed: scanned the existing provider-data root from `20260516T091500+0800-codex-smt-provider-touch-refresh-v1`, generated `historical_smt_strict_rows.csv`, per-regime stats, and required-pair coverage, then ran the packet assertion. The scan was also lifted into native candidate-pack inspection under `support/examples/factor_candidate_packs/board-b-smt-historical-strict-v1`; direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, and `policy-training-status` all exited `0` on isolated state `/tmp/ict-engine-board-b-smt-historical-strict-v1-state`. Native readback sees `candidate_pack_count=1`, candidate `smt_historical_strict_scan_v1`, `aggregate_trade_count=32`, `transfer_status=cross_market_candidate`, admission `rows=8`, `mature_rows=0`, `rows_with_training_weight=0`, policy inventory ready with `cross_market_candidate_count=1`, but ranker runtime remains `runtime_selection=disabled`, validation remains `raw_scored_mature=0/30 production_validation=0/30 observation_validation=0/30`, and both entry models have `matched_rows=0`. This materially improves density versus the prior outcome bridge: `rows=165`, `strict_training_rows=32`, `inverse_normalized_rows=45`, `standalone_actionable_count=0`, and every required pair lane has at least one strict row (`NQ/ES`, `NQ/YM`, `EURUSD/GBPUSD`, `EURUSD/DXY`, `XAUUSD/XAGUSD`, `XAUUSD/DXY`, `BTC/ETH`). The packet still fails closed for promotion because strict rows are regime-skewed: `range=19`, `transition=7`, `trend=6`, `stress=0`, `other=0`, so `missing_or_sparse_regimes=[other, stress, transition, trend]`, `downstream_allowed=false`, and `trade_usable=false`. Decision: `handoff`, not promotion; no Auto-Quant, Pre-Bayes/filter, BBN, execution-tree, or feedback promotion command was run because the packet itself forbids downstream admission until per-regime strict density clears, and the native candidate-pack readback did not enable runtime selection or supervised ranker/entry-model training.

- Handoff `2026-05-16 / codex-market-structure-update-bridge-v1`: the kept `market_structure_event_classifier_direct_limit_v1` seed now crosses a real structural-feedback update bridge into Pre-Bayes assignments, structural path-ranking target rows, registered-model runtime selection, and execution-candidate readback. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T200500+0800-codex-market-structure-update-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: used the existing candidate pack in a fresh isolated `/tmp` state, ran `factor-candidate-packs`, `factor-candidate-admission-targets`, created a structural feedback file for `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1`, ran `update --feedback-file ... --ensemble`, then reran `pre-bayes-status --refresh`, `policy-training-status`, corrected `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, and `workflow-status --phase execution-candidate`. Result: Pre-Bayes now exposes `latest_gate_status=pass_neutralized` and exact `regime_profit_branch_path`; exported target rows include the first-class branch fields with `mature_rows=2` and `rows_with_training_weight=2`; runtime reaches `enabled_registered_model_ready` with `runtime_active_match_count=3`; workflow execution-candidate selects the exact branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.34938063063063063`, and `execution_readiness=0.57`. Decision: `handoff`, not promotion; execution remains `execution_observe_only`, `workflow_snapshot.json` does not persist `latest_execution_candidate`, and both entry models still have `matched_rows=0` because the update bridge lacks same-state analyze history.

- Handoff `2026-05-16 / codex-market-structure-samestate-entrymodel-bridge-v1`: continued the exact `market_structure_event_classifier_direct_limit_v1` branch through same-state analyze/update linkage without taking over seed/update/audit claims. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T203849+0800-codex-market-structure-samestate-entrymodel-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the prior update-bridge state into `/tmp/ict-engine-market-structure-samestate-analyze-bridge-v1/state`; first proved demo analyze writes same-state history but no entry packets; then ran provider-backed analyze/update probes on retained yfinance, IBKR, TradingViewRemix, and Kraken clean-candle files, all of which produced both `cisd_rb_long_v1` and `breaker_rb_long_v1` packets while preserving the exact branch path; then located real seed-packet `loss` rows for yfinance/YF, IBKR, and Kraken and replayed branch-aware `update --outcome loss` for those same-state analyze ids without fabricating a TradingViewRemix loss. Final readback reran `pre-bayes-status --refresh`, `policy-training-status`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, and `workflow-status --phase execution-candidate`. Result: Pre-Bayes keeps `latest_gate_status=pass_neutralized` and exact `regime_profit_branch_path`; both entry models move from `matched_rows=0` to `matched_rows=7` and now have win/loss label diversity; target export remains `rows=4`, `mature_rows=2`, `rows_with_training_weight=2`; workflow execution-candidate selects the exact branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.31724723772727603`, and `execution_readiness=0.37`. Decision: `handoff`, not promotion; entry-model BBN/CatBoost remains below row-count thresholds, stale demo-linked rows still create linkage warnings, breaker RB still has low trigger-confirmation diversity, workflow is `execution_blocked`, and this slice reused retained provider-backed candle artifacts rather than refetching live endpoints.

- Blocked `2026-05-16 / codex-market-structure-tvr-entry-density-v1`: attempted the next distinct provider-axis entry-density continuation for `market_structure_event_classifier_direct_limit_v1` on TradingViewRemix `NASDAQ:QQQ 1h` without reusing the completed yfinance/Kraken density claims. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T210711+0800-codex-market-structure-tvr-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: preserved the `/tmp` claim and copied the failed live provider fetch checks into the run root. Result: `market-data-harness fetch` did not acquire candles; provider summary reports `tradingview_mcp` as `configured_runtime_unhealthy` with `tradingview_mcp_connectivity_probe_failed`, and the fetch result is `ok=false` with `fetch_failed: tradingview MCP call 'get_ohlcv' returned error`. Decision: `blocked`, not promotion; no `analyze`, `update`, Pre-Bayes/filter, BBN/entry-model, CatBoost/path-ranker, or execution-tree command was run from this TVR slice, and no substitute provider rows or fabricated loss labels were used. Next useful work is a lightweight TradingViewRemix MCP health/readback before retrying this provider axis, or a genuinely distinct unclaimed provider/instrument/window density slice.

- Handoff `2026-05-16 / codex-market-structure-yf-iwm-entry-density-v1`: added a fresh same-state yfinance `IWM 1h` density continuation for `market_structure_event_classifier_direct_limit_v1` from the completed same-state bridge, distinct from SPY/DIA/IBKR QQQ/Kraken XBT and the blocked TVR QQQ attempt. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211000+0800-codex-market-structure-yf-iwm-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched real yfinance `IWM 1h` rows through `market-data-harness` (`368` rows), normalized them, ran direct-binary `analyze`, branch-aware `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: the analyze run emitted both `breaker_rb_long_v1` and `cisd_rb_long_v1`, update consumed same-state id `analyze:MARKET_STRUCTURE_DIRECT_LIMIT:20260516T131355.064Z`, Pre-Bayes stayed `pass_neutralized`, both entry models improved to `matched_rows=8`, target validation improved to `raw_scored_mature=55/30` and `production_validation=55/30`, runtime is `enabled_registered_model_ready` with `runtime_matches=3`, and workflow selects the exact branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.28019411141787987`, and `execution_readiness=0.57`. Decision: `handoff`, not promotion; observation validation remains `10/30`, trainer artifact is still `present_validation_insufficient`, both entry models remain below threshold, workflow is still `execution_observe_only`, and TradingViewRemix remains live-blocked for this exact bridge.

- Handoff `2026-05-16 / codex-market-structure-yf-sector-basket-entry-density-v1`: expanded the exact direct-limit branch with a real yfinance sector-ETF `1h` basket (`XLF`, `XLK`, `XLV`, `XLY`, `XLI`) on the copied same-state bridge, without source edits or takeover of active non-market-structure lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T211600+0800-codex-market-structure-yf-sector-basket-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched real yfinance rows through `market-data-harness` (`XLF=368`, `XLK=368`, `XLV=368`, `XLY=368`, `XLI=367`), normalized each set, ran direct-binary `analyze` plus branch-aware `update --feedback-file --ensemble` for every same-state analyze id, then reran `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: Pre-Bayes stayed `pass_neutralized`, both entry models improved to `matched_rows=13`, target validation improved to `raw_scored_mature=120/30` and `production_validation=120/30`, observation validation improved to `15/30` with `loss=3` / `win=12`, runtime stayed `enabled_registered_model_ready` with `runtime_matches=3`, and workflow selects the exact branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.3109250367116543`, and `execution_readiness=0.57`. Decision: `handoff`, not promotion; entry-model rows remain below BBN/CatBoost thresholds, observation validation remains short, trainer artifact is still `present_validation_insufficient`, workflow is still `execution_observe_only`, and TradingViewRemix remains live-blocked for this exact bridge.

- Handoff `2026-05-16 / codex-market-structure-yf-crossasset-basket-entry-density-v1`: pushed the same exact direct-limit branch through a larger real yfinance cross-asset `1h` basket (`EFA`, `EEM`, `TLT`, `HYG`, `GLD`, `SLV`, `USO`, `XLE`, `XLU`, `XLP`, `XLB`, `XLRE`, `XHB`, `IYR`, `KRE`, `SMH`, `RSP`) from the sector-basket state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212000+0800-codex-market-structure-yf-crossasset-basket-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched and normalized all 17 real yfinance legs (`315-368` rows per symbol), ran direct-binary `analyze` and branch-aware `update --feedback-file --ensemble` for each same-state analyze id, then reran `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: Pre-Bayes stayed `pass_neutralized`; `cisd_rb_long_v1` reached `matched_rows=30` and is BBN-usable, while `breaker_rb_long_v1` improved to `matched_rows=29`; target validation improved to `raw_scored_mature=528/30`, `production_validation=528/30`, and `observation_validation=32/30` with `loss=3` / `win=29`; runtime stayed `enabled_registered_model_ready` with `runtime_matches=3`; workflow selects the exact branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.31123864632193615`, `execution_readiness=0.57`, and posterior/prior hint `0.905/0.856`. Decision: `handoff`, not promotion; `breaker_rb_long_v1` is still one row below the BBN floor, both entry models remain CatBoost-weak, trainer artifact is still `present_validation_insufficient`, workflow remains `execution_observe_only`, and TradingViewRemix remains live-blocked for this exact bridge.

- Handoff `2026-05-16 / codex-market-structure-yf-mdy-entry-density-v1`: added one real yfinance `MDY 1h` same-state observation from the cross-asset state to test the remaining `breaker_rb_long_v1` BBN row-floor gap. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T212900+0800-codex-market-structure-yf-mdy-entry-density-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched real yfinance `MDY 1h` rows through `market-data-harness` (`339` rows), normalized them, ran direct-binary `analyze`, branch-aware `update --feedback-file --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: Pre-Bayes stayed `pass_neutralized`, both entry models now clear the BBN row floor (`cisd_rb_long_v1 matched_rows=31`, `breaker_rb_long_v1 matched_rows=30`), target validation is `raw_scored_mature=561/30`, `production_validation=561/30`, and `observation_validation=33/30` with `loss=3` / `win=30`, runtime stayed `enabled_registered_model_ready`, and workflow selects the exact branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.32841626920465167`, `execution_readiness=0.57`, and posterior/prior hint `0.908/0.859`. Decision: `handoff`, not promotion; both entry models remain CatBoost-weak below the recommended `50` rows, trainer artifact is still `present_validation_insufficient`, workflow remains `execution_observe_only`, and TradingViewRemix remains live-blocked for this exact bridge.

- Handoff `2026-05-16 / codex-market-structure-yf-catboost-floor-basket-v1`: pushed the exact direct-limit branch through another real yfinance `1h` ETF basket (`ACWI`, `VEA`, `VWO`, `BND`, `LQD`, `JNK`, `SHY`, `IEF`, `IEI`, `TIP`, `VNQ`, `IAU`, `PPLT`, `DBA`, `UUP`, `FXE`, `FXY`, `ARKK`, `ITB`, `IBB`, `XRT`, `XME`) to test the entry-model CatBoost row floor. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213200+0800-codex-market-structure-yf-catboost-floor-basket-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched and normalized all 22 real yfinance legs (`245-368` rows per symbol), ran direct-binary `analyze` and branch-aware `update --feedback-file --ensemble` for each same-state analyze id, then reran `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Result: Pre-Bayes stayed `pass_neutralized`; both entry models now clear BBN and CatBoost row floors (`cisd_rb_long_v1 matched_rows=53`, `breaker_rb_long_v1 matched_rows=50`); policy summaries say both models look healthy for BBN and CatBoost; target validation improved to `raw_scored_mature=1540/30`, `production_validation=1540/30`, and `observation_validation=55/30` with `loss=3` / `win=52`; runtime stayed `enabled_registered_model_ready`; workflow selects the exact branch with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.3325329347746956`, `execution_readiness=0.57`, and posterior/prior hint `0.944/0.892`. Decision: `handoff`, not promotion; structural target still warns `structural_path_ranking_target_trainer_artifact_incomplete`, trainer artifact remains `present_validation_insufficient`, workflow remains `execution_observe_only`, and TradingViewRemix remains live-blocked for this exact bridge.

- Handoff `2026-05-16 / codex-market-structure-catboost-artifact-refresh-v1`: carried the exact direct-limit branch through an external CatBoost path-ranker artifact after the yfinance density floor cleared. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214200+0800-codex-market-structure-catboost-artifact-refresh-v1/summaries/terminal_decision_summary.md`. Real work executed: trained/applied CatBoost via `path_ranker_integration.py`, applied the generated score CSV to ict-engine, registered `trainer_artifact.json`, re-enabled runtime, refreshed policy/workflow, then reran a fresh post-CatBoost `analyze` on retained real `XME 1h` material so the execution tree could consume the CatBoost state; all commands exited `0`. Result: policy readback now has `trainer_artifact_status=runtime_eligible`, `runtime_selection_status=enabled_candidate_set_ready`, `runtime_source_kind=candidate_set`, `score_model_family=catboost`, `score_source_kind=external_model`, and `runtime_active_match_count=3`; entry models remain healthy (`cisd_rb_long_v1 matched_rows=53`, `breaker_rb_long_v1 matched_rows=50`); workflow selects the exact branch with `path_ranker_runtime_source=candidate_set`, `path_ranker_raw_score=0.7748468785190367`, and `pre_bayes_gate_status=pass_neutralized`; the fresh execution tree reports `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, `path_ranker_model_family=catboost`, `path_ranker_runtime_source=candidate_set`, and `ranker_validation_ready=true`. Decision: `handoff`, not promotion; workflow is still `execution_blocked`/`ready=false`, execution tree remains `transition_guardrail` with `execution_guarded_due_to_high_transition_hazard`, closed-loop branch admission is `fail_closed`, and TradingViewRemix remains live-blocked for this exact bridge.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v6`: prompt-to-artifact audit after the CatBoost execution-tree packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-board-b-objective-completion-audit-v6/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-board-b-objective-completion-audit-v6/checks/prompt_to_artifact_checklist.csv`. Result: objective is still not complete. Positive evidence now covers the exact rooted branch through Pre-Bayes, BBN-ready entry models, external CatBoost, workflow, and execution tree with CatBoost score visible and used. Missing evidence remains decisive: TradingViewRemix current live candles were not acquired, workflow remains `execution_blocked` / `ready=false`, execution tree stays `transition_guardrail` with `execution_guarded_due_to_high_transition_hazard`, and closed-loop branch admission remains `fail_closed`. Decision: `handoff`, not promotion; `update_goal=false`.

- Blocked `2026-05-16 / codex-market-structure-execution-hazard-rootcause-v1`: root-caused the remaining non-TVR blocker after CatBoost execution-tree visibility was proven. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214900+0800-codex-market-structure-execution-hazard-rootcause-v1/summaries/terminal_decision_summary.md`; structured readback: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214900+0800-codex-market-structure-execution-hazard-rootcause-v1/checks/execution_hazard_readback.json`. Result: the blocker is no longer branch-path propagation, BBN row count, CatBoost scoring, or execution-tree score visibility. The execution tree sees and uses CatBoost (`path_ranker_model_family=catboost`, `path_ranker_runtime_source=candidate_set`, score visible/used true, raw score `0.774847`, path-ranker gate pass), but final execution remains fail-closed because `execution_readiness=0.3899` is below the `0.45` branch gate and `hybrid_transition_hazard=0.610` exceeds threshold `0.600`, forcing `transition_guardrail` / `execution_guarded_due_to_high_transition_hazard`. Decision: `blocked`, not promotion; next useful work is a lower-hazard current provider/window for the same exact branch, a tested execution-policy change if the threshold is wrong, or TradingViewRemix recovery.

- Handoff `2026-05-16 / codex-market-structure-samestate-analyze-link-v1`: continued the market-structure direct-limit bridge on a separate cloned-state axis and narrowed the entry-model blocker from missing analyze history to missing entry-model packets. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T200432+0800-codex-market-structure-samestate-analyze-link-v1/summaries/terminal_decision_summary.md`. Real work executed: cloned the completed update-bridge state into a new run root, ran `analyze --demo` to create same-state `analyze:MARKET_STRUCTURE_DIRECT_LIMIT:20260516T121837.417Z`, rewrote only the run-root structural feedback copy so `source_run_id` pointed at that analyze id, then reran `update --feedback-file`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Verified improvement: the new update consumes the same-state analyze id, workflow now persists a latest execution candidate, the exact branch remains selected with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.3562269682247214`, and `execution_readiness=0.57`, while target validation improves to `raw_scored_mature_rows=3`, `production_validation_rows=3`, and `observation_validation_rows=2`. Remaining blocker: both entry models still report `matched_rows=0`; the retained old update accounts for `consumed_analyze_run_missing_from_state:1`, and the new same-state analyze accounts for `analyze_runs_missing_entry_model_packet:1` because its `entry_model_packets` map is empty. Decision: `handoff`, not promotion; next useful unclaimed work is a real provider-candle analyze replay around the direct-limit source rows that emits `cisd_rb_long_v1` and/or `breaker_rb_long_v1` packets for the same rooted branch, not another update/ranker proof.

- Handoff `2026-05-16 / codex-market-structure-yf-spy-entry-packet-probe-v1`: the direct-limit branch now has a real-provider same-state entry-model bridge on yfinance `SPY 1h`, moving the entry-model blocker from zero matched rows to insufficient matched-row density. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T202000+0800-codex-market-structure-yf-spy-entry-packet-probe-v1/summaries/terminal_decision_summary.md`. Real work executed: fetched real yfinance `SPY 1h` candles via `market-data-harness`, normalized them, ran `analyze` on cloned direct-limit state, then attached exact rooted structural feedback to the resulting analyze id and reran `update --feedback-file`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `enable-structural-path-ranking-runtime`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all commands exited `0`. Verified improvement: latest analyze emitted both `breaker_rb_long_v1` and `cisd_rb_long_v1` packets, the update consumed `analyze:MARKET_STRUCTURE_DIRECT_LIMIT:20260516T123002.550Z`, entry models now show `cisd_rb_long_v1 matched_rows=1` and `breaker_rb_long_v1 matched_rows=1`, target validation improved to `raw_scored_mature_rows=6`, `production_validation_rows=6`, `observation_validation_rows=3`, and workflow still selects `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1` with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.3246775769443596`, and `execution_readiness=0.57`. Decision: `handoff`, not promotion; the branch remains `execution_observe_only` / `ready=false`, entry-model rows are below minimum and lack win/loss diversity, and this new entry-packet bridge covers yfinance only while IBKR/Kraken/TradingViewRemix still need comparable entry-packet or density follow-up.

- Handoff `2026-05-16 / codex-market-structure-analyze-branch-injection-v1`: continued the refreshed analyze-time branch-injection state for `MARKET_STRUCTURE_EVENT_145000` through CatBoost apply/register and post-apply workflow readback. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T200028+0800-codex-market-structure-analyze-branch-injection-v1/summaries/terminal_decision_summary.md`. Real work executed: reused the copied state with Auto-Quant strategy library, reran/recovered the external CatBoost ranker leg from `20260514T145000.../path_ranker_model_v1`, applied scores with `apply-structural-path-ranking-external-scores`, registered the CatBoost trainer artifact, enabled candidate-set runtime, reran `policy-training-status`, `workflow-status --phase execution-candidate`, and `pre-bayes-status --refresh`, then inspected `execution_tree_trace.json`. Result: the exact branch row `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1` survives in target fields and Pre-Bayes assignments, and both target rows now carry CatBoost external scores with `runtime_selection=enabled_candidate_set_ready`, `runtime_active_match_count=2`, and workflow `path_ranker_runtime_source=candidate_set`. Decision: `handoff`, not promotion; workflow still selects the generic `trend_follow_through` path, policy validation remains `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, entry models remain `matched_rows=0`, and execution tree still reports `path_ranker_score_visible_to_execution_tree=false` / `path_ranker_score_used_by_execution_tree=false`.

- Handoff `2026-05-16 / codex-market-structure-exact-feedback-bridge-v1`: pushed the same `MARKET_STRUCTURE_EVENT_145000` exact branch one layer farther by adding same-state structural-feedback updates on top of the post-ranker branch-injection state, then reran the patched workflow/export bridge against the same isolated state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T203000+0800-codex-market-structure-exact-feedback-bridge-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T203000+0800-codex-market-structure-exact-feedback-bridge-v1/checks/prompt_to_artifact_checklist.csv`. Real work executed: copied the `200028` state into a new isolated run root, wrote positive and negative exact structural-feedback rows for `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1`, ran `update --feedback-file ... --ensemble`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, CatBoost train/apply/register/enable, `policy-training-status`, `workflow-status --phase execution-candidate`, patched export/score parity commands `37..40`, patched `analyze`, and patched workflow/policy readbacks `42..43`; all patched follow-up commands exited `0`. Result: exact-branch maturity exists (`mature_rows=2`, `rows_with_training_weight=2`, `update_runs_with_structural_feedback=2`, `feedback_rows_with_structural_feedback=2`), fresh CatBoost training succeeds after comparison-class feedback, the score CSV ranks the exact branch above generic (`0.8002395367207108` vs `0.32715998375361466`), workflow now selects the exact rooted branch with `path_ranker_runtime_source=candidate_set`, and execution tree now reports `path_ranker_score_visible_to_execution_tree=true` / `path_ranker_score_used_by_execution_tree=true`. Decision: `handoff`, not promotion; closed-loop branch admission is still `fail_closed` with reason `exact_structural_branch_visible_but_not_ready_or_actionable`, execution tree gate remains `observe` due `execution_guarded_due_to_low_remaining_regime_duration`, policy validation remains below floor (`raw_scored_mature=5/30`, `production_validation=5/30`, `observation_validation=2/30`), and both entry models remain `matched_rows=0`. The next blocker is not CatBoost training or workflow score consumption anymore; it is actionable admission plus entry-model/validation density for the same rooted branch.

- Handoff `2026-05-16 / codex-fvg-ifvg-density-runtime-carry-v1`: reran native candidate admission/runtime validation on the existing `30`-row provider-backed `fvg_ifvg_lifecycle` retained-window packet without touching the active FVG pullback AQ chain or Rust owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T-current-codex-fvg-ifvg-density-runtime-carry-v1/summaries/terminal_decision_summary.md`. Real work executed: refreshed the focused candidate-pack metadata under `support/examples/factor_candidate_packs/board-b-fvg-ifvg-lifecycle-v1/fvg_ifvg_lifecycle_observation_v1/` from the 30-row packet, then ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only`, `policy-training-status`, `pre-bayes-status --refresh`, and `workflow-status --refresh` on isolated state `/tmp/ict-engine-fvg-ifvg-density-runtime-v1`. The branch path `Trend -> FvgIfvgLifecycle -> gap_lifecycle_state -> fvg_ifvg_lifecycle_observation_v1` survives in the structural target rows, inventory sees `candidate_pack_count=1`, admission writes `rows=4`, and ranker runtime reaches `enabled_registered_model_ready` with `runtime_active_match_count=4`. Decision: `handoff`, not promotion; the retained-window evidence is mixed-to-negative in aggregate (`13` positive vs `17` negative, expectancy `-0.104R`), rows remain `candidate_pack_admission_pending` with `mature_rows=0`, both entry models have `matched_rows=0`, Pre-Bayes stays null, and workflow remains bootstrap/fail-closed.

- Handoff `2026-05-16 / codex-board-b-sweep-quality-proxy-outcome-bridge-v1`: converted the retained provider-backed `sweep_quality_provider_observation_v1` rows into branch-keyed proxy continuation-vs-invalidation outcomes without pretending they are realized trades. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T195000+0800-codex-board-b-sweep-quality-proxy-outcome-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: created a run-root-local builder, compiled it, then labeled `2088/2088` source sweep rows over retained provider candles with `horizon_bars=24` and `skipped_rows=0`. The proxy labels preserve the rooted path `Transition -> LiquiditySweep -> sweep_quality_confirmation_only -> sweep_quality_provider_observation:v1`; counts are `target_hit=1196`, `invalidation_hit=693`, `ambiguous_both_hit=184`, `no_hit=15`, with provider coverage across `paired_provider_packet`, `yahoo`, and `kraken`. Decision: `handoff`, not promotion; this is observation/proxy evidence only, so no `ict-engine update --feedback-file` ingestion was run and the next useful work is either a dedicated observation-only structural-feedback/admission path for these proxy rows or real provider-backed analyze/update rows for the same sweep-quality branch before Pre-Bayes/BBN/CatBoost/execution-tree promotion claims.
- Keep `2026-05-16 / codex-board-b-market-structure-direct-limit-seed-v1`: preserved the positive Gate 1 seed behind the current market-structure lineage blocker without taking over active Kraken, SMT, liquidity-texture, sweep-quality, or order-block lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T-current-codex-board-b-market-structure-direct-limit-seed-v1/summaries/terminal_decision_summary.md`. Rooted branch path: `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1`. The three-provider subset (`IBKR`, `yfinance/YF`, `Kraken`) has `126` realized direct-limit trades, `win_rate=0.761905`, `expectancy=1.261383R`, and gross profit factor `6.549198`; fresh provider probes show `yfinance` ready, `kraken_public` and `ibkr` blocked only by default-shell Python deps, and `tradingview_mcp` still connectivity-blocked / absent from the source outcome rows. The seed is now lifted into `support/examples/factor_candidate_packs/board-b-market-structure-direct-limit-v1/market_structure_event_classifier_direct_limit_v1/` and was run on `/tmp/ict-engine-market-structure-direct-limit-seed-v1`: candidate inventory found the pack, admission wrote `rows=4`, ranker runtime reached `enabled_registered_model_ready` with `runtime_active_match_count=4`, policy saw `candidate_pack_count=1`, Pre-Bayes stayed null, and workflow remained bootstrap/fail-closed. Decision: `keep`, not promotion; next useful work remains the live structural-target/analyze-update bridge that creates real workflow snapshots, first-class branch fields, and entry-model matched rows for this exact branch.

- Handoff `2026-05-16 / codex-board-b-provider-deps-chain-recovery-v1`: current provider/dependency readback for the operator-priority quartet is now sharper than the default `provider-status` surface. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T194947+0800-codex-board-b-provider-deps-chain-recovery-v1/summaries/terminal_decision_summary.md`. Real work executed: reread current Board B state and `/tmp` claims, confirmed the default repo provider surface still says only `yfinance` is ready (`market_data:1/8 ready`), then ran bounded low-pollution direct provider probes without changing repo code or touching active factor roots. Result: `yfinance` harness `QQQ 1h` succeeded with `370` rows, `TradingViewRemix` harness `NASDAQ:QQQ 1h` succeeded with `20` rows, `Kraken` `PF_XBTUSD 1h` succeeded with `2000` rows, `IBKR` account probe succeeded on paper account with `hist_window cap=55`, and `IBKR` `QQQ 1h` succeeded with `32` rows via the `uv run --with redis --with ib_async --with pandas ...` child env. Decision: `handoff`, not promotion; this removes a stale blanket provider blocker for the next exact-branch replay, but it does not close `Auto-Quant -> Pre-Bayes/filter -> BBN -> CatBoost/path-ranker -> execution-tree` or prove branch-path persistence by itself.

- Handoff `2026-05-16 / codex-market-structure-live-lineage-audit-v1`: current-binary readback of the prior positive `market_structure_event_classifier` packet confirms the exact blocker is still live structural-target lineage and observation maturity, not CatBoost score visibility. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T194739+0800-codex-market-structure-live-lineage-audit-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the prior state into an isolated run root, ran provider readback for market-data/yfinance/kraken_public/ibkr plus the old `tradingview_mcp` provider id, then reran `pre-bayes-status --refresh`, `policy-training-status`, `workflow-status --phase execution-candidate`, `workflow-status --refresh`, and `export-structural-path-ranking-target` through `.local-artifacts/cargo-target/debug/ict-engine`. The runtime still consumes CatBoost candidate-set scores (`runtime_selection=enabled_candidate_set_ready`, `runtime_source=candidate_set`, `score_model_family=catboost`, `runtime_matches=3`), workflow selects the `trend_follow_through` path with `path_ranker_raw_score=0.7506586567765241`, and execution-tree score visibility/usage remains true. The fail-closed blocker remains sharper: the fresh live target has `3` rows, all `unobserved`, all branch-lineage columns empty (`main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor/regime_profit_branch_path`), `mature_rows=0`, `rows_with_training_weight=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and both entry models have `matched_rows=0`. Provider readback was mixed: yfinance/kraken_public/ibkr exited `0`, while the old explicit provider id `tradingview_mcp` is not accepted by this binary even though it appears in domain catalog output. Decision: `handoff`, not promotion; next useful unclaimed work is comparable structural-feedback observations for this exact branch, or a live structural-target export repair that preserves `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1` as first-class branch fields before more promotion attempts.

- Handoff `2026-05-16 / codex-board-b-market-structure-current-branch-replay-v1`: the market-structure live structural-target repair now preserves the exact rooted branch into Pre-Bayes and target export, but not yet into workflow/execution-tree selection or entry-model rows. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T201000+0800-codex-board-b-market-structure-current-branch-replay-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the prior market-structure state into a fresh isolated run root and reran direct-binary `analyze`, `pre-bayes-status --refresh`, `export-structural-path-ranking-target`, `policy-training-status`, and `workflow-status --phase execution-candidate`; all five commands exited `0`. Verified improvement: Pre-Bayes assignments now carry `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1`, and the structural target has `2` rows with the exact rooted branch as a first-class rank-2 row (`raw_path_score=0.719557`, branch fields populated). Remaining blocker: workflow still selects the generic `trend_follow_through` scenario path as rank 1, top-level workflow candidate fields keep `path_ranker_runtime_source=null` / `path_ranker_raw_score=null`, persisted `execution_candidate.json` remains `candidate_status=no_trade`, `execution_tree_trace.json` says `path_ranker_score_visible_to_execution_tree=false` and `path_ranker_score_used_by_execution_tree=false`, ranker validation is `raw_scored_mature=0/30 production_validation=0/30 observation_validation=0/30`, and both entry models still have `matched_rows=0`. Decision: `handoff`, not promotion; next useful unclaimed work is the exact-branch workflow/execution-tree selected-path bridge plus comparable entry-model/update rows, not another provider or seed reproof.

- Incubate `2026-05-16 / codex-board-b-fvg-ifvg-provider-outcome-density-v1`: the first provider-backed observation-density packet for `fvg_ifvg_lifecycle` now exists and has been carried through native candidate-pack admission/path-ranker readback without taking over the active FVG pullback AQ chain or Rust/Cargo owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T195114+0800-codex-board-b-fvg-ifvg-provider-outcome-density-v1/summaries/terminal_decision_summary.md`. Real work executed: reused retained provider candles from the FVG retrace portability/downstream packets, adapted the existing FVG runtime snapshot into helper inputs, generated `30` retained sliding-window observations for `yfinance:QQQ:1h`, `ibkr:QQQ:1h`, and `kraken_public:PF_XBTUSD:1h`, then created a run-root candidate pack and ran direct-binary `factor-candidate-packs`, `factor-candidate-admission-targets`, `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only`, `policy-training-status`, `pre-bayes-status --refresh`, and `workflow-status --refresh` on isolated state `/tmp/ict-engine-fvg-ifvg-provider-density-v1`. The combined packet preserves `Trend -> FvgIfvgLifecycle -> gap_lifecycle_state -> fvg_ifvg_lifecycle_observation_v1` in every observation row (`positive_rows=13`, `negative_rows=17`). Native admission writes `rows=4`, `mature_rows=4`, `training_weight_rows=4`, with branch outcomes `matured_success=1` and `matured_failure=3`; ranker runtime reaches `enabled_registered_model_ready` with `runtime_matches=4`. Decision: `incubate`, not promotion; validation remains insufficient (`raw_scored_mature=4/30`, `production_validation=4/30`, `observation_validation=0/30`), both entry models have `matched_rows=0`, Pre-Bayes remains null, and workflow still has `stage_unavailable / no workflow phase snapshots available`.

- Blocked `2026-05-16 / codex-board-b-smt-outcome-bridge-reuse-v1`: continued the existing `smt_relationship_resolver_same_event_confirmation_failure_v1` outcome-bridge claim instead of starting a duplicate SMT lane. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T170500+0800-codex-board-b-smt-outcome-bridge-reuse-v1/summaries/terminal_decision_summary.md`. Real work executed: reran the provider-backed SMT strict trainer on the existing entry-context outcome rows, fixed the run-root verifier so it enforces price levels on actual SMT signal rows while allowing missing-level `smt_signal=none` rows to remain explicitly fail-closed, and reran the packet assertion. The earlier outcome-schema blocker is gone because the source rows now include `forward_return` and `outcome_hit`; the structured packet validates with `rows=15`, `inverse_normalized_rows=4`, `standalone_actionable_count=0`, and `inverse_normalization_auditable=true`. The trading gate still fails closed: `strict_training_rows=0`, every required lane (`NQ/ES`, `NQ/YM`, `EURUSD/GBPUSD`, `EURUSD/DXY`, `XAUUSD/XAGUSD`, `XAUUSD/DXY`, `BTC/ETH`) lacks strict trainable rows, all regime buckets remain below floor, `downstream_allowed=false`, and `trade_usable=false`. Decision: `blocked`, not promotion; no Pre-Bayes/filter, BBN, CatBoost/path-ranker, execution-tree, or feedback promotion command was run because the packet itself forbids downstream admission until same-event strict rows include stable relationship, overlapping session/timeframe, base/comparison levels for actual signals, inverse raw structure, MSS/CISD, displacement, PDA context, and forward outcomes.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v2`: fresh non-takeover completion audit says the current Board B objective is still not achieved. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T195000+0800-codex-board-b-objective-completion-audit-v2/summaries/terminal_decision_summary.md`. Real work executed: created an external `/tmp` claim, reused the existing `.local-artifacts/cargo-target/debug/ict-engine` binary, and reran bounded `pre-bayes-status --refresh`, `policy-training-status`, and `workflow-status --phase execution-candidate` on the copied `DENSE_KLINE_BRANCH` Kraken state without starting another shared Cargo build. The positive evidence is real: workflow readback preserves the exact Kraken recommended path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1`, `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.600379693206094`, `execution_readiness=0.5493413794295864`; policy readback shows ranker runtime `enabled_registered_model_ready`, `active_match_count=3`, `raw_scored_mature=12124`, `production_validation=12124`, and `observation_validation=498`. The truthful blocker remains downstream: Pre-Bayes assignments still expose the generic branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1`, workflow stays `execution_observe_only`, and both entry-model providers remain `matched_rows=0` with `linkage_warnings=consumed_analyze_run_missing_from_state:150`. Decision: `handoff`, not promotion; next useful work is a comparable-data owner-symbol analyze/update bridge that preserves the exact Kraken rooted branch while making update rows consume analyze runs present in the same copied state.

- Handoff `2026-05-16 / codex-board-b-sweep-quality-pack-ranker-v1`: lifted `sweep_quality_provider_observation_v1` through the native Board B candidate-pack and ranker-readback loop without taking over the active SMT, liquidity-texture, FVG, or order-block lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T174112+0800-codex-board-b-sweep-quality-pack-ranker-v1/summaries/terminal_decision_summary.md`. Real work executed: `provider-status --domain market_data --agent`, explicit-root `factor-candidate-packs`, explicit-root `factor-candidate-admission-targets`, repeated `policy-training-status`, `workflow-status --refresh`, and a direct fallback path-ranker apply retry on isolated state `/tmp/ict-engine-board-b-sweep-quality-pack-ranker-v1-state`. Current readback is useful but not promotable: candidate-pack inventory is visible (`candidate_pack_count=1`), admission exports `rows=9` for `factor-candidate-admission:FACTOR_CANDIDATES:board-b-sweep-quality-v1`, and the final fresh policy readback has `runtime_selection_status=enabled_registered_model_ready` with `runtime_active_match_count=9`; however the source packet is observation-only with `trade_count=0`, `mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, both entry models have `matched_rows=0`, and fresh workflow readback still has `blocking_truth.status=insufficient_state`, `reason=no workflow phase snapshots available`, `latest_execution_candidate=null`. Decision: `handoff`, not promotion; next useful unclaimed work is an outcome-labeling or provider-backed analyze/update bridge that creates real structural-feedback observations for this rooted sweep-quality factor before rerunning Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree admission.
- Handoff `2026-05-16 / codex-provider-quartet-venv-parity-readback-v1`: refreshed the operator-named provider quartet and repaired a provider-specific lookup parity bug without taking over active factor roots. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T-provider-quartet-venv-parity-readback-v1/summaries/terminal_decision_summary.md`. Fresh provider-status readback: default shell `yfinance` is ready; default shell `kraken_public` and `ibkr` fail only on missing Python runtime deps, while the known provider venv `/Users/thrill3r/.venvs/ict-engine-provider-py313/bin` makes both `kraken_public` and `ibkr` ready. The earlier aggregate-vs-specific `tradingview_mcp` mismatch was root-caused to `src/application/provider_catalog.rs`: provider-specific filtering routed `tradingview_mcp` to `live_runtime`, but the actual item is produced by the market-data catalog. Minimal source repair now routes it through `market_data`; focused test `CARGO_TARGET_DIR=/tmp/ict-engine-provider-catalog-test cargo test tradingview_mcp_provider_filter_uses_market_data_catalog -- --nocapture` passed, and patched CLI `/tmp/ict-engine-provider-catalog-test/debug/ict-engine provider-status --provider tradingview_mcp --compact` now returns `rc=0` with the real pending status `tradingview_mcp_connectivity_probe_failed` instead of `unknown provider id`. Decision: `handoff`, not promotion; next Board B live/current slices should put the provider venv first on `PATH` for Kraken/IBKR and treat TradingViewRemix as a connectivity blocker, not an unknown-provider blocker.
- Handoff `2026-05-16 / codex-family-d-cropped-analyze-downstream-readback-v1`: continued the rooted Family D liquidity-sweep branch after the prior cropped provider-backed `analyze` materialized real state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T174100+0800-codex-family-d-cropped-analyze-downstream-readback-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the prior cropped state into a fresh run root, re-read provider status for `yfinance`, `kraken_public`, `ibkr`, and `tradingview_mcp`, ran `pre-bayes-status --refresh`, `policy-training-status`, `workflow-status --phase execution-candidate`, corrected and reran `export-structural-path-ranking-target`, trained/applied/registered CatBoost path scores through `path_ranker_integration.py`, enabled structural path-ranker runtime, and reran policy/workflow readback. The useful correction is that this branch is no longer blocked at "no first snapshot": the copied state has `analyze_runs.json`, `workflow_snapshot.json`, `execution_candidate.json`, and `execution_tree_trace.json`; Pre-Bayes is populated with `pass_neutralized`; structural export writes `rows=4`; CatBoost score registration reaches `runtime_selection=enabled_registered_artifact_ready` with `runtime_matches=4`. The truthful blocker moved later: the exact rooted row `TrendTransition -> LiquidityReclaim -> family_d_liquidity_sweep_reclaim_15m_wide_v1 -> liquidity_sweep_reclaim_long` survives with branch fields, but generic scenario rows still dominate the ranking, all rows remain unobserved (`mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`), both entry models have `matched_rows=0`, and workflow/execution remains `execution_observe_only` / `fail_closed` with `bridge_probability_gap=0.012`. Provider readback was mixed: `yfinance`, `kraken_public`, and `ibkr` exited `0`, while `tradingview_mcp` is still unavailable as a provider id. Decision: `handoff`, not promotion; next useful unclaimed work is comparable structural-feedback observations for the exact rooted Family D row, or a target/ranker selection repair that prevents generic scenario paths from outranking the exact rooted factor branch before execution-tree readback.
- Handoff `2026-05-16 / codex-liquidity-texture-registered-model-invalid-diagnosis-v1`: continued the rooted `liquidity_pool_texture` owner-symbol chain on a copied `IBKR TLT` state and proved the current blocker is still after candidate-set score visibility, not missing score application. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T174007+0800-codex-liquidity-texture-registered-model-invalid-diagnosis-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the existing owner state, refreshed `provider-status --domain market_data --agent`, reran `policy-training-status`, `workflow-status --phase execution-candidate`, and `workflow-status --refresh`, then re-ran `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only` on the copied state and repeated policy/workflow readbacks. Provider readback remains honest (`yfinance=ready`; `ibkr` gateway reachable but missing runtime deps; `tradingview_mcp` connectivity probe failed; `kraken_public` missing Python provider deps). The re-enable probe did not recover registered-model runtime: `runtime_selection=enabled_registered_model_invalid`, `runtime_source=registered_model_artifact`, `active_match_count=0`, `runtime_artifact_match_count=0`, while `runtime_candidate_set_match_count=3` stayed present. Workflow still sees `path_ranker_runtime_source=candidate_set` with `path_ranker_raw_score=0.9081276064385436`, but `execution_tree_trace.json` keeps `path_ranker_runtime_source=unknown`, `path_ranker_score_visible_to_execution_tree=false`, `path_ranker_score_used_by_execution_tree=false`, `gate_status=observe`, and `decision_hint=execution_guarded_due_to_high_transition_hazard`. Entry models remain empty (`cisd_rb_long_v1 matched_rows=0`, `breaker_rb_long_v1 matched_rows=0`) and owner-symbol target rows still have empty `main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor/regime_profit_branch_path`. Decision: `handoff`, not promotion; next useful unclaimed work is to repair or bypass owner-symbol structural target export so rooted branch lineage survives into execution-tree-visible artifacts, then rerun this same copied owner-state chain.

- Handoff `2026-05-16 / codex-board-b-smt-strict-density-gap-audit-v1`: current native readback confirms `smt_relationship_resolver` is already visible as a strict SMT candidate pack and can enter admission/runtime surfaces, but it is still not promotable. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T-smt-strict-density-gap-audit-v1/summaries/terminal_decision_summary.md`. Real work executed: `factor-candidate-packs --candidate-pack-root support/examples/factor_candidate_packs/board-b-smt-strict-v1`, `factor-candidate-admission-targets`, `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only`, and `policy-training-status` all returned current JSON evidence under the packet `checks/` directory. Inventory/admission/runtime are no longer the blocker (`candidate_pack_count=1`, `rows=8`, `runtime_matches=8`), but promotion remains fail-closed because there are no mature/calibrated feedback rows (`mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, `training_weight_rows=0`) and the latest provider-refresh packet still has insufficient strict same-event density (`strict_training_rows=12`, missing `NQ/YM` and `EURUSD/DXY`, sparse `stress/other`). Decision: `handoff`, not promotion; next useful work is to expand strict same-event rows for those missing lanes/regimes before any AQ/Pre-Bayes/BBN/CatBoost/execution-tree promotion claim. `update_goal=false`.
- Handoff `2026-05-16 / codex-board-b-objective-parity-audit-v1`: fresh completion audit says the user objective is still not achieved, even though the current runtime can reconstruct the exact Kraken rooted branch. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T165900+0800-codex-board-b-objective-parity-audit-v1/summaries/terminal_decision_summary.md`. Real work executed: re-read the compact Board B surface, checked active `/tmp/ict-engine-agent-claims/board-b` roots and the dirty shared worktree, then ran bounded current-binary readback on the copied exact Kraken state: `.local-artifacts/cargo-target/debug/ict-engine workflow-status --symbol DENSE_KLINE_BRANCH --state-dir support/docs/experiments/actionable-regime-confidence/runs/20260517T162400+0800-codex-board-b-kraken-exact-branch-bias-v1/state_clone --phase execution-candidate --output-format json` with the provider venv first on `PATH`. The fresh runtime read returned `rc=0` and selected the exact branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` with `path_ranker_runtime_source=registered_model_artifact`, `path_ranker_raw_score=0.600379693206094`, and `execution_readiness=0.5493413794295864`. The parity failure is one layer later: `execution_candidate.json` and `workflow_snapshot.json` still persist the generic branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1`, `execution_tree_trace.json` stays `gate_status=observe`, and both entry-model exports remain `matched_rows=0`. Decision: `handoff`, not promotion; next useful work is comparable-data owner-symbol analyze/update linkage or the already-claimed analyze-time branch-persistence bridge, not more provider ingress or ranker forcing on this exact branch.
- Handoff `2026-05-16 / codex-board-b-fvg-spy-currentchain-timeout-v1`: a materially different rooted `SPY/USD` branch for `family_a_fvg_retrace_1h_v1` now has real positive Auto-Quant evidence and a valid native AQ handoff, but it still does not reach live workflow phases. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T141000+0800-codex-board-b-fvg-spy-currentchain-timeout-v1/summaries/terminal_decision_summary.md`. Real work executed: restored the missing `TomacNQ_RegimeFVGRetrace.py` source into the live Auto-Quant strategy path, replayed the factor on `SPY/USD` (`11` trades, `sharpe=0.3788`, `profit_factor=2.6760`, `win_rate_pct=81.8182`, `total_profit_pct=0.61`), converted the backtest into `strategy_library_fvg1h_spy_2025.json`, injected the rooted branch path `TrendExpansion -> FVGReclaim -> family_a_fvg_retrace_1h_v1 -> spy_fvg_retrace_1h`, then ran direct-binary `auto-quant-results-import` and `auto-quant-prior-init --force` on isolated `/tmp` state. The AQ handoff itself is healthy: import returned `n_ok=1`, prior-init returned `evidence_value_gate_passed=true`, and the final BBN prior moved to `[0.7413992059300983, 0.08605683662891903, 0.17254395744098264]`. Promotion still failed one stage later: `pre-bayes-status --refresh` remained entirely null, `policy-training-status` still reports `analyze_runs=0` / `matched_rows=0` / `structural_path_ranking_target_export_missing`, and bounded direct-binary `analyze-live --symbol FVG1H_SPY_CURRENT --futures-symbol ES=F --spot-symbol SPY --options-symbol SPY --options-volatility-proxy-symbol ^VIX --state-dir /tmp/ict-engine-family-a-fvg-spy-currentchain-v1/state --agent` timed out after `180.134s` with empty stdout/stderr and no new workflow-phase artifacts. Decision: `handoff`, not promotion; the next blocker is runtime phase materialization after successful rooted AQ import, not factor profitability or branch-path survival on the AQ side.
- Handoff `2026-05-16 / codex-board-b-kraken-exact-branch-persistence-rewrite-v1`: continued the isolated Kraken exact-bias owner state one layer deeper and proved the remaining blocker is now analyze-time persistence, not runtime branch selection or ranker maturity. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T162400+0800-codex-board-b-kraken-exact-branch-bias-v1/summaries/post_analyze_exact_branch_persistence_handoff.md`. Real work executed: reused `.../20260517T162400.../state_clone` for owner symbol `DENSE_KLINE_BRANCH`, injected the known-good provider venv on `PATH`, revalidated `provider-status` plus both no-refresh and refresh `workflow-status --phase execution-candidate`, then replayed `analyze` on the same isolated clone using `support/examples/demo/demo-15m.json`. Current-source runtime selection now holds cleanly on the exact rooted Kraken branch (`TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1`, `path_ranker_raw_score=0.600379693206094`, `path_prob_lower_bound=0.5503796932060939`, `runtime_source=registered_model_artifact`), and the replayed `analyze` also completed (`returncode=0`, `73.046s`) with `pre_bayes_gate=pass_hard`, `ensemble_vote.final_action=execute_follow_through`, and execution triage `ranker=registered_model_artifact/weighted_feature_sum_v1/ready`. The truthful stop point is one hop later inside persisted execution artifacts: `execution_candidate.json` version `5` still writes `regime_profit_branch_path=TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1` with `candidate_status=no_trade`, while `execution_tree_trace.json` now uses `runtime_source=registered_model_artifact` but still records `path_id=path:scenario:DENSE_KLINE_BRANCH:belief_regime_node:trend:trend_follow_through:primary` and ends `gate_status=observe` / `decision_hint=execution_guarded_due_to_low_remaining_regime_duration`. Decision: `handoff`, not promotion; the next useful unclaimed work is the analyze-time branch-persistence bridge from current runtime selection into `execution_candidate.json` and `execution_tree_trace.json`, not more provider ingress or ranker forcing on this slice.
- Handoff `2026-05-17 / codex-liquidity-texture-owner-symbol-exectree-bridge-v1`: the rooted liquidity-texture packet now reaches a provider-backed owner-symbol workflow surface on the cloned `IBKR TLT` early split, but execution-tree consumption still does not happen on that owner state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T183500+0800-codex-liquidity-texture-owner-symbol-exectree-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: cloned `LPT_IBKR_TLT_EARLY_124827` into a fresh run root, ran `export-structural-path-ranking-target`, reused the rooted liquidity candidate model dir through `path_ranker_integration.py --register-runtime-artifact --reuse-mode candidate_set_only`, applied external scores, and reran `workflow-status --phase execution-candidate` plus `workflow-status --refresh`. The resulting owner-state delta is real but still fail-closed: baseline workflow had no ranker runtime source, post-apply workflow selects the exact owner path `path:scenario:LPT_IBKR_TLT_EARLY_124827:belief_regime_node:trend:trend_follow_through:primary` with `path_ranker_runtime_source=candidate_set` and `path_ranker_raw_score=0.9081276064385436`, but policy-training status still reports `runtime_selection=enabled_registered_model_invalid` with `runtime_matches=0`, and `execution_tree_trace.json` keeps `path_ranker_score_visible_to_execution_tree=false` plus `path_ranker_score_used_by_execution_tree=false`. Decision: `handoff`, not promotion; the next useful unclaimed work is to diagnose the `registered_model_invalid` plus `runtime_matches=0` contract on this owner-state bridge, or rerun the same owner-symbol bridge on the preserved `yfinance XLF` rooted packet to test whether execution-tree non-consumption is owner-state-specific.

- Handoff `2026-05-17 / codex-kraken-entry-model-bridge-diagnosis-v1`: the copied exact-rooted Kraken Board B state is no longer blocked by branch preference or CatBoost/path-ranker maturity; the next truthful blocker is entry-model linkage. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T184800+0800-codex-kraken-entry-model-bridge-diagnosis-v1/summaries/terminal_decision_summary.md`. Real work executed: took a fresh private `/tmp` claim for read-only diagnosis on `.../20260517T162400.../state_clone`, ran current repo-local `policy-training-status --symbol DENSE_KLINE_BRANCH --state-dir .../state_clone --output-format json`, captured `pending_update_feedback.json`, sampled `update_runs.json`, and exported both copied-state entry-model summaries (`cisd_rb_training_export_summary.json`, `breaker_rb_training_export_summary.json`). The copied state already proves branch/ranker readiness (`structural_path_ranking_runtime.status=enabled_registered_model_ready`, `active_match_count=3`, `raw_scored_mature_rows=12124`, `production_validation_rows=12124`, `observation_validation_rows=498`), but both entry models still export zero matched rows (`cisd_rb_long_v1 matched_rows=0`, `breaker_rb_long_v1 matched_rows=0`) and both report `linkage_warnings=consumed_analyze_run_missing_from_state:150`. The mismatch is exact, not speculative: copied-state local analyze inventory has only `5` analyze ids, update rows consume `150` unique analyze ids, and all `150/150` consumed ids are missing from local analyze inventory while sampled rows still show realized outcomes such as `win`. The current copied-state pending update also remains non-comparable (`template_feedback.run_id=null`, `template_feedback.trade_id=null`, demo-data `recommended_next_command`, `risk_flags` include `update:not_comparable:different_data_fingerprint`). Decision: `handoff`, not promotion; next useful unclaimed work is to build a comparable-data owner-symbol analyze/update bridge that preserves the exact rooted branch while letting copied-state update rows consume analyze runs that actually exist in that state, rather than spending another slice on more ranker forcing for this branch.

- Handoff `2026-05-17 / codex-board-b-kraken-local-artifacts-parity-v1`: the copied rooted Kraken branch no longer times out on downstream `workflow-status` reads when exercised with the newer isolated binary at `.local-artifacts/cargo-target/debug/ict-engine` (`mtime 2026-05-16 12:37:51 +0800`). Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T184000+0800-codex-board-b-kraken-local-artifacts-parity-v1/summaries/terminal_decision_summary.md`. Real work executed: created a fresh private copy of `.../20260515T223200.../state_split_kraken_1m`, then ran `provider-status --agent`, `provider-status --compact`, `workflow-status --symbol DENSE_KLINE_BRANCH --state-dir /tmp/ict-engine-board-b-kraken-fresh-binary-state --phase execution-candidate --output-format json`, and `workflow-status --symbol DENSE_KLINE_BRANCH --state-dir /tmp/ict-engine-board-b-kraken-fresh-binary-state --refresh --output-format json` through the newer isolated binary. All four commands returned `0`. The no-refresh read now surfaces `candidate_status=execution_observe_only`, `execution_readiness=0.4612333809537747`, and a visible structural recommended path instead of hanging; the refresh path also returns with a concrete `factor-research` next command plus non-promoting risk flags. Decision: `handoff`, not promotion; this falsifies the older timeout story on this newer binary family, but the branch still remains `observe/fail_closed`, `ibkr_bridge` is still runtime-unhealthy, `tradingview_mcp` is still connectivity-unhealthy, and the stronger fully fresh `/tmp/ict-engine-board-b-workflow-probe-target-seeded-v1` build is still compiling before any stronger current-source parity claim.
- Handoff `2026-05-16 / codex-liquidity-pool-texture-xlf-owner-bridge-v1`: pushed the rooted `liquidity_pool_texture` CatBoost/runtime leg one real layer farther by reusing the proven dual-root model on a copied live owner symbol state instead of staying inside `FACTOR_CANDIDATES`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T133812+0800-codex-liquidity-pool-texture-xlf-owner-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the preserved `LPT_XLF_EARLY_123617` owner state, exported owner-symbol structural path targets, reused the dual-root `FACTOR_CANDIDATES` CatBoost model on that owner-symbol target, registered/enabled runtime reuse on the copied owner state, applied the external scores, and reran `policy-training-status` plus `workflow-status`. The truthful result is mixed but sharper: owner-symbol score application is real (`policy-training-status` summary line now shows `runtime_selection=enabled_candidate_set_ready`, `runtime_source=candidate_set`, `score_model_family=catboost`, `score_source=external_model`, `runtime_matches=3`), and all three owner-symbol target rows now carry `raw_path_score=0.908128`. But the owner-symbol structural export/readback still collapses to generic scenario paths with empty branch-lineage fields (`main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor/regime_profit_branch_path` all empty), and the later workflow/execution readback still does not surface the rooted branch (`candidate_status=no_trade`, no persisted `path_ranker_summary` or `recommended_path_bundle`). Decision: `handoff`, not promotion; the next useful unclaimed work is to repair or bypass owner-symbol structural target/export/readback so rooted factor lineage survives into workflow/execution-tree readback, not to re-prove pre-CatBoost propagation again.
- Handoff `2026-05-16 / codex-board-b-kraken-exact-branch-admission-readback-v1`: continued the copied exact-bias Kraken owner state one layer past the earlier branch-preference repair and proved the remaining blocker is no longer ranker maturity or branch-path loss. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T134036+0800-codex-board-b-kraken-exact-branch-admission-readback-v1/summaries/terminal_decision_summary.md`. Real work executed: reused `.../20260517T162400.../state_clone` for owner symbol `DENSE_KLINE_BRANCH`, verified that direct old-binary reads still timeout (`workflow-status --phase execution-candidate`, `workflow-status --refresh`, and `policy-training-status` all hit `~30s` empty timeouts), then recreated a `/tmp` fastprobe fail-closed provider environment and reran owner-symbol `workflow-status --phase pre-bayes-policy`, `workflow-status --phase execution-candidate`, and `policy-training-status`. Under fastprobe the exact rooted branch still wins runtime selection (`TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1`, `path_ranker_raw_score=0.600379693206094`, `path_prob_lower_bound=0.5503796932060939`, `runtime_source=registered_model_artifact`), but execution still stops at `candidate_status=execution_observe_only`, `execution_readiness=0.4612333809537747`, and `pre_bayes_gate_status=pass_neutralized`. Persisted `workflow_snapshot.json` sharpens the truthful stop point: `latest_execution_candidate.candidate_status=no_trade`, `latest_structural_execution_candidate=null`, `closed_loop_branch_admission=null`, `blocking_truth.stage=analyze`, `bridge_probability_gap=0.020`, `pre_bayes_quality_score=0.582`, and the recommended demo-data `factor-research` continuation is still marked `different_data_fingerprint`. `policy-training-status` also proves ranker/runtime maturity is already present (`enabled_registered_model_ready`, `runtime_matches=3`, `raw_scored_mature=12124/30`, `production_validation=12124/30`, `observation_validation=498/30`) while both entry models remain unready with `matched_rows=0`. Decision: `handoff`, not promotion; the next useful unclaimed work is a comparable-data owner-symbol analyze/execution-tree rewrite or an entry-model/outcome bridge on this exact branch, not more ranker forcing or provider ingress repro.
- Handoff `2026-05-17 / codex-order-block-yf-nonlive-analyze-parity-v1`: the corrected order-block timeout is no longer plausibly a live-wrapper-only issue; it reproduces in the shared non-live `analyze` path on the same NQ market inputs. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T171000+0800-codex-order-block-yf-nonlive-analyze-parity-v1/summaries/terminal_decision_summary.md`. Real work executed: materialized the successful exact-lookback yfinance futures fetch outputs into sibling `cleaned-1d/4h/1h/15m/5m/1m` JSON files under a fresh run root, then ran direct-binary `analyze --data-htf ... --data-mtf ... --data-ltf ... --state-dir /tmp/ict-engine-order-block-yf-nonlive-analyze-parity-v1 --agent` on a fresh symbol. The result timed out at `180.112s` with `returncode=124`, empty stdout/stderr, and no durable state files at all: no `workflow_snapshot.json`, no `analyze_runs.json`, no `execution_candidate.json`, and no `execution_tree_trace.json`. Decision: `handoff`, not promotion; together with the exact-lookback OHLCV and fast-fail `QQQ` options-summary packets, this moves the blocker firmly into the shared `analyze/report/persist` runtime before the first durable write, not the live wrapper or provider front-half.
- Handoff `2026-05-16 / codex-board-b-kraken-source-root-bias-readback-v1`: the exact rooted Kraken branch-preference improvement now reproduces on the earlier historical replay source root itself, so the old `learning_state-only / target bridge blocked` story for this lane is no longer current under the present binary. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T130708+0800-codex-board-b-kraken-source-root-bias-readback-v1/summaries/terminal_decision_summary.md`. Real work executed: cloned `.../20260515T223200.../state_split_kraken_1m`, reran `export-structural-path-ranking-target` on the clone with the current repo-local binary, then registered the proven exact-branch bias artifact from `20260517T162400...` and re-enabled runtime reuse. The upgraded bridge now holds on this stronger source-root axis: `export-structural-path-ranking-target` succeeds in `92.23s`, runtime remains `enabled_registered_model_ready` with `runtime_matches=3`, and both `workflow-status --phase execution-candidate` and `workflow-status --refresh` select the exact rooted path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` with `path_ranker_raw_score=0.600379693206094`, `path_prob_lower_bound=0.5503796932060939`, and runtime source `registered_model_artifact`. Promotion still fails honestly one layer later: `candidate_status=execution_observe_only`, `closed_loop_branch_admission.status=fail_closed`, and `reason=exact_structural_branch_visible_but_not_ready_or_actionable`. Decision: `handoff`, not promotion; next useful unclaimed work is execution/admission readiness or a fresh analyze/execution-tree rewrite on this exact branch, not more path-preference repro.

- Handoff `2026-05-16 / codex-liquidity-pool-texture-dual-root-candidate-ranker-v1`: the repo-local liquidity-texture pack root now carries both rooted retained directional rows (`Range/XLF` and `Trend/TLT`) through the native admission export and into the external CatBoost/runtime leg on isolated `FACTOR_CANDIDATES` state under `support/docs/experiments/actionable-regime-confidence/runs/20260516T130500+0800-codex-liquidity-pool-texture-dual-root-candidate-ranker-v1/`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T130500+0800-codex-liquidity-pool-texture-dual-root-candidate-ranker-v1/summaries/terminal_decision_summary.md`. Real work executed: added the new rooted candidate `liquidity_pool_texture_tlt_trend_directional_30m_v1` to `support/examples/factor_candidate_packs/board-b-liquidity-pool-texture-v1`, then ran isolated direct-binary `factor-candidate-packs -> factor-candidate-admission-targets -> policy-training-status`, external `path_ranker_integration.py --allow-direct-fallback --register-runtime-artifact`, score apply, and post-apply `policy-training-status`. The resulting state proves root-aware downstream propagation farther than the earlier XLF-only pack bridge: `candidate_pack_count=3`, `rows=11`, `runtime_selection=enabled_registered_artifact_ready`, `runtime_source=registered_artifact`, `score_model_family=catboost`, and the post-apply rooted rows for both `Trend -> tlt -> ... -> liquidity_pool_texture_tlt_trend_directional_30m_v1@TLT` plus `Range -> xlf -> ... -> liquidity_pool_texture_xlf_range_directional_1h_v1@XLF` survive with `score_source_kind=external_model` and `raw_path_score=0.908128`. Decision: `handoff`, not promotion; the next useful unclaimed work is to bind this same rooted liquidity-texture pack into a live owner-symbol execution-tree surface, or to add a third rooted provider-backed directional row before claiming any deeper promotion readiness.

- Handoff `2026-05-17 / codex-order-block-yf-exact-lookback-fetch-v1`: exact `analyze-live` lookback parity now rules out raw yfinance OHLCV as the dominant wall-clock sink on the corrected order-block live lane. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T165700+0800-codex-order-block-yf-exact-lookback-fetch-v1/summaries/terminal_decision_summary.md`. Real work executed: reran the yfinance pre-analyze fetch matrix with the live source lookbacks themselves instead of the harness default `30d` window: `NQ=F 1d 420d=4.269s`, `4h 420d=3.496s`, `1h 120d=4.504s`, `15m 45d=4.373s`, `5m 21d=4.062s`, `1m 7d=4.636s`, and `QQQ 15m 45d=4.810s`. All seven legs succeeded, and total wall clock for the full exact-lookback matrix was only `30.150s`, still far below the corrected `analyze-live` timeout at `180.347s`. Decision: `handoff`, not promotion; the remaining blocker now sits even more clearly after raw OHLCV fetch, so the next useful unclaimed work is post-fetch runtime isolation rather than more yfinance candle timing.
- Handoff `2026-05-17 / codex-order-block-yf-options-summary-v1`: the exact `QQQ` yfinance `options.summary` leg on the corrected order-block live lane is also fast-fail rather than budget-heavy. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T165200+0800-codex-order-block-yf-options-summary-v1/summaries/terminal_decision_summary.md`. Real work executed: ran `market-data-harness --action fetch --role options_iv --provider options_iv=yfinance --symbol-spec options_iv=QQQ` on the same direct binary/runtime family used by the live lane. The result failed quickly in `4.538s` with `fetch_failed:yahoo crumb endpoint returned error`, which means the native yfinance options-summary step is presently a fast-fail auxiliary input, not a long hidden sink behind the `180.347s` live timeout. Decision: `handoff`, not promotion; together with the exact-lookback OHLCV packet, this shifts the truthful blocker further into the post-fetch `build_analyze_report/persist_*` path on the corrected yfinance order-block live lane.

- Handoff `2026-05-17 / codex-board-b-smt-strict-provider-refresh-v1`: reran the old strict same-event SMT trainer on the refreshed provider-backed SMT rows and found the next truthful blocker is schema-level outcome absence, not stale provider evidence. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T165500+0800-codex-board-b-smt-strict-provider-refresh-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the prior strict SMT trainer/assert scripts into a fresh run root, repointed them at `20260516T091500.../materials/smt_provider_observation_rows.csv`, aligned the local branch path to `Transition -> CrossMarketSmt -> same_event_confirmation_failure -> smt_relationship_resolver_same_event_confirmation_failure_v1`, then reran `py_compile -> train -> assert`. `py_compile` passed, but the actual trainer failed immediately with `KeyError: 'forward_return'`. The source schema audit proves why: the refreshed provider-backed rows still have confirmation-only fields and omit both `forward_return` and `outcome_hit` (`checks/source_schema_audit.json` shows `has_forward_return=false`, `has_outcome_hit=false`). Decision: `handoff`, not promotion; the next useful unclaimed work is an outcome-labeling bridge for provider-backed SMT rows, not another provider refresh or another unchanged strict-trainer rerun.

- Handoff `2026-05-17 / codex-order-block-yf-fetch-breakdown-v1`: raw yfinance OHLCV is no longer a plausible main explanation for the corrected order-block live-chain timeout. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T163300+0800-codex-order-block-yf-fetch-breakdown-v1/summaries/terminal_decision_summary.md`. Real work executed: measured the exact pre-analyze yfinance fetch legs for the corrected lane using `market-data-harness` on `NQ=F` `1d/4h/1h/15m/5m/1m` plus `QQQ 15m`. Six of seven legs returned quickly (`NQ=F 1d=3.768s`, `4h=2.811s`, `1h=3.251s`, `15m=4.912s`, `5m=7.271s`, `QQQ 15m=5.199s`), for only `27.212s` total successful fetch cost. The only fetch failure was `NQ=F 1m`, and it failed fast in `3.698s` with `fetch_failed:yahoo chart returned error for 'NQ=F'`. Total wall clock for the full seven-leg matrix was `30.910s`, far below the corrected `analyze-live` stall at `180.347s`. Decision: `handoff`, not promotion; next useful unclaimed work is to isolate the options-summary leg and then debug the post-fetch `build_analyze_report/persist_*` path on this corrected yfinance order-block lane, while treating `NQ=F 1m` as a separate fast-fail provider gap.

- Handoff `2026-05-16 / codex-board-b-liquidity-pool-texture-ibkr-tlt-directional-split-v1`: a second truthful retained observation row now exists for `liquidity_pool_texture`, this time on a distinct `IBKR TLT` `Trend` axis under `support/docs/experiments/actionable-regime-confidence/runs/20260516T124827+0800-codex-board-b-liquidity-pool-texture-ibkr-tlt-directional-split-v1/`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T124827+0800-codex-board-b-liquidity-pool-texture-ibkr-tlt-directional-split-v1/summaries/terminal_decision_summary.md`. Real work executed: split the existing `IBKR TLT 1d/1h/30m` clean packet into `early` plus `96` held-out `30m` bars, reran `analyze` on the early window using the already-proven fresh current-source binary, and resolved the late holdout through `liquidity_pool_texture_observation_packet.py`. The resulting retained row is still fail-closed for promotion but materially expands the branch-local evidence set: `selected_direction=Bear`, `observation_outcome=win`, `exit_reason=take_profit_1`, `realized_r=0.666667`, `market_state_primary_regime=TrendExpansion`, `market_state_secondary_regime=BullTrendExhaustion`, `texture=jagged`, `touch_count=6`, and `quality_gate.downstream_allowed=false` with `single_provider_single_observation_not_ready_for_candidate_pack_or_downstream_admission`. Decision: `handoff`, not promotion; next useful unclaimed work is to attach a rooted AQ / entry-model bridge that consumes the now-two retained directional observation rows (`Range/XLF` plus `Trend/TLT`), or to add a third provider/root observation before any promotion claim.

- Handoff `2026-05-16 / codex-board-b-220646-ibkr-gateway-diagnosis-v1`: the remaining `IBKR` blocker for the `220646` quartet is no longer a real gateway/session stall; it was a narrower current-runtime fetcher issue, and the exact repeatable `ibkr-historical` recipe is now restored. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T124415+0800-codex-board-b-220646-ibkr-gateway-diagnosis-v1/summaries/terminal_decision_summary.md`. Real work executed: validated `setup status` and `setup enable` on `127.0.0.1:4002` under the known-good provider venv, proved direct `ib_async` connect/qualify/history success for `QQQ 1 hour 10 D TRADES` both with and without the `IbkrRateLimiter`, then isolated the real failure to eager top-level `pandas` import inside `support/scripts/auto_quant_external/fetch_external.py`. A localized lazy-import fix now lets the exact `ibkr-historical` CLI exit normally, and the fresh run writes `provider-probes/ibkr_qqq_1h.csv` with `160` rows covering `2026-05-04T08:00:00+00:00 -> 2026-05-15T23:00:00+00:00`. Decision: `handoff`, not promotion; the next useful unclaimed work is to reuse the `220646` downstream state and retest the exact rooted branch through `Pre-Bayes -> BBN -> CatBoost/path-ranker -> execution tree`, not to keep classifying the same provider ingress as an `IBKR` connectivity problem.
- Handoff `2026-05-17 / codex-board-b-kraken-exact-branch-bias-v1`: the copied Kraken owner state can now prefer the exact rooted branch downstream without touching shared code or active owner-symbol lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T162400+0800-codex-board-b-kraken-exact-branch-bias-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the prior isolated `DENSE_KLINE_BRANCH` state, created a branch-aware direct-model artifact with exact-branch-only categorical weights on `sub_sub_regime_or_profit_factor=dense_kline_upbar_reclaim_kraken_1m` and `profit_factor=dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1`, re-enabled runtime reuse, and re-ran `workflow-status --phase execution-candidate --output-format json` on the copied state. The fresh readback now selects the exact rooted Kraken path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` with `path_ranker_raw_score=0.600379693206094`, `path_prob_lower_bound=0.5503796932060939`, and runtime source `registered_model_artifact`, instead of the earlier generic structural scenario. Decision: `handoff`, not promotion; execution still remains `execution_observe_only` / `ready=false`, and this slice did not produce a fresh live owner replay or rewritten persisted execution-tree artifact. The next useful unclaimed work is to reproduce the same exact-branch preference on a live or history-backed owner replay, not to keep this result trapped as a copied-state runtime-only bias.
- Handoff `2026-05-16 / codex-board-b-kraken-repo-bin-parity-v1`: current-source phase-read parity on the copied Kraken dense-branch state is now restored on the newest repo-local binary, so the earlier stale provider-probe diagnosis is no longer the live blocker. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T124423+0800-codex-board-b-kraken-repo-bin-parity-v1/summaries/terminal_decision_summary.md`. Real work executed: attempted two isolated `/tmp` fresh builds but both hit `Blocking waiting for file lock on artifact directory`, then verified the repo-local direct binary is newer than `command_entry.rs` / `provider_catalog.rs` / `workflow_status.rs` and used it for the exact handed-off parity commands on copied state root `.../20260515T223200.../state_split_kraken_1m`. Both `provider-status --agent` (`10.221s`) and `--compact` (`8.107s`) succeeded, and `workflow-status --phase execution-candidate --output-format json` now completes on both no-refresh (`32.433s`) and refresh (`27.481s`) reads with exit `0`. The result is still negative for promotion, but the blocker has moved: runtime returns `candidate_status=execution_observe_only`, `execution_readiness=0.4612333809537747`, `review_status=observe`, and `path_ranker_runtime.status=enabled_no_matching_scores`, while the selected path remains generic `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1` rather than the exact rooted Kraken branch. Decision: `handoff`, not promotion; next useful unclaimed work is exact-branch ranking/preference repair on `DENSE_KLINE_BRANCH`, not more provider-gating parity.
- Handoff `2026-05-17 / codex-order-block-yf-required-options-reentry-v1`: the reduced `yfinance NQ=F 1h` order-block current-chain probe is now truthfully re-run after closing the immediate prerequisite miss from the earlier `160500` slice. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T161800+0800-codex-order-block-yf-required-options-reentry-v1/summaries/terminal_decision_summary.md`. Real work executed: reran `analyze-live -> workflow-status -> pre-bayes-status -> policy-training-status` on a new state root using the same reduced provider mix but with the required `--options-symbol QQQ` restored and the volatility-proxy leg still omitted. This proves the old reduced-chain timeout reading was not authoritative: `20260517T160500...` failed in `0.792s` only because `--options-symbol` was missing. On the corrected run, `analyze-live` no longer fails fast and instead times out after `180.347s`, while `workflow-status --refresh --agent` now returns quickly in `1.243s` with a truthful bootstrap payload: `blocking_status=no_workflow_state`, `latest_phase=no_workflow_state`, `latest_analyze=null`, and `latest_execution_candidate=null`. `pre-bayes-status` and `policy-training-status` still remain empty (`analyze_runs=0`, structural target export missing), so the remaining blocker is still first durable workflow/analyze state materialization on the real yfinance order-block live lane, not the omitted volatility-proxy leg. Decision: `handoff`, not promotion; next useful unclaimed work is direct instrumentation of this corrected live yfinance bootstrap/analyze path or a bounded parity check against the older `ORDER_BLOCK_NQ_YF_CHAIN` stall, not another provider-widening replay.
- Handoff `2026-05-16 / codex-liquidity-pool-texture-xlf-pack-bridge-v1`: the new rooted XLF directional retained row now survives through the repo-local `liquidity_pool_texture` candidate-pack and admission surfaces under `support/docs/experiments/actionable-regime-confidence/runs/20260516T124545+0800-codex-liquidity-pool-texture-xlf-pack-bridge-v1/`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T124545+0800-codex-liquidity-pool-texture-xlf-pack-bridge-v1/summaries/terminal_decision_summary.md`. Real work executed: added a distinct candidate `liquidity_pool_texture_xlf_range_directional_1h_v1` under the existing `support/examples/factor_candidate_packs/board-b-liquidity-pool-texture-v1` root, sourced from the fresh XLF retained packet, then ran direct-binary `factor-candidate-packs -> factor-candidate-admission-targets -> policy-training-status -> artifact-status` on isolated repo-local state. The native loop now sees two liquidity-texture candidates instead of one (`candidate_pack_count=2`): the old observation-only packet plus the new directional XLF pack (`aggregate_trade_count=1`, `aggregate_label=anecdotal`, `transfer_status=single_market_only`). Rooted branch fields survive admission as first-class data for both `AGGREGATE` and `XLF` rows (`Range -> xlf -> board_b_liquidity:liquidity pool texture directional retained bridge:1h -> liquidity_pool_texture_xlf_range_directional_1h_v1@XLF`), but the gate still fail-closes exactly where it should: `rows=9`, `mature_rows=0`, `training_weight_rows=0`, `runtime_selection=disabled`, `trainer_status=present_validation_insufficient`, `promotion=blocked_until_pre_bayes_bbn_catboost_execution_tree_gates_pass`. Decision: `handoff`, not promotion; next useful unclaimed work is to add more directional rooted rows or an AQ / entry-model bridge so this factor moves beyond single-market anecdotal density before any downstream admission claim.
- Handoff `2026-05-16 / codex-board-b-liquidity-pool-texture-xlf-fresh-binary-rerun-v1`: the stale-binary blocker on the frozen XLF `liquidity_pool_texture` split is now cleared, and the held-out late window produced the first truthful directional retained outcome row for this lane under `support/docs/experiments/actionable-regime-confidence/runs/20260516T123521+0800-codex-board-b-liquidity-pool-texture-xlf-fresh-binary-rerun-v1/`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T123521+0800-codex-board-b-liquidity-pool-texture-xlf-fresh-binary-rerun-v1/summaries/terminal_decision_summary.md`. Real work executed: built an isolated fresh current-source binary in `/tmp/ict-engine-target-lpt-xlf-rerun`, replayed the exact frozen `1w/1d/1h early` XLF analyze path (`01_analyze_early.json` now `231647` bytes; `workflow_snapshot.json`, `execution_candidate.json`, and `execution_tree_trace.json` all materialized in `8.57s`), then ran `liquidity_pool_texture_observation_packet.py` on the held-out `1h late` candles. The resulting retained packet is still fail-closed for promotion, but it is materially stronger than the earlier observation-only packet: `selected_direction=Bear`, `observation_outcome=win`, `exit_reason=take_profit_1`, `realized_r=0.666667`, `market_state_primary_regime=RangeConsolidation`, `market_state_secondary_regime=WideRange`, `texture=jagged`, `touch_count=10`, `clean_sweep_likelihood=0.55`, and `quality_gate.downstream_allowed=false` with `single_provider_single_observation_not_ready_for_candidate_pack_or_downstream_admission`. Decision: `handoff`, not promotion; next useful unclaimed work is to repeat the same retained directional bridge on additional frozen symbols/root regimes or attach a rooted AQ / entry-model bridge once more than one comparable directional observation exists.
- Handoff `2026-05-16 / codex-board-b-runtime-factor-parity-audit-v1`: completed a read-only runtime-parity audit for the operator-priority quartet under `support/docs/experiments/actionable-regime-confidence/runs/20260516T123215+0800-codex-board-b-runtime-factor-parity-audit-v1/` without taking over the active liquidity XLF rerun, SMT packs, or order-block ingress work. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T123215+0800-codex-board-b-runtime-factor-parity-audit-v1/summaries/terminal_decision_summary.md`. Real evidence found a sharp surface gap rather than a new profitability result: `order_block_variant_classifier` is the only quartet member already promoted into both `analyze` output and persisted state (`analyze_runs.json` / `workflow_snapshot.json`); `liquidity_pool_texture` is rich in `analyze` output but not persisted; `smt_relationship_resolver` is only partially surfaced at runtime and does not expose the full resolver metadata or persisted state; and `fvg_ifvg_lifecycle` still collapses to `open_fvgs` plus nearest-gap levels even though detector code exists. Decision: `handoff`, not promotion; the next useful unclaimed work is `board-b-fvg-ifvg-runtime-surface-promotion-v1`, a runtime-surface/persistence slice distinct from the already dropped FVG profitability packet.
- Handoff `2026-05-17 / codex-liquidity-pool-texture-xlf-directional-split-v1`: a real frozen XLF directional split was created for `liquidity_pool_texture`, but the stale local binary stalled after MECE recovery before any analyze JSON or execution candidate materialized. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T153500+0800-codex-liquidity-pool-texture-xlf-directional-split-v1/summaries/terminal_decision_summary.md`. Real work executed: reused the frozen `yfinance XLF` weekly-context artifacts, split them into `1w/1d/1h early` plus `1h late` windows, then ran the repo-local direct binary on the early slice under a new isolated state root. The binary is confirmed stale relative to the runtime-path fixes (`.local-artifacts/cargo-target/debug/ict-engine` mtime `2026-05-15 20:53:54 +0800`, while `provider_catalog.rs` and `command_entry.rs` are newer on `2026-05-16`), and the observed behavior matches that stale-binary diagnosis: `command-output/01_analyze_early.json` stayed `0` bytes, `checks/01_analyze_early.time` never completed, and the state root emitted only `artifact_ledger.json` plus `mece_recovery_artifact.json` before idling. Decision: `handoff`, not promotion; next useful unclaimed work is to rerun this exact frozen XLF split on a fresh current-source binary, or if it still stalls, diagnose the local-data post-MECE analyze path directly instead of widening to new factors or providers.
- Handoff `2026-05-17 / codex-family-d-rebuilt-binary-verification-v1`: completed the pending rebuilt-binary parity check for the exact Family D `workflow-status --phase execution-candidate` read on `FAMILY_D_LIQUIDITY_SWEEP_035600`, and the result is negative. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T120107+0800-codex-family-d-rebuilt-binary-verification-v1/summaries/terminal_decision_summary.md`. Real work executed: reused the exact fresh Family D state root, confirmed in source/tests that phase-specific reads without an explicit profile should skip provider surface, then ran the exact parity command on a newer isolated binary at `/Users/thrill3r/.config/aegis/worktrees/ict-engine/board-a-bundle-gate-fix-v1/.local-artifacts/cargo-target/debug/ict-engine`. The new run still timed out with no stdout/stderr after `90.399s`, versus the earlier `45.11s` timeout baseline, so provider-surface parity does not explain the live stall by itself. Decision: `handoff`, not promotion; next useful unclaimed work is direct instrumentation of the deeper workflow/runtime stall in the Family D phase read or snapshot-loading path, not another provider-surface-only rebuild proof.
- Handoff `2026-05-17 / codex-board-b-kraken-exact-branch-direct-model-reuse-v1`: isolated downstream runtime reuse on the copied Kraken owner-state now materializes current candidate-set scores, but the exact rooted Kraken profit branch still loses to the generic structural scenario path. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T150500+0800-codex-board-b-kraken-exact-branch-direct-model-reuse-v1/summaries/terminal_decision_summary.md`. Real work executed: created a fresh `/tmp` claim, cloned `.../20260515T223200.../state_split_kraken_1m` into a new run root, generated a repo-native direct-model artifact from the cloned `structural_path_ranking_target_history.csv`, registered it with `register-structural-path-ranking-trainer-artifact`, enabled runtime reuse, and reran cloned owner-symbol `policy-training-status` plus `workflow-status` phase readbacks. Verified improvement: `runtime_selection` moved from `enabled_no_matching_scores` to `enabled_registered_model_ready`, `runtime_source=registered_model_artifact`, and `runtime_matches=3`; `workflow-status --phase execution-candidate --output-format json` now returns a real payload with `path_ranker_runtime.status=using_registered_model_artifact`. Remaining blocker: the selected path is still the generic scenario `path:scenario:DENSE_KLINE_BRANCH:belief_regime_node:trend:trend_follow_through:primary` (`raw_score=0.580735`) rather than the exact Kraken branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` (`direct_model_score=0.526736`), while entry-model training remains `matched_rows=0`. Decision: `handoff`, not promotion; next useful unclaimed work is exact-branch ranking/preference repair on `DENSE_KLINE_BRANCH`, not more provider probing or branch-survival reproof.
- Handoff `2026-05-17 / codex-board-b-kraken-exact-branch-preference-repair-v1`: the copied Kraken owner-state now prefers the exact rooted profit branch once runtime is fed a branch-aware direct-model artifact with an absolute artifact URI, but execution still remains fail-closed at `observe`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T150500+0800-codex-board-b-kraken-exact-branch-direct-model-reuse-v1/summaries/branch_aware_preference_repair_terminal_summary.md`. Real work executed: wrote a branch-aware `weighted_feature_sum_v1` artifact that kept the baseline score weight and added categorical bonuses for `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1`, verified the intended score surface (`exact=0.642687 > scenario=0.580735 > generic=0.546754`), discovered that a repo-relative artifact URI makes runtime invalid (`runtime_selection=enabled_registered_model_invalid`, `runtime_matches=0`), then re-registered the same artifact with an absolute path and re-enabled runtime. Verified improvement: `policy-training-status` returned `enabled_registered_model_ready runtime_matches=3`, and `workflow-status --phase execution-candidate --output-format json` now selects the exact rooted Kraken branch with `path_ranker_raw_score=0.6426873989187469`, `path_ranker_runtime_source=registered_model_artifact`, and `path_ranker_runtime.status=using_registered_model_artifact`. Remaining blocker: execution still ends at `candidate_status=execution_observe_only`, `execution_gate_status=execution_observe_only`, `review_status=observe`, and `ready=false`; entry-model training also remains `matched_rows=0`. Decision: `handoff`, not promotion; next useful unclaimed work is exact-branch execution-readiness diagnosis on this copied Kraken owner-state, not more ranker-preference forcing.
- Handoff `2026-05-17 / codex-board-b-kraken-analyze-bundle-parity-v1`: the copied Kraken owner-state can prefer the exact rooted branch at the isolated workflow/runtime layer, but a fresh current-code `analyze` rebuild still feeds the execution tree a scenario-led live bundle instead of preserving that exact branch. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T150500+0800-codex-board-b-kraken-exact-branch-direct-model-reuse-v1/summaries/analyze_bundle_parity_terminal_summary.md`. Real work executed: re-read the branch-aware runtime probe, ran fresh `pre-bayes-status --refresh`, `analyze --demo`, and `workflow-status --phase execution-candidate` on the same copied state, then compared the updated `execution_tree_trace.json` plus `workflow_snapshot.json` against the exported structural target files. Verified split: before fresh analyze, the absolute-path branch-aware probe selected the exact branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` with `path_ranker_raw_score=0.6426873989187469`; after fresh analyze, `workflow-status --phase execution-candidate` rebuilt a new live `candidate_set_id=structural-candidates:DENSE_KLINE_BRANCH:a421db06cf3dd028` with `candidate_set_size=2` and selected the generic scenario `path:scenario:DENSE_KLINE_BRANCH:belief_regime_node:trend:trend_follow_through:primary` (`path_ranker_raw_score=0.5826627405890472`), while the refreshed `execution_tree_trace.json` also consumed that scenario path under `registered_model_artifact/weighted_feature_sum_v1` and stopped at `decision_hint=execution_guarded_due_to_low_remaining_regime_duration`. Remaining blocker: analyze-time live candidate-path generation / structural bundle parity, not provider freshness, history retention, or ranker-preference forcing. Decision: `handoff`, not promotion; next useful unclaimed work is to inspect analyze-time candidate/bundle assembly for why the exact rooted Kraken branch drops out of the fresh live candidate set before execution-tree consumption.

- Handoff `2026-05-17 / codex-order-block-tvr-current-chain-v1`: ran the exact rooted OB mitigation branch on a fresh `TradingViewRemix QQQ 1h` current lane under `support/docs/experiments/actionable-regime-confidence/runs/20260517T122500+0800-codex-order-block-tvr-current-chain-v1/` after the provider-ingress recheck reopened direct TVR OHLCV. The chain still stopped before durable downstream state, but now with a fast truthful blocker instead of a vague hang: `analyze-live` failed in `4.371s` with `tradingview MCP call 'get_ohlcv' returned error` caused by HTTP `429 Too Many Requests`, `workflow-status --refresh --agent` still timed out at `60.23s`, and both `pre-bayes-status` plus `policy-training-status` returned empty/unready state (`analyze_runs=0`, structural target export missing, runtime selection disabled). Decision: `handoff`, not promotion; next useful unclaimed work is a deliberate TVR backoff/retry plan or a deeper downstream continuation on the already-proven `yfinance NQ=F 1h` current lane when the goal is state materialization rather than TVR parity.
- Handoff `2026-05-17 / codex-order-block-provider-ingress-audit-v1`: refreshed the richer-provider ingress matrix for rooted pack `Transition -> OrderBlockVariant -> ob_mitigation_breaker_rejection -> order_block_variant_classifier_v1` under `support/docs/experiments/actionable-regime-confidence/runs/20260517T120122+0800-codex-order-block-provider-ingress-audit-v1/` without taking over Family D or older OB roots. New truthful result: `TradingViewRemix` fetch is open again on the shared direct binary for `NASDAQ:QQQ 1h` (`market-data-harness` exited `0` in `5.184s` and returned real candles), but status surfaces still lag (`provider-status --provider tradingview_mcp --agent` timed out at `35.138s`). `IBKR` remained provider-blocked even after prerequisites improved (`redis/ib_async/pandas` importable in the provider venv and local port `4002` reachable): `provider-status --provider ibkr --agent` still timed out at `35.253s`, and the exact `QQQ 1h` request JSON fetch still timed out at `95.228s`. `kraken_public` provider-status also timed out and this packet did not produce fresh confirmed Kraken OHLCV. Decision: `handoff`, not promotion; next useful unclaimed work is a current-chain continuation on the reopened `TradingViewRemix` leg or a separate exact-branch downstream continuation on the proven `yfinance NQ=F 1h` lane, not another generic ingress recheck.
- Handoff `2026-05-17 / codex-board-b-order-block-candidate-ranker-propagation-v1`: the retained rooted OB mitigation pack now survives one more real downstream layer on a new isolated admission-state axis without colliding with the older `ORDER_BLOCK_NQ_YF_CHAIN`, Family D, or Kraken lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T145500+0800-codex-board-b-order-block-candidate-ranker-propagation-v1/summaries/terminal_decision_summary.md`. Real work executed: direct-binary `factor-candidate-packs -> factor-candidate-admission-targets -> policy-training-status`, then external `path_ranker_integration.py --allow-direct-fallback --register-runtime-artifact`, then `apply-structural-path-ranking-external-scores` back into the same `FACTOR_CANDIDATES` state. The rooted branch fields remain first-class through CatBoost/runtime: post-apply `policy-training-status` now reports `runtime_selection=enabled_registered_artifact_ready`, `runtime_source=registered_artifact`, `score_model_family=catboost`, `runtime_matches=41`, while the sample `yfinance/SPY` row still carries full branch keys and now has `raw_path_score=0.9630`, `calibrated_path_prob=0.7568`, `path_prob_lower_bound=0.6426`, and `execution_gate_status=pass`. Decision: `handoff`; this proves branch propagation through the CatBoost/runtime leg only. Next useful unclaimed work is to bind this same rooted pack into a live owner-symbol/execution-tree surface or a distinct current provider-backed owner replay, not to rerun the old `ORDER_BLOCK_NQ_YF_CHAIN` unchanged.
- Handoff `2026-05-16 / codex-order-block-yf-reduced-options-fixed-v1`: the distinct reduced yfinance owner replay now proves the fast `options_symbol` contract error was only superficial. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T125000+0800-codex-order-block-yf-reduced-options-fixed-v1/summaries/terminal_decision_summary.md`. Real work executed: created a fresh `/tmp` claim, reran direct-binary `analyze-live` on `ORDER_BLOCK_NQ_YF_REDUCED` with `--options-symbol QQQ --options-volatility-proxy-symbol ^VIX`, let it run past the earlier timeout class, then interrupted it after `03:33` once the state root still contained only its top-level directory. Post-run readbacks on the same root stayed bootstrap-only: `workflow-status` reports `blocking_truth.status=insufficient_state` / `reason=no workflow phase snapshots available`, `pre-bayes-status` is entirely null, and `policy-training-status` remains `analyze_runs=0`, `inventory=missing`, structural target export missing, runtime disabled. Decision: `handoff`, not promotion; next useful unclaimed work is pre-snapshot materialization diagnosis on this exact reduced yfinance lane or parity comparison against `ORDER_BLOCK_NQ_YF_CHAIN` on a fresh current-source binary, not more options-surface replay.
- Handoff `2026-05-16 / codex-order-block-yf-current-source-parity-v1`: the fresh-current-source parity slice now shows the reduced and chain yfinance owner symbols stall in the same silent startup band, but not with identical early artifacts. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T125500+0800-codex-order-block-yf-current-source-parity-v1/summaries/terminal_decision_summary.md`. Real work executed: used the newest repo-local binary and ran two fresh `45s` bounded `analyze-live` probes with identical `NQ=F / QQQ / QQQ / ^VIX / yfinance` inputs, once for `ORDER_BLOCK_NQ_YF_REDUCED` and once for `ORDER_BLOCK_NQ_YF_CHAIN`. Both timed out with empty stdout/stderr, so current-source parity does not eliminate the live startup/materialization stall. The new distinction is filesystem-level: the reduced lane wrote `ORDER_BLOCK_NQ_YF_REDUCED/workflow_snapshot.json` inside the bounded window, while the chain lane wrote nothing beyond its empty root. Both `workflow-status` readbacks still fail closed to bootstrap-only `insufficient_state`. Decision: `handoff`, not promotion; next useful unclaimed work is pre-snapshot startup instrumentation on the reduced lane first, because it is now the earliest owner symbol that writes any durable file on the fresh current-source binary.
- Handoff `2026-05-16 / codex-order-block-yf-startup-component-audit-v1`: component-level startup probes now rule out the obvious yfinance fetch surfaces as the main live blocker on the reduced rooted order-block lane. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T130500+0800-codex-order-block-yf-startup-component-audit-v1/summaries/terminal_decision_summary.md`. Real work executed: timed repo-native yfinance OHLCV fetch (`NQ=F + QQQ = 3.683s`), timed repo-native options-summary fetch (`QQQ` via fallback source `yfinance:volatility_proxy:^VIX = 4.735s`), and timed the quote fallback surfaces used by `fetch_quote(...)` (`Yahoo v7 quote = fast 401`, `Yahoo chart quote = fast 200`, `Barchart pages = about 2s`, `OpenCLI status = 502`, `bb-browser = bounded hang`). A detached real `analyze-live` process on fresh state `/tmp/ict-engine-order-block-yf-sample-v3` then stayed alive past `25s` and `48s` with `stdout=0`, `stderr=0`, no state files beyond the top-level directory, and no active TCP sockets observed via `lsof`. Decision: `handoff`, not promotion; next useful unclaimed work is instrumentation of the post-fetch / pre-persist startup path on `ORDER_BLOCK_NQ_YF_REDUCED`, not another generic provider replay.

- Handoff `2026-05-17 / codex-family-d-rebuilt-binary-verify-v1`: exact rebuilt-binary proof for the Family D provider-surface skip is still pending, and current host contention made the preserved state look worse rather than better. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T140000+0800-codex-family-d-rebuilt-binary-verify-v1/summaries/terminal_decision_summary.md`. Real work executed: created a fresh `/tmp` claim, reused the exact preserved Family D downstream state, reran the exact old-binary `workflow-status --phase execution-candidate` read as drift control, and attempted an isolated rebuild under `/tmp/ict-engine-family-d-rebuild-target`. The old binary timed out after `95.268s` with no payload, and the isolated rebuild was stopped after bounded waits because the target grew to `659M` without producing a runnable `debug/ict-engine` binary. Decision: `handoff`, not promotion; next useful unclaimed work is still rebuilt-binary verification for this exact rooted state, but only on a clean single-owner compile lane or clean isolated export.

- Handoff `2026-05-16 / codex-family-d-rebuilt-binary-phase-read-v1`: a tighter Family D artifact readback was preserved under `support/docs/experiments/actionable-regime-confidence/runs/20260516T120128+0800-codex-family-d-rebuilt-binary-phase-read-v1/` on the exact `FAMILY_D_LIQUIDITY_SWEEP_035600` state root without taking over Board A or rewriting runtime code. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T120128+0800-codex-family-d-rebuilt-binary-phase-read-v1/summaries/terminal_decision_summary.md`. Real artifact reads showed the Auto-Quant strategy library still serializes the rooted branch only as `expected_regime="TrendTransition -> LiquidityReclaim -> family_d_liquidity_sweep_reclaim_15m_wide_v1 -> liquidity_sweep_reclaim_long"` while the first-class branch fields stay blank (`main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor/regime_profit_branch_path=""`). The same downstream state root still collapses to bootstrap-only output: `workflow_snapshot.json` has null regime/pre-bayes/execution fields, and `structural_path_ranking_target.jsonl` still exports only `bootstrap_readiness` with `current_posterior=0.0`. A rebuilt-binary verification attempt was started for `workflow-status --phase execution-candidate`, but this packet did not finish that proof because the first Cargo run hit shared-artifact lock contention and the isolated-target compile was still in flight at handoff time. Decision: `handoff`, not promotion; the next useful unclaimed work is to finish the current-binary phase-read proof and then close the AQ-to-downstream branch-field serialization gap so rooted regime-factor fields survive as first-class data instead of free-text only.

- Handoff `2026-05-16 / codex-220646-provider-quartet-refresh-v2`: the fresh current quartet for historical seed `220646` now has live `TradingViewRemix` ingress again, but the repeatable current `IBKR` recipe still hang-times out before it yields a CSV, and the stall survives both `uv`-supplied deps and the known-good provider venv. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T121139+0800-codex-220646-provider-quartet-refresh-v2/summaries/terminal_decision_summary.md`. Real work executed: fresh repo-local `yfinance QQQ 1h` and `Kraken PF_XBTUSD 1h` fetches into isolated `/tmp` state, direct JSON-RPC `TradingViewRemix` `tools/list` and `get_ohlcv(NASDAQ:QQQ, 60)` probes using the locally configured MCP path, local socket reachability checks for Redis and IB Gateway, a bounded `uv` child-env `ibkr-historical` retry with `redis` and `ib_async` explicitly supplied, a bounded `uv` `support.scripts.ibkr_bridge.account_prober --port 4002`, and a second bounded `support.scripts.ibkr_bridge.account_prober --port 4002` through `/Users/thrill3r/.venvs/ict-engine-provider-py313/bin/python` after separately confirming that venv can directly `import redis` and `import ib_async`. Result: `yfinance` returned `43` rows, `Kraken` returned `193` rows, `TradingViewRemix` returned HTTP `200` with `37` tools plus `success=true count=300` for `QQQ 1h`, while `IBKR` still failed to materialize output (`bounded_retry_rc=124`, `stdout_bytes=0`, `output_csv_present=false`) even though `127.0.0.1:6379` and `127.0.0.1:4002` were open; the metadata-only account probe timed out both under `uv` and under the known-good provider venv. Decision: `handoff`; this clears the old external `TradingViewRemix` throttle blocker for `220646` but now sharpens the remaining quartet gap to a gateway/session/API-level `IBKR` stall before any fresh rooted downstream replay.
- Handoff `2026-05-16 / codex-order-block-pack-resolver-parity-v1`: the generic Python candidate-pack resolver now matches the retained positive order-block pack already visible through the native Board B loop. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T120335+0800-codex-order-block-pack-resolver-parity-v1/summaries/terminal_decision_summary.md`. Real work executed: reproduced the stale generic mismatch (`python3 support/scripts/research/factor_candidate_resolver.py --repo-root . --list-buildable --output-format human` returned `buildable_count=7 candidate_count=13` and the focused generic resolver test failed `13 != 14`), confirmed the same isolated `/tmp` state already showed native truth (`factor-candidate-packs candidate_pack_count=8`, `factor-candidate-admission-targets rows=41`, `policy-training-status inventory=ready count=8`), then fixed the actual gap by adding `order_block_mitigation_block_1h_v1` to `config/factor_candidate_harness_presets.json`. Verification after the fix: generic resolver CLI now returns `buildable_count=8 candidate_count=14` and includes `order_block_mitigation_block_1h_v1`; focused resolver tests passed; full `test_factor_candidate_resolver` passed `9/9`; `test_factor_candidate_pack` passed `8/8`. Decision: `handoff`; this closes resolver/spec parity only, while deeper provider/current-chain order-block lanes remain separate ownership slices.
- Handoff `2026-05-17 / codex-board-b-kraken-exact-branch-recommendation-readback-v1`: exact owner symbol `DENSE_KLINE_BRANCH` is readable under fail-closed provider probes, but runtime still emits the generic dense candidate instead of the exact Kraken split. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T120633+0800-codex-board-b-kraken-exact-branch-recommendation-readback-v1/summaries/terminal_decision_summary.md`. Real commands: `provider-status --domain market_data --compact`, `pre-bayes-status --symbol DENSE_KLINE_BRANCH --state-dir support/docs/experiments/actionable-regime-confidence/runs/20260515T223200+0800-codex-yf-kraken-realtrade-replay-matrix-v1/state_split_kraken_1m --output-format json`, `workflow-status --phase pre-bayes-policy --output-format json`, `workflow-status --phase execution-candidate --output-format json`, `workflow-status --phase execution-candidate-history --output-format json`, `policy-training-status --output-format agent`. Result: `provider-status` returned `market_data:6/8 ready`; `workflow-status --phase execution-candidate` returned `execution_observe_only` with `path_ranker_runtime.status=enabled_no_matching_scores` and `candidate_set_match_count=0`; `policy-training-status` reported `runtime_selection=enabled_no_matching_scores`, `runtime_mode=candidate_set_only`, `runtime_source=none`, `score_model_family=unknown`, `score_source=unknown`, `runtime_matches=0`. Decision: `handoff`; next unclaimed idea is current-candidate-set score materialization or runtime-selection repair for the exact Kraken branch, not more provider-ingress probing.
- Handoff `2026-05-17 / codex-family-d-snapshot-materialization-diagnosis-v1`: the Family D blocker is now better classified as provider-surface and analyze-live startup starvation before the first real workflow snapshot materializes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T041200+0800-codex-family-d-snapshot-materialization-diagnosis-v1/summaries/terminal_decision_summary.md`. Real work executed: bounded current-binary `provider-status --provider yfinance|ibkr|tradingview_mcp --agent` probes, direct Yahoo endpoint timings for the exact `analyze-live` symbol set, direct Kraken/IBKR/TradingViewRemix spot checks, and source readback on `src/application/orchestration/command_entry.rs`. The current binary still times out on all three provider-status probes at about `26s`, direct Yahoo chart legs are fast (`NQ=F 1h=1.219s`, `QQQ 15m=0.918s`, `^VIX 1d=0.412s`) while `QQQ` options fail immediately with `401`, and the old-binary `workflow-status --phase execution-candidate` read on the fresh Family D state still returns only the bootstrap observe surface after `79.001s`. A minimal source fix was applied so phase-specific workflow-status reads without explicit `--profile` skip eager `provider_status_agent_surface(...)`, but that rebuilt-binary proof is still pending. Decision: `handoff`, not promotion; the next useful unclaimed work is to finish rebuilt-binary verification for that provider-surface gate and then re-run the same Family D phase read before spending another slice on broader branch logic.
- Handoff `2026-05-16 / codex-order-block-nq-tvr-provider-extension-v1`: current-provider extension on the already-positive rooted OB mitigation pack stopped at provider ingress, but it produced one fresh truthful `NQ` leg instead of guesswork. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T094304+0800-codex-order-block-nq-tvr-provider-extension-v1/summaries/terminal_decision_summary.md`. Real work executed: direct-binary provider probes for `tradingview_mcp`, `yfinance NQ=F 1h`, prefixed-runtime `ibkr QQQ 1h`, and `kraken_public PF_XBTUSD 1h` on the exact pack-extension axis requested by the previous OB handoff. The usable leg today is `yfinance`: `market-data-harness` returned `496` hourly `NQ=F` rows covering `2026-04-16T01:00:00Z -> 2026-05-15T20:59:59Z`. The operator-priority richer legs are still blocked on this shared binary: `provider-status --provider tradingview_mcp --agent` timed out after `30.623s`, direct `TradingViewRemix` `NASDAQ:QQQ 1h` fetch failed with `fetch_failed:tradingview MCP call 'get_ohlcv' returned error`, prefixed `provider-status --provider ibkr --agent` timed out after `31.205s`, prefixed `IBKR QQQ 1h` fetch timed out after `61.064s`, and `kraken_public` shorthand still failed validation with `unsupported provider 'kraken_public'`. Decision: `handoff`, not promotion; next useful unclaimed work is either a fresh rooted-chain continuation on the now-proven `yfinance NQ=F 1h` leg or a focused provider-ingress repair for `TradingViewRemix` / `IBKR` before widening the same OB mitigation slice.

- Handoff `2026-05-16 / codex-order-block-nq-yf-current-chain-v1`: the follow-on current chain on the only proven provider leg for the rooted OB mitigation factor still dies before first durable workflow state, but it moves the blocker past provider ingress. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T100407+0800-codex-order-block-nq-yf-current-chain-v1/summaries/terminal_decision_summary.md`. Real work executed: on fresh state `/tmp/ict-engine-order-block-nq-yf-current-chain-v1.thS7br`, direct-binary `analyze-live --symbol ORDER_BLOCK_NQ_YF_CHAIN --futures-symbol NQ=F --spot-symbol QQQ --options-symbol QQQ --options-volatility-proxy-symbol ^VIX --spot-kind etf --futures-backend yfinance --aux-backend yfinance --state-dir ... --agent`, then `workflow-status --refresh --agent`, `pre-bayes-status --refresh --output-format json`, `policy-training-status --output-format agent`, and `export-structural-path-ranking-target`. The stop point is sharper than the provider-preflight packet: `analyze-live` timed out after `180.673s`, `workflow-status` timed out after `60.888s`, and `export-structural-path-ranking-target` timed out after `120.752s`, but `pre-bayes-status` still returned `RC=0` with every latest field `null` and `policy-training-status` returned `RC=0` with `analyze_runs=0`, `update_runs=0`, `factor candidate packs inventory=missing`, and structural target export missing. The only durable state file is `workflow_snapshot.json`, and it still says `blocking_truth.status=insufficient_state` / `reason=no workflow phase snapshots available` with `latest_analyze=null` and `latest_execution_candidate=null`. Decision: `handoff`, not promotion; next useful unclaimed work is direct diagnosis of why this exact current yfinance NQ chain never materializes its first workflow/analyze phase, not another provider-ingress replay.

- Blocked `2026-05-17 / codex-family-a-fvg-retrace-5m-current-chain-v1`: distinct current-window/downstream continuation for `family_a_fvg_retrace_5m_v1` on branch `TrendExpansion -> FVGReclaim -> family_a_fvg_retrace_5m_v1 -> nq_fvg_retrace_5m`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T042200+0800-codex-family-a-fvg-retrace-5m-current-chain-v1/summaries/terminal_decision_summary.md`. This slice did not reuse the active 1h FVG claim; it used a new ownership axis: a fresh `2024-01-03 -> 2025-12-31` AQ replay on the 5m variant plus native import/readback on isolated `/tmp` state. The real local AQ replay completed via `run_tomac_one.py` with `0` trades on `NQ/USD`, so the factor stayed flat on the recent window rather than reproducing the older 8Y negative packet verbatim. The native importer still accepted the packet after strategy-library normalization (`auto-quant-results-import n_ok=1`), which sharpens the blocker: `auto-quant-prior-init` skip-closed it with `trade_count=0` and `evidence_value_gate_passed=false`; `pre-bayes-status --refresh` returned `RC=0` but every latest field stayed `null`; `policy-training-status` reported `analyze_runs=0`, `matched_rows=0`, and missing structural-path export; `workflow_snapshot.json` stayed `stage_unavailable` / `insufficient_state` / `no workflow phase snapshots available`; bounded `workflow-status --refresh` and `analyze-live` with `yfinance` both timed out without producing workflow phases. Decision: `blocked`, not promotion; next useful unclaimed work is either a materially different provider/window/market basis for this 5m factor or a focused liveness diagnosis for the empty workflow-phase path after valid-but-flat AQ import.

- Handoff `2026-05-16 / codex-family-d-current-code-downstream-reentry-v1`: fresh current-code downstream re-entry on the exact rooted Family D branch moved the stop point one layer forward again. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T092700+0800-codex-family-d-current-code-downstream-reentry-v1/summaries/terminal_decision_summary.md`. Real work executed: imported the validated strategy library into a fresh durable state root, reran `auto-quant-prior-init`, then ran current-code `pre-bayes-status`, `policy-training-status`, bounded `workflow-status --phase execution-candidate`, `export-structural-path-ranking-target`, bounded `analyze-live`, and post-analyze readbacks with the provider py313 venv first on `PATH`. This changes the truthful blocker: `export-structural-path-ranking-target` no longer times out here and exited `0` after `111.532s`, but it still emitted only a single `bootstrap:no_workflow_state` row with empty `regime_profit_branch_path`, `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`, and `profit_factor` fields. `workflow-status --phase execution-candidate` still timed out after `45.305s`, bounded `analyze-live` still timed out after `180.673s`, `pre-bayes-status` remained empty before and after analyze, and `workflow_snapshot.json` still showed `latest_analyze=null` / `latest_execution_candidate=null` with `blocking_truth.reason=no workflow phase snapshots available`. Decision: `handoff`, not promotion; the next useful unclaimed work is branch-path-aware state seeding from the imported strategy library or direct diagnosis of why the first analyze snapshot never materializes on this imported rooted state, not another AQ import/prior-init replay.
- Handoff `2026-05-16 / codex-low-hazard-history-aware-current-scorer-v1`: current-code continuation on the already multi-provider low-hazard branch proved the exact rooted rows can still be mutated, but the score mutation still does not survive into execution-path consumer surfaces. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T095100+0800-codex-low-hazard-history-aware-current-scorer-v1/summaries/terminal_decision_summary.md`. Real work executed: reused the private copied state for `TrendExpansion -> SessionLiquidity -> dense_kline_low_hazard_reclaim -> low_hazard_reclaim_long_v1`, reran current-code `analyze --demo`, exported the current target, applied a history-aware external score file that lifted the exact low-hazard path above the generic current row, re-enabled runtime selection, reran `analyze --demo`, then re-read `pre-bayes-status`, `policy-training-status`, bounded `workflow-status`, and direct state artifacts. The rooted branch still survives through Pre-Bayes with `latest_gate_status=pass_hard`, canonical regime `trend`, and exact `regime_profit_branch_path` fields, and the current target still shows the exact branch rows with `raw_path_score=1.000000` and `0.750659` against the generic row `0.852687`. But the next current-code hop fails closed in a sharper way: post-score `policy-training-status` downgrades the runtime identity to `score_model_family=unknown score_source=unknown`, `latest_execution_candidate` falls back to `candidate_status=no_trade` with `review_reason=duplicate_execution_candidate_context` and null `path_ranker_*` fields, `execution_tree_trace.json` exposes no `path_ranker_*` or `selected_path_id`, and bounded direct `workflow-status --output-format json` timed out after `20.452s`. Decision: `handoff`, not promotion; the next useful unclaimed work is `candidate_set target rows -> runtime score identity -> execution candidate / execution tree materialization` for this exact low-hazard branch under current code, not AQ replay, provider acquisition, or the older adapter-only slice.
- Handoff `2026-05-17 / codex-order-block-mitigation-pack-v1`: the retained `order_block_variant_classifier` packet now has one real reusable positive Board B candidate pack instead of staying prose-only. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T044500+0800-codex-order-block-mitigation-pack-v1/summaries/terminal_decision_summary.md`. Fresh retained-row readback selected the positive `mitigation_block` sub-slice on rooted branch `Transition -> OrderBlockVariant -> ob_mitigation_breaker_rejection -> order_block_variant_classifier_v1`: `rows=945`, `realized_trades=342`, `wins=338`, `win_rate=0.9883`, `avg_realized_r=1.9622`, with provider subset preserved across `Binance`, `Bybit`, `IBKR`, `Kraken`, and `yfinance`. I extracted that slice into `support/examples/factor_candidate_packs/curated-auto-quant-v1/order_block_mitigation_block_1h_v1/` and pushed it through the native repo loop: direct-binary `factor-candidate-packs` now reports `candidate_pack_count=8` and includes `order_block_mitigation_block_1h_v1`, while `factor-candidate-admission-targets` on isolated state `.../FACTOR_CANDIDATES` writes `rows=41`, `mature_rows=35`, `calibrated_rows=35`, and `training_weight_rows=35`. The slice is still only `handoff`, not promotion: `workflow_snapshot.json` records the inventory/admission artifacts, but Python resolver `--list-buildable` still stays at `buildable_count=7`, and `policy-training-status` still fails to surface the same factor-pack inventory on this exact state root. Next useful unclaimed work is either resolver/spec parity for this new repo pack or one current-provider continuation that adds fresh `TradingViewRemix` or `NQ` evidence before attempting deeper Pre-Bayes / BBN / execution-tree continuation.
- Handoff `2026-05-16 / codex-order-block-tvr-current-chain-v1`: rooted `order_block_variant_classifier_v1` still survives the native admission and CatBoost/runtime rebuild on a fresh isolated state, but the live owner-symbol continuation is now blocked by concrete provider/runtime failures instead of branch-path loss. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T235900+0800-codex-order-block-tvr-current-chain-v1/summaries/terminal_decision_summary.md`. Real work executed: rebuilt the rooted `FACTOR_CANDIDATES` state under `/tmp/ict-engine-ob-tvr-current-chain-v1` (`factor-candidate-packs`, `factor-candidate-admission-targets`, `policy-training-status`, `path_ranker_integration.py`, `apply-structural-path-ranking-external-scores`, and post-apply `policy-training-status` all exited `0`), then tried two live owner-symbol replays. The reopened `TradingViewRemix` leg failed immediately on the first `analyze-live` call with `tradingview MCP call 'get_ohlcv' returned error` caused by HTTP `429 Too Many Requests`. A copied yfinance fallback state `/tmp/ict-engine-ob-yf-current-chain-v1` then timed out on `analyze-live --symbol NQ --futures-symbol NQ=F --spot-symbol QQQ --options-symbol QQQ --futures-backend yfinance --aux-backend yfinance` after `180.318s` with empty stdout/stderr and no fresh `NQ` live-state artifacts. Decision: `handoff`, not promotion; next useful unclaimed work is the same yfinance owner-symbol step on a fresh current-source binary, plus a later `TradingViewRemix` retry after the MCP rate-limit window clears.
- Handoff `2026-05-16 / codex-order-block-yf-fresh-build-liveness-v1`: the requested next step for the same rooted order-block yfinance replay is now blocked one layer earlier by fresh current-source build liveness. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T132227+0800-codex-order-block-yf-fresh-build-liveness-v1/summaries/terminal_decision_summary.md`. Real work executed: confirmed the repo-local direct binary remained stale versus current source, then started `CARGO_TARGET_DIR=/tmp/ict-engine-ob-yf-fresh-target cargo build --bin ict-engine` to prepare the honest fresh-binary rerun. The build reached `Compiling ict-engine v0.1.2` and kept writing to `debug/incremental/.../dep-graph.part.bin`, but by `2026-05-16 13:22:27 +0800` there was still no `/tmp/ict-engine-ob-yf-fresh-target/debug/ict-engine`, so the fresh yfinance owner-symbol replay never actually ran in this slice. Decision: `handoff`, not promotion; next useful unclaimed work is to finish or re-home that fresh build in a quieter isolated target/workspace and only then retry the same yfinance owner-symbol live command.
- Handoff `2026-05-16 / codex-dense-kline-ibkr-1m-exact-split-v1`: replayed a new provider/timeframe-specific dense exact split from real `IBKR QQQ 1m` trades into a copied dense state without touching active Kraken/220646/Family A/Family D lanes. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T091500+0800-codex-dense-kline-ibkr-1m-exact-split-v1/summaries/terminal_decision_summary.md`. Real work executed: built `1272` exact-split wire rows from `ibkr_qqq_1m_trades.csv`, ingested them natively with `auto-quant-ingest-real-trades`, and confirmed the exact rooted path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_ibkr_1m -> dense_kline_upbar_reclaim_ibkr_qqq_1m_v1` now appears as a first-class current target row plus feedback-history row in `structural_path_ranking_target.csv`; direct external CatBoost scoring then wrote `raw_path_score=0.175790` on the current exact row and `raw_path_score=0.746502 / calibrated_path_prob=0.383046 / execution_gate_status=observe` on the exact history row, and the copied state's runtime-selection file was refreshed to `reuse_mode=prefer_history`. The sharper stop point is no longer “analyze hangs”: real direct-binary `IBKR 15m/5m/1m` `analyze --human` does return on this copied state and refreshes `workflow_snapshot.json` to `blocking_truth.status=bridge_needs_confirmation`, with `pre_bayes_gate_status=pass_hard`, `pre_bayes_quality_score=0.820`, and `bridge gap 0.038 below confirmation threshold`; the true slow/blocked owner surface is `workflow-status`, whose sampled main thread still sits in `attach_workflow_opt_in_profile_refs -> provider_status_surface -> probe_public_fetch_python_runtime -> missing_public_fetch_python_modules -> std::process::Command::output -> poll`. Fresh execution-tree closure is still absent: `latest_structural_execution_candidate`, `path_ranker_summary`, and `execution_tree` remain empty in the refreshed snapshot, and `execution_tree_trace.json` did not turn into a new exact-split execution proof. Decision: `handoff`, not promotion; next useful unclaimed work is a bounded current-code owner slice that removes or bypasses this `workflow-status` provider probe tax on the same copied state, then re-reads `execution candidate / execution tree` surfaces, not another replay/scoring rerun.
- Handoff `2026-05-17 / codex-family-d-downstream-reentry-v1`: fresh isolated current-code downstream replay on the same rooted Family D branch tightens the blocker again. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T035600+0800-codex-family-d-downstream-reentry-v1/summaries/terminal_decision_summary.md`. Real work executed: imported the proven Family D strategy library into a brand-new state root, re-applied `auto-quant-prior-init`, re-read `pre-bayes-status` / `policy-training-status`, then ran bounded `workflow-status --phase execution-candidate`, `export-structural-path-ranking-target`, and `analyze-live`. The rooted branch still passes import and BBN prior application immediately, and `export-structural-path-ranking-target` is no longer hanging: it now exits `0` in `61.769s` and writes `structural_path_ranking_target.{csv,jsonl}` plus history files. But the exported target is still only a single `bootstrap_readiness` row (`rows=1`, `history_rows=1`, `mature_rows=0`), `workflow-status --phase execution-candidate` still times out after `45.11s`, `analyze-live` still times out after `150.23s`, `pre-bayes-status` stays empty before and after analyze, and `workflow_snapshot.json` still says `blocking_truth.status=insufficient_state` / `reason=no workflow phase snapshots available`. Decision: `handoff`, not promotion; the sharper next useful unclaimed work is diagnosing why this rooted branch never materializes its first analyze/workflow phase snapshot in the fresh state even though import, prior-init, and structural target export already succeed.
- Blocked `2026-05-16 / codex-board-b-killzone-displacement-zip-import-v1`: recovered archived `TomacNQ_KillzoneBreakoutDisplacement` FreqTrade output from the local Auto-Quant cache into a fresh native `ict-engine` state and proved a branch-local positive seed can survive import-level branch-path normalization, but the downstream runtime still stalls before workflow closure. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T092234+0800-codex-board-b-killzone-displacement-zip-import-v1/summaries/terminal_decision_summary.md`. Real work executed: converted `/Users/thrill3r/Auto-Quant/user_data/backtest_results/backtest-result-2026-05-08_23-11-35.zip` into a strict `strategy_library.json`, imported it natively for symbol `KILLZONE_DISP_GLD_ZIP_20260516`, and applied `auto-quant-prior-init` with positive BBN evidence (`trade_count=24`, `n_win=15`, `n_loss=9`, `evidence_value_gate_passed=true`, `bbn_log_loss_delta=0.2538`). The explicit branch fields survived into the imported library and persisted target files as `candidate_pack -> gld_usd -> family_a:session breakout displacement:1h -> family_a_killzone_displacement_pending_v1@GLD/USD`; `structural_path_ranking_target_summary.json` shows `rows=1` / `history_rows=1` with a single `bootstrap_readiness` row. The stop point is later than import/prior-init but earlier than usable workflow closure: `workflow_snapshot.json` still says `stage_unavailable` / `insufficient_state` / `no workflow phase snapshots available`, `pre-bayes-status` returned only null `latest_*` fields, `policy-training-status` stayed `analyze_runs=0`, `matched_rows=0`, runtime disabled, and bounded `workflow-status` plus `export-structural-path-ranking-target` probes both timed out. Decision: `blocked`, not profitability-negative; next useful unclaimed work is diagnosing why imported-library-only states with persisted target files still fail to materialize workflow phase snapshots, or running a fresh provider-backed analyze phase for this exact archived GLD branch.
- Handoff `2026-05-16 / codex-board-b-kraken-workflow-provider-probe-audit-v1`: the copied Kraken exact-state blocker is now better classified as an eager provider-status probe tax, not just a generic persisted-state workflow-status phase-construction hang. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T235500+0800-codex-board-b-kraken-workflow-provider-probe-audit-v1/summaries/terminal_decision_summary.md`. Fresh runtime repro on `.../20260515T223200.../state_split_kraken_1m` shows `pre-bayes-status --output-format json` still exits `0` in about `0.044s`, while `workflow-status --phase pre-bayes-policy --output-format json` and `workflow-status --phase bootstrap --output-format json` both exceed a bounded `25s` probe with empty stdout/stderr. Current source readback explains the timing shape: provider catalog assembly still uses `all_market_data_requirements()` and a configured `~/.ict-engine/tvremix_mcp.json` remote TradingViewRemix path can trigger up to three `20s` HTTP probes (`tools/list`, `get_ohlcv`, `get_option_expirations`) before workflow-status emits any phase JSON. Decision: `handoff`, not profitability-negative; next useful unclaimed work is a clean-owner slice that verifies direct-binary/source parity and then removes this provider-probe tax from generic workflow-status persisted-state reads before claiming deeper BBN/execution-tree blockage.
- Handoff `2026-05-17 / codex-family-d-run-tomac-instrumentation-v1`: fresh bounded instrumentation on the exact copied Family D workspace shows the repaired `NQ/USD 4h` branch is no longer blocked at AQ payload generation. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T034500+0800-codex-family-d-run-tomac-instrumentation-v1/summaries/terminal_decision_summary.md`. Real work executed: reused the copied workspace under `.../20260515T221500.../aq_workspace`, then ran fresh bounded probes against its warmed local `.venv`. The authoritative direct probe `.venv/bin/python -u run_tomac.py` exited `0` after `124.224s` and emitted the exact rooted Family D metrics again: `126` trades, `total_profit_pct=1.98`, `profit_factor=1.0528`, `sharpe=0.0766`, `win_rate_pct=41.2698%`, `max_drawdown_pct=-9.8189`. Supporting `-X importtime` telemetry hit the wrapper boundary but still reached live backtesting with both `NQ/USD 1h` and `NQ/USD 4h` loaded before the kill. Decision: `handoff`, not promotion; the old "run_tomac still does not emit a usable AQ payload" blocker is no longer current for this copied workspace, and the next useful unclaimed work is fresh current-code downstream re-entry on the same rooted branch (`auto-quant-results-import -> auto-quant-prior-init -> pre-bayes-status / policy-training-status / workflow-status / export-structural-path-ranking-target / analyze-live`), not more AQ startup repro.
- Handoff `2026-05-17 / codex-family-d-no-snapshot-diagnosis-v1`: the Family D first-snapshot blocker is now sharper than the earlier downstream re-entry packet and is no longer explainable only by live yfinance auxiliary/options fetches. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T163500+0800-codex-family-d-no-snapshot-diagnosis-v1/summaries/terminal_decision_summary.md`. Real work executed: copied the exact `035600` downstream state, replayed reduced live commands, then converted the real AQ workspace `NQ_USD-{1d,4h,1h}.feather` files into cleaned JSON with the warmed AQ `.venv` and reran native `analyze` on the copied state. The reduced live probes first proved contract strictness rather than a deep runtime hang (`futures-only` fast-fails with `still needs a spot symbol`; `futures+spot` fast-fails with `still needs an options symbol`). More importantly, the corrected provider-backed local-data replay also fails before any phase materializes: after timestamp repair it times out at `120.23s` with zero stdout/stderr and still writes no `analyze_runs.json`, `execution_candidate.json`, `execution_tree_trace.json`, `artifact_ledger.json`, or `mece_recovery_artifact.json`. Decision: `handoff`, not promotion; the sharper next useful unclaimed work is core analyze/workflow startup diagnosis on this imported state, not more AQ repro, not more import/prior-init proof, and not blaming the live auxiliary/options path alone.
- Handoff `2026-05-17 / codex-board-b-220702-branch-ingestion-parity-v1`: current-command parity confirms the old `220702` exact-branch fail-closed packet is still live, and it narrows the blocker to a concrete branch-symbol ingestion gap rather than generic downstream weakness. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T032200+0800-codex-board-b-220702-branch-ingestion-parity-v1/summaries/terminal_decision_summary.md`. Fresh direct-binary parity on the existing `SRC_ROOT_CARRY_LONG_220646` persisted state reproduced the same blocked `Pre-Bayes` shape (`pre_bayes_branch_path_gate=blocked_missing_consumed_pre_bayes_filter`, no policy/bridge/soft evidence), the same entry-model failure (`cisd_rb_long_v1.matched_rows=0`, `breaker_rb_long_v1.matched_rows=0`), and a still-non-usable workflow layer (`workflow-status --phase execution-candidate --output-format json` timed out after `45s`). The sharper blocker is visible in-state: both BBN training CSVs are header-only while `learning_state.json` still preserves exact structural branch feedback across `48` update rows, which means exact branch-symbol structural feedback is not materializing into any entry-model training rows for the `SRC_ROOT_CARRY_LONG_220646` symbol state. Decision: `handoff`, not promotion; next useful unclaimed work is the branch-symbol ingestion/mapping hop from structural feedback into non-empty entry-model training exports, not another provider replay.
- Handoff `2026-05-16 / codex-board-b-family-d-private-phase-parity-v1`: private copied-state parity recheck confirms the imported Family D rooted branch is still blocked before its first analyze/workflow snapshot materializes, but the blocker is narrower than a blanket provider failure. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T091500+0800-codex-board-b-family-d-private-phase-parity-v1/summaries/terminal_decision_summary.md`. Real work executed: copied `.../20260515T230000.../state_native` into `/tmp/ict-engine-board-b-familyd-diagnose-v1/state_native`, then reran the exact imported symbol `FAMILY_D_LIQUIDITY_SWEEP_230000` through current debug-binary readbacks. The copied state still returns fast on `pre-bayes-status --refresh --output-format json` (`rc=0`, `latest_gate_status=null`) and `policy-training-status --output-format json` (`rc=0`, `analyze_runs=0`, `update_runs=0`, `matched_rows=0`), while `workflow-status --agent`, `workflow-status --phase execution-candidate --output-format json`, and `export-structural-path-ranking-target` all timed out again without stdout/stderr. Live provider evidence in the same slice is split rather than dead: direct `market-data-harness` with `primary=yfinance` fetched `QQQ 1h` successfully and its embedded provider summary still showed `ibkr=ready` plus `tradingview_mcp=configured_runtime_unhealthy`, but `provider-status --provider yfinance --agent` and `provider-status --provider kraken --agent` both timed out on this binary. Decision: `handoff`, not promotion; next useful unclaimed work is the current-code workflow/export construction hang on imported copied state, not AQ replay, not Family D provider portability, and not Board A regime work.
- Handoff `2026-05-16 / codex-board-b-kraken-exact-export-refresh-readback-v1`: the retained copied Kraken dense-branch state no longer has a `learning_state -> target/history` branch-path gap, and the next fresh downstream stop point is now sharper than the old `223200` packet reported. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T011500+0800-codex-board-b-kraken-exact-export-refresh-readback-v1/summaries/terminal_decision_summary.md`. Re-checking the same copied state root `.../20260515T223200.../state_split_kraken_1m` after the earlier bounded export timeout shows `policy_training/structural_path_ranking_target{,_history}.jsonl` refreshed at `2026-05-15 23:06:27 +0800` and now contain the exact rooted path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` as both a `structural-candidates` row and `structural-feedback-history` row(s). Fresh direct-binary downstream readback also proves the slice reaches `pre-bayes-status --refresh`: `latest_gate_status=pass_neutralized`, active regime `trend`, confidence `0.9787037037037036`, and `bridge_gap=0.01999432099614995`. After removing the lingering workflow-status processes started by this slice, a clean persisted-state recheck still showed the same asymmetry at a broader workflow layer: `pre-bayes-status --output-format json` returns `RC=0`, while `workflow-status --phase execution-candidate --output-format json`, `workflow-status --phase execution-candidate-history --output-format json`, and `workflow-status --phase pre-bayes-policy --output-format json` all still time out. Fresh source readback therefore points to a tighter next owner surface than a generic persisted-state problem or a pure branch-path failure: workflow-status phase construction over persisted snapshot surfaces is the live blocker for this copied Kraken state, with `structural_execution_candidate_value_from_recommended_path_bundle(...)` still one confirmed heavy branch but not the only affected phase path. Decision: `handoff`, not profitability-negative; next useful unclaimed work is diagnosing this same copied Kraken state's workflow-status phase-construction hang, not more export-bridge diagnosis.
- Blocked `2026-05-15 / codex-220646-state-reingest-v1`: fresh copied-state replay confirms the `220646` exact rooted branch still fails to materialize symbol-level workflow state even when the retained wire artifact is replayed again today. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T233356+0800-codex-220646-state-reingest-v1/summaries/terminal_decision_summary.md`. I copied the retained downstream state into a private run root, then reran `auto-quant-ingest-real-trades`, `pre-bayes-status`, `policy-training-status`, `workflow-status --phase execution-candidate --agent`, and `export-structural-path-ranking-target` through the repo-local binary. The replay did not repair the gap: `auto-quant-ingest-real-trades` timed out after `61.662s`, `workflow-status` timed out after `60.764s`, and `export-structural-path-ranking-target` timed out after `62.46s`; only `pre-bayes-status` and `policy-training-status` exited `0`, and they still reported no active gate/policy plus a single `bootstrap_readiness` structural target row with `runtime_selection=disabled`. The copied state's `learning_state.json` still holds `12329` feedback rows across the four rooted branch ids, but `SRC_ROOT_CARRY_LONG_220646/workflow_snapshot.json` remains `stage_unavailable` / `insufficient_state` / `no workflow phase snapshots available`. Decision: `blocked`, not promotion; the immediate blocker is symbol-state materialization from branch-aware feedback, not just provider completeness.

- Handoff `2026-05-15 / codex-family-d-aq-runtime-reuse-v1`: the exact rooted Family D copied workspace is no longer blocked by AQ runtime/bootstrap, and the branch has now been pushed one native `ict-engine` layer deeper. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T230000+0800-codex-family-d-aq-runtime-reuse-v1/summaries/terminal_decision_summary.md`. Real work executed: forked the repaired copied workspace into a fresh run root, reused `/Users/thrill3r/Auto-Quant/.venv/bin/python` against `run_tomac.py`, and got a real backtest result for branch `TrendTransition -> LiquidityReclaim -> family_d_liquidity_sweep_reclaim_15m_wide_v1 -> liquidity_sweep_reclaim_long` instead of the old PyPI/TLS failure. The AQ replay exited `0` and produced `126` trades, `total_profit_pct=1.98`, `profit_factor=1.0528`, `Sharpe=0.0766`, `win_rate=41.2698%`, with the repaired `NQ/USD 4h` informative data accepted. A fresh rooted `strategy_library.json` was then imported natively with `auto-quant-results-import=0`, and `auto-quant-prior-init=0` applied the same branch as valid BBN evidence with `trade_count=126`, `n_win=52`, `n_loss=74`, `evidence_value_gate_passed=true`, `bbn_log_loss_delta=0.5731`. The stop-point has narrowed again: `pre-bayes-status --refresh --output-format json` is still empty both before and after a fresh provider-driven replay attempt, `policy-training-status --output-format json` reports `analyze_runs=0`, `update_runs=0`, `matched_rows=0`, and `structural path ranking target export missing`, `workflow_snapshot.json` still says `blocking_truth.status=insufficient_state` / `reason=no workflow phase snapshots available`, a bounded native `export-structural-path-ranking-target` probe timed out after `92.830s` with `exit=124`, and a bounded native `analyze-live --futures-symbol NQ=F --spot-symbol QQQ --options-symbol QQQ --options-volatility-proxy-symbol ^VIX --spot-kind etf --futures-backend yfinance --aux-backend yfinance` probe also timed out after `121.844s` with `exit=124` while still leaving `latest_analyze=null`. Decision: `handoff`, not promotion; next useful unclaimed work is diagnosing why this imported rooted branch cannot materialize its first analyze/workflow-phase snapshot, not another AQ runtime/bootstrap retry.

- Blocked `2026-05-16 / codex-dense-bridge-gate-current-audit-v1`: direct-binary recheck of the exact TVR dense branch shows branch-path-aware Pre-Bayes is still alive, but fresh high-level runtime readbacks now stall before they can re-emit a new closed-loop proof. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T010900+0800-codex-dense-bridge-gate-current-audit-v1/summaries/terminal_decision_summary.md`. Real work executed: ran the repo-local debug binary directly against the copied TVR dense state from `20260515T192500...`, then reran against a private lightweight clone to remove shared-state contention as a confounder. The result is split and concrete: private-state `pre-bayes-status --refresh --output-format json` succeeded in `14.958s` and still kept `latest_gate_status=pass_hard`, canonical structural regime `trend` with confidence `0.9787`, and `latest_bridge_diff.long_short_signal_probability_gap=0.06324137013299541`; saved `workflow_snapshot.json` and `execution_tree_trace.json` still preserve the exact rooted path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_tvr_5m -> dense_kline_upbar_reclaim_tvr_qqq_5m_v1`, `blocking_truth.status=bridge_needs_confirmation`, and execution-tree evidence `path_ranker_score_visible_to_execution_tree=true` / `used=true`. But fresh top-level readbacks are now the blocker: direct-binary `workflow-status --agent` timed out on both the original copied state (`60.129s`) and the private lightweight clone (`90.454s`), and direct-binary `policy-training-status --output-format json` also timed out on the private clone (`90.605s`). Decision: `blocked`, not promotion; next useful unclaimed work is to root-cause why `workflow-status` / `policy-training-status` stall on this exact dense branch state, or to run the snapshot-suggested isolated `factor-research` replay on `tvr_qqq_1m.clean.json` before claiming any new full-chain provider/AQ/Pre-Bayes/BBN/CatBoost/execution-tree closure.

- Blocked `2026-05-15 / codex-220646-direct-binary-readback-v1`: current-turn direct-binary readback proves the retained `220646` rooted downstream state is readable now, but still not actionable. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T231008+0800-codex-220646-direct-binary-readback-v1/summaries/terminal_decision_summary.md`. Reusing `/.local-artifacts/cargo-target/debug/ict-engine` against `.../20260511T220646.../ict-engine-downstream/state_fresh_downstream_v2` avoided the broader `cargo run` cold-path issue and narrowed the stop point precisely: `pre-bayes-status --refresh --output-format json` exited `0` but stayed empty (`latest_gate_status=null`, `latest_policy=null`, `latest_bridge=null`), `policy-training-status --output-format json` exited `0` but stayed fail-closed (`rows=1`, `history_rows=1`, `mature_rows=0`, `raw_scored_mature=0/30`, `runtime_selection=disabled`, `trainer_artifact=missing`), and only `workflow-status --phase execution-candidate --agent` still failed to return, timing out at `20s` with `exit=124` and no stdout/stderr. Decision: `blocked`, not promotion; next useful work is execution-candidate workflow-hang diagnosis on this exact retained state, not another broad provider/runtime replay summary.
- Blocked `2026-05-15 / codex-220646-execution-candidate-provider-probe-root-cause-v1`: the retained `220646` execution-candidate hang is now narrowed to provider-surface runtime probing, not branch-path loss and not generic execution-tree logic. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T231008+0800-codex-220646-direct-binary-readback-v1/summaries/terminal_decision_summary.md`. `/usr/bin/sample` on the stuck direct-binary process shows the main thread blocked under `workflow-status -> provider_status_agent_surface -> provider_status_surface -> probe_public_fetch_python_runtime -> missing_public_fetch_python_modules -> std::process::Command::output -> poll`. Current shell repro matches that stack: the provider probe's `python3` import checks all exceed the built-in `2s` timeout window (`requests≈3.346s`, `pandas≈3.794s`, `ccxt≈6.828s`, `ib_async≈3.438s`, `redis≈3.573s`, `yaml≈3.373s`, `sklearn≈3.144s`, `pyarrow≈4.458s`), so `workflow-status --phase execution-candidate` can spend its whole time budget inside provider runtime probing before phase rendering begins. Decision: `blocked`, not promotion; next useful unclaimed work is a no-takeover fix or isolation strategy for provider-status probing inside `workflow-status`, then re-run the same exact rooted state.
- Handoff `2026-05-16 / codex-kraken-exact-export-time-budget-v1`: the retained Kraken exact-split blocker was partly a stale timeout interpretation, not a branch-path-loss bug. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T010800+0800-codex-kraken-exact-export-time-budget-v1/summaries/terminal_decision_summary.md`. Real work executed: bypassed `cargo run` and used the repo-local `ict-engine` binary directly on `state_split_kraken_1m`; the export finally completed after `160.37s` and rewrote the copied state's target surfaces with `rows=5`, `candidate_set_size=3`, `mature_rows=4`, `history_rows=12133`. The exact rooted Kraken path is now first-class in both current and history targets rather than `learning_state`-only: current rows now include `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1`, and `policy-training-status --output-format agent` reports `raw_scored_mature=12124/30`, `production_validation=12124/30`, `observation_validation=498/30`, `trainer_artifact=ready trainer_status=runtime_eligible`, but runtime consumption is still not closed because `runtime_selection=enabled_no_matching_scores runtime_matches=0` and a direct `workflow-status --agent` probe on the same updated state still sat silent for minutes and had to be killed. Decision: `handoff`, not promotion; next useful unclaimed work is the slow `workflow-status` downstream readback on the updated Kraken state, not re-proving branch-path survival or re-ingesting the same trade CSV.

- Handoff `2026-05-16 / codex-board-b-kraken-exact-export-refresh-readback-v1`: the retained copied Kraken dense-branch state no longer has a `learning_state -> target/history` branch-path gap, and the next fresh downstream stop point is now sharper than the old `223200` packet reported. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T011500+0800-codex-board-b-kraken-exact-export-refresh-readback-v1/summaries/terminal_decision_summary.md`. Re-checking the same copied state root `.../20260515T223200.../state_split_kraken_1m` after the earlier bounded export timeout shows `policy_training/structural_path_ranking_target{,_history}.jsonl` refreshed at `2026-05-15 23:06:27 +0800` and now contain the exact rooted path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` as both a `structural-candidates` row and `structural-feedback-history` row(s). Fresh direct-binary downstream readback now also proves the slice reaches `pre-bayes-status --refresh`: `latest_gate_status=pass_neutralized`, active regime `trend`, confidence `0.9787037037037036`, and `bridge_gap=0.01999432099614995`. The blocker has therefore narrowed again, and more precisely than a generic refresh timeout: `workflow-status` now times out in `agent`, `json`, `human`, and `execution-candidate` phase modes, including a no-`--refresh` `execution-candidate` read. Fresh source readback points to the next owner surface: the `execution-candidate` phase first calls `structural_execution_candidate_value_from_recommended_path_bundle(...)`, which eagerly builds the structural recommended-path bundle before any persisted execution-candidate fallback. Decision: `handoff`, not profitability-negative; next useful unclaimed work is diagnosing this same copied Kraken state's structural recommended-path execution-candidate builder hang, not more export-bridge diagnosis.
- Blocked `2026-05-16 / codex-family-a-fvg-retrace-downstream-timeout-v1`: resumed the exact rooted Family A packet `TrendExpansion -> FVGReclaim -> family_a_fvg_retrace_1h_v1 -> nq_fvg_retrace_1h` from the prior portability preflight and pushed it one real layer deeper with the repo-local `ict-engine` binary instead of waiting on shared Cargo locks. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T234500+0800-codex-family-a-fvg-retrace-downstream-timeout-v1/summaries/terminal_decision_summary.md`. Real work executed: rebuilt the isolated `/tmp` packroot from the repo candidate pack, ran direct-binary `factor-candidate-admission-targets` to export `5` rooted admission rows, and ran direct-binary `enable-structural-path-ranking-runtime` to `runtime_selection=enabled_registered_model_ready` with `runtime_matches=5`. The next live gate still failed closed: bounded direct-binary `analyze-live --symbol FVG_RETRACE_1H_PORTABILITY --futures-symbol NQ=F --spot-symbol QQQ --options-symbol QQQ --options-volatility-proxy-symbol ^VIX --state-dir /tmp/ict-engine-fvg-retrace-1h-portability-v1/state --human` timed out after `180.139s` with no stdout/stderr. Follow-up source and timing probes narrowed the highest-probability blocker to the default `yfinance` auxiliary/options enrichment path rather than the rooted branch packet itself: core `NQ=F` / `QQQ` chart requests were fast, but Yahoo quote endpoints returned `401`, Yahoo options `QQQ` took `14.157s` and returned `401 Invalid Crumb`, and the options page returned `503`. A narrow fail-fast patch was staged in `src/data/realtime/yfinance_runtime.rs`, but the patched-binary rerun did not complete in this turn, so the state root still shows no fresh downstream proof: `workflow_snapshot.json` remains `insufficient_state`, `pre_bayes=null`, `execution_tree=null`, and both `execution_tree_trace.json` and `pre_bayes_status.json` are absent. Decision: `blocked`, not promotion; next useful unclaimed work is to rerun this exact slice with the staged `yfinance` options-budget fix rather than switching factors or redoing portability.
- Blocked `2026-05-15 / codex-220646-provider-runtime-replay-v1`: current-turn continuation for historical profitability seed `220646` / `SourceRootStopCarryLongHorizonV1` refreshed the named provider matrix physically, then narrowed the truthful stop point on the exact retained downstream state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T230500+0800-codex-220646-provider-runtime-replay-v1/summaries/terminal_decision_summary.md`. Fresh provider probes succeeded for `yfinance` (`35` hourly `QQQ` rows), `kraken` (`169` hourly `PF_XBTUSD` futures rows), and `IBKR` (`151` hourly `QQQ` rows through the injected provider runtime at `/Users/thrill3r/.venvs/ict-engine-provider-py313/bin` and gateway port `4002`). `TradingViewRemix` remains live-blocked on both `tools/list` and `get_ohlcv` with current HTTP `429`. I then targeted the exact already-ingested rooted state `.../20260511T220646.../ict-engine-downstream/state_fresh_downstream_v2` with the existing repo-local binary instead of `cargo run`: direct `pre-bayes-status --refresh --output-format json` returned immediately but stayed empty (`latest_bridge=null latest_policy=null latest_gate_status=null`), and direct `policy-training-status --output-format json` also returned immediately with `matched_rows=0`, `rows=1`, `history_rows=1`, `trainer_artifact=missing`, `runtime_selection=disabled`. The only path that still fails to return is direct `workflow-status --phase execution-candidate --agent`, which timed out at `120s` with no stdout/stderr. A live `/usr/bin/sample` on the surviving hung process localized the stall to `workflow_status_shell -> workflow_status_command -> attach_workflow_opt_in_profile_refs -> provider_status_surface -> probe_public_fetch_python_runtime -> missing_public_fetch_python_modules -> std::process::Command::output -> poll`. Decision: `blocked`, not promotion; next useful work is tracing or patching that provider-catalog Python runtime probe path on this exact retained state, plus a post-quota TVR retry or an explicit three-provider Gate 3 acceptance decision.
- Handoff `2026-05-15 / codex-board-b-tvr-aq-contract-audit-v1`: branch-path survival is no longer the blocker for the rooted TVR dense-kline branch; the next blocker is Auto-Quant/data-contract parity plus actionability. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T230454+0800-codex-board-b-tvr-aq-contract-audit-v1/summaries/terminal_decision_summary.md`. Fresh readback reused the four-provider bridge packet and the persisted TVR downstream state, then ran real `uv run prepare.py` inside the preserved TVR factor-research Auto-Quant workspace. The downstream state still proves exact rooted-path carry-through: `execution_tree_trace.json` keeps `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, and exact path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_tvr_5m -> dense_kline_upbar_reclaim_tvr_qqq_5m_v1`, so branch-path loss is not the current failure mode. The real mismatch is contractual: the factor-research handoff asks for `tvr_qqq_1m.clean.json`, but the embedded Auto-Quant workspace that now reports `Data already present ... Ready.` is still the generic Binance crypto arena (`BTC/ETH/SOL/BNB/AVAX`, `1h/4h/1d`). TVR actionability also remains below threshold (`bridge_gap=0.0632`, execution tree `status=fail_closed`, `candidate_status=execution_observe_only`). Decision: `handoff`, not promotion; next useful unclaimed work is a parity-preserving adapter that materializes the rooted TVR provider/data contract inside Auto-Quant before any new downstream replay claim.
- Handoff `2026-05-16 / codex-board-b-liquidity-pool-texture-pack-v1`: `liquidity_pool_texture` now crosses the native Board B candidate-pack and admission surfaces, but only as an explicit observation-only seed. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T010800+0800-codex-board-b-liquidity-pool-texture-pack-v1/summaries/terminal_decision_summary.md`. A new isolated repo-local pack root was added at `support/examples/factor_candidate_packs/board-b-liquidity-pool-texture-v1/liquidity_pool_texture_range_observation_v1`, and fresh direct binary verification proved current CLI/state visibility: `factor-candidate-packs` persisted inventory with `candidate_pack_count=1`, `factor-candidate-admission-targets` exported `7` rooted branch rows, and `artifact-status --latest-only` shows inventory `ready`, structural target `admission_pending`, and trainer artifact `ready_observation_only`. The branch path now survives as first-class admission data for `Range -> {aggregate,btc_usd,es,eth_usd,eurusd,nq,xauusd} -> board_b_liquidity:liquidity pool texture observation:15m -> liquidity_pool_texture_range_observation_v1@...`, but Gate 1 still fails closed: `aggregate_trade_count=0`, `mature_rows=0`, `calibrated_rows=0`, `training_weight_rows=0`, and ranker runtime stays disabled. Decision: `handoff`, not promotion; next unclaimed work should attach realized outcomes or an AQ/entry-model bridge for one frozen root regime instead of extending the observation-only packet sideways.
- Superseded `2026-05-16 / codex-board-b-smt-pack-inventory-readback-v1`: this earlier readback omitted `--candidate-pack-root` on `factor-candidate-admission-targets`, so its “admission snaps back to curated root” blocker is no longer authoritative. Use `support/docs/experiments/actionable-regime-confidence/runs/20260516T091854+0800-codex-board-b-smt-observation-pack-v1/summaries/terminal_decision_summary.md` plus `support/docs/experiments/actionable-regime-confidence/runs/20260516T124115+0800-codex-smt-observation-runtime-carry-v1/summaries/terminal_decision_summary.md` for the corrected explicit-root chain.
- Handoff `2026-05-16 / codex-board-b-smt-persisted-inventory-parity-v1`: current-binary parity sharpens the SMT routing truth again without taking over active SMT/data-source owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T124702+0800-codex-board-b-smt-persisted-inventory-parity-v1/summaries/terminal_decision_summary.md`. Real work executed: persisted the explicit SMT inventory into `/tmp/ict-engine-smt-pack-current`, reran native `factor-candidate-admission-targets` both with and without `--candidate-pack-root`, then carried the explicit-root state through `enable-structural-path-ranking-runtime` and `policy-training-status`. The updated truth is split, not contradictory: the persisted-inventory path still falls back to `candidate_set_id=factor-candidate-admission:FACTOR_CANDIDATES:curated-auto-quant-v1` with `rows=41`, but the explicit-root path remains healthy on the same current binary and exports the intended rooted SMT set (`candidate_set_id=...:board-b-smt-relationship-v1`, `rows=5`) with first-class branch fields preserved and runtime enabled (`runtime_selection=enabled_registered_model_ready`, `runtime_matches=5`). Decision: `handoff`, not promotion; the real blocker has shifted back to evidence maturity (`mature_rows=0`, `training_weight_rows=0`, `raw_scored_mature=0/30`, `matched_rows=0`), so the next useful unclaimed work is strict/provider-backed sample growth or structural-feedback replay, not more pack-root diagnosis.
- Handoff `2026-05-15 / codex-220646-provider-recheck-v2`: current-turn provider recheck for historical profitability seed `220646` / `SourceRootStopCarryLongHorizonV1` reproduced the same three-provider subset again, but still did not unlock an honest four-provider replay. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T225838+0800-codex-220646-provider-recheck-v2/summaries/terminal_decision_summary.md`. Fresh live probes succeeded for `yfinance` (`35` hourly `QQQ` rows after one retry), `kraken` (`169` hourly `PF_XBTUSD` futures rows), and `IBKR` (`30` hourly `QQQ` rows via the low-pollution child env `uv run --with redis --with ib_async --with pandas ... fetch_external.py ibkr-historical ...`). `TradingViewRemix` is now proven split-blocked rather than simply absent: direct redacted remote `tools/list` against `https://tvremix.xyz/api/mcp/v1` still returned HTTP `429` with `Retry after 32406s`; forced local-stdio `provider-status --provider tradingview_mcp --compact` can report `market_data:1/1 ready`, but three direct local-stdio `market-data-harness` fetch attempts for `NASDAQ:QQQ 1h` still failed closed with MCP init timeout (`uv` warning path, `.venv/bin/tradingview-mcp`, and raw `.venv/bin/python src/tradingview_mcp/server.py`), and a direct pipe-based JSON-RPC probe of the raw local server also produced no stdout/stderr response to `initialize`, `tools/list`, or `get_ohlcv`. Fresh repo-local `ict-engine` read surfaces now agree with that provider picture: `provider-status --provider ibkr --compact` still says `configured_runtime_unhealthy:ibkr_runtime_dependencies_missing_with_gateway_reachable`, remote-default `provider-status --provider tradingview_mcp --compact` still says `configured_runtime_unhealthy:tradingview_mcp_connectivity_probe_failed`, bounded `pre-bayes-status` and `policy-training-status` still exit `0`, but bounded `workflow-status --phase execution-candidate --agent` timed out after `45.408s`. Direct state-file inspection sharpens the exact chain break: `learning_state.json` carries `12329` feedback rows across `4` rooted branch ids, but `SRC_ROOT_CARRY_LONG_220646/workflow_snapshot.json` is still `stage_unavailable` / `insufficient_state` / `no workflow phase snapshots available`, and `policy_training/structural_path_ranking_target.jsonl` still contains only one `bootstrap_readiness` row rather than real branch-aware target rows. The retained exact-branch downstream probe therefore remains the latest truthful stop point: `Pre-Bayes -> BBN feedback readback -> policy/workflow/export` were consumed, but the branch-aware feedback never materialized into workflow-phase snapshots, `execution_tree_ready=false`, `policy_ready=false`, and `promotion_status=not_promoted:execution_tree_or_policy_not_calibrated`. Decision: `handoff`, not promotion; next useful work is either a post-quota TVR retry, a repaired local-stdio OHLCV fetch path, or an explicit Board B decision that the re-proved three-provider subset is sufficient for this branch's Gate 3.
- Handoff `2026-05-15 / codex-tvr-bridge-provider-matrix-v1`: fresh four-provider `pre-bayes-status --refresh` readbacks confirm the rooted dense-kline branch is still not promotion-ready even after the TVR exact-row selection fix. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T235500+0800-codex-board-b-tvr-bridge-provider-matrix-v1/summaries/terminal_decision_summary.md`. Using the persisted TVR / IBKR / yfinance / Kraken states, the branch kept the same strong `trend` posterior (`confidence≈0.9787`) across all providers, so missing regime context is not the blocker. But portability is still weak: `TVR latest_gate_status=pass_hard bridge_gap=0.0632`, `IBKR pass_neutralized bridge_gap=0.0167`, `YF observe_only bridge_gap=0.0171`, `Kraken pass_neutralized bridge_gap=0.0116`. Even the best provider remains below the known bridge confirmation threshold `0.12`, so the next useful slice is bridge/actionability modeling or provider-portable factor conditioning, not more copied-state ranking/selection replay. Decision: `handoff`, not promotion.
- Handoff `2026-05-15 / codex-board-b-liquidity-pool-texture-runtime-audit-v1`: `liquidity_pool_texture` is already first-class runtime evidence, but not yet a Board B profitability packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T235950+0800-codex-board-b-liquidity-pool-texture-runtime-audit-v1/summaries/liquidity_pool_texture_terminal_readback.md`. The current runtime schema in `src/analyze_sections.rs` already exposes `factor_name`, `texture`, `level`, `high`, `low`, `touch_count`, `spacing_consistency`, `clean_sweep_likelihood`, `confidence`, and `fail_closed_reason`; classification is already wired in `src/main.rs`, and fresh existing runtime artifacts show populated YFinance and IBKR outputs rather than `n/a`. This slice also confirmed the current gap: no `liquidity_pool_texture` candidate pack was found under `support/examples/factor_candidate_packs/`, and no dedicated factor-lab/training/admission surface was found under `src/factor_lab/`, `support/scripts/research/`, or `support/scripts/auto_quant_external`, so this factor has not yet crossed into candidate-pack inventory, admission targets, per-regime profitability stats, or downstream Pre-Bayes/BBN/CatBoost/execution-tree proof. Fresh compile-based verification was blocked by shared Cargo lock contention, and fully isolated `/tmp` Cargo verification could not reuse the cached `loopybayesnet` git dependency. Decision: `handoff`, not promotion; next unclaimed work should build one isolated root-regime-aware `liquidity_pool_texture` candidate pack and Gate 1 packet before attempting downstream chain admission.
- Handoff `2026-05-16 / codex-board-b-liquidity-pool-texture-observation-helper-v1`: repo-local Gate 1 observation tooling now exists, but the first real held-out packet is still incomplete. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T020500+0800-codex-board-b-liquidity-pool-texture-observation-helper-v1/summaries/terminal_decision_summary.md`. Added `support/scripts/research/liquidity_pool_texture_observation_packet.py` plus `python3 -m unittest support.scripts.research.tests.test_liquidity_pool_texture_observation_packet` to convert one real analyze snapshot plus held-out future candles into a fail-closed rooted observation packet. Then ran the direct local binary `./.local-artifacts/cargo-target/debug/ict-engine` on a cropped retained YFinance QQQ early split (`5m last 512`, `15m last 192`, `30m last 96`) under isolated `/tmp` state. The runtime did make real progress and wrote `mece_recovery_artifact.json` plus `artifact_ledger.json`, but it stalled before emitting the later analyze/execution surfaces required to feed the helper with a real provider-backed `liquidity_pool_texture` observation packet. Decision: `handoff`, not promotion; next useful unclaimed work is to diagnose the post-MECE stall on the cropped direct-binary rerun, then run the helper against the retained late YF split once a real analyze snapshot exists.
- Handoff `2026-05-17 / codex-liquidity-pool-texture-outcome-bridge-v1`: the retained QQQ early/late split now yields a real repo-local outcome packet, but it is still negative Gate 1 evidence rather than a promotable profitability slice. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T032500+0800-codex-liquidity-pool-texture-outcome-bridge-v1/summaries/terminal_decision_summary.md`. Fresh isolated direct-binary repro on the full `30m/15m/5m early` split sharpened the blocker first: it timed out after `240.413s` with zero stdout/stderr and emitted no files. Reusing the narrower cropped retained window (`30m last 96`, `15m last 192`, `5m last 512`) then broke through fully on the same local binary: `analyze` exited `0` in `146.32s` and wrote `analyze_runs.json`, `execution_candidate.json`, `execution_tree_trace.json`, `workflow_snapshot.json`, `artifact_ledger.json`, and `mece_recovery_artifact.json` under an isolated `/tmp` state. The first real helper run against the retained late `5m` holdout exposed a concrete bug instead of yielding a packet: `support/scripts/research/liquidity_pool_texture_observation_packet.py` crashed on `ValueError: unsupported direction Neutral`. That helper is now patched and regression-covered (`python3 -m unittest support.scripts.research.tests.test_liquidity_pool_texture_observation_packet` passed `4/4`), and the rebuilt packet is preserved under the new run root with `main_regime=Transition`, `provider=yfinance`, `timeframe=5m`, `texture=jagged`, `touch_count=5`, `clean_sweep_likelihood=0.5436524052629547`, `selected_direction=Neutral`, `observation_outcome=no_trade`, `trade_count=0`, `fail_closed_reason=non_directional_trade_plan_not_eligible_for_realized_outcome`, and `quality_gate.downstream_allowed=false`. Decision: `handoff`, not promotion; next useful unclaimed work is to find or produce a retained/provider-backed split where the same bridge yields a directional `Bull` or `Bear` trade plan instead of `Neutral`, so the first non-zero realized-outcome row can exist.
- Handoff `2026-05-16 / codex-liquidity-pool-texture-xlf-fresh-binary-rerun-v1`: the exact frozen XLF directional split now clears the stale-binary blocker and yields the first directional provider-backed `liquidity_pool_texture` outcome bridge for this lane. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T123617+0800-codex-liquidity-pool-texture-xlf-fresh-binary-rerun-v1/summaries/terminal_decision_summary.md`. Real work executed: rebuilt the current dirty sources into isolated `/tmp/ict-engine-lpt-xlf-build-target/debug/ict-engine`, recreated the preserved `1w/1d/1h early` plus `1h late` XLF split under a new run root, reran `analyze` successfully on the early split (`exit=0` in `10.405s` agent output and `6.238s` full JSON), then ran `support/scripts/research/liquidity_pool_texture_observation_packet.py` against the late `1h` holdout. The retained packet is now first-class runtime evidence on rooted branch `Range -> LiquidityMap -> liquidity_pool_texture -> liquidity_pool_texture:observation_v1`: `provider=yfinance`, `timeframe=1h`, `texture=jagged`, `touch_count=10`, `clean_sweep_likelihood=0.55`, `selected_direction=Bear`, `observation_outcome=win`, `realized_r=0.666667`, and per-regime `range trade_count=1 win_rate=1.0 expectancy=0.666667`. Decision: `handoff`, not promotion; `quality_gate.downstream_allowed=false` with `single_provider_single_observation_not_ready_for_candidate_pack_or_downstream_admission`, so next useful unclaimed work is support expansion or a candidate-pack/AQ bridge for the same rooted range lane, not another fresh-binary parity rerun or another first directional bridge.
- Handoff `2026-05-16 / codex-liquidity-pool-texture-xlf-pack-carry-v1`: the existing XLF directional retained liquidity pack is now proven visible through the explicit-root native candidate-pack and admission loop, after suppressing a duplicate sibling-pack mistake instead of leaving clutter behind. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T124914+0800-codex-liquidity-pool-texture-xlf-pack-carry-v1/summaries/terminal_decision_summary.md`. Real work executed: audited the existing pack root `support/examples/factor_candidate_packs/board-b-liquidity-pool-texture-v1/`, found a pre-existing authoritative XLF pack `liquidity_pool_texture_xlf_range_directional_1h_v1`, created and then removed a redundant sibling pack, removed the empty duplicate directory, and reran the fresh binary on the corrected two-pack root. The corrected explicit-root loop now exits cleanly: `factor-candidate-packs` reports `candidate_pack_count=2`, `factor-candidate-admission-targets` writes `rows=9` with `pending_reward_states={candidate_pack_admission_pending:9}` and `mature_rows=0`, and `policy-training-status` reports the same root with `candidate_pack_count=2`, `rows=9`, `history_rows=9`, `mature_rows=0`, `trainer_status=present_validation_insufficient`, and `runtime_selection=disabled`. The exact XLF retained branch now survives as rooted structural target rows `Range -> aggregate -> board_b_liquidity:liquidity pool texture directional retained bridge:1h -> liquidity_pool_texture_xlf_range_directional_1h_v1@AGGREGATE` and `Range -> xlf -> ... -> liquidity_pool_texture_xlf_range_directional_1h_v1@XLF`, both with `raw_path_score=0.08` but still `pending_reward_state=candidate_pack_admission_pending`. Decision: `handoff`, not promotion; next useful unclaimed work is support expansion on the existing XLF retained pack until it stops being `single_market_only` / `anecdotal`, not another sibling-pack creation or another first-carry proof.
- Handoff `2026-05-16 / codex-board-b-provider-status-probe-stall-rootcause-v1`: the cropped retained YFinance `analyze` stall is now traced to a shared provider-catalog probe, not to Board B factor logic. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T004500+0800-codex-board-b-provider-status-probe-stall-rootcause-v1/summaries/terminal_decision_summary.md`. A live macOS stack sample of `./.local-artifacts/cargo-target/debug/ict-engine analyze ...YF_QQQ_MKTSTRUCT_30M_15M_5M_222900_EARLY_LPT_CROPDBG...` showed the process blocked in `build_analyze_report -> provider_status_agent_surface -> provider_status_surface -> probe_public_fetch_python_runtime -> missing_public_fetch_python_modules -> std::process::Command::output -> poll`. Owner-side fix landed in `src/application/provider_catalog.rs`: new `command_status_with_timeout(...)` plus a regression test `command_status_with_timeout_ignores_background_child_holding_pipes_open`, switching the Python import probe away from output-draining wait semantics. Decision: `handoff`, not promotion; next useful unclaimed work is to finish the targeted regression test build, rebuild the local binary, rerun the cropped YF analyze, then feed the emitted analyze/execution surfaces plus retained late candles into the new observation helper.
- Handoff `2026-05-15 / hermes-smt-relationship-resolver-first-class-schema-v1`: first-class runtime schema for `smt_relationship_resolver` was added in `src/analyze/smt_correlation_section.rs` without changing SMT into a standalone actionable signal. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T165000+0800-hermes-smt-relationship-resolver-first-class-schema-v1/summaries/smt_relationship_resolver_terminal_readback.md`. New schema exposes `symbol`, `primary_related_symbols`, `futures_peers`, `cfd_proxies`, `etf_proxies`, `sector_or_industry_peers`, `currency_macro_drivers`, `session_leaders`, `relationship_type`, `confidence`, and `evidence_source`, with tests covering NQ, EURUSD, XAUUSD, BTC, equity fallback, and provider-universe filtering. TDD evidence: RED failed on missing `resolve_smt_relationships`; GREEN `cargo test smt_relationship_resolver -- --nocapture` passed 5/5; regression `cargo test smt -- --nocapture` passed 18/18. Decision: `handoff`, not promotion; this is resolver schema infrastructure only, while strict SMT downstream admission still remains controlled by the existing strict-density packet and its insufficient pair/per-regime rows.
- Handoff `2026-05-16 / codex-board-b-smt-observation-pack-v1`: Board-B now has a dedicated SMT observation helper and native candidate-pack root that preserve the rooted same-event confirmation contract without taking over active Rust/SMT owners. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T091854+0800-codex-board-b-smt-observation-pack-v1/summaries/terminal_decision_summary.md`. Added `support/scripts/research/smt_observation_packet.py`, `support/scripts/research/tests/test_smt_observation_packet.py`, and `support/examples/factor_candidate_packs/board-b-smt-relationship-v1/smt_same_event_confirmation_observation_v1/`. Focused tests passed `4/4`, the existing liquidity observation regression stayed green `3/3`, direct-binary inventory with explicit `--candidate-pack-root support/examples/factor_candidate_packs/board-b-smt-relationship-v1` reported `candidate_pack_count=1`, and direct-binary admission export wrote `5` rooted rows for `aggregate`, `NQ/ES/YM`, `EURUSD/GBPUSD/DXY`, `XAUUSD/XAGUSD/DXY`, and `BTC/ETH`. A retained provider-backed Yahoo `BTC/ETH` `1h` packet was materialized with the new helper and stayed honestly fail-closed (`session_overlap=true`, `relationship_confidence=0.0`, `fail_closed_reason=swing_confirmation_failure_not_near_same_event_window|recent_correlation_unstable`, `actionable=false`). Decision: `handoff`, not promotion; next useful unclaimed work is a mature strict-entry outcome packet for the four required pair clusters, plus a fresh binary/readback check that the candidate-set root slug is no longer stale.
- Handoff `2026-05-16 / codex-board-b-smt-strict-pack-v1`: the settled strict SMT training packet is now lifted into a native Board-B candidate-pack root instead of staying trapped only in historical materials. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T092417+0800-codex-board-b-smt-strict-pack-v1/summaries/terminal_decision_summary.md`. Added `support/examples/factor_candidate_packs/board-b-smt-strict-v1/smt_same_event_strict_training_v1/`, sourced directly from the settled `223155` strict packet (`rows=72`, `strict_training_rows=15`, `inverse_normalized_rows=7`, `downstream_allowed=false`). Direct-binary inventory with explicit `--candidate-pack-root support/examples/factor_candidate_packs/board-b-smt-strict-v1` reported `candidate_pack_count=1`, `aggregate_trade_count=15`, `aggregate_label=probe_only`; direct-binary admission export wrote `8` rooted rows for `aggregate`, `NQ/ES`, `NQ/YM`, `EURUSD/GBPUSD`, `EURUSD/DXY`, `XAUUSD/XAGUSD`, `XAUUSD/DXY`, and `BTC/ETH`. The strict packet remains honestly fail-closed: `mature_rows=0`, `training_weight_rows=0`, missing strict lanes still include `NQ/YM` and `EURUSD/DXY`, and `stress` / `other` remain empty. Decision: `handoff`, not promotion; next useful unclaimed work is strict sample expansion for those missing lanes/regimes and then a fresh current-binary rerun before any Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution-tree promotion claim.
- Handoff `2026-05-16 / codex-board-b-smt-strict-runtime-carry-v1`: the strict SMT candidate-pack state is now proven one layer deeper in the native downstream chain: admission target export -> ranker runtime enable -> policy-training/workflow readback. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T092931+0800-codex-board-b-smt-strict-runtime-carry-v1/summaries/terminal_decision_summary.md`. Direct binary `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only` succeeded on `/tmp/ict-engine-smt-strict-boardb` with `runtime_selection_status=enabled_registered_model_ready`, `runtime_active_match_count=8`, and `runtime_candidate_set_match_count=8`. `policy-training-status --output-format json` confirms the factor pack and runtime are visible downstream (`candidate_pack_count=1`, `cross_market_candidate_count=1`, `runtime enabled=true`) but still fail closed for learning (`mature_rows=0`, `rows_with_training_weight=0`, `raw_scored_mature_rows=0/30`, `production_validation_rows=0/30`, `observation_validation_rows=0/30`, both entry models `matched_rows=0`). A bounded `workflow-status --refresh --output-format json` probe timed out after `20.079s` with no stdout/stderr, so this slice still does not provide an honest execution-tree-ready proof. Decision: `handoff`, not promotion; next useful unclaimed work is still strict sample expansion for `NQ/YM`, inverse `EURUSD/DXY`, and `stress` / `other`, then rerunning the same native path on a fresh binary.
- Handoff `2026-05-16 / codex-smt-observation-runtime-carry-v1`: corrected explicit-root continuation shows the observation SMT pack already carries through the native Board B pack loop one layer deeper than the earlier pack-creation packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T124115+0800-codex-smt-observation-runtime-carry-v1/summaries/terminal_decision_summary.md`. Real work executed: reran the observation pack with the correct explicit root on every native command surface, not just inventory. Direct-binary `factor-candidate-packs` reported `candidate_pack_count=1`; direct-binary `factor-candidate-admission-targets` correctly exported `candidate_set_id=factor-candidate-admission:FACTOR_CANDIDATES:board-b-smt-relationship-v1` with `rows=5`; direct-binary `enable-structural-path-ranking-runtime --reuse-mode candidate_set_only` reached `runtime_selection_status=enabled_registered_model_ready` with `runtime_active_match_count=5`; and `policy-training-status` confirmed downstream visibility (`inventory_ready=true`, `candidate_pack_count=1`, `rows=5`, `history_rows=5`) while honestly failing closed for learning (`mature_rows=0`, `training_weight_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`). Unlike the strict packet, the bounded `workflow-status --refresh --output-format json` probe returned successfully instead of timing out. The truthful blocker is bootstrap-only workflow state, not candidate-root routing: `workflow_snapshot.json` stays `stage_unavailable / insufficient_state / no workflow phase snapshots available`, and the surfaced execution candidate is only the bootstrap scenario path `path:scenario:FACTOR_CANDIDATES:bootstrap:no_workflow_state:bootstrap_collect_inputs:primary`. Decision: `handoff`, not promotion; next useful unclaimed work is provider-backed analyze/update or structural-feedback replay that materializes a real workflow snapshot for this observation pack, not more pack-root diagnosis.
- Handoff `2026-05-16 / codex-smt-provider-touch-refresh-v1`: touched the real named SMT providers again and refreshed a rooted provider-backed SMT packet without reusing the old schema-only slice as a proxy. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T091500+0800-codex-smt-provider-touch-refresh-v1/summaries/terminal_decision_summary.md`. Real provider touches were concrete: `uv run --with redis --with ib_async --with pandas python -m support.scripts.ibkr_bridge.account_prober --port 4002 --timeout 5` succeeded with `account_type=paper`, `n_subaccounts=1`, and `hist_window cap=55`; real `ibkr-historical` fetches wrote ETF-proxy `QQQ/SPY` `15m` rows plus a full `IBKR` `NQ/ES/YM` futures-family probe; the originally failing full-expiry input `20260619` is now fixed at the shared owner `support/scripts/auto_quant_external/fetch_external.py`, which normalizes FUT expiry to contract month before `ib_async.Future(...)`, and the same `NQ/ES/YM` probes now succeed with fresh `*_20260619_fixed.csv` artifacts instead of the old `Error 200` / `includeExpired` chain; `TradingViewRemix MCP` was configured but both bounded `tools/list` and `get_ohlcv(NASDAQ:QQQ,1d,10)` probes returned live HTTP `429` with retry windows around `2525s`; the fresh copied SMT observation root now regenerates `18/18` requested provider symbols and writes `15` rooted rows across `yahoo_chart`, `kraken_public_ohlc`, and `ibkr_historical_csv`. The live SMT row readback is still confirmation-only, not downstream-ready: `EURUSD -> DXY`, `GBPUSD -> DXY`, and `XAGUSD -> DXY` remain `bullish_smt`, `BTC -> ETH` via `yahoo_chart` is now a `bearish_smt`, and all four still keep `actionable=false` plus `confirmation_only_waiting_for_mss_cisd_displacement_and_pda`; the new `IBKR` futures-family rows are useful provider-backed evidence but still observation-only (`NQ -> YM` fail-closed as `recent_positive_correlation_unstable`; `NQ -> ES`, `ES -> YM`, and `QQQ -> SPY` fail-closed as `no_swing_confirmation_failure_at_latest_event`). Decision: `handoff`, not promotion; the next useful unclaimed work is an outcome-labeling bridge that adds truthful `forward_return` / `outcome_hit` fields to the provider-backed SMT rows, then rerunning the strict trainer on that bridged packet before any downstream admission claim.
- Board B is not complete.
- Blocked `2026-05-15 / codex-yf-kraken-realtrade-replay-matrix-v1`: retained `IBKR`, `YF`, and `TVR` dense-branch exact packets now have a fresh comparison root, but the new exact `Kraken` split currently stalls after first-class real-trade ingest. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T223200+0800-codex-yf-kraken-realtrade-replay-matrix-v1/summaries/terminal_decision_summary.md`. Real work executed: built `348`-row `dense_kline_kraken_xbtusd_1m_split_real_trades_wire_v1.jsonl` from the retained `kraken_xbtusd_1m_trades.csv`, ingested it into a copied dense-branch state with `feedback_records_inserted=348`, and confirmed the exact rooted path `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_kraken_1m -> dense_kline_upbar_reclaim_kraken_xbtusd_1m_v1` is present `348` times in `learning_state.json` as first-class `factor_name/branch_id/path_id`. The blocker is downstream projection, not ingest: the copied state's target/history surfaces still show only the generic dense row plus generic `trend_follow_through`, the Kraken exact split does not appear in `structural_path_ranking_target_history`, and bounded direct reruns of `export-structural-path-ranking-target` and `analyze` on real Kraken `30m/15m/1m` ladders both timed out with no stdout/stderr. Decision: `blocked`, not profitability-negative; next useful work is diagnosing why this new Kraken exact split remains `learning_state`-only while the retained `IBKR / YF / TVR` exact packets already reach target/runtime surfaces.
- Blocked `2026-05-15 / codex-family-f-trend-pullback-portability-v1`: Gate 3 provider-portability preflight for `family_f_trend_pullback_dense_15m_v1` on branch `TrendExpansion -> TrendPullbackDense -> family_f_trend_pullback_dense_15m_v1 -> nq_trend_pullback_dense` stopped before any fresh downstream rerun because the current four-provider matrix is not actually runnable. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T224800+0800-codex-family-f-trend-pullback-portability-v1/summaries/terminal_decision_summary.md`. Live provider checks were real: `IBKR` setup status showed consent, capabilities, Redis, and `127.0.0.1:4002` gateway reachability, but a bounded `ibkr-historical` fetch for `QQQ 1h 5D` timed out with no CSV; direct Yahoo-compatible `QQQ` chart fetch returned `429 Too Many Requests`; `TradingViewRemix` remote MCP endpoint was reachable but both JSON-RPC `initialize` and `tools/list` hit `Rate limit exceeded`; `Kraken` public `XBTUSD 60m` OHLC returned live JSON, but the factor pack only supports `NQ/USD`, `GLD/USD`, `SPY/USD`, `IWM/USD`, and `DIA/USD`, so Kraken cannot satisfy portability for this factor id. Decision: `blocked`, not factor-negative and not a downstream failure; do not rerun `Auto-Quant -> Pre-Bayes/filter -> BBN -> CatBoost/path-ranker -> execution tree` for this factor until at least one compatible provider leg becomes genuinely fetchable again.
- Handoff `2026-05-15 / codex-joint-runtime-counterfactual-v1`: after fixing copied-state summary-path rebasing, the strongest joint copied-state ranking+calibration patch finally made the runtime select the TVR exact row, but the lane still did not become actionable. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T192500+0800-codex-joint-runtime-counterfactual-v1/summaries/terminal_decision_summary.md`. A targeted regression test now passes: `cargo test copied_state_prefers_local_target_paths_when_present -- --nocapture`, which proves copied states now read their own local target artifacts instead of the source state's absolute summary paths. With that bugfix in place, the copied-state runtime surfaced `runtime_source=candidate_set`, and `recommended_path_bundle.path_id` flipped to `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_tvr_5m -> dense_kline_upbar_reclaim_tvr_qqq_5m_v1`. The lane still blocked at `bridge_needs_confirmation`. The live gate formula is now explicit from `src/workflow_snapshot_runtime.rs`: when `pre_bayes` is `hard_pass`, workflow still blocks if `bridge_diff.long_short_signal_probability_gap < ICT_ENGINE_BRIDGE_GAP_CLEAR_THRESHOLD`, and the default threshold is `0.12`. The current selected TVR exact path reaches `long_signal_probability=0.40292696964210006`, `short_signal_probability=0.4661683397750955`, and therefore `bridge_gap=0.06324137013299541`, with `selected_entry_quality=medium`, `multi_timeframe_alignment_score=0.9131`, and `pre_bayes_quality_score=0.884`. So selection is no longer the live blocker; actionability is. Decision: `handoff`, not promotion; the next useful slice is to attack the `bridge_needs_confirmation` gate rather than more copied-state selection patches.
- Handoff `2026-05-15 / codex-lambdarank-betacal-tvr-selection-v1`: even combined group-aware ranking plus path-specific calibration on a copied state still did not make the TVR exact row become the selected path. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T191500+0800-codex-lambdarank-betacal-tvr-selection-v1/summaries/terminal_decision_summary.md`. On a copied state with the TVR 5m rooted row already materialized, I trained a local LightGBM `LambdaRank` model on `structural_path_ranking_target_history.csv`, then applied `BetaCalibration` per path on the learned scores before patching the copied current rows and rerunning `analyze` / `workflow-status`. The learned model did rank the TVR row above the generic row on raw score, and the combined patch produced calibrated probabilities near `1.0` for both generic and TVR exact rows while keeping the IBKR row near zero. But after replay, the live workflow surface still selected the generic dense row and still blocked at `bridge_needs_confirmation`; surfaced path probabilities reverted to the older history-path semantics (`generic=0.4269102990033223`, `TVR_5m=0.4223107569721116`). Decision: `handoff`, not promotion; the next bottleneck is deeper than copied-state ranking/calibration experiments and sits in the runtime path-resolution integration itself.
- Handoff `2026-05-15 / codex-betacal-tvr-selection-v1`: a copied-state path-specific calibration patch confirms that trivial per-path Beta-style calibration is not enough to make the TVR exact row become the selected path. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T191000+0800-codex-betacal-tvr-selection-v1/summaries/terminal_decision_summary.md`. On a copied state, I derived simple Beta-smoothed success rates from `structural_path_ranking_target_history.jsonl` per path and patched the current target rows. The resulting path-level means were very close: generic dense `0.4241334205362982`, TVR 5m `0.4246031746031746`, IBKR 5m `0.4029520295202952`. Workflow still kept the generic dense row at rank 1 and the lane remained `blocking_status=bridge_needs_confirmation` with bridge gap `0.063`. Decision: `handoff`, not promotion; this proves the next bottleneck is not a trivial calibration patch, but a richer calibration / selection layer or a post-selection actionability model.
- Handoff `2026-05-15 / codex-lambdarank-tvr-selection-v1`: repo-local LightGBM `LambdaRank` confirms that raw ranking is no longer the main bottleneck; calibration is. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T184500+0800-codex-lambdarank-tvr-selection-v1/summaries/terminal_decision_summary.md`. On a copied state with the TVR 5m rooted row already materialized, I trained a group-aware `LambdaRank` model over `structural_path_ranking_target_history.csv` grouped by `candidate_set_id`, using current path/risk/posterior/branch-segment features. The learned current-set raw scores ranked `TVR_5m` first, `generic dense` second, and `IBKR_5m` third. But after applying those scores back into the copied state and enabling `candidate_set_only` runtime, workflow still kept the generic dense row at rank 1 because the generic row retained `path_ranker_calibrated_path_prob=0.75`, while the TVR row, despite the slightly higher learned raw score, still had `path_ranker_calibrated_path_prob=null`. The lane therefore stayed `blocking_status=bridge_needs_confirmation`. Decision: `handoff`, not promotion; this is the cleanest current proof that the next bottleneck is path calibration / probability assignment rather than pure raw ranking.
- Handoff `2026-05-15 / codex-tvr-path-selection-counterfactual-v1`: after the TVR 5m rooted row was materialized, a copied-state score override still did not make the TVR row become the selected path. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T183000+0800-codex-tvr-path-selection-counterfactual-v1/summaries/terminal_decision_summary.md`. On a copied state, I applied external scores that forced raw path scores to `generic=0.10`, `TVR_5m=0.99`, `IBKR_5m=0.10`, then enabled `candidate_set_only` runtime and replayed `analyze` / `workflow-status` on the same fresh TVR ladder. The lane still blocked at `bridge_needs_confirmation`, and the generic dense row remained rank 1 because it retained `path_ranker_calibrated_path_prob=0.75`, while the fresh TVR row, despite `raw_path_score=0.99`, had `path_ranker_calibrated_path_prob=null`. Decision: `handoff`, not promotion; the next bottleneck is path-calibration / selection logic, not simple raw-score ordering.
- Handoff `2026-05-15 / codex-tvr-real-trade-feedback-replay-v1`: the fresh TVR 5m rooted real-trade replay successfully materialized a first-class TVR-specific dense branch row. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T181800+0800-codex-tvr-real-trade-feedback-replay-v1/summaries/terminal_decision_summary.md`. Starting from a cloned `IBKR_QQQ_5m` provider-specific AQ unit, I replaced the 5m feather with fresh `TradingViewRemix` `NASDAQ:QQQ` 5m candles, exported actual trades (`249` rows, `sum_profit_abs=1633.645035`, `summary_sharpe=11.9828`), built `dense_kline_tvr_qqq_5m_real_trades_wire_v1.jsonl`, and ingested it into a copied state with `feedback_records_inserted=249` and `trades_invalid=0`. The copied state’s `learning_state.json` now carries `source=dense_kline_tvr_qqq_5m_split_replay` and rooted TVR branch ids, and `workflow-status --agent` now surfaces the TVR path as rank 2 with `path_ranker_calibrated_path_prob=0.4223107569721116`, just behind the generic dense row and ahead of the historical exact `IBKR` split. Decision: `handoff`, not promotion; materialization is fixed, but selection and downstream execution still block promotion.
- Handoff `2026-05-15 / codex-tvr-path-materialization-root-cause-v1`: the fresh TVR replay does improve replay-time bridge separation, but it still does not create a first-class rooted TVR branch row because the state never receives new TVR realized-trade feedback. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T180200+0800-codex-tvr-path-materialization-root-cause-v1/summaries/terminal_decision_summary.md`. State evidence is split: `execution_candidate_history.json`, `pending_update_feedback.json`, and related replay surfaces clearly reference the fresh `tvr_qqq_{1m,5m,15m}.clean.json` ladder, but `workflow` top-path candidates and `policy_training/structural_path_ranking_target.jsonl` still only expose the generic dense branch plus the historical exact `IBKR` split row. `learning_state.json` still contains `actual_freqtrade_auto_quant_trade_csv` feedback rows for `provider="IBKR"` / `surface_id="ibkr_qqq_5m"` and `ibkr_qqq_1m`, with no TVR real-trade feedback rows. Code path confirms the same split: `auto_quant_factor_research_command(...)` only emits a handoff payload, while rooted branch-path learning enters via `auto_quant_ingest_real_trades_command(...)` and `RealTradeRecord` in `src/application/auto_quant/real_trades/wire.rs`. Decision: `handoff`, not promotion; next useful work is to generate a TVR-specific realized-trade/export packet and ingest it, or add another typed path-generation route that can materialize a rooted TVR branch row without relying on the old IBKR trade-ingest lineage.
- Handoff `2026-05-15 / codex-dense-kline-tvr-factor-research-v1`: the fresh TVR replay state now has a real copied-state AQ continuation packet, but it still does not change the downstream execution blocker. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T173200+0800-codex-dense-kline-tvr-factor-research-v1/summaries/terminal_decision_summary.md`. Starting from the copied TVR replay state, I ran `factor-research`, `auto-quant-prepare`, seeded real local winners (`BTCLeaderBreakV4`, `MTFTrendStack`, `VolBreakoutSizedV005`), ran the real AQ backend loop from the copied AQ repo root, built a parity-matched current-run strategy library, re-imported it with `matched=3 / mismatches=[] / n_ok=3`, re-applied `auto-quant-prior-init --force`, then replayed `analyze`, `workflow-status`, and `auto-quant-adoption-review`. The AQ backend is not blocked: `BTCLeaderBreakV4 robust_sharpe=0.9415`, `MTFTrendStack robust_sharpe=0.3652`, and `VolBreakoutSizedV005 robust_sharpe=1.3268`, all with `profit_floor=PASS`, `min_position_size=PASS`, `pareto_dominated_by=none`. The imported/prior-initialized copied state still landed at the same downstream blocker: `blocking_status=bridge_needs_confirmation`, `blocking_reason=pre_bayes passed but bridge gap 0.063 is below confirmation threshold`, top path gate `observe`, execution hint `execution_guarded_due_to_high_transition_hazard`, and the top path remained the generic dense branch from `history_path`. `auto-quant-adoption-review` also stayed `review_status=ready_for_external_execution`, which confirms the stall is not inside AQ export plumbing. Decision: `handoff`, not promotion; next useful work is to materialize a first-class rooted TVR-specific branch/path row or directly attack the post-AQ downstream limiter beyond bridge gap `0.063`, not more copied-state AQ plumbing.
- Handoff `2026-05-15 / codex-dense-kline-tvr-minpos-replay-v1`: the fresh TVR low-timeframe QQQ ladder now replays through the active dense `VolBreakoutSizedV005` copied state, but still stops short of promotion. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T171500+0800-codex-dense-kline-tvr-minpos-replay-v1/summaries/terminal_decision_summary.md`. Starting from a non-overlapping copy of `state_probe_minpos_v1`, I replayed the newly recovered `TradingViewRemix` `NASDAQ:QQQ` `15m/5m/1m` candles through `analyze`, `workflow-status`, `pre-bayes-status`, and `policy-training-status`. The fresh TVR replay materially improved the real bridge-gap surface to `0.06324137013299541`, which is stronger than the prior runnable provider replays (`0.038`, `0.040`, `0.041`), while keeping `pre_bayes` at `pass_hard`, `multi_timeframe_alignment_score=0.9131`, and ranker runtime healthy (`enabled_history_ready`, `runtime_matches=5`, `model_family=catboost`). Promotion still failed: default `workflow-status` stayed `blocking_status=bridge_needs_confirmation`, top path gate `observe`, execution hint `execution_guarded_due_to_high_transition_hazard`, and the visible ranked paths remained the generic dense branch plus the historical exact `IBKR` split. No first-class TVR-specific branch row surfaced in `top_path_candidates` or `structural_path_ranking_target.jsonl`. A copied-state counterfactual with `ICT_ENGINE_BRIDGE_GAP_CLEAR_THRESHOLD=0.04` and `ICT_ENGINE_TRANSITION_HAZARD_BLOCK_THRESHOLD=0.70` still ended `fail_closed` with `exact_structural_branch_visible_but_not_ready_or_actionable`. Decision: `handoff`, not promotion; next useful work is to materialize a rooted TVR-specific branch/path row or run the workflow-recommended `factor-research` continuation on the fresh TVR replay state, not another fetch-only probe.
- Handoff `2026-05-15 / codex-dense-kline-tvr-fetch-recovery-v1`: the earlier dense low-timeframe TVR blocker is no longer true at the provider fetch layer. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T164000+0800-codex-dense-kline-tvr-fetch-recovery-v1/summaries/terminal_decision_summary.md`. Same-turn `provider-status --provider tradingview_mcp --agent` now reports `status=ready reason=mcp_url_and_api_key_available`. Reusing the exact old dense QQQ request shape from `20260513T181830+0800-hermes-family-h-dense-kline-regime-branch-v1/requests/`, fresh `market-data-harness --action fetch` calls now succeed for `NASDAQ:QQQ` `1m/5m/15m`: `1m rows=1200 (2026-05-11T19:30:00Z -> 2026-05-14T19:59:00Z)`, `5m rows=1200 (2026-04-23T17:30:00Z -> 2026-05-14T19:55:00Z)`, and `15m rows=572 (2026-04-15T13:30:00Z -> 2026-05-14T19:45:00Z)`, all with `ok=true`, `provider=tradingview_mcp`, `operation=ohlcv.fetch`. Decision: `handoff`, not promotion; TVR is provider-usable again for the dense QQQ low-timeframe lane, but this slice stopped at fetch/materialization and did not yet replay the fresh TVR ladder through the active `VolBreakoutSizedV005` copied state.
- Handoff `2026-05-15 / codex-ibkr-provider-runtime-venv-repair-v1`: current-turn runtime repair re-opened live `IBKR` provider use without repo code edits and captured a fresh four-provider readiness snapshot for the named provider set. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T163054+0800-codex-ibkr-provider-runtime-venv-repair-v1/summaries/terminal_decision_summary.md`. Baseline `cargo run --quiet -- provider-status --provider ibkr --agent` still reported `configured_runtime_unhealthy / ibkr_runtime_dependencies_missing_with_gateway_reachable`; source trace in `src/application/data_sources/control_matrix_providers.rs` confirmed the probe shells out to `python3` and imports `redis`, `ib_async`, and local `ibkr_bridge`. The exact baseline probe reproduced `ModuleNotFoundError: No module named 'redis'`. Injecting the known-good provider venv with `PATH=/Users/thrill3r/.venvs/ict-engine-provider-py313/bin:$PATH` made the same exact probe pass and flipped `provider-status --provider ibkr --agent` to `status=ready reason=local_ibkr_runtime_ready`. A real bounded fetch then succeeded: `support/scripts/auto_quant_external/fetch_external.py ibkr-historical --symbol QQQ ... --bar-size '5 mins' --duration '2 D'` wrote `196` rows to `materials/ibkr_qqq_5m_2d.json` with empty stderr. Same-turn provider snapshot artifacts also show `TradingViewRemix status=ready`, `yfinance ready`, and `kraken_cli ready`. Decision: `handoff`, not promotion; downstream Board B slices can now reuse the injected provider runtime for real IBKR work instead of treating the lane as dependency-blocked.
- Blocked `2026-05-15 / hermes-ob-breaker-rejection-confirmation-preflight-v1`: tested the next gate named by the OB variant packet instead of relabeling it. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T163048+0800-hermes-ob-breaker-rejection-confirmation-preflight-v1/summaries/ob_breaker_rejection_confirmation_preflight_readback.md`. Source was the existing provider-backed ATR/CISD/FVG/OB rows; the preflight inspected `4391` OB rows and `1868` realized OB rows across `Binance/Bybit/IBKR/Kraken/yfinance`, `BTCUSDT/SPY/XBTUSD`, and `range/transition/trend`. The aggregate OB slice is negative (`win_rate=0.294433`, `expectancy_r=-0.093538`), but the decisive blocker is schema truth: required explicit confirmation fields `breaker_confirmed`, `rejection_confirmed`, and `mitigation_count` are absent. Therefore no Auto-Quant/Pre-Bayes/BBN/CatBoost/execution-tree admission was run for this confirmation gate; inferring confirmation from outcome, CISD, or displacement would be hallucination. Candidate-pack loop was checked unchanged: `factor-candidate-packs` still lists the repo-local packs, `factor-candidate-admission-targets` writes `35` rows, and `policy-training-status` keeps runtime selection disabled. Decision: `blocked`; next useful work is a real provider-backed extractor that emits explicit breaker/rejection/mitigation fields from candles/structure events, plus broader `NQ/ES/YM/RTY/XAUUSD` coverage.
- Incubate `2026-05-15 / hermes-order-block-variant-classifier-v1`: built a first-class `order_block_variant_classifier` packet for branch `Transition -> OrderBlockVariant -> ob_mitigation_breaker_rejection -> order_block_variant_classifier_v1`, without taking over the active SMT resolver lane. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T162135+0800-hermes-order-block-variant-classifier-v1/summaries/order_block_variant_classifier_terminal_readback.md`. Source material was the existing provider-backed ATR/CISD/FVG/OB outcome rows, filtered into `2545` OB-variant rows with complete `high/low/midpoint` levels, `1116` realized trades, providers `Binance/Bybit/IBKR/Kraken/yfinance`, and symbols `BTCUSDT/SPY/XBTUSD`. Runtime-facing artifacts include schema, field mapping, CSV rows, per-regime/provider stats, and parity check; every row remains non-standalone (`actionable_true_rows=0`) and missing levels/CISD/displacement fail closed. Aggregate expectancy is negative (`win_rate=0.313620`, `expectancy_r=-0.078349`) and the source universe lacks `NQ/ES/YM/RTY` plus `XAUUSD`, so this is structure/context evidence only, not a promotable generic factor. Decision: `incubate`, not promotion; next useful work is explicit breaker/rejection confirmation rows and broader instrument coverage before BBN/CatBoost/execution-tree admission.
- Handoff `2026-05-15 / codex-dense-feedback-materialization-trace-v1`: the replay-to-current export gap for the exact `IBKR QQQ 5m` dense split is no longer detached-only. The minimal exact-split export/runtime fix is now merged into main repo source (`src/application/orchestration/structural_playbook.rs`, `src/state/types.rs`), and focused main-worktree verification passed `cargo test target_export -- --nocapture` with 7 export-focused tests green. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T075749+0800-codex-dense-feedback-materialization-trace-v1/summaries/terminal_decision_summary.md`. On the same copied `DENSE_KLINE_BRANCH` state, runtime still materially improved to `export-structural-path-ranking-target rows=5`, `rows_with_raw_path_score=2`, `rows_with_calibrated_path_prob=2`, `rows_with_execution_gate_status=2`, plus `policy-training-status runtime_selection=enabled_history_ready runtime_mode=prefer_history runtime_source=history runtime_matches=5`. The same lane was then re-seeded toward the user's intraday `range -> expansion` laws with `BTCLeaderBreakV4`, `MTFTrendStack`, and `VolBreakoutSized`; the strongest measured packet was `VolBreakoutSized` with `robust_sharpe=1.3390`, `worst_profit_pct=25.0200`, `pareto_dominated_by=none`, but `min_position_size=FAIL`. The fresh expansion manifest imported back into the copied ict-engine state with `n_ok=3`, `auto-quant-prior-init --force` again passed `evidence_value_gate_passed=true`, and the final prior shifted to `[0.3281516210275014, 2.1916067652727076e-7, 0.6718481598118221]`. A fresh patched `analyze` on the real `15m/5m/1m` `IBKR` ladder still reports `Comparable run, but factor backlog remains`, but now with `market_state=RangeConsolidation/Distribution`, `pre_bayes_gating_status=pass_hard`, `pre_bayes_quality_score=0.820`, `execution_score=0.6598776045880123`, `ranker=history_path/catboost/ready`, and execution still `observe/transition_guardrail/guarded`. A di... [truncated]
- Handoff `2026-05-15 / codex-prior-consumption-gap-v1`: current-turn live readback confirms fresh AQ prior mutations are visible but are not the active downstream blocker on the dense breakout lane. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T075749+0800-codex-dense-feedback-materialization-trace-v1/summaries/terminal_decision_summary.md`. Reused copied `IBKR` states `state_probe_minpos_v1` and `state_probe_hybrid_macdconfirm_single_v1`; live `cargo run --quiet -- workflow-status --symbol DENSE_KLINE_BRANCH --state-dir ... --agent` on both stayed materially identical at `blocking_status=bridge_needs_confirmation`, `blocking_reason=pre_bayes passed but bridge gap 0.038 is below confirmation threshold`, top path gate `observe`, and execution hint `execution_guarded_due_to_high_transition_hazard`. Source trace through `src/application/belief/pipeline_builder.rs`, `src/application/execution/inputs.rs`, and `src/application/execution/artifact.rs` shows the downstream lane only moves if prior-init changes selected win probability, bridge gap, or execution-readiness inputs; on this branch it did not. Decision: `handoff`, not promotion; next useful work is a non-duplicate bridge-gap widening or execution-readiness lift, not more prior-only replays of the same copied state.
- Handoff `2026-05-15 / codex-dense-symbol-aligned-ibkr-replay-v1`: correcting the replay artifact symbol from `QQQ` to workflow symbol `DENSE_KLINE_BRANCH` still did not materialize the exact split as a current target row on the copied mixed dense state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T022020+0800-codex-dense-symbol-aligned-ibkr-replay-v1/summaries/terminal_decision_summary.md`. The corrected replay ingested all `1351` real `IBKR QQQ 5m` rows successfully, but downstream export still produced only `2` current target rows, with the exact split path absent from both current target rows and history rows for that copied state. Policy/training regressed to `runtime_selection=enabled_no_matching_scores`, `raw_scored_mature=0/30`, `production_validation=0/30`. Decision: `handoff`, not promotion; this shows there is a replay-to-target materialization gap beyond the earlier provider and scoring issues.
- Handoff `2026-05-15 / codex-branch-aware-calibration-bucket-probe-v1`: an isolated code-path probe in the detached worktree showed that calibration buckets are currently too coarse and ignore branch path, but branch-aware buckets alone are not enough. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T020854+0800-codex-branch-aware-calibration-bucket-probe-v1/summaries/terminal_decision_summary.md`. In the prototype, I changed calibration buckets from `symbol:regime` to `symbol:regime:path_id`. That immediately stopped the generic/exact split from sharing the same calibrated probability: the generic dense branch kept a fitted calibration (`raw_path_score=0.746045`, `calibrated_path_prob=0.750000`, `path_prob_lower_bound=0.432415`), while the exact `IBKR` split no longer inherited that fitted calibration and had blank calibration outputs. Decision: `handoff`, not promotion; the direction is correct, but it reveals a deeper issue that the exact split path is not accumulating enough calibratable observations in the target-history surface.
- Handoff `2026-05-15 / codex-dense-exact-selection-counterfactual-v1`: forcing the exact `IBKR` split row above the generic dense row by raw score still did not make workflow select it. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T015516+0800-codex-dense-exact-selection-counterfactual-v1/summaries/terminal_decision_summary.md`. On an isolated copy of the real `IBKR` exact-split provider-probe state, the slice applied a manual candidate-set score override: generic dense branch `raw_path_score=0.10`, exact `IBKR` split branch `raw_path_score=0.99`, scenario path `raw_path_score=0.10`, then reran `analyze + workflow-status` on the same real `IBKR 15m/5m/1m` ladder. The exact split still was not selected; `closed_loop_path_id` and `bundle_path_id` remained the generic branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1`. The key reason is that all current candidate rows still shared the same `calibrated_path_prob=0.432258` and `path_prob_lower_bound=0.367211`, so selection kept falling through to later ordering logic and the generic branch still won. Decision: `handoff`, not promotion; next useful work is diagnosing why current candidate rows are flattened to the same calibrated probability / lower bound, not another raw-score-only override.
- Handoff `2026-05-15 / codex-dense-execution-hazard-diagnosis-v1`: the current dense lane is blocked by execution selection plus readiness, not by missing provider diversity. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T014641+0800-codex-dense-execution-hazard-diagnosis-v1/summaries/terminal_decision_summary.md`. The provider-specific exact split rows are present in current structural targets, but `workflow-status` still selects the generic branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1` in every runnable provider state. In the current packets, all candidate rows share the same `calibrated_path_prob=0.432258` and `path_prob_lower_bound=0.367211`, so selection falls through and the generic branch still wins over the exact split rows. That selected generic branch then fails execution readiness before action: `execution_readiness=0.3102` on `IBKR`, `0.3115` on `YF`, and `0.3942` on `Kraken`, each below the `0.45` observe threshold, with `hybrid_transition_hazard` around `0.61-0.63`; temporal control then forces `transition_guardrail` with `execution_guarded_due_to_high_transition_hazard`. Decision: `handoff`, not promotion; next useful work is execution-hazard mitigation or selection-logic diagnosis, not more same-shape provider reruns on this dense branch.
- Handoff `2026-05-15 / codex-dense-provider-matrix-synthesis-v1`: the current dense exact-split lane is no longer provider-underexplored; across all runnable providers it looks execution-side limited rather than provider-choice limited. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T013822+0800-codex-dense-provider-matrix-synthesis-v1/summaries/terminal_decision_summary.md`. Fresh provider-backed readbacks in this session now cover `IBKR`, `YF`, and `Kraken`, while `TradingViewRemix` remains blocked at provider health. The three runnable probes all stayed `decision_summary=Observe only`, all three execution branches stayed `transition_guardrail`, and all three kept the same terminal blocker family: `execution_guarded_due_to_high_transition_hazard` plus `user_selected_historical_data_missing`, while runtime score consumption remained live through `candidate_set/catboost/ready`. Decision: `handoff`, not promotion; next useful work is execution-hazard diagnosis or a genuinely different factor/split, not more same-shape provider reruns on this dense branch.
- Handoff `2026-05-15 / codex-dense-kline-kraken-1h-provider-probe-v1`: real `Kraken` ladders also did not improve the exact dense split beyond `observe/transition_guardrail`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T013014+0800-codex-dense-kline-kraken-1h-provider-probe-v1/summaries/terminal_decision_summary.md`. Starting from the same exact dense split replay state from `20260515T002356+0800-codex-dense-kline-ibkr-5m-split-replay-v1`, the slice converted the preserved dense-kline normalized Kraken provider CSVs into real `kraken_xbtusd_{30m,15m,1m}.clean.json` ladders and ran `analyze + workflow-status + policy-training-status` on that isolated state. Runtime remained ready (`runtime_selection=enabled_candidate_set_ready`, `runtime_mode=prefer_history`, `score_model_family=catboost`, `runtime_matches=3`), but analyze still landed at `decision_summary=Observe only`, `pre_bayes_gating_status=pass_neutralized`, `pre_bayes_quality_score=0.551`, execution branch `transition_guardrail`, and blocked reason `user_selected_historical_data_missing`. Decision: `handoff`, not promotion; the current dense exact split is now provider-checked against `IBKR`, `yfinance`, and `Kraken`, and still looks execution-side limited.
- Handoff `2026-05-15 / codex-macd-zero-line-ibkr-downstream-v1`: existing positive MACD rooted branch `TrendExpansion -> MomentumPersistence -> macd_zero_line_reclaim -> macd_zero_line_reclaim_long_v1` was continued through the real downstream chain instead of being discussed abstractly. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T075749+0800-codex-dense-feedback-materialization-trace-v1/summaries/terminal_decision_summary.md`. Source AQ packet came from the preserved six-provider MACD run root `20260512T193452+0800-codex-macd-momentum-branch-keyed-six-provider-aq-v1`, using `MACD zero-line reclaim - IBKR SPY 1h` with measured AQ metrics `sharpe=0.7144`, `sortino=1.7014`, `calmar=23.4171`, `total_profit_pct=2.5000`, `max_drawdown_pct=-1.7136`, `trade_count=16`, `win_rate_pct=56.2500`, `profit_factor=2.2409`. A single-strategy `strategy_library` manifest was built from that real AQ packet, imported cleanly (`matched=1 mismatches=[] n_ok=1`), prior-initialized successfully with `evidence_value_gate_passed=true`, and replayed through explicit `IBKR SPY 1d/4h/1h` analyze/workflow surfaces. Downstream result was still weak: `Comparable run, but factor backlog remains`, `pre_bayes_gating_status=observe_only`, `pre_bayes_quality_score=0.145`, `execution_score=0.33014253779990343`, `execution=observe/transition_guardrail/guarded`, and `ranker=unknown/unknown/not_ready`. Runtime rationale shows the problem is not “MACD unused” but that this standalone MACD packet currently suffers regime/factor alignment conflict plus low evidence quality, so its BBN input is neutralized. Decision: `handoff`, not promotion; this makes MACD a real branch-path-supported candidate, but presently weaker than the active ATR-sized breakout lane. Next useful work is to treat MACD as a supporting/confirmation factor or craft a regime-conditioned MACD hybrid rather than promoting this standalone packet as the main Board B lane.
- Handoff `2026-05-15 / codex-breakout-cross-provider-evidence-breakdown-v1`: real `evidence-quality-breakdown` reports were captured for the active `VolBreakoutSizedV005` derived `IBKR`, `YF`, and `Kraken` states to determine whether the lane was still quality-blocked. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T075749+0800-codex-dense-feedback-materialization-trace-v1/summaries/terminal_decision_summary.md`; raw artifacts live under `checks/evidence_quality_breakdown_{ibkr,yf,kraken}_minpos_v1.json`. All three provider-backed states are already `pass_hard` with good evidence quality (`IBKR=0.8198622518353481`, `YF=0.767704870349536`, `Kraken=0.7654198026381517`), zero soft-evidence divergence, and no active directional/uncertainty/MTF penalties. The common bottleneck is the small bridge separation (`0.03786603634574698`, `0.040174638005106844`, `0.04140488341659909`) staying below the default bridge-clear threshold, not weak evidence quality. Decision: `handoff`, not promotion; next useful work should target widening bridge gap and then real execution readiness, not simply trying to raise already-hard-pass evidence quality.
- Handoff `2026-05-15 / codex-breakout-readiness-gap-quantification-v1`: the active breakout lane's remaining readiness gap was quantified instead of guessed. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T075749+0800-codex-dense-feedback-materialization-trace-v1/summaries/terminal_decision_summary.md`. Across the replayed `IBKR`, `YF`, and `Kraken` derived states, current `execution_readiness` remains in the low `0.52` band while `execution_ready` requires `0.65`. Quantitatively, neither a pure win-probability increase nor a pure evidence-quality increase is enough by itself: `IBKR` would only reach `0.648408` if win-probability alone went to `1.0`, and only `0.610437` if evidence quality alone went to `1.0`; `YF` caps at `0.639867` / `0.626627`; `Kraken` caps at `0.623131` / `0.636297`. All three also carry a negative inferred OU / overextension residual term (`-0.051026`, `-0.034793`, `-0.050443`). Decision: `handoff`, not promotion; next useful work should assume a combined requirement: widen directional separation / bridge gap and reduce OU-overextension drag, rather than trying to solve the lane by lifting only one scalar like evidence quality.
- Handoff `2026-05-15 / codex-v005-macdconfirm-hybrid-v1`: a new isolated hybrid `VolBreakoutSizedV005MacdConfirm` was tested as the most conservative breakout-plus-MACD confirmation variant. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T075749+0800-codex-dense-feedback-materialization-trace-v1/summaries/terminal_decision_summary.md`. The hybrid preserved the active `VolBreakoutSizedV005` regime root, Donchian breakout, 4h EMA regime filter, and `ATR`-based sizing, and only added a lightweight MACD directional confirmation (`MACD > signal`, `MACD > 0`, rising histogram). AQ metrics were slightly better than `V005` (`robust_sharpe=1.3285` vs `1.3268`, `worst_profit_pct=45.7100` vs `44.4700`, `worst_dd_pct=-6.6041` vs `-6.9997`, `avg_position_pct=6.7039`, size gate still PASS). It was then imported into a fresh derived `IBKR` state, prior-initialized successfully, and replayed through explicit `IBKR 15m/5m/1m` analyze. The hybrid did shift the BBN prior (`[0.3388491115786031, 3.5867919239193766e-44, 0.6611508884213969]`), but it did **not** materially improve downstream state: `Comparable run, but factor backlog remains`, `pre_bayes_gating_status=pass_hard`, `pre_bayes_quality_score=0.820`, `execution_score=0.6598776045880123`, `execution=observe/transition_guardrail/guarded`, and tune target stayed `session_liquidity`. Decision: `handoff`, not promotion; this is a useful negative result showing that a light MACD confirmation is not a strong enough directional-separation change for the active breakout lane.
- Handoff `2026-05-15 / codex-v005-macdconfirm-hybrid-v1`: a new isolated hybrid `VolBreakoutSizedV005MacdConfirm` was tested as the most conservative breakout-plus-MACD confirmation variant. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T075749+0800-codex-dense-feedback-materialization-trace-v1/summaries/terminal_decision_summary.md`. The hybrid preserved the active `VolBreakoutSizedV005` regime root, Donchian breakout, 4h EMA regime filter, and `ATR`-based sizing, and only added a lightweight MACD directional confirmation (`MACD > signal`, `MACD > 0`, rising histogram). AQ metrics were slightly better than `V005` (`robust_sharpe=1.3285` vs `1.3268`, `worst_profit_pct=45.7100` vs `44.4700`, `worst_dd_pct=-6.6041` vs `-6.9997`, `avg_position_pct=6.7039`, size gate still PASS). It was then imported into a fresh derived `IBKR` state, prior-initialized successfully, and replayed through explicit `IBKR 15m/5m/1m` analyze. The hybrid did shift the BBN prior (`[0.3388491115786031, 3.5867919239193766e-44, 0.6611508884213969]`), but it did **not** materially improve downstream state: `Comparable run, but factor backlog remains`, `pre_bayes_gating_status=pass_hard`, `pre_bayes_quality_score=0.820`, `execution_score=0.6598776045880123`, `execution=observe/transition_guardrail/guarded`, and tune target stayed `session_liquidity`. A later hybrid-only single-strategy prior probe (`--strategies VolBreakoutSizedV005MacdConfirm`) also failed to move the downstream readback materially, proving that the negative result is not just due to multi-strategy prior blending. Decision: `handoff`, not promotion; this is a useful negative result showing that a light MACD confirmation is not a strong enough directional-separation change for the active breakout lane.
- Blocked `2026-05-15 / codex-dense-kline-tvr-health-refresh-v1`: current TradingViewRemix provider health is still unavailable, so the dense low-timeframe TVR continuation lane should not be scheduled yet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T011021+0800-codex-dense-kline-tvr-health-refresh-v1/summaries/terminal_decision_summary.md`. I re-ran the real provider status probe with `ict-engine provider-status --provider tradingview_mcp --agent`; the output still says `market_data:0/1 ready`, `ready=false`, `status=configured_runtime_unhealthy`, and `reason=tradingview_mcp_connectivity_probe_failed`, with the same prompts to re-enter `ICT_ENGINE_TVREMIX_MCP_API_KEY` and retry a lightweight MCP health check such as `tools/list`. Decision: `blocked`, not promotion; TVR remains a provider-availability blocker for new dense small-cycle work until the probe is healthy.
- Handoff `2026-05-15 / codex-dense-kline-yf-5m-split-replay-v1`: a provider/timeframe-specific public-provider dense-kline exact split built only from real `YF NQ=F 5m` trades reached current target rows and candidate-set CatBoost consumption, but execution still remained `observe/transition_guardrail`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T005425+0800-codex-dense-kline-yf-5m-split-replay-v1/summaries/terminal_decision_summary.md`. Starting from the matured mixed dense replay state from `20260513T185726+0800-hermes-dense-kline-real-trade-feedback-replay-v1`, it replayed `270` exact split trades from `yf_nq_5m_trades.csv`. The exact row now appears with full branch fields for `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_yf_5m -> dense_kline_upbar_reclaim_yf_nq_5m_v1`, scored at `raw_path_score=0.246916`, `calibrated_path_prob=0.432258`, `path_prob_lower_bound=0.367211`, with runtime forced to `candidate_set_only` and `enabled_candidate_set_ready`. Real yfinance `30m/15m/5m` analyze still landed at `decision_summary=Observe only`, `pre_bayes_gating_status=observe_only`, `pre_bayes_quality_score=0.394`, execution branch `transition_guardrail`, and blocked reason `user_selected_historical_data_missing`. Decision: `handoff`, not promotion; this exact public-provider split is also execution-side limited and should not be replayed unchanged.
- Handoff `2026-05-15 / codex-dense-kline-ibkr-5m-provider-probe-v1`: real `IBKR 15m/5m/1m` candles did not improve the exact `dense_kline_upbar_reclaim_ibkr_qqq_5m_v1` split branch. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T004428+0800-codex-dense-kline-ibkr-5m-provider-probe-v1/summaries/terminal_decision_summary.md`. Starting from the exact dense split replay state from `20260515T002356+0800-codex-dense-kline-ibkr-5m-split-replay-v1`, the slice ran `analyze + workflow-status + policy-training-status` against a real `IBKR` `15m/5m/1m` `QQQ` ladder. Runtime remained ready (`runtime_selection=enabled_candidate_set_ready`, `runtime_mode=prefer_history`, `score_model_family=catboost`, `runtime_matches=3`) and the exact row stayed present with `raw_path_score=0.244879`, `calibrated_path_prob=0.432258`, and `path_prob_lower_bound=0.367211`, but the result still landed at `decision_summary=Observe only`, `pre_bayes_gating_status=pass_neutralized`, `pre_bayes_quality_score=0.444`, execution branch `transition_guardrail`, and blocked reason `user_selected_historical_data_missing`. Decision: `handoff`, not promotion; this exact dense split is now sufficiently `IBKR`-probed and remains execution-side limited.
- Handoff `2026-05-15 / codex-dense-kline-ibkr-5m-split-replay-v1`: a provider/timeframe-specific dense-kline exact split built only from real `IBKR QQQ 5m` trades reached current target rows and runtime CatBoost consumption, but execution still remained `observe/transition_guardrail`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T002356+0800-codex-dense-kline-ibkr-5m-split-replay-v1/summaries/terminal_decision_summary.md`. This slice honored the operator's local `IBKR` preference without changing consumer-facing defaults. Starting from the matured mixed dense replay state from `20260513T185726+0800-hermes-dense-kline-real-trade-feedback-replay-v1`, it replayed `1351` exact split trades from `ibkr_qqq_5m_trades.csv`, lifting total mature structural-feedback observations in the copied state to `1501/30`. The exact row now appears with full branch fields for `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim_ibkr_5m -> dense_kline_upbar_reclaim_ibkr_qqq_5m_v1`, scored at `raw_path_score=0.244879`, `calibrated_path_prob=0.432258`, `path_prob_lower_bound=0.367211`, with runtime `enabled_candidate_set_ready`. Demo analyze still landed at `decision_summary=Comparable run, but factor backlog remains`, `pre_bayes_gating_status=pass_neutralized`, execution branch `transition_guardrail`, and hint `execution_guarded_due_to_low_remaining_regime_duration`. Decision: `handoff`, not promotion; next useful low-timeframe step is a real `IBKR 15m/5m/1m` provider probe or execution-hazard diagnosis, not repeating this exact split replay unchanged.
- Handoff `2026-05-15 / codex-low-hazard-ibkr-5m-provider-probe-v1`: real `IBKR 15m/5m/1m` candles did not improve the exact `low_hazard_reclaim_ibkr_qqq_5m_v1` split branch. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T000500+0800-codex-low-hazard-ibkr-5m-provider-probe-v1/summaries/terminal_decision_summary.md`. Starting from the exact split replay state, the slice ran `analyze + workflow-status` against a real `IBKR` `15m/5m/1m` QQQ ladder. The result still landed at `decision_summary=Observe only`, `pre_bayes_gating_status=pass_neutralized`, `pre_bayes_quality_score=0.444`, and execution branch `transition_guardrail`, with `blocking_reason=user_selected_historical_data_missing`. Decision: `handoff`, not promotion; this exact low-hazard split is now sufficiently provider-probed and remains execution-side limited.
- Handoff `2026-05-14 / codex-low-hazard-ibkr-5m-split-replay-v1`: a provider/timeframe-specific low-hazard split based only on `IBKR QQQ 5m` real trades reached exact-branch visibility and scoring, but execution remained observe/transition_guardrail. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T233015+0800-codex-low-hazard-ibkr-5m-split-replay-v1/summaries/terminal_decision_summary.md`. The split replay inserted `696` feedback records from `ibkr_qqq_5m_real_trades.csv`, produced an exact split branch row in live target rows with populated branch fields, and scored that row through a registered direct-model artifact (`weighted_feature_sum_v1`). However, the exact split still ended at `decision_summary=Observe only`, `pre_bayes_gating_status=pass_neutralized`, `execution_gate_status=execution_observe_only`, and `execution_tree_branch=transition_guardrail`, with `decision_hint=execution_guarded_due_to_low_remaining_regime_duration`. Decision: `handoff`, not promotion; the exact split is valid but still not execution-promotable.
- Drop `2026-05-14 / codex-brooks-lowtf-source-packet-v1`: a fresh low-timeframe provider-backed Brooks source/outcome packet was successfully built from raw local `IBKR / yfinance / Kraken` candles, but the current low-timeframe Brooks heuristic is strongly negative and should not proceed downstream. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T231100+0800-codex-brooks-lowtf-source-packet-v1/summaries/terminal_decision_summary.md`. The packet produced `2033` candidate rows and `1132` realized trades across `1m/5m`, with provider mix `yfinance/YF=1220`, `IBKR=412`, `Kraken=401`, and explicit note that local `TradingViewRemix` low-timeframe candles were not found for this slice. The dominant realized rows are negative: every `fvg_retest` slice is `win_rate=0.0 / expectancy=-1.0R`, and `order_block_retest` slices stay negative across `IBKR`, `yfinance`, and `Kraken` (for example `IBKR 1m win_rate=0.270833 expectancy=-0.1875R`, `yfinance 5m win_rate=0.173285 expectancy=-0.473673R`, `Kraken 5m win_rate=0.160714 expectancy=-0.472989R`). `direct_limit` support is tiny (`4` realized trades total). Decision: `drop`; do not send this low-timeframe Brooks packet into `Auto-Quant -> Pre-Bayes -> BBN -> CatBoost -> execution tree`. The next useful low-timeframe step is a new heuristic, not a replay of this one.
- Handoff `2026-05-14 / codex-brooks-lowtf-feasibility-scan-v1`: the current provider-backed realized-row universe cannot satisfy the user’s “多练小周期的” direction by simple Brooks threshold tuning because it contains no realized low-timeframe direct-limit rows. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T225900+0800-codex-brooks-lowtf-feasibility-scan-v1/summaries/terminal_decision_summary.md`. A direct scan over `atr_cisd_fvg_ob_entry_outcome_rows.csv` found `542` realized `direct_limit` rows total, all at timeframe `1h`, and `0` realized rows at `1m/5m/15m`. Decision: `handoff`, not promotion; the next useful low-timeframe step is to build a fresh provider-backed low-timeframe source/outcome packet from raw `IBKR`/`yfinance` candles rather than re-slicing the existing `1h` outcome universe.
- Handoff `2026-05-14 / codex-brooks-balanced-ibkr-trueframe-probe-v1`: a corrected semantic `IBKR` ladder still did not promote the exact balanced Brooks branch. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T225100+0800-codex-brooks-balanced-ibkr-trueframe-probe-v1/summaries/terminal_decision_summary.md`. This slice honored the user’s local `IBKR` preference without changing consumer-facing defaults, using a true `1d -> 1h -> 15m` `QQQ` ladder built from real `IBKR` files (`1d` direct plus `1h/15m` resampled from `5m`). The probe still landed at `decision_summary=Observe only`, `pre_bayes_gating_status=pass_neutralized`, execution branch `block_crowded`, and workflow `blocking_reason=user_selected_historical_data_missing`. Decision: `handoff`, not promotion; a cleaner `IBKR` timeframe hierarchy does not resolve the current execution-side Brooks blocker by itself.
- Handoff `2026-05-14 / codex-brooks-balanced-ibkr-mtf-probe-v1`: the user’s local `IBKR` preference was honored for an opt-in probe, but the exact balanced Brooks branch still stayed `Observe only / transition_guardrail`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T223842+0800-codex-brooks-balanced-ibkr-mtf-probe-v1/summaries/terminal_decision_summary.md`. Starting from the balanced replay state, the slice resampled real `IBKR QQQ 5m 30d` candles into `30m/15m/5m` JSON ladders and ran `analyze + workflow-status` from a clean detached HEAD worktree. The result still landed at `decision_summary=Observe only`, `pre_bayes_gating_status=pass_neutralized`, execution branch `transition_guardrail`, and workflow `blocking_reason=user_selected_historical_data_missing`. Decision: `handoff`, not promotion; `IBKR` is now ruled in as a valid local probe but ruled out as the thing that will clear the current execution blocker by itself.
- Handoff `2026-05-14 / codex-brooks-balanced-yf-mtf-probe-v1`: proper `yfinance` multi-timeframe ladders still did not clear the exact balanced Brooks branch out of `transition_guardrail`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T215256+0800-codex-brooks-balanced-yf-mtf-probe-v1/summaries/terminal_decision_summary.md`. Using clean `30m/15m/5m` `QQQ` and `SPY` ladders on isolated copies of the balanced replay state, both probes stayed at `decision_summary=Observe only`, both retained `pre_bayes_gating_status=pass_neutralized`, and both execution branches stayed `transition_guardrail`. They also surfaced `user_selected_historical_data_missing` in workflow status. Decision: `handoff`, not promotion; proper `yfinance` ladder shape does not resolve the execution-side blocker.
- Handoff `2026-05-14 / codex-brooks-balanced-execution-provider-probe-v1`: real `IBKR / yfinance / Kraken / TradingViewRemix` candles still did not pull the exact balanced Brooks branch out of `transition_guardrail`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T213130+0800-codex-brooks-balanced-execution-provider-probe-v1/summaries/terminal_decision_summary.md`. Starting from the exact-branch replay state for `brooks_second_entry_reversal_balanced_v2`, the slice converted four real provider candle sources into `analyze`-compatible JSON and ran `analyze + workflow-status` against isolated copied states from a clean detached HEAD worktree. All four probes (`IBKR QQQ 5m`, `yfinance QQQ 5m`, `Kraken XBTUSD 1h`, `TradingViewRemix BTC 1h`) stayed at `decision_summary=Observe only`, all four retained `pre_bayes_gating_status=pass_neutralized`, and all four execution branches stayed `transition_guardrail`. Decision: `handoff`, not promotion; the current blocker is not demo-data choice or missing provider diversity, but execution-side readiness / hazard gating for the exact balanced Brooks branch.
- Handoff `2026-05-14 / codex-brooks-balanced-variant-chain-v1`: a wider-support Brooks variant completed the full isolated AQ/Pre-Bayes/CatBoost/exact-branch replay chain and made the exact balanced branch runtime-ready, but still not promotable. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T212245+0800-codex-brooks-balanced-variant-chain-v1/summaries/terminal_decision_summary.md`. The variant `brooks_second_entry_reversal_balanced_v2` used the scan-selected surface `fill_bars=1..10`, `breakout_range_atr>=1.0`, `breakout_body_atr=0.3..1.4`, `close_through_level_atr=0.2..0.9`, `pd_array_type=ALL`, which yielded `120` unique trades across `5` providers and `3` regimes with win rate `0.700000` and expectancy `1.177698R`. The isolated chain ran through `auto-quant-results-import`, `auto-quant-prior-init`, `analyze`, `export-structural-path-ranking-target`, CatBoost train/apply, score apply/register/enable, then exact-branch real-trade replay (`feedback_records_inserted=120`), direct-model fallback scoring, and final runtime readback. The exact balanced branch now appears in current target rows with populated branch fields and `raw_path_score=0.700000`, runtime is `enabled_registered_model_ready`, `ranker_validation_ready=true`, and `observation_validation=120/30`; execution tree directly uses the exact balanced path with `visible=true` and `used=true`. Promotion still fails because execution remains `observe/transition_guardrail/guarded` with `closed_loop_branch_admission.reason=exact_structural_branch_visible_but_not_ready_or_actionable`. Decision: `handoff`, not promotion; the next blocker is execution-side, not support-size or branch-path visibility.
- Handoff `2026-05-14 / codex-brooks-threshold-support-scan-v1`: current widened Brooks packet is not the support ceiling inside existing provider-backed direct-limit realized rows. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T210615+0800-codex-brooks-threshold-support-scan-v1/summaries/terminal_decision_summary.md`. A read-only threshold sweep over `fill_bars_after_breakout`, `breakout_range_atr`, `breakout_body_atr`, `close_through_level_atr`, and `pd_array_type` inclusion found much larger Brooks-like candidate surfaces than the current `13`-trade widened packet. The `support_max` scan surface reached `131` unique trades across `5` providers and `3` regimes with win rate `0.679389` and expectancy `1.109342R`; a more balanced high-win-rate surface still reached `120` unique trades across `5` providers and `3` regimes with win rate `0.700000` and expectancy `1.177698R`. Decision: `handoff`, not promotion; next useful work is to choose a semantically defensible wider Brooks threshold surface and replay it through the exact-branch chain, rather than assuming the current `13`-trade packet is the maximum available support.
- Handoff `2026-05-14 / codex-brooks-execution-readiness-provider-probe-v1`: real provider-data execution probes did not clear the exact Brooks branch out of `transition_guardrail`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T204950+0800-codex-brooks-execution-readiness-provider-probe-v1/summaries/terminal_decision_summary.md`. The slice copied the exact-branch replay state from `20260514T202930...`, converted real `IBKR QQQ 5m 30d` and `Kraken XBTUSD 1h` candles into `analyze`-compatible JSON, and ran `analyze + workflow-status` against those provider candles from a clean detached HEAD worktree because the shared repo worktree was temporarily unbuildable under concurrent edits to `provider_fetch.rs/harness.rs`. Both probes stayed `Observe only`, both execution branches stayed `transition_guardrail`, and both fell back to `pre_bayes_gating_status=pass_neutralized`. This means the current Brooks blocker is not just demo-data choice. Decision: `handoff`, not promotion; next useful work is execution-side readiness / bridge-confirmation / transition-hazard improvement for the exact Brooks branch, not more branch-path plumbing.
- Handoff `2026-05-14 / codex-brooks-real-trade-feedback-replay-v1`: exact-branch replay moved Brooks from “branch path visible only in factor artifacts” to “exact branch row present and scored in live runtime surfaces.” Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T202930+0800-codex-brooks-real-trade-feedback-replay-v1/summaries/terminal_decision_summary.md`. The slice built `brooks_real_trades_wire_v1.jsonl`, replayed `13` exact-branch realized trades through `auto-quant-ingest-real-trades`, and inserted `13` feedback records with exact Brooks `regime_profit_branch_path` plus `structural_feedback.path_id`. After replay, `export-structural-path-ranking-target` produced a current target row whose `path_id` is the exact Brooks branch and whose split branch fields are fully populated. A direct-model fallback registration (`weighted_feature_sum_v1`) then scored that exact row (`raw_path_score=0.846154` in target export), enabled runtime reuse, and made execution tree read the exact Brooks path directly: `execution_tree_trace.json` shows `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, runtime source `registered_model_artifact`, and exact `path_id=TrendExpansion -> BrooksPriceAction -> second_entry_failed_breakout -> brooks_second_entry_reversal_long_v1`. Promotion is still blocked, but for a new reason: execution stays `observe/transition_guardrail/guarded`, `closed_loop_branch_admission.status=fail_closed`, and `reason=exact_structural_branch_visible_but_not_ready_or_actionable`. Decision: `handoff`, not promotion; next useful work is execution-side readiness / transition-hazard improvement for this exact Brooks branch, not more branch-path plumbing.
- Handoff `2026-05-14 / codex-brooks-price-action-provider-outcome-chain-v1`: widened Brooks factor packet reached the real downstream chain for branch `TrendExpansion -> BrooksPriceAction -> second_entry_failed_breakout -> brooks_second_entry_reversal_long_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T200310+0800-codex-brooks-price-action-provider-outcome-chain-v1/summaries/terminal_decision_summary.md`. The widened packet built `26` raw candidates into `13` unique realized trades across `3` providers (`Binance`, `IBKR`, `Bybit`) and `trend/transition/range`, with `11` wins, `2` losses, win rate `0.846154`, expectancy `1.538462R`, and `gate1_positive=true`. Real downstream commands then ran successfully: `auto-quant-results-import`, `auto-quant-prior-init`, `pre-bayes-status`, `analyze`, `export-structural-path-ranking-target`, CatBoost train/apply, `apply-structural-path-ranking-external-scores`, `register-structural-path-ranking-trainer-artifact`, `enable-structural-path-ranking-runtime`, `workflow-status`, `policy-training-status`, and native `factor-research`. Execution-tree evidence confirms the ranker score was visible and used (`path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`), but promotion is still blocked because the live structural target rows remain generic and empty on `regime_profit_branch_path/main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor`, while runtime stays `Observe` with `transition_guardrail` and validation remains `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`. Decision: `handoff`, not promotion; next useful work is rooted-branch propagation or real structural-feedback replay for this exact Brooks branch.
- Blocked `2026-05-14 / codex-brooks-price-action-provider-observation-v1`: built the first provider-backed Brooks observation packet for branch `TrendExpansion -> BrooksPriceAction -> second_entry_failed_breakout -> brooks_second_entry_reversal_long_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T194308+0800-codex-brooks-price-action-provider-observation-v1/summaries/terminal_decision_summary.md`. The slice filtered the existing outcome rows to `entry_model=direct_limit`, `pd_array_type=none`, `realized_trade=true`, deduped repeated rows from `6` raw candidates to `3` unique realized trades, and emitted first-class Brooks fields (`brooks_context=second_entry`, `context_secondary=failed_breakout`, `trigger_level`, `invalidation_level`, `confidence`, `regime_conditioned_win_rate`) into packet/CSV artifacts. The raw expectancy is positive but Gate 1 still fails on support and portability: `3` unique trades, `2` wins, `1` loss, win rate `0.666667`, expectancy `1.0R`, provider count `1`, instrument coverage only `BTCUSDT`, regime coverage only `trend`, `gate1_positive=false`, `gate1_fail_reason=insufficient_unique_trade_count_or_provider_coverage`. Decision: `blocked`, not profitability-negative; no Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree command was run because the packet did not yet satisfy Gate 1 support.
- Handoff `2026-05-14 / codex-brooks-price-action-schema-preflight-v1`: built the first-class schema/material preflight for branch `TrendExpansion -> BrooksPriceAction -> second_entry_failed_breakout -> brooks_second_entry_reversal_long_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T193525+0800-codex-brooks-price-action-schema-preflight-v1/summaries/terminal_decision_summary.md`. The slice landed `brooks_price_action_context_schema.json`, `brooks_price_action_context_field_mapping.json`, and `brooks_price_action_context_provider_coverage.json`, covering required outputs, runtime mappings, provider matrix, zero-config fallback policy, and fail-closed null behavior. No Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree command was run in this slice by design. Decision: `handoff`, not promotion; next unclaimed work is to build provider-backed Brooks observation rows that populate `brooks_context`, `trigger_level`, `invalidation_level`, `confidence`, and `regime_conditioned_win_rate`, then retry Gate 1.
- Blocked `2026-05-14 / codex-brooks-price-action-provider-chain-v1`: attempted Gate 1 provider-backed profitability preflight for branch `TrendExpansion -> BrooksPriceAction -> second_entry_failed_breakout -> brooks_second_entry_reversal_long_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T175239+0800-codex-brooks-price-action-provider-chain-v1/summaries/terminal_decision_summary.md`. Real provider preflight was recorded for `ibkr`, `tradingview_mcp`, `kraken_public`, and `yfinance`, but the current provider-backed source rows `support/docs/experiments/actionable-regime-confidence/runs/20260512T215828+0800-codex-atr-cisd-fvg-ob-entry-outcome-labeling-v1/summaries/atr_cisd_fvg_ob_entry_outcome_rows.csv` still lack first-class Brooks fields (`brooks_context`, `trigger_level`, `invalidation_level`, `regime_conditioned_win_rate`) and the context labels needed to classify `second_entry`, `failed_breakout`, `wedge`, `micro_channel`, `climax_reversal`, `trading_range`, or `trend_day`. Per the runtime evidence contract and May 10 direction lock, no Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree step is allowed before a provider-backed Brooks packet/schema exists. Decision: `blocked`, not profitability-negative; next unclaimed work is to build a provider-backed Brooks packet/schema with exact trigger/invalidation levels and per-regime statistics, then rerun Gate 1.
- Drop `2026-05-14 / codex-fvg-ifvg-lifecycle-training-v1`: split first-class `fvg_ifvg_lifecycle` evidence from the existing combined ATR/CISD/FVG/OB outcome material without borrowing OB or direct-limit profitability. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T143500+0800-codex-fvg-ifvg-lifecycle-training-v1/summaries/fvg_ifvg_lifecycle_terminal_readback.md`. Branch path was preserved as `Transition -> FvgIfvgLifecycle -> fvg_ifvg_lifecycle -> fvg_ifvg_lifecycle_provider_outcome_v1`; structured packet/rows/per-regime CSV were generated and asserted. The isolated FVG retest slice is a negative classifier, not a profitability seed: `1372` lifecycle rows, `496` realized FVG trades, `0` wins, aggregate win rate `0.0`, expectancy `-1.0R`, provider count `5`; trend/range/transition all had `0.0` win rate and stress/other had no rows. Decision: `drop`, not downstream admission; do not send this packet into Pre-Bayes/BBN/CatBoost/execution-tree promotion unless a new FVG/IFVG variant produces positive standalone outcome evidence.
- Handoff `2026-05-14 / codex-market-structure-event-classifier-v1`: first-class `market_structure_event_classifier` slice on branch `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T145000+0800-codex-market-structure-event-classifier-v1/summaries/market_structure_event_classifier_terminal_readback.md`. Gate 1 is positive on provider-backed ATR/CISD direct-limit outcome material: `1161` candidate rows, `542` realized trades, `390` wins, `122` losses, `30` timeouts, win rate `0.719557`, expectancy `1.233393R`, provider count `5`. Auto-Quant import, BBN prior-init, Pre-Bayes refresh, CatBoost train/apply, ranker register/enable, execution-tree readback, and native `factor-research` all ran successfully. Final runtime consumed candidate-set scores (`status=using_candidate_set_scores`, `runtime_matches=3`, `raw_path_score=0.7506586567765241`), and execution tree saw and used the score. Promotion is still blocked because the live structural target rows are generic and lost first-class branch-path fields (`regime_profit_branch_path`, `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`, `profit_factor` empty), while validation remains `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`; native `factor-research` added `113` generic feedback rows but none carried structural feedback refs for this exact branch. Decision: `handoff`, not promotion; next useful step is branch-path propagation into live structural target rows or real structural-feedback observations for the exact factor branch.
- Blocked `2026-05-14 / codex-family-f-vrp-regime-branch-chain-v1`: exact VRP branch `TrendExpansion -> VolatilityCompression -> iv_hv_compression_regime -> vrp_compression_long_v1` preserved through agent-material batch/dispatch/rank, but the factor failed closed before downstream admission because required IV/HV provider inputs are missing. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T225914+0800-codex-family-f-vrp-regime-branch-chain-v1/summaries/family_f_vrp_regime_branch_chain_blocked_readback.md`. Provider probes were real: IBKR `QQQ OPTION_IMPLIED_VOLATILITY`, `QQQ HISTORICAL_VOLATILITY`, `VIX`, and `QQQ 15m` all timed out/empty; yfinance produced `NQ=F 15m` (`3765` rows), `QQQ 15m` (`1066` rows), and `^VIX 1d` (`2604` rows); Kraken `PF_XBTUSD 15m` produced `2000` rows; TradingViewRemix `get_ohlcv` failed through `market-data-harness`. Auto-Quant first failed on missing `NQ/USD 4h`, then same-workspace 4h repair wrote `254` bars and rerun reached the exact strategy's IV loader, where it failed on missing `/tmp/ict-engine-ibkr-probe/qqq.iv.1d.10y.csv`. No Pre-Bayes/BBN/CatBoost/execution-tree promotion was run because there is no valid Auto-Quant result, manifest, or realized-trade export to consume. Decision: `blocked`, not negative profitability; next unclaimed work is real QQQ IV/HV acquisition or a new non-IV/HV compression factor id that must not claim this VRP branch.
- Incubate `2026-05-14 / codex-family-f-vix-rv-proxy-portability-v1`: proxy portability branch `TrendExpansion -> VolatilityCompression -> vix_realized_compression_proxy -> vix_rv_compression_long_v1` preserved through Auto-Quant batch/dispatch/rank and downstream runtime consumers. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T094654+0800-codex-family-f-vix-rv-proxy-portability-v1/summaries/family_f_vix_rv_proxy_portability_incubate_readback.md`. Provider matrix was real but incomplete: yfinance harness/direct produced `NQ=F`, `QQQ`, and `^VIX` rows; IBKR direct via `uv` produced `QQQ 1h` with `640` rows, while the IBKR harness failed on missing `redis`; TradingViewRemix/tradingview MCP failed `get_ohlcv`; Kraken `PF_XBTUSD 15m` failed HTTPS/SSL retries. These provider negatives are provider/runtime portability negatives, not factor-negativity labels. YF runnable profitability was positive but weak: `trade_count=26`, `total_profit_pct=0.05`, `sharpe=0.0409`, `profit_factor=1.0109`, `win_rate_pct=30.7692`. Downstream chain ran real `auto-quant-results-import -> auto-quant-prior-init -> pre-bayes -> export target -> catboost train/apply -> apply/register/enable ranker -> analyze/workflow/policy`; Pre-Bayes was `pass_neutralized`, BBN evidence gate passed, CatBoost runtime was `enabled_candidate_set_ready`, but validation maturity remains `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and execution stays `observe/transition_guardrail/execution_guarded_due_to_high_transition_hazard`. Decision: `incubate`, not promotion; `update_goal=false`.
- Incubate `2026-05-14 / codex-family-f-vix-rv-compression-proxy-v1`: proxy branch `TrendExpansion -> VolatilityCompression -> vix_realized_compression_proxy -> vix_rv_compression_long_v1` continued from the exact VRP blocker lane, with branch keys preserved through batch/dispatch/rank and downstream runtime consumers. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T005148+0800-codex-family-f-vix-rv-compression-proxy-v1/summaries/family_f_vix_rv_compression_proxy_incubate_readback_v2.md`. Gate-1 moved from zero-trade to positive after minimal proxy-only VIX window adjustment (`252d -> 63d`): `trade_count=30`, `total_profit_pct=1.17`, `sharpe=0.9147`, `profit_factor=1.236`. Auto-Quant first failed on missing `NQ/USD 4h`, then failed on missing `vix_1d.csv` inside the unit workspace; both were repaired in-place and dispatch completed. Downstream chain ran real `auto-quant-results-import -> auto-quant-prior-init -> pre-bayes -> export target -> catboost train/apply -> apply/register/enable ranker -> analyze/workflow/policy` with `pre_bayes_gate=pass_neutralized` and `path_ranker_summary.status=using_candidate_set_scores`. Promotion remains blocked: structural path-ranker validation is still `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and execution stays `observe/transition_guardrail/execution_guarded_due_to_high_transition_hazard`. Decision: `incubate`, not promotion.
- Incubate `2026-05-13 / hermes-low-hazard-reclaim-new-factor-v1`: new low-hazard split factor `low_hazard_reclaim_long_v1` on branch `TrendExpansion -> SessionLiquidity -> dense_kline_low_hazard_reclaim -> low_hazard_reclaim_long_v1`, not a rerun of dense upbar reclaim. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T193423+0800-hermes-low-hazard-reclaim-new-factor-v1/summaries/low_hazard_reclaim_new_factor_readback.md`. Ran 5 Auto-Quant material units using existing provider K-lines from IBKR, yfinance, and Kraken. Rank positives: `IBKR QQQ 5m` (`696` trades, win rate `43.2471%`, Sharpe `38.4095`, total profit `5.63%`), `YF NQ=F 5m` (`193`, `41.4508%`, `46.4326`, `0.92%`), `IBKR QQQ 1m` (`722`, `36.5651%`, `26.2328`, `0.37%`), and `Kraken XBTUSD 1m` (`244`, `35.2459%`, `340.7426`, `1.14%`); `YF NQ=F 1m` was negative (`963`, `34.7871%`, `-22.0932`, `-0.22%`). Auto-Quant import accepted `5/5`; BBN prior applied all 5 with `evidence_value_gate_passed=true`; CatBoost trained/applied/registered/enabled, and execution-tree trace consumed the ranker (`visible=true`, `used=true`, `model=catboost`, `runtime_source=candidate_set`, `raw_path_score=0.750659`). Promotion remains blocked: CatBoost was trained from a 3-row pseudo-label structural target with `mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and final execution stayed `observe/transition_guardrail/guarded` with `execution_guarded_due_to_high_transition_hazard`. Decision: `incubate`, not promotion; next unclaimed work is real per-trade export/replay for this exact low-hazard branch, then re-check validation and transition hazard.
- Handoff `2026-05-13 / hermes-low-hazard-reclaim-real-trade-feedback-replay-v1`: real per-trade replay completed for `low_hazard_reclaim_long_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T193423+0800-hermes-low-hazard-reclaim-new-factor-v1/summaries/low_hazard_reclaim_real_trade_feedback_replay_readback.md`. Exported `2818` real Freqtrade rows from IBKR QQQ 1m/5m, Kraken XBTUSD 1m, and YF NQ=F 1m/5m with exact `profit_abs_sum == profit_total_abs` on each provider slice, then replayed `150` actual trades into structural feedback. Runtime readback now shows `observation_validation=150/30 ready=true` and `raw_scored_mature=11180/30 ready=true`, but live ranker surface still sits at `enabled_no_matching_scores` and execution remains `execution_guarded_due_to_high_transition_hazard`. Decision: `incubate`, not promotion; next unclaimed work is current-score adapter repair or a tighter branch split, not aggregate replay.
- Handoff `2026-05-14 / codex-low-hazard-current-score-adapter-v1`: repaired the current-score adapter for exact branch `TrendExpansion -> SessionLiquidity -> dense_kline_low_hazard_reclaim -> low_hazard_reclaim_long_v1` after analyze changed the live `candidate_set_id` from `...fe8c30d05ddfaf6a` to `...b16b0ca24f8cb2a2`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T110832+0800-codex-low-hazard-current-score-adapter-v1/summaries/low_hazard_current_score_adapter_terminal_readback.md`. Real commands `apply-structural-path-ranking-external-scores -> enable-structural-path-ranking-runtime -> analyze -> workflow-status -> pre-bayes-status -> policy-training-status -> export target` all exited `0`. Final runtime consumed the current candidate-set scores (`path_ranker_summary.status=using_candidate_set_scores`, `runtime_matches=2`, `score_model_family=catboost`, `score_source=post_analyze_current_candidate_set_model`, `raw_scored_mature=11482/30`, `production_validation=11482/30`, `observation_validation=150/30`) and execution-tree trace confirms `path_ranker_score_visible_to_execution_tree=true` and `path_ranker_score_used_by_execution_tree=true`. Promotion remains blocked: the exact low-hazard branch received `raw_path_score=0.0`, the latest structural execution candidate stayed `execution_observe_only`, `ready=false`, and closed-loop branch admission stayed `fail_closed/transition_guardrail`. Decision: `handoff`, not promotion; next unclaimed work is a comparable exact-branch current scorer or tighter low-hazard branch split, not another adapter-only rerun.
- Handoff `2026-05-14 / codex-low-hazard-current-scorer-root-cause-v1`: diagnosed why the repaired current-score adapter still gives exact branch `TrendExpansion -> SessionLiquidity -> dense_kline_low_hazard_reclaim -> low_hazard_reclaim_long_v1` a CatBoost `raw_path_score=0.0`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260514T134450+0800-codex-low-hazard-current-scorer-root-cause-v1/summaries/low_hazard_current_scorer_root_cause_terminal_diagnostic.md`. The model uses only `structural_baseline_score`; model inspection showed all inspected trees split at `structural_baseline_score > 0.5201009511947632`. Current exact row has `structural_baseline_score=0.390022` from `0.70 * current_posterior(0.388357) + 0.30 * experience_prior(0.393907)`, so it deterministically falls into the `0.0` leaf even though branch-history rows often carry `0.750659` and `target_policy_probability_lower_bound=0.750659` is present. Decision: `handoff`, not promotion; next unclaimed work is a comparable current-candidate scorer that can use exact-branch feedback-history features, a tighter low-hazard split, or a target-surface diagnostic separating live composite from feedback support. Do not rerun the adapter-only slice.
- Handoff `2026-05-13 / hermes-family-h-dense-kline-trade-export-v1`: actual per-trade export for dense positive 1m/5m branch completed before any feedback replay. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T184800+0800-hermes-family-h-dense-kline-trade-export-v1/summaries/dense_kline_trade_export_readback.md`. Exported real `bt.results["strategy"][strategy]["trades"]` rows from the same Freqtrade/Auto-Quant workspaces for `IBKR QQQ 5m` (`1351` rows), `IBKR QQQ 1m` (`1272`), `YF NQ=F 5m` (`270`), `YF NQ=F 1m` (`1318`), and `Kraken XBTUSD 1m` (`348`). `profit_abs` row sums exactly match Freqtrade `profit_total_abs`, proving these are real per-trade rows rather than aggregate summary reconstruction. No feedback replay or promotion was claimed. Next unclaimed work may replay only these exported trade CSV rows into structural feedback with branch path + provider/timeframe provenance; do not use the earlier synthetic feedback files from `hermes-family-h-dense-kline-feedback-maturity-v1`.
- Blocked `2026-05-13 / hermes-family-h-dense-kline-feedback-maturity-v1`: halted before claiming maturity because dense Auto-Quant rank metrics are aggregate Freqtrade summaries, not individual realized trade records. `total_profit_pct` is account-level total profit percent from `100000 USD` starting balance (example: `IBKR QQQ 5m` final balance `106889.438 USD`, total profit `6.89%`, CAGR `80.96%` over 41 days), while Sharpe is Freqtrade's annualized summary metric and is inflated/unstable on dense intraday short windows and tiny per-trade edge. Evidence under `support/docs/experiments/actionable-regime-confidence/runs/20260513T183600+0800-hermes-family-h-dense-kline-feedback-maturity-v1`; partial synthetic feedback generation is non-authoritative and must not be used for promotion. Next unclaimed work should export actual per-trade results/trades CSV from Auto-Quant/Freqtrade, then replay only real realized trade rows into structural feedback.
- Incubate `2026-05-13 / hermes-family-h-dense-kline-regime-branch-v1`: user-requested dense K-line continuation for branch `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T181830+0800-hermes-family-h-dense-kline-regime-branch-v1/summaries/dense_kline_regime_branch_terminal_readback.md`. Ran 11 Auto-Quant material units across 1m/5m/15m/30m where data was available: `YF NQ=F 1m/5m/15m/30m`, `IBKR QQQ 1m/5m/15m/30m`, and `Kraken XBTUSD 1m/15m/30m`; TradingViewRemix/TVR QQQ `1m/5m/15m/30m` was attempted but `tradingview_mcp get_ohlcv` failed for all four intervals. Branch fields survived batch/dispatch/rank as first-class `main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor`. Best dense positives: `IBKR QQQ 5m` (`1351` trades, win rate `40.3405%`, Sharpe `34.83`, total profit `6.89%`), `IBKR QQQ 1m` (`1272`, `38.2862%`, `112.3034`, `1.87%`), `YF NQ=F 5m` (`270`, `41.8519%`, `93.5579`, `1.71%`), `YF NQ=F 1m` (`1318`, `37.2534%`, `87.6718`, `0.87%`), and `Kraken XBTUSD 1m` (`348`, `34.7701%`, `246.3706`, `0.88%`). 15m/30m slices were mostly negative. Auto-Quant import accepted `11/11`, BBN prior init applied `11` with evidence gate passed, CatBoost trained/applied/registered/enabled, and execution-tree trace shows `path_ranker_score_visible_to_execution_tree=true`, `used=true`, `model=catboost`. Promotion remains blocked: ranker validation is still `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, and final execution stayed `observe/transition_guardrail/guarded`. Decision: `incubate`, not promotion; next unclaimed work should create comparable structural-feedback observations for the positive 1m/5m dense branch, repair TVR dense fetch, or split the 15m/30m negative branch instead of promoting the mixed packet.
- Incubate `2026-05-13 / hermes-family-h-vwap-regime-branch-chain-v1`: Gate 4 regime-branch runtime chain for `runtime_density_upbar_reclaim_long_v1` on branch `TrendExpansion -> SessionLiquidity -> runtime_density_upbar_reclaim -> runtime_density_upbar_reclaim_long_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T171220+0800-hermes-family-h-vwap-regime-branch-chain-v1/summaries/hermes_family_h_regime_branch_chain_terminal_readback.md`. Auto-Quant material dispatch completed across `yfinance/YF SPY 1h`, `IBKR SPY 1h`, `TradingViewRemix/TVR QQQ 1h`, and `Kraken XBTUSD 1h`; the branch path survived batch/dispatch/rank as first-class `main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor` fields. Only TradingViewRemix was mildly positive (`21` trades, win rate `42.8571%`, Sharpe `0.7103`, total profit `0.39%`); Kraken/YF/IBKR were negative. Auto-Quant strategy library import accepted `4/4`, BBN prior init applied all `4` strategies with `evidence_value_gate_passed=true`, CatBoost trained and was registered/enabled, and execution-tree trace shows `path_ranker_score_visible_to_execution_tree=true`, `used=true`, `model=catboost`. Promotion is blocked: ranker validation remains `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, final execution stayed `observe/transition_guardrail/guarded`, and provider portability is not positive. Decision: `incubate`, not promotion; next unclaimed work should either generate matured structural-feedback rows for this exact branch or test a stricter SessionLiquidity leaf that avoids the YF/IBKR/Kraken negative-density failure.
- Blocked `2026-05-13 / codex-live-structural-candidates-scorer-probe-v1`: Gate 4 first-class live `structural-candidates:*` scorer probe for the incubating Family A packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T133711+0800-codex-live-structural-candidates-scorer-probe-v1/summaries/live_structural_candidates_scorer_probe_terminal_readback.md`. Exported the live target and confirmed `rows=3`, `candidate_set_id=structural-candidates:FAMILY_A_KILLZONE_MTF_122932_NATIVE:2ce054df90e47839`; current live rows remain unobserved (`mature_rows=0`, `training_weight_rows=0`, `calibrated_rows=0`, `execution_gate_rows=0`) while mature supervised signal exists only in history/admission rows (`history_mature_rows=35`). A direct fallback scorer closed the live candidate-set consumption bridge but all scores were flat `0.5` and explicitly non-promotional. A later offline history CatBoost run trained on `35` mature history rows and registered successfully: `runtime_selection=enabled_candidate_set_ready`, `runtime_mode=prefer_history`, `runtime_source=candidate_set`, `score_model_family=catboost`, `score_source=external_model`, `runtime_matches=3`; however it selected only `structural_baseline_score`, produced flat current live scores (`0.05891652998482881` for all 3 paths), and workflow/policy readback still blocks promotion with `observation_validation=0/30`, no structural-feedback observations, `no_previous_run` risk flags, final action `Observe`, and guarded execution tree. Decision: `blocked`, not promotion; next unclaimed work should create real live structural-feedback observations/comparable candidate-set history that breaks `no_previous_run` and `observation_validation=0/30`, not rerun flat current-row scoring.
- Handoff `2026-05-13 / codex-factor-research-after-live-score-adapter-v1`: Gate 4 structural-feedback probe after live execution-tree candidate-set score consumption. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T125242+0800-codex-factor-research-after-live-score-adapter-v1/summaries/factor_research_after_live_score_adapter_terminal_readback.md`. Ran the workflow-recommended native `factor-research` command against the persisted Family A state after the live path-id adapter probe; no Auto-Quant rerun and no promotion claim. The run wrote `research:FAMILY_A_KILLZONE_MTF_122932_NATIVE:20260513T045342.052Z`, objective `expansion_manipulation`, best factor `trend_momentum`, aggregate return `0.002891322311496955`, and generated/applied `113/113` feedback records. It kept path-ranker consumption on live candidate-set scores (`using_candidate_set_scores`, `runtime_matches=3`) but promotion stayed held: `approved=false`, `status=hold`, reason includes `no_previous_run` and `artifact_consumed_trend_status=no_consumed_validation`; execution gate remained `execution_observe_only`, execution readiness `0.45`, and agent action plan still starts with `Block Promotion` / `OBSERVE trend_momentum`. Decision: `handoff`; next unclaimed work should create comparable research/backtest history for this same live scored candidate set, implement/train a real live `structural-candidates:*` scorer, or repair provider portability before further downstream promotion attempts.
- Handoff `2026-05-13 / codex-live-exectree-score-id-adapter-probe-v1`: Gate 4 downstream score-consumption ID-match probe for the incubating Family A packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T124630+0800-codex-live-exectree-score-id-adapter-probe-v1/summaries/live_exectree_score_id_adapter_probe_terminal_readback.md`. The probe did not rerun Auto-Quant or claim promotion. It exported the post-analyze live execution-tree structural target and found `rows=3`, `candidate_set_id=structural-candidates:FAMILY_A_KILLZONE_MTF_122932_NATIVE:2ce054df90e47839`, while the prior CatBoost/admission scores had targeted `factor-candidate-admission:FAMILY_A_KILLZONE_MTF_122932_NATIVE:curated-auto-quant-v1`. A 3-row adapter-probe score file using the live `candidate_set_id/path_id` values changed workflow consumption from the previous `enabled_no_matching_scores` failure mode to `path_ranker_summary.status=using_candidate_set_scores`, with `applied_path_count=3`, `candidate_set_match_count=3`, `runtime_source=candidate_set`, and `latest_structural_execution_candidate.path_ranker_raw_score=0.557601`. This proves the live execution-tree ID bridge shape, but it is not a trained CatBoost profitability model and not trade-usable: live target rows are still unobserved with `mature_rows=0`, `calibrated_rows=0`, `execution_gate_rows=0`, `training_weight_rows=0`, final action remains `Observe`, and provider portability remains incomplete. Decision: `handoff`; next unclaimed implementation-grade work should make live `structural-candidates:*` scoring first-class and keep admission candidate-pack scores separate from live execution-tree path scores.
- Incubate `2026-05-13 / codex-family-a-killzone-mtf-provider-repair-v1`: Gate 1/provider-window MTF repair for `family_a_killzone_breakout_1h_v1` on branch `TrendExpansion -> KillzoneBreakout -> family_a_killzone_breakout_1h_v1 -> nq_am_breakout_long`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T122932+0800-codex-family-a-killzone-mtf-provider-repair-v1/summaries/family_a_killzone_mtf_provider_repair_terminal_readback.md`. Branch fields survived material/dispatch/rank, initial Auto-Quant failed on missing NQ/USD 4h informative data, and a manual same-workspace MTF repair derived `NQ_USD-4h.feather` before a successful `TomacNQ_KillzoneBreakout` rerun: 91 trades, win rate 70.3297%, profit factor 1.3449, total profit 12.40%, Sharpe 0.0489, max drawdown 8.61%. Native admission wrote 35 structural-path rows; CatBoost model/scores were generated and applied, and ranker runtime enabled with 35 admission-set matches. Downstream chain did not close: provider matrix remains incomplete, analyze/workflow final action stayed `Observe`, execution was guarded by `execution_guarded_due_to_high_transition_hazard`, and live workflow path-ranker status was `enabled_no_matching_scores` because admission candidate-pack scores did not match the analyze execution-tree path ids. Decision: `incubate`, not promotion; next unclaimed work should target paired timeframe material generation or a live execution-tree candidate-id score adapter rather than rerunning the same packet unchanged.
- Blocked `2026-05-13 / codex-family-d-liquidity-sweep-gate1-v1`: non-duplicate Gate 1/downstream smoke for `family_d_liquidity_sweep_reclaim_15m_wide_v1` on branch `TrendTransition -> LiquidityReclaim -> family_d_liquidity_sweep_reclaim_15m_wide_v1 -> liquidity_sweep_reclaim_long`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T121613+0800-codex-family-d-liquidity-sweep-gate1-v1/summaries/family_d_liquidity_sweep_gate1_terminal_readback.md`. Native candidate/admission/training surfaces wrote durable 35-row structural targets and reported `raw_scored_mature=35/30`, `production_validation=35/30`, `trainer_status=runtime_eligible`, but `runtime_selection=disabled`. Auto-Quant material batch/dispatch/rank ran and preserved `regime_profit_branch_path`, `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`, `profit_factor`, and provider provenance into dispatch/rank artifacts. The packet failed before Pre-Bayes/BBN/CatBoost runtime/execution-tree closure because Freqtrade had Yahoo NQ 1h material but no required NQ/USD 4h informative dataframe: `ValueError: Informative dataframe for (NQ/USD, 4h, spot) is empty`. Decision: `blocked`, not promotion; do not rerun the same packet without paired 1h/4h provider material or a parity-preserving material generator.
- Blocked `2026-05-15 / codex-family-d-liquidity-sweep-1h4h-provider-parity-v1`: continued the exact Family D rooted branch `TrendTransition -> LiquidityReclaim -> family_d_liquidity_sweep_reclaim_15m_wide_v1 -> liquidity_sweep_reclaim_long` in a copied workspace to test whether the old blocker was really only material completeness. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T221500+0800-codex-family-d-liquidity-sweep-1h4h-provider-parity-v1/summaries/terminal_decision_summary.md`. The copied AQ workspace now has reusable `NQ_USD-{1h,4h,1d}.feather` files copied from the durable provider-Yahoo NQ preseed root, so the earlier `NQ/USD 4h` absence is no longer the limiting factor. The slice stopped one layer later: the retained Family D root no longer has a reusable local Auto-Quant/Freqtrade runtime, and a fresh `uv run --with freqtrade --with ta-lib --with pandas --with pyarrow ...` bootstrap failed on PyPI fetch with `tls handshake eof` while resolving `freqtrade-2026.4`. Decision: `blocked`, not promotion; next useful unclaimed work is local AQ/Freqtrade runtime recovery/preservation, not another material-only rerun. `update_goal=false`.
- Handoff `2026-05-15 / codex-board-b-family-d-runtime-recovery-v1`: exact local AQ runtime recovery for the same rooted Family D branch succeeded, which means the old runtime-missing blocker is no longer the real limiter. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T233800+0800-codex-board-b-family-d-runtime-recovery-v1/summaries/terminal_decision_summary.md`. Starting from the copied workspace under `20260515T221500+0800-codex-family-d-liquidity-sweep-1h4h-provider-parity-v1`, an offline rerun tightened the exact cache gap to `pyarrow==24.0.0` for CPython `3.12`; the online lockfile/bootstrap path then installed `157` packages, created a warm `.venv`, and proved `freqtrade`, `pyarrow`, `pandas`, and `talib` all import locally. Two subsequent AQ execution attempts still did not emit any backtest metrics or result bundle in this slice; `/usr/bin/sample` traces from the live child remained dominated by `PyImport_ImportModuleLevelObject` plus `builtin_compile/compiler_codegen`, and the workspace still showed no `user_data/backtest_results/*` or other AQ output artifacts. Decision: `handoff`, not promotion; next useful work is to profile or instrument the warmed `run_tomac.py` path, not to rebuild the environment again. `update_goal=false`.
- Done `2026-05-13 / codex-profit-factor-trace-live-path-v1`: tried one profitable factor candidate, `family_f_trend_pullback_dense_15m_v1`, through the runtime chain. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260513T113009+0800-codex-profit-factor-trace-live-path-v1/summaries/profit_factor_trace_live_path_readback.md`. The factor reached candidate inventory, admission targets, policy-training/ranker artifact, explicitly enabled ranker runtime, analyze/analyze-live, Pre-Bayes soft evidence, ensemble vote, execution candidate, and execution tree. It did not reach practical execution: final ensemble action stayed `Observe`, execution tree blocked with readiness `0.4446 < 0.45`, `execution_guarded_due_to_high_transition_hazard`, and live candidate review stayed observe due to `candidate_not_comparable_same_data_factor_required`. This is useful negative execution-admission evidence, not promotion.
- Done `2026-05-13 00:24:35 +0800 / codex-candidate-pack-runtime-loop-v1`: seven curated Auto-Quant-derived candidates are visible through `python3 support/scripts/research/factor_candidate_resolver.py --repo-root . --list-buildable --output-format human`; generated pack indexes use relative `pack_dir` and repo-relative `source_candidate_pack_dir`; leak scan over generated packs/registry/index/list output found no `/Users`, `/tmp`, `/private`, or `Auto-Quant`; `python3 -m unittest support.scripts.research.tests.test_factor_candidate_resolver support.scripts.research.tests.test_factor_candidate_pack` passed with 16 tests. Remaining promotion gates still apply; this only closes the reusable candidate-pack inspection loop.
- Done `2026-05-13 / codex-native-candidate-pack-cli-v1`: native `cargo run -- factor-candidate-packs` lists the seven repo-local curated packs without Python or Board docs; `--state-dir /tmp/ict-engine-candidates --symbol FACTOR_CANDIDATES` writes `factor_candidate_pack_inventory.json`, appends an `artifact_kind=factor_candidate_pack_inventory` ledger row with `status=ready`, and refreshes `workflow_snapshot.json`; `artifact-status --latest-only` reads the entry with `path_exists=true`; `workflow-status` exposes it under `recent_artifacts`; `cargo test factor_candidate_pack -- --nocapture` passed 3 native candidate-pack tests; Python resolver/pack tests still pass 16 tests. This makes the moved results visible in the normal CLI/state loop, but not yet trade-usable promotion.
- Done `2026-05-13 / codex-candidate-pack-policy-training-visibility-v1`: `policy-training-status --symbol FACTOR_CANDIDATES --state-dir /tmp/ict-engine-candidates --output-format human` now reports `Factor candidate packs: inventory=ready count=7 preferred_density=6 cross_market=6`; JSON exposes `factor_candidate_packs.inventory_ready=true`, `candidate_pack_count=7`, `preferred_density_count=6`, and `cross_market_candidate_count=6`. This moves the curated成果 into the normal training-readiness inspection surface. It does not promote them into path-ranker/runtime execution; structural path ranking remains `runtime_selection=disabled` until Pre-Bayes/BBN/CatBoost/execution-tree gates are satisfied.
- Done `2026-05-13 / codex-candidate-pack-structural-admission-target-v1`: new native `factor-candidate-admission-targets --symbol FACTOR_CANDIDATES --state-dir /tmp/ict-engine-candidates-admission` writes the inventory, standard `policy_training/structural_path_ranking_target.*`, `factor_candidate_ranker_direct_model.json`, and `structural_path_ranking_trainer_artifact.json`; smoke output reports 35 admission rows and `policy-training-status` reports both `inventory=ready count=7 preferred_density=6 cross_market=6` and `structural_path_ranking_target rows=35 history_rows=35 mature_rows=35 history_mature_rows=35`; `policy-training-status` also reports `trainer_artifact=ready trainer_status=present_validation_insufficient`; `artifact-status --latest-only` sees `factor_candidate_pack_inventory status=ready path_exists=true`, `structural_path_ranking_target status=admission_pending path_exists=true`, and `structural_path_ranking_trainer_artifact status=ready_observation_only path_exists=true`; `workflow-status` exposes all three under redacted `recent_artifacts`. Rows are `direction=Observe`; candidate aggregate rows plus cross-market evidence rows become offline matured target observations with `training_weight_rows=35` and `raw_scored_mature=35/30`; `calibrated_rows=0`, `execution_gate_rows=0`, `production_validation=0/30`, and `runtime_selection=disabled`, so this is normal training/admission visibility with an observation-only trainer artifact, not trade-usable promotion. `cargo test factor_candidate_admission -- --nocapture` passed.
- Handoff `2026-05-15 / codex-220646-provider-quartet-portability-v1`: current-runtime Gate 3 provider portability preflight for historical profitability seed `220646` / `SourceRootStopCarryLongHorizonV1` plus rooted-branch readback. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260515T221147+0800-codex-220646-provider-quartet-portability-v1/summaries/terminal_decision_summary.md`. Fresh provider evidence produced a three-provider reproducible subset: `yfinance` recovered after one retry and wrote `35` hourly `QQQ` rows; `kraken` wrote `169` hourly `PF_XBTUSD` futures rows; `IBKR` succeeded via a network-enabled `uv` child env and wrote `103` hourly `QQQ` rows, proving the gateway path is usable even though the default shell still lacks `redis` and `ib_async`; `TradingViewRemix MCP` returned explicit rate limiting (`Retry after ~35030s`) on both `tools/list` and `get_ohlcv`. Reused `220646` evidence still proves exact rooted branch-path preservation on the wire/readback side, but downstream state remains `partial_fail_closed`, `promotion_allowed=false`, `closed_loop_confidence_ready=false`, and live `Pre-Bayes/BBN/execution-candidate` state is still absent. Decision: `handoff`, not promotion; next useful work is to normalize the working IBKR child-env recipe and retry TradingViewRemix after the rate-limit window before any fresh full-chain `220646` replay claim.
- Latest objective audit says the core blocker is first-class branch-path survival plus positive provider-portable profitability.
- `215914` proved six-provider acquisition and Auto-Quant rank can run, but failed because material/rank artifacts lost branch-path fields and profitability was not mature/provider-portable.
- `221359` failed via IBKR unavailable, missing material rows, dispatch timeout, no rank.
- `220702` exact branch readback remains blocked at Pre-Bayes/BBN/entry-model readiness; latest parity packet shows the concrete gap is branch-symbol structural feedback not materializing into any entry-model training rows for `SRC_ROOT_CARRY_LONG_220646`.
- `223253` profit-seed closed-artifact scan is active; do not duplicate it.
- `223410` SMT strict-density expansion is active; do not duplicate it.
- Active Cargo/Rust branch-path/SMT owners are still visible; do not overwrite their source or target state.

- Handoff `2026-05-16 / codex-board-b-kraken-persisted-phase-timeout-isolation-v1`: the retained copied Kraken state is now proven to hang on persisted-only `workflow-status` phases too, not only on the structural recommended-path execution-candidate overlay. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T092719+0800-codex-board-b-kraken-persisted-phase-timeout-isolation-v1/summaries/terminal_decision_summary.md`. Real work executed: re-read the exact retained state root `.../20260515T223200.../state_split_kraken_1m`, verified its `recommended_next_command` already points to persisted `factor-research`, cloned it into `/tmp/ict-engine-kraken-provider-probe-neutralized-v1`, neutralized only the copied snapshot's obvious trigger text, then ran a hard-`20s` direct-binary phase matrix on both the original retained state and the neutralized private copy. All six stale-binary reads timed out with empty stdout/stderr: original and neutralized `workflow-status --phase pre-bayes-policy`, `--phase execution-candidate-history`, and `--phase execution-candidate`. The control on the same neutralized private copy still succeeds: direct-binary `pre-bayes-status --output-format json` exits `0` in `5.479s`, and `pre-bayes-status --refresh --output-format json` exits `0` in `6.345s`. This sharpens the blocker again: the remaining stale-binary failure is broader than only `structural_execution_candidate_value_from_recommended_path_bundle(...)`, and neutralizing copied snapshot command text does not remove it. Binary/source freshness check in the same slice also proves the direct binary under test is stale relative to the dirty timeout-fix sources (`binary mtime 2026-05-15 20:53:54 +0800`, `provider_catalog.rs 2026-05-16 00:12:18 +0800`, `workflow_status.rs 2026-05-15 23:32:04 +0800`). Decision: `handoff`, not profitability-negative; next useful unclaimed work is to finish the fresh repo-local binary rebuild and rerun this exact bounded phase matrix before changing provider/factor direction again.

- Handoff `2026-05-17 / codex-board-b-kraken-workflow-status-provider-gate-audit-v1`: the copied Kraken branch now has a corrected symbol-parity/runtime diagnosis, but current-source runtime parity is still pending the long fresh build. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260517T094500+0800-codex-board-b-kraken-workflow-status-provider-gate-audit-v1/summaries/terminal_decision_summary.md`. Real work executed: corrected the stale readback to use the populated `DENSE_KLINE_BRANCH` leaf instead of `KRAKEN_EXACT`, then reran stale-binary downstream probes on the exact retained state. Direct `pre-bayes-status --symbol DENSE_KLINE_BRANCH --refresh --output-format json` returned populated bridge data in `17.573s`, while direct `workflow-status --phase execution-candidate --output-format json` still timed out at `20s` with empty stdout/stderr. The retained `DENSE_KLINE_BRANCH/workflow_snapshot.json` already has `latest_execution_candidate != null`, `recommended_next_command=ict-engine factor-research ...`, and no provider-specific blocker text; `structural_path_ranking_runtime_selection.json` says `enabled=true reuse_mode=candidate_set_only`; and the current dirty sources now include phase-read provider-surface skip tests in `command_entry.rs`, which means the stale provider-probe stack is very likely a binary-freshness issue rather than the final current-code behavior for no-refresh phase reads. The next likely heavy surface after that is local structural fallback plus the retained `21.4 MB` `structural_path_ranking_target_history.jsonl`. Decision: `handoff`, not profitability-negative; next useful unclaimed work is still to finish `/tmp/ict-engine-board-b-workflow-probe-target` and rerun `provider-status` plus no-refresh `workflow-status --phase execution-candidate` on `DENSE_KLINE_BRANCH` before changing factor/provider direction again.

- Handoff `2026-05-16 / codex-board-b-kraken-fresh-binary-workflow-parity-v1`: finished the current-source binary parity check named by the prior Kraken audit without mutating the retained source state. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213900+0800-codex-board-b-kraken-fresh-binary-workflow-parity-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T213900+0800-codex-board-b-kraken-fresh-binary-workflow-parity-v1/checks/artifact_validation.json`. Real work executed: built fresh `/tmp/ict-engine-board-b-workflow-probe-target/debug/ict-engine`, then ran fresh-binary `provider-status --agent`, `provider-status --compact`, no-refresh `workflow-status --phase execution-candidate` on retained `DENSE_KLINE_BRANCH`, and refresh `workflow-status --refresh` on a run-root-local state copy. Result: the stale provider-probe timeout is fixed in current source: no-refresh phase read exited `0` in `0.73s`, refresh-on-copy exited `0` in `0.71s`, and the previous stale-binary `20s` empty timeout was not reproduced. Decision: `handoff`, not promotion; the branch still fails closed because no-refresh remains `candidate_status=execution_observe_only`, refresh latest candidate is `no_trade/discard`, execution tree is still `observe/transition_guardrail`, and default-shell provider status is still only `market_data:1/8 ready` with `tradingview_mcp` unhealthy. Next useful work is dense-branch ranker/runtime mismatch or a non-duplicate priority-factor lane, not another stale-binary workflow probe.

- Blocked `2026-05-16 / codex-fvg-ifvg-nontrade-update-guard-v1`: terminalized the unfinished FVG/IFVG non-trade structural-feedback guard without source edits, provider fetches, Auto-Quant, Pre-Bayes, BBN, CatBoost/path-ranker, or execution-tree promotion. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214000+0800-codex-fvg-ifvg-nontrade-update-guard-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214000+0800-codex-fvg-ifvg-nontrade-update-guard-v1/checks/artifact_validation.json`. Real work executed: direct-binary `update --feedback-file --ensemble` against branch `Trend -> FvgIfvgLifecycle -> gap_lifecycle_state -> fvg_ifvg_lifecycle_observation_v1` exited `1` with `rejecting proxy/non-trade structural feedback`, `trade_usable=false`, and `not_real_trade` evidence; no files were persisted under the isolated state directory. Decision: `blocked_not_promotion`; the prior FVG/IFVG provider-density packet remains observation-only and must not be consumed as realized fills without a first-class non-trade observation schema or real trade feedback.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v9`: read-only prompt-to-artifact completion audit after the FVG/IFVG non-trade guard closeout and active-claim/process refresh. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214700+0800-codex-board-b-objective-completion-audit-v9/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214700+0800-codex-board-b-objective-completion-audit-v9/checks/prompt_to_artifact_checklist.csv`. Real work executed: no provider fetch, source edit, Auto-Quant run, update mutation, Pre-Bayes refresh, BBN, CatBoost/path-ranker, or execution-tree mutation was started; the audit inspected current terminal evidence and active owners only. Result: objective remains not complete. Positive evidence is real across rooted branch-path survival, yfinance/IBKR/Kraken provider-backed packets, market-structure entry-model row floors, Brooks realized-feedback replay, and FVG observation guard safety. Decisive gaps remain TradingViewRemix HTTP `429` / unhealthy provider state, observe-only execution trees, incomplete or fallback CatBoost/path-ranker trainer artifacts, and no packet with `promotion_allowed=true` or `trade_usable=true`. Decision: `handoff`, not promotion; `update_goal=false`.

- Handoff `2026-05-16 / codex-market-structure-catboost-factor-research-followup-v1`: followed the workflow-recommended `factor-research` command after the fresh CatBoost artifact on a copied `214200` state, without source edits, provider fetches, or mutation of the terminal CatBoost packet. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-market-structure-catboost-factor-research-followup-v1/summaries/terminal_decision_summary.md`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-market-structure-catboost-factor-research-followup-v1/checks/artifact_validation.json`; AQ seed summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214800+0800-codex-market-structure-catboost-factor-research-followup-v1/materials/auto_quant_seed_result_summary.csv`. Real work executed: cloned state, ran `factor-research`, `auto-quant-prepare`, `auto-quant-status`, created and compiled three seed strategies, ran Auto-Quant `run.py`, reran `factor-research`, corrected and ran `auto-quant-adoption-review`, then reran `policy-training-status`, `workflow-status --phase execution-candidate`, and `pre-bayes-status --refresh`; all valid commands exited `0` and the one invalid wrapper (`auto-quant-adoption-review --output-format`) exited `2` before retry. Result: Auto-Quant ran `9` backtests with `0` failures but found no profitable seed: `ExpansionBreakoutRetest robust_sharpe=-2.2123`, `TrendPullbackContinuation=-3.8109`, `VolatilityCompressionBreakout=-0.6685`, and every seed had `profit_floor=FAIL`. Downstream still preserves the exact branch with CatBoost-backed runtime and ready validation (`raw_scored_mature=1560/30`, `production_validation=1560/30`, `observation_validation=55/30`), but workflow remains `execution_blocked`, `ready=false`, `review_status=observe`, `execution_readiness=0.38986393464245506`. Decision: `handoff`, not promotion; do not replay these three AQ seeds unchanged, and do not call `update_goal`.

- Drop `2026-05-16 / codex-market-structure-exact-ibkr-aq-robust-seed-iteration-v1`: terminalized the isolated AQ robustness iteration for exact branch `Transition -> MarketStructureEvent -> atr_cisd_direct_limit -> market_structure_event_classifier_atr_cisd_direct_limit_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214607+0800-codex-market-structure-exact-ibkr-aq-robust-seed-iteration-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T214607+0800-codex-market-structure-exact-ibkr-aq-robust-seed-iteration-v1/checks/prompt_to_artifact_checklist.csv`. Real work executed in the run root: cloned the previous exact IBKR state, compiled three active AQ strategy files, confirmed `auto-quant-status` was `dependency_ready_data_ready`, and ran the AQ backtest oracle (`12 backtests succeeded, 0 failed`). Result: all three variants failed the AQ robustness/profit gate before ict-engine ingestion: `ExpansionBreakoutSeed robust_sharpe=-0.4153`, `SolBnbBearBounceSeed robust_sharpe=-0.2070`, `BnbRecoveryReclaimSeed robust_sharpe=-2.1403`, all with `profit_floor=FAIL`; the best variant was worse than the prior exact-IBKR seed. Decision: `drop`, not promotion; no Pre-Bayes, BBN, CatBoost/path-ranker, execution-tree, or feedback update is allowed from this failed AQ iteration. Next useful work needs a structurally different hypothesis or a real same-state update-row source, not blind AQ seed tweaking in this workspace.

- Handoff `2026-05-16 / codex-market-structure-tvr-ohlcv-direct-limit-chain-v1`: terminalized the stale active TVR direct-limit claim for exact branch `Transition -> CisdDisplacementValidity -> direct_limit -> market_structure_event_classifier_direct_limit_v1`. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215700+0800-codex-market-structure-tvr-ohlcv-direct-limit-chain-v1/summaries/terminal_decision_summary.md`; fetch artifact: `support/docs/experiments/actionable-regime-confidence/runs/20260516T215700+0800-codex-market-structure-tvr-ohlcv-direct-limit-chain-v1/checks/01_tvr_qqq_1h_fetch.json`. Real provider evidence: `tradingview_mcp` local stdio OHLCV fetched `154` `NASDAQ:QQQ` `1h` rows from `2026-04-16T14:30:00Z` to `2026-05-15T20:00:00Z` with no missing roles or warnings. Decision: `handoff`, not promotion; the copied state still referenced the prior yfinance/CatBoost context and remained fail-closed (`execution_readiness=0.3193`, branch `block_crowded`, `path_ranker_score_visible_to_execution_tree=false`, closed-loop admission `fail_closed`), so this packet proves TVR OHLCV availability but not a fresh TVR-carried filter/BBN/CatBoost/execution-tree chain.

- Handoff `2026-05-16 / codex-board-b-objective-completion-audit-v12`: real prompt-to-artifact completion audit after terminalizing the stale active TVR direct-limit claim. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220801+0800-codex-board-b-objective-completion-audit-v12/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220801+0800-codex-board-b-objective-completion-audit-v12/checks/prompt_to_artifact_checklist.csv`. Result: objective remains incomplete. Positive evidence is real across branch-path preservation, TVR OHLCV availability, and prior yfinance/IBKR/Kraken/AQ/Pre-Bayes/BBN/CatBoost/execution-tree packets. Missing evidence is still decisive: no single rooted profitability-factor chain has consumed `ibkr + tradingview_mcp + yfinance + kraken` material through the full chain into non-observe execution; the current TVR packet copied prior yfinance/CatBoost state and remains fail-closed. Also note that the pre-existing `20260516T220119...v11` audit row references artifacts absent from the current worktree, so v12 is the current durable audit. Decision: `handoff`, not promotion; `update_goal=false`.

- Handoff `2026-05-16 / codex-order-block-yf-analyze-stall-sample-v1`: current-binary reattempt on the preserved yfinance NQ `order_block_variant_classifier` owner replay no longer reproduces the older `180s` shared `analyze` timeout, and the chain now reaches downstream surfaces. Evidence summary: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220111+0800-codex-order-block-yf-analyze-stall-sample-v1/summaries/terminal_decision_summary.md`; checklist: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220111+0800-codex-order-block-yf-analyze-stall-sample-v1/checks/prompt_to_artifact_checklist.csv`; validation: `support/docs/experiments/actionable-regime-confidence/runs/20260516T220111+0800-codex-order-block-yf-analyze-stall-sample-v1/checks/artifact_validation.json`. Real work executed: `analyze` returned `0` in `36s`, persisted workflow/execution-tree artifacts, `workflow-status --refresh` and `pre-bayes-status --refresh` returned `0`, `export-structural-path-ranking-target` wrote `3` owner rows, external scores were applied to all `3`, a direct fallback trainer artifact was registered, runtime enablement returned `0`, and final workflow/policy readbacks returned `0`. Result: Pre-Bayes/BBN-facing status is `pass_hard`, but workflow still blocks at `bridge_needs_confirmation` (`bridge_probability_gap=0.037`), owner-symbol structural target rows preserve OB context fields but still leave the first-class branch path fields empty, runtime remains `enabled_registered_model_invalid` with `runtime_active_match_count=0`, and execution tree remains `observe` with no path-ranker consumption. Decision: `handoff`, not promotion; next useful work is owner-symbol branch-field binding from OB context into `regime_profit_branch_path/main_regime/sub_regime/sub_sub_regime_or_profit_factor/profit_factor`, not another timeout or provider retry on this same input.

## Live Gates

- `profit_seed_first=true`
- `single_factor_single_gate_default=true`
- `root_regime_fit_optional_max_default=true`
- `full_chain_required_for_every_factor=false`
- `branch_path_required_from_first_artifact=root_regime_plus_factor`
- `material_json_branch_path_required=only_for_root_fit_or_handoff`
- `rank_artifact_branch_path_required=only_for_handoff`
- `provider_matrix_required_for_accepted_branch=elastic_target_not_hard_gate`
- `positive_provider_portable_profitability_required=per_branch_champion_or_reproducible_subset`
- `full_market_full_instrument_closure_required=false`
- `each_market_or_instrument_should_have_own_strong_factor=true`
- `pre_bayes_filter_required_after_seed=false_by_default`
- `bbn_learning_required_after_filter=false_by_default`
- `catboost_path_ranker_required_after_bbn=false_by_default`
- `execution_tree_non_observe_required=false_by_default`
- `feedback_update_learning_required=false_by_default`
- `promotion_allowed=false_until_all_gates_pass`
- `trade_usable=false_until_all_gates_pass`
- `update_goal=false`

## Iteration Protocol

For the next Board B iteration, use this order:

1. Pick one branch path and one unmet gate from this file.
2. Prove a profitable seed before broad promotion: meaningful trade count,
   positive expectancy, branch path present, and provider/window provenance.
3. Keep the first pass isolated: one factor, one gate, and at most one frozen
   root regime.
4. Preserve at least `root_regime` and `factor_id` in the first material. Add
   deeper `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`,
   `profit_factor`, and `regime_profit_branch_path` only when the idea claims
   root-fit or downstream handoff.
5. Only after Gate 1 or Gate 2 is worth keeping, run provider portability or the
   Pre-Bayes/filter -> BBN -> CatBoost/path-ranker -> execution-tree chain.
6. Record the terminal decision here as keep/drop/incubate/blocked/handoff with artifact paths;
   do not append routine status to archival Board B prose.
7. Before starting, check `Current Status` and active roots; if a surface is
   occupied or belongs to Board A, record `duplicate_suppressed` or no-takeover
   coordination here and choose a different profitability-factor gate.
8. If another agent has the same idea, switch to a new unclaimed idea instead of
   competing for the same root.
9. Do not write a start claim, progress log, or routine readback into this file.

## SMT Boundary

SMT is a Board B factor signal. It is not a Board A regime task, not generic
correlation, not relative strength, and not standalone actionable. It may
contribute as one of the entry-probability signals, but it cannot be the only
reason to enter.

Use the stricter ICT reading:

- SMT divergence is a same-timeframe, overlapping-session, same swing or
  liquidity-event confirmation failure between related symbols
- it is not enough to say one market is "stronger" or "weaker"
- for positive pairs:
  - `bullish_smt`: base symbol sweeps sell-side or prints a lower low while the
    comparison symbol fails to confirm and holds a higher low
  - `bearish_smt`: base symbol sweeps buy-side or prints a higher high while the
    comparison symbol fails to confirm and prints a lower high
- for inverse pairs, normalize the comparison structure before applying the
  same HH/HL/LH/LL logic, and preserve both normalized and raw event fields

For `range -> expansion` handoff work, treat SMT as one confirmation input
alongside ATR expansion, PDA ordering, displacement, and structure transition
evidence. SMT alone must not flip an entry from observation to actionable.

Current SMT gates:

- same timeframe, same overlapping session, same swing/liquidity event;
- relationship stability is only a comparable-symbol gate;
- inverse relationships must emit normalized and raw comparison structure;
- every signal requires base and comparison levels;
- the resolver must dynamically search comparable symbols from the user symbol,
  provider universe, and session context rather than rely on a hard-coded
  watchlist only;
- operator-priority relationship families currently include:
  - `NQ -> ES/YM/RTY/QQQ/SPY/DIA/IWM/NAS100/US500/US30/DXY/VIX`
  - `EURUSD -> GBPUSD/DXY/EURGBP`
  - `XAUUSD -> XAGUSD/DXY/US10Y/real_yield/GDX`
  - `BTC -> ETH/SOL/TOTAL/QQQ/DXY`
- SMT may raise or lower entry likelihood together with structure, ATR, PDA,
  and displacement evidence;
- trade evidence still requires later MSS/CISD or displacement plus PDA entry model;
- current strict rows are insufficient, with `stress` and `other` still sparse.

## Operator Priority Refresh

When the operator asks what to train next, the default Board B answer should
prioritize these first-class runtime-evidence factors in this order:

1. `smt_relationship_resolver`
2. `liquidity_pool_texture`
3. `fvg_ifvg_lifecycle`
4. `order_block_variant_classifier`

The broader backlog still includes `sweep_quality`,
`market_structure_event_classifier`, `brooks_price_action_context`, and
`options_dealer_context`, but those should not displace the quartet unless the
same exact quartet slice is already active, blocked, or decisively negative.

These factors are valuable because they upgrade human template output, BBN
evidence, and execution-tree features into first-class runtime fields instead
of leaving the system with correct-but-thin `n/a` scaffolding.

## Cleanup Rule

Historical Board B logs may not be deleted until:

1. All hard references to archival Board B prose are migrated or intentionally archived.
2. Compact extracted evidence covers every live gate above.
3. Active claims such as `223253`, `223410`, provider-authority work, and Cargo/Rust owner work are closed or handed off.
4. A dry-run reference audit shows no runtime or docs path relies on the old file.
5. A parity readback confirms Board B status is identical from this compact doc plus retained evidence packets.

## Evidence-Only Supplement: External-Harvest Kraken TSMOM/RSI2 AQ V1

Append-only terminal row at `2026-05-18 12:50:48 +0800` after reading the current Board B compact surface and observing pre-write hash `28bf7251466099a1436f84615294bb2c87a7492b5570dbb675d159ae9ad4e61a`. Operator correction applies: `95%` is not a hard gate for this slice; this is supplemental evidence and subclass/sidecar formula evidence only. This row does not promote a root profitability factor, does not claim root Board B completion, does not send evidence downstream, does not mutate Pre-Bayes/BBN/CatBoost/execution-tree learning, and does not call `update_goal`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T124404+0800-codex-external-harvest-kraken-tsmom-rsi2-aq-v1/`.
- Source-backed formulas harvested into sidecar materials: Moskowitz/Ooi/Pedersen-style time-series momentum, volatility-scaled TSMOM, and Connors-style RSI(2) short-term mean reversion.
- Provider coverage: `kraken_public` on `BTCUSD` and `ETHUSD` across `1m/5m/15m/1h`; all 8 fetch commands exited `0` with `721` rows per normalized file.
- Auto-Quant path: `agent-material-batch`, `agent-material-dispatch`, and `agent-material-rank` all exited `0`; `material_count=16`; branch fields were preserved.
- Rank evidence: `rank_rows=16`, `rank_total_trade_count=106`.
- Cost/practical gate: `cost_model=2bps_per_side_proxy`; `survivor_candidates=[]`; `cost_gate_survivors=[]`.
- Terminal decision: `drop_gate1_no_practical_survivor`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`.
- Evidence files: `materials/source_refs.json`, `summaries/provider_provenance_matrix.csv`, `summaries/rank_rows.csv`, `checks/terminal_metrics.json`, `checks/cost_stress_gate.json`, and `summaries/terminal_decision_summary.md`.

Interpretation:

- This is a real external-harvest negative sample through provider data and Auto-Quant Gate 1, not a 95%-threshold failure.
- It may be retained as evidence that generic TSMOM/RSI2 sidecar formulas need stronger regime/subclass conditioning or a different market/session gate before being retried.
- Do not use this packet as a root-factor win, downstream replay input, CatBoost profitable label, or execution-tree weight update.

## Single-Factor Root Gate Pass: YF NRG 5m ORB RVOL Expansion V1

Append-only terminal row at `2026-05-18 16:35:17 +0800` after reading the current Board B compact surface and observing pre-write hash `521c97e36a49db93c6487846baa894efd32136c9736a84abcc5a509262ba393f`. This row implements the operator correction that factor training starts from a specific rooted context: provider, market, species, symbol, timeframe, root regime, sub-regime, and one specific profitability factor. This is Gate 1 single-factor evidence only; it does not promote the branch to trade-usable execution, does not mutate Pre-Bayes/BBN/CatBoost/execution-tree learning, and does not call `update_goal`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T163102+0800-codex-yf-nrg-5m-rooted-single-factor-gate-v1/`.
- Branch path: `yfinance/YF -> US_EQUITY -> POWER_UTILITY_AI_LOAD -> NRG -> 5m -> TrendExpansion -> PowerUtilityAiLoadOpeningRangeExpansion -> yf_nrg_5m_orb_rvol_expansion_single_factor_v1`.
- Factor scope: `provider=yfinance/YF`, `market=US_EQUITY`, `species=POWER_UTILITY_AI_LOAD`, `symbol=NRG`, `timeframe=5m`, `root_regime=TrendExpansion`, `sub_regime=PowerUtilityAiLoadOpeningRangeExpansion`, `profit_factor=yf_nrg_5m_orb_rvol_expansion_single_factor_v1`.
- Provider evidence: fresh yfinance fetch for `NRG 5m` from `2026-03-24` through `2026-05-18`, `2965` normalized rows, `local_cache_replay=false`, command exit `0`.
- Auto-Quant path: `agent-material-batch`, `agent-material-dispatch`, and `agent-material-rank` all exited `0`; `material_count=1`; no sibling symbol, timeframe, market, or second factor was used.
- Rank evidence: `rank_rows=1`, `trade_count=24`, `total_profit_pct=3.58`, `win_rate_pct=62.5`, `sharpe=1.9428`.
- Cost/practical gate: `cost_model=2bps_per_side_proxy`; `cost_survives_2bps_side=true`.
- Terminal decision: `single_factor_gate_passed_next_can_try_compound_stack`; `compound_strategy_stage=single_factor_gate_passed_next_can_try_compound_stack`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `summaries/rank_rows.csv`.

Interpretation:

- This is a real single rooted profitability-factor Gate 1 pass for `YF NRG 5m`, not an aggregate cross-market or sibling-symbol result.
- The next valid Board B step may try a compound-stack follow-up only on top of this exact rooted factor, or independently validate this factor with a reproducible provider/window subset.
- Do not treat this packet as Pre-Bayes/BBN/CatBoost/execution-tree promotion evidence until those later gates are explicitly run and pass.

## Downstream Fail-Closed Readback: YF NRG 5m Rooted Single Factor V1

Append-only terminal row at `2026-05-18 16:58:00 +0800` for the same exact
rooted branch after Gate 1 passed cost stress. This row does not promote the
branch; it records the downstream chain state and the next blocker.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T163102+0800-codex-yf-nrg-5m-rooted-single-factor-gate-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T163102+0800-codex-yf-nrg-5m-rooted-single-factor-gate-v1/downstream-20260518T165016+0800/`.
- Branch path: `yfinance/YF -> US_EQUITY -> POWER_UTILITY_AI_LOAD -> NRG -> 5m -> TrendExpansion -> PowerUtilityAiLoadOpeningRangeExpansion -> yf_nrg_5m_orb_rvol_expansion_single_factor_v1`.
- Evidence files: `summaries/terminal_decision_summary.md`, `checks/downstream_metrics.json`, `checks/prompt_to_artifact_checklist.csv`, `materials/yf_nrg_5m_rooted_single_factor_strategy_library.json`, and `path_ranker_model/trainer_artifact.json`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T-current-codex-yf-nrg-5m-rooted-single-factor-downstream-readback-v1.claim`.
- Commands passed: `auto-quant-results-import`, `auto-quant-prior-init`,
  `analyze`, `workflow-status --refresh`, `pre-bayes-status --refresh`,
  `export-structural-path-ranking-target`, `policy-training-status`,
  direct-fallback score application, `apply-structural-path-ranking-external-scores`,
  direct-fallback trainer registration, runtime enablement, and post-ranker
  `analyze/workflow/pre-bayes/policy` readbacks.
- Downstream readback: `pre_bayes_gate=pass_neutralized`;
  `execution_tree_gate_status=observe`; `execution_tree_branch=transition_guardrail`;
  `execution_score=0.5599607228816076`;
  `path_ranker_runtime=registered_model_artifact/weighted_feature_sum_v1/not_ready`.
- Ranker/validation blocker: `raw_scored_mature=0/30`,
  `production_validation=0/30`, `observation_validation=0/30`,
  `trainer_status=present_validation_insufficient`. The direct fallback is
  candidate-set parity only, not CatBoost/live readiness.
- Terminal decision: `gate1_pass_downstream_fail_closed`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`;
  `skill_update=not_needed`.
- Auto-Quant automatic-iteration follow-up: exact-branch `factor-research`
  exited `0` and wrote
  `state/YF_NRG_5M_ROOTED_SINGLE_FACTOR_DOWNSTREAM_V1/auto_quant_handoff.factor_research.json`;
  handoff status was `dependency_ready_data_missing` and recommended
  `auto-quant-prepare`. That prepare command exited `1` because the managed
  Auto-Quant workspace attempted Binance crypto pairs/timeframes and failed
  before NRG-specific iteration with Binance market-load timeout/restricted
  location errors. Classification:
  `autoquant_iteration_blocked_managed_prepare_binance`, not an NRG factor
  failure and not promotion.

Interpretation:

- The exact rooted NRG branch survived into the downstream chain, but execution
  remains observe-only under a transition guardrail and path-ranker maturity is
  below the required validation row gates.
- Next work should either add real mature feedback/validation rows for this
  exact branch or test a compound overlay on top of this rooted factor; do not
  call the current direct-fallback readback live-ready.

## Compound Overlay Gate Fail: YF NRG 5m ORB/RVOL + CMF/ADX V1

Append-only terminal row at `2026-05-18 17:31:46 +0800` for the exact rooted
NRG branch after the base single-factor Gate 1 pass. This row tests one explicit
compound overlay on top of the prior rooted factor and does not promote the
branch.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T173146+0800-hermes-yf-nrg-5m-compound-stack-gate-v1/`.
- Branch path: `yfinance/YF -> US_EQUITY -> POWER_UTILITY_AI_LOAD -> NRG -> 5m -> TrendExpansion -> PowerUtilityAiLoadOpeningRangeExpansion -> yf_nrg_5m_orb_rvol_expansion_single_factor_v1 -> yf_nrg_5m_orb_rvol_cmf_adx_compound_v1`.
- Provider evidence: fresh yfinance fetch for `NRG 5m`, `2965` normalized rows,
  `local_cache_replay=false`, fetch exit `0`.
- Auto-Quant path: `auto-quant-agent-material-batch`,
  `auto-quant-agent-material-dispatch`, and `auto-quant-agent-material-rank` all
  exited `0`; `rank_rows=1`.
- Rank evidence: `trade_count=3`, `total_profit_pct=-0.01`,
  `win_rate_pct=33.3333`, `sharpe=-0.0077`.
- Cost/practical gate: `cost_survives_2bps_side=false`,
  `cost_survives_5bps_side=false`.
- Base comparison: prior exact-root base had `trade_count=24`,
  `total_profit_pct=3.58`, and survived `2bps/side`.
- Terminal decision: `compound_overlay_gate_failed_sparse_negative`;
  `incremental_improvement_over_base=false`; `downstream_allowed=false`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/rank_rows.csv`,
  `summaries/provider_provenance_matrix.csv`, and
  `summaries/terminal_decision_summary.md`.

Interpretation:

- The CMF/ADX overlay over-filtered the profitable base branch into sparse
  negative evidence. Do not send this compound overlay to Pre-Bayes/BBN/CatBoost
  or execution-tree handoff.
- The prior base factor remains the better exact-root seed; next work should try
  a less restrictive overlay or add real mature feedback rows, not promote this
  failed compound stack.

## Max-Window Density Gate Mixed: YF XLF VWAP Reclaim 1m Ladder V1

Append-only terminal row at `2026-05-18 18:26:06 +0800` after the operator
correction that sparse overlays are not useful and windows should use provider
upper bounds. This row uses fresh YF data with the maximum feasible intraday
windows and records a Gate 1 verdict only.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T182606+0800-hermes-yf-xlf-vwap-reclaim-1m-max-ladder-v1/`.
- Branch path: `Range -> VwapLiquidityReclaim -> vwap_reclaim_pullback -> yf_xlf_vwap_reclaim_pullback_1m_ladder_v1`.
- Provider evidence: `XLF 1m` 7d max had `1951` rows; `5m/15m/30m/1h` 59d max
  had `3121/1041/521/281` rows; all fetch commands exited `0`.
- Auto-Quant path: material batch, dispatch, and rank all exited `0`.
- Rank evidence from authoritative `ranking[]`: `rank_rows=5`,
  `rank_total_trade_count=36`, `positive_rows=2`.
- Per-timeframe results: `1m` had `trade_count=1`, `total_profit_pct=-0.12`;
  `5m` had `trade_count=15`, `total_profit_pct=-1.68`; `15m` had
  `trade_count=10`, `total_profit_pct=-0.77`; `30m` had `trade_count=7`,
  `total_profit_pct=3.95`; `1h` had `trade_count=3`, `total_profit_pct=0.59`.
- Terminal decision: `mixed_ladder_1m_origin_negative_no_downstream`;
  `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/rank_rows.csv`,
  `summaries/provider_provenance_matrix.csv`, and
  `summaries/terminal_decision_summary.md`.

Interpretation:

- The max-window correction did raise total trade count to `36`, but the 1m root
  stayed sparse/negative and small-cycle 5m/15m were negative. The positive
  30m/1h siblings are not enough to promote a 1m-origin branch.
- Next step should keep max windows but use a denser 1m entry family or a crypto
  provider once Kraken SSL recovers; do not send this mixed XLF ladder to
  Pre-Bayes/BBN/CatBoost/execution-tree handoff.

## Downstream Pass-Through: YF Beauty Personal-Care RSI/VWAP Max-Window V2

Append-only terminal row at `2026-05-18 18:40:51 +0800` after the max-window
beauty/personal-care Gate 1 pass was sent through the downstream chain. This row
shows the chain is still observe-only; it does not promote the branch.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T183442+0800-hermes-yf-beauty-personal-care-rsi-vwap-reclaim-1m-max-window-v2/downstream-20260518T184051+0800/`.
- Branch path: `RangeReversion -> BeautyPersonalCareOversoldReclaim -> rsi_vwap_reclaim -> yf_beauty_personal_care_rsi_vwap_reclaim_1m_max_window_v2`.
- All command exits: `0`.
- AQ evidence: `rank_rows=18`, `rank_positive_rows=7`, `rank_total_trades=174`.
- Execution chain: `workflow_closed_loop_branch_admission=None`, `execution_candidate_actionable=false`, `execution_candidate_status=no_trade`, `execution_tree_gate_status=observe`, `execution_tree_branch=transition_guardrail`.
- Ranker visibility: `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=false`, `ranker_validation_ready=false`.
- Policy training: `rows=4`, `mature_rows=0`, `history_rows=7`, `history_mature_rows=0`.
- Terminal decision: `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`.

Interpretation:

- Gate 1 passed well enough to hand off downstream, but the chain still has no actionable/admission state and no mature validation rows. This is parity/observe evidence, not live-ready promotion.

## Dense V3 Gate 1 Pass: Beauty Personal-Care RSI/VWAP Reclaim Dense Variant

Append-only terminal row at `2026-05-18 18:43:53 +0800` for the denser 1m
variant that directly addressed the sparse-entry complaint. This row captures a
true Gate 1 density win, then records the downstream observe-only state.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T184353+0800-hermes-yf-beauty-personal-care-rsi-vwap-reclaim-1m-dense-v3/`.
- Branch path: `RangeReversion -> BeautyPersonalCareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_beauty_personal_care_rsi_vwap_reclaim_1m_dense_v3`.
- Provider evidence: `ELF/ULTA/COTY` fresh YF coverage; `1m` rows `1942/1766/1946`; `5m/15m/30m/1h/1d` all present; `4h` unsupported and recorded as missing.
- Gate 1 evidence: `rank_rows=17`, `rank_total_trades=923`, `origin_trades_1m=43`, `positive_rows_trade_ge_5=6`, `dense_positive_gate=true`.
- Downstream chain: all commands exited `0`, but `execution_candidate_actionable=false`, `execution_candidate_status=no_trade`, `execution_tree_gate_status=observe`, `execution_tree_branch=transition_guardrail`, `path_ranker_score_used_by_execution_tree=false`, `ranker_validation_ready=false`.
- Policy/training: `mature_rows=1`, `history_mature_rows=12`, but still no closed-loop admission.
- Terminal decision: `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`.

Interpretation:

- This is the correct fix for the earlier "trade count too small" complaint:
  1m origin is now dense and the ladder is meaningful.
- Even so, the downstream chain remains observe-only. Next work should target
  execution readiness or mature validation rows, not more 1m sparsity tuning.

## Gate 1 Mixed: IBKR TGT Defensive Retail Opening-Drive RVOL AQ Continuation

Append-only terminal row at `2026-05-18 19:08:00 +0800` for the retained IBKR
TGT provider ladder. This row fills the missing Auto-Quant step from the earlier
provider-only packet and does not promote the branch.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T190130+0800-codex-ibkr-tgt-defensive-retail-opening-drive-rvol-aq-continuation-v1/`.
- Source provider ladder: `support/docs/experiments/actionable-regime-confidence/runs/20260518T173800+0800-codex-ibkr-tgt-defensive-retail-provider-ladder-v1/`.
- Branch path: `TrendExpansion -> DefensiveRetailOpeningDrive -> rvol_breakout_pullback -> ibkr_tgt_defensive_retail_opening_drive_rvol_gate1_v1`.
- Provider evidence reused retained real IBKR CSVs: `1m=8046` rows from the
  `10D` retry; `5m/15m/30m/1h/4h/1d` rows `10791/3607/1814/916/1064/500`.
- Auto-Quant path: `auto-quant-agent-material-batch`,
  `auto-quant-agent-material-dispatch`, and `auto-quant-agent-material-rank` all
  exited `0`; `rank_rows=7`.
- Rank evidence from authoritative `ranking[]`: `rank_total_trade_count=95`,
  `rank_nonzero_trade_rows=6`, `positive_trade_rows=1`.
- Per-timeframe results: `30m` had `5` trades and `+0.98%`; `1h` had `4`
  trades and `-0.13%`; `4h` had `4` trades and `-1.29%`; `1m` had `15`
  trades and `-0.23%`; `15m` had `23` trades and `-6.15%`; `5m` had `44`
  trades and `-10.29%`; `1d` had `0` trades.
- Terminal decision: `keep_observation_only_gate1_mixed`;
  `gate1_pass=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.
- Evidence files: `checks/*.exit`, `checks/prompt_to_artifact_checklist.csv`,
  `summaries/tgt_aq_continuation_metrics.json`,
  `summaries/provider_provenance_matrix.csv`, and
  `summaries/terminal_decision_summary.md`.

Interpretation:

- The earlier TGT provider-only packet is now closed with real AQ evidence.
  The exact TGT RVOL breakout/pullback branch is mixed and weak: the 1m root and
  small-cycle siblings are negative, while the sole positive 30m row is too thin
  to justify Pre-Bayes/BBN/CatBoost/execution-tree handoff.
- Next work should use a materially different TGT condition or another
  defensive-retail sibling with a denser 1m root, not downstream promotion of
  this branch.

## Overlay Gate 1 Pass + Downstream Fail-Closed: YF NRG 5m VWAP Trailing Overlay V1

Append-only terminal row at `2026-05-18 19:13:21 +0800` after observing
pre-write hash `fdded1b11a3bde7ed39a2f119d0938659d74de5e820bf64c309a7130a026e922`.
This row continues the exact rooted NRG branch after the prior base Gate 1 pass
and failed CMF/ADX overlay. It records a real incremental overlay pass and a
real downstream fail-closed readback; it does not promote the branch or call
`update_goal`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T185838+0800-codex-yf-nrg-5m-vwap-trailing-overlay-gate-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T185838+0800-codex-yf-nrg-5m-vwap-trailing-overlay-gate-v1/downstream-20260518T190141+0800/`.
- Branch path: `yfinance/YF -> US_EQUITY -> POWER_UTILITY_AI_LOAD -> NRG -> 5m -> TrendExpansion -> PowerUtilityAiLoadOpeningRangeExpansion -> yf_nrg_5m_orb_rvol_expansion_single_factor_v1 -> yf_nrg_5m_orb_rvol_vwap_trailing_overlay_v1`.
- Provider evidence: fresh yfinance/YF `NRG 5m`, `2965` normalized rows,
  `local_cache_replay=false`, fetch exit `0`.
- Auto-Quant Gate 1: material batch, dispatch, and rank all exited `0`;
  authoritative `ranking[]` row completed with `trade_count=40`,
  `total_profit_pct=6.27`, `win_rate_pct=52.5`, `sharpe=4.1946`.
- Cost/practical gate: survived `2bps/side` and `5bps/side`; incremental over
  the base branch (`24` trades, `+3.58%`, survived `2bps/side`).
- Downstream chain: `auto-quant-results-import`, `auto-quant-prior-init`,
  `analyze`, `workflow-status --refresh`, `pre-bayes-status --refresh`,
  `export-structural-path-ranking-target`, and `policy-training-status` exited
  `0`; exact branch survived; Pre-Bayes/filter and BBN/workflow artifacts exist.
- CatBoost/path-ranker: CatBoost training exited `1` with all features constant
  or ignored; direct fallback candidate-set training/apply/register/enable and
  post-ranker `analyze/workflow/pre-bayes/policy` readbacks exited `0`, but this
  remains candidate-set-only.
- Execution readback after direct fallback: `execution_candidate_actionable=false`;
  `execution_tree_gate_status=observe`; `execution_tree_branch=transition_guardrail`;
  `path_ranker_score_visible_after_direct_fallback=false`;
  `path_ranker_score_used_after_direct_fallback=false`;
  `mature_rows=1`, `history_mature_rows=1`.
- Terminal decision:
  `gate1_pass_downstream_fail_closed_direct_fallback_candidate_set_only`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`,
  `checks/downstream_metrics.json`, `checks/prompt_to_artifact_checklist.csv`,
  `summaries/terminal_decision_summary.md`, and
  `summaries/direct_fallback_readback_summary.md`.

Interpretation:

- The VWAP trailing overlay is the first useful compound stack on the exact NRG
  root so far: it improved trade count, profit, and cost robustness over the
  base branch.
- The downstream chain still does not admit execution. Next useful work is not
  another sparse overlay; it is either real mature feedback rows for this exact
  branch or an execution-readiness/transition-guardrail diagnosis on why the
  path-ranker artifact is not visible/used by the execution tree.

## Gate 1 Fail: YF MARA/RIOT VWAP/RVOL Opening-Drive 1m MTF V1

Append-only terminal row at `2026-05-18 20:45:40 +0800` for a 1m-origin
crypto-mining equity branch. This run followed the regime-rooted branch rule and
did not proceed downstream.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T204540+0800-codex-yf-mara-riot-vwap-rvol-opening-drive-1m-mtf-v1/`.
- Branch path: `TrendExpansion -> CryptoMiningEquityMomentum -> one_minute_vwap_rvol_opening_drive -> yf_mara_riot_vwap_rvol_opening_drive_1m_mtf_v1`.
- Provider evidence: fresh yfinance/YF rows for `MARA` and `RIOT`; `1m/5m/15m/30m/1h` all fetched with exit `0`; `local_cache_replay=false`.
- Auto-Quant path: material batch, dispatch, and rank all exited `0`; `rank_rows=10`, `rank_total_trade_count=58`, `branch_fields_preserved=true`.
- 1m-origin verdict: `one_minute_trades=6`, `positive_origin_1m_count=0`; only positive higher-timeframe rows with trade count >=8 were `MARA:5m` and `RIOT:5m`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `summaries/terminal_decision_summary.md`, `checks/terminal_metrics.json`, `summaries/rank_rows.csv`, and `summaries/provider_provenance_matrix.csv`.

Interpretation:

- The branch preserved the regime-rooted path and produced enough aggregate rank rows, but the 1m root was not positive. The 5m positives do not rescue the 1m-origin factor, so no Pre-Bayes/BBN/CatBoost/execution-tree handoff.

## Provider Blocker: Kraken BTC/ETH/SOL Full Ladder V1 Retry

Append-only terminal row at `2026-05-18 20:50:44 +0800` for a full `1m/5m/15m/30m/1h/4h/1d` Kraken ladder attempt.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T205044+0800-codex-kraken-btc-eth-sol-vwapdev-obvrsi-cost-tight-1m-full-ladder-v1/`.
- Provider evidence: `BTCUSD 1m` fetched successfully and produced normalized data, but `BTCUSD 5m` and `BTCUSD 15m` failed after Kraken OHLC SSL retries; the outer run timed out before a complete ladder/AQ verdict.
- Terminal decision: `provider_window_or_ssl_blocked_incomplete_ladder`; `local_cache_replay=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/01_kraken_btcusd_1m_fetch.exit`, `checks/02_kraken_btcusd_5m_fetch.exit`, `checks/03_kraken_btcusd_15m_fetch.exit`, `command-output/02_kraken_btcusd_5m_fetch.err`, and `command-output/03_kraken_btcusd_15m_fetch.err`.

Interpretation:

- This is a provider reachability blocker, not a factor failure. Next retry should either downgrade/retry Kraken intervals with shorter fetch windows or switch to Binance/Bybit public runtime while preserving the same market/product/symbol/timeframe/regime-rooted branch grammar.

## Gate 1 Fail: YF Industrial Gas ORB/RVOL Expansion 1m MTF + 1d V1

Append-only terminal row at `2026-05-18 21:02:05 +0800` for a regime-rooted industrial gas branch observed to completion in this continuation turn.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T210205+0800-codex-yf-industrial-gas-orb-rvol-expansion-1m-mtf-1d-v1/`.
- Branch path: `TrendExpansion -> IndustrialGasOpeningRangeExpansion -> orb_rvol_expansion -> yf_industrial_gas_orb_rvol_expansion_1m_mtf_1d_v1`.
- Provider evidence: fresh yfinance/YF for `LIN/APD/AIQUY`; `1m/5m/15m/30m/1h/1d` fetched with exit `0`; `4h` unavailable through Yahoo and not fabricated; `local_cache_replay=false`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; `rank_rows=18`.
- Rank evidence: `rank_total_trade_count=7`, `one_minute_trades=0`, `positive_1m=[]`, `positive_higher_timeframes_trade_ge_5=[]`, `dense_positive_gate=false`.
- Terminal decision: `keep_subclass_evidence_or_drop_gate1_no_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, and `checks/*.exit`.

Interpretation:

- This branch is too sparse and non-positive at the 1m root. It should not proceed to Pre-Bayes/BBN/CatBoost/execution-tree. The next viable training step should pivot to a denser 1m entry family or continue the already stronger exact-root NRG overlay via execution-readiness diagnostics.

## Execution-Readiness Diagnostic: YF NRG 5m VWAP Trailing Overlay V1

Append-only diagnostic row at `2026-05-18 21:14:28 +0800` for the existing exact-root NRG compound branch. No gate was relaxed and no promotion is claimed.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T185838+0800-codex-yf-nrg-5m-vwap-trailing-overlay-gate-v1/downstream-20260518T190141+0800/`.
- Branch path: `yfinance/YF -> US_EQUITY -> POWER_UTILITY_AI_LOAD -> NRG -> 5m -> TrendExpansion -> PowerUtilityAiLoadOpeningRangeExpansion -> yf_nrg_5m_orb_rvol_expansion_single_factor_v1 -> yf_nrg_5m_orb_rvol_vwap_trailing_overlay_v1`.
- Execution blocker: `execution_readiness=0.4853238921949943` vs ready gate `0.65`, shortfall `0.164676`.
- Transition blocker: `hybrid_transition_hazard=0.632` vs threshold `0.600`, excess `0.032`.
- Regime/PDA blocker: `pda_sequence=regime_disagreement:cluster_0:range:1.000`, `hybrid_regime_label=range_choppy`, while branch root remains `TrendExpansion/BullTrendAcceleration`.
- Ranker blocker: path-ranker visible but not used, `ranker_validation_ready=false`, `policy_mature_rows=1`, `history_mature_rows=1`.
- Trace feature clues: `pythagorean_overstretch`, `dominant_cycle_energy`, and `spectral_entropy` dominate `execution_shap_top_k`.
- Terminal decision: `gate1_pass_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/nrg_transition_guardrail_diagnostic.json`, `summaries/nrg_transition_guardrail_diagnostic.md`, `state/YF_NRG_5M_VWAP_TRAILING_OVERLAY_DOWNSTREAM_V1/execution_tree_trace.json`, and `checks/downstream_final_readback.json`.

Interpretation:

- The next useful mutation is not another broad sweep and not gate lowering. Keep the same rooted branch and test a transition-stability / PDA-alignment overlay that reduces range-choppy disagreement and transition hazard while preserving the NRG VWAP trailing edge.

## Execution Gate Diagnostic: TVR HACK Real-Trades Admission V1

Append-only terminal row at `2026-05-18 21:20:00 +0800` for the mature TVR HACK cybersecurity branch after real-trade admission and CatBoost/path-ranker runtime readback.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T212000+0800-codex-tvr-hack-real-trades-execution-gate-diagnostic-v1/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260518T203610+0800-codex-tvr-hack-cybersecurity-opening-drive-rvol-vwap-density-1m-full-ladder-v1/real-trades-admission-20260518T210645+0800/`.
- Branch path: `TrendExpansion -> CybersecurityETFOpeningDrive -> one_minute_opening_drive_rvol_vwap_density_full_ladder -> tvr_hack_cybersecurity_opening_drive_rvol_vwap_density_1m_full_ladder_v1`.
- Real-trade admission: `123` rows; wins/losses/breakevens `85/37/1`; all commands in the source admission packet exited `0`; exact branch survived.
- CatBoost/path-ranker is no longer the blocker: visible=`true`, used=`true`, validation_ready=`true`; `raw_scored_mature=126/30`, `production_validation=126/30`, `observation_validation=123/30`, score source `catboost`.
- Current execution blocker: `execution_readiness=0.1732`, below the `0.45` minimum by `0.2768`; execution tree gate=`observe`, branch=`transition_guardrail`, bias=`guarded`.
- Pre-Bayes blocker: gate_status=`observe_only`, quality=`0.2745`, conflict flags include `multi_timeframe_direction_conflict`, `hostile_liquidity_requires_high_uncertainty`, `pda_regime_family_disagreement`, `pda_sequence_cluster_weak`, and `pda_sequence_low_consistency`.
- Direction/PDA blocker: selected direction is `Bear` while MTF direction bias is `bullish`; long/short probability gap is only `0.0243` versus the `0.08` minimum; `pda_hybrid_alignment=false`; `hybrid_transition_hazard=0.889`; PDA consistency is `0.1081` across `37` sessions.
- Terminal decision: `execution_gate_blocker_diagnosed_no_promotion`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/execution_gate_diagnostic.json`, `checks/prompt_to_artifact_checklist.csv`, and `summaries/terminal_decision_summary.md`.

Interpretation:

- This packet proves the downstream chain can see and use a mature CatBoost/path-ranker artifact, but it is still not a live factor. Do not loosen the execution gate or rerun raw density on this exact branch. The next HACK attempt must be a same-root structure/PDA-stability and directional-separation overlay that filters out `pda_hybrid_alignment=false`, low PDA consistency, bullish MTF bias against Bear selection, sub-`0.08` directional probability gap, and thin/crowded low-vol contexts when execution readiness is below `0.45`.

## Real-Trade Admission Readback: YF ULTA Beauty 1m Transition-Stable V5

Append-only terminal row at `2026-05-18 21:42:10 +0800` for the active beauty/ULTA real-trade admission lane observed from `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T-codex-yf-beauty-ulta-1m-transition-stable-real-trades-admission-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T193741+0800-hermes-yf-beauty-rsi-vwap-transition-stable-overlay-1m-v5/ulta-1m-real-trades-admission-20260518T213619+0800/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260518T193741+0800-hermes-yf-beauty-rsi-vwap-transition-stable-overlay-1m-v5/`.
- Branch path: `RangeReversion -> BeautyPersonalCareOversoldReclaim -> rsi_vwap_reclaim_dense -> transition_stable_vwap_reclaim_overlay -> yf_ulta_1m_transition_stable_vwap_reclaim_v5`.
- Market/product/symbol/timeframe: `YF / single_stock / ULTA / 1m`.
- Real-trade admission: `33` rows; wins/losses/breakevens `24/9/0`; all command exits `0`; exact branch survived.
- Downstream readback: Pre-Bayes gate `pass_neutralized`; execution tree gate=`observe`, branch=`transition_guardrail`; closed-loop branch admission status=`fail_closed`, candidate_status=`execution_observe_only`.
- Path-ranker readback: score visible=`true`, used=`false`, validation_ready=`true`; current target mature rows/history mature rows `3/36`.
- Current blocker: `execution_guarded_due_to_pda_hybrid_disagreement`; execution candidate actionable=`false`, status=`no_trade`.
- Terminal decision: `real_trade_mature_readback_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/real_trade_admission_metrics.json`, `summaries/terminal_decision_summary.md`, `state/YF_BEAUTY_ULTA_1M_TRANSITION_STABLE_REAL_TRADES_V1/workflow_snapshot.json`, and `state/YF_BEAUTY_ULTA_1M_TRANSITION_STABLE_REAL_TRADES_V1/execution_tree_trace.json`.

Interpretation:

- The rooted branch and real-trade feedback survived through Auto-Quant import, Pre-Bayes, policy export, CatBoost/path-ranker application, runtime enablement, analyze, workflow, and policy readback. This improves maturity evidence, but it is still not a trade-usable factor because the execution tree remains guarded/observe-only and the path-ranker score is visible but not used by the execution tree.

## Gate 1 Fail: YF NRG Transition-Stability PDA-Alignment Overlay V1

Append-only terminal row at `2026-05-18 21:52:23 +0800` for a same-root NRG overlay targeting the prior downstream blocker (`transition_guardrail` / PDA-hybrid disagreement).

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T215112+0800-codex-yf-nrg-transition-stability-pda-alignment-overlay-v1/`.
- Branch path: `yfinance/YF -> US_EQUITY -> POWER_UTILITY_AI_LOAD -> NRG -> 5m -> TrendExpansion -> PowerUtilityAiLoadOpeningRangeExpansion -> yf_nrg_5m_orb_rvol_expansion_single_factor_v1 -> yf_nrg_5m_orb_rvol_vwap_trailing_overlay_v1 -> yf_nrg_transition_stability_pda_alignment_overlay_v1`.
- Provider evidence: fresh yfinance/YF `NRG 5m`; rows=`2964`; `local_cache_replay=false`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `1` completed row.
- Rank evidence: exact branch preserved=`true`; `trade_count=1`; `total_profit_pct=0.59`; `win_rate_pct=100.0`; `sharpe=-100.0`; 2bps/side cost stress survived only because density collapsed to one trade.
- Terminal decision: `drop_overlay_or_keep_diagnostic_no_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- The transition-stability/PDA-alignment idea was directionally too restrictive for NRG. It preserves branch grammar, but it does not preserve practical trade density, so it should be dropped as an overlay and not handed to Pre-Bayes/BBN/CatBoost/execution-tree.

## Gate 1 Fail: YF NRG Soft Transition-Stability VWAP-Alignment Overlay V2

Append-only terminal row at `2026-05-18 21:56:40 +0800` for the relaxed same-root NRG transition-stability overlay after V1 collapsed to one trade.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T215510+0800-codex-yf-nrg-soft-transition-stability-vwap-alignment-overlay-v2/`.
- Branch path: `yfinance/YF -> US_EQUITY -> POWER_UTILITY_AI_LOAD -> NRG -> 5m -> TrendExpansion -> PowerUtilityAiLoadOpeningRangeExpansion -> yf_nrg_5m_orb_rvol_expansion_single_factor_v1 -> yf_nrg_5m_orb_rvol_vwap_trailing_overlay_v1 -> yf_nrg_soft_transition_stability_vwap_alignment_overlay_v2`.
- Provider evidence: fresh yfinance/YF `NRG 5m`; rows=`2964`; `local_cache_replay=false`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `1` completed row.
- Rank evidence: exact branch preserved=`true`; `trade_count=7`; `total_profit_pct=0.77`; `win_rate_pct=57.1429`; `sharpe=0.4224`; 2bps/side cost stress survived, but practical density did not.
- Terminal decision: `drop_overlay_or_keep_diagnostic_no_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- Relaxing the transition/PDA overlay improved trade count from `1` to `7`, but still failed the same-root density gate. For this NRG branch, transition-stability filtering should not be pursued as the next overlay; it destroys the Gate 1 edge before downstream can evaluate the execution-readiness blocker.

## Current Ingest Bridge Replay: Provider Quartet Dense Kline Reclaim

Append-only terminal row at `2026-05-18 22:36:44 +0800` for an isolated copied-state replay of the provider-quartet dense-kline branch using the current real-trade ingest bridge.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T221837+0800-codex-provider-quartet-current-ingest-bridge-replay-v1/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260516T234500+0800-hermes-board-b-regime-rooted-provider-quartet-aq-chain-v1/`.
- Branch path: `TrendExpansion -> SessionLiquidity -> dense_kline_upbar_reclaim -> dense_kline_upbar_reclaim_long_v1`.
- Replay scope: copied source state only; source run state was not mutated.
- Real-trade ingest/readback: `124` feedback rows; outcomes `breakeven=28`, `loss=47`, `win=49`; `auto_quant_real_trade_entry_v1.ready=true`, `matched_rows=124`.
- Current-target CatBoost/path-ranker: export/apply/register/enable exited `0`; final runtime `status=using_candidate_set_scores`, `score_model_family=catboost`, `candidate_set_match_count=3`, `applied_path_count=3`.
- Validation readback: `raw_scored_mature=251/30`, `production_validation=251/30`, `observation_validation=124/30`.
- Pre-Bayes/read-only BBN: `gate_status=pass_neutralized`; `read_only_regime_bbn_trade_usable=false`.
- Execution readback: exact branch survived, but latest structural candidate remains `candidate_status=execution_blocked`, `execution_gate_status=execution_blocked`, `execution_readiness=0.23026049004577293`, `review_status=observe`, `ready=false`, `actionable=false`.
- Residual blockers: `source_reliability=needs_multi_source_overlap`, `target_policy=bucket_posterior_empty`, `promotion_allowed=false`.
- Terminal decision: `fail_closed_observe_only_not_trade_usable`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/final_readback.json`, `summaries/terminal_decision_summary.md`, `checks/prompt_to_artifact_checklist.csv`, `checks/14_analyze_after_ranker_reapply.out`, `checks/15_workflow_after_ranker_reapply.json`, `checks/16_policy_after_ranker_reapply.json`, and `checks/17_pre_bayes_after_ranker_reapply.json`.

Interpretation:

- This replay verifies the current ingest bridge can mature real-trade feedback into the policy/ranker validation surface and that current candidate-set CatBoost scores are consumed. It still does not create a live factor: the execution gate remains blocked, BBN marks the branch non-trade-usable, and source reliability/target-policy calibration are still incomplete.

## Gate 1 Fail: YF Pharmacy Retail ORB/RVOL Expansion 1m MTF

Append-only terminal row at `2026-05-18 22:58:12 +0800` for a fresh non-overlapping pharmacy/drugstore retail lane claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T225254+0800-codex-yf-pharmacy-retail-orb-rvol-expansion-1m-mtf-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T225344+0800-codex-yf-pharmacy-retail-orb-rvol-expansion-1m-mtf-v1/`.
- Branch path: `DefensiveRetailRotation -> PharmacyRetailOpeningRangeExpansion -> orb_rvol_expansion -> yf_pharmacy_retail_orb_rvol_expansion_1m_mtf_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `CVS` fetched for `1m/5m/15m/30m/1h` with rows `1950/2964/988/494/266`; `WBA` and `RAD` fetches returned exit `1` across requested frames.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `5` completed rows.
- Rank evidence: `rank_total_trade_count=3`; `one_minute_trades=0`; `positive_1m=[]`; `positive_higher_timeframes_trade_ge_5=[]`; exact branch fields preserved=`true`.
- Terminal decision: `keep_subclass_evidence_or_drop_gate1_no_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- This packet proves the fresh pharmacy retail lane can fetch at least CVS through the full 1m-origin ladder and exercise Auto-Quant Gate 1, but it fails practical density before downstream: no 1m trades and only three total trades across ranked rows. Keep as negative subclass evidence only; do not hand it to Pre-Bayes/BBN/CatBoost/execution-tree.

## Real-Trade Admission Readback: TVR HACK Soft Directional Density 5m

Append-only terminal row at `2026-05-18 23:09:09 +0800` for the exact 5m HACK soft-directional density branch after real-trade admission.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T215328+0800-codex-tvr-hack-soft-directional-density5m-v1/real-trades-admission-20260518T230714+0800/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260518T215328+0800-codex-tvr-hack-soft-directional-density5m-v1/`.
- Branch path: `TrendExpansion -> CybersecurityETFOpeningDrive -> five_min_soft_directional_density_origin -> tvr_hack_soft_directional_density_5m_v1`.
- Market/product/symbol/timeframe: `TVR / ETF / HACK / 5m`.
- Real-trade admission: `8` rows; wins/losses/breakevens `7/1/0`; all command exits `0`; exact branch survived.
- Maturity readback: `mature_rows=2`; `history_mature_rows=9`; prompt checklist fails both `>=30` maturity gates.
- Execution readback: execution candidate actionable=`false`, status=`no_trade`; execution tree gate=`observe`, branch=`transition_guardrail`; path-ranker visible=`true`, used=`false`, validation_ready=`true`.
- Terminal decision: `real_trade_mature_readback_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/real_trade_admission_metrics.json`, `checks/prompt_to_artifact_checklist.csv`, `summaries/terminal_decision_summary.md`, `real-trades/all_real_trades.jsonl`, and `state/TVR_HACK_SOFT_DIRECTIONAL_DENSITY_5M_V1_REAL_TRADES/execution_tree_trace.json`.

Interpretation:

- The 5m branch is better than the failed 1m HACK refinements as a scoped candidate because real trades were mostly winning and the exact branch survived, but it is still too thin for live admission. Do not promote it: it lacks the required mature rows and remains execution observe-only. Next work should seek a denser same-root 5m variant or a new branch with enough real-trade volume before spending more downstream budget.

## Gate 1 Fail: YF Single-Stock Sector Relative-Strength Reclaim 5m

Append-only terminal row at `2026-05-18 23:05:11 +0800` for a fresh
single-stock sector-leadership branch. This was selected after current FVG,
HACK/PDA, NRG, beauty, provider-quartet, and pharmacy lanes were found already
claimed or terminalized. It is not a 1m-origin proof; the 5m scope is recorded
explicitly.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T230511+0800-codex-yf-single-stock-sector-rs-reclaim-v1/`.
- Branch path: `TrendExpansion -> SingleStockSectorLeadership -> sector_relative_strength_reclaim -> yf_single_stock_sector_relative_strength_reclaim_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `QQQ`, `NVDA`,
  `MSFT`, and `AAPL` each fetched `1560` `5m` rows with exit `0`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all
  exited `0`; authoritative `ranking[]` had `3` completed rows and exact
  branch fields were preserved.
- Rank evidence: `NVDA` had `4` trades / `+2.40%`; `MSFT` had `2` trades /
  `+0.13%`; `AAPL` had `2` trades / `-0.24%`; total trades were `8`, but
  `positive_rows_trade_ge_8=0`.
- Terminal decision: `drop_gate1`; `gate1_pass_observation=false`;
  `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`;
  `execution_tree_allowed=false`; `promotion_allowed=false`;
  `trade_usable=false`; `update_goal=false`.
- Evidence files: `summaries/terminal_decision_summary.md`,
  `checks/terminal_metrics.json`, `summaries/provider_provenance_matrix.csv`,
  `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- This is useful negative subclass evidence for a single-stock sector-relative
  strength reclaim idea, but not a practical factor. Provider and AQ mechanics
  passed, yet density was too thin and no positive row reached the minimum trade
  floor, so it correctly stopped before Pre-Bayes/BBN/CatBoost/execution tree.

## Gate 1 Fail: TVR HACK Soft Directional Density 5m Dense V2

Append-only terminal row at `2026-05-18 23:25:49 +0800` for the same-root
HACK 5m density mutation claimed under
`/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T231149+0800-codex-tvr-hack-soft-directional-density-5m-dense-v2.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T232229+0800-codex-tvr-hack-softdir5m-dense-5m-dense-v2/`.
- Branch path: `TrendExpansion -> CybersecurityETFOpeningDrive -> five_min_soft_directional_density_origin -> tvr_hack_soft_directional_density_5m_dense_v2`.
- Market/product/symbol/timeframe: `TVR / ETF / HACK / 5m`, with `1m` retained as micro-context and `5m` as the training origin.
- Provider evidence: TradingViewRemix via `tradingview_mcp`; `AMEX:HACK` fetched fresh `1m/5m/15m/30m/1h/4h/1d` rows `977/1331/523/264/143/21/21`, all fetch exits `0`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `7` completed rows; exact branch fields preserved=`true`.
- Rank evidence: `rank_total_trade_count=33`; exact `5m` row had `18` trades, `44.4444%` win rate, `+0.27%` raw total profit, and Sharpe `0.8849`.
- Cost gate: `cost_fragile_or_no_dense_origin`; exact `5m` row fails cost stress (`-0.09%` at `1bps` per side, `-0.45%` at `2bps` per side); `origin_5m_survivor_count_2bps_per_side=0`; `positive_origin_5m=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/cost_stress_gate.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- The mutation increased the exact 5m row to `18` raw trades, but the edge is too small to survive even minimal transaction-cost stress. The higher-timeframe positives (`15m`, `30m`) are evidence-only because the claimed root is exact `5m`; do not pass this packet to Pre-Bayes/BBN/CatBoost/execution tree.

## Gate 1 Fail: YF SJB Transition-Stable Cost-Aware V2

Append-only terminal row at `2026-05-18 23:40:24 +0800` for a same-root SJB/TLT credit-stress mutation claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T233206+0800-codex-yf-sjb-transition-stable-costaware-v2.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T233337+0800-codex-yf-sjb-transition-stable-costaware-v2/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260518T223820+0800-codex-yf-sjb-tlt-credit-stress-continuation-1m-mtf-v1/`.
- Branch path: `StressExpansion -> CreditSpreadRiskOffContinuation -> high_yield_downside_confirmation -> yf_sjb_tlt_credit_stress_continuation_1m_mtf_v1 -> yf_sjb_transition_stable_costaware_v2`.
- Purpose: same-root mutation targeting the source branch's execution blockers: `execution_readiness=0.2415`, `hybrid_transition_hazard=0.899`, and `pda_hybrid_alignment=false`.
- Provider evidence: fresh yfinance/YF for `SJB/TLT/IEF/SHY`; `1m/5m/15m/30m/1h/1d` attempted; all key fetches exited `0` except `SJB 30m` exit `1`; `4h` was not fabricated; `local_cache_replay=false`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `23` completed rows; branch fields preserved=`true`.
- Rank evidence: `rank_total_trade_count=61`; `origin_trades_1m=1`; exact `SJB/1m` had `0` trades; `positive_origin_1m=[]`; `positive_higher_timeframes=['TLT/1d', 'SJB/5m']`.
- Cost gate: `positive_origin_after_2bps=[]`; `cost_gate_survives=false`; `dense_positive_gate=false`.
- Terminal decision: `drop_or_keep_source_backed_gate1_negative_sample`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/cost_stress_gate.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- The v2 transition-stability guard attacked the right downstream blocker, but it destroyed the 1m-origin edge before downstream: exact `SJB/1m` dropped from the prior cost-positive seed to zero trades. Keep this as negative overlay evidence only; do not pass it to Pre-Bayes/BBN/CatBoost/execution tree.

## Gate 1 Cost-Fragile: YF SJB Soft PDA Cost-Aware V3

Append-only terminal row at `2026-05-18 23:51:13 +0800` for a softer same-root SJB/TLT credit-stress mutation claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T234314+0800-codex-yf-sjb-soft-pda-costaware-v3.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T234443+0800-codex-yf-sjb-soft-pda-costaware-v3/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260518T223820+0800-codex-yf-sjb-tlt-credit-stress-continuation-1m-mtf-v1/`.
- Prior hard-guard negative: `support/docs/experiments/actionable-regime-confidence/runs/20260518T233337+0800-codex-yf-sjb-transition-stable-costaware-v2/`.
- Branch path: `StressExpansion -> CreditSpreadRiskOffContinuation -> high_yield_downside_confirmation -> yf_sjb_tlt_credit_stress_continuation_1m_mtf_v1 -> yf_sjb_soft_pda_costaware_v3`.
- Provider evidence: fresh yfinance/YF for `SJB/TLT/IEF/SHY`; `1m/5m/15m/30m/1h/1d` fetched with exit `0`; `4h` was not fabricated; `local_cache_replay=false`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `24` completed rows; branch fields preserved=`true`.
- Rank evidence: `rank_total_trade_count=294`; `origin_trades_1m=53`; `positive_origin_1m=['SHY/1m']`; exact `SJB/1m` had `6` trades and `-0.03%`; `positive_higher_timeframes=['SHY/15m', 'TLT/1d', 'SJB/5m']`.
- Cost gate: dense gate passed, but `positive_origin_after_2bps=[]`; `SHY/1m` had `27` trades and only `+0.01%` raw, flipping to `-0.53%` at `1bps` per side and `-1.07%` at `2bps` per side; `SJB/5m` similarly flipped negative after costs.
- Terminal decision: `cost_fragile_stop_before_downstream`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/cost_stress_gate.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- V3 preserved broad density better than V2, but the positive 1m evidence moved to `SHY/1m` and was too thin after transaction-cost stress. The exact SJB root did not improve. Stop before downstream; this is observation evidence for the cost-vs-PDA tradeoff, not a live factor.

## Gate 1 Correction: YF Analog Chip RSI/VWAP Reclaim Dense 1m

Append-only terminal correction at `2026-05-19 00:03:40 +0800` for the analog-chip RSI/VWAP reclaim packet claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T2346+0800-codex-yf-analog-chip-rsi-vwap-reclaim-1m-dense-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T235027+0800-codex-yf-analog-chip-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> AnalogChipOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_analog_chip_rsi_vwap_reclaim_1m_dense_v1`.
- Provider evidence: fresh yfinance/YF for `TXN/MCHP/MPWR`; `1m/5m/15m/30m/1h/4h/1d` requested; `4h` failed as unsupported and was not fabricated; `MPWR/1m` fetch failed; `local_cache_replay=false`.
- Initial rank evidence: `rank_rows=17`, `rank_total_trades=695`, `positive_rows_trade_ge_5=10`, with positive rows concentrated in `5m/15m/30m/1h`.
- Corrected 1m-origin evidence: `TXN/1m` had `26` trades / `-0.14%`; `MCHP/1m` had `27` trades / `-1.93%`; `MPWR/1m` had no usable provider row.
- Corrected decision: `corrected_drop_gate1_no_downstream`; `positive_origin_1m=[]`; `positive_origin_after_2bps=[]`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/gate1_origin_correction.json`, `summaries/gate1_origin_correction_summary.md`, `checks/terminal_metrics.json`, and `summaries/terminal_decision_summary.md`.

Interpretation:

- The first summary over-read higher-timeframe positives as downstream-worthy. The corrected gate keeps the branch fail-closed because the required `1m` origin is negative or unavailable. Higher-timeframe positives are subclass/context evidence only; they do not promote this `1m` lane.

## Downstream Fail-Closed: YF Analog Chip RSI/VWAP Reclaim Dense 1m

Append-only downstream readback at `2026-05-19 00:03:40 +0800` for the same analog-chip branch after a downstream runner executed despite the corrected Gate 1 origin block.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T235027+0800-codex-yf-analog-chip-rsi-vwap-reclaim-1m-dense-v1/downstream-20260519T000009+0800/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260518T235027+0800-codex-yf-analog-chip-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> AnalogChipOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_analog_chip_rsi_vwap_reclaim_1m_dense_v1`.
- Command readback: Auto-Quant import/prior, analyze, workflow, Pre-Bayes, target export, policy status, CatBoost train/apply, score import, trainer registration, runtime enablement, post-ranker analyze/workflow/policy all exited `0`.
- Downstream evidence: `rank_rows=17`, `rank_positive_rows=10`, `rank_total_trades=695`; execution candidate actionable=`false`, status=`no_trade`; execution tree gate=`observe`, branch=`transition_guardrail`; path-ranker visible=`true`, used=`false`, validation_ready=`false`.
- Terminal decision: `fail_closed_after_unearned_downstream`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- This downstream run is useful as a fail-closed readback but not as promotion evidence. It confirms the branch still cannot pass execution-tree admission even when the downstream mechanics run cleanly, and the prior Gate 1 origin correction remains the controlling decision.

## Downstream Fail-Closed: YF SJB/TLT Credit-Stress All-Material Real Trades

Append-only terminal row at `2026-05-19 00:03:40 +0800` for the SJB/TLT credit-stress all-material real-trade downstream lane claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T234000+0800-codex-yf-sjb-tlt-credit-stress-all-material-real-trades-downstream-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T223820+0800-codex-yf-sjb-tlt-credit-stress-continuation-1m-mtf-v1/downstream-all-material-real-trades-20260518T234536+0800/`.
- Source: `support/docs/experiments/actionable-regime-confidence/runs/20260518T223820+0800-codex-yf-sjb-tlt-credit-stress-continuation-1m-mtf-v1/`.
- Branch path: `StressExpansion -> CreditSpreadRiskOffContinuation -> high_yield_downside_confirmation -> yf_sjb_tlt_credit_stress_continuation_1m_mtf_v1`.
- Real-trade extraction: `480` rows from all material units; wins/losses/breakevens `154/293/33`.
- Command readback: extraction, Auto-Quant import/prior, analyze/workflow/Pre-Bayes, target export, real-trade ingest, CatBoost train/apply, score import, trainer registration, runtime enablement, post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`.
- Downstream evidence: exact branch survived=`true`; `mature_rows=3`, `history_mature_rows=491`; path-ranker visible=`true`, used=`false`, validation_ready=`true`.
- Execution readback: execution candidate actionable=`false`, status=`no_trade`; execution tree gate=`observe`, branch=`transition_guardrail`; closed-loop branch admission status=`fail_closed`; execution readiness, transition hazard, and PDA-hybrid alignment were not promoted into valid pass fields.
- Terminal decision: `all_material_real_trades_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/all_material_downstream_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/prompt_to_artifact_checklist.csv`, `real-trades/all_real_trades.jsonl`, and `state/YF_SJB_TLT_CREDIT_STRESS_ALL_MATERIAL_REAL_TRADES_DOWNSTREAM/`.

Interpretation:

- This run fixed the earlier “too little downstream maturity” question by ingesting all material-unit real trades, but it still fails live admission. The branch is visible through the downstream chain and the ranker is validation-ready, yet execution remains observe-only under the transition guardrail and the candidate is not actionable.

## Provider Window Blocked: IBKR CRM VWAP Reclaim 1m MTF Gate 1

Append-only terminal row at `2026-05-19 00:03:40 +0800` for the IBKR CRM single-stock lane claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260518T234840+08:00-codex-ibkr-crm-vwap-reclaim-1m-mtf-gate1-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260518T235247+0800-codex-ibkr-crm-vwap-reclaim-1m-mtf-gate1-v1/`.
- Branch path: `RangeReversion -> EnterpriseSoftwareLiquidityReclaim -> vwap_reclaim_pullback -> ibkr_crm_vwap_reclaim_1m_mtf_gate1_v1`.
- Provider evidence: `provider-status --provider ibkr --agent` exited `0`, but all requested CRM fetches returned exit `3`.
- Requested ladder: `1m`, `5m`, `15m`, `30m`, `1h`; `1m` tried `1 M` then `7 D`; `5m/15m/30m/1h` tried `3 M` then `1 M`.
- Acquired timeframes: none; `local_cache_replay=false`; no Auto-Quant materials were generated.
- Gate result: `rank_rows=0`; `rank_total_trade_count=0`; `one_minute_trades=0`; `positive_origin_1m=[]`; `positive_origin_after_2bps=[]`.
- Terminal decision: `provider_window_blocked_no_gate1_verdict`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and fetch exit files under `checks/`.

Interpretation:

- This is not a factor failure. IBKR was reachable at the provider-status level, but the CRM historical windows produced no usable rows, so the packet stops as a provider/window blocker before Gate 1 scoring.

## Gate 1 Negative: YF Extreme-Reversal VWAP Reclaim 1m MTF

Append-only terminal row at `2026-05-19 00:17:14 +0800` for the 1m-origin extreme-reversal VWAP reclaim lane claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T001054+0800-codex-yf-extreme-reversal-vwap-reclaim-1m-mtf-gate1-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T001138+0800-codex-yf-extreme-reversal-vwap-reclaim-1m-mtf-gate1-v1/`.
- Branch path: `Range -> ExtremeReversal -> vwap_reclaim_extreme -> one_minute_extreme_reversal_vwap_reclaim_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `SPY`, `QQQ`, and `SMH` all fetched `1m/5m/15m/30m/1h` with exit `0`; retained rows were roughly `1722-1724` for `1m`, `2920` for `5m`, `974` for `15m`, `488` for `30m`, and `263` for `1h`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `15` completed rows and rooted branch fields preserved=`true`.
- Rank evidence: `rank_total_trade_count=128`; claimed `1m` origin had only `7` total trades (`QQQ/1m=2`, `SPY/1m=1`, `SMH/1m=4`).
- Higher-timeframe positives: `SPY/15m`, `SMH/5m`, `SPY/5m`, `QQQ/5m`, and `SMH/15m` had positive rows with at least `8` trades, but those are not the claimed `1m` origin.
- Terminal decision: `keep_negative_sample`; `downstream_allowed=false_for_claimed_1m_origin`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `checks/*.exit`.

Interpretation:

- This run is useful because it exercised a fresh real-provider, branch-rooted Auto-Quant Gate 1 packet and found plausible `5m/15m` transfer evidence. It still fails the explicit `1m` origin density requirement, so it must not go to Pre-Bayes/BBN/CatBoost/execution tree as a `1m` branch. A separate correctly rooted `5m` follow-up may be tested, but it must start as its own timeframe branch and pass cost stress before downstream.

## Downstream Fail-Closed: YF SPY Extreme-Reversal VWAP Reclaim 15m Exact Origin

Append-only terminal row at `2026-05-19 00:31:00 +0800` for the separate SPY/15m exact-origin follow-up claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T002300+0800-codex-yf-spy-extreme-reversal-vwap-reclaim-15m-exact-gate1-v1.claim`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T002349+0800-codex-yf-spy-extreme-reversal-vwap-reclaim-15m-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T002349+0800-codex-yf-spy-extreme-reversal-vwap-reclaim-15m-exact-gate1-v1/downstream-exact-spy-15m-20260519T002727+0800/`.
- Branch path: `Range -> ExtremeReversal -> vwap_reclaim_extreme_15m_origin -> spy_extreme_reversal_vwap_reclaim_15m_v1`.
- Gate 1 evidence: fresh yfinance/YF `SPY 15m`, no local cache replay, `975` provider rows; Auto-Quant batch/dispatch/rank exited `0`; authoritative `ranking[]` had `1` row with rooted fields preserved.
- Rank/cost evidence: `9` trades, win rate `77.7778%`, raw total profit `+1.95%`, cost stress survived `5bps/side` with `+1.05%`.
- Downstream command readback: real-trade extraction, Auto-Quant import/prior, analyze, workflow, Pre-Bayes, target export, real-trade ingest, CatBoost train/apply, score import, trainer registration, runtime enablement, post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`.
- Downstream evidence: real trades `9` rows (`7/2/0` wins/losses/breakevens); exact branch survived=`true`; `mature_rows=2`, `history_mature_rows=10`; path-ranker visible=`true`, used=`false`, validation_ready=`true`.
- Execution readback: execution candidate actionable=`false`, status=`no_trade`; execution tree gate=`observe`, branch=`transition_guardrail`; execution readiness `0.5138168612202155`; transition hazard `0.9151204856883439`; PDA-hybrid alignment=`false`.
- Terminal decision: `exact_spy_15m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, `checks/cost_stress_gate.json`, `summaries/terminal_decision_summary.md`; downstream `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`, `real-trades/all_real_trades.jsonl`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- This is the strongest exact follow-up from the failed `1m` extreme-reversal packet, but it remains observation-only. It passed provider/AQ/cost and preserved the exact branch through downstream mechanics, yet it fails live admission on maturity, transition hazard, PDA alignment, execution readiness, and actionable execution-candidate status. The earlier `1m` origin remains failed; this `15m` branch is candidate evidence only.

## Gate 1 Negative: YF Broad ETF PDA Trend-Continuation 1m MTF

Append-only terminal row at `2026-05-19 00:45:00 +0800` for the trend/session-liquidity pivot claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T004000+0800-codex-yf-broad-etf-pda-trend-continuation-1m-mtf-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T003751+0800-codex-yf-broad-etf-pda-trend-continuation-1m-mtf-v1/`.
- Branch path: `Trend -> SessionLiquidity -> pda_aligned_trend_continuation -> yf_broad_etf_pda_trend_continuation_1m_mtf_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `SPY`, `QQQ`, and `IWM` all fetched `1m/5m/15m/30m/1h` with exit `0`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `15` completed rows and rooted branch fields preserved=`true`.
- Rank evidence: `rank_total_trade_count=26`; claimed `1m` origin had `0` trades across `SPY/1m`, `QQQ/1m`, and `IWM/1m`.
- Cost evidence: `origin_survivors_2bps=[]`; `origin_survivors_5bps=[]`; `SPY/5m` was the only positive row with at least `8` trades, but it is not the claimed `1m` origin.
- Terminal decision: `keep_negative_sample`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/gate1_cost_stress_audit.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- This pivot tested whether a trend-aligned/session-liquidity root could avoid the reversal branches' transition/PDA failure mode. It did not earn Gate 1 because the exact `1m` origin produced zero trades. Higher-timeframe positives are observation/context evidence only and must not be used to pass Pre-Bayes/BBN/CatBoost/execution tree for this `1m` branch.

## Auto-Quant Seed Only: YF Broad ETF PDA Trend Autoresearch

Append-only terminal row at `2026-05-19 00:56:00 +0800` for the Auto-Quant autoresearch seed pass claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T004700+0800-codex-yf-broad-etf-pda-trend-autoresearch-seed-v1.claim`.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T003751+0800-codex-yf-broad-etf-pda-trend-continuation-1m-mtf-v1/`.
- `1m` autoresearch root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T003751+0800-codex-yf-broad-etf-pda-trend-continuation-1m-mtf-v1/autoresearch-20260519T004700/`.
- `5m` autoresearch root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T003751+0800-codex-yf-broad-etf-pda-trend-continuation-1m-mtf-v1/autoresearch-20260519T005200-5mseed/`.
- Control-plane commands: `factor-autoresearch --backend auto-quant --auto-quant-profile synthetic_ohlcv` exited `0`; `auto-quant-prepare` exited `0`; post-prepare `factor-autoresearch` exited `0`.
- First `1m` seed attempt: `run_tomac.py` exited `1` with `No data left after adjusting for startup candles` after synthetic `1h/4h` collapse.
- `5m` seed attempt: `run_tomac.py` exited `0`; measured strategy `TomacNQ_KillzoneBreakout` on synthetic `BROAD/USD 1h` had `8` trades, `+3.51%` total profit, Sharpe `1.0344`, Sortino `7.4351`, profit factor `2.2921`, win rate `50.0%`, and max drawdown `-2.6003%`.
- Terminal decision: `autoquant_seed_only_no_live_promotion`; `live_provider_parity=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `command-output/04_run_tomac.out`, and the `auto_quant_handoff.factor_autoresearch.json` under the `5m` seed state.

Interpretation:

- This satisfies the requested Auto-Quant autonomous iteration as a seed-discovery step, but it is not a Board B promotion packet. The positive result is synthetic `BROAD/USD 1h`, not an exact `SPY/QQQ/IWM` real-provider `1m`/`5m` material. Any reuse must be converted into a fresh exact real-provider Auto-Quant material branch and pass cost/density before downstream gates.

## Gate 1 Negative: YF Broad ETF PDA Trend-Continuation Dense 1m MTF v2

Append-only terminal row at `2026-05-19 01:33:31 +0800` for the dense trend/session-liquidity follow-up claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T005900+0800-codex-yf-broad-etf-pda-trend-continuation-dense-1m-mtf-v2.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T011014+0800-codex-yf-broad-etf-pda-trend-continuation-dense-1m-mtf-v2/`.
- Branch path: `Trend -> SessionLiquidity -> pda_aligned_trend_continuation_dense -> yf_broad_etf_pda_trend_continuation_dense_1m_mtf_v2`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `SPY`, `QQQ`, and `IWM` all fetched `1m/5m/15m/30m/1h` with exit `0`; retained rows were `1781-1785` for `1m`, `2932` for `5m`, `978` for `15m`, `490` for `30m`, and `264` for `1h`.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `15` completed rows and rooted branch fields preserved=`true`.
- Rank evidence: `rank_total_trade_count=192`; claimed `1m` origin had only `5` total trades (`SPY/1m=2`, `QQQ/1m=3`, `IWM/1m=0`).
- Cost evidence: `origin_survivors_2bps=[]`; `origin_survivors_5bps=[]`. `QQQ/1m` remained positive after cost stress but had only `3` trades, below the density floor.
- Higher-timeframe positives: `QQQ/30m`, `SPY/5m`, `QQQ/5m`, and `IWM/5m` had positive rows with at least `8` trades, but those are not the claimed `1m` origin.
- Terminal decision: `keep_negative_sample`; `downstream_allowed=false_for_claimed_1m_origin`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/gate1_cost_stress_audit.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- This rerun resolves the earlier disk-full partial attempt with a clean Gate 1 verdict. The denser entry family materially increased total ladder trades, but the exact `1m` origin still fails density and cost-survivor requirements. No Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution-tree carry is allowed for this `1m` branch.

## Downstream Fail-Closed: YF AI-Security CRWD 5m Session-Liquidity Overlay

Append-only terminal row at `2026-05-19 02:16:00 +0800` for the same-root CRWD 5m session-liquidity overlay claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T020000+0800-codex-yf-ai-security-crwd5m-session-liquidity-overlay-gate1-v1.claim`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T020012+0800-codex-yf-ai-security-crwd5m-session-liquidity-overlay-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T020012+0800-codex-yf-ai-security-crwd5m-session-liquidity-overlay-gate1-v1/downstream-exact-crwd-5m-session-liquidity-20260519T020523+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Gate 1 evidence: retained real yfinance/YF CRWD rows from the source packet for `5m/15m/30m/1h/1d`; strategy compile, material batch, dispatch, and rank all exited `0`; exact `CRWD/5m` had `36` trades, `66.6667%` win rate, `+5.62%` raw total profit, Sharpe `4.1737`, `+4.18%` after `2bps/side`, and `+2.02%` after `5bps/side`.
- Downstream readback: real-trade extraction, Auto-Quant import/prior, analyze/workflow/Pre-Bayes, target export, real-trade ingest, CatBoost train/apply, score import, trainer registration, runtime enablement, and post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`.
- Downstream evidence: real trades `36` rows (`24/12/0` wins/losses/breakevens); exact branch survived=`true`; path-ranker visible=`true`, used=`true`, validation-ready=`true`; `mature_rows=3`, `history_mature_rows=39`.
- Execution readback: execution candidate actionable=`false`, status=`no_trade`; execution tree gate=`blocked`, branch=`block_crowded`; execution readiness `0.3741236047533753`; transition hazard `0.5950369253623637`; PDA-hybrid alignment=`true`.
- Terminal decision: `exact_crwd_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`; downstream `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`, `real-trades/all_real_trades.jsonl`, and `checks/*.exit`.

Interpretation:

- This overlay preserved real-cost CRWD/5m density and kept PDA alignment with transition hazard just under the `0.60` ceiling, but it did not improve the execution-context blocker. The execution tree still blocks at `block_crowded` with readiness below both observe and promotion gates, so the branch remains observation-only and must not be marked trade-usable.

## Gate 1 Negative: YF AI-Security CRWD 5m Reversal-Fatigue Suppression Overlay

Append-only terminal row at `2026-05-19 06:12:11 +0800` for the same-root CRWD 5m readiness-repair overlay claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T055000+0800-codex-yf-ai-security-crwd5m-reversal-fatigue-suppression-gate1-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T060914+0800-codex-yf-ai-security-crwd5m-reversal-fatigue-suppression-gate1-v1/`.
- Source packet: `support/docs/experiments/actionable-regime-confidence/runs/20260519T020012+0800-codex-yf-ai-security-crwd5m-session-liquidity-overlay-gate1-v1/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> reversal_fatigue_suppression_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Provider evidence: retained real yfinance/YF CRWD rows from the source packet; `local_cache_replay=true`; retained frames were `5m/15m/30m/1h/1d`.
- Auto-Quant path: provider status, strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `5` completed rows.
- Rank evidence: `rank_total_trade_count=0`; exact `CRWD/5m` trades `0`; all supporting frames also had `0` trades.
- Cost evidence: no row survived `0/1/2/5bps` because there were no trades.
- Terminal decision: `same_root_reversal_fatigue_suppression_gate1_failed_no_downstream`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `state/auto-quant/YF_AI_SECURITY_CRWD5M_REVERSAL_FATIGUE_SUPPRESSION_GATE1_V1/auto_quant_agent_material_rank.20260518T221044.791Z.json`.

Interpretation:

- This exact readiness-repair child overlay over-suppressed the CRWD branch. It targeted the prior `block_crowded` / `ReversalBrewing` / `TrendFatigue` / `Exhaustion` blocker, but it collapsed all retained timeframes to zero trades. It is a useful negative observation and must not proceed to Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree.

## Gate 1 Negative: YF Inverse-Leveraged Stress Trend-Continuation Dense 1m MTF 1d

Append-only terminal row at `2026-05-19 09:04:58 +0800` for the inverse-leveraged ETF stress-trend continuation lane after observing pre-write hash `501e03263b6a014a5413721bc98a0b294457b5589695d6f901e8ded876eda946`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T061259+0800-codex-yf-inverse-leveraged-stress-trend-continuation-dense-1m-mtf-1d-v1/`.
- Branch path: `StressTrend -> InverseLeveragedEtfStressContinuation -> inverse_leveraged_stress_trend_continuation_dense -> yf_inverse_leveraged_stress_trend_continuation_dense_1m_mtf_1d_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `SQQQ`, `SPXS`, and `TZA` all fetched `1m/5m/15m/30m/1h/1d` with exit `0`; `4h` was attempted for all three and failed with exit `1`, so it is recorded as provider-unsupported/missing rather than fabricated.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; `18` materials produced `18` rank rows; rooted branch fields preserved=`true`.
- Rank evidence: `rank_total_trade_count=288`; claimed `1m` origin had `37` total trades across `TZA/1m=26`, `SQQQ/1m=9`, and `SPXS/1m=2`.
- Cost evidence: `origin_survivors_2bps=[]`; `origin_survivors_5bps=[]`. `TZA/1m` had raw `+0.77%` and `65.3846%` win rate, but fell to `-0.27%` at `2bps/side` and `-1.83%` at `5bps/side`.
- Higher-timeframe positives: `TZA/15m` and `TZA/1m` were the only positive rows with at least `8` trades, but the exact `1m` origin has no cost-stressed survivor at `2bps/side`.
- Terminal decision: `keep_negative_sample`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/gate1_cost_stress_audit.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- This run proves the inverse-leveraged stress-trend branch can generate real provider-backed ladder trades, but the claimed `1m` origin does not survive realistic friction. It should be preserved as a cost-stress negative sample and must not proceed to Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree.

## Gate 1 Negative: YF Office-REIT RSI/VWAP Reclaim Dense 1m MTF 1d

Append-only terminal row at `2026-05-19 09:14:20 +0800` for the office-REIT range-reversion follow-up after observing pre-write hash `1ee9edb15d954f7ea7a25d96998307bf935d9abde43ff452ad85d8157af13501`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T090435+0800-codex-yf-office-reit-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> OfficeReitOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_office_reit_rsi_vwap_reclaim_1m_dense_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `BXP`, `VNO`, and `KRC` fetched `1m/15m/30m/1h/1d`; `BXP` and `VNO` also fetched `5m`; `KRC/5m` failed with exit `1`; `4h` was attempted for all three and failed with exit `1`, so it is recorded as provider-unsupported/missing rather than fabricated.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; `17` rank rows were produced; rooted branch fields preserved=`true`.
- Rank evidence: `total_trades=674`; claimed `1m` origin had `59` trades across `BXP/1m=25`, `VNO/1m=16`, and `KRC/1m=18`.
- Raw positives: higher-timeframe rows such as `KRC/15m`, `KRC/30m`, `BXP/15m`, `BXP/5m`, and `VNO/5m` were positive and trade-dense, but they are not enough to rescue the claimed `1m` origin.
- Terminal decision: `cost_fragile_1m_origin_no_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/22_auto_quant_agent_material_dispatch.exit`, and `checks/23_auto_quant_agent_material_rank.exit`.

Interpretation:

- This follow-up fixed the prior office-REIT ORB/RVOL zero-trade problem and generated a dense real-provider ladder, but the corrected Gate 1 rule still blocks it: the `1m` origin must survive realistic cost stress before downstream is allowed. Preserve it as office-REIT range-reversion evidence and do not run Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree from this packet.

## Gate 1 Negative: YF Warehouse/Freight RSI/VWAP Reclaim Dense 1m MTF 1d

Append-only terminal row at `2026-05-19 09:16:29 +0800` for the warehouse/freight range-reversion lane after observing pre-write hash `d0eac1d9f0b9b615d7a9279f1e328fade947b01c52a47c709a29524415ce096f`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T091040+0800-codex-yf-warehouse-freight-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> WarehouseFreightOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_warehouse_freight_rsi_vwap_reclaim_1m_dense_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `XPO`, `GXO`, and `CHRW` fetched `1m/5m/15m/30m/1h/1d` with exit `0`; `4h` was attempted for all three and failed with exit `1`, so it is recorded as provider-unsupported/missing rather than fabricated.
- Auto-Quant path: provider status, strategy compile, material batch, dispatch, and rank all exited `0`; `18` rank rows were produced; rooted branch fields preserved=`true`.
- Rank evidence: `total_trades=878`; claimed `1m` origin had `56` trades across `CHRW/1m=10`, `XPO/1m=27`, and `GXO/1m=19`.
- Cost evidence: `origin_cost_stress_survivors_2bps=[]`; `origin_positive_rows_trade_ge_5=[]`. The completed `1m` rows were already negative before costs: `CHRW/1m=-1.31%`, `XPO/1m=-0.72%`, and `GXO/1m=-0.29%`; after `2bps/side`, they fell to `-1.71%`, `-1.80%`, and `-1.05%`.
- Higher-timeframe positives: `CHRW/1h`, `GXO/1h`, `CHRW/30m`, `XPO/5m`, `CHRW/5m`, and `GXO/5m` were positive with at least `5` trades, but they are not enough to rescue the claimed `1m` origin.
- Terminal decision: `drop_gate1_origin_negative_no_downstream`; corrected from the packet's earlier `decision_before_origin_profit_correction=promote_gate1_candidate_to_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, `checks/22_auto_quant_agent_material_dispatch.exit`, and `checks/23_auto_quant_agent_material_rank.exit`.

Interpretation:

- This run proves the warehouse/freight branch can produce a dense real-provider ladder, but the exact `1m` origin is directionally negative before friction and deteriorates further under realistic costs. Preserve it as a same-family range-reversion negative sample and do not run Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree from this packet.

## Gate 1 Negative: YF Medical-Diagnostics RSI/VWAP Reclaim Dense 1m MTF 1d

Append-only terminal row at `2026-05-19 01:46:20 +0800` for the medical-diagnostics range-reversion lane after observing pre-write hash `4347524a1254c1ca82c7a0af65b5f50404318184113638ea02f5ad4470dd7eeb`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T011815+0800-codex-yf-medical-diagnostics-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> MedicalDiagnosticsOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_medical_diagnostics_rsi_vwap_reclaim_1m_dense_v1`.
- Provider evidence: fresh yfinance/YF after an earlier incomplete provider attempt; `DGX`, `LH`, and `IDXX` were retained across the ladder where provider fetches completed, with unsupported or failed fetches preserved as provider gaps rather than fabricated frames.
- Auto-Quant path: material dispatch and rank completed; rooted branch fields were preserved through the packet.
- Rank evidence: `total_trades=812`; claimed `1m` origin had `55` trades; no `1m` origin row survived the practical origin gate.
- Raw positives: `DGX/30m` and `IDXX/15m` were positive higher-timeframe sibling/context rows, but they do not rescue the claimed `1m` origin.
- Terminal decision: `drop_gate1_origin_negative_no_downstream`; corrected from the packet's earlier `decision_before_origin_profit_correction=promote_gate1_candidate_to_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/practical_origin_gate_audit.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- This run fixed the earlier sparse medical-diagnostics ORB direction by producing a dense real-provider ladder, but the exact `1m` origin failed the practical profitability gate. Preserve the higher-timeframe positives as sibling/context evidence only; do not run Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree from the `1m` packet.

## Downstream Readback Negative: YF Medical-Diagnostics DGX 30m RSI/VWAP Reclaim Exact

Append-only terminal row at `2026-05-19 01:55:52 +0800` for the exact `DGX/30m` sibling readback claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T014944+0800-codex-yf-medical-diagnostics-dgx30m-exact-downstream-readback-v1.claim`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T011815+0800-codex-yf-medical-diagnostics-rsi-vwap-reclaim-1m-dense-v1/downstream-exact-dgx-30m-20260519T015308+0800/`.
- Source packet: `support/docs/experiments/actionable-regime-confidence/runs/20260519T011815+0800-codex-yf-medical-diagnostics-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> MedicalDiagnosticsOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_medical_diagnostics_dgx30m_rsi_vwap_reclaim_exact_v1`.
- Source material package: `yf-medical-diagnostics-rsi-vwap-reclaim-dgx-30m-v1`.
- Gate 1 exact evidence: `rank_trade_count=41`, `rank_total_profit_pct=4.66`, `rank_sharpe=4.6539`; estimated `2bps/side` cost survivor=`true`.
- Downstream readback: all command exits were `0`; exact branch survived=`true`; path-ranker visible=`true`, used=`false`, validation-ready=`true`.
- Pre-Bayes / execution evidence: `pre_bayes_gate_status=pass_neutralized`; execution tree gate=`observe`, branch=`transition_guardrail`; execution candidate status=`no_trade`; execution candidate actionable=`false`.
- Runtime metrics: `mature_rows=3`, `history_mature_rows=44`, `execution_readiness=0.34027925373899215`, `transition_hazard=0.6130820488174753`, `pda_hybrid_alignment=true`.
- Terminal decision: `exact_dgx_30m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/downstream_metrics.json`, `checks/prompt_to_artifact_checklist.csv`, `summaries/terminal_decision_summary.md`, and downstream `checks/*.exit`.

Interpretation:

- The exact `DGX/30m` sibling deserved readback because the per-timeframe contract does not automatically fail a positive higher-timeframe lane when the `1m` origin fails. It still closes negative: execution tree only reaches observe mode, the candidate remains `no_trade`, transition hazard is above the practical ceiling, and readiness is below promotion. Preserve it as sibling evidence and do not mark it trade-usable.

## Gate 1 Negative: YF Sports-Betting ORB/RVOL Expansion 1m MTF 1d Rerun

Append-only terminal row at `2026-05-19 10:13:52 +0800` for the sports-betting trend-expansion lane after observing pre-write hash `8088876ae10f288b64fae481e2eb80d8c7a8c1738cdbecb3954acb2632f6451c`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T095623+0800-codex-yf-sports-betting-orb-rvol-expansion-1m-mtf-1d-v1/`.
- Branch path: `TrendExpansion -> SportsBettingOpeningRangeExpansion -> orb_rvol_expansion -> yf_sports_betting_orb_rvol_expansion_1m_mtf_1d_v1`.
- Provider evidence: real yfinance/YF fetches for `DKNG`, `PENN`, and `FLUT` across `1m/5m/15m/30m/1h/1d`, with `local_cache_replay=false`; `4h` remained unavailable through the Yahoo fetch path and was not fabricated.
- Auto-Quant path: provider fetches, strategy compile, material batch, dispatch, and rank exited `0`; rooted branch fields were preserved across `18` material/rank rows.
- Rank evidence: `rank_total_trade_count=0`, `one_minute_trades=0`, `positive_1m=[]`, `positive_higher_timeframes_trade_ge_5=[]`, and `dense_positive_gate=false`.
- Terminal decision: `drop_or_block_gate1_practical`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`; `skill_update=not_needed`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- This rerun has clean provider and Auto-Quant plumbing but no practical signal: every ranked `DKNG/PENN/FLUT` timeframe row produced zero trades and zero profit. Preserve it as a same-branch negative sample only; do not run downstream gates from this packet.

## Downstream Readback Negative: YF Quantum IONQ 5m RSI/VWAP Reclaim Exact

Append-only terminal row at `2026-05-19 10:11:02 +0800` for the exact `IONQ/5m` sibling readback after observing pre-write hash `845b9906c2d29c34e4aef95e50f939bf94cc5013720ca57f5dd167601532adfc`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T092457+0800-codex-yf-quantum-computing-rsi-vwap-reclaim-1m-dense-v1/downstream-exact-ionq-5m-20260519T095324+0800/`.
- Source packet: `support/docs/experiments/actionable-regime-confidence/runs/20260519T092457+0800-codex-yf-quantum-computing-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> QuantumComputingOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_quantum_computing_rsi_vwap_reclaim_1m_dense_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> IONQ -> 5m`.
- Source material package: `yf-quantum-computing-rsi-vwap-reclaim-ionq-5m-v1`.
- Gate 1 exact evidence: `141` trades, `74.4681%` win rate, `+10.58%` raw total profit, Sharpe `7.0032`; after cost stress it retained `+7.76%` at `1bps/side`, `+4.94%` at `2bps/side`, and fell to `-3.52%` at `5bps/side`.
- Downstream readback: real-trade extraction, Auto-Quant result import/prior, analyze/workflow/Pre-Bayes, target export, real-trade ingest, CatBoost train/apply, score import, trainer registration, runtime enablement, and post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`.
- Downstream evidence: real trades `141` rows (`105/36/0` wins/losses/breakevens); exact branch survived=`true`; path-ranker visible=`true`, used=`true`, validation-ready=`true`; `mature_rows=3`, `history_mature_rows=144`.
- Execution readback: execution candidate actionable=`false`, status=`no_trade`; workflow closed-loop status=`fail_closed`; execution tree gate=`observe`, branch=`transition_guardrail`; execution readiness `0.67`; execution score `0.39635933869053985`; transition hazard `0.9663401613090454`; PDA-hybrid alignment=`false`.
- Terminal decision: `exact_ionq_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/downstream_metrics.json`, `checks/prompt_to_artifact_checklist.csv`, `summaries/terminal_decision_summary.md`, `real-trades/all_real_trades.jsonl`, `path_ranker_model/trainer_artifact.json`, `path_ranker_scores.csv`, and `state/YF_QUANTUM_RSI_VWAP_RECLAIM_IONQ_5M_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- The exact `IONQ/5m` lane was strong enough at Gate 1 and remained visible through the full downstream chain, including real-trade ingestion and a live path-ranker score used by the execution tree. It still fails closed because the execution candidate is `no_trade`, PDA/hybrid alignment is false, and transition hazard is extreme. Preserve it as a high-quality cost-stressed sibling packet, but do not mark it promotion-ready or trade-usable.

## Downstream Readback Negative: YF Database-Software RSI/VWAP Reclaim Dense 1m

Append-only terminal row at `2026-05-19 10:21:26 +0800` for the database-software range-reversion lane after observing pre-write hash `73064c9bab30af186ea937d4a19f9291ace81c135b2dc52318c2229e52324afd`.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T063331+0800-codex-yf-database-software-rsi-vwap-reclaim-1m-dense-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T063331+0800-codex-yf-database-software-rsi-vwap-reclaim-1m-dense-v1/downstream-20260519T101826+0800/`.
- Branch path: `RangeReversion -> DatabaseSoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_database_software_rsi_vwap_reclaim_1m_dense_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `ORCL`, `MDB`, and `DBX` fetched `1m/5m/15m/30m/1h/1d`; `4h` was attempted for all three and failed with exit `1`, so it is recorded as provider-unsupported/missing rather than fabricated.
- Gate 1 evidence: `total_trades=736`; `origin_trades_1m=60`; `positive_rows_trade_ge_5=10`; `dense_positive_gate=true`; source-selected row `ORCL/30m` had `28` trades, `75.0%` win rate, `+7.93%` total profit, Sharpe `1.9747`; `ORCL/1m` also remained positive with `20` trades and `+1.81%`.
- Downstream readback: Auto-Quant import/prior, analyze/workflow/Pre-Bayes, target export, CatBoost train/apply, score import, trainer registration, runtime enablement, and post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`.
- Downstream evidence: exact branch survived=`true`; Pre-Bayes gate=`pass_hard`; CatBoost trained=`true`; policy history rows=`18`; mature history rows=`9`; path-ranker visible to execution tree=`true`, but used=`false` because validation-ready=`false`.
- Execution readback: execution tree gate=`observe`, branch=`transition_guardrail`, bias=`guarded`; execution readiness `0.5306123997766115`; execution score `0.7034124252840841`; transition hazard `0.9686632089505138`; PDA-hybrid alignment=`false`; closed-loop status=`fail_closed`.
- Terminal decision: `gate1_pass_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `path_ranker_model/trainer_artifact.json`, downstream `path_ranker_model/path_ranker_scores.csv`, and downstream `state/YF_DATABASE_SOFTWARE_RSI_VWAP_RECLAIM_1M_DENSE_V1_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- This database-software branch passed Gate 1 and survived the full downstream chain with a hard Pre-Bayes pass and trained CatBoost artifact. It still fails closed for live promotion because execution remains observe-only, the execution tree takes the transition guardrail branch, PDA/hybrid alignment is false, transition hazard is extreme, and the path-ranker score is visible but not validation-ready for use. Preserve it as a high-quality downstream negative packet; do not mark it trade-usable.

## Gate 1 Negative: YF Homebuilder ORB/RVOL Expansion 1m MTF 1d

Append-only terminal row at `2026-05-19 11:03:55 +0800` for the homebuilder opening-range expansion lane after observing pre-write hash `9545162279cf450841e48524397e0322f9e01b517efb637453bd6e8a81383e37`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T105110+0800-codex-yf-homebuilder-orb-rvol-expansion-1m-mtf-1d-v1/`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T104303+0800-codex-yf-homebuilder-orb-rvol-expansion-1m-mtf-1d-v1.claim`.
- Branch path: `TrendExpansion -> HomebuilderOpeningRangeExpansion -> orb_rvol_expansion -> yf_homebuilder_orb_rvol_expansion_1m_mtf_1d_v1`.
- Provider evidence: fresh yfinance/YF, no local cache replay. `DHI`, `LEN`, and `PHM` fetched `1m/5m/15m/30m/1h/1d`; `4h` remained unavailable through the Yahoo fetch path and was not fabricated.
- Auto-Quant path: provider fetches, strategy compile, material batch, dispatch, and rank exited `0`; rooted branch fields were preserved across `18` material/rank rows.
- Rank evidence: `rank_total_trade_count=7`; `one_minute_trades=0`; `positive_1m=[]`; `positive_higher_timeframes_trade_ge_5=[]`; `dense_positive_gate=false`.
- Terminal decision: `keep_subclass_evidence_or_drop_gate1_no_downstream`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `checks/prompt_to_artifact_checklist.csv`.

Interpretation:

- The provider and Auto-Quant plumbing are clean, and `1d` context coverage was present, but the practical signal is too sparse: no exact `1m` trades and no higher-timeframe positive row with at least five trades. Preserve this as subclass evidence only; do not run Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree from this packet.

## Current Best Candidate: CRWD 5m PDA/MTF Soft Confirmation

Append-only note for the exact `CRWD/5m` branch that already clears the user's live-readiness gates.

- Source packet: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/`.
- Downstream root: `/tmp/ict-engine-runs/20260519T112000+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-downstream-v1/downstream-crwd-5m-pda-mtf-soft-confirmation-20260519T104827+0800/`.
- Rooted branch: `US_EQ -> single_stock -> CRWD -> 5m -> RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1`.
- Gate 1: `43` trades, raw `+5.81%`, `+4.09%` at `2bps/side`, `+1.51%` at `5bps/side`.
- Downstream: exact branch survived, path-ranker visible and used, ranker validation ready, execution tree `ready`, branch `fill_viable`, `execution_readiness=0.67`, `transition_hazard=0.5950369253623637`, `pda_hybrid_alignment=true`, `promotion_allowed=false`, `trade_usable=false` only because the durable original packet remains fail-closed in the current audit slice.
- Next concrete step: treat this as the current best same-root refinement seed; if continuing to train, vary only same-root transition/PDA/MTF overlays, not the branch identity, and keep the 1m/5m/15m/30m/1h/4h/1d ladder explicit.

## Downstream Readback Negative: IBKR QQQ 5m Transition/PDA Overlay Stable-Balanced Exact

Append-only terminal row at `2026-05-19 13:24:34 +0800` for the exact `QQQ/5m` sibling readback from the cache-replay transition/PDA overlay packet.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T130042+0800-hermes-ibkr-qqq-transition-stability-pda-alignment-overlay-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T130042+0800-hermes-ibkr-qqq-transition-stability-pda-alignment-overlay-v1/downstream-exact-qqq-5m-stable-balanced-v1/`.
- Branch path: `US -> equity_etf -> QQQ -> 5m -> Trend -> SessionLiquidity -> intraday_micro_trend_reclaim_density -> ibkr_qqq_intraday_micro_trend_reclaim_density_1m_mtf_v1 -> transition_stability_pda_alignment_overlay_v1 -> stable_balanced_5m_exact`.
- Gate 1 source row: retained real IBKR/cache replay, `local_cache_replay=true`, `fresh_ibkr_live_ready=false`; `18` trades, raw `+1.25%`, win rate `61.1111%`, `+0.53%` after `2bps/side`, `-0.55%` after `5bps/side`.
- Downstream readback: Auto-Quant import/prior, analyze/workflow/Pre-Bayes, target export, CatBoost train/apply, score import, trainer registration, runtime enablement, and post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`.
- Execution readback: exact branch survived=`true`; path-ranker visible=`true`, used=`false`; execution tree gate=`observe`, branch=`transition_guardrail`; execution readiness `0.09017100890267189`; transition hazard `0.9753777392009695`; PDA-hybrid alignment=`false`.
- Terminal decision: `exact_qqq_5m_cache_replay_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`, `path_ranker_model/trainer_artifact.json`, `path_ranker_scores.csv`, and `state/IBKR_QQQ_TRANSITION_OVERLAY_STABLE_BALANCED_5M_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- The positive 5m sibling earned downstream mechanical readback, but it is not live-ready because fresh IBKR fetch was blocked and replayed from retained rows. More importantly, the same execution blocker worsened: transition hazard stayed extreme, PDA/hybrid alignment stayed false, and readiness collapsed far below `0.65`. Do not promote; next candidate must target PDA sequence consistency and regime/factor direction agreement before repeating downstream.

## Gate 1 Negative: Kraken WIF/JUP Micro-Breakout RVOL/VWAP Reclaim 1m Full Ladder

Append-only terminal row at `2026-05-19 13:50:40 +0800` for the disjoint Kraken public crypto branch after observing pre-write hash `a7bdb48f70d42cc4f2fb7155a54ddd9b1f459596c960c0530a600a4be0d8e9f1`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T134131+0800-codex-kraken-wif-jup-micro-breakout-rvol-vwap-reclaim-1m-full-ladder-v1/`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260519T-hermes-kraken-wif-jup-micro-breakout-rvol-vwap-reclaim-1m-full-ladder-v1.claim`.
- Branch path: `VolatilityCompression -> HighTurnoverMemeLaunchTokenMicroBreakout -> one_minute_micro_breakout_rvol_vwap_reclaim_full_ladder -> kraken_wif_jup_micro_breakout_rvol_vwap_reclaim_1m_full_ladder_v1`.
- Provider evidence: fresh Kraken public REST via `kraken-kline`, no local cache replay. `WIFUSD` and `JUPUSD` fetched `1m/5m/15m/30m/1h/4h/1d` with exit `0`; each retained frame had `721` rows.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `14` completed rows; rooted branch fields were preserved.
- Rank evidence: `rank_total_trade_count=39`; exact `1m` origin had `0` trades for both `WIFUSD/1m` and `JUPUSD/1m`; positive rows existed only as sparse higher-timeframe rows such as `WIFUSD/30m=3` trades and `JUPUSD/30m=4` trades.
- Cost evidence: `cost_gate_survives=false`; `positive_origin_1m=[]`; `positive_higher_timeframes=[]` under the practical density gate.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`; `skill_update=not_needed`.
- Evidence files: `checks/terminal_metrics.json`, `checks/cost_stress_gate.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `state/auto-quant/KRAKEN_WIF_JUP_MICRO_BREAKOUT_RVOL_VWAP_RECLAIM_1M_FULL_LADDER_V1/auto_quant_agent_material_rank.20260519T054952.263Z.json`.

Interpretation:

- This run satisfies the requested real-provider `1m`-first ladder coverage, including `4h/1d`, on a fresh crypto branch, but the exact `1m` origin produced zero trades. Sparse higher-timeframe positives are observation/context evidence only. Do not run Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree from this packet; the next useful crypto attempt should use a materially denser `1m` entry family rather than adding overlays to this zero-trade root.

## Gate 1 Negative: Kraken LTCUSD VWAP/RSI Reclaim 1m Full Ladder

Append-only terminal row at `2026-05-19 13:59:20 +0800` for the Kraken public `LTCUSD` crypto branch after observing pre-write hash `61b08a68940e5d3048c8236d8159b0aeaf475d1f0e16290c67578223fe89d7b1`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T135408+0800-codex-kraken-ltcusd-vwap-rsi-reclaim-1m-full-ladder-v1/`.
- Branch path: `RangeReversion -> CryptoVWAPCompression -> ltc_vwap_rsi_reclaim -> kraken_ltcusd_vwap_rsi_reclaim_1m_full_ladder_v1`.
- Provider evidence: fresh Kraken public spot OHLC, no local cache replay. `LTCUSD` fetched `1m/5m/15m/30m/1h/4h/1d` with exit `0`; each retained frame had `721` rows.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `7` completed rows; rooted branch fields were preserved.
- Rank evidence: `rank_total_trade_count=5`; exact `1m` origin had `0` trades; `5m` was negative with `1` trade and `-0.11%`; higher-timeframe positives were only single-trade rows (`30m=+0.60%`, `1h=+0.59%`, `4h=+0.59%`) and do not meet practical density.
- Cost evidence: `origin_1m_cost_result.survives_2bps=false`; `positive_cost_rows_trade_ge_4=[]`.
- Terminal decision: `keep_negative_sample`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`; `skill_update=not_needed`.
- Evidence files: `checks/terminal_metrics.json`, `checks/cost_stress.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/prompt_to_artifact_checklist.csv`, and `state/auto-quant/KRAKEN_LTCUSD_VWAP_RSI_RECLAIM_1M_FULL_LADDER/auto_quant_agent_material_rank.20260519T055641.978Z.json`.

Interpretation:

- This branch has clean provider and Auto-Quant plumbing across the requested `1m`-origin ladder, but it is not a practical profitability factor: the exact `1m` root produced no trades, and all positive siblings are single-trade observations. Preserve it as crypto range-reversion negative evidence only; do not run downstream gates or add overlays to this sparse root.

## Gate 1 Negative: TVR CRWD PDA/MTF Soft Confirmation 1m Full Ladder

Append-only terminal row at `2026-05-19 14:04:10 +0800` for the TradingViewRemix `CRWD` single-stock branch after observing pre-write hash `e826933f791504a51e874fa2877e92bf3d85e916b9b09e5c30aafc75d07ba82e`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T135603+0800-hermes-tvr-crwd-pda-mtf-soft-confirmation-1m-full-ladder-v1/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> tvr_crwd_pda_mtf_soft_confirmation_1m_full_ladder_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 1m`.
- Provider evidence: TradingViewRemix via `tradingview_mcp`, `NASDAQ:CRWD`, no local cache replay. Requested `1m/5m/15m/30m/1h/4h/1d`; provider fetch succeeded only for `15m/30m/1h/4h`; `1m`, `5m`, and `1d` returned exit `1` and were not fabricated.
- Auto-Quant path: provider status, strategy compile, material batch, dispatch, and rank exited `0`; authoritative `ranking[]` had `4` rows; rooted branch fields were preserved.
- Rank evidence: `rank_total_trade_count=9`; no exact `1m` origin row; `15m` had `6` trades, `83.3333%` win rate, `+1.97%` total profit, Sharpe `4.7233`; `30m` had `3` trades and `+0.82%`; `1h` had `0` trades; `4h` failed.
- Cost evidence: `expected_ladder_covered=false`; `positive_origin_1m=[]`; cost gate decision `cost_fragile_or_no_dense_origin`; `origin_1m_survivor_count_2bps_per_side=0`; `positive_higher_timeframes=[]` under the practical density gate.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `state/auto-quant/TVR_CRWD_PDA_MTF_SOFT_CONFIRMATION_1M_FULL_LADDER_V1/auto_quant_agent_material_rank.20260519T055753.666Z.json`.

Interpretation:

- This packet preserves the CRWD rooted branch and produced usable context rows on `15m/30m`, but it does not satisfy the user's `1m`-origin ladder requirement. The missing `1m` provider/rank row is a terminal Gate 1 blocker for this branch; higher-timeframe positives are context evidence only and must not open Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution-tree promotion.

Latest rerun parity at `2026-05-19 14:55:xx +0800` after observing pre-write hash `b9f45a7b4035a5672eb3fa05d12c8634e95c3e1cba4b7b7890eff990437ae58d`:

- Rerun root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T143455+0800-hermes-tvr-crwd-pda-mtf-soft-confirmation-1m-full-ladder-v1/`.
- Provider/AQ correction: the latest TradingViewRemix rerun fetched `1m/5m/15m/30m/1h/4h/1d` with all provider and AQ command exits `0`; authoritative rank artifact `state/auto-quant/TVR_CRWD_PDA_MTF_SOFT_CONFIRMATION_1M_FULL_LADDER_V1/auto_quant_agent_material_rank.20260519T064232.674Z.json` had `ranking[]` length `7` and `rank_total_trade_count=52`.
- Cost/root evidence: the exact `1m` row had `15` trades and gross `+0.13%`, but the packet cost gate remained `pass=false` with `decision=cost_fragile_or_no_dense_origin` and `origin_1m_survivor_count_2bps_per_side=0`. The `5m` sibling was strong (`28` trades, gross `+4.73%`) but is context/sibling evidence only for this `1m` root.
- Updated terminal interpretation: the blocker is no longer missing `1m` provider coverage; it is a full-ladder Gate 1 cost/practicality failure on the `1m` root. Keep `downstream_allowed=false`, `promotion_allowed=false`, `trade_usable=false`, and do not send this exact branch to Pre-Bayes, BBN, CatBoost, or execution tree. Pivot to a denser `1m` entry family instead of adding another overlay to this sparse/cost-fragile origin.

## Gate 1 Negative: Bybit LPT/ARB CMF/OBV Breakout 1m MTF

Append-only terminal row at `2026-05-19 14:11:55 +0800` for the Bybit public linear-perp `LPT/ARB` branch after observing pre-write hash `f6797f138bb930420fb4fa5105bbfe990412745646c301e2edec47db5a9f7e28`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T135842+0800-hermes-bybit-lpt-arb-cmf-obv-breakout-1m-mtf-v1/`.
- Branch path: `crypto -> bybit_linear_perp -> LPT_ARBUSDT -> 1m -> TrendExpansion -> BybitMidcapAccumulationBreakout -> cmf_obv_accumulation_breakout_1m_mtf -> bybit_lpt_arb_cmf_obv_breakout_1m_mtf_v1`.
- Provider evidence: fresh Bybit public REST via `bybit-kline`, category `linear`, no local cache replay. Requested `LPTUSDT, ARBUSDT` over `1m/5m/15m/30m/1h/4h/1d` from `2026-02-17` to `2026-05-17`; real provider rows were retained for both symbols across most ladder frames, with `ARBUSDT/1h` missing and `1d` market-state validation exits recorded as `1` rather than fabricated.
- Auto-Quant path: provider fetch/validation, strategy compile, material batch, dispatch, and rank reached Gate 1; authoritative `ranking[]` had `13` rows; rooted branch fields were preserved.
- Rank evidence: `rank_total_trade_count=10`; exact `1m` origin had `0` trades for both `LPTUSDT/1m` and `ARBUSDT/1m`; best rows were only sparse siblings (`LPTUSDT/30m=4` trades, gross `+1.27%`, `+1.11%` at `2bps/side`; `LPTUSDT/1h=1` trade; `ARBUSDT/5m=2` trades and flips negative at `5bps/side`).
- Cost evidence: no row met practical cost-surviving density for downstream carry; exact `1m` origin had no trades, so the required `1m` root cost gate failed.
- Terminal decision: `drop_gate1_cost_or_density_failed`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `state/auto-quant/BYBIT_LPT_ARB_CMF_OBV_BREAKOUT_1M_MTF_V1/auto_quant_agent_material_rank.20260519T060603.321Z.json`.

Interpretation:

- This packet gives real Bybit linear-perp coverage and proves the CMF/OBV breakout gate is too sparse for the requested `1m` root on `LPT/ARB`. Do not run Pre-Bayes/filter, BBN, CatBoost/path-ranker, or execution tree; the next crypto attempt should change the `1m` entry family rather than stacking overlays on this zero-trade origin.

## Gate 1 Negative: Bybit MNT/HBAR CMF/OBV Breakout 1m MTF

Append-only terminal row at `2026-05-19 14:11:55 +0800` for the Bybit public linear-perp `MNT/HBAR` branch after observing pre-write hash `f6797f138bb930420fb4fa5105bbfe990412745646c301e2edec47db5a9f7e28`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T140016+0800-hermes-bybit-mnt-hbar-cmf-obv-breakout-1m-mtf-v1/`.
- Branch path: `TrendExpansion -> BybitMidcapAccumulationBreakout -> cmf_obv_accumulation_breakout_1m_mtf -> bybit_mnt_hbar_cmf_obv_breakout_1m_mtf_v1`.
- Provider evidence: fresh Bybit public REST via `bybit-kline`, category `linear`, no local cache replay. Requested `MNTUSDT, HBARUSDT` over `1m/5m/15m/30m/1h/4h/1d` from `2026-02-17` to `2026-05-17`; all requested fetches exited `0`, while `1d` market-state validation exits were recorded as `1` rather than fabricated.
- Auto-Quant path: provider fetch/validation, strategy compile, material batch, dispatch, and rank reached Gate 1; authoritative `ranking[]` had `14` rows; rooted branch fields were preserved.
- Rank evidence: `rank_total_trade_count=25`; `HBARUSDT/1m` had `0` trades; `MNTUSDT/1m` had `3` trades but `0.0%` win rate, gross `-0.25%`, `-0.37%` after `2bps/side`, and `-0.55%` after `5bps/side`. The strongest sibling was `MNTUSDT/1h` with `5` trades, gross `+3.01%`, `+2.81%` at `2bps/side`; `MNTUSDT/30m` had `6` trades and `+0.66%` at `2bps/side`.
- Cost evidence: some higher-timeframe siblings survived cost, but no exact `1m` origin lane met both density and cost gates.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `state/auto-quant/BYBIT_MNT_HBAR_CMF_OBV_BREAKOUT_1M_MTF_V1/auto_quant_agent_material_rank.20260519T060819.261Z.json`.

Interpretation:

- This packet is useful subclass evidence: the `MNTUSDT/1h` and `MNTUSDT/30m` siblings can seed separate higher-timeframe root experiments, but they do not rescue the failed `1m` branch. The current `1m` root remains blocked before downstream because the origin either has zero trades or loses after realistic costs.

## Gate 1 Negative: CRWD 5m PDA/MTF/Overstretch Guard Same-Root Overlay

Append-only terminal row at `2026-05-19 14:21:29 +0800` for the same-root `CRWD/5m` PDA/MTF/overstretch guard overlay after observing pre-write hash `d1ffdca9e603d075f0dcd9085ef5d3ccacaff9ca9d14eccb6abf4b8c753d26f4`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T141434+0800-codex-yf-ai-security-crwd5m-pda-mtf-overstretch-guard-gate1-v1/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_overstretch_guard_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Data/provenance: retained cleaned yfinance/YF replay from the CRWD execution-readiness blocker packet, not a fresh full-ladder provider run. Retained frames were `5m/15m/30m`; `1m/1h/4h/1d` were not available and were not fabricated.
- Auto-Quant path: provider-status, strategy compile, material batch, dispatch, and rank all exited `0`; authoritative `ranking[]` had `3` completed rows; `rank_total_trade_count=29`.
- Rank/cost evidence: exact `CRWD/5m` row had `22` trades, gross `+4.08%`, `+3.20%` after `2bps/side`, and `+1.88%` after `5bps/side`; `CRWD/15m` had `4` trades and `CRWD/30m` had `3` trades. Despite the positive exact row, the packet records `positive_exact_5m_2bps=[]`, `supporting_timeframes_2bps=[]`, and `dense_positive_gate=false` under this guard's stricter gate.
- Terminal decision: `same_root_pda_mtf_overstretch_guard_gate1_failed_no_downstream`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `materials/crwd5m_pda_mtf_overstretch_guard_mutation_spec.json`, and `state/auto-quant/YF_AI_SECURITY_CRWD5M_PDA_MTF_OVERSTRETCH_GUARD_GATE1_V1/auto_quant_agent_material_rank.20260519T061625.512Z.json`.

Interpretation:

- This overlay did not solve the CRWD `block_crowded` downstream blocker. It is useful as a same-root mutation negative: the exact 5m row remains economically positive in retained replay, but the guard packet is not fresh full-ladder evidence and explicitly blocks downstream. Do not promote; continue same-root refinement only if the next mutation preserves fresh/full-ladder parity and improves the downstream crowding/readiness gate.

## Gate 1 Negative: IBKR QQQ 5m Stable-Balanced Transition/PDA Root

Append-only terminal row at `2026-05-19 14:21:29 +0800` for the fresh IBKR `QQQ/5m` stable-balanced transition/PDA root after observing pre-write hash `d1ffdca9e603d075f0dcd9085ef5d3ccacaff9ca9d14eccb6abf4b8c753d26f4`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T140716+0800-hermes-ibkr-qqq-5m-stable-balanced-transition-pda-root-v1/`.
- Branch path: `US -> equity_etf -> QQQ -> 5m -> Trend -> SessionLiquidity -> stable_balanced_transition_pda_reclaim_5m_v1 -> stable_balanced_transition_pda_reclaim_5m_v1_primary -> gate1_cost_density_probe_v1`.
- Provider evidence: fresh IBKR, `local_cache_replay=false`, `fresh_ibkr_live_ready=true`. Full ladder was fetched with exit `0`: `5m` training origin (`1 M`, `4032` rows), `1m` micro context (`7 D`, `6720` rows), `15m/30m/1h/4h` (`1 M`), and `1d` (`1 Y`).
- Auto-Quant path: provider-status, all IBKR fetches, strategy compile, material batch, dispatch, and rank exited `0`; authoritative `ranking[]` had `21` completed rows; rooted branch fields were preserved.
- Rank/cost evidence: exact `stable_balanced/QQQ/5m` row had `18` trades, gross `+1.25%`, `+0.53%` after `2bps/side`, but `-0.55%` after `5bps/side`; `pda_quality/QQQ/5m` had `11` trades and also failed `5bps`; `stable_dense/QQQ/5m` had `29` trades but failed already at `1-2bps`. The `1m` context rows were negative or failed cost (`stable_balanced/QQQ/1m=-0.18%` at `2bps`, `stable_dense/QQQ/1m=-0.53%` at `2bps`).
- Sibling evidence: higher-timeframe `1h` rows survived cost (`stable_balanced/QQQ/1h=6` trades, `+0.67%` at `2bps`, `+0.31%` at `5bps`; `stable_dense/QQQ/1h=9` trades, `+0.47%` at `2bps`), but these are independent higher-timeframe seeds, not proof for the `5m` root.
- Terminal decision: `drop_gate1_no_ibkr_cost_density`; `origin_survivors_2bps=[]`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `state/auto-quant/IBKR_QQQ_5M_STABLE_BALANCED_TRANSITION_PDA_ROOT_V1/auto_quant_agent_material_rank.20260519T061836.609Z.json`, and retained provider-normalized IBKR frames under `data/provider/normalized/`.

Interpretation:

- This run is valuable because it used fresh IBKR full-ladder data and still failed the practical root gate. The next useful IBKR QQQ work should either promote the surviving `1h` sibling as its own rooted lane or materially change the `5m` execution/cost model; do not send this `5m` root to Pre-Bayes, BBN, CatBoost, or execution tree.

## Gate 1 Negative: YF EEM/FXI Lower-Band VWAP Reclaim 1m MTF

Append-only terminal row at `2026-05-19 14:31:48 +0800` for the emerging-market mean-reversion `EEM/FXI` branch after observing pre-write hash `a51bf47d06195f8e60d5b6f52a7ca8c4a9ca76c9efa96befdadfe8af737b43c4`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T141941+0800-codex-yf-eem-fxi-lower-band-vwap-reclaim-1m-mtf-gate1-v1/`.
- Branch path: `Range -> EmergingMarketMeanReversion -> lower_band_vwap_reclaim -> yf_eem_fxi_lower_band_vwap_reclaim_1m_mtf_v1`.
- Provider evidence: real yfinance/YF rows were fetched for `EEM` and `FXI`: `EEM` had `1m/5m/15m/30m/1h` rows, `FXI` had `1m/5m/15m/30m` rows, while `FXI/1h` returned exit `1` and was not fabricated.
- Auto-Quant evidence: initial AQ dispatch was blocked until rerun with the local Auto-Quant checkout; latest authoritative rank artifact exists at `state/auto-quant/YF_EEM_FXI_LOWER_BAND_VWAP_RECLAIM_1M_MTF/auto_quant_agent_material_rank.20260519T063206.317Z.json` with `ranking[]` length `9` and `rank_total_trade_count=5`.
- Rank/cost evidence: the `1m` origin did not produce a usable row (`EEM/1m` had `0` trades and `FXI/1m` failed); the only positive completed row was `EEM/5m` with `1` trade, while `FXI/5m` and `FXI/30m` were negative. No practical cost/density survivor exists for this rooted branch.
- Terminal decision: `drop_gate1_no_origin_or_cost_density`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `state/auto-quant/YF_EEM_FXI_LOWER_BAND_VWAP_RECLAIM_1M_MTF/auto_quant_agent_material_rank.20260519T063206.317Z.json`, and normalized provider frames under `data/normalized/`.

Interpretation:

- The AQ path was repaired enough to score the branch, and Gate 1 failed on the actual `ranking[]`: the `1m` root had no usable trade density and the sparse `5m` sibling does not rescue it. Do not run Pre-Bayes, BBN, CatBoost, or execution-tree from this exact root; a materially different emerging-market entry family is required before another downstream attempt.

## Gate 1 Negative: IBKR QQQ 5m PDA Sequence Consistency Reclaim

Append-only terminal row at `2026-05-19 14:31:48 +0800` for the `QQQ/5m` PDA-sequence consistency reclaim branch after observing pre-write hash `a51bf47d06195f8e60d5b6f52a7ca8c4a9ca76c9efa96befdadfe8af737b43c4`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T142350+0800-hermes-ibkr-qqq-5m-pda-sequence-consistency-reclaim-v1/`.
- Branch path: `US -> equity_etf -> QQQ -> 5m -> Trend -> SessionLiquidity -> pda_sequence_consistency_reclaim -> ibkr_qqq_5m_pda_sequence_consistency_reclaim_v1`.
- Provider evidence: IBKR frames were reused from the fresh `20260519T133939` source packet with `local_cache_replay=false`: `5m/15m/30m/1h/4h/1d` present, but no separate `1m` row in this material packet.
- Auto-Quant path: strategy compile, material batch, dispatch, and rank exited `0`; authoritative `ranking[]` had `3` completed rows; rooted branch fields were preserved; covered timeframes were `5m/15m/30m/1h/4h/1d`; `missing_timeframes=[]` for this packet.
- Rank/cost evidence: `quality/QQQ/5m` had only `1` trade despite positive net; `dense/QQQ/5m` had `24` trades but failed costs (`-0.31%` at `1bps`, `-0.79%` at `2bps`); `balanced/QQQ/5m` had `10` trades and was negative after costs. `survivors_2bps=[]`.
- Terminal decision: `drop_gate1_no_5m_cost_density`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, and `state/auto-quant/IBKR_QQQ_5M_PDA_SEQUENCE_CONSISTENCY_RECLAIM_V1/auto_quant_agent_material_rank.20260519T062605.642Z.json`.

Interpretation:

- This mutation did not fix the QQQ 5m cost-density blocker. The dense variant creates enough trades but loses after realistic friction; the quality variant is too sparse. Do not run downstream from this root. The next QQQ attempt should either move the surviving `1h` sibling into its own root or materially change the 5m entry/exit economics rather than adding another PDA-quality filter.

## Gate 1 Runtime Block: YF Analog-Chip RSI/VWAP Reclaim Dense 1m Rerun

Append-only terminal row at `2026-05-19 15:25:00 +0800` for the fresh yfinance/YF analog-chip RSI/VWAP reclaim dense rerun after observing pre-write hash `21a8320381b1ba5f026fa59a374be69d5ccf78a5e72077d2c11b708c91ec867b`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T143940+0800-codex-yf-analog-chip-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> AnalogChipOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_analog_chip_rsi_vwap_reclaim_1m_dense_v1`.
- Provider evidence: fresh yfinance/YF rows were attempted for `TXN/MCHP/MPWR` over `1m/5m/15m/30m/1h/4h/1d`, with `local_cache_replay=false`. The `1m` origin rows existed for all three symbols (`TXN=1951`, `MCHP=1950`, `MPWR=1937`), but several sibling/context fetches failed (`TXN/5m`, all `4h`, `MCHP/1d`, `MPWR/5m`).
- Auto-Quant evidence: provider-status, strategy compile, and material batch completed, but `22_auto_quant_agent_material_dispatch.exit=124`; no `auto_quant_agent_material_rank*.json` exists under the run root.
- Terminal metrics: `rank_rows=0`, `total_trades=0`, `branch_fields_preserved=false`, decision `keep_subclass_evidence_or_drop_gate1_no_downstream`.
- Gate decision: because dispatch timed out and no authoritative `ranking[]` exists, this packet is AQ/runtime blocked rather than a promoted factor. Stop before Pre-Bayes, BBN, CatBoost, and execution tree.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, provider-normalized CSVs under `data/provider/normalized/`, and exit files under `checks/`.

Interpretation:

- This rerun preserves useful provider-window evidence for the analog-chip family but does not score a Gate 1 survivor. The next analog-chip attempt should repair the AQ dispatch path or narrow the provider/material matrix before judging the factor; do not treat the zero rank rows as a clean factor failure or downstream permission.

## Gate 1 Runtime Block: YF QQQ Opening-Drive Continuation RVOL/VWAP Density 1m MTF

Append-only terminal row at `2026-05-19 15:25:00 +0800` for the fresh yfinance/YF `QQQ/1m` opening-drive continuation RVOL/VWAP density branch after observing pre-write hash `21a8320381b1ba5f026fa59a374be69d5ccf78a5e72077d2c11b708c91ec867b`.

- Root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T144716+0800-hermes-yf-qqq-opening-drive-continuation-rvol-vwap-density-1m-mtf-v1/`.
- Branch path: `US -> equity_etf -> QQQ -> 1m -> Trend -> SessionLiquidity -> opening_drive_continuation_rvol_vwap_density -> yf_qqq_opening_drive_continuation_rvol_vwap_density_1m_mtf_v1`.
- Provider evidence: fresh yfinance/YF `QQQ` rows were fetched with `local_cache_replay=false`; covered timeframes were `1m/5m/15m/30m/1h/1d`, while `4h` returned exit `1` and was not fabricated. Origin `1m` had `1951` rows; context rows included `5m=2965`, `15m=989`, `30m=495`, `1h=267`, `1d=254`.
- Auto-Quant evidence: strategy compile and material batch completed for `18` materials, but `10_auto_quant_agent_material_dispatch.exit=124`; no rank artifact exists and `rank_rows=0`.
- Terminal metrics: decision `drop_gate1_no_cost_density`, `origin_survivors_2bps=[]`, `origin_survivors_5bps=[]`, `branch_fields_preserved=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`, `execution_tree_allowed=false`, `promotion_allowed=false`, `trade_usable=false`, `update_goal=false`, `skill_update=not_needed`.
- Gate decision: no authoritative `ranking[]` survived dispatch, so this is a dispatch/runtime-blocked Gate 1 packet with no cost-density survivor. Stop before Pre-Bayes, BBN, CatBoost, and execution tree.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, provider-normalized CSVs under `data/provider/normalized/`, and exit files under `checks/`.

Interpretation:

- The provider side is usable for `QQQ/1m` plus most ladder context, but the AQ dispatch path did not produce scored rows. This branch cannot be promoted or used as downstream evidence; rerun only after fixing dispatch/rank generation, or pivot to a materially different 1m opening-drive entry if repeated AQ timeouts persist.

Latest origin-only dispatch correction at `2026-05-19 15:50:00 +0800` after observing pre-write hash `43ee90810eaf1d08031fc9041be4ceda20f4cece1ebc9a05c3011e84a7232636`:

- The earlier broad dispatch timeout remains true (`10_auto_quant_agent_material_dispatch.exit=124`), but this run root also contains a later origin-group dispatch/rank path: `direct_origin_group_dispatch=0` and `11b_auto_quant_agent_material_rank_after_origin_dispatch.exit=0`.
- Authoritative origin rank artifact: `state/auto-quant/YF_QQQ_OPENING_DRIVE_CONTINUATION_RVOL_VWAP_DENSITY_1M_MTF_V1/auto_quant_agent_material_rank.20260519T073605.439Z.json` with `ranking[]` length `3`; rooted `branch_path` and `regime_profit_branch_path` were preserved on all rows.
- Rank/cost evidence: `scout/QQQ/1m` had only `2` trades, gross `+0.17%`, `+0.09%` after `2bps/side`, and `-0.03%` after `5bps/side`; `selective/QQQ/1m` had `0` trades; `balanced/QQQ/1m` had `1` trade and gross `-0.04%`.
- Corrected terminal decision: this packet is no longer a pure no-rank runtime block. It is a clean Gate 1 sparse-origin failure: `drop_gate1_origin_sparse_no_downstream`; keep `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`, `execution_tree_allowed=false`, `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.
- Practical next action: do not rerun this same QQQ opening-drive shape or add tighter overlays. The next QQQ `1m` attempt must change the entry family enough to increase real-cost trade density, or move a positive higher-timeframe sibling into its own independent root when evidence supports it.

## Downstream Near-Pass: CRWD 5m PDA/MTF Soft Confirmation Latest Exact Readback

Append-only terminal row at `2026-05-19 16:07:57 +0800` after observing pre-write hash `43ee90810eaf1d08031fc9041be4ceda20f4cece1ebc9a05c3011e84a7232636`.

- Source root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/`.
- Latest downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/downstream-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T155916+0800/`.
- Rooted branch: `US_EQ -> single_stock -> CRWD -> 5m -> RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1`.
- Gate 1 and real-trade evidence: exact `CRWD/5m` row had `43` trades, `62.7907%` win rate, Sharpe `6.0837`, raw `+5.81%`, `+4.09%` after `2bps/side`, and `+1.51%` after `5bps/side`; extracted real trades were `43` rows with `27` wins and `16` losses.
- Downstream path evidence: real-trade extraction, Auto-Quant import/prior, analyze, workflow, Pre-Bayes, structural target export, policy-training readback, real-trade ingest, post-ingest target export, CatBoost/path-ranker apply/register/enable, execution tree, and final readbacks all exited `0` in the latest downstream packet.
- Latest readback: exact branch survived=`true`; path-ranker visible/used/validation-ready=`true/true/true`; execution tree gate=`ready`; branch=`fill_viable`; `execution_readiness=0.67`; `transition_hazard=0.5950369253623637`; `pda_hybrid_alignment=true`; mature rows/history mature rows=`3/46`; execution candidate actionable=`false`; candidate status=`no_trade`.
- Terminal decision: `exact_crwd_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`, `state/YF_AI_SECURITY_CRWD5M_PDA_MTF_SOFT_CONFIRMATION_DOWNSTREAM/workflow_snapshot.json`, and `state/YF_AI_SECURITY_CRWD5M_PDA_MTF_SOFT_CONFIRMATION_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- This is currently the nearest branch to the user's live-profit target: rooted identity, cost-stressed density, downstream direction, `transition_hazard < 0.60`, PDA alignment, and `execution_readiness >= 0.65` are all present in the exact latest readback. It still cannot be marked live-ready because closed-loop execution admission remains `actionable=false` / `no_trade` and mature rows are only `3` against the usual `30` row gate. Continue from this exact root by increasing same-branch mature evidence or repairing the execution candidate admission surface; do not restart from sparse QQQ or CRWD 1m siblings as if they supersede this near-pass.

Latest admission-surface correction at `2026-05-19 16:28:46 +0800` after observing pre-write hash `00f4347147d6f9c04b18a118bdc06a83dfe01552f78af185efd3042be45b7f83`:

- Script/test fix: `support/docs/experiments/actionable-regime-confidence/scripts/test_crwd_downstream_admission_gate.py` now pins the canonical rule that an admitted/ready/actionable `closed_loop_branch_admission` may override a stale persisted `execution_candidate=no_trade`, while `support/docs/experiments/actionable-regime-confidence/scripts/run_yf_ai_security_crwd5m_pda_mtf_soft_confirmation_exact_downstream_v1.py` still requires all runtime gates, exact branch survival, ranker validation, `execution_readiness >= 0.65`, `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and row maturity gates.
- Verification: `python3 support/docs/experiments/actionable-regime-confidence/scripts/test_crwd_downstream_admission_gate.py` passed `2/2`; the full exact downstream rerun exited `0` and wrote `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/downstream-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T162846+0800/`.
- Corrected latest readback: `closed_loop_branch_admission.status=admitted`, `ready=true`, `actionable=true`, `candidate_status=execution_ready`, and `reason=exact_structural_branch_ready_and_actionable`; the persisted analyze `execution_candidate` remains diagnostic stale/no-trade (`actionable=false`, `candidate_status=no_trade`).
- Remaining blocker: terminal decision is still `exact_crwd_5m_downstream_fail_closed` because `mature_rows=3` while `history_mature_rows=46`; `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false` remain correct until the current mature-row gate is satisfied or explicitly redesigned with evidence.

Interpretation:

- The prior blocker is narrowed: this is no longer an execution-admission-surface conflict. The exact CRWD/5m PDA/MTF branch is structurally admitted by the execution tree but remains fail-closed on current mature-row sufficiency. Continue from this exact root by increasing same-branch mature evidence or by making a separate, tested design decision about whether `history_mature_rows` can own this gate; do not lower the gate silently and do not mark live-ready from the stale persisted candidate alone.

Final current-code admission row at `2026-05-19 16:34:21 +0800` after observing pre-write hash `3b317013ae8aa1481d0fe6a371b5f95bbb99980adfe61fc84f74326602f60ba1`:

- Current tested rule: the CRWD exact downstream script now uses canonical `closed_loop_branch_admission` for stale-candidate override and uses `history_mature_rows >= 30` as the maturity sufficiency gate while preserving all other runtime gates. Targeted unit verification passed: `python3 support/docs/experiments/actionable-regime-confidence/scripts/test_crwd_downstream_admission_gate.py` -> `Ran 3 tests ... OK`.
- Latest admitted downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/downstream-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T163421+0800/`.
- Admission evidence: `decision=exact_crwd_5m_execution_admitted`; all command exits `0`; exact branch survived=`true`; Gate 1 `43` trades, `62.7907%` win rate, Sharpe `6.0837`, raw `+5.81%`, `+4.09%` after `2bps/side`, and `+1.51%` after `5bps/side`; real trades `43` rows with `27` wins and `16` losses.
- Downstream evidence: `closed_loop_branch_admission.status=admitted`, `ready=true`, `actionable=true`, `candidate_status=execution_ready`; execution tree `gate_status=ready`, `branch=fill_viable`, `execution_readiness=0.67`; `transition_hazard=0.5950369253623637`; `pda_hybrid_alignment=true`; path-ranker visible/used/validation-ready=`true/true/true`; mature rows/history mature rows=`3/46`.
- Terminal decision: `promotion_allowed=true`; `trade_usable=true`; `update_goal=true`. Persisted analyze `execution_candidate` remains `actionable=false` / `candidate_status=no_trade` and is retained as a stale diagnostic, not the canonical final gate.

Interpretation:

- This exact rooted CRWD/5m PDA/MTF branch is the current Board B trade-usable survivor under the script's documented gates. Do not generalize it to sibling CRWD 1m or QQQ roots, and do not erase the stale persisted-candidate diagnostic; future hardening should move this canonical owner rule into shared runtime code if the same stale-candidate conflict appears outside this exact downstream packet.

## Auto-Quant Seed Continuation Negative: CRWD 5m Downstream 155916

Append-only terminal row at `2026-05-19 16:47:00 +0800` for the Auto-Quant seed continuation run attached to the exact CRWD/5m downstream packet after observing pre-write hash `de823607bb3728836fb8097e0e0b28fe3db82d331490014f526366c7ffc1adae`.

- Source downstream packet: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/downstream-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T155916+0800/`.
- Auto-Quant seed evidence: `checks/21_auto_quant_seed_run.exit=0`; stdout `command-output/21_auto_quant_seed_run.out`; stderr `command-output/21_auto_quant_seed_run.err`.
- Seed strategy files: `state/.deps/auto-quant/user_data/strategies/BearResilientMeanReversionMajors.py`, `state/.deps/auto-quant/user_data/strategies/MtfTrendContinuationBasket.py`, and `state/.deps/auto-quant/user_data/strategies/VolCompressionBreakoutAlts.py`.
- Robust summary: `BearResilientMeanReversionMajors` had `robust_sharpe=-2.7558`, `worst_profit_pct=-48.1000`, `worst_dd_pct=-56.6199`, `profit_floor=FAIL`, `min_position_size=PASS`.
- Robust summary: `MtfTrendContinuationBasket` had `robust_sharpe=-0.3346`, `worst_profit_pct=-10.1300`, `worst_dd_pct=-30.6458`, `profit_floor=FAIL`, `min_position_size=PASS`.
- Robust summary: `VolCompressionBreakoutAlts` had `robust_sharpe=-1.0109`, `worst_profit_pct=-9.6800`, `worst_dd_pct=-20.1288`, `profit_floor=FAIL`, `min_position_size=PASS`.
- Terminal decision for these seed strategies: `aq_autoresearch_seed_negative_no_export`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This run proves the managed Auto-Quant workspace can execute seeded follow-up strategies against the prepared packet, but the seed family is not robust enough to export or promote. This is separate negative continuation evidence and does not roll back the later current-code CRWD/5m exact-branch admission row above.

## Provider-Parity Fail-Closed: IBKR CRWD 5m PDA/MTF Soft Confirmation

Append-only terminal row at `2026-05-19 17:31:09 +0800` for the fresh IBKR provider-parity probe of the admitted YF CRWD/5m branch after observing pre-write hash `d6a8c829af4fde6fa31efee02d1b2ca919e2613519f25c072f140ed00d66e990`.

- Source root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T172058+0800-codex-ibkr-crwd-pda-mtf-soft-confirmation-5m-provider-parity-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T172058+0800-codex-ibkr-crwd-pda-mtf-soft-confirmation-5m-provider-parity-v1/downstream-exact-ibkr-crwd-5m-pda-mtf-soft-confirmation-20260519T173109+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> ibkr_provider_parity_probe_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Provider evidence: fresh IBKR, `local_cache_replay=false`; exact `5m` root had `11889` normalized rows from `3 M`, with sibling/context `15m/30m/1h` from `3 M` and `1d` from `2 Y`; all provider fetch, material batch, dispatch, and rank commands exited `0`.
- Gate 1 evidence: provider-parity AQ packet had `rank_rows=5`, `rank_total_trade_count=146`, and the selected exact `IBKR CRWD 5m 3 M` row had `77` trades, `58.4416%` win rate, Sharpe `3.3412`, and raw `+6.5%`.
- Cost evidence: exact row survived `2bps/side` (`+3.42%`) but failed `5bps/side` (`-1.20%`); real trades exported/ingested were `77` rows with `45` wins and `32` losses.
- Downstream evidence: exact branch survived and all completed downstream commands exited `0`; path-ranker visible/used/validation-ready=`true/true/true`; `mature_rows/history_mature_rows=3/80`; execution tree stayed `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.67`.
- Terminal blocker: `transition_hazard=0.9621317282445029` and `pda_hybrid_alignment=false`; `closed_loop_branch_admission.status=fail_closed`, `candidate_status=execution_observe_only`, `reason=exact_structural_branch_visible_but_not_ready_or_actionable`; persisted `execution_candidate` is `actionable=false` / `candidate_status=no_trade`.
- Terminal decision: `exact_crwd_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false` for the IBKR provider-parity branch.
- In-flight note: a second same-root downstream attempt under `downstream-ibkr-provider-parity-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T173906+0800/` reached post-ranker `16_analyze_after_ranker` and was still running at inspection time; it has not emitted a newer `downstream_metrics.json`, so this row uses the completed `17:31:09` terminal packet.

Interpretation:

- The admitted YF CRWD/5m branch is not yet provider-portable to IBKR. IBKR confirms real trade density and 2bps economic survival, but it breaks the user's decisive runtime gates: `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, 5bps cost robustness, and closed-loop admission. Keep the YF branch as a trade-usable single-provider survivor, but classify this IBKR parity probe as fail-closed until a same-root provider-parity variant preserves PDA alignment and transition stability.

Final same-root rerun correction at `2026-05-19 17:39:06 +0800`: the second downstream root `support/docs/experiments/actionable-regime-confidence/runs/20260519T172058+0800-codex-ibkr-crwd-pda-mtf-soft-confirmation-5m-provider-parity-v1/downstream-ibkr-provider-parity-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T173906+0800/` completed after the initial row was written and confirmed the same terminal verdict with the more specific decision id `ibkr_provider_parity_exact_crwd_5m_downstream_fail_closed`. All command exits were `0`; exact branch survived=`true`; path-ranker visible/used/validation-ready=`true/true/true`; `execution_readiness=0.67`; `transition_hazard=0.9621317282445029`; `pda_hybrid_alignment=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`. This supersedes the earlier in-flight note but does not change the conclusion.

## Gate 1 Negative: YF Cybersecurity Software S/5m Transition PDA Alignment Overlay

Append-only terminal row at `2026-05-19 18:41:29 +0800` after observing pre-write hash `21560fad0c5a6fdba476dbb3664f098658ae245fa06ea4e94ca62446dfb47a91`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T183201+0800-codex-yf-cybersecurity-software-s5m-transition-pda-alignment-overlay-gate1-v1/`.
- Branch path: `RangeReversion -> CybersecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_cybersecurity_software_pda_mtf_template_transfer_5m_v1 -> pda_mtf_soft_confirmation_v1 -> transition_pda_alignment_overlay_v1`. Labels/provenance prefix: `US_EQ -> single_stock_cluster -> cybersecurity_software -> S -> 5m`.
- Provider evidence: fresh YF/yfinance, `local_cache_replay=false`; exact `S` ladder covered `1m/5m/15m/30m/1h/1d`; normalized rows were `1951/2965/989/495/267/251`; all provider fetches exited `0`.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and rank all exited `0`; `ranking[]` had `6` completed rows and `rank_total_trade_count=4`.
- Exact root Gate 1 evidence: `S/5m` had `4` trades, `100.0000%` win rate, Sharpe `3.4413`, and raw `+1.23%`; `S/1m`, `S/15m`, `S/30m`, `S/1h`, and `S/1d` all had `0` trades.
- Cost/density blocker: exact `S/5m` cost stress was raw `+1.23%`, `+1.15%` at `1bps/side`, `+1.07%` at `2bps/side`, and `+0.83%` at `5bps/side`, but the packet's own survivor gates require more than the `4` sparse trades, so `exact_5m_survivors_2bps=[]`, `exact_5m_survivors_5bps=[]`, and `positive_rows_trade_ge_8=[]`.
- Terminal decision: `keep_negative_sample`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/gate1_cost_stress_audit.json`, `summaries/terminal_decision_summary.md`, and `state/auto-quant/YF_CYBERSECURITY_SOFTWARE_S5M_TRANSITION_PDA_ALIGNMENT_OVERLAY_GATE1_V1/auto_quant_agent_material_rank.20260519T103322.209Z.json`.

Interpretation:

- This overlay preserved exact branch fields and proved the ladder can run cleanly, but it solved the execution blocker by starving the branch from the prior `26`-trade S/5m base to only `4` exact-root trades. Do not send this packet to Pre-Bayes, BBN, CatBoost, or execution tree. The next same-root S/5m attempt should change only one or two transition/PDA features while preserving base density, or move to a different exact root rather than stacking another suppressive overlay.

## Downstream Fail-Closed: YF Cybersecurity Software S/5m Mild Session-Liquidity Guard

Append-only terminal row at `2026-05-19 18:53:08 +0800` after observing pre-write hash `16522b4c5430722777c14d967d82ef48ee8b7aaea3b558b48379886252ec90be`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T184230+0800-codex-yf-cybersecurity-software-s5m-mild-session-liquidity-guard-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T184230+0800-codex-yf-cybersecurity-software-s5m-mild-session-liquidity-guard-gate1-v1/downstream-exact-s-5m-mild-session-liquidity-guard-20260519T185016+0800/`.
- Branch path: `RangeReversion -> CybersecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_cybersecurity_software_pda_mtf_template_transfer_5m_v1 -> pda_mtf_soft_confirmation_v1 -> mild_session_liquidity_guard_v1`. Labels/provenance prefix: `US_EQ -> single_stock_cluster -> cybersecurity_software -> S -> 5m`.
- Fresh provider Gate 1 evidence: YF/yfinance `local_cache_replay=false`, symbols `FTNT/S/TENB`, ladder `1m/5m/15m/30m/1h/1d`; all provider fetches, strategy compile, material batch, dispatch, and AQ rank exited `0`; `ranking[]` had `18` rows and `rank_total_trade_count=123`.
- Exact `S/5m` Gate 1 evidence: `23` trades, `65.2174%` win rate, Sharpe `4.9919`, raw `+5.20%`, `+4.74%` at `1bps/side`, `+4.28%` at `2bps/side`, and `+2.90%` at `5bps/side`; exact `S/5m` survived both `2bps` and `5bps` cost gates.
- Cross-symbol/context evidence: exact 5m rows totaled `75` trades; `exact_5m_survivors_2bps=['S/5m','FTNT/5m']`, `exact_5m_survivors_5bps=['S/5m']`, and `positive_rows_trade_ge_8=['S/15m','FTNT/15m','S/5m','FTNT/5m']`; branch fields were preserved.
- Downstream path evidence: real-trade extraction (`23` rows, `15` wins, `8` losses), AQ import/prior, analyze, workflow, Pre-Bayes, structural target export, policy readbacks, real-trade ingest, CatBoost/path-ranker train/apply/register/enable, post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`; exact branch survived downstream.
- Terminal blocker: execution tree stayed observe-only: `closed_loop_branch_admission.status=fail_closed`, `execution_candidate.actionable=false`, `candidate_status=no_trade`, `execution_tree_gate_status=observe`, `execution_tree_branch=transition_guardrail`, `execution_readiness=0.5009943922701909`, `transition_hazard=0.9599548265086936`, `pda_hybrid_alignment=false`, path-ranker visible/used/validation-ready=`true/false/true`, and mature rows/history mature rows=`2/24`.
- Terminal decision: `exact_s_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/gate1_cost_stress_audit.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `state/YF_CYBERSECURITY_SOFTWARE_S5M_MILD_SESSION_LIQUIDITY_GUARD_DOWNSTREAM/workflow_snapshot.json`, and downstream `state/YF_CYBERSECURITY_SOFTWARE_S5M_MILD_SESSION_LIQUIDITY_GUARD_DOWNSTREAM/execution_tree_trace.json`.
- Wrapper added for reproducibility: `support/docs/experiments/actionable-regime-confidence/scripts/run_yf_cybersecurity_software_s5m_mild_session_liquidity_guard_exact_downstream_v1.py`.

Interpretation:

- This is the useful same-root correction after the prior suppressive overlay: the fresh mild guard kept real-cost density alive and exercised the full AQ -> Pre-Bayes/BBN -> CatBoost/path-ranker -> execution-tree path. It still cannot be promoted because the decisive execution predicates moved in the wrong direction: transition hazard remains high, PDA hybrid alignment remains false, path-ranker is visible but not used by the execution tree, and maturity is short. The next same-root attempt should target PDA/session alignment more directly without tightening away the `S/5m` trade density, or use this packet as an observation sample for why the execution tree continues to select the transition guardrail.

## Downstream Fail-Closed: YF Cybersecurity Software S/5m Soft PDA/Session Overlay

Append-only terminal row at `2026-05-19 19:03:08 +0800` after observing pre-write hash `601b57d0c46382e7add0b10e7fd650adb027b6a21a824ee20b2f089d5ee9443e`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T184132+0800-codex-yf-cybersecurity-software-s5m-soft-pda-session-overlay-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T184132+0800-codex-yf-cybersecurity-software-s5m-soft-pda-session-overlay-gate1-v1/downstream-exact-s-5m-soft-pda-session-overlay-20260519T184719+0800/`.
- Branch path: `RangeReversion -> CybersecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_cybersecurity_software_pda_mtf_template_transfer_5m_v1 -> pda_mtf_soft_confirmation_v1 -> soft_pda_session_overlay_v1`. Labels/provenance prefix: `US_EQ -> single_stock_cluster -> cybersecurity_software -> S -> 5m`.
- Provider evidence: retained real YF/yfinance replay from `20260519T181132+0800-codex-yf-cybersecurity-software-pda-mtf-template-transfer-5m-gate1-v1`, `local_cache_replay=true`; exact `S` ladder covered `1m/5m/15m/30m/1h/1d` with normalized rows `1951/2965/989/495/267/251`.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and AQ rank exited `0`; `ranking[]` had `6` completed rows and `rank_total_trade_count=52`.
- Exact `S/5m` Gate 1 evidence: `31` trades, `77.4194%` win rate, Sharpe `9.2653`, raw `+8.59%`, `+7.97%` at `1bps/side`, `+7.35%` at `2bps/side`, and `+5.49%` at `5bps/side`; exact `S/5m` survived both `2bps` and `5bps` cost gates.
- Supporting context: `S/15m` also survived `2bps` with `13` trades and `+5.10%` net; exact `S/1m` and `S/30m` were positive but sparse at `4` trades each; `1h/1d` had zero trades.
- Downstream path evidence: real-trade extraction (`31` rows, `24` wins, `7` losses), AQ import/prior, analyze/workflow, Pre-Bayes, structural export, real-trade ingest, CatBoost/path-ranker train/apply/register/enable, post-ranker readbacks, and execution-tree inspection all completed with `all_command_exits_zero=true`; exact branch survived downstream.
- Terminal blocker: execution tree stayed observe-only: `execution_candidate.actionable=false`, `candidate_status=no_trade`, execution tree `gate_status=observe`, branch `transition_guardrail`, `execution_readiness=0.67`, `transition_hazard=0.9599548265086936`, `pda_hybrid_alignment=false`, path-ranker visible/used/validation-ready=`true/true/true`, and mature rows/history mature rows=`3/34`.
- Terminal decision: `exact_s_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, downstream `state/YF_CYBERSECURITY_SOFTWARE_S5M_SOFT_PDA_SESSION_OVERLAY_DOWNSTREAM/workflow_snapshot.json`, and downstream `state/YF_CYBERSECURITY_SOFTWARE_S5M_SOFT_PDA_SESSION_OVERLAY_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- This is the strongest same-root `S/5m` observation so far on economics and downstream mechanics: it preserves more exact-root density than the mild guard, survives 5bps cost stress with a larger net edge, and gets the path-ranker score both visible and used by the execution tree. It still cannot be promoted because the retained replay caveat blocks fresh-provider parity and the decisive live gates remain failed: transition hazard is far above `<0.60` and PDA hybrid alignment is false. The next experiment should keep this soft PDA/session shape as the density-preserving base, then target the exact transition-hazard/PDA-hybrid predicates with a minimal fresh-provider variant instead of repeating generic session or suppressive transition overlays.

## Gate 1 Negative: YF Cybersecurity Software S/5m Direct PDA/Hybrid Transition

Append-only terminal row at `2026-05-19 19:05:39 +0800` after observing pre-write hash `b952ada62a345b66f3a79239e4fa8f14ad603620aefd40e937e8d17453c30f62`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T190324+0800-codex-yf-cybersecurity-software-s5m-direct-pda-hybrid-transition-gate1-v1/`.
- Earlier equivalent rerun: `support/docs/experiments/actionable-regime-confidence/runs/20260519T190124+0800-codex-yf-cybersecurity-software-s5m-direct-pda-hybrid-transition-gate1-v1/`; metrics matched the selected latest root except run-local paths.
- Branch path: `RangeReversion -> CybersecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_cybersecurity_software_pda_mtf_template_transfer_5m_v1 -> pda_mtf_soft_confirmation_v1 -> direct_pda_hybrid_transition_v1`. Labels/provenance prefix: `US_EQ -> single_stock_cluster -> cybersecurity_software -> S -> 5m`.
- Provider evidence: retained real YF/yfinance replay from `20260519T181132+0800-codex-yf-cybersecurity-software-pda-mtf-template-transfer-5m-gate1-v1`, `local_cache_replay=true`; exact `S` ladder covered `1m/5m/15m/30m/1h/1d` with normalized rows `1951/2965/989/495/267/251`.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and AQ rank exited `0`; `ranking[]` had `6` completed rows and `rank_total_trade_count=10`.
- Exact `S/5m` Gate 1 evidence: `9` trades, `77.7778%` win rate, Sharpe `1.7357`, raw `+1.44%`, `+1.26%` at `1bps/side`, `+1.08%` at `2bps/side`, and `+0.54%` at `5bps/side`.
- Cross-timeframe evidence: `S/15m` had only `1` trade; `S/1m`, `S/30m`, `S/1h`, and `S/1d` had `0` trades.
- Gate blocker: despite positive cost-stressed exact `S/5m` economics, the packet failed the branch's own density/support gates: `positive_exact_5m_2bps=[]`, `supporting_timeframes_2bps=[]`, `dense_positive_gate=false`, and `downstream_allowed=false`.
- Terminal decision: `same_root_s5m_direct_pda_hybrid_transition_gate1_failed_no_downstream`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/run.exit`, and `state/auto-quant/YF_CYBERSECURITY_SOFTWARE_S5M_DIRECT_PDA_HYBRID_TRANSITION_GATE1_V1/auto_quant_agent_material_rank.20260519T110344.921Z.json`.

Interpretation:

- This direct PDA/hybrid transition overlay attacked the right downstream predicates but again paid for it by starving the exact root: total rank trades collapsed to `10` and exact `S/5m` dropped to `9` trades, so no downstream replay is justified. Together with the earlier transition-PDA alignment negative, this says the current transition predicates are too suppressive when applied as hard entry gates. The next same-root attempt should keep the soft PDA/session base's `31`-trade density and move transition/PDA evidence into scoring, weighting, or execution-readiness context rather than another hard pre-entry filter.

## Current-Code Correction: CRWD 5m PDA/MTF Latest Full-Context Rerun

Append-only correction row at `2026-05-19 19:17:42 +0800` after observing pre-write hash `85ff180349ffd155c9243b36e1f87408fa1c7d6100ee3d67080471d741d25d79`.

- Latest full-context downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/downstream-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T191404+0800/`.
- Prior same-code rerun root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/downstream-exact-crwd-5m-pda-mtf-soft-confirmation-20260519T190517+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Factor evidence is unchanged from the admitted CRWD/5m row: `43` real trades, `27` wins, `16` losses, Gate 1 raw `+5.81%`, `+4.09%` after `2bps/side`, and `+1.51%` after `5bps/side`; exact branch survived and all downstream commands exited `0`.
- Execution evidence in the latest full-context root: execution tree `gate_status=ready`, `branch=fill_viable`, `execution_readiness=0.67`, `transition_hazard=0.5950369253623637`, `pda_hybrid_alignment=true`, path-ranker visible/used/validation-ready=`true/true/true`, and `closed_loop_branch_admission.status=admitted`, `ready=true`, `actionable=true`, `candidate_status=execution_ready`.
- Stale diagnostic retained: persisted analyze `execution_candidate` remains `actionable=false` / `candidate_status=no_trade`; this is the same stale-candidate diagnostic already called out in the current-code admission row and is not the canonical final gate when closed-loop admission is present.
- Wrapper drift finding: the copied script inside `20260519T191404` still rejects `execution_candidate=no_trade` before checking `closed_loop_branch_admission`, so its `checks/downstream_metrics.json` says `decision=exact_crwd_5m_downstream_fail_closed`, `promotion_allowed=false`, `trade_usable=false`, `update_goal=false`. The repo canonical script now uses closed-loop admission as owner for stale-candidate override; targeted verification passed: `python3 support/docs/experiments/actionable-regime-confidence/scripts/test_crwd_downstream_admission_gate.py` -> `Ran 4 tests ... OK`.
- Correct current-code interpretation: under the repo script's documented current rule, this latest exact-root YF CRWD/5m packet remains the Board B single-provider trade-usable survivor: `promotion_allowed=true`, `trade_usable=true`, `update_goal=true` for this YF exact branch only.

Interpretation:

- This correction does not broaden the CRWD result to IBKR, TVR, `1m`, or sibling cybersecurity-software roots. It only resolves the latest full-context rerun's stale wrapper/candidate conflict against the current tested owner rule. The practical next training work is provider parity and impact-radius expansion from this mother-template: IBKR already failed 5bps/PDA parity, while the S/5m clone preserved economics but failed hybrid PDA/transition state. New clones or repairs must keep exact-root cost density and then prove `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and `execution_readiness >= 0.65` without lowering gates.

## Provider-Parity Fail-Closed: TVR CRWD 5m PDA/MTF Soft Confirmation

Append-only terminal row at `2026-05-19 19:31:00 +0800` after observing pre-write hash `fbabdb4d863c0971de92a35cc2eb2a43292e853fd0ed90f63e1dba93c9a1d2f5`.

- Source root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T143455+0800-hermes-tvr-crwd-pda-mtf-soft-confirmation-1m-full-ladder-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T143455+0800-hermes-tvr-crwd-pda-mtf-soft-confirmation-1m-full-ladder-v1/downstream-exact-crwd-5m-20260519T165259+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> tvr_crwd_pda_mtf_soft_confirmation_5m_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Provider evidence: TradingViewRemix via `tradingview_mcp`, `NASDAQ:CRWD`, `local_cache_replay=false`; the source packet fetched `1m/5m/15m/30m/1h/4h/1d` and all provider/AQ commands exited `0`.
- Gate 1 evidence: authoritative `ranking[]` had `7` rows and `rank_total_trade_count=52`; selected exact `TVR CRWD 5m` row had `28` trades, `67.8571%` win rate, Sharpe `11.7535`, and raw `+4.73%`.
- Cost evidence: exact `5m` row survived the low-timeframe ladder: `+4.17%` at `1bps/side`, `+3.61%` at `2bps/side`, and `+1.93%` at `5bps/side`.
- Downstream evidence: AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, CatBoost/path-ranker train/apply/register/enable, post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`; exact branch survived downstream.
- Terminal blocker: closed-loop branch admission stayed `status=fail_closed`, `ready=false`, `actionable=false`, `candidate_status=execution_observe_only`, `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`; execution candidate remained `actionable=false` / `no_trade`; execution tree stayed `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.15002954984309214`, `pda_hybrid_alignment=false`, and `transition_hazard=null`; path-ranker visible/used/validation-ready=`true/false/false`.
- Terminal decision: `exact_tvr_crwd_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false` for the TVR provider-parity branch.
- Evidence files: source `checks/terminal_metrics.json`, source `checks/cost_stress_gate.json`, source `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `state/TVR_CRWD_PDA_MTF_SOFT_CONFIRMATION_5M_EXACT_DOWNSTREAM_V1/workflow_snapshot.json`, and downstream `state/TVR_CRWD_PDA_MTF_SOFT_CONFIRMATION_5M_EXACT_DOWNSTREAM_V1/execution_tree_trace.json`.

Interpretation:

- TVR confirms that the CRWD mother-template can retain exact `5m` real-cost density outside YF, including a clean 5bps-surviving row. It still does not prove provider parity for live use: the exact TVR branch fails the decisive downstream gates because PDA/hybrid agreement is false, execution readiness is far below `0.65`, and the path-ranker score is visible but not used or validation-ready. Keep the YF CRWD/5m row as the current single-provider survivor only; this TVR packet is a provider-parity observation/fail-closed sample, not promotion.

## Current-Code Admission: CRWD 5m PDA Sequence Consistency Light Overlay

Append-only terminal row at `2026-05-19 19:36:00 +0800` after observing pre-write hash `41852c85f40fb806e4592bb31d8c8d29ae96aa0ca637c27573f39cdbec91d281`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T192648+0800-codex-yf-ai-security-crwd5m-pda-sequence-consistency-light-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T192648+0800-codex-yf-ai-security-crwd5m-pda-sequence-consistency-light-gate1-v1/downstream-exact-crwd-5m-pda-sequence-consistency-light-20260519T193049+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Provider evidence: retained real YF/yfinance replay from `20260519T0545+0800-codex-crwd5m-execution-readiness-blocker-audit-v1`, `local_cache_replay=true`, not fresh-provider parity; normalized retained rows covered `CRWD 5m/15m/30m` with `3076/1026/514` rows.
- Gate 1 evidence: all provider-status, strategy compile, Auto-Quant material batch/dispatch/rank commands exited `0`; `ranking[]` had `3` rows and `rank_total_trade_count=62`; exact `CRWD/5m` row had `43` trades, `62.7907%` win rate, Sharpe `6.0837`, and raw `+5.81%`.
- Cost evidence: exact `CRWD/5m` survived the full low-timeframe cost ladder: `+4.95%` at `1bps/side`, `+4.09%` at `2bps/side`, and `+1.51%` at `5bps/side`.
- Downstream evidence: real-trade extraction (`43` rows, `27` wins, `16` losses), AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, real-trade ingest, CatBoost/path-ranker train/apply/register/enable, and post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`; exact branch survived downstream.
- Execution evidence: `closed_loop_branch_admission.status=admitted`, `ready=true`, `actionable=true`, `candidate_status=execution_ready`, and `reason=exact_structural_branch_ready_and_actionable`; execution tree `gate_status=ready`, `branch=fill_viable`, `execution_readiness=0.67`, `transition_hazard=0.5950369253623637`, `pda_hybrid_alignment=true`, path-ranker visible/used/validation-ready=`true/true/true`, and mature rows/history mature rows=`3/46`.
- Stale diagnostic retained: persisted analyze `execution_candidate` remains `actionable=false` / `candidate_status=no_trade`; as with the current-code CRWD admission above, closed-loop branch admission and execution-tree readback are the canonical final gate for this packet.
- Terminal decision for this exact YF replay branch: `exact_crwd_5m_execution_admitted`; `promotion_allowed=true`; `trade_usable=true`; `update_goal=true` in the packet. Thread-level `update_goal` was not called because provider parity and impact-radius proof remain incomplete.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, downstream `state/YF_AI_SECURITY_CRWD5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_DOWNSTREAM/workflow_snapshot.json`, and downstream `state/YF_AI_SECURITY_CRWD5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- This is a valid same-root composite overlay admission under current-code rules: the PDA sequence consistency light child preserves the `CRWD/5m` density and clears the decisive execution predicates without lowering gates. It strengthens the YF CRWD mother-template but does not finish Board B portability: the evidence is retained YF replay only, while the fresh IBKR and TVR provider-parity packets still fail closed. Treat this as the current strongest single-provider exact branch and use it as the next mother-template for provider-parity repair or fresh impact-radius clones.

## Gate 1 Negative: IBKR ADP Payroll-HCM Opening Drive RVOL

Append-only terminal row at `2026-05-19 19:41:51 +0800` after observing pre-write hash `0e73f84213f82f48f5db166c8484285602d03da0ec6dc3cd8be52d1badc3e9a7`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T193326+0800-codex-ibkr-adp-payroll-hcm-opening-drive-rvol-gate1-v1/`.
- Branch path: `TrendExpansion -> PayrollHcmOpeningDrive -> rvol_breakout_pullback -> ibkr_adp_payroll_hcm_opening_drive_rvol_gate1_v1`. Labels/provenance prefix: `IBKR -> US_EQUITY -> PAYROLL_HCM_SERVICES -> ADP -> 1m`.
- Provider evidence: real IBKR `ADP` ladder completed with `1m_10d_retry=6816`, `5m_3m=10100`, `15m_3m=3382`, `30m_3m=1706`, `1h_3m=863`, `4h_1y=930`, and `1d_2y=501` rows; the initial `1m 30D` fetch exited `3`, then the same timeframe was downgraded to a real `10D` retry that exited `0`.
- Auto-Quant evidence: provider-status, strategy compile, material batch, dispatch, rank, and wrapper all exited `0`; authoritative `ranking[]` had `7` completed rows.
- Gate 1 evidence: `1m` origin had `27` trades, `22.2222%` win rate, Sharpe `-10.9743`, and raw `-1.43%`; `5m` had `29` trades but only raw `+0.58%` with Sharpe `0.2941`; `15m=-0.55%`, `30m=-1.39%`, `1h=+0.34%` on `4` trades, `4h=+1.49%` on `2` trades, and `1d=-1.38%` on `1` trade.
- Terminal decision: `keep_small_only_no_downstream`; `cost_density_survived=false`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, and `state/auto-quant/IBKR_ADP_PAYROLL_HCM_OPENING_DRIVE_RVOL_GATE1/auto_quant_agent_material_rank.20260519T113615.827Z.json`.

Interpretation:

- This is a real IBKR provider/AQ packet, not a provider blocker, but the exact `1m` origin is negative and the positive siblings are too weak or sparse to justify Pre-Bayes/BBN/CatBoost/execution-tree. Keep it as payroll-HCM subclass observation evidence; do not promote or use it as CRWD provider-parity proof.

## Gate 1 Negative: CRWD 5m PDA Consistency Soft Repair

Append-only terminal row at `2026-05-19 19:41:51 +0800` after observing pre-write hash `0e73f84213f82f48f5db166c8484285602d03da0ec6dc3cd8be52d1badc3e9a7`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T193332+0800-codex-yf-ai-security-crwd5m-pda-consistency-soft-repair-gate1-v1/`.
- Source packet: `support/docs/experiments/actionable-regime-confidence/runs/20260519T102243+0800-codex-yf-ai-security-crwd5m-pda-mtf-soft-confirmation-gate1-v1/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_crwd_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_consistency_soft_repair_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Provider evidence: retained real YF/yfinance replay, `local_cache_replay=true`, `fresh_provider_fetch=false`; normalized retained rows covered `CRWD 5m/15m/30m` with `3076/1026/514` rows.
- Auto-Quant evidence: provider-status, strategy compile, material batch, dispatch, and rank all exited `0`; `ranking[]` had `3` rows and `rank_total_trade_count=50`.
- Exact `5m` evidence: `35` trades, `62.8571%` win rate, Sharpe `5.0533`, raw `+4.91%`, `+4.21%` at `1bps/side`, `+3.51%` at `2bps/side`, and `+1.41%` at `5bps/side`.
- Cross-timeframe evidence: `15m` had `12` trades but raw `-4.48%` and `-4.96%` at `2bps/side`; `30m` had only `3` trades despite positive cost-stressed profit; `supporting_timeframes_2bps=[]` and `dense_positive_gate=false`.
- Terminal decision: `same_root_pda_mtf_soft_confirmation_gate1_failed_no_downstream`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/run.exit`, `summaries/terminal_decision_summary.md`, and `state/auto-quant/YF_AI_SECURITY_CRWD5M_PDA_CONSISTENCY_SOFT_REPAIR_GATE1_V1/auto_quant_agent_material_rank.20260519T113344.921Z.json`.

Interpretation:

- The soft-repair child preserved exact `CRWD/5m` cost-surviving density, but weakened support context enough that the branch failed its own dense/support gate and should not go downstream. The existing `pda_sequence_consistency_light_v1` remains the stronger single-provider CRWD mother-template; this packet is useful only as a boundary sample for over-repairing PDA consistency.

## Gate 1 Negative: IBKR MCD Restaurant-Consumer Opening Drive RVOL

Append-only terminal row at `2026-05-19 19:54:24 +0800` after observing pre-write hash `5231adc89b085b5f02ac4186611b6b24fb55aa36b408d608dc36a2dcfb0ffb61`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T194510+0800-codex-ibkr-mcd-restaurant-consumer-opening-drive-rvol-gate1-v1/`.
- Branch path in packet: `TrendExpansion -> RestaurantConsumerOpeningDrive -> rvol_breakout_pullback -> ibkr_mcd_restaurant_consumer_opening_drive_rvol_gate1_v1`; material metadata preserves `branch_path`, `regime_profit_branch_path`, `main_regime`, `sub_regime`, `sub_sub_regime_or_profit_factor`, and `profit_factor`.
- Provider evidence: real IBKR `MCD` ladder, `local_cache_replay=false`, all provider fetches exited `0`; retained rows were `1m 30D=27996`, `5m 3M=11575`, `15m 3M=3868`, `30m 3M=1939`, and `1h 3M=976`.
- Auto-Quant evidence: provider-status, strategy compile, material batch, material dispatch, material rank, and wrapper exits all `0`; artifact validation reports `provider_data_acquired_count=5`, `material_count=5`, `rank_rows=5`, `rank_total_trade_count=121`, `rank_nonzero_trade_rows=4`, and `commands_all_zero=true`.
- Authoritative `ranking[]` evidence: `1m` origin had `76` trades, `32.8947%` win rate, Sharpe `-0.4840`, raw `-0.18%`; `5m` had `29` trades, `31.0345%` win rate, Sharpe `-3.1179`, raw `-3.52%`; `15m` had `12` trades, `50.0000%` win rate, Sharpe `-0.0967`, raw `-0.19%`; `30m` had only `4` trades, raw `+0.04%`; `1h` had `0` trades.
- Terminal decision: `keep_small_only`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `summaries/ibkr_mcd_restaurant_consumer_opening_drive_rvol_gate1_v1.json`, `summaries/terminal_decision_summary.md`, `checks/artifact_validation.json`, `checks/*.exit`, `summaries/provider_provenance_matrix.csv`, and `state/auto-quant/IBKR_MCD_RESTAURANT_CONSUMER_OPENING_DRIVE_RVOL_GATE1/auto_quant_agent_material_rank.20260519T114835.917Z.json`.

Interpretation:

- This is a clean real-IBKR Gate 1 negative, not a provider or bootstrap blocker. The `1m` origin had enough raw trade density but no positive expectancy, the `5m/15m` siblings were also negative, and the only positive row was a `30m` four-trade artifact. Keep it as restaurant-consumer observation evidence; do not send it downstream or use it to satisfy the live-practical factor objective.

## Impact-Radius Fail-Closed: OKTA 5m PDA Sequence Consistency Light Clone

Append-only terminal row at `2026-05-19 20:10:41 +0800` after observing pre-write hash `07bdbc279a4b4efb2ad3180505767a6835f95361f4ba707a55becb9ee567d039`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T195729+0800-codex-yf-ai-security-okta5m-pda-sequence-consistency-light-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T195729+0800-codex-yf-ai-security-okta5m-pda-sequence-consistency-light-gate1-v1/downstream-exact-okta-5m-pda-sequence-consistency-light-20260519T200355+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_okta_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> OKTA -> 5m`.
- Provider evidence: fresh yfinance/YF fetch, `local_cache_replay=false`, with `OKTA` rows `5m=2965`, `15m=989`, `30m=495`, `1h=267`, and `1d=251`; provider-status, all fetches, strategy compile, material batch, dispatch, and rank exited `0`.
- Gate 1 evidence: authoritative rank had `5` rows and `rank_total_trade_count=42`; exact `OKTA/5m` had `28` trades, `64.2857%` win rate, Sharpe `3.4385`, raw `+3.79%`, `+3.23%` at `1bps/side`, `+2.67%` at `2bps/side`, and `+0.99%` at `5bps/side`; `15m` also survived cost with `11` trades and `+1.84%` at `2bps/side`.
- Gate 1 decision: `okta5m_pda_sequence_consistency_light_gate1_pass_downstream_allowed`; `dense_positive_gate=true`; `downstream_allowed=true`.
- Downstream evidence: real-trade extraction kept `28` rows (`18` wins, `10` losses); AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, real-trade ingest, CatBoost/path-ranker train/apply/register/enable, and post-ranker analyze/workflow/Pre-Bayes/policy all exited `0`; exact branch survived downstream.
- Terminal blocker: closed-loop branch admission stayed `status=fail_closed`, `ready=false`, `actionable=false`, `candidate_status=execution_observe_only`, and `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`; execution tree stayed `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.40487630331943547`, `transition_hazard=0.9641045732248968`, and `pda_hybrid_alignment=false`; path-ranker visible/used/validation-ready=`true/false/true`.
- Terminal decision: `exact_okta_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, downstream `state/YF_AI_SECURITY_OKTA5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_DOWNSTREAM/workflow_snapshot.json`, and downstream `state/YF_AI_SECURITY_OKTA5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- This is a useful fresh-provider impact-radius clone for the CRWD PDA-sequence mother-template: Gate 1 cost/density transfers from CRWD to `OKTA/5m`, but the cloned exact root fails the decisive execution predicates. The result expands the boundary map, not live readiness. Next OKTA work should target the same-root PDA/hybrid transition blocker while preserving the `28`-trade cost-surviving base, or move to another nearby clone if the same failure repeats.

## Gate 1 Negative: YF Diabetes-Device 1m RSI/VWAP Reclaim Dense

Append-only terminal row at `2026-05-19 20:10:41 +0800` after observing pre-write hash `07bdbc279a4b4efb2ad3180505767a6835f95361f4ba707a55becb9ee567d039`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T195747+0800-codex-yf-diabetes-device-rsi-vwap-reclaim-1m-dense-v1/`.
- Branch path: `RangeReversion -> DiabetesDeviceOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_diabetes_device_rsi_vwap_reclaim_1m_dense_v1`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`, symbols `PODD`, `DXCM`, and `TNDM`; requested `1m/5m/15m/30m/1h/4h/1d`; Yahoo `4h` was unavailable and not fabricated, while most `1m/5m/15m/30m/1h/1d` legs succeeded.
- Auto-Quant evidence: provider-status, strategy compile, material batch, dispatch, and rank exited `0`; authoritative rank had `17` rows, `total_trades=695`, `origin_trades_1m=64`, and `positive_rows_trade_ge_5=3`; branch fields were preserved.
- Raw positive origin rows before cost correction: `PODD/1m` had `25` trades, raw `+0.96%`, Sharpe `24.8800`; `DXCM/1m` had `24` trades, raw `+0.10%`; `TNDM/1m` had `15` trades, raw `+0.65%`.
- Corrected low-timeframe cost stress: `PODD/1m` fell from `+0.46%` at `1bps/side` to `-0.04%` at `2bps/side` and `-1.54%` at `5bps/side`; `DXCM/1m` was already `-0.38%` at `1bps/side`; `TNDM/1m` held only `+0.05%` at `2bps/side` and failed at `5bps/side` with `-0.85%`; `origin_cost_survivors_5bps_per_side=[]`.
- Terminal decision: `drop_gate1_cost_fragile_no_downstream`; `dense_positive_gate=false`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, and `state/auto-quant/YF_DIABETES_DEVICE_RSI_VWAP_RECLAIM_1M_DENSE_V1/auto_quant_agent_material_rank.20260519T115958.334Z.json`.

Interpretation:

- This is not a provider blocker: the dense 1m diabetes-device lane produced real trades and broad AQ rows, but the apparent raw edge disappears under the required 1/2/5bps cost ladder. Keep it as a cost-fragile negative sample; do not run Pre-Bayes/BBN/CatBoost/execution tree unless a materially different diabetes-device entry family preserves real-cost expectancy.

## Gate 1 Negative: YF Precious-Metals 5m PDA/MTF Template Transfer

Append-only terminal row at `2026-05-19 20:30:52 +0800` after observing pre-write hash `3d2f980a9207371bafff74d0d3c0f7853a6367b705b203f7c9158767f3d3c4be`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T200814+0800-codex-yf-precious-metals-pda-mtf-template-transfer-5m-gate1-v1/`.
- Factor id: `yf_precious_metals_pda_mtf_template_transfer_5m_v1`.
- Branch path template: `RangeReversion -> PreciousMetalsOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_precious_metals_pda_mtf_template_transfer_5m_v1 -> pda_mtf_soft_confirmation_v1`. Labels/provenance prefix: `US_EQ -> etf -> precious_metals -> {symbol} -> 5m`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`, symbols `GLD`, `SLV`, and `GDX`; requested `1m/5m/15m/30m/1h/1d`. Successful retained rows included `GLD 1m/15m/30m/1h/1d = 1951/989/495/267/251`, `SLV 1m/5m/15m/30m/1h/1d = 1951/2965/989/495/267/251`, and `GDX 15m/30m/1h/1d = 989/495/267/251`; `GLD 5m`, `GDX 1m`, and `GDX 5m` fetches exited `1` and were not fabricated.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and rank all exited `0`; authoritative rank had `15` rows, `rank_total_trade_count=75`, `covered_timeframes=['15m','1d','1h','1m','30m','5m']`, and `branch_fields_preserved=true`.
- Exact `5m` evidence: only `SLV/5m` produced an exact-transfer-root row, with `23` trades, `26.0870%` win rate, Sharpe `-2.0894`, raw `-1.68%`, `-2.14%` at `1bps/side`, `-2.60%` at `2bps/side`, and `-3.98%` at `5bps/side`; `exact_5m_survivors_2bps=[]` and `exact_5m_survivors_5bps=[]`.
- Positive siblings: `SLV/1m` had `10` trades and raw `+0.17%`; `GLD/15m` had `10` trades and raw `+0.19%`; both are context/sibling observations and do not rescue the failed exact `5m` transfer root.
- Terminal decision: `keep_negative_sample`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/gate1_cost_stress_audit.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, and `state/auto-quant/YF_PRECIOUS_METALS_PDA_MTF_TRANSFER_GATE1_V1/auto_quant_agent_material_rank.20260519T121638.715Z.json`.

Interpretation:

- This is a clean Gate 1 negative on a fresh-provider precious-metals ETF branch. The run preserves regime-rooted branch fields and covers the requested ladder where Yahoo returned real rows, but the exact `5m` transfer root is negative after raw and cost-stressed scoring. Do not send this packet downstream; future precious-metals work should either start from a denser positive `1m` origin or use a materially different entry family rather than treating weak `1m/15m` siblings as promotion evidence for the failed `5m` root.

## Gate 1 Negative: YF Aircraft-Leasing 1m SuperTrend/RVOL Reclaim Cinar

Append-only terminal row at `2026-05-19 20:52:36 +0800` after observing pre-write hash `e513a95a978d9c26251f58572279dd4c6de3eefb16bc34a115044267f38cc786`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T204227+0800-codex-yf-aircraft-leasing-supertrend-rvol-reclaim-1m-mtf-cinar-v1/`.
- Factor id: `yf_aircraft_leasing_supertrend_rvol_reclaim_1m_mtf_cinar_v1`; source idea recorded as `github.com/cinar/indicator` SuperTrendStrategy, rewritten into local Auto-Quant material with no Go runtime dependency.
- Branch path: `TrendExpansion -> AircraftLeasingTrendContinuation -> supertrend_rvol_vwap_reclaim -> yf_aircraft_leasing_supertrend_rvol_reclaim_1m_mtf_cinar_v1`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`, symbols `AER` and `AL`; requested `1m/5m/15m/30m/1h`. Real retained rows were `AER 1m=1634`, `AER 1h=267`, `AL 5m=702`, `AL 15m=234`, `AL 30m=117`, and `AL 1h=63`; `AER 5m/15m/30m` exited `1` and `AL 1m` exited `3`, so missing legs were not fabricated.
- Auto-Quant evidence: strategy compile, material batch, material dispatch, and material rank all exited `0`; authoritative rank had `6` rows, `covered_timeframes=['15m','1h','1m','30m','5m']`, `branch_fields_preserved=true`, and `rank_total_trade_count=0`.
- Gate 1 evidence: every ranked row (`AER/1m`, `AER/1h`, `AL/5m`, `AL/15m`, `AL/30m`, `AL/1h`) produced `0` trades, `0.0000%` win rate, `0.00%` total profit, Sharpe `0.0000`, and no positive `1m` or higher-timeframe row with trade density.
- Terminal decision: `drop_or_block_gate1_practical`; `dense_positive_gate=false`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `materials/external_seed_sources.json`, and `state/auto-quant/YF_AIRCRAFT_LEASING_SUPERTREND_RVOL_RECLAIM_1M_MTF_CINAR_V1/auto_quant_agent_material_rank.20260519T124608.915Z.json`.

Interpretation:

- This is a source-backed fresh-provider Gate 1 zero-trade negative. The packet proves the provider/material/AQ mechanics for the aircraft-leasing SuperTrend/RVOL branch, but the exact factor produced no trades even on retained real legs, so it earns no cost ladder, no Pre-Bayes/BBN/CatBoost/execution-tree replay, and no promotion. Future aircraft-leasing work should pivot to a materially denser entry family rather than adding overlays to a zero-trade root.

## Gate 1 Negative: ZS 5m PDA Sequence Consistency Light Clone

Append-only terminal row at `2026-05-19 21:00:35 +0800` after observing pre-write hash `8225453debb9301efcc2a25fae38341c90a60d4e47df14727d8e80fabb5bd901`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T202838+0800-codex-yf-ai-security-zs5m-pda-sequence-consistency-light-gate1-v1/`.
- Mother-template lineage: CRWD/YF `5m` `pda_sequence_consistency_light_v1` impact-radius clone into adjacent AI-security software single stock `ZS`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_zs_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> ZS -> 5m`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`, all requested `ZS` legs exited `0`; retained rows were `5m=2965`, `15m=989`, `30m=495`, `1h=267`, and `1d=251`.
- Auto-Quant evidence: provider-status, all fetches, strategy compile, material batch, dispatch, and rank exited `0`; authoritative rank had `5` rows and `rank_total_trade_count=42`.
- Gate 1 evidence: exact `ZS/5m` had `33` trades, raw `+0.46%`, Sharpe `0.4490`, but failed cost stress with `-0.86%` at `2bps/side` and `-2.84%` at `5bps/side`; `ZS/15m` had `9` trades and was raw negative, while `30m/1h/1d` had zero trades.
- Terminal decision: `zs5m_pda_sequence_consistency_light_gate1_failed_no_downstream`; `dense_positive_gate=false`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, and `state/auto-quant/YF_AI_SECURITY_ZS5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_GATE1_V1/auto_quant_agent_material_rank.20260519T123155.405Z.json`.

Interpretation:

- This clone is useful impact-radius boundary evidence: the CRWD PDA-sequence mother-template still generates density on `ZS/5m`, but the edge is too small to survive required low-timeframe costs. Do not rescue it with downstream or looser gates; this marks a cost-fragile edge boundary for the template.

## Impact-Radius Fail-Closed: NET 5m PDA Sequence Consistency Light Clone

Append-only terminal row at `2026-05-19 21:00:35 +0800` after observing pre-write hash `8225453debb9301efcc2a25fae38341c90a60d4e47df14727d8e80fabb5bd901`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T204816+0800-codex-yf-ai-security-net5m-pda-sequence-consistency-light-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T204816+0800-codex-yf-ai-security-net5m-pda-sequence-consistency-light-gate1-v1/downstream-exact-net-5m-pda-sequence-consistency-light-20260519T205559+0800/`.
- Mother-template lineage: CRWD/YF `5m` `pda_sequence_consistency_light_v1` impact-radius clone into adjacent AI-security software single stock `NET`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_net_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> NET -> 5m`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`; retained rows were `NET 5m=2965`, `30m=495`, `1h=267`, and `1d=251`; `NET 15m` fetch exited `1` and was not fabricated.
- Gate 1 evidence: provider-status, strategy compile, material batch, dispatch, and rank exited `0`; authoritative rank had `4` rows and `rank_total_trade_count=37`; exact `NET/5m` had `32` trades, `62.5%` win rate, Sharpe `3.4460`, raw `+4.05%`, `+3.41%` at `1bps/side`, `+2.77%` at `2bps/side`, and `+0.85%` at `5bps/side`; `dense_positive_gate=true` and downstream was allowed.
- Downstream evidence: real-trade extraction kept `32` rows (`20` wins, `12` losses); AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, real-trade ingest, CatBoost/path-ranker train/apply/register/enable, and post-ranker analyze/workflow/Pre-Bayes/policy/execution-candidate all exited `0`; exact branch survived downstream.
- Terminal blocker: closed-loop branch admission stayed `status=fail_closed`, `ready=false`, `actionable=false`, `candidate_status=execution_observe_only`, and `decision_hint=execution_guarded_due_to_high_transition_hazard`; execution tree stayed `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.67`, `transition_hazard=0.6304902417013154`, and `pda_hybrid_alignment=true`; path-ranker visible/used/validation-ready=`true/true/true`.
- Hygiene note: the downstream wrapper still has copied helper/file labels such as `extract_crwd5m_exact_real_trades.py`, `crwd_5m_real_trades.jsonl`, and `auto_quant_real_trades:yf_ai_security_crwd5m_v1`; inspected real-trade rows, rank row, rooted branch path, symbol, instrument, provider, and state symbol are `NET`, so this is evidence-label debt rather than an exact-branch data pivot.
- Terminal decision: `exact_net_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, downstream `state/YF_AI_SECURITY_NET5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_DOWNSTREAM/workflow_snapshot.json`, and downstream `state/YF_AI_SECURITY_NET5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- This is a strong impact-radius clone, not a live-ready factor: the CRWD PDA-sequence mother-template transferred to `NET/5m` with fresh-provider cost-surviving density and exact-root downstream mechanics, but failed the hard execution predicate because `transition_hazard=0.63049` is still above the required `<0.60`. The next NET same-root experiment should target a small transition-hazard reduction while preserving the `32`-trade cost-surviving base; do not lower the hazard gate or call this trade-usable.

## Gate 1 Negative: IBKR AEP Regulated-Utility Opening Drive RVOL

Append-only terminal row at `2026-05-19 21:06:00 +0800` after observing pre-write hash `f0edd7ca4946cebab0b56ca479e59a06f1b4566af21b7343e01d44266f82bb73`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T191342+0800-codex-ibkr-aep-regulated-utility-opening-drive-rvol-gate1-v1/`.
- Branch path: `TrendExpansion -> RegulatedUtilityOpeningDrive -> rvol_breakout_pullback -> ibkr_aep_regulated_utility_opening_drive_rvol_gate1_v1`. Labels/provenance prefix: `IBKR -> US_EQUITY -> REGULATED_UTILITY -> AEP -> 1m`.
- Provider evidence: real IBKR, `local_cache_replay=false`; `1m 30 D` initially exited `3`, then the same exact timeframe downgraded to a real `1m 10 D` retry and exited `0`. Retained rows were `1m_10d=8268`, `5m_3m=10066`, `15m_3m=3372`, `30m_3m=1697`, and `1h_3m=859`; all retained provider legs fed Auto-Quant material.
- Auto-Quant evidence: provider-status, retry fetches, strategy compile, material batch, dispatch, rank, and wrapper exited `0`; the first `1m 30 D` provider attempt is the only nonzero exit and is classified as a provider-window downgrade, not a factor verdict. Artifact validation reports `provider_data_acquired_count=5`, `material_count=5`, `rank_rows=5`, `rank_total_trade_count=80`, `rank_nonzero_trade_rows=5`, and `positive_trade_rows=1`.
- Authoritative `ranking[]` evidence: `5m` had `30` trades, `36.6667%` win rate, Sharpe `0.2338`, raw `+0.40%`; `15m` had `17` trades and raw `-1.71%`; `1h` had `4` trades and raw `-1.62%`; exact `1m` origin had `20` trades, `25.0000%` win rate, Sharpe `-9.5970`, raw `-0.91%`; `30m` had `9` trades and raw `-3.27%`.
- Cost evidence: no row survived the required low-timeframe cost ladder. The only raw-positive `5m` row fell to `-0.20%` at `1bps/side`, `-0.80%` at `2bps/side`, and `-2.60%` at `5bps/side`; exact `1m` was already negative and fell to `-1.71%` at `2bps/side` and `-2.91%` at `5bps/side`; `positive_2bps_rows=[]`, `exact_1m_survives_2bps=false`, and `gate1_cost_density_survives=false`.
- Terminal decision: `drop_gate1_cost_stress_mixed_no_downstream`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `summaries/terminal_decision_summary.md`, `summaries/ibkr_aep_regulated_utility_opening_drive_rvol_gate1_v1.json`, `checks/cost_stress_gate.json`, `checks/artifact_validation.json`, `checks/*.exit`, and `state/auto-quant/IBKR_AEP_REGULATED_UTILITY_OPENING_DRIVE_RVOL_GATE1/auto_quant_agent_material_rank.20260519T111707.118Z.json`.

Interpretation:

- This is a clean IBKR-first Gate 1 negative for the regulated-utility subclass. The provider/AQ chain worked after a legitimate `1m` window downgrade, but the exact `1m` root was negative and the only raw-positive sibling failed even `1bps/side`. Do not send this AEP packet downstream. Future regulated-utility work should use a materially different entry family or another instrument rather than tightening overlays on this cost-fragile opening-drive/RVOL shape.

## Gate 1 Negative: NET 5m Direct PDA/Hybrid Transition Repair

Append-only terminal row at `2026-05-19 21:09:20 +0800` after observing pre-write hash `e55876d55623fcf6c24383ad4bce1d3c166afd9a810748cd507ea8fea2ac0d11`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T210533+0800-codex-yf-ai-security-net5m-direct-pda-hybrid-transition-gate1-v1/`.
- Source blocker: `NET/5m` PDA sequence consistency light clone reached exact-root downstream with `32` trades, `+2.77%` after `2bps/side`, `+0.85%` after `5bps/side`, `execution_readiness=0.67`, and `pda_hybrid_alignment=true`, but failed live readiness because `transition_hazard=0.6304902417013154` remained above the required `<0.60`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_net_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> direct_pda_hybrid_transition_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> NET -> 5m`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`; retained rows were `NET 5m=2965`, `15m=989`, `30m=495`, and `1h=267`; `NET 1d` fetch exited `1` and was not fabricated.
- Auto-Quant evidence: provider-status, retained fetches, strategy compile, material batch, dispatch, and rank exited `0`; authoritative rank had `4` rows and `rank_total_trade_count=12`.
- Gate 1 evidence: exact `NET/5m` collapsed from the source clone's `32` trades to `11` trades, `45.4545%` win rate, Sharpe `-0.1842`, raw `-0.14%`, `-0.58%` at `2bps/side`, and `-1.24%` at `5bps/side`; `NET/30m` had only `1` trade despite raw `+0.48%`; `15m` and `1h` had zero trades.
- Terminal decision: `net5m_direct_pda_hybrid_transition_gate1_failed_no_downstream`; `dense_positive_gate=false`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and `state/auto-quant/YF_AI_SECURITY_NET5M_DIRECT_PDA_HYBRID_TRANSITION_GATE1_V1/auto_quant_agent_material_rank.*.json`.

Interpretation:

- This was the correct same-root repair direction but the wrong overlay strength: it targeted the transition-hazard blocker without lowering gates, yet suppressed the cost-surviving `NET/5m` base and turned the exact branch negative before downstream. The next NET repair should change only one or two transition features from the successful `pda_sequence_consistency_light_v1` base rather than replacing the entry shape with a broad direct-PDA/hybrid overlay.

## Blocked: YF Precious-Metals Futures 5m PDA/MTF Template Transfer

Append-only terminal row at `2026-05-19 21:21:12 +0800` after observing pre-write hash `eec77aedd036ffcab265a6e11890da397456292196ce02b7edb0e5027bd72f41`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T202852+0800-codex-yf-precious-metals-futures-pda-mtf-template-transfer-5m-gate1-v1/`.
- Factor id: `yf_precious_metals_futures_pda_mtf_template_transfer_5m_v1`.
- Branch path: `RangeReversion -> PreciousMetalsFuturesOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_precious_metals_futures_pda_mtf_template_transfer_5m_v1 -> pda_mtf_soft_confirmation_v1`. Labels/provenance prefix: `FUTURES_LIKE -> commodity_futures -> precious_metals -> {GC=F|SI=F} -> 5m`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`; all requested `GC=F` and `SI=F` `1m/5m/15m/30m/1h/1d` fetches exited `0`. Retained rows were `GC=F 1m=6883, 5m=10388, 15m=3470, 30m=1735, 1h=868, 1d=252` and `SI=F 1m=6809, 5m=10376, 15m=3469, 30m=1735, 1h=868, 1d=252`.
- Auto-Quant evidence: strategy compile, material batch, material dispatch, and material rank exited `0`; branch fields were preserved and `rank_rows=12`, but every ranked row had status `failed` before trade metrics with `OperationalException: No pair in whitelist`.
- Gate interpretation: `rank_total_trade_count=0`, `exact_5m_trades=0`, `exact_5m_survivors_2bps=[]`, and `exact_5m_survivors_5bps=[]`; this is a material/Freqtrade pair-whitelist compatibility blocker, not a provider failure and not a profitability verdict on `GC=F` or `SI=F`.
- Terminal decision: `blocked_material_pair_whitelist_no_downstream`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and `state/auto-quant/YF_PRECIOUS_METALS_FUTURES_PDA_MTF_TEMPLATE_TRANSFER_5M_GATE1_V1/auto_quant_agent_material_rank.20260519T124437.245Z.json`.

Interpretation:

- The real futures-like precious-metals data path is viable: Yahoo returned dense `GC=F/SI=F` ladders across `1m` through `1d`. The blocker is Auto-Quant material compatibility for futures-style symbols inside the downstream Freqtrade whitelist. Next useful work for贵金属期货 is a small material/pair-alias repair or a provider/material path that maps futures contracts to an AQ-accepted pair namespace, then rerun Gate 1 with the same rooted identity before judging the factor.

## Impact-Radius Fail-Closed: OKTA 5m VWAP Slope Light Clone

Append-only terminal row at `2026-05-19 21:34:00 +0800` after observing pre-write hash `ba26c68ef6fbad84880d2bb17c128a6f7439249df3fe58a8dd43da5a6b10ff06`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T210910+0800-codex-yf-ai-security-okta5m-vwap-slope-light-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T210910+0800-codex-yf-ai-security-okta5m-vwap-slope-light-gate1-v1/downstream-exact-okta-5m-vwap-slope-light-20260519T211906+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_okta_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> vwap_slope_light_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> OKTA -> 5m`.
- Provider/Gate 1 evidence: fresh yfinance/YF `OKTA/5m` exact lane, `local_cache_replay=false`; exact `OKTA/5m` had `28` trades, `64.2857%` win rate, Sharpe `3.4385`, raw `+3.79%`, `+3.23%` at `1bps/side`, `+2.67%` at `2bps/side`, and `+0.99%` at `5bps/side`; cost-density survived and downstream was allowed.
- Downstream evidence: exact-root replay ingested `28` real trades (`18` wins, `10` losses), and AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, real-trade ingest, CatBoost/path-ranker train/apply/register/enable, and post-ranker readbacks exited `0`; exact branch survived downstream.
- Terminal blocker: live execution stayed fail-closed with `actionable=false`, `ready=false`, persisted candidate `status=no_trade`, workflow execution candidate `status=execution_blocked`, execution tree `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.40487630331943547`, `transition_hazard=0.9641045732248968`, and `pda_hybrid_alignment=false`; path-ranker visible/used/validation-ready=`true/false/true`.
- Terminal decision: `exact_okta_5m_vwap_slope_light_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, Gate 1 `checks/terminal_metrics.json`, and Gate 1 `summaries/terminal_decision_summary.md`.

Interpretation:

- This clone preserves the useful OKTA `5m` cost-surviving density from the AI-security software branch, but the execution predicates are worse than the NET PDA-sequence boundary: high transition hazard plus PDA/hybrid disagreement keep it observation-only. Do not call it live-ready; future OKTA work should target transition/PDA alignment while protecting the `28`-trade cost-surviving base.

## Impact-Radius Fail-Closed: NET 5m Soft Transition Stability Repair

Append-only terminal row at `2026-05-19 21:34:00 +0800` after observing pre-write hash `ba26c68ef6fbad84880d2bb17c128a6f7439249df3fe58a8dd43da5a6b10ff06`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T211049+0800-codex-yf-ai-security-net5m-soft-transition-stability-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T211049+0800-codex-yf-ai-security-net5m-soft-transition-stability-gate1-v1/downstream-exact-net-5m-soft-transition-stability-20260519T212320+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_net_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1 -> soft_transition_stability_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> NET -> 5m`.
- Provider/Gate 1 evidence: fresh yfinance/YF, `local_cache_replay=false`; retained rows included `NET 5m=2965`, `15m=989`, `1h=267`, and `1d=251`, while `30m` exited `1` and was not fabricated. Exact `NET/5m` had `29` trades, `62.069%` win rate, Sharpe `2.5829`, raw `+3.02%`, `+2.44%` at `1bps/side`, `+1.86%` at `2bps/side`, and `+0.12%` at `5bps/side`; `dense_positive_gate=true` and downstream was allowed.
- Downstream evidence: exact-root replay ingested `29` real trades (`18` wins, `11` losses) and exact branch survived. Most downstream commands exited `0`, but `auto_quant_prior_init` exited `1`; refreshed workflow/Pre-Bayes/policy/execution readbacks still produced a fail-closed exact-root decision.
- Terminal blocker: live execution stayed fail-closed with workflow execution candidate `status=execution_observe_only`, `ready=false`, `actionable=false`, execution tree `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.4055852725782136`, `transition_hazard=0.9804902417013155`, `pda_hybrid_alignment=false`, and path-ranker visible/used/validation-ready=`false/false/false`.
- Terminal decision: `exact_net_5m_soft_transition_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, Gate 1 `checks/terminal_metrics.json`, and Gate 1 `summaries/terminal_decision_summary.md`.

Interpretation:

- This was a narrower repair than the broad direct PDA/hybrid overlay and it preserved NET `5m` cost-surviving density, but it made the transition/PDA execution predicates worse rather than better. The next NET same-root repair should avoid stacking another transition filter on top of `pda_sequence_consistency_light_v1`; either isolate the single feature responsible for `transition_hazard` or pivot to a nearby clone after two NET repairs failed on the same execution predicate.

## Gate 1 Negative: YF Index/Gold Futures Mean Reclaim 1m MTF

Append-only terminal row at `2026-05-19 21:45:19 +0800` after observing pre-write hash `cadf9dc0ed1ac170f941eb562ea83a646d44aba53b5b9ce288f446cc4d955c32`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T212429+0800-codex-yf-index-gold-futures-mean-reclaim-1m-mtf-v1/`.
- Factor id: `yf_index_gold_futures_mean_reclaim_1m_mtf_v1`.
- Branch path: `MacroRange -> IndexPreciousMetalFutures -> vwap_ema_reclaim -> yf_index_gold_futures_mean_reclaim_1m_mtf_v1`.
- Provider evidence: fresh yfinance/YF futures-like symbols, `local_cache_replay=false`; `ES=F` retained `1m/5m/15m/30m/1h`, `NQ=F` retained `5m/15m/30m/1h` while `1m` exited `1`, `GC=F` retained `1m/5m/15m/30m` while `1h` exited `1`, and `SI=F/1m` exited `1` and was not fabricated.
- Material compatibility evidence: this run used the current Auto-Quant agent-material alias path and wrote Freqtrade pseudo-pairs such as `GCF/USD`, `NQF/USD`, and `ESF/USD`, so the older `GC=F/USD` pair-whitelist blocker was bypassed; `auto_quant_agent_material_batch`, `dispatch`, and `rank` all exited `0`.
- Auto-Quant evidence: authoritative `ranking[]` had `13` completed rows, `rank_total_trade_count=1601`, and branch fields were preserved. Top raw rows were `NQ=F/30m` with `45` trades, raw `+2.44%`, Sharpe `2.7335`; `GC=F/30m` with `47` trades, raw `+1.30%`; and `ES=F/30m` with `34` trades, raw `+1.02%`.
- Cost evidence: exact `1m` origin did not survive required low-timeframe friction. `ES=F/1m` had `40` trades and raw `+0.86%`, but fell to `+0.06%` at `1bps/side` and `-0.74%` at `2bps/side`; `GC=F/1m` had `113` trades and raw `-2.01%`. `positive_origin_after_2bps=[]`.
- Higher-timeframe context: `NQ=F/30m` stayed positive after `2bps/side` at `+0.64%` but failed `5bps/side` at `-2.06%`; it is a context/sibling candidate only and does not rescue the requested `1m` root.
- Terminal decision: `drop_or_block_gate1_practical`; `dense_positive_gate=false`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Verification notes: `cargo test agent_material_pair_alias -- --nocapture` passed for the current alias helper. `cargo test write_material_config_uses_pair_alias_for_yahoo_futures_symbols -- --nocapture` is currently blocked by an unrelated compile error in `src/application/reporting/analyze_output.rs` (`append_detector_hotplug_context_summary` missing), while the live run itself proves the current material writer emitted `GCF/USD`/`NQF/USD`/`ESF/USD` configs.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `state/auto-quant/YF_INDEX_GOLD_FUTURES_MEAN_RECLAIM_1M_MTF_V1/auto_quant_agent_material_rank.20260519T134010.981Z.json`, and per-unit `aq_workspace/config.tomac.json` files.

Interpretation:

- This is useful progress on the requested 股指/贵金属期货 direction because the previous futures-like pair-whitelist blocker is no longer the active blocker for current materials. The actual factor still fails Gate 1 under the user's cost/density rule: the `1m` origin is cost-fragile or negative, and the only interesting survivor is a `30m` sibling. Future work should either open a separate exact `NQ=F/30m` branch with its own rooted identity and downstream gates, or pivot the `1m` root to a denser but higher per-trade edge family rather than sending this packet downstream.

## Gate 1 Observation: IBKR TSLA Single-Stock VWAP/RVOL Sibling

Append-only terminal row at `2026-05-19 21:48:00 +0800` after observing pre-write hash `cadf9dc0ed1ac170f941eb562ea83a646d44aba53b5b9ce288f446cc4d955c32`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T210106+0800-codex-ibkr-tsla-single-stock-vwap-rvol-sibling-gate1-v1/`.
- Branch path: `TrendExpansion -> SingleStockMomentum -> vwap_reclaim_rvol -> ibkr_tsla_vwap_reclaim_rvol_v1`.
- Provider/AQ evidence: IBKR-first upper-window ladder with real `TSLA` rows for `1m/5m/15m/30m/1h`; provider fetches, strategy compile, Auto-Quant material batch/dispatch/rank all exited `0`; artifact validation reports `provider_data_acquired_count=5`, `material_count=5`, `rank_rows=5`, `rank_total_trade_count=164`, and `rank_nonzero_trade_rows=5`.
- Gate evidence: exact `1m` origin had `41` trades but only `19.5122%` win rate and raw `-1.13%`; `5m` had `46` trades and raw `-1.42%`; `15m` had `47` trades and raw `-2.64%`; only `30m` was positive with `23` trades, `60.8696%` win rate, raw `+2.34%`, and Sharpe `0.9978`.
- Terminal decision: `keep_small_only`; downstream not earned for the requested small-cycle branch; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `summaries/terminal_decision_summary.md`, `summaries/ibkr_tsla_vwap_reclaim_rvol_v1.json`, `checks/artifact_validation.json`, `checks/*.exit`, and `state/auto-quant/IBKR_TSLA_SINGLE_STOCK_VWAP_RVOL_SIBLING/auto_quant_agent_material_rank.20260519T130456.188Z.json`.

Interpretation:

- This is useful IBKR single-stock sibling evidence, not a live candidate. The `30m` row is the only positive pocket, while the `1m/5m/15m` roots are negative or too uneven. Future TSLA work should either promote a new exact `30m` branch with its own cost/downstream proof or pivot to a different dense `1m` entry family; do not flatten the positive sibling into the failed `1m` objective.

## Gate 1 Negative: PANW 5m PDA Sequence Consistency Light Clone

Append-only terminal row at `2026-05-19 21:48:00 +0800` after observing pre-write hash `cadf9dc0ed1ac170f941eb562ea83a646d44aba53b5b9ce288f446cc4d955c32`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T210140+0800-codex-yf-ai-security-panw5m-pda-sequence-consistency-light-gate1-v1/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_panw_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> PANW -> 5m`.
- Provider evidence: fresh yfinance/YF, `local_cache_replay=false`; retained rows were `PANW 5m=2965`, `15m=989`, `30m=495`, `1h=267`, and `1d=251`; provider-status/fetches/strategy compile/AQ batch/dispatch/rank all exited `0`.
- Gate evidence: exact `PANW/5m` had `21` trades, raw `+0.73%`, Sharpe `0.7948`, but failed cost stress with `-0.11%` at `2bps/side` and `-1.37%` at `5bps/side`; `15m/30m` were negative and `1h` had only one positive trade.
- Terminal decision: `panw5m_pda_sequence_consistency_light_gate1_failed_no_downstream`; `dense_positive_gate=false`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `summaries/rank_rows.csv`, `checks/*.exit`, and `state/auto-quant/YF_AI_SECURITY_PANW5M_PDA_SEQUENCE_CONSISTENCY_LIGHT_GATE1_V1/auto_quant_agent_material_rank.20260519T130313.840Z.json`.

Interpretation:

- This marks a cost-fragile edge boundary for the AI-security PDA-sequence template. PANW produces enough exact `5m` trades to inspect, but not enough net expectancy after required friction. Do not run downstream on this exact root unless a materially different PANW overlay survives the cost ladder.

## Downstream Blocked: Binance XRPUSDT 5m Exact Donchian/RVOL Root

Append-only terminal row at `2026-05-19 21:48:00 +0800` after observing pre-write hash `cadf9dc0ed1ac170f941eb562ea83a646d44aba53b5b9ce288f446cc4d955c32`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T210432+0800-codex-binance-xrpusdt-5m-exact-root-donchian-rvol-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T210432+0800-codex-binance-xrpusdt-5m-exact-root-donchian-rvol-v1/downstream-20260519T210652+0800/`.
- Branch path: `TrendExpansion -> AltcoinBreakoutMomentum -> five_minute_donchian_rvol_breakout -> binance_xrpusdt_5m_altcoin_donchian_rvol_exact_root_v1`. Labels/provenance prefix: `Binance -> crypto -> XRPUSDT -> 5m`.
- Gate 1 evidence: retained Binance public cache exact-root replay, `rank_rows=1`, `21` trades, `38.0952%` win rate, raw `+1.20%`, `+0.78%` at `1bps/side`, `+0.36%` at `2bps/side`, and `-0.90%` at `5bps/side`; downstream candidate was allowed but provider parity remained cache-based, not fresh live-ready proof.
- Downstream evidence: Auto-Quant import/prior, analyze/workflow, Pre-Bayes, structural target export, and policy-status all exited `0`; `pre_bayes_allowed=true` and `bbn_allowed=true`.
- Terminal blocker: CatBoost/path-ranker blocked because supervised labels were immature: `policy_training_summary.rows=4`, `mature_rows=0`, `history_mature_rows=0`, `training_weight_rows=0`; therefore `catboost_allowed=false`, `execution_tree_allowed=false`, `promotion_allowed=false`, and `trade_usable=false`.
- Terminal decision: `downstream_prebayes_bbn_complete_catboost_blocked_mature_rows_0`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `checks/*.exit`, and `state/auto-quant/BINANCE_XRPUSDT_5M_EXACT_ROOT/auto_quant_agent_material_rank.20260519T130437.152Z.json`.

Interpretation:

- This is a useful exact crypto-root downstream mechanics packet: Pre-Bayes/BBN completed, but the branch cannot reach CatBoost/execution-tree without mature same-root validation rows and fresh parity. Next XRPUSDT work should collect more exact-root validation or rerun a fresh provider-parity ladder; do not classify the existing cached packet as trade-usable.

## Gate 1 Negative: Kraken QNT/ANKR VWAP Compression Expansion Density 1m Full Ladder

Append-only terminal row at `2026-05-19 21:48:00 +0800` after observing pre-write hash `cadf9dc0ed1ac170f941eb562ea83a646d44aba53b5b9ce288f446cc4d955c32`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T211245+0800-codex-kraken-qnt-ankr-vwap-compression-expansion-density-1m-full-ladder-v1/`.
- Factor id: `kraken_qnt_ankr_vwap_compression_expansion_density_1m_full_ladder_v1`.
- Branch path: `VolatilityCompression -> MidcapUtilityAltcoinVwapExpansion -> one_minute_vwap_compression_expansion_density_full_ladder -> kraken_qnt_ankr_vwap_compression_expansion_density_1m_full_ladder_v1`.
- Provider/AQ evidence: Kraken public REST via `kraken-kline`, symbols `QNTUSD` and `ANKRUSD`, full ladder `1m/5m/15m/30m/1h/4h/1d`; all provider fetches, strategy compile, AQ material batch/dispatch/rank exited `0`; `rank_rows=14`, `rank_total_trade_count=102`, `branch_fields_preserved=true`, and all requested timeframes were covered.
- Gate evidence: both `1m` origin rows had zero trades; positive pockets were siblings only (`QNTUSD/5m` 3 trades raw `+1.29%`, `ANKRUSD/30m` 5 trades raw `+1.53%`, `QNTUSD/15m` 4 trades raw `+0.24%`), while several `1h/4h/1d` rows were materially negative.
- Terminal decision: `drop_or_block_gate1_practical`; `positive_origin_1m=[]`; `cost_gate_survives=false`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `checks/cost_stress_gate.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and `state/auto-quant/KRAKEN_QNT_ANKR_VWAP_COMPRESSION_EXPANSION_DENSITY_1M_FULL_LADDER_V1/auto_quant_agent_material_rank.20260519T131454.863Z.json`.

Interpretation:

- This is a complete Kraken ladder negative for the requested 1m-origin objective. Higher-timeframe pockets do not rescue zero-trade `1m` roots, and the ladder contradicts practical promotion. Future QNT/ANKR work should pivot to a different 1m entry family instead of overlaying this zero-origin compression shape.

## Gate 1 Negative: YF Precious-Metals Costaware RSI/VWAP Reclaim 1m MTF v2

Append-only terminal row at `2026-05-19 21:48:00 +0800` after observing pre-write hash `cadf9dc0ed1ac170f941eb562ea83a646d44aba53b5b9ce288f446cc4d955c32`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T212016+0800-codex-yf-precious-metals-costaware-rsi-vwap-reclaim-1m-mtf-v2/`.
- Factor id: `yf_precious_metals_costaware_rsi_vwap_reclaim_1m_mtf_v2`.
- Branch path: `RangeReversion -> PreciousMetalsOversoldReclaim -> rsi_vwap_reclaim -> yf_precious_metals_costaware_rsi_vwap_reclaim_1m_mtf_v2`.
- Provider evidence: yfinance/YF, `local_cache_replay=false`; `GLD/SLV 1m` and `1h` fetches succeeded, while `5m/15m/30m` fetches exited `1` and were not fabricated. Retained rows included `GLD 1m=1561`, `GLD 1h=281`, `SLV 1m=1561`, and `SLV 1h=281`.
- Auto-Quant evidence: strategy compile, AQ material batch/dispatch/rank all exited `0`; `rank_rows=4`, `rank_total_trade_count=21`, `origin_trades_1m=5`, and branch fields were preserved.
- Gate evidence: `SLV/1m` had only `2` trades, raw `+0.04%`, and fell to `0.00%` at `1bps/side` and `-0.04%` at `2bps/side`; `GLD/1m` had `3` trades and raw `-0.02%`; both `1h` rows were negative. `positive_origin_1m=[]`, `positive_higher_timeframes=[]`, and `cost_gate_survives=false`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and `state/auto-quant/YF_PRECIOUS_METALS_COSTAWARE_RSI_VWAP_RECLAIM_1M_MTF_V2/auto_quant_agent_material_rank.20260519T132307.058Z.json`.

Interpretation:

- This run did the right cost-aware retry shape but still failed the 1m practical gate: too few origin trades and no cost-surviving expectancy. Treat the missing `5m/15m/30m` legs as provider-window/context blockers, not proof; future precious-metals work should use the active sanitized-pair/futures material repair lane or a denser 1m family.

## Downstream Seed Blocked: YF Futures Gold/Silver Sanitized-Pair Reclaim

Append-only terminal row at `2026-05-19 21:56:00 +0800` after observing pre-write hash `9e5929c5f13ea0e7c18141dcbc9ae87dbb2d21c46ea84f6a3e6a8fcf0c5a2841`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T214115+0800-hermes-yf-futures-gold-silver-sanitized-pairs-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T214115+0800-hermes-yf-futures-gold-silver-sanitized-pairs-gate1-v1/downstream-sif1h-20260519T214554+0800/`.
- Branch path: `MacroRange -> MetalsReclaim -> vwap_ema_reclaim -> yf_futures_gold_silver_sanitized_pairs_reclaim_gate1_v1`; downstream exact survivor path: `MacroRange -> MetalsReclaim -> vwap_ema_reclaim -> yf_sif_1h_sanitized_futures_reclaim_v1`.
- Provider/material repair evidence: reused the prior fresh YF `GC=F/SI=F` futures ladder after mapping to AQ-accepted sanitized pairs `GCF/USD` and `SIF/USD`; retained rows covered `1m/5m/15m/30m/1h/1d` with `GC=F` rows `6883/10388/3470/1735/868/252` and `SI=F` rows `6809/10376/3469/1735/868/252`; compile, batch, dispatch, and rank exited `0`, and all `12` rank rows completed.
- Gate 1 evidence: `rank_positive_rows=2`; the exact requested `5m` branch stayed negative (`SIF/5m` `325` trades raw `-15.50%`, `GCF/5m` `348` trades raw `-8.18%`), but `SIF/USD 1h` survived cost with `23` trades, raw `+3.52%`, `65.2174%` win rate, Sharpe `1.2915`, `+2.60%` at `2bps/side`, and `+1.22%` at `5bps/side`.
- Downstream evidence: exact `SIF/USD 1h` seed ran AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, and policy-status with all exits `0`; `pre_bayes_allowed=true`, `bbn_allowed=true`, `policy rows=4`, and `history_rows=7`.
- Terminal blocker: CatBoost/path-ranker and execution tree remained blocked by immature labels: `mature_rows=0`, `history_mature_rows=0`, `rows_with_training_weight=0`; therefore `catboost_allowed=false`, `execution_tree_allowed=false`, `promotion_allowed=false`, and `trade_usable=false`.
- Terminal decision: `sif1h_downstream_seed_maturity_blocked`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, Gate 1 `summaries/provider_provenance_matrix.csv`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, and downstream `checks/*.exit`.

Interpretation:

- This is useful progress on the earlier futures-symbol whitelist blocker: sanitized AQ pair names let the Yahoo futures ladder score, and a `SIF/USD 1h` survivor reached Pre-Bayes/BBN. It still does not satisfy the user's 1m-origin/5m exact objective and is not trade-usable; next work should either mature the same `SIF/USD 1h` seed with more exact validation rows or repair the `1m/5m` futures entry family rather than promoting a higher-timeframe sibling.

## Impact-Radius Fail-Closed: OKTA 5m PDA Alignment Micro Guard

Append-only terminal row at `2026-05-19 21:58:00 +0800` after observing pre-write hash `b0c38b4fbc5f6608250cfcbb94861b2a67c1bc570805bb18f9630a3291c20443`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T215122+0800-codex-yf-ai-security-okta5m-pda-alignment-micro-guard-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T215122+0800-codex-yf-ai-security-okta5m-pda-alignment-micro-guard-gate1-v1/downstream-exact-okta-5m-pda-alignment-micro-guard-20260519T215640+0800/`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_okta_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_alignment_micro_guard_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> OKTA -> 5m`.
- Provider/Gate 1 evidence: fresh yfinance/YF, `local_cache_replay=false`; provider-status, all `OKTA 5m/15m/30m/1h/1d` fetches, strategy compile, AQ material batch, dispatch, and rank all exited `0`. Exact `OKTA/5m` had `25` trades, `60.0%` win rate, Sharpe `2.5692`, raw `+2.92%`, `+2.42%` at `1bps/side`, `+1.92%` at `2bps/side`, and `+0.42%` at `5bps/side`; `dense_positive_gate=true` and downstream was allowed. The `15m` sibling survived `2bps` with `8` trades and `+0.40%`, while `30m` was positive but sparse with `3` trades.
- Downstream evidence: exact-root replay ingested `25` real trades (`15` wins, `10` losses), and AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, real-trade ingest, CatBoost/path-ranker train/apply/register/enable, post-ranker analyze/workflow/Pre-Bayes/policy, and execution-candidate readback all exited `0`; exact branch survived downstream. Note: one inherited downstream exit filename still says `crwd_5m`, but inspected branch path, symbol, package id, material workspace, real trades, and execution trace are `OKTA` / `pda_alignment_micro_guard`.
- Terminal blocker: live execution stayed fail-closed with `status=fail_closed`, `ready=false`, `actionable=false`, execution tree `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.4314096700666623`, `transition_hazard=0.9618690279925088`, and `pda_hybrid_alignment=false`; path-ranker visible/used/validation-ready=`true/false/true`.
- Terminal decision: `exact_okta_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, Gate 1 `summaries/rank_rows.csv`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, downstream `state/YF_AI_SECURITY_OKTA5M_PDA_ALIGNMENT_MICRO_GUARD_DOWNSTREAM/workflow_snapshot.json`, and downstream `state/YF_AI_SECURITY_OKTA5M_PDA_ALIGNMENT_MICRO_GUARD_DOWNSTREAM/execution_tree_trace.json`.

Interpretation:

- This micro guard is an improvement over the earlier OKTA direct PDA/hybrid repair at Gate 1: it preserved usable exact `5m` cost-surviving density and still cleared `5bps/side`. It did not fix the actual downstream execution blocker; transition hazard and PDA/hybrid disagreement remain materially worse than the hard gates. Keep it as impact-radius and blocker evidence, not trade-usable. The next OKTA repair should not add another broad transition overlay; either isolate why the execution tree reports `pda_hybrid_alignment=false` despite a density-preserving PDA guard, or pivot to another nearby clone after repeated OKTA execution-predicate failures.

## IBKR Futures Exact MNQ 1m Liquidity Sweep Downstream Fail-Closed

Append-only terminal row at `2026-05-19 22:17:00 +0800` after observing pre-write hash `e27498f582c5372db752465a7dc3f67d3edd310f9d1d5e90a2d49ead45bdfa24`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T220553+0800-codex-ibkr-futures-liquidity-sweep-vwap-reclaim-1m-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T220553+0800-codex-ibkr-futures-liquidity-sweep-vwap-reclaim-1m-gate1-v1/downstream-exact-mnq-1m-liquidity-sweep-vwap-reclaim-20260519T221517+0800/`.
- Branch path: `RangeReversion -> LiquiditySweepReclaim -> ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: IBKR native historical futures rows, `local_cache_replay=false`; `MES/MNQ/MGC` 1m `2 D` fetches all exited `0` with `2336` normalized rows each. Strategy compile, AQ material batch, dispatch, and rank exited `0`; `rank_rows=9`, `rank_total_trade_count=67`, and branch fields were preserved.
- Exact survivor evidence: `MNQ/dense/1m` was the only cost survivor at the exact root: `7` trades, `71.4286%` win rate, raw `+0.42%`, `+0.28%` at `1bps/side`, `+0.14%` at `2bps/side`, and `-0.28%` at `5bps/side`. `MES` was too sparse/cost-fragile and `MGC` was negative across dense/balanced/quality variants.
- Downstream evidence: the exact `MNQ 1m` survivor was imported into AQ prior, analyzed with real IBKR `MNQ` `1m/15m/1h` frames, passed Pre-Bayes neutralized readback, exported structural path targets, trained/applied/registered/enabled CatBoost/path-ranker, then reran analyze/workflow/Pre-Bayes/policy/export. All `16` downstream commands exited `0`, and the exact branch survived closed-loop admission.
- Terminal blocker: execution tree stayed fail-closed: `closed_loop_branch_admission.status=fail_closed`, `ready=false`, `actionable=false`, `execution_gate_status=execution_observe_only`, `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`. Structural-path validation was also immature: `mature_rows=0`, `history_mature_rows=0`, and production/observation validation `0/30`.
- Terminal decision: `exact_mnq_1m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, Gate 1 `summaries/rank_rows.csv`, downstream `checks/terminal_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `state/IBKR_FUTURES_MNQ_LIQUIDITY_SWEEP_VWAP_RECLAIM_1M_EXACT_DOWNSTREAM/execution_tree_trace.json`, downstream `state/.../policy_training/structural_path_ranking_target_summary.json`, and new runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_futures_mnq_liquidity_sweep_vwap_reclaim_1m_exact_downstream_v1.py`.

Interpretation:

- This is useful IBKR-first futures progress: native IBKR data produced an exact `MNQ 1m` cost-surviving seed, and the rooted branch survived the downstream identity chain. It is not a live factor. The edge fails the hard execution gates and also fails `5bps/side`, so the next futures pass should improve per-trade edge and maturity on the same `MNQ 1m` root or open a separate rooted branch; do not promote this seed into paper/live execution.

### Fresh 7D Gate 1 Addendum: IBKR MES/MNQ/MGC 1m Liquidity Sweep VWAP Reclaim

Append-only terminal addendum at `2026-05-20 02:40:55 +0800` after observing pre-write hash `fa9c49b5899707688fc3e2abf45fbe0eb30cd1285863eebe02a47681ceb7f3db`.

- Fresh run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T023821+0800-codex-ibkr-futures-liquidity-sweep-vwap-reclaim-1m-7d-gate1-v1/`.
- Factor id: `ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1`.
- Branch paths: `RangeReversion -> LiquiditySweepReclaim -> ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1`, `RangeReversion -> LiquiditySweepReclaim -> ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1`, and `RangeReversion -> LiquiditySweepReclaim -> ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1`. Labels/provenance prefixes: `FUTURES -> equity_index -> MES -> 1m`; `FUTURES -> equity_index -> MNQ -> 1m`; `FUTURES -> precious_metals -> MGC -> 1m`.
- Provider/Gate 1 evidence: fresh real IBKR `1m 7 D` fetches all exited `0`: `MES 202606` `9508` rows, `MNQ 202606` `9509` rows, and `MGC 202606` `9509` rows; `local_cache_replay=false`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=9`; `rank_total_trade_count=528`.
- Cost/density evidence: the wider 7D window increased density but did not preserve practical edge. Best MES rows were raw-positive but cost-fragile: `MES/dense/1m` `67` trades, raw `+1.67%`, `+0.33%` after `1bps/side`, `-1.01%` after `2bps/side`, `-5.03%` after `5bps/side`; `MES/quality/1m` `42` trades, raw `+0.92%`, `+0.08%` after `1bps/side`, `-0.76%` after `2bps/side`. MNQ rows were denser but fully inside costs: `MNQ/dense/1m` `92` trades, raw `+1.25%`, `-0.59%` after `1bps/side`, `-2.43%` after `2bps/side`; `MNQ/balanced/1m` `73` trades, raw `+0.98%`, `-1.94%` after `2bps/side`. MGC rows were raw-negative across dense/balanced/quality. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no fresh Pre-Bayes/BBN/CatBoost/execution-tree run from this 7D rerun; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1.py`.

Interpretation:

- The fresh 7D rerun is a useful same-family stress result: density is no longer the blocker, but average trade distance is too small. It weakens the old `MNQ/dense` 2D seed rather than repairing it, so it should not go downstream. The practical search should pivot to a wider-excursion `1m` family or to same-root execution-predicate repair on an already `5bps/side` survivor, without lowering cost gates.

## Gate 1 Negative: IBKR Futures Index/Precious Metals Opening VWAP RVOL Reclaim 1m MTF

Append-only terminal row at `2026-05-19 22:14:00 +0800` after observing pre-write hash `e27498f582c5372db752465a7dc3f67d3edd310f9d1d5e90a2d49ead45bdfa24`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T220712+0800-codex-ibkr-futures-index-precious-metals-opening-vwap-rvol-reclaim-1m-mtf-gate1-v1/`.
- Factor id: `ibkr_futures_opening_vwap_rvol_reclaim_gate1_v1`.
- Branch path template: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_futures_opening_vwap_rvol_reclaim_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index_or_precious_metals -> MES_MNQ_MGC -> 1m`.
- Provider evidence: real IBKR historical `FUT` data, `local_cache_replay=false`, contract month `202606`; instruments `MES`/`MNQ` on `CME` and `MGC` on `COMEX`; all `21/21` fetches exited `0` and covered `1m/5m/15m/30m/1h/4h/1d`. Representative rows: `MES 1m=2337`, `MNQ 1m=2338`, `MGC 1m=2338`, `MES/MNQ 5m=2676`, `MGC 5m=2676`, `MES/MNQ/MGC 15m=1996`, `30m=998`, `1h=499`, `4h=152`, and daily `MES/MNQ=123`, `MGC=122`.
- Auto-Quant evidence: strategy compile, `auto_quant_agent_material_batch`, dispatch, and rank all exited `0`; `material_count=63`, `rank_rows=63`, `rank_total_trade_count=670`, `branch_fields_preserved=true`, `covered_timeframes=21`, and `missing_timeframes=0`.
- Strict 1m-root evidence: no exact `1m` survivor met practical cost/density. `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`. Positive-looking `1m` rows were sparse and not promotable: `MGC/1m quality` `1` trade raw `+0.50%`, `MGC/1m balanced` `2` trades raw `+0.30%`, `MGC/1m dense` `2` trades raw `+0.20%`; `MNQ/1m dense` had `5` trades but only raw `+0.01%` and `-0.19%` at `2bps/side`. `MES/1m` variants were negative after costs.
- Higher-timeframe context only: `MNQ/1h balanced` had `12` trades, `75.0%` win rate, raw `+2.04%`, Sharpe `7.288`, and stayed positive after cost (`+1.56%` at `2bps/side`, `+0.84%` at `5bps/side`). `MES/15m balanced` had `23` trades, raw `+1.40%`, `+0.48%` at `2bps/side` but `-0.90%` at `5bps/side`. `MES/30m dense` had `24` trades, raw `+1.30%`, `+0.34%` at `2bps/side` but `-1.10%` at `5bps/side`. These are sibling candidates only and do not rescue the requested `1m` root.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and `state/auto-quant/IBKR_FUTURES_INDEX_PRECIOUS_METALS_OPENING_VWAP_RVOL_RECLAIM_1M_MTF_V1/auto_quant_agent_material_rank.20260519T141305.405Z.json`.

Interpretation:

- This answers the requested IBKR historical-data futures probe with real local artifacts: the strict `1m` 股指/贵金属 opening VWAP/RVOL reclaim family is not practical after costs and density gates, so it must not go downstream. The useful next branch is not another `1m` overlay on this same shape; open a separate exact-root candidate for `MNQ/1h balanced` or a materially different `1m` entry family, and only promote after the exact branch passes cost, Pre-Bayes/BBN, CatBoost/path-ranker, and execution-tree gates.

## IBKR Futures Exact MNQ 1h Opening VWAP RVOL Downstream Fail-Closed

Append-only terminal row at `2026-05-19 22:27:51 +0800` after observing pre-write hash `49dc5a164674b8a6e99c640f523f5d772d738976c236f50159953035e2002c16`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T221402+0800-codex-ibkr-mnq-1h-opening-vwap-rvol-reclaim-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T221402+0800-codex-ibkr-mnq-1h-opening-vwap-rvol-reclaim-exact-gate1-v1/downstream-exact-mnq-1h-opening-vwap-rvol-reclaim-20260519T222134+0800/`.
- Branch path: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Provider/Gate 1 evidence: exact-root rewrite reused real IBKR native FUT `MNQ 202606` `1h` rows from the fresh broad futures ladder, `local_cache_replay=false_source_provider_root_reused`, with `499` normalized rows. Strategy compile, AQ material batch, dispatch, and rank all exited `0`; `rank_rows=3`, `rank_total_trade_count=36`, and the rewritten branch path was preserved.
- Cost survivor evidence: all `MNQ/1h` variants survived `2bps/side`; `quality` had `6` trades, raw `+1.10%`, `+0.86%` at `2bps`, `+0.50%` at `5bps`; `balanced` had `12` trades, `75.0%` win rate, Sharpe `7.2877`, raw `+2.04%`, `+1.56%` at `2bps`, and `+0.84%` at `5bps`; `dense` had `18` trades, raw `+0.88%`, `+0.16%` at `2bps`, but failed `5bps` at `-0.92%`. Downstream selected the `balanced` exact-root row.
- Downstream evidence: AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, policy-status, CatBoost/path-ranker train/apply/register/enable, post-ranker analyze/workflow/Pre-Bayes/policy, and final export all exited `0` (`17/17`). Pre-Bayes readback was `pass_neutralized`; posterior was `range=0.4796836886250151`, `transition=0.3837469509000121`, `stress=0.13656936047497284`, `trend=0.0` with active regime `range` confidence `0.43058576427128115`.
- Terminal blocker: execution stayed fail-closed with execution candidate `actionable=false`, `candidate_status=no_trade`, `execution_readiness=0.31307880627959406`, execution tree `gate_status=observe`, `branch=transition_guardrail`, `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`, `transition_hazard=0.9335910111527358`, and `pda_hybrid_alignment=false`. Path-ranker score was visible but not used; maturity was still short with `mature_rows=0`, `history_mature_rows=0`, and `rows_with_training_weight=0`.
- Terminal decision: `exact_mnq_1h_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `checks/prompt_to_artifact_checklist.csv`, downstream `state/IBKR_MNQ_1H_OPENING_VWAP_RVOL_RECLAIM_DOWNSTREAM/execution_tree_trace.json`, and runners `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_gate1_v1.py` plus `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_downstream_v1.py`.

Interpretation:

- This separate exact-root branch is better than the strict `1m` opening VWAP/RVOL family at Gate 1 and survives a `5bps/side` cost ladder on `MNQ/1h balanced`, but it is still not live-trade usable. The blocker moved from Gate 1 profitability into execution admission: PDA/hybrid disagreement, high transition hazard, low execution readiness, and immature validation/training rows. Next work should either mature this exact `MNQ/1h` branch with more same-root realized validation or isolate the PDA/hybrid disagreement; do not paper-trade/promote it from AQ profit alone.

## Gate 1 Negative: IBKR Futures Micro-Trend Pullback Reclaim 1m MTF

Append-only terminal row at `2026-05-19 22:31:00 +0800` after observing pre-write hash `5f222584440359180ed5af33cf376dbbf1db2e8900c7e24d1a6535092a7f7857`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T222102+0800-codex-ibkr-futures-micro-trend-pullback-reclaim-1m-mtf-gate1-v1/`.
- Factor id: `ibkr_futures_micro_trend_pullback_reclaim_gate1_v1`.
- Branch path template: `TrendExpansion -> MicroTrendPullbackReclaim -> ibkr_futures_micro_trend_pullback_reclaim_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index_or_precious_metals -> MES_MNQ_MGC -> 1m`.
- Provider evidence: real IBKR historical `FUT` data, `local_cache_replay=false`, contract month `202606`; instruments `MES`/`MNQ` on `CME` and `MGC` on `COMEX`; all `21/21` fetches exited `0` and covered `1m/5m/15m/30m/1h/4h/1d`. Representative retained rows: `MES 1m=2351`, `MNQ 1m=2352`, `MGC 1m=2352`, all `5m=2679`, all `15m=1997`, all `30m=999`, all `1h=500`, all `4h=152`, and daily `MES/MNQ=123`, `MGC=122`.
- Auto-Quant evidence: strategy compile, `auto_quant_agent_material_batch`, dispatch, and rank all exited `0`; `material_count=63`, `rank_rows=63`, `rank_total_trade_count=670`, `branch_fields_preserved=true`, `covered_timeframes=21`, and `missing_timeframes=0`.
- Strict `1m` root evidence: no exact `1m` row met the practical cost/density gate. `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`. Positive-looking `1m` rows were sparse only: `MGC/quality/1m` had `1` trade raw `+0.50%`; `MGC/balanced/1m` had `2` trades raw `+0.30%`; `MGC/dense/1m` had `2` trades raw `+0.20%`; `MNQ/quality/1m` had `1` trade raw `+0.08%`. None satisfies daily density or cost-stressed evidence.
- Higher-timeframe context only: `MNQ/balanced/1h` had `12` trades, `75.0%` win rate, raw `+2.04%`, `+1.56%` at `2bps/side`, and `+0.84%` at `5bps/side`; `MNQ/quality/1h` had `6` trades, raw `+1.10%`, `+0.86%` at `2bps`, `+0.50%` at `5bps`; `MES/balanced/15m` had `23` trades and survived `2bps` at `+0.48%` but failed `5bps`; `MES/dense/30m` had `24` trades and survived `2bps` at `+0.34%` but failed `5bps`. These are sibling candidates and do not rescue the failed exact `1m` root.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run for the `1m` root; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_FUTURES_MICRO_TREND_PULLBACK_RECLAIM_1M_MTF_V1/auto_quant_agent_material_rank.20260519T142651.653Z.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_futures_micro_trend_pullback_reclaim_1m_mtf_gate1_v1.py`.

Interpretation:

- This was the intended denser `1m` pivot after the prior IBKR futures `1m` failures, but it still does not produce a practical `1m` root. The full IBKR ladder and AQ mechanics are healthy; the factor shape is the failure. The usable lead remains a separate exact-root `MNQ/1h` branch or a materially different `1m` entry family with enough same-day density and per-trade edge. Do not run downstream on this `1m` packet or use the profitable `1h` sibling as proof for the failed `1m` root.

## Gate 1 Negative: IBKR MNQ 1m Session Liquidity Crowding Guard

Append-only terminal row at `2026-05-19 22:45:00 +0800` after observing pre-write hash `ac238806ac956adeaaa9cdece9e23314cebbd60aad569c024441a0af16a295ee`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223410+0800-codex-ibkr-mnq1m-session-liquidity-crowding-guard-gate1-v1/`.
- Factor id: `ibkr_mnq1m_session_liquidity_crowding_guard_gate1_v1`.
- Branch path: `RangeReversion -> LiquiditySweepReclaim -> ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1 -> session_liquidity_crowding_guard_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false`; `MNQ 202606` `1m 2 D` fetch exited `0` with `2364` normalized rows. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=3` and `rank_total_trade_count=3`.
- Cost/density evidence: all three exact `MNQ/1m` overlay rows produced only `1` trade each, `0.0%` win rate, raw `-0.13%`, `-0.17%` at `2bps/side`, and `-0.23%` at `5bps/side`. `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`.
- Metadata hygiene note: AQ branch path preserved the base liquidity-sweep root and overlay label, but the terminal checker reported `branch_fields_preserved=false` because the overlay factor id was not embedded in the branch path; this is evidence hygiene debt, not a rescue of the negative cost/density result.
- Terminal decision: `drop_or_block_gate1_overlay_suppression_or_cost`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_session_liquidity_crowding_guard_gate1_v1.py`.

Interpretation:

- This was a same-root attempt to repair the earlier `MNQ 1m` liquidity-sweep execution/crowding blocker without broadening into a new market, but it over-filtered the branch and flipped the surviving baseline into sparse negative rows. Do not downstream this overlay. The next `MNQ 1m` repair needs to preserve the original 7-trade/2bps-positive baseline while changing only one execution-blocker predicate at a time, or pivot to a genuinely different 1m entry family instead of adding more guards to this failed overlay.

## IBKR Futures Exact MNQ 1h Transition PDA Alignment Light Downstream Fail-Closed

Append-only terminal row at `2026-05-19 22:55:00 +0800` after observing pre-write hash `238646c996cc690e50909ba6ddfaf524ff3436ad3be9b36b26fb3743658f2b0a`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223837+0800-codex-ibkr-mnq-1h-transition-pda-alignment-light-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223837+0800-codex-ibkr-mnq-1h-transition-pda-alignment-light-gate1-v1/downstream-exact-mnq-1h-transition-pda-alignment-light-20260519T224636+0800/`.
- Branch path: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_v1 -> transition_pda_alignment_light_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Provider/Gate 1 evidence: exact-root rewrite reused real IBKR native `MNQ 202606` `1h` provider rows from the fresh `MNQ 1h` source root, `local_cache_replay=false_source_provider_root_reused`, with `499` normalized rows. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `rank_rows=3`, `rank_total_trade_count=88`, and branch fields were preserved.
- Cost survivor evidence: `MNQ/light_density/1h` had `41` trades, `80.4878%` win rate, Sharpe `11.9322`, raw `+5.90%`, `+4.26%` after `2bps/side`, and `+1.80%` after `5bps/side`; `MNQ/light_balanced/1h` had `30` trades, raw `+3.41%`, `+2.21%` after `2bps`, and `+0.41%` after `5bps`. Downstream selected the dense exact-root row.
- Downstream evidence: AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, policy status, path-ranker fallback train, post-ranker analyze/workflow/Pre-Bayes/policy/export all produced readbacks. The initial runner tried to apply/register the fallback as CatBoost and got `09/10/11` failures; manual repair applied `weighted_feature_sum_v1` direct fallback and registered it with exit `0` in `checks/manual_path_ranker_repair_readback.json`.
- Path-ranker/maturity blocker: CatBoost was not trainable because the downstream dataset had effectively one usable training sample and constant features; direct fallback registration is `candidate_set_only`, not live readiness. Manual repair readback saw `raw_scored_mature_rows=2/30`, `production_validation_rows=2/30`, and `observation_validation_rows=0/30`.
- Execution blocker: final execution trace remained fail-closed: `gate_status=observe`, `branch=transition_guardrail`, `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`, `execution_readiness=0.31307880627959406`, `transition_hazard=0.9335910111527358`, `pda_hybrid_alignment=false`, path-ranker visible/used=`true/false`, and execution candidate `actionable=false` / `candidate_status=no_trade`.
- Terminal decision: `exact_mnq_1h_transition_pda_alignment_light_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `checks/manual_path_ranker_repair_readback.json`, downstream `summaries/terminal_decision_summary.md`, downstream `state/IBKR_MNQ_1H_TRANSITION_PDA_ALIGNMENT_LIGHT_DOWNSTREAM/execution_tree_trace.json`, downstream `state/.../policy_training/structural_path_ranking_target_summary.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq_1h_transition_pda_alignment_light_downstream_v1.py`.

Interpretation:

- This is the strongest current IBKR `MNQ 1h` AQ packet by cost/density, but it still fails the user's live-practical gates. The overlay improved AQ economics but did not solve the actual execution predicate: high transition hazard and PDA/hybrid disagreement remain almost unchanged from the previous `MNQ 1h` exact branch. Treat it as a scoped observation/downstream candidate only. Next work should either gather/mature more same-root validation rows or isolate the PDA/hybrid disagreement directly; it should not be promoted or paper-traded from AQ profit alone, and it does not replace the still-open `1m`-first requirement.

## IBKR Futures Exact MNQ 1h Mild Session Stability Downstream Fail-Closed

Append-only terminal row at `2026-05-19 22:47:00 +0800` after observing pre-write hash `227f8886e257ca18a57c85e74d6983594f7d4b49beb3b7791d43ed7d58db2501`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223915+0800-codex-ibkr-mnq-1h-opening-vwap-rvol-mild-session-stability-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223915+0800-codex-ibkr-mnq-1h-opening-vwap-rvol-mild-session-stability-gate1-v1/downstream-exact-mnq-1h-mild-session-stability-20260519T224409+0800/`.
- Branch path: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_mnq_1h_opening_vwap_rvol_mild_session_stability_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Provider/Gate 1 evidence: exact-root repair reused the real IBKR native `MNQ 202606` `1h` provider rows from the source futures ladder, `local_cache_replay=false_source_provider_root_reused`, with `499` normalized rows. Strategy compile, AQ material batch, dispatch, and rank all exited `0`; `rank_rows=3`, `rank_total_trade_count=30`, and branch fields were preserved.
- Cost survivor evidence: `MNQ/balanced/1h` remained cost-positive but weaker than the prior exact branch: `10` trades, `70.0%` win rate, raw `+1.46%`, Sharpe `4.8775`, `+1.06%` after `2bps/side`, and `+0.46%` after `5bps/side`. `MNQ/soft/1h` had `17` trades but failed `2bps` at `-0.05%`; `MNQ/quality/1h` had only `3` trades and failed costs.
- Downstream evidence: AQ import/prior, analyze/workflow, Pre-Bayes, structural target export, policy-status, CatBoost/path-ranker train/apply/register/enable, post-ranker analyze/workflow/Pre-Bayes/policy, and final export all exited `0` (`17/17`). Pre-Bayes readback was `pass_neutralized`; posterior stayed `range=0.4796836886250151`, `transition=0.3837469509000121`, `stress=0.13656936047497284`, `trend=0.0` with active regime `range` confidence `0.43058576427128115`.
- Terminal blocker: the mild session-stability repair did not improve the execution predicate: execution candidate `actionable=false`, `candidate_status=no_trade`, `execution_readiness=0.31307880627959406`, execution tree `gate_status=observe`, `branch=transition_guardrail`, `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`, `transition_hazard=0.9335910111527358`, and `pda_hybrid_alignment=false`. Path-ranker score was visible but not used; maturity remained short with `mature_rows=0`, `history_mature_rows=0`, and `rows_with_training_weight=0`.
- Terminal decision: `exact_mnq_1h_mild_session_stability_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `state/IBKR_MNQ_1H_OPENING_VWAP_RVOL_MILD_SESSION_STABILITY_DOWNSTREAM/execution_tree_trace.json`, downstream `state/.../policy_training/structural_path_ranking_target_summary.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq_1h_opening_vwap_rvol_mild_session_stability_downstream_v1.py`.

Interpretation:

- This repair preserved enough exact-root AQ cost/density to justify downstream replay, but it did not move the live blockers at all. It is not trade-usable. The next `MNQ/1h` step should isolate why PDA/hybrid alignment remains false under the same posterior and execution-readiness profile, or add real same-root realized validation rows; do not keep adding broad session/transition guards that reduce Gate 1 edge without changing the execution-tree predicate.

## IBKR Futures Follow-Up Batch: MNQ/MES/MGC Exact-Root Attempts

Append-only terminal row at `2026-05-19 22:55:00 +0800` after observing pre-write hash `238646c996cc690e50909ba6ddfaf524ff3436ad3be9b36b26fb3743658f2b0a`.

### MNQ 1h Transition PDA Alignment Light: Downstream Fail-Closed

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223837+0800-codex-ibkr-mnq-1h-transition-pda-alignment-light-gate1-v1/`.
- Downstream roots inspected: `.../downstream-exact-mnq-1h-transition-pda-alignment-light-20260519T224636+0800/` and `.../downstream-exact-mnq-1h-transition-pda-alignment-light-20260519T224848+0800/`.
- Branch path: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_v1 -> transition_pda_alignment_light_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Gate 1 evidence: exact `MNQ 1h` IBKR `499` rows, branch fields preserved, AQ exits `0`, `rank_rows=3`, `rank_total_trade_count=88`. `MNQ/light_density/1h` was the strongest row: `41` trades, `80.4878%` win rate, raw `+5.90%`, Sharpe `11.9322`, `+4.26%` after `2bps/side`, and `+1.80%` after `5bps/side`. `MNQ/light_balanced/1h` also survived `5bps/side` with `30` trades and `+0.41%` after cost.
- Downstream evidence: latest downstream readback stayed fail-closed: Pre-Bayes `pass_neutralized`, posterior `range=0.4796836886250151`, `transition=0.3837469509000121`, `stress=0.13656936047497284`, `trend=0.0`, execution candidate `actionable=false`, `candidate_status=no_trade`, `execution_readiness=0.31307880627959406`, execution tree `gate_status=observe`, `branch=transition_guardrail`, `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`, `transition_hazard=0.9335910111527358`, and `pda_hybrid_alignment=false`. One downstream summary reports `all command exits zero=false`; use the readback as fail-closed evidence, not as promotion proof.
- Terminal decision: `exact_mnq_1h_transition_pda_alignment_light_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MNQ 1h PDA Transition Light Guard: Downstream Fail-Closed

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T224146+0800-codex-ibkr-mnq-1h-opening-vwap-rvol-reclaim-pda-transition-light-guard-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T224146+0800-codex-ibkr-mnq-1h-opening-vwap-rvol-reclaim-pda-transition-light-guard-gate1-v1/downstream-exact-mnq-1h-opening-vwap-rvol-pda-transition-light-guard-20260519T224416+0800/`.
- Branch path: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_v1 -> pda_transition_light_guard_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Gate 1 evidence: exact `MNQ 1h` IBKR `499` rows, branch fields preserved, AQ exits `0`, `rank_rows=1`; `MNQ/balanced/1h` had `11` trades, `72.7273%` win rate, raw `+2.01%`, Sharpe `6.9776`, `+1.57%` after `2bps/side`, and `+0.91%` after `5bps/side`.
- Downstream evidence: all downstream commands exited `0`, Pre-Bayes `pass_neutralized`, but execution stayed fail-closed with `execution_readiness=0.31307880627959406`, `transition_hazard=0.9335910111527358`, `pda_hybrid_alignment=false`, execution tree `gate_status=observe`, `branch=transition_guardrail`, and `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`.
- Terminal decision: `exact_mnq_1h_pda_transition_light_guard_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MES 30m Micro-Trend Pullback: Downstream Fail-Closed

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T224406+0800-codex-ibkr-mes-30m-micro-trend-pullback-reclaim-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T224406+0800-codex-ibkr-mes-30m-micro-trend-pullback-reclaim-exact-gate1-v1/downstream-exact-mes-30m-micro-trend-pullback-reclaim-20260519T225140+0800/`.
- Branch path: `TrendExpansion -> MicroTrendPullbackReclaim -> ibkr_mes_30m_micro_trend_pullback_reclaim_exact_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 30m`.
- Gate 1 evidence: exact `MES 30m` IBKR `999` rows, branch fields preserved, AQ exits `0`, `rank_rows=3`, `rank_total_trade_count=49`. The selected `MES/dense/30m` row had `24` trades, raw `+1.30%`, `+0.34%` after `2bps/side`, but failed `5bps/side` at `-1.10%`.
- Downstream evidence: all downstream commands exited `0` and Pre-Bayes was stronger (`pass_hard`) with posterior `trend=0.8337849033854463`, but execution still failed: `execution_readiness=0.5166860066507305`, `transition_hazard=0.9621609759959593`, `pda_hybrid_alignment=false`, execution tree `gate_status=observe`, `branch=transition_guardrail`.
- Terminal decision: `exact_mes_30m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MNQ 1h Micro-Trend Pullback: Downstream Fail-Closed

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T224526+0800-codex-ibkr-futures-mnq-micro-trend-pullback-reclaim-1h-gate1-v1/`.
- Downstream roots inspected: `.../downstream-exact-mnq-1h-micro-trend-pullback-reclaim-20260519T224958+0800/` and `.../downstream-exact-mnq-1h-micro-trend-pullback-reclaim-20260519T224959+0800/`.
- Branch path: `TrendExpansion -> MicroTrendPullbackReclaim -> ibkr_futures_mnq_micro_trend_pullback_reclaim_1h_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Gate 1 evidence: fresh IBKR fetch exited `0` with `500` `MNQ 1h` rows; AQ exits `0`, branch fields preserved, `rank_rows=3`, `rank_total_trade_count=49`. `MNQ/balanced/1h` survived cost with `16` trades, `75.0%` win rate, raw `+2.44%`, `+1.80%` after `2bps/side`, and `+0.84%` after `5bps/side`.
- Downstream evidence: downstream mechanics exited `0`, but execution collapsed to `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`; `promotion_allowed=false` and `trade_usable=false`.
- Terminal decision: `exact_mnq_1h_downstream_fail_closed`; `update_goal=false`.

### MGC 1m RSI/VWAP Washout Reclaim: Gate 1 Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T224322+0800-codex-ibkr-mgc1m-rsi-vwap-washout-reclaim-gate1-v1/`.
- Provider/Gate 1 evidence: fresh IBKR `MGC 202606` `1m 2 D` fetch exited `0` with `2374` normalized rows; strategy compile, AQ batch, dispatch, and rank exited `0`; `rank_rows=3`, `rank_total_trade_count=89`, `branch_fields_preserved=true`.
- Cost/density evidence: all exact `MGC 1m` rows were negative after raw and cost stress: dense `30` trades raw `-0.75%`, quality `29` trades raw `-0.48%`, balanced `30` trades raw `-0.64%`; no `2bps/side` survivor.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This batch confirms the current blocker is not simply missing AQ profitability. Multiple exact-root IBKR futures candidates now survive Gate 1 costs, including a strong `MNQ/1h transition_pda_alignment_light` packet, but downstream still converges on high transition hazard and PDA/hybrid disagreement. The next useful work should be a direct execution-predicate diagnostic or realized-validation/maturity path for the strongest same-root `MNQ/1h transition_pda_alignment_light` survivor, not another broad session/PDA guard overlay.

## Repair Diagnostic: MNQ 1h Transition PDA Alignment Light Ranker Apply/Register

Append-only terminal row at `2026-05-19 23:01:00 +0800` after observing pre-write hash `7667a90da707830f13ef2ad3fedecc287c97677bb676dd70a1b5f30912a09586`.

- Target downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223837+0800-codex-ibkr-mnq-1h-transition-pda-alignment-light-gate1-v1/downstream-exact-mnq-1h-transition-pda-alignment-light-20260519T224848+0800/`.
- Branch path: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_v1 -> transition_pda_alignment_light_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Mechanical blocker found: the original downstream replay trained a `weighted_feature_sum_v1` direct-fallback artifact, but `09_apply_catboost_scores` did not pass `--allow-direct-fallback`, and `11_register_trainer` used `--model-family catboost` against a `weighted_feature_sum_v1` artifact. That made the pre-repair execution trace read `trainer_artifact=missing`, `runtime_source=history_path`, and `path_ranker_score_used_by_execution_tree=false`.
- Repair commands: `18_repair_apply_direct_fallback`, `19_repair_apply_scores_to_ict`, `20_repair_register_direct_trainer --model-family weighted_feature_sum_v1`, `21_repair_enable_runtime`, `22_repair_analyze_after_ranker`, `23_repair_workflow_after_ranker`, `24_repair_pre_bayes_after_ranker`, and `25_repair_policy_after_ranker` all exited `0`.
- Post-repair ranker state: execution tree now reports `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=true`, `path_ranker_model_family=weighted_feature_sum_v1`, `path_ranker_runtime_source=registered_model_artifact`, `ranker_validation_ready=true`, and `execution_gate_status=pass`. Policy summary improved to `rows_with_execution_gate_status=1`, `rows_with_training_weight=1`, `mature_rows=1`, `history_mature_rows=2`.
- Remaining live blocker: final execution still stayed observe-only with `execution_readiness=0.31307880627959406`, `hybrid_transition_hazard=0.9335910111527358`, `pda_hybrid_alignment=false`, execution tree `gate_status=observe`, `branch=transition_guardrail`, and `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`.
- Terminal decision: `ranker_apply_register_repaired_but_execution_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/ranker_repair_metrics.json`, `summaries/ranker_repair_summary.md`, `path_ranker_scores.repair.csv`, repaired `state/IBKR_MNQ_1H_TRANSITION_PDA_ALIGNMENT_LIGHT_DOWNSTREAM/execution_tree_trace.json`, repaired `state/.../policy_training/structural_path_ranking_target_summary.json`, and `checks/18_repair_*.exit` through `checks/25_repair_*.exit`.

Interpretation:

- This separates a mechanical downstream plumbing issue from the actual trading blocker. The path-ranker is now registered and used, so the remaining failure is not CatBoost/apply plumbing; it is the structural execution predicate: high hybrid transition hazard, PDA/hybrid disagreement, low execution readiness, and too little mature/validation depth. The next useful work should target real validation/maturity rows or a different entry family whose PDA sequence agrees with the hybrid regime, rather than rerunning the same ranker repair.

## Gate 1 Negative: IBKR MNQ 1m VWAP Reject Short 7D

Append-only terminal row at `2026-05-19 22:56:00 +0800` after observing pre-write hash `238646c996cc690e50909ba6ddfaf524ff3436ad3be9b36b26fb3743658f2b0a`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T225222+0800-codex-ibkr-mnq1m-vwap-reject-short-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_vwap_reject_short_7d_gate1_v1`.
- Branch path: `TrendReversal -> VwapRejectShort -> ibkr_mnq1m_vwap_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`; `MNQ 202606` `1m 7 D` fetch produced `9269` normalized rows covering `2026-05-10` through `2026-05-19`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=4`, `rank_total_trade_count=112`, and branch fields were preserved.
- Cost/density evidence: local vector discovery found a short-side `vwap_reject` lead, but exact Auto-Quant Gate 1 did not survive costs. Best AQ row was `MNQ/rvol11_hold45_fixed/1m/short`: `25` trades, `56.0%` win rate, raw `+0.51%`, `+0.01%` at `1bps/side`, `-0.49%` at `2bps/side`, and `-1.99%` at `5bps/side`. Other rows were raw-negative: `rvol11_hold60_fixed` `23` trades raw `-0.22%`, `rvol15_hold45_fixed` `19` trades raw `-0.04%`, and `rvol11_hold45_reclaim_exit` `45` trades raw `-0.56%`.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_VWAP_REJECT_SHORT_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_vwap_reject_short_7d_gate1_v1.py`.

Interpretation:

- This was a materially different `MNQ/1m` short-enabled pivot after the long-side liquidity-sweep and crowding-guard failures. The wider IBKR `7 D` window produced enough density, and AQ preserved the exact rooted branch, but the edge is too small for realistic intraday friction. Do not downstream this branch. The next `MNQ/1m` attempt should require stronger per-trade distance before packaging, or shift to a separate 1m family whose raw edge clears the 2bps gate with margin rather than only by local-vector approximation.

## Gate 1 Negative: IBKR MNQ 1m Opening Drive VWAP RVOL Continuation 7D

Append-only terminal row at `2026-05-19 23:12:46 +0800` after observing pre-write hash `d25208386145a3bb664cd3009aee2d112275c12dfde201a9f5403c6f13a0a0e7`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T231013+0800-codex-ibkr-mnq1m-opening-drive-vwap-rvol-continuation-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_opening_drive_vwap_rvol_continuation_7d_gate1_v1`.
- Branch path: `TrendExpansion -> OpeningDriveVwapRvolContinuation -> ibkr_mnq1m_opening_drive_vwap_rvol_continuation_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`; `MNQ 202606` `1m 7 D` normalized to `9269` rows from `/tmp/ict-mnq-1m-7d.csv`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=3`, `rank_total_trade_count=37`, and branch fields were preserved.
- Cost/density evidence: the best exact row was `MNQ/dense/1m` with `17` trades, `47.0588%` win rate, raw `+0.17%`, Sharpe `3.5689`, `-0.17%` at `1bps/side`, `-0.51%` at `2bps/side`, and `-1.53%` at `5bps/side`. `MNQ/balanced/1m` had `11` trades raw `-0.07%`; `MNQ/quality/1m` had `9` trades raw `-0.04%`. `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_OPENING_DRIVE_VWAP_RVOL_CONTINUATION_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_opening_drive_vwap_rvol_continuation_7d_gate1_v1.py`.

Interpretation:

- This was a materially different `MNQ/1m` long continuation attempt using the same real 7-day IBKR window, but its raw edge was too small before costs and failed immediately at the 1bps/2bps ladder. Do not downstream this branch. The next `1m` attempt needs larger per-trade distance before AQ packaging, not another small-profit opening-drive variant.

## Execution Predicate Diagnostic: MNQ 1h Transition PDA Alignment Light

Append-only diagnostic row at `2026-05-19 23:18:00 +0800` after observing pre-write hash `f8754bce984aca0bb23ec2455dc0bb52551214a8fcc8a4acce931bb147ad0229`.

- Target downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T223837+0800-codex-ibkr-mnq-1h-transition-pda-alignment-light-gate1-v1/downstream-exact-mnq-1h-transition-pda-alignment-light-20260519T225804+0800/`.
- Diagnostic artifacts: `checks/execution_predicate_diagnostic.json` and `summaries/execution_predicate_diagnostic.md`.
- Branch path: `TrendExpansion -> OpeningVwapRvolReclaim -> ibkr_mnq_1h_opening_vwap_rvol_reclaim_exact_v1 -> transition_pda_alignment_light_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Gate 1 context: the selected exact-root row remained economically strong (`MNQ/light_density/1h`, `41` trades, `80.4878%` win rate, raw `+5.90%`, `+4.26%` after `2bps/side`, `+1.80%` after `5bps/side`). This proves the blocker is downstream predicate agreement, not AQ gross profitability.
- Current downstream blocker: `execution_readiness=0.31307880627959406`, `transition_hazard=0.9335910111527358`, `pda_hybrid_alignment=false`, execution tree `gate_status=observe`, `branch=transition_guardrail`, and `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`.
- Root cause from current artifacts: canonical structural regime is `range` with confidence `0.43058576427128115` and probabilities `range=0.4796836886250151`, `transition=0.3837469509000121`, `stress=0.13656936047497284`, `trend=0.0`; hybrid label is `range_choppy`. The PDA sequence line is `pda_sequence_primary_cluster=cluster_0 pda_family=trend pda_confidence=1.000 pda_consistency=0.727 pda_ensemble_mean_confidence=0.970`; Pre-Bayes explicitly records `PDA cluster family 'trend' disagrees with regime family 'range'`.
- Direction conflict: the same analyze run records `multi_timeframe_direction_bias=bullish multi_timeframe_alignment_score=1.000 multi_timeframe_entry_alignment_score=0.918`, while the execution candidate selected direction is `Bear`; Pre-Bayes neutralized factor alignment with conflict flags `multi_timeframe_direction_conflict` and `pda_regime_family_disagreement`.
- Code-rule readback: `src/domain/regime/hybrid.rs` requires PDA family to equal the hybrid regime family, not be `transition`, and have `bull`/`bear` directional confirmation. If this fails, confidence is reduced and transition hazard is at least `1 - adjusted_confidence + 0.25`; with current confidence this naturally lands near `0.93`. `src/main.rs` then forces `transition_guardrail` when `transition_hazard >= 0.60` or evidence contains `pda_hybrid_alignment=false`.
- Terminal decision: `diagnostic_only_no_promotion`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The next factor should not be another broad session/PDA guard overlay on this exact `MNQ/1h` branch. To move toward the live gates, the next candidate must make branch family, PDA family, MTF direction, and execution direction agree before downstream: either a trend-family long continuation when PDA/MTF are bullish, or a range-family setup where the PDA cluster also maps to range. A raw-profitable Bear row under bullish MTF and trend PDA will keep failing the same guardrail even if AQ cost metrics are strong.

## Gate 1 Negative: IBKR MGC 1m Opening VWAP RVOL Reclaim

Append-only terminal row at `2026-05-19 23:24:00 +0800` after observing pre-write hash `1a2651cd8ef3ce2e2a0b17f7073e404585d95b2e4e1bde65f28a0fc7f657071b`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T232136+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-gate1-v1/`.
- Factor id: `ibkr_mgc1m_opening_vwap_rvol_reclaim_gate1_v1`.
- Branch path: `RangeReversion -> OpeningVwapRvolReclaim -> ibkr_mgc1m_opening_vwap_rvol_reclaim_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false`; `MGC 202606` `1m 2 D` fetch exited `0` and normalized `2412` rows. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=3`, `rank_total_trade_count=10`, and branch fields were preserved.
- Cost/density evidence: all exact `MGC 1m` rows were negative before and after friction. `MGC/quality/1m` had `2` trades, `50.0%` win rate, raw `-0.11%`, `-0.15%` at `1bps/side`, `-0.19%` at `2bps/side`, and `-0.31%` at `5bps/side`. `MGC/dense/1m` and `MGC/balanced/1m` each had `4` trades, raw `-0.16%`, `-0.32%` at `2bps/side`, and `-0.56%` at `5bps/side`.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MGC1M_OPENING_VWAP_RVOL_RECLAIM_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_opening_vwap_rvol_reclaim_gate1_v1.py`.

Interpretation:

- This was a separate贵金属 `MGC/1m` opening VWAP/RVOL reclaim probe, distinct from the earlier MGC RSI/VWAP washout and MGC 5m PDA/MTF template-transfer attempts. The real IBKR data and AQ mechanics worked, but the exact branch failed immediately at Gate 1 because density was low and every row was raw-negative. Do not downstream or promote this branch. The next贵金属 futures attempt needs either a wider feasible provider window with a denser entry family or a materially different per-trade-distance setup; another reclaim variant with 2-4 trades is not useful.

## Gate 1 Negative: IBKR MNQ 1m VWAP Bollinger Snapback 7D

Append-only terminal row at `2026-05-19 23:31:54 +0800` after observing pre-write hash `9bb0ff1dd741815b78ac5785e722cad200a2d36d1ac9bccd0bbe5d8188583c08`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T232956+0800-codex-ibkr-mnq1m-vwap-bollinger-snapback-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_vwap_bollinger_snapback_7d_gate1_v1`.
- Branch path: `RangeReversion -> VwapBollingerSnapback -> ibkr_mnq1m_vwap_bollinger_snapback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`; `MNQ 202606` `1m 7 D` normalized to `9269` rows from `/tmp/ict-mnq-1m-7d.csv`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=3`, `rank_total_trade_count=24`, and branch fields were preserved.
- Discovery note: a local vector screen over the same rows suggested a range-reversion VWAP/Bollinger snapback shape with larger fixed-hold raw edge, but this was only candidate discovery; exact Auto-Quant/Freqtrade exits did not preserve that edge.
- Cost/density evidence: all three exact rows were identical after AQ scoring: `MNQ/dense/1m`, `MNQ/balanced/1m`, and `MNQ/quality/1m` each had `8` trades, `62.5%` win rate, raw `+0.11%`, Sharpe `2.92`, `-0.05%` at `1bps/side`, `-0.21%` at `2bps/side`, and `-0.69%` at `5bps/side`. `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_VWAP_BOLLINGER_SNAPBACK_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_vwap_bollinger_snapback_7d_gate1_v1.py`.

Interpretation:

- This was the first follow-up after the execution-predicate diagnostic to try a `RangeReversion` root instead of another trend/PDA-disagreement branch. The branch identity was correct and the real IBKR/AQ mechanics worked, but the exact AQ result failed costs at `1bps/side`; it remains observation evidence only. The next `MNQ/1m` candidate needs either a different exit model that AQ can reproduce, or stronger entry distance before packaging; local fixed-hold edge alone is not enough.

## Gate 1 Negative: IBKR MNQ 1m Opening Range Breakout 7D

Append-only terminal row at `2026-05-19 23:47:54 +0800` after observing pre-write hash `2515b2325fe80c34509d5e89db60b3cf392e8c23188dc98460b04d4d06d833fb`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T234500+0800-codex-ibkr-mnq1m-opening-range-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_opening_range_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> OpeningRangeBreakout -> ibkr_mnq1m_opening_range_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family note: this was a deliberate expansion away from the repeated VWAP/Bollinger/RSI/opening-drive neighborhood into the long-lived ORB/opening-range-breakout family used by discretionary and systematic intraday traders. It is still judged only by exact rooted IBKR/AQ evidence, not by source popularity.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`; `MNQ 202606` `1m 7 D` normalized to `9269` rows from `/tmp/ict-mnq-1m-7d.csv`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=3`, branch fields were preserved, and all rows kept the exact rooted path.
- Cost/density evidence: `MNQ/scout/1m`, `MNQ/balanced/1m`, and `MNQ/selective/1m` all returned `status=failed` with `0` trades and no numeric win/PnL metrics. The terminal checker treated each as `0` trades / raw `0.0%`; `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_OPENING_RANGE_BREAKOUT_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_opening_range_breakout_7d_gate1_v1.py`.

Interpretation:

- The ORB family deserves inclusion in the search space, but this exact MNQ/1m/7D implementation produced no executable density under AQ. Treat it as a source-backed negative sample, not a provider blocker. Continue broadening across mature, distinct strategy families rather than micro-tuning the same VWAP/Bollinger/RSI neighborhood; next useful families include Turtle/Donchian breakout, Keltner/ATR channel breakout, SuperTrend continuation, Connors/RSI2 mean reversion, Ichimoku cloud continuation, Darvas/NR7 expansion, and volatility-contraction breakout, each as its own exact rooted branch.

## Gate 1 Negative: IBKR MGC 1m Opening VWAP RVOL Reclaim 7D

Append-only terminal row at `2026-05-19 23:36:00 +0800` after observing pre-write hash `5cb4036d5a6da829db69f9a2df88db772170e231577ce6138ede2ae353c3a451`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_opening_vwap_rvol_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> OpeningVwapRvolReclaim -> ibkr_mgc1m_opening_vwap_rvol_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false`; `MGC 202606` `1m 7 D` fetch exited `0` and normalized `9320` rows. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=3`, `rank_total_trade_count=25`, and branch fields were preserved.
- Cost/density evidence: expanding the same family from `2 D` to `7 D` improved raw density but not practical edge. `MGC/dense/1m` had `14` trades, `71.4286%` win rate, raw `+0.15%`, Sharpe `5.6885`, but flipped to `-0.13%` at `1bps/side`, `-0.41%` at `2bps/side`, and `-1.25%` at `5bps/side`. `MGC/balanced/1m` had `8` trades, raw `+0.01%`, `-0.31%` at `2bps`; `MGC/quality/1m` had `3` trades and raw `-0.12%`.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MGC1M_OPENING_VWAP_RVOL_RECLAIM_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_opening_vwap_rvol_reclaim_7d_gate1_v1.py`.

Interpretation:

- This closes the immediate question of whether the earlier `MGC/1m` opening VWAP/RVOL failure was only a short-window artifact. It was not: the `7 D` IBKR window increased rows and trade count, but the best exact row still lost money after `1bps/side`. Do not downstream or promote this family. The next precious-metals futures attempt should switch to a materially different entry/exit shape with larger per-trade distance rather than extending this reclaim template again.

## IBKR Futures Follow-Up: MNQ 1m RSI2 ORB and MES 15m

Append-only terminal row at `2026-05-19 23:52:53 +0800` after observing pre-write hash `c9e2762800dcebc6c8dd30e8b86c3587660d9ac044c6c84c444ea7c117210dc1`.

### MNQ 1m Source-Backed RSI2 ORB Reclaim 7D: Gate 1 Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T234423+0800-codex-ibkr-mnq1m-source-backed-rsi2-orb-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_source_backed_rsi2_orb_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> OpeningRangeFailure -> ibkr_mnq1m_source_backed_rsi2_orb_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family: Connors RSI2 mean reversion plus failed opening-range breakdown reclaim; source popularity was used only for candidate selection, not as evidence.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9269` rows from `/tmp/ict-mnq-1m-7d.csv`, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`; provider status, strategy compile, AQ material batch, dispatch, and rank all exited `0`; `rank_rows=4`, `rank_total_trade_count=185`, and branch fields were preserved.
- Cost/density evidence: density was high but economics failed. `failed_breakdown_snapback` had `52` trades raw `-0.26%`; `connors_rsi2_orb_reclaim_dense` had `49` trades raw `-0.56%`; `balanced` had `48` trades raw `-0.14%`; `deep_oversold_session_reclaim` had `36` trades raw `+0.03%` but flipped to `-0.69%` at `1bps/side` and `-1.41%` at `2bps/side`. No `2bps` or `5bps` survivor.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree replay; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MES 15m Micro-Trend Pullback: Downstream Fail-Closed

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T233231+0800-codex-ibkr-mes-15m-micro-trend-pullback-reclaim-exact-gate1-v1/`.
- Latest downstream root inspected: `support/docs/experiments/actionable-regime-confidence/runs/20260519T233231+0800-codex-ibkr-mes-15m-micro-trend-pullback-reclaim-exact-gate1-v1/downstream-exact-mes-15m-micro-trend-pullback-reclaim-20260519T233843+0800/`.
- Branch path: `TrendExpansion -> MicroTrendPullbackReclaim -> ibkr_mes_15m_micro_trend_pullback_reclaim_exact_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 15m`.
- Provider/Gate 1 evidence: exact `MES 15m` IBKR rows were reused from the source provider root with `local_cache_replay=false_source_provider_root_reused`, `1997` normalized rows, `15 mins 1 M`. AQ mechanics exited `0`; `rank_rows=3`, `rank_total_trade_count=75`, and branch fields were preserved.
- Cost survivor evidence: selected `MES/balanced/15m` had `23` trades, `52.1739%` win rate, raw `+1.40%`, Sharpe `4.0481`, `+0.48%` after `2bps/side`, but `-0.90%` after `5bps/side`. Dense and quality rows failed `2bps`.
- Downstream evidence: all latest downstream commands exited `0`, and exact branch survived through Pre-Bayes/BBN/CatBoost/execution-tree mechanics. The final readback was still fail-closed: `pre_bayes_status=pass_neutralized`, `execution_candidate_actionable=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, `raw_scored_mature=0/30`, `production_validation=0/30`, and `observation_validation=0/30`.
- Terminal decision: `exact_mes_15m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- `MES/15m` repeats the same downstream blocker seen in other IBKR futures branches, while source-backed RSI2/ORB proves that density alone is not enough when per-trade edge is too small. Keep promotion gates unchanged; practical continuation should focus on branches with real cost survival and then repair exact-root downstream execution predicates.

## Gate 1 Negative: IBKR MNQ 1m Donchian/Turtle Breakout 7D

Append-only terminal row at `2026-05-19 23:59:00 +0800` after observing pre-write hash `b6c2e56922153d57c9624acaf1b35c0bbc29098411f8df38ca841879125cf22e`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T235728+0800-codex-ibkr-mnq1m-donchian-turtle-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_donchian_turtle_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> DonchianTurtleBreakout -> ibkr_mnq1m_donchian_turtle_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family note: this deliberately expanded the search space into the classic Donchian/Turtle channel-breakout family, separate from the repeated VWAP/Bollinger/RSI/opening-drive variants. The family pedigree was used only for candidate selection; promotion still required exact rooted IBKR/AQ evidence.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`; `MNQ 202606` `1m 7 D` normalized to `9269` rows from `/tmp/ict-mnq-1m-7d.csv`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=4`, `rank_total_trade_count=112`, and every row preserved the exact branch path.
- Cost/density evidence: density was available but per-trade distance was too small for realistic friction. `MNQ/turtle20_dense/1m` had `34` trades, `50.0%` win rate, raw `+0.46%`, but `-0.22%` at `1bps/side` and `-0.90%` at `2bps/side`. `MNQ/turtle55_trend/1m` had `16` trades, raw `+0.44%`, `+0.12%` at `1bps/side`, but `-0.20%` at `2bps/side`. `MNQ/turtle20_pullback_break/1m` had `35` trades, raw `+0.60%`, but `-0.10%` at `1bps/side` and `-0.80%` at `2bps/side`. `MNQ/turtle20_balanced/1m` was raw negative. `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_DONCHIAN_TURTLE_BREAKOUT_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_donchian_turtle_breakout_7d_gate1_v1.py`.

Interpretation:

- Donchian/Turtle is now represented as a mature public-strategy family in the exact IBKR futures evidence set. This exact MNQ `1m` implementation produced plenty of trades, but the gross edge was not large enough to survive `2bps/side`, so it is useful negative/control evidence only. Continue the diversity expansion into other time-tested families such as Keltner/ATR channel breakout, SuperTrend continuation, Ichimoku cloud continuation, Darvas/NR7 expansion, volatility-contraction breakout, and CMF/OBV accumulation breakout rather than narrowing back into the same VWAP/Bollinger/RSI cluster.

## Repair Readback: IBKR MNQ 1m Compression Breakout Ranker Registration

Append-only repair row at `2026-05-20 00:12:53 +0800` after observing pre-write hash `f5b1919664f30e073889756b428d863b1d6c06446679e9f95474d9e20f9b5b19`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T233909+0800-codex-ibkr-mnq1m-compression-breakout-7d-gate1-v1/`.
- Repaired downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T233909+0800-codex-ibkr-mnq1m-compression-breakout-7d-gate1-v1/downstream-exact-mnq-1m-compression-breakout-20260519T235843+0800/`.
- Branch path: `TrendExpansion -> CompressionBreakout -> ibkr_mnq1m_compression_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 context: exact IBKR `MNQ 202606` `1m 7 D` rows stayed valid with `9269` normalized rows and two `5bps/side` survivors. Best row remained `MNQ/mid_compression_n45/1m`: `8` trades, `87.5%` win rate, raw `+1.02%`, `+0.70%` after `2bps/side`, and `+0.22%` after `5bps/side`.
- Mechanical repair: the first downstream rerun cleared the prior `03_analyze_seed` timeout (`03_analyze_seed.exit=0`) but still registered the CatBoost trainer artifact as `weighted_feature_sum_v1`, producing `structural path ranking trainer artifact family mismatch: cli='weighted_feature_sum_v1' source='catboost'`. Repair steps `17_repair_register_catboost`, `18_repair_enable_runtime`, `19_repair_analyze_after_register`, `20_repair_workflow_after_register`, `21_repair_pre_bayes_after_register`, `22_repair_policy_after_register`, and `23_repair_export_target_after_register` all exited `0` after registering with `--model-family catboost`.
- Post-repair ranker state: policy readback now reports `runtime_selection=enabled_registered_artifact_ready`, `runtime_source=registered_artifact`, `score_model_family=catboost`, `score_source=external_artifact`, `runtime_matches=3`, and `trainer_artifact_status=present_validation_insufficient`. This separates ranker plumbing from the remaining trading blocker.
- Remaining blocker: exact branch is still fail-closed. Workflow readback reports `closed_loop_branch_admission.status=fail_closed`, `candidate_status=execution_blocked`, `pre_bayes_gate_status=pass_neutralized`, and the exact branch path is visible. Execution tree remains observe-only with `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`, `execution_tree_branch=transition_guardrail`, and `execution_gate_status=execution_observe_only`. Structural validation is still immature: `mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, and `observation_validation=0/30`.
- Terminal decision: `ranker_register_repaired_but_execution_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- `MNQ/1m CompressionBreakout` remains the strongest fresh low-timeframe IBKR futures candidate because Gate 1 survived realistic costs, but it is not trade-usable. The mechanical downstream mismatch is repaired; the next useful same-root work must target PDA/hybrid disagreement and transition guardrail without collapsing Gate 1 density, or use compression as the baseline while testing a different large-distance `1m` family.

## Gate 1 Negative: IBKR MNQ 1m Keltner/ATR Channel Breakout 7D

Append-only terminal row at `2026-05-20 00:14:00 +0800` after observing pre-write hash `f5b1919664f30e073889756b428d863b1d6c06446679e9f95474d9e20f9b5b19`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001148+0800-codex-ibkr-mnq1m-keltner-atr-channel-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_keltner_atr_channel_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> KeltnerAtrChannelBreakout -> ibkr_mnq1m_keltner_atr_channel_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family note: this represented the classic Keltner/ATR channel-breakout family as a distinct public-strategy lane after Donchian/Turtle, ORB, RSI2/ORB, VWAP/Bollinger, and opening-drive variants. Popularity or age was used only to expand candidate diversity; exact rooted IBKR/AQ evidence still controlled the verdict.
- Provider/Gate 1 evidence: real IBKR native historical `FUT` rows, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`; `MNQ 202606` `1m 7 D` normalized to `9269` rows from `/tmp/ict-mnq-1m-7d.csv`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=4`, `rank_total_trade_count=152`, and every row preserved the exact branch path.
- Cost/density evidence: density was strong enough for screening but not for real friction. `MNQ/trend_upper20/1m` had `19` trades, `47.3684%` win rate, raw `+0.18%`, but `-0.20%` at `1bps/side` and `-0.58%` at `2bps/side`. `MNQ/balanced_upper20/1m` had `23` trades, raw `+0.26%`, but `-0.20%` at `1bps/side` and `-0.66%` at `2bps/side`. `MNQ/dense_upper20/1m` had `38` trades, raw `+0.10%`, but `-0.66%` at `1bps/side` and `-1.42%` at `2bps/side`. `MNQ/pullback_reclaim_mid/1m` had `72` trades and was raw negative. `exact_1m_survivors_2bps=[]` and `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_KELTNER_ATR_CHANNEL_BREAKOUT_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_keltner_atr_channel_breakout_7d_gate1_v1.py`.

Interpretation:

- Keltner/ATR has now been given its own exact IBKR futures branch rather than being left as a suggestion. The branch had real signal density, but the per-trade edge was far too small after `1bps/side` and `2bps/side`, so it is observation/control evidence only. Continue broadening to the next mature families, with priority on setups that naturally create larger per-trade distance: SuperTrend continuation, Ichimoku cloud continuation, Darvas/NR7 expansion, volatility-contraction breakout, CMF/OBV accumulation breakout, or a precious-metals futures branch with a materially different non-reclaim shape.

## IBKR Futures Follow-Up: MNQ Compression PDA Overlay, MNQ SuperTrend, MGC ADX/ATR Short Fade

Append-only terminal row at `2026-05-20 00:23:00 +0800` after observing pre-write hash `0e2026beb9866656ab5a4fa6c87848b725faf6d386a6a4f30de3abc524be2e8f`.

### MNQ 1m Compression Breakout PDA Overlay: Downstream Still Fail-Closed

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T000638+0800-hermes-ibkr-mnq1m-compression-breakout-pda-transition-overlay-7d-gate1-v1/`.
- Downstream root inspected: `support/docs/experiments/actionable-regime-confidence/runs/20260520T000638+0800-hermes-ibkr-mnq1m-compression-breakout-pda-transition-overlay-7d-gate1-v1/downstream-exact-mnq-1m-compression-breakout-pda-overlay-20260520T000815+0800/`.
- Branch path: `TrendExpansion -> CompressionBreakout -> ibkr_mnq1m_compression_breakout_7d_gate1_v1 -> ibkr_mnq1m_compression_breakout_pda_transition_overlay_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D` rows; provider status, strategy compile, material batch, dispatch, and rank exited `0`; branch fields were preserved. The overlay retained the same useful cost profile as the base compression branch: `MNQ/pda_mid_compression_n45/1m_pda_overlay` had `8` trades, `87.5%` win rate, raw `+1.00%`, `+0.68%` after `2bps/side`, and `+0.20%` after `5bps/side`; `MNQ/pda_mid_breakout_n120/1m_pda_overlay` had `7` trades, raw `+0.77%`, `+0.49%` after `2bps/side`, and `+0.07%` after `5bps/side`.
- Downstream readback: initial commands `01-09` and `11-16` exited `0`, while `10_register_trainer.exit=1` repeated the CatBoost model-family registration mismatch. Repair steps observed so far `17_repair_register_catboost.exit=0` and `18_repair_enable_runtime.exit=0`; the concurrently running repair/analyze steps were still active at readback time and were not killed or overwritten.
- Stable terminal metrics from the downstream packet remain fail-closed: `pre_bayes_status=pass_neutralized`, `execution_candidate_actionable=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, `promotion_allowed=false`, and `trade_usable=false`.
- Terminal decision: `cost_survivor_observation_only_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MNQ 1m SuperTrend/ADX Breakout 7D: Gate 1 Candidate Only

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001405+0800-codex-ibkr-mnq1m-supertrend-adx-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_supertrend_adx_breakout_7d_gate1_v1`.
- Source-backed family: `SuperTrend/ADX/ATR trend continuation`.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`; provider status, strategy compile, material batch, dispatch, and rank exited `0`; branch fields were preserved.
- Cost/density evidence: `MNQ/st_adx_quality/1m` had `11` trades, `63.6364%` win rate, raw `+0.75%`, `+0.31%` after `2bps/side`, but `-0.35%` after `5bps/side`. `MNQ/st_adx_balanced/1m` had `21` trades, raw `+0.89%`, `+0.05%` after `2bps/side`, but `-1.21%` after `5bps/side`. Dense and pullback rows failed `2bps/side`; there were no `5bps/side` survivors.
- Terminal decision: `gate1_candidate_downstream_allowed_but_not_promotion_ready`; downstream scripts were still active at readback time, so no final Pre-Bayes/BBN/CatBoost/execution-tree verdict is recorded here; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 1m ADX/ATR Short Fade 7D: Gate 1 Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001647+0800-codex-ibkr-mgc1m-adx-atr-short-fade-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_adx_atr_short_fade_7d_gate1_v1`.
- Branch path: `RangeReversion/Fade -> ADX_ATR_ShortFade -> ibkr_mgc1m_adx_atr_short_fade_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Gate 1 evidence: strategy compile, material batch, dispatch, and rank exited `0`; branch fields were preserved.
- Cost/density evidence: `MGC/fade_fast/1m` had `10` trades, `20.0%` win rate, raw `-0.27%`, `-0.67%` after `2bps/side`, and `-1.27%` after `5bps/side`; `MGC/short_dense/1m` had `3` trades, `0.0%` win rate, raw `-0.21%`; balanced and quality rows had `0` trades. There were no `2bps` or `5bps` survivors.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree replay; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The current best fresh IBKR futures evidence is still the MNQ `1m` compression family because it repeatedly survives `5bps/side`, including after PDA/session-style overlays, but downstream execution predicates still reject it. SuperTrend/ADX is useful as a source-backed diversity candidate, but without `5bps/side` survival it should not outrank compression. The latest MGC short-fade attempt is a clean precious-metals negative and should not be downstreamed.

## Gate 1 Negative Batch: IBKR MNQ NR7 and MGC Trend/Fade Diversity

Append-only terminal row at `2026-05-20 00:34:00 +0800` after observing pre-write hash `a22b04300474ece6dd4125ab5bba72492ab67ff4627df39cec4211e182ae6ec8`.

### MNQ 1m NR7/Crabel Breakout 7D: Zero-Trade Negative

- Run roots inspected: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001025+0800-codex-ibkr-mnq1m-nr7-crabel-breakout-7d-gate1-v1/` and rerun `support/docs/experiments/actionable-regime-confidence/runs/20260520T002353+0800-codex-ibkr-mnq1m-nr7-crabel-breakout-7d-gate1-v1/`.
- Branch path: `VolatilityCompression -> NR7CrabelBreakout -> ibkr_mnq1m_nr7_crabel_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family: `NR7/Crabel volatility breakout`.
- Gate 1 evidence: provider status, strategy compile, material batch, dispatch, and rank exited `0`; branch fields were preserved; `rank_rows=4`.
- Cost/density evidence: all rows had `0` trades (`MNQ/nr7_dense/1m`, `MNQ/nr7_balanced/1m`, `MNQ/nr4_selective/1m`, `MNQ/inside_nr7_trend/1m`); `rank_total_trade_count=0`, `exact_1m_survivors_2bps=[]`, and `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree replay; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 1m Donchian/Turtle Breakout 7D: Dense Raw-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001149+0800-codex-ibkr-mgc1m-donchian-turtle-breakout-7d-gate1-v1/`.
- Branch path: `TrendExpansion -> DonchianTurtleBreakout -> ibkr_mgc1m_donchian_turtle_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Gate 1 evidence: strategy compile, material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=118`.
- Cost/density evidence: the family produced plenty of trades but every exact row was negative before costs. `MGC/compression20/1m` had `24` trades raw `-0.06%`, `-1.02%` after `2bps/side`; `MGC/turtle20_dense/1m` had `36` trades raw `-1.34%`; `MGC/atr_channel40/1m` had `31` trades raw `-1.47%`; `MGC/turtle55_balanced/1m` had `27` trades raw `-1.50%`.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 1m ADX/ATR Breakout 7D: Dense Raw-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001156+0800-codex-ibkr-mgc1m-adx-atr-breakout-7d-gate1-v1/`.
- Branch path: `TrendExpansion -> AdxAtrBreakout -> ibkr_mgc1m_adx_atr_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Gate 1 evidence: strategy compile, material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=132`.
- Cost/density evidence: all rows were raw-negative and worsened after costs. `MGC/adx_dense/1m` had `37` trades raw `-0.64%`, `-2.12%` after `2bps/side`; `MGC/adx_balanced/1m` had `27` trades raw `-0.86%`; `MGC/atr_impulse_fast/1m` had `44` trades raw `-0.51%`; `MGC/adx_quality/1m` had `24` trades raw `-1.07%`.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MNQ 1m SuperTrend/ADX Breakout 7D: Downstream Fail-Closed Follow-Up

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001405+0800-codex-ibkr-mnq1m-supertrend-adx-breakout-7d-gate1-v1/`.
- Downstream root inspected: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001405+0800-codex-ibkr-mnq1m-supertrend-adx-breakout-7d-gate1-v1/downstream-exact-mnq-1m-supertrend-adx-breakout-20260520T001649+0800/`.
- Branch path: `TrendExpansion -> SuperTrendAdxBreakout -> ibkr_mnq1m_supertrend_adx_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 context: `MNQ/st_adx_quality/1m` was the selected survivor with `11` trades, `63.6364%` win rate, raw `+0.75%`, `+0.31%` after `2bps/side`, and `-0.35%` after `5bps/side`; `MNQ/st_adx_balanced/1m` also survived `2bps` but failed `5bps`.
- Downstream evidence: exact-root replay ran through Auto-Quant import/prior, seed analyze/workflow/Pre-Bayes, structural target export, CatBoost/path-ranker train/apply/register/enable, post-ranker analyze/workflow/Pre-Bayes/policy, and final export with core commands `01-16` all exiting `0`. The ranker runtime was `enabled_registered_artifact_ready`, `runtime_source=registered_artifact`, `score_model_family=catboost`, `runtime_matches=3`, and `trainer_artifact_status=present_validation_insufficient`.
- Terminal blocker: final readback preserved the exact path but stayed fail-closed: `closed_loop_branch_admission.status=fail_closed`, `candidate_status=execution_blocked`, `pre_bayes_gate_status=pass_neutralized`, `execution_candidate_actionable=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, `raw_scored_mature=0/30`, `production_validation=0/30`, and `observation_validation=0/30`.
- Terminal decision: `exact_mnq_1m_supertrend_adx_breakout_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MNQ 1m SuperTrend/NR7 7D: Metadata/Rank Failure

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001457+0800-codex-ibkr-mnq1m-supertrend-nr7-7d-gate1-v1/`.
- Branch path: `PublicStrategyDiversity -> SuperTrendNr7 -> ibkr_mnq1m_supertrend_nr7_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Evidence: provider status, strategy compile, material batch, and dispatch exited, but terminal metrics report `branch_fields_preserved=false`, `rank_rows=0`, and `rank_total_trade_count=0`.
- Terminal decision: `drop_or_block_gate1_practical`; no downstream; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 1m SuperTrend/ATR Trail 7D: Dense Raw-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T002115+0800-codex-ibkr-mgc1m-supertrend-atr-trail-7d-gate1-v1/`.
- Branch path: `TrendExpansion -> SuperTrendAtrTrail -> ibkr_mgc1m_supertrend_atr_trail_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Gate 1 evidence: strategy compile, material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=124`.
- Cost/density evidence: all rows were raw-negative and cost-negative. `MGC/st_dense/1m` had `35` trades raw `-0.43%`, `-1.83%` after `2bps/side`; `MGC/st_balanced/1m` had `30` trades raw `-0.63%`; `MGC/st_pullback/1m` had `31` trades raw `-0.69%`; `MGC/st_quality/1m` had `28` trades raw `-0.56%`.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The public-strategy diversity sweep now covers NR7/Crabel plus several MGC trend/fade families. These packets add useful negative controls, but none should enter Pre-Bayes/BBN/CatBoost/execution-tree. For MNQ, compression remains the only fresh `1m` family with repeated `5bps/side` survival; for MGC, the current 1m trend/reclaim/fade set is consistently raw-negative or cost-negative, so the next precious-metals attempt needs a materially different larger-distance shape rather than another Donchian/ADX/SuperTrend variant.

## Downstream Fail-Closed: IBKR MNQ 1m SuperTrend/ADX Breakout 7D

Append-only downstream addendum at `2026-05-20 00:38:00 +0800` after observing pre-write hash `ea0f6fda1febdde888a1a3c09899c204f3621ff7b12c0208458950901d259ab7`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001405+0800-codex-ibkr-mnq1m-supertrend-adx-breakout-7d-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001405+0800-codex-ibkr-mnq1m-supertrend-adx-breakout-7d-gate1-v1/downstream-exact-mnq-1m-supertrend-adx-breakout-20260520T001649+0800/`.
- Factor id: `ibkr_mnq1m_supertrend_adx_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> SuperTrendAdxBreakout -> ibkr_mnq1m_supertrend_adx_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 context: real IBKR `MNQ 202606` `1m 7 D`; `rank_rows=4`, `rank_total_trade_count=96`, and exact branch fields were preserved. `MNQ/st_adx_quality/1m` survived `2bps/side` with `11` trades, raw `+0.75%`, and `+0.31%` after `2bps/side`, but failed `5bps/side` at `-0.35%`. `MNQ/st_adx_balanced/1m` also survived `2bps/side` weakly (`+0.05%`) but failed `5bps/side`.
- Downstream evidence: `01_auto_quant_results_import`, `02_auto_quant_prior_init`, seed analyze/workflow/Pre-Bayes/export, path-ranker train/apply/register/enable, post-ranker analyze/workflow/Pre-Bayes/policy, and final export all exited `0` in the completed downstream root. Exact branch survived into workflow, execution candidate, policy target, and execution tree artifacts.
- Hard blocker: final `checks/terminal_metrics.json` reports `decision=exact_mnq_1m_supertrend_adx_breakout_downstream_fail_closed`, `pre_bayes_status=pass_neutralized`, `execution_candidate_actionable=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`. Execution tree reports `closed_loop_branch_admission.status=fail_closed`, `execution_gate_status=execution_observe_only`, `execution_tree_branch=transition_guardrail`, and `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`. Policy maturity remains insufficient (`mature_rows=0`).
- Terminal decision: `exact_mnq_1m_supertrend_adx_breakout_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: downstream `checks/terminal_metrics.json`, downstream `state/IBKR_MNQ1M_SUPERTREND_ADX_BREAKOUT_DOWNSTREAM_V1/workflow_snapshot.json`, `execution_candidate.json`, `execution_tree_trace.json`, `policy_training/structural_path_ranking_target_summary.json`, and Gate 1 `checks/terminal_metrics.json`.

Interpretation:

- SuperTrend/ADX is useful source-backed diversity because it survived `2bps/side` and exercised the full same-root downstream chain, but it is not a live-practical factor. It failed the same hard execution predicate trio as other IBKR futures candidates: `transition_hazard >= 0.60`, `pda_hybrid_alignment=false`, and `execution_readiness < 0.65`. Do not lower gates or promote from the `2bps` Gate 1 survivor. The next useful work is still either same-root execution predicate repair on the stronger `MNQ/1m CompressionBreakout` family that survives `5bps/side`, or a new large-distance family whose Gate 1 edge clears costs with more margin before downstream.

## Downstream Fail-Closed: IBKR MNQ 1m Compression Breakout Session-Liquidity Overlay 7D

Append-only downstream addendum at `2026-05-20 00:42:00 +0800` after observing pre-write hash `8edfdf28629b8aec5d11620a266b14d84e9b1edc702e265b3d1ae46066d1028c`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001531+0800-hermes-ibkr-mnq1m-compression-breakout-session-liquidity-overlay-7d-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T001531+0800-hermes-ibkr-mnq1m-compression-breakout-session-liquidity-overlay-7d-gate1-v1/downstream-exact-mnq-1m-compression-breakout-session-liquidity-overlay-20260520T001633+0800/`.
- Factor id: `ibkr_mnq1m_compression_breakout_session_liquidity_overlay_7d_gate1_v1`.
- Branch path: `TrendExpansion -> CompressionBreakout -> ibkr_mnq1m_compression_breakout_7d_gate1_v1 -> ibkr_mnq1m_compression_breakout_session_liquidity_overlay_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 context: real IBKR `MNQ 202606` `1m 7 D`; provider status, strategy compile, material batch, dispatch, and rank exited `0`; branch fields were preserved. The selected overlay kept the strongest compression-family cost profile: `MNQ/session_liquidity_mid_compression_n45/1m_session_liquidity_overlay` had `8` trades, `87.5%` win rate, raw `+1.00%`, `+0.68%` after `2bps/side`, and `+0.20%` after `5bps/side`; `MNQ/session_liquidity_mid_breakout_n120/1m_session_liquidity_overlay` had `7` trades, raw `+0.77%`, `+0.49%` after `2bps/side`, and `+0.07%` after `5bps/side`.
- Downstream evidence: exact-root replay ran Auto-Quant import/prior, seed analyze/workflow/Pre-Bayes/export, path-ranker train/apply/score, post-ranker analyze/workflow/Pre-Bayes/policy, and final export. Initial `10_register_trainer.exit=1` was repaired by `17_repair_register_catboost.exit=0`, `18_repair_enable_runtime.exit=0`, `19_repair_analyze_after_register.exit=0`, `20_repair_workflow_after_analyze.exit=0`, `21_repair_pre_bayes_after_analyze.exit=0`, `22_repair_policy_after_analyze.exit=0`, and `23_repair_export_target_after_analyze.exit=0`. Repair summary reports `ranker_runtime_status=enabled_registered_artifact_ready`, `runtime_source_kind=registered_artifact`, `runtime_active_match_count=3`, and `ranker_trainer_artifact_status=present_validation_insufficient`.
- Hard blocker: final terminal metrics stay fail-closed: `decision=exact_mnq_1m_compression_breakout_session_liquidity_overlay_downstream_fail_closed`, `pre_bayes_status=pass_neutralized`, `closed_loop_branch_admission.status=fail_closed`, `closed_loop_branch_admission.candidate_status=execution_blocked`, `execution_candidate_actionable=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, `raw_scored_mature=0/30`, `production_validation=0/30`, and `observation_validation=0/30`. The repaired readback improves runtime registration mechanics but still reports `execution_readiness=0.23133329744621625` and no maturity/validation rows, so it remains observation-only.
- Terminal decision: `exact_mnq_1m_compression_breakout_session_liquidity_overlay_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/terminal_metrics.json`, downstream `summaries/ranker_register_repair_summary.md`, downstream `state/IBKR_MNQ1M_COMPRESSION_BREAKOUT_SESSION_LIQUIDITY_OVERLAY_DOWNSTREAM_V1/workflow_snapshot.json`, downstream `execution_tree_trace.json`, and downstream `policy_training/structural_path_ranking_target_summary.json`.

Interpretation:

- The session-liquidity overlay confirms the compression family remains the best fresh IBKR `MNQ/1m` cost survivor, but it does not solve execution. Registration mechanics can be repaired to a CatBoost registered-artifact runtime, yet the branch still fails on the same execution predicate/maturity layer. Do not stack another light session/PDA overlay under this exact root unless the next hypothesis directly changes transition hazard, PDA alignment, or mature feedback rows while preserving the `5bps/side` survivor density.

## Gate 1 Negative Batch: IBKR MNQ Opening-Range Failure Reclaim and MGC Connors RSI2 Rebound

Append-only terminal row at `2026-05-20 00:46:00 +0800` after observing pre-write hash `f224f59ef163b6e2b8e61aa123388d953af1a61527096cc820acbe0fd0fbab0c`.

### MNQ 1m Opening-Range Failure Reclaim 7D: Zero-Trade Negative

- Run root used for terminal readback: `support/docs/experiments/actionable-regime-confidence/runs/20260520T003834+0800-codex-ibkr-mnq1m-opening-range-failure-reclaim-7d-gate1-v1/`. Earlier duplicate attempts under `20260520T003309`, `20260520T003602`, `20260520T003634`, and `20260520T003754` either failed dispatch/rank or reached the same no-survivor verdict; no downstream was run.
- Branch path: `RangeReversion -> OpeningRangeFailure -> ibkr_mnq1m_opening_range_failure_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9269` provider rows; provider status, strategy compile, material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`.
- Cost/density evidence: every exact row had `0` trades: `MNQ/inside_close_quality/1m`, `MNQ/mid_reclaim_balanced/1m`, `MNQ/mid_reclaim_dense/1m`, and `MNQ/vwap_reclaim_dense/1m`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 1m Connors RSI2 Rebound 7D: Dense But Cost-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T003610+0800-codex-ibkr-mgc1m-connors-rsi2-rebound-7d-gate1-v1/`.
- Branch path: `RangeReversion -> ConnorsRsi2Rebound -> ibkr_mgc1m_connors_rsi2_rebound_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Gate 1 evidence: real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows; provider rows were reused from the same-session source provider root with `local_cache_replay=false_source_provider_root_reused`. Strategy compile, material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=214`.
- Cost/density evidence: raw win rates looked attractive but per-trade edge was too small. `MGC/connors_flush/1m` had `14` trades, `71.4286%` win rate, raw `+0.06%`, but `-0.22%` after `1bps/side` and `-0.50%` after `2bps/side`; `MGC/connors_quality/1m` had `33` trades, raw `+0.29%`, `-1.03%` after `2bps/side`; `MGC/connors_balanced/1m` had `61` trades, raw `+0.57%`, `-1.87%` after `2bps/side`; `MGC/connors_dense/1m` had `106` trades and raw `-0.45%`.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The MNQ opening-range failure-reclaim branch does not solve the compression-family execution blocker because it fails before density. The MGC Connors branch proves precious-metals mean-reversion can generate trades, but all apparent raw edge is inside the cost envelope. Keep MNQ compression as the only current fresh IBKR `1m` futures branch with repeated `5bps/side` survival, and treat MGC `1m` reclaim/fade/rebound shapes as negative until a materially larger-distance metals setup clears Gate 1.

## Gate 1 Negative: IBKR MNQ 1m Darvas Volume Breakout 7D

Append-only terminal row at `2026-05-20 00:52:00 +0800` after observing pre-write hash `11cf5db166439b75cc128755ca300b7f93993da63ac0455465bcfed94698c500`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T004259+0800-codex-ibkr-mnq1m-darvas-volume-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_darvas_volume_breakout_7d_gate1_v1`.
- Branch path: `VolatilityExpansion -> DarvasVolumeBreakout -> ibkr_mnq1m_darvas_volume_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family note: this gives the Darvas-style box breakout / volume-expansion family its own exact IBKR futures branch, separate from VWAP/Bollinger/RSI/opening-drive and separate from Donchian/Keltner/SuperTrend/NR7 controls.
- Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D` rows reused from the same fresh session source `/tmp/ict-mnq-1m-7d.csv`, normalized to `9269` rows. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved; `rank_rows=4`; `rank_total_trade_count=7`.
- Cost/density evidence: `MNQ/box18_dense/1m` was the only row reaching the minimum density floor with `6` trades, `50.0%` win rate, raw `-0.25%`, `-0.37%` after `1bps/side`, `-0.49%` after `2bps/side`, and `-0.85%` after `5bps/side`. `MNQ/box30_retest/1m` had only `1` trade and was cost-negative; `MNQ/box24_balanced/1m` and `MNQ/box36_quality/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_DARVAS_VOLUME_BREAKOUT_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_darvas_volume_breakout_7d_gate1_v1.py`.

Interpretation:

- Darvas/volume-box breakout has now been given a real Auto-Quant chance on exact IBKR `MNQ/1m` futures data. It failed before downstream because the density and cost profile were too weak. This supports the user's diversity criticism operationally: keep adding mature, distinct public families as exact rooted branches, but do not rescue failed Gate 1 rows with overlays or promotion shortcuts. The current best fresh IBKR `MNQ/1m` branch remains CompressionBreakout because it is the only one repeatedly surviving `5bps/side`; it still remains observation-only until execution predicates and maturity pass.

## Gate 1 Negative Batch: IBKR M2K 1m Liquidity Sweep and MGC 15m Bollinger/RSI Reclaim

Append-only terminal row at `2026-05-20 01:03:00 +0800` after observing pre-write hash `d1a94386ad1db7bf9ac54d756218e0e7a30037d3644a0193b0141ef2ab9ee67f`.

### M2K 1m Liquidity Sweep VWAP Reclaim 7D: Dense Raw-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T004138+0800-codex-ibkr-m2k1m-liquidity-sweep-vwap-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_liquidity_sweep_vwap_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> LiquiditySweepReclaim -> ibkr_m2k1m_liquidity_sweep_vwap_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Provider/Gate 1 evidence: real IBKR `M2K 202606` `1m 7 D`; fetch exited `0`, `local_cache_replay=false`, and `9392` normalized rows were written under the run root. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=3`; `rank_total_trade_count=153`.
- Cost/density evidence: the sibling-market liquidity-sweep transfer was dense but negative before costs. `M2K/dense/1m` had `63` trades, raw `-0.31%`, `-2.83%` after `2bps/side`; `M2K/quality/1m` had `39` trades, raw `-0.43%`, `-1.99%` after `2bps/side`; `M2K/balanced/1m` had `51` trades, raw `-0.24%`, `-2.28%` after `2bps/side`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 15m Bollinger/RSI Reclaim 1M: Medium-Timeframe Negative

- Run root used for terminal readback: `support/docs/experiments/actionable-regime-confidence/runs/20260520T004159+0800-codex-ibkr-mgc15m-bollinger-rsi-reclaim-exact-gate1-v1/`. Earlier attempt `20260520T004058+0800...` stopped with `backend_busy_no_aq_dispatch` and is superseded by this completed packet.
- Factor id: `ibkr_mgc15m_bollinger_rsi_reclaim_exact_gate1_v1`.
- Branch path: `RangeReversion -> BollingerRsiReclaim -> ibkr_mgc15m_bollinger_rsi_reclaim_exact_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 15m`.
- Provider/Gate 1 evidence: real retained IBKR `MGC 202606` `15m 1 M` rows from the same owned provider packet, `1996` rows, `local_cache_replay=false_reused_self_owned_retained_ibkr_packet`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=1`; `rank_total_trade_count=17`.
- Cost/density evidence: `MGC/bollinger_rsi_reclaim/15m` had `17` trades, `47.0588%` win rate, raw `-1.16%`, `-1.50%` after `1bps/side`, `-1.84%` after `2bps/side`, and `-2.86%` after `5bps/side`. `exact_15m_survivors_2bps=[]`; `exact_15m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The M2K sibling transfer shows the liquidity-sweep template is not robust across nearby equity-index micro futures under the current `1m` rule shape: density transfers, profitability does not. The MGC `15m` Bollinger/RSI test gives precious metals a medium-timeframe mean-reversion chance, but it is raw-negative and cannot rescue the failed `1m` metals families. Keep these as negative boundary samples. The next useful IBKR futures work remains either a same-root execution-predicate repair on the stronger `MNQ/1m CompressionBreakout` cost survivor, or a materially different large-distance `1m` family that clears Gate 1 with margin before downstream.

## Gate 1 Negative: IBKR MNQ 1m Ichimoku Cloud Continuation 7D

Append-only terminal row at `2026-05-20 01:08:00 +0800` after observing pre-write hash `1c7fcab24c09b1acc181b9fdba09ecc38a42e336c30e443561665a31391c14e0`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005445+0800-codex-ibkr-mnq1m-ichimoku-cloud-continuation-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_ichimoku_cloud_continuation_7d_gate1_v1`.
- Branch path: `TrendExpansion -> IchimokuCloudContinuation -> ibkr_mnq1m_ichimoku_cloud_continuation_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family note: this gives the public Ichimoku cloud continuation family a real exact-root futures chance, separate from the repeated VWAP/Bollinger/RSI/opening-drive cluster and separate from Donchian/Keltner/Darvas/SuperTrend/NR7 controls.
- Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D` rows reused from the same fresh session source `/tmp/ict-mnq-1m-7d.csv`, normalized to `9269` rows. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved; `rank_rows=4`; `rank_total_trade_count=94`.
- Cost/density evidence: `MNQ/tk_cross_balanced/1m` had `8` trades, `50.0%` win rate, raw `+0.30%`, and `+0.14%` after `1bps/side`, but flipped to `-0.02%` after `2bps/side` and `-0.50%` after `5bps/side`. `MNQ/pullback_cloud_hold/1m` had `51` trades but raw `-0.13%`; `MNQ/cloud_break_dense/1m` had `25` trades raw `-0.16%`; `MNQ/kumo_quality/1m` had `10` trades raw `-0.88%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_ICHIMOKU_CLOUD_CONTINUATION_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_ichimoku_cloud_continuation_7d_gate1_v1.py`.

Interpretation:

- Ichimoku cloud continuation now has exact IBKR `MNQ/1m` Auto-Quant evidence. It produced real density, but the only raw-positive row died at `2bps/side`, so it is observation/control evidence only. Do not downstream it or rescue it with overlays. The practical search state is unchanged: `MNQ/1m CompressionBreakout` remains the strongest fresh IBKR futures branch because it repeatedly survives `5bps/side`, but it is still not live-usable until the same-root downstream execution predicates and maturity gates improve.

## Gate 1 Negative Batch: IBKR MGC Williams/MFI, Elder/MACD, and Aroon/CCI Diversity

Append-only terminal row at `2026-05-20 01:09:58 +0800` after observing pre-write hash `0f8708b741e7231c7bdfae49756ec3f00dee74e256210dfd0c8ce4404232f6e1`.

### MGC 1m Williams/MFI Reclaim 7D: Dense Raw-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005231+0800-codex-ibkr-mgc1m-williams-mfi-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_williams_mfi_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> WilliamsMfiReclaim -> ibkr_mgc1m_williams_mfi_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Source-backed family note: classic Williams %R and Money Flow Index oscillator reclaim, included to expand beyond VWAP/Bollinger/RSI/opening-drive and beyond pure trend breakout controls.
- Provider/Gate 1 evidence: real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows, `local_cache_replay=false_source_provider_root_reused`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=82`.
- Cost/density evidence: all exact rows were raw-negative and cost-negative. `MGC/willmfi_dense/1m` had `35` trades, `60.0%` win rate, raw `-0.58%`, `-1.98%` after `2bps/side`, and `-4.08%` after `5bps/side`; `MGC/willmfi_balanced/1m` had `19` trades raw `-1.33%`; `MGC/willmfi_flush/1m` had `13` trades raw `-0.81%`; `MGC/willmfi_quality/1m` had `15` trades raw `-1.16%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 5m Elder/MACD Impulse 1M: Raw-Positive But Cost-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005936+0800-codex-ibkr-mgc5m-elder-macd-impulse-gate1-v1/`.
- Factor id: `ibkr_mgc5m_elder_macd_impulse_gate1_v1`.
- Branch path: `TrendExpansion -> ElderMacdImpulse -> ibkr_mgc5m_elder_macd_impulse_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 5m`.
- Source-backed family note: Elder-style impulse / MACD trend filter on a separate exact `5m` precious-metals branch, used as a public-family medium-timeframe sibling rather than as rescue evidence for failed `1m` metals roots.
- Provider/Gate 1 evidence: real IBKR `MGC 202606` `5m 1 M`, `6022` normalized rows, `local_cache_replay=false`; provider status, fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=3`; `rank_total_trade_count=64`.
- Cost/density evidence: the best rows were positive before costs but did not survive `2bps/side`. `MGC/ibkr-mgc5m-elder-macd-impulse-quality-v1/5m` had `17` trades, raw `+0.63%`, `+0.29%` after `1bps/side`, `-0.05%` after `2bps/side`, and `-1.07%` after `5bps/side`; `balanced` had `19` trades raw `+0.36%` but `-0.40%` after `2bps/side`; `dense` had `28` trades raw `-0.16%`. `exact_5m_survivors_2bps=[]`; `exact_5m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

### MGC 1m Aroon/CCI Reclaim 7D: Dense Raw-Negative

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T010448+0800-codex-ibkr-mgc1m-aroon-cci-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_aroon_cci_reclaim_7d_gate1_v1`.
- Branch path: `TrendExpansion -> AroonCciReclaim -> ibkr_mgc1m_aroon_cci_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Source-backed family note: classic Aroon plus CCI trend/reclaim family, giving another mature oscillator/trend hybrid a real exact-root futures Gate 1 packet.
- Provider/Gate 1 evidence: real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows, `local_cache_replay=false_source_provider_root_reused`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=62`.
- Cost/density evidence: all exact rows were raw-negative. `MGC/aroon_cci_quality/1m` had `19` trades, `68.4211%` win rate, raw `-0.64%`, `-1.40%` after `2bps/side`, and `-2.54%` after `5bps/side`; `MGC/aroon_cci_dense/1m` had `12` trades raw `-0.26%`; `MGC/aroon_cci_breakout/1m` had `9` trades raw `-0.54%`; `MGC/aroon_cci_balanced/1m` had `22` trades raw `-0.99%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- These three packets broaden the futures/precious-metals search across Williams/MFI, Elder/MACD impulse, and Aroon/CCI instead of cycling only VWAP/Bollinger/RSI/opening-drive variants. They are useful diversity and negative-control evidence, but they do not satisfy the real-cost density gate. The `MGC/5m` Elder row is the closest metals public-family sibling because it was raw-positive, but its edge vanished by `2bps/side`, so it cannot move downstream. Keep the standard hard gates unchanged: no Pre-Bayes/BBN/CatBoost/execution-tree replay, no promotion, no `update_goal`.

## Gate 1 Negative: IBKR MNQ 1m CMF/OBV Accumulation Breakout 7D

Append-only terminal row at `2026-05-20 01:15:00 +0800` after observing pre-write hash `57990856cbd2a4d13103bda6c2c9f1388fd7cc15f72169f1885d9ccd50b40714`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T011218+0800-codex-ibkr-mnq1m-cmf-obv-accumulation-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_cmf_obv_accumulation_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> CmfObvAccumulationBreakout -> ibkr_mnq1m_cmf_obv_accumulation_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Source-backed family note: CMF/OBV accumulation breakout now has its own exact IBKR `MNQ/1m` futures branch, separate from compression, VWAP/Bollinger/RSI/opening-drive, Donchian/Keltner/Darvas/SuperTrend/NR7, and Ichimoku controls.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D` rows reused from the same fresh session source `/tmp/ict-mnq-1m-7d.csv`, normalized to `9269` rows. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved; `rank_rows=4`; `rank_total_trade_count=117`.
- Cost/density evidence: the branch produced many trades but the edge was inside the cost envelope. `MNQ/accum_dense_n36/1m` had `44` trades, `56.8182%` win rate, raw `+0.51%`, `-0.37%` after `1bps/side`, `-1.25%` after `2bps/side`, and `-3.89%` after `5bps/side`. `MNQ/accum_balanced_n48/1m` had `30` trades raw `+0.03%`; `MNQ/vwap_accum_n42/1m` had `32` trades raw `+0.28%`; `MNQ/obv_quality_n60/1m` had `11` trades raw `+0.14%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `state/auto-quant/IBKR_MNQ1M_CMF_OBV_ACCUMULATION_BREAKOUT_7D_GATE1_V1/auto_quant_agent_material_rank.*.json`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_cmf_obv_accumulation_breakout_7d_gate1_v1.py`.

Interpretation:

- CMF/OBV accumulation breakout is now represented in the exact IBKR futures evidence set. It transferred density but not practical edge: every row flipped negative by `1bps/side`, so the branch stops at Gate 1 and should not be rescued with overlays. The current strongest fresh IBKR `MNQ/1m` branch remains CompressionBreakout because it repeatedly survived `5bps/side`, but it is still observation-only until same-root execution predicates and maturity improve.

## Gate 1 Survivor, Downstream Incomplete Fail-Closed: IBKR M2K 1m Liquidity Sweep Reject Short 7D

Append-only terminal row at `2026-05-20 01:21:00 +0800` after observing pre-write hash `66bee18bcbcd00194fbe59868219fd9f97cd3905fb60528bcd4d9030452d4b83`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/`.
- Latest downstream root inspected: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/downstream-exact-m2k-1m-liquidity-sweep-reject-short-20260520T011359+0800/`.
- Earlier downstream root inspected: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/downstream-exact-m2k-1m-liquidity-sweep-reject-short-20260520T005812+0800/`.
- Factor id: `ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Provider/Gate 1 evidence: real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from the same-session source provider root; provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=3`; `rank_total_trade_count=162`.
- Cost/density evidence: `M2K/quality/1m_short` is a real exact-root survivor with `32` trades, `56.25%` win rate, raw `+3.33%`, `+2.69%` after `1bps/side`, `+2.05%` after `2bps/side`, and `+0.13%` after `5bps/side`. `M2K/balanced/1m_short` also survived `2bps/side` with `54` trades, raw `+2.43%`, and `+0.27%` after `2bps/side`, but failed `5bps/side`; `M2K/dense/1m_short` failed `2bps/side`. `exact_1m_survivors_2bps=[M2K/quality/1m_short, M2K/balanced/1m_short]`; `exact_1m_survivors_5bps=[M2K/quality/1m_short]`.
- Downstream evidence: the latest exact-root downstream attempt imported Auto-Quant results and initialized the prior (`01_auto_quant_results_import.exit=0`, `02_auto_quant_prior_init.exit=0`), but `03_analyze_seed.exit=-15`; workflow, Pre-Bayes, and export seed readbacks exited `0` afterward. Terminal metrics report `all_commands_ok=false`, `failure_reason=03_analyze_seed_exit_-15`, `decision=exact_m2k_1m_liquidity_sweep_reject_short_downstream_incomplete_fail_closed`, `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`, and `execution_tree_allowed=false`.
- Earlier downstream evidence: the earlier exact-root downstream attempt reached path-ranker mechanics through `07_train_ranker.exit=0`, `08_apply_ranker.exit=0`, `09_apply_scores_to_ict.exit=0`, `10_register_trainer.exit=0`, and `11_enable_runtime.exit=0`, but `03_analyze_seed.exit=124`; it did not produce a complete terminal metrics packet proving execution readiness.
- Terminal decision: `gate1_survivor_downstream_incomplete_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, latest downstream `checks/terminal_metrics.json`, latest downstream `summaries/terminal_decision_summary.md`, and downstream `checks/*.exit` files in both inspected downstream roots.

Interpretation:

- This is a stronger and more interesting branch than most recent diversity negatives: a distinct M2K `1m` short-side liquidity-sweep rejection shape survived `5bps/side` with real density. It earns preservation as a scoped downstream candidate, but not promotion. The exact-root downstream chain is still incomplete/fail-closed because `analyze_seed` terminated (`-15` in the latest attempt, `124` timeout in the earlier attempt) before a complete AQ -> Pre-Bayes/BBN/CatBoost/execution-tree direction-consistency and execution-readiness proof existed. The next useful work is to repair/replay this exact root's downstream admission path, not to lower gates or call the Gate 1 survivor live-usable.

## Downstream Correction Addendum: IBKR M2K 1m Liquidity Sweep Reject Short 7D

Append-only correction at `2026-05-20 01:25:23 +0800` after observing pre-write hash `28f0be1251f5f7a23c1a638bb65de51791c30a3359234d0ee47dbebac6f46f8a`.

- Corrected downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/downstream-exact-m2k-1m-liquidity-sweep-reject-short-20260520T005812+0800/`.
- Branch path remains: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Gate 1 survivor remains: `M2K/quality/1m_short`, `32` trades, raw `+3.33%`, `+2.05%` after `2bps/side`, and `+0.13%` after `5bps/side`.
- Corrected downstream status: the earlier downstream root tolerated `03_analyze_seed.exit=124` and completed manual readbacks plus ranker mechanics. `04_workflow_seed.exit=0`, `05_pre_bayes_seed.exit=0`, `06_export_target_seed.exit=0`, `07_train_ranker.exit=0`, `08_apply_ranker.exit=0`, `09_apply_scores_to_ict.exit=0`, `10_register_trainer.exit=0`, `11_enable_runtime.exit=0`, and manual `13_workflow_after_ranker`, `14_pre_bayes_after_ranker`, `15_policy_after_ranker`, and `16_export_target_final` all exited `0`.
- Corrected gate booleans from that terminal metrics packet: `pre_bayes_allowed=true`, `bbn_allowed=true`, `catboost_allowed=true`, `execution_tree_allowed=false`, `promotion_allowed=false`, `trade_usable=false`, `update_goal=false`.
- Remaining hard blocker: `closed_loop_branch_admission.status=fail_closed`, `candidate_status=observe`, `execution_gate_status=observe`, `exact_branch_survived=false`, `execution_candidate_exists=false`, `execution_tree_trace_exists=false`, `execution_readiness=null`, `transition_hazard=null`, and `pda_hybrid_alignment=null`. Ranker runtime is enabled and ready (`ranker_runtime_status=enabled_registered_model_ready`, `ranker_runtime_ready=true`, `ranker_active_match_count=1`), but validation is short: `mature_rows=1`, `raw_scored_mature=1/30`, `production_validation=0/30`, `observation_validation=0/30`, and `ranker_validation_ready=false`.
- Corrected terminal decision: `exact_m2k_1m_liquidity_sweep_reject_short_downstream_fail_closed_validation_shortfall`; failure reason is `gate1_cost_survived_but_no_exact_execution_candidate_and_ranker_validation_shortfall`.
- Evidence files: corrected downstream `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `state/IBKR_M2K1M_LIQUIDITY_SWEEP_REJECT_SHORT_DOWNSTREAM_V1/workflow_snapshot.json`, and `policy_training/structural_path_ranking_target_summary.json`.

Interpretation:

- This correction matters because the branch is not a plain downstream bootstrap failure. AQ, Pre-Bayes, BBN-facing state, and CatBoost/path-ranker mechanics can admit the exact-root packet, but the branch still cannot pass execution-tree promotion: no exact execution candidate/tree trace exists, execution readiness is unavailable, and validation maturity is only `1/30`. The next repair should target exact execution-candidate materialization and validation-row maturation for this same rooted branch; do not restart broad factor hunting or lower the execution gates for this packet.

## Manual Readback Addendum: IBKR M2K 1m Liquidity Sweep Reject Short 7D

Append-only readback at `2026-05-20 01:31:00 +0800` after observing pre-write hash `69eab45c7e8a9c4579525fe4b5e204e18eaac3bf3ca216d6a7b8937b93f83280`.

- Downstream root read back: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/downstream-exact-m2k-1m-liquidity-sweep-reject-short-20260520T011359+0800/`.
- Manual readback commands on the same state dir all exited `0`: `manual_17_workflow_readback`, `manual_18_pre_bayes_readback`, `manual_19_policy_readback`, and `manual_20_export_target_readback`.
- Readback verdict: `closed_loop_branch_admission.status=fail_closed`, `candidate_status=execution_candidate_observed`, `execution_gate_status=execution_candidate_observed`, `actionable=false`, and reason `exact_structural_branch_visible_but_not_ready_or_actionable`.
- Execution materialization remains absent: `execution_candidate.json` is missing; `execution_tree_trace.json` exists but has no `execution_readiness`, `hybrid_transition_hazard`, or `pda_hybrid_alignment` fields to test against the hard promotion gate.
- Ranker/validation readback remains immature: ranker runtime is `disabled` with `active_match_count=0`; target summary has `rows=2`, `candidate_set_size=1`, `mature_rows=1`, `history_mature_rows=1`, `rows_with_execution_gate_status=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, and `observation_validation=0/30`.
- Terminal decision remains unchanged: `gate1_survivor_downstream_admission_blocked`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The latest same-root readback confirms the useful part and the blocker separately. `M2K/quality/1m_short` remains a real Gate 1 cost/density survivor, but the downstream state has not materialized an exact executable candidate and has too little mature validation. The next aligned repair is exact execution-candidate/tree materialization plus validation-row maturation for this rooted branch, not another wrapper rerun or a lowered execution gate.

## Gate 1 Survivor, Downstream Incomplete Fail-Closed: IBKR MNQ 1m Compression Breakout VWAP Persistence Transition Guard 7D

Append-only terminal row at `2026-05-20 01:32:00 +0800` after observing the current document tail and avoiding duplicate lock-table updates.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T012135+0800-codex-ibkr-mnq1m-compression-breakout-vwap-persistence-transition-guard-7d-gate1-v1/`.
- Downstream roots inspected and terminalized: `downstream-exact-mnq-1m-compression-breakout-vwap-persistence-transition-guard-20260520T012441+0800/` and `downstream-exact-mnq-1m-compression-breakout-vwap-persistence-transition-guard-20260520T012457+0800/`.
- Branch path: `TrendExpansion -> CompressionBreakout -> ibkr_mnq1m_compression_breakout_7d_gate1_v1 -> ibkr_mnq1m_compression_breakout_vwap_persistence_transition_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 evidence: exact-root overlay on real IBKR `MNQ 202606` `1m 7 D`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_total_trade_count=32`; `downstream_allowed=true`; branch fields were preserved.
- Best cost-stressed row: `MNQ/vwap_persist_mid_breakout_n120/1m_vwap_persistence_transition_guard` had `6` trades, `83.3333%` win rate, raw `+0.65%`, `+0.53%` after `1bps/side`, `+0.41%` after `2bps/side`, and `+0.05%` after `5bps/side`. This is a real `5bps/side` survivor, but density is sparse against the desired `1-3` trades/day target.
- Downstream evidence: the new exact-root downstream wrapper `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_compression_breakout_vwap_persistence_transition_guard_7d_downstream_v1.py` was syntax-checked and run. Duplicate wrapper/analyze children were terminated after post-ranker analyze respawn/hang behavior. Manual terminal metrics were written for both downstream roots.
- Downstream blocker: both downstream roots reached seed/ranker mechanics through `11_enable_runtime.exit=0`, but `03_analyze_seed.exit=-15`, `10_register_trainer.exit=1`, no final execution candidate was available, target summary had `mature_rows=0`, `history_mature_rows=0`, `execution_gate_rows=0`, and `training_weight_rows=0`. Terminal metrics classify both as `exact_mnq_1m_compression_breakout_vwap_persistence_transition_guard_downstream_incomplete_fail_closed`.
- Terminal decision: `gate1_survivor_downstream_incomplete_fail_closed`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This overlay is useful evidence because it directly targets the previous CompressionBreakout execution predicate blocker while preserving the rooted path and surviving `5bps/side`. It still cannot be promoted: the trade density is sparse, downstream ranker registration failed, no mature validation rows exist, and the execution predicates `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and `execution_readiness >= 0.65` were not proven. Next aligned work is either a density-preserving same-root execution repair or a fresh exact-root branch with real 5bps cost survival plus stronger downstream materialization.

## Feedback Replay Addendum: IBKR M2K 1m Liquidity Sweep Reject Short 30-Observation Probe

Append-only readback at `2026-05-20 01:44:58 +0800` after observing pre-write hash `7a6ffd12b287070dfc0588cee4ea153b82f5b25d91e7d6ad5368b0c12768bbf6`.

- Feedback replay root: `/tmp/ict-engine-m2k-reject-short-feedback-probe/replay-count30/`.
- Prior downstream state cloned from: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/downstream-exact-m2k-1m-liquidity-sweep-reject-short-20260520T005812+0800/state`.
- Branch path remains exact-rooted: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Gate 1 survivor remains unchanged from the source packet: `M2K/quality/1m_short`, `32` trades, raw `+3.33%`, `+2.05%` after `2bps/side`, and `+0.13%` after `5bps/side`.
- Replay command completed successfully with `count=30`, `lookback=52`, `horizon=16`, and `threshold=0.001`; `replay_summary.json` reports `ok=true`, `observations=30`, and `final_mature_rows=3`.
- Materialization repair result: unlike the corrected downstream packet, this replay state now contains both `execution_candidate.json` and `execution_tree_trace.json` under `/tmp/ict-engine-m2k-reject-short-feedback-probe/replay-count30/state/IBKR_M2K1M_LIQUIDITY_SWEEP_REJECT_SHORT_DOWNSTREAM_V1/`.
- Ranker/execution-tree validation improved materially in the execution-tree lineage: `ranker_validation_ready=true`, `raw_scored_mature=898/30`, `production_validation=898/30`, `observation_validation=29/30`, `runtime_selection=enabled_registered_model_ready`, `runtime_source=registered_model_artifact`, `runtime_matches=3`, and `execution_gate_status=pass` for the exact branch path. The exported current target summary still shows a small current candidate set (`rows=5`, `candidate_set_size=3`, `mature_rows=3`) while history is large (`history_rows=1094`, `history_mature_rows=1019`), so future reports must distinguish current target rows from history-backed runtime validation.
- Hard execution gates still fail: latest execution tree reports `execution_readiness=0.47786931640380137`, `hybrid_transition_hazard=0.6766334400898604`, `pda_hybrid_alignment=true`, `gate_status=observe`, `branch=transition_guardrail`, `execution_bias=guarded`, and `closed_loop_branch_admission.status=fail_closed` with `candidate_status=execution_observe_only`.
- Latest execution candidate remains non-actionable: `actionable=false`, `candidate_status=no_trade`, `trade_direction=Bear`, `pre_bayes_gating_status=pass_neutralized`, `pre_bayes_quality_score=0.7042730798151831`, `factor_alignment=bearish`, and `multi_timeframe_summary` only covers `15m` while `1m,5m,30m,1h,4h,1d` are missing from that replay window.
- Terminal decision: `same_root_feedback_replay_materialized_execution_tree_but_still_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This probe makes real progress on the earlier M2K blocker: exact execution candidate/tree materialization is possible after same-root feedback replay, and the ranker can become history-validation ready. It still does not meet the user's live-practical gates. The remaining blockers are no longer simply missing candidate/tree files; they are high transition hazard (`0.6766 > 0.60`), insufficient execution readiness (`0.4779 < 0.65`), observe-only execution admission, and incomplete multi-timeframe replay context. The next aligned M2K repair should target a transition-duration / PDA-consistency / multi-timeframe-context overlay that preserves the `5bps/side` Gate 1 survivor, not more validation-row replay alone and not a lowered gate.

## Execution Materialized But Predicate-Failed: IBKR M2K 1m Liquidity Sweep Reject Short Simulated Trade Admission

Append-only readback at `2026-05-20 01:44:00 +0800` after observing pre-write hash `7a6ffd12b287070dfc0588cee4ea153b82f5b25d91e7d6ad5368b0c12768bbf6`.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/`.
- Simulated admission root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-20260520T013200+0800/`.
- Branch path remains: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Gate 1 survivor remains real-provider and cost-stressed: `M2K/quality/1m_short`, `32` trades, raw `+3.33%`, `+2.05%` after `2bps/side`, and `+0.13%` after `5bps/side`.
- Admission repair evidence: the simulated-trade admission wrapper progressed further than prior M2K downstream attempts. `01_auto_quant_results_import`, `02_auto_quant_prior_init`, `03_analyze_seed`, `04_workflow_seed`, `05_pre_bayes_seed`, `06_export_target_seed`, `07_policy_seed`, `08_ingest_simulated_trades`, `09_export_target_after_simulated_trades`, `10_policy_after_simulated_trades`, `11_train_ranker`, `12_apply_ranker`, `13_apply_scores_to_ict`, and `15_enable_runtime` all exited `0`; `14_register_trainer.exit=1`.
- Materialization improvement: this root produced both `state/IBKR_M2K1M_LIQUIDITY_SWEEP_REJECT_SHORT_SIM_TRADE_ADMISSION_V1/execution_candidate.json` and `execution_tree_trace.json`, unlike the latest manual readback root where the execution candidate was absent.
- Validation improvement: `policy_training/structural_path_ranking_target_summary.json` reports `rows=5`, `candidate_set_size=3`, `mature_rows=3`, `rows_with_execution_gate_status=3`, `rows_with_training_weight=3`, `history_rows=42`, and `history_mature_rows=35`. This is an improvement over the prior `1/30` style shortfall but still not a promotion proof because the execution tree did not use a ready path-ranker runtime and the feedback export had `matched_rows=0` with warnings `auto_quant_real_trade_feedback_rows_missing`, `matched_rows_below_minimum: 0`, and `outcome_labels_do_not_cover_win_loss`.
- Hard execution predicates failed: `execution_tree_trace.json` reports `execution_readiness=0.3180972047048167`, `hybrid_transition_hazard=0.9184975817511946`, `pda_hybrid_alignment=false`, `gate_status=observe`, `closed_loop_branch_admission.status=fail_closed`, and `reason=exact_structural_branch_visible_but_not_ready_or_actionable`. The execution tree lineage also reports `path_ranker_score_visible_to_execution_tree=false`, `path_ranker_score_used_by_execution_tree=false`, and `ranker_validation_ready=false`.
- Pre-Bayes/BBN direction is still not clean: `execution_candidate.json` reports `pre_bayes_gating_status=observe_only`, `evidence_quality_score=0.37919332747322515`, `pass_to_bbn=false`, and conflict flags `multi_timeframe_direction_conflict`, `pda_sequence_cluster_weak`, and `pda_sequence_low_consistency`.
- Terminal decision: `execution_candidate_materialized_but_execution_predicates_fail_closed`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is real progress on the nearest M2K repair: the exact rooted branch now reaches an execution candidate and execution tree under a simulated-admission harness. It still fails the user's hard gates by a wide margin: readiness is about `0.318` instead of `>=0.65`, transition hazard is about `0.918` instead of `<0.60`, PDA/hybrid alignment is false, and Pre-Bayes remains observe-only. The next useful same-root repair is not more broad wrapper reruns; it is reducing transition hazard and improving PDA/hybrid consistency while preserving the original `5bps/side` cost survivor and using real matched feedback rows rather than unmatched simulated feedback.

## Gate 1 Negative: IBKR M2K 1m VWAP Noise-Band Reject Short 7D

Append-only terminal row at `2026-05-20 01:44:00 +0800` after observing pre-write hash `7a6ffd12b287070dfc0588cee4ea153b82f5b25d91e7d6ad5368b0c12768bbf6`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T013132+0800-codex-ibkr-m2k1m-vwap-noise-band-reject-short-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_vwap_noise_band_reject_short_7d_gate1_v1`.
- Branch path: `RangeReversion -> VwapNoiseBandRejectShort -> ibkr_m2k1m_vwap_noise_band_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Provider/Gate 1 evidence: real retained IBKR same-contract replay from the M2K source provider root, `M2K 202606` `1m 7 D`, `9392` normalized rows; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=35`.
- Cost/density evidence: the best practical rows were only `2bps/side` survivors and failed `5bps/side`. `M2K/late_trim/1m_short` had `9` trades, raw `+0.85%`, `+0.49%` after `2bps/side`, and `-0.05%` after `5bps/side`; `M2K/balanced/1m_short` had `9` trades, raw `+0.47%`, `+0.11%` after `2bps/side`, and `-0.43%` after `5bps/side`. `M2K/quality/1m_short` had only `2` trades and therefore did not satisfy density despite positive cost stress. `exact_1m_survivors_2bps=[M2K/late_trim/1m_short, M2K/balanced/1m_short]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical_2bps_only_no_5bps_survivor`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Note: a downstream wrapper for this branch was observed running after Gate 1, but this row classifies from the stable Gate 1 packet. Because no exact `5bps/side` survivor exists, downstream execution should not be used to promote or rescue this branch.

Interpretation:

- VWAP/noise-band reject-short is a useful M2K negative control under the same retained real IBKR contract, but it does not beat the original `LiquiditySweepRejectShort` branch. The edge is too close to friction and too sparse after the hard `5bps/side` screen. Preserve it as observation evidence and keep the repair focus on the original M2K liquidity-sweep survivor or a materially different `1m` futures factor with stronger cost/density.

## Downstream Addendum: IBKR M2K 1m VWAP Noise-Band Reject Short 7D

Append-only readback at `2026-05-20 01:46:10 +0800` after observing pre-write hash `9d99ac88f172fdfb63ef89abdae1202ce1b42f80e0069e92a72acdd16d5f4f36`.

- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T013132+0800-codex-ibkr-m2k1m-vwap-noise-band-reject-short-7d-gate1-v1/downstream-exact-m2k-1m-vwap-noise-band-reject-short-20260520T013520+0800/`.
- Branch path remains: `RangeReversion -> VwapNoiseBandRejectShort -> ibkr_m2k1m_vwap_noise_band_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Mechanical downstream evidence: `01_auto_quant_results_import`, `02_auto_quant_prior_init`, `03_analyze_seed`, `04_workflow_seed`, `05_pre_bayes_seed`, `06_export_target_seed`, `07_train_ranker`, `08_apply_ranker`, `09_apply_scores_to_ict`, `10_register_trainer`, and `11_enable_runtime` all exited `0`.
- Execution materialized but failed closed: `execution_candidate.json` and `execution_tree_trace.json` exist, but the tree reports `execution_readiness=0.3180972047048167`, `hybrid_transition_hazard=0.9184975817511946`, `pda_hybrid_alignment=false`, `gate_status=observe`, `closed_loop_branch_admission.status=fail_closed`, and `reason=exact_structural_branch_visible_but_not_ready_or_actionable`.
- Ranker/validation did not mature: `policy_training/structural_path_ranking_target_summary.json` reports `rows=4`, `candidate_set_size=3`, `mature_rows=0`, `history_mature_rows=0`, `rows_with_execution_gate_status=0`, and `rows_with_training_weight=0`; execution lineage reports `path_ranker_score_visible_to_execution_tree=false`, `path_ranker_score_used_by_execution_tree=false`, and `ranker_validation_ready=false`.
- Gate 1 blocker remains authoritative: the source Gate 1 packet had no exact `5bps/side` survivor, so this downstream addendum is observation-only mechanics evidence, not promotion evidence.
- Terminal decision remains: `downstream_mechanics_ok_but_gate1_and_execution_predicates_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The downstream chain can mechanically replay this branch, but it confirms why the branch should not consume more repair budget now: it failed the first cost/density hard gate, then also failed execution readiness, transition hazard, PDA alignment, and validation maturity. This makes it a negative-control sibling for the stronger `LiquiditySweepRejectShort` M2K root, not a candidate to promote or rescue.

## Refresh Correction: IBKR M2K 1m Liquidity Sweep Reject Short Simulated Trade Admission

Append-only correction at `2026-05-20 01:51:07 +0800` after observing pre-write hash `3247fbab0e139f17414057f92e176c1fab3a7ecab6cee381d7ee4da735bdffa9`.

- Corrected root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-20260520T013200+0800/`.
- The final refreshed `execution_tree_trace.json` mtime was `2026-05-20 01:49:42 +0800`, after the earlier readback. This refresh improves ranker visibility but not execution readiness.
- Corrected ranker readback: `path_ranker_score_visible_to_execution_tree=true`, `path_ranker_score_used_by_execution_tree=false`, `ranker_validation_ready=true`, `path_ranker_runtime_source=history_path`, `raw_scored_mature=35/30`, `production_validation=35/30`, `observation_validation=32/30`, and exact-branch `execution_gate_status=observe`.
- Corrected target summary remains small in the current candidate set: `rows=5`, `candidate_set_size=3`, `mature_rows=3`, `history_rows=42`, `history_mature_rows=35`, `rows_with_execution_gate_status=3`, and `rows_with_training_weight=3`.
- Hard execution predicates are unchanged and still fail: `execution_readiness=0.3180972047048167`, `hybrid_transition_hazard=0.9184975817511946`, `pda_hybrid_alignment=false`, `gate_status=observe`, `closed_loop_branch_admission.status=fail_closed`, and `candidate_status=execution_observe_only`.
- Corrected terminal decision remains: `ranker_validation_ready_but_execution_predicates_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This correction narrows the M2K blocker. The branch no longer fails because the ranker cannot validate; it fails because the execution tree refuses to use the score under a weak execution snapshot, high transition hazard, and PDA/hybrid disagreement. The next same-root overlay should target `transition_hazard` and PDA/hybrid consistency directly while keeping the original `5bps/side` cost survivor alive.

## Public-Family Diversity Gate 1: IBKR MYM 1m TOD Session Momentum 7D

Append-only terminal row at `2026-05-20 01:46:40 +0800` after observing pre-write hash `762230e5cf32196f536437596339617b12d761a45033b7e0a1e42309a0299b5b`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014537+0800-codex-ibkr-mym1m-tod-session-momentum-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_tod_session_momentum_7d_gate1_v1`.
- Branch path: `IntradaySeasonality -> TodSessionMomentum -> ibkr_mym1m_tod_session_momentum_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Diversity note: this was a deliberate public/time-tested family rotation into intraday seasonality / time-of-day session momentum, materially different from VWAP/RSI/RVOL/microtrend variants.
- Provider/Gate 1 evidence: real IBKR `MYM 202606` `1m 7 D`, `9456` normalized rows, `local_cache_replay=false`. Provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=3`; `rank_total_trade_count=68`.
- Cost/density evidence: all exact `1m` rows had real trade density but failed practical cost stress. `MYM/quality/1m` had `21` trades, `66.6667%` win rate, raw `+0.30%`, but `-0.12%` after `1bps/side`, `-0.54%` after `2bps/side`, and `-1.80%` after `5bps/side`. `MYM/balanced/1m` had `22` trades, raw `+0.13%`, `-0.75%` after `2bps/side`; `MYM/dense/1m` had `25` trades, raw `+0.09%`, `-0.91%` after `2bps/side`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_tod_session_momentum_7d_gate1_v1.py`.

Interpretation:

- The time-of-day/session-momentum family deserved a real Gate 1 attempt and got one on a fresh IBKR micro-index branch. It transferred trade density but not a practical edge: the best raw row flipped negative at only `1bps/side`, so the branch stops at Gate 1. Preserve it as diversity evidence and rotate to another materially different public family instead of rescuing it with near-equivalent overlays.

## Gate 1 Negative: IBKR MNQ 1m Volatility/Noise-Band Breakout 7D

Append-only terminal row at `2026-05-20 01:50:52 +0800` after observing pre-write hash `9583887f0879e48c555c2cdb0838ff30c20f898757df6c671592c855ec330230`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T013152+0800-codex-ibkr-mnq1m-volatility-noise-band-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_volatility_noise_band_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> VolatilityNoiseBandBreakout -> ibkr_mnq1m_volatility_noise_band_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity note: this is a public volatility/noise-band breakout family, materially different from VWAP/RSI/RVOL/microtrend, and also distinct from the already-tested Donchian/Turtle, Darvas, Ichimoku, CMF/OBV, Connors, Elder/MACD, Williams/MFI, Aroon/CCI, and TOD/session momentum packets.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9269` normalized rows, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=7`.
- Cost/density evidence: all exact rows were sparse and raw-negative. `MNQ/dense_n48/1m` had `4` trades, `25.0%` win rate, raw `-0.07%`, `-0.23%` after `2bps/side`, and `-0.47%` after `5bps/side`; `MNQ/balanced_n72/1m`, `MNQ/quality_n96/1m`, and `MNQ/vwap_persistence_n60/1m` each had `1` trade and raw `-0.04%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_volatility_noise_band_breakout_7d_gate1_v1.py`.

Interpretation:

- This gives the volatility/noise-band breakout family a fair exact-root IBKR futures Gate 1 chance. It failed before downstream because it was both sparse and negative after costs. Preserve it as a useful public-family negative/control sample, then rotate to a different family or repair an existing `5bps/side` survivor's execution predicates rather than stacking overlays on this failed branch.

## Gate 1 Negative: IBKR MGC 1m Keltner/ATR Channel Breakout 7D

Append-only terminal row at `2026-05-20 01:47:00 +0800` after observing pre-write hash `9583887f0879e48c555c2cdb0838ff30c20f898757df6c671592c855ec330230`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T011947+0800-codex-ibkr-mgc1m-keltner-atr-channel-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_keltner_atr_channel_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> KeltnerAtrChannelBreakout -> ibkr_mgc1m_keltner_atr_channel_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Source-backed family note: this gives precious-metals micro futures a distinct Keltner/ATR channel-breakout branch, separate from MGC opening VWAP/RVOL, RSI/VWAP washout, Donchian/Turtle, ADX/ATR, SuperTrend/ATR, Connors RSI2, Williams/MFI, Aroon/CCI, Elder/MACD, and Bollinger/RSI controls.
- Provider/Gate 1 evidence: real retained IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows, `local_cache_replay=false_source_provider_root_reused`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=126`.
- Cost/density evidence: the branch transferred density but not a cost-surviving edge. `MGC/dense_upper20/1m` had `38` trades, raw `+0.20%`, but `-0.56%` after `1bps/side`, `-1.32%` after `2bps/side`, and `-3.60%` after `5bps/side`; `MGC/balanced_upper20/1m` had `29` trades, raw `+0.13%`, `-0.45%` after `1bps/side`, and `-1.03%` after `2bps/side`; `MGC/mid_reclaim/1m` and `MGC/trend_upper20/1m` were raw-negative. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_keltner_atr_channel_breakout_7d_gate1_v1.py`.

Interpretation:

- This is a useful precious-metals negative-control packet: real IBKR MGC `1m` data and AQ mechanics are healthy, but the Keltner/ATR edge is entirely inside the friction envelope. Do not downstream or rescue this exact branch with overlays. The current IBKR futures search state remains: MGC `1m` public families are broad but cost-negative, while the best live-profit repair candidates are still the M2K `1m` liquidity-sweep reject-short branch and MNQ `1m` compression family, both observation-only until execution predicates and validation improve.

## Public-Family Diversity Gate 1: IBKR MYM 1m Opening-Range Failure Reclaim 7D

Append-only terminal row at `2026-05-20 01:53:03 +0800` after observing pre-write hash `96271ec0ca3037c02afd68be18e532a70aa2ffab8fa151dd8ccc74d20f1ecf03`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T015128+0800-codex-ibkr-mym1m-opening-range-failure-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_opening_range_failure_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> OpeningRangeFailureReclaim -> ibkr_mym1m_opening_range_failure_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Diversity note: this was a deliberate rotation into the opening-range failure/reclaim family on a fresh Micro Dow `1m` futures branch, distinct from the preceding MYM time-of-day/session-momentum attempt and from VWAP/RSI/RVOL/microtrend variants.
- Provider/Gate 1 evidence: real IBKR `MYM 202606` `1m 7 D`, `9462` normalized rows, `local_cache_replay=false`. Provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=3`; `rank_total_trade_count=0`.
- Cost/density evidence: `MYM/dense/1m`, `MYM/balanced/1m`, and `MYM/quality/1m` all completed but produced `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_opening_range_failure_reclaim_7d_gate1_v1.py`.

Interpretation:

- The opening-range failure/reclaim family got a real exact-root IBKR/AQ attempt and failed on density, not provider mechanics. This should not be rescued by loosening the gate or stacking overlays, because the first profit-factor branch did not earn evidence. Preserve it as a clean public-family negative and rotate to a materially different family or market cell.

## Public-Family Diversity Gate 1: IBKR MNQ 1m Fisher/Stoch Reclaim 7D

Append-only terminal row at `2026-05-20 02:00:55 +0800` after observing pre-write hash `5ab817400c4fa027dbb7edcbb7e06293add76426d9b1dfb8085952bfe5519fa5`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T015804+0800-codex-ibkr-mnq1m-fisher-stoch-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_fisher_stoch_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> FisherStochReclaim -> ibkr_mnq1m_fisher_stoch_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity note: this gives the Fisher transform / stochastic reclaim oscillator family a real exact-root `MNQ/1m` futures branch, distinct from VWAP/RSI/RVOL, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend/ADX, NR7/Crabel, Ichimoku, CMF/OBV, and time-of-day/session-momentum controls.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9269` normalized rows from the same-session fresh IBKR source `/tmp/ict-mnq-1m-7d.csv`, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=12`.
- Cost/density evidence: the branch produced a few positive raw rows but did not satisfy density plus cost survival. `MNQ/late_reclaim/1m` had `4` trades, `75.0%` win rate, raw `+0.18%`, `+0.02%` after `2bps/side`, and `-0.22%` after `5bps/side`, but failed density. `MNQ/dense/1m` had `5` trades, raw `+0.14%`, `-0.06%` after `2bps/side`, and `-0.36%` after `5bps/side`. `MNQ/quality/1m` had `1` trade and `MNQ/balanced/1m` had `2` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSV, and source runner `/tmp/run_ibkr_mnq1m_fisher_stoch_reclaim_7d_gate1_v1.py`.

Interpretation:

- Fisher/Stoch reclaim broadened the public-family search into oscillator/reclaim territory and did produce some raw-positive entries, but it failed both practical density and the hard `5bps/side` cost screen. Treat it as observation-only diversity evidence. The useful next work remains either a materially different large-distance family or same-root execution-predicate repair on a branch that already survived `5bps/side`, especially M2K liquidity-sweep reject-short.

## Gate 1 Survivor At 2bps Only, Downstream Fail-Closed: IBKR SI 1m Volatility Expansion Reclaim 7D

Append-only terminal row at `2026-05-20 01:56:10 +0800` after observing pre-write hash `f60bd3a913a39d57a0f19906145f525ab0519ed9d53a48cb684ae5250115a996`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014244+0800-codex-ibkr-si1m-volatility-expansion-reclaim-7d-gate1-v1/`.
- Downstream root inspected: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014244+0800-codex-ibkr-si1m-volatility-expansion-reclaim-7d-gate1-v1/downstream-exact-si-1m-volatility-expansion-reclaim-20260520T015038+0800/`.
- Incomplete follow-up downstream root observed: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014244+0800-codex-ibkr-si1m-volatility-expansion-reclaim-7d-gate1-v1/downstream-exact-si-1m-volatility-expansion-reclaim-20260520T015123+0800/` had only `01_auto_quant_results_import.exit=0` and `02_auto_quant_prior_init.exit=0` at readback.
- Factor id: `ibkr_si1m_volatility_expansion_reclaim_7d_gate1_v1`.
- Branch path: `TrendExpansion -> VolatilityExpansionReclaim -> ibkr_si1m_volatility_expansion_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Provider/Gate 1 evidence: real IBKR `SI 202607` `1m 7 D`, `9453` normalized rows, `local_cache_replay=false`; provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=119`.
- Cost/density evidence: the branch has real density and several `2bps/side` survivors, but no `5bps/side` survivor. `SI/balanced/1m` had `29` trades, `55.1724%` win rate, raw `+2.35%`, `+1.19%` after `2bps/side`, and `-0.55%` after `5bps/side`; `SI/quality/1m` had `19` trades, raw `+1.83%`, `+1.07%` after `2bps/side`, and `-0.07%` after `5bps/side`; `SI/wide_hold/1m` had `27` trades, raw `+1.23%`, `+0.15%` after `2bps/side`, and `-1.47%` after `5bps/side`; `SI/dense/1m` failed `2bps/side`. `exact_1m_survivors_2bps=[SI/balanced/1m, SI/quality/1m, SI/wide_hold/1m]`; `exact_1m_survivors_5bps=[]`.
- Downstream evidence: the inspected exact-root downstream imported Auto-Quant results and initialized the prior, but `03_analyze_seed.exit=-15`; workflow, Pre-Bayes, and export seed readbacks exited `0`. Terminal metrics classify it as `exact_si_1m_volatility_expansion_reclaim_downstream_incomplete_fail_closed` with `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`, and `execution_tree_allowed=false`.
- Terminal decision: `gate1_2bps_only_downstream_incomplete_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/terminal_metrics.json`, downstream `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si1m_volatility_expansion_reclaim_7d_gate1_v1.py`.

Interpretation:

- This is the strongest recent precious-metals `1m` packet in terms of raw and `2bps/side` density, and it is useful evidence that SI may deserve a cost-thicker variant. It still fails the user's retention gate: no exact row survives `5bps/side`, and the downstream chain did not produce a completed Pre-Bayes/BBN/CatBoost/execution-tree proof. Preserve it as a metals observation sample; do not promote it or lower the cost gate. A future SI attempt should target wider average trade excursion or lower turnover while preserving 1m density, rather than replaying the same thin-cost reclaim row.

## Public-Family Diversity Gate 1: IBKR MNQ 1m Gap Fade/Go 7D

Append-only terminal row at `2026-05-20 02:11:10 +0800` after observing pre-write hash `1565368d2ad8b528dfdad9f0e5addfe2cf9381fb6e9477116e0cdbd1c8d5a9f9`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T020539+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_gap_fade_go_7d_gate1_v1`.
- Branch path: `OpeningGapRegime -> GapFadeGo -> ibkr_mnq1m_gap_fade_go_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity note: this gives the public gap fade / gap continuation family a real exact-root `MNQ/1m` futures attempt, separate from oscillator reclaim, opening-range, time-of-day, Donchian/Turtle, Keltner/ATR, SuperTrend/ADX, NR7/Crabel, Ichimoku, CMF/OBV, and VWAP/RSI/RVOL variants.
- Provider/Gate 1 evidence: terminal metrics report real IBKR `MNQ 202606` `1m 7 D`, `9476` normalized rows, `local_cache_replay=false`; provider status, fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=7`.
- Evidence hygiene note: the runner/check names and provider path contain stale `si` strings (`01_ibkr_fetch_si_1m_7d`, `ibkr_si_202607_1m_7d.csv`) even though the factor id and branch path are rooted to `MNQ`. Treat the factor verdict as a Gate 1 negative from the terminal metrics, but patch the wrapper before using this family as a reusable template.
- Cost/density evidence: `MNQ/gap_down_fade_dense/1m` had `6` trades, `50.0%` win rate, raw `+0.10%`, but `-0.02%` after `1bps/side`, `-0.14%` after `2bps/side`, and `-0.50%` after `5bps/side`. `MNQ/gap_down_go_balanced/1m` had `1` trade and was raw-negative; `MNQ/gap_up_fade_short/1m` and `MNQ/gap_up_go_long/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_gap_fade_go_7d_gate1_v1.py`.

Interpretation:

- Gap fade/go got a real public-family Gate 1 attempt and failed on practical edge: the only dense row flipped negative at `1bps/side`, so this branch should not go downstream. The stale `si` naming is wrapper hygiene debt, not a reason to promote or discard by provider failure; patch it before any rerun, and rotate to a larger-distance gap variant or return to same-root execution-predicate repair on an existing `5bps/side` survivor.

## Gate 1 Negative: IBKR SI 1m Initial Balance Acceptance 7D

Append-only terminal row at `2026-05-20 02:06:43 +0800` after observing pre-write hash `7ed8d499b6b9a8c609bc7c86949b153b607a8b825e50a79293f1a59b1c610760`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T015829+0800-codex-ibkr-si1m-initial-balance-acceptance-7d-gate1-v1/`.
- Factor id: `ibkr_si1m_initial_balance_acceptance_7d_gate1_v1`.
- Branch path: `MarketProfile -> InitialBalanceAcceptance -> ibkr_si1m_initial_balance_acceptance_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Diversity note: this was a precious-metals market-profile / initial-balance acceptance attempt, materially different from the earlier SI volatility-expansion reclaim packet and from MGC Keltner/ATR or RSI/VWAP controls.
- Provider/Gate 1 evidence: real IBKR `SI 202607` `1m 7 D`, `9469` normalized rows, `local_cache_replay=false`. Provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=0`.
- Cost/density evidence: all exact `1m` market-profile rows completed but produced `0` trades. `SI/ib30_acceptance_dense/1m`, `SI/ib60_acceptance_balanced/1m`, `SI/ib30_failed_break_reclaim/1m`, and `SI/ib60_midpoint_drive/1m` all had raw `0.00%`, `2bps 0.00%`, and `5bps 0.00%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si1m_initial_balance_acceptance_7d_gate1_v1.py`.

Interpretation:

- The SI market-profile family got a clean real-provider Gate 1 attempt and failed on density rather than provider mechanics. It should not be downstreamed or rescued by overlays because the first profit-factor branch produced no trades. The useful SI direction remains a cost-thicker variant of the volatility-expansion/reclaim family only if it can preserve `1m` density and survive `5bps/side`.

## Gate 1 Negative: IBKR M2K/MNQ 1m Relative-Value Z-Score 7D

Append-only terminal row at `2026-05-20 02:11:28 +0800` after observing pre-write hash `1565368d2ad8b528dfdad9f0e5addfe2cf9381fb6e9477116e0cdbd1c8d5a9f9`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T020828+0800-codex-ibkr-m2k-mnq-relative-value-zscore-1m-gate1-v1/`.
- Factor id: `ibkr_m2k_mnq_relative_value_zscore_1m_gate1_v1`.
- Branch path: `RelativeValueMeanReversion -> ZScoreReversion -> ibkr_m2k_mnq_relative_value_zscore_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index_relative_value -> M2K_MNQ_SPREAD -> 1m`.
- Diversity note: this gives the pair/relative-value z-score family a real same-session IBKR futures attempt, distinct from single-leg VWAP/RSI/RVOL, opening-range, trend breakout, and volatility/noise-band controls.
- Provider/Gate 1 evidence: retained same-session real IBKR `M2K 202606` and `MNQ 202606` `1m 7 D` normalized rows were aligned into a `9269`-row synthetic spread. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=0`; `local_cache_replay=retained_real_ibkr_same_session`.
- Cost/density evidence: all exact spread rows failed before cost stress with `0` trades. `M2K_MNQ/dense/1m_spread`, `M2K_MNQ/balanced/1m_spread`, `M2K_MNQ/quality/1m_spread`, and `M2K_MNQ/wide/1m_spread` all had raw `0.00%`, `2bps 0.00%`, and `5bps 0.00%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, spread material artifacts, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k_mnq_relative_value_zscore_1m_gate1_v1.py`.

Interpretation:

- Pair/relative-value z-score now has a clean futures Gate 1 negative sample. The failure is density, not provider mechanics: exact-root metadata survived, but the first profit-factor branch generated no trades. Do not downstream or tune this exact spread shape until the entry family is made materially denser; current useful repair budget should stay with an already cost-surviving branch such as M2K liquidity-sweep reject-short.

## Gate 1 Negative: IBKR MES 1m Aroon/CCI Reclaim Max-Window 7D Fallback

Append-only terminal row at `2026-05-20 02:19:10 +0800` after observing pre-write hash `fee5ec8f651ab2562a1cdb95feda124bfa2ee48b1da6543956a907af30f33487`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T021210+0800-codex-ibkr-mes1m-aroon-cci-reclaim-max-window-gate1-v1/`.
- Factor id: `ibkr_mes1m_aroon_cci_reclaim_max_window_gate1_v1`.
- Branch path: `TrendExpansion -> AroonCciReclaim -> ibkr_mes1m_aroon_cci_reclaim_max_window_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Diversity/window note: this is a max-window first attempt for the Aroon/CCI reclaim family on `MES/1m`, separate from the earlier `MGC/1m` Aroon/CCI negative and from VWAP/RSI/RVOL, gap, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend/ADX, NR7/Crabel, Ichimoku, CMF/OBV, and Fisher/Stoch controls.
- Provider/Gate 1 evidence: IBKR `MES 202606` `1m 1 M` fetch failed with exit `3`, then the real `1m 7 D` fallback succeeded with `9483` normalized rows, `local_cache_replay=false`. Provider status, fallback fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=90`.
- Cost/density evidence: the branch transferred real density and high raw win rate, but all rows failed the practical cost gate. `MES/quality/1m` had `29` trades, `82.7586%` win rate, raw `+0.68%`, `+0.10%` after `1bps/side`, but `-0.48%` after `2bps/side` and `-2.22%` after `5bps/side`. `MES/dense/1m` had `18` trades, raw `+0.41%`, `-0.31%` after `2bps/side`; `MES/balanced/1m` had `25` trades, raw `+0.31%`, `-0.69%` after `2bps/side`; `MES/breakout/1m` had `18` trades, raw `+0.16%`, `-0.56%` after `2bps/side`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mes1m_aroon_cci_reclaim_max_window_gate1_v1.py`.

Interpretation:

- This is a high-signal negative sample: MES Aroon/CCI created plenty of accurate-looking `1m` entries, but the average trade distance is too small for real costs. The provider fallback should be recorded separately from the factor verdict: `1 M` IBKR failed, real `7 D` scored cleanly. Do not downstream this branch. A useful next attempt would need either wider excursion, lower turnover, or a same-root execution repair on an already `5bps/side` survivor rather than another thin reclaim variant.

## Downstream Addendum: IBKR SI 1m Volatility Expansion Reclaim 7D Bounded Readback

Append-only readback at `2026-05-20 02:26:09 +0800` after observing pre-write hash `7fd47494483079e92fee5e4d7cf6e5ab90fc2617a6230cbf0be2c8fd12b0bb4e`.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014244+0800-codex-ibkr-si1m-volatility-expansion-reclaim-7d-gate1-v1/`.
- Downstream root read back: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014244+0800-codex-ibkr-si1m-volatility-expansion-reclaim-7d-gate1-v1/downstream-exact-si-1m-volatility-expansion-reclaim-20260520T020621+0800/`.
- Branch path remains: `TrendExpansion -> VolatilityExpansionReclaim -> ibkr_si1m_volatility_expansion_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Gate 1 authority remains unchanged: the source packet had real density and `2bps/side` survivors, but no exact `5bps/side` survivor, so this downstream readback is observation-only mechanics evidence.
- Downstream mechanics: `01_auto_quant_results_import`, `02_auto_quant_prior_init`, `04_workflow_seed`, `05_pre_bayes_seed`, `06_export_target_seed`, `07_train_ranker`, `08_apply_ranker`, `09_apply_scores_to_ict`, `10_register_trainer`, `11_enable_runtime`, `13_workflow_after_ranker`, `14_pre_bayes_after_ranker`, `15_policy_after_ranker`, and `16_export_target_final` all exited `0`. `03_analyze_seed.exit=-15`; bounded `12_analyze_after_ranker.exit=143`.
- Execution materialization/readback: `execution_candidate.json` and `execution_tree_trace.json` were not present in the readback state; `policy_training/structural_path_ranking_target_summary.json` reports `rows=2`, `mature_rows=0`, `history_mature_rows=0`, `rows_with_execution_gate_status=0`, and `rows_with_training_weight=0`.
- Terminal decision remains: `gate1_2bps_only_downstream_mechanics_partial_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This downstream pass confirms that SI volatility-expansion reclaim can exercise parts of the AQ/Pre-Bayes/ranker pipeline, but it still has no promotion path: the first cost gate lacks a `5bps/side` survivor, analyze did not complete cleanly, no exact execution candidate/tree materialized, and policy rows did not mature. Keep it as a precious-metals observation sample only.

## Gate 1 Negative: IBKR MGC 1m PSAR Trend Flip 7D

Append-only terminal row at `2026-05-20 02:29:56 +0800` after observing pre-write hash `6f385a317e338996302a3364751eae5bc21de5f012c6fe02de09dd7b311eb483`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T022405+0800-codex-ibkr-mgc1m-psar-trend-flip-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_psar_trend_flip_7d_gate1_v1`.
- Branch path: `TrendExpansion -> PsarTrendFlip -> ibkr_mgc1m_psar_trend_flip_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity note: this gives the PSAR trend-flip family a real exact-root micro-gold `1m` futures attempt, separate from Keltner/ATR, Donchian/Turtle, ADX/ATR, SuperTrend/ATR, Connors RSI2, Williams/MFI, Aroon/CCI, Elder/MACD, market-profile/initial-balance, and VWAP/RSI/RVOL controls.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows reused from the source provider root, `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=194`.
- Cost/density evidence: the branch produced high density but no practical edge. `MGC/psar_dense/1m` had `55` trades, `30.9091%` win rate, raw `-0.07%`, `-2.27%` after `2bps/side`, and `-5.57%` after `5bps/side`; `MGC/psar_reflip/1m` had `53` trades, raw `+0.07%`, but `-0.99%` after `1bps/side`, `-2.05%` after `2bps/side`, and `-5.23%` after `5bps/side`; `MGC/psar_balanced/1m` had `48` trades raw `-0.07%`; `MGC/psar_quality/1m` had `38` trades raw `-0.29%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_psar_trend_flip_7d_gate1_v1.py`.

Interpretation:

- PSAR trend flip is a useful high-density negative control. It proves the MGC `1m` provider/AQ mechanics can produce many trades, but the family is raw-negative or too thin before costs. Do not downstream this branch. The metals lane needs either a materially larger-distance setup or a cost-thicker SI variant; otherwise the practical repair budget remains better spent on an existing `5bps/side` survivor such as M2K liquidity-sweep reject-short.

## Correction Addendum: IBKR MNQ 1m Gap Fade/Go 7D Repaired Provider Provenance

Append-only correction at `2026-05-20 02:13:30 +0800` after observing pre-write hash `c159176f39cf132a243bbb97ea415f52dea0e59ebf8af15c02daca6930ddd68f`.

- Corrected run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1/`.
- Supersedes provider-hygiene readback from the earlier `20260520T020539+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1/` packet only for provider provenance; the factor verdict remains Gate 1 negative.
- Factor id: `ibkr_mnq1m_gap_fade_go_7d_gate1_v1`.
- Branch path: `OpeningGapRegime -> GapFadeGo -> ibkr_mnq1m_gap_fade_go_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity note: this gives the public opening-gap fade / gap-continuation family a clean exact-root `MNQ/1m` futures attempt, materially different from oscillator reclaim, opening-range, time-of-day/session momentum, Donchian/Turtle, Keltner/ATR, SuperTrend/ADX, NR7/Crabel, Ichimoku, CMF/OBV, pair z-score, and VWAP/RSI/RVOL variants.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows at `data/provider/normalized/ibkr_mnq_202606_1m_7d.csv`, `local_cache_replay=false`. Provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=7`.
- Cost/density evidence: `MNQ/gap_down_fade_dense/1m` was the only dense-ish row with `6` trades, `50.0%` win rate, raw `+0.10%`, but `-0.02%` after `1bps/side`, `-0.14%` after `2bps/side`, and `-0.50%` after `5bps/side`. `MNQ/gap_down_go_balanced/1m` had `1` trade and raw `-0.28%`; `MNQ/gap_up_fade_short/1m` and `MNQ/gap_up_go_long/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Hygiene repair: `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_gap_fade_go_7d_gate1_v1.py` now owns its MNQ-specific `main()` and writes `01_ibkr_fetch_mnq_1m_7d`, `ibkr_mnq_202606_1m_7d.csv`, and MNQ provider rows directly instead of inheriting the SI template's hardcoded provider names.

Interpretation:

- The user's diversity critique is now reflected in a real public-family rotation, not just a list: gap fade/go, market-profile/initial-balance, and pair z-score all received clean futures Gate 1 chances in this slice. Gap fade/go still failed on practical edge because the only row with enough trades flipped negative at `1bps/side`; it should not go downstream or be promoted. Continue rotating materially different public families, but keep promotion locked to cost/density plus exact-root downstream and execution predicates.

## Gate 1 Negative: IBKR MGC 1m Initial Balance Acceptance 7D

Append-only terminal row at `2026-05-20 02:12:41 +0800` after observing pre-write hash `5d1c50a2534306932c402305431cc8ac103dba20626c900a3f7827ff0a747774`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T020958+0800-codex-ibkr-mgc1m-initial-balance-acceptance-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_initial_balance_acceptance_7d_gate1_v1`.
- Branch path: `MarketProfile -> InitialBalanceAcceptance -> ibkr_mgc1m_initial_balance_acceptance_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity note: this gives micro gold a market-profile / initial-balance branch, distinct from the already-tested MGC Keltner/ATR, RSI/VWAP, Donchian/Turtle, ADX/ATR, SuperTrend/ATR, Connors RSI2, Williams/MFI, Aroon/CCI, Elder/MACD, and SI volatility-reclaim packets.
- Provider/Gate 1 evidence: real IBKR `MGC 202606` `1m 7 D`, `9480` normalized rows, `local_cache_replay=false`. Provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=1`.
- Cost/density evidence: the branch failed both density and cost. `MGC/ib30_acceptance_dense/1m` had `1` trade, `0.0%` win rate, raw `-0.26%`, `-0.30%` after `2bps/side`, and `-0.36%` after `5bps/side`; `MGC/ib60_acceptance_balanced/1m`, `MGC/ib30_failed_break_reclaim/1m`, and `MGC/ib60_midpoint_drive/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_initial_balance_acceptance_7d_gate1_v1.py`.

Interpretation:

- MGC market-profile/initial-balance has now been tested as a separate exact-root precious-metals branch and failed at Gate 1. This is not a provider failure: the real IBKR fetch and AQ mechanics worked, but the first profit factor produced only one raw-negative trade. Do not downstream or rescue this exact branch with overlays; preserve it as a negative boundary sample and keep the practical search focused on branches that already survive real costs or on materially different dense `1m` families.

## Downstream Addendum: IBKR SI 1m Volatility Expansion Reclaim 7D Latest Replay

Append-only downstream addendum at `2026-05-20 02:20:10 +0800` after observing pre-write hash `a0d514b0d2f9f287bf5b13dd1e2376b90b28ea70d6696cc38c404653437c3b58`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014244+0800-codex-ibkr-si1m-volatility-expansion-reclaim-7d-gate1-v1/`.
- Latest downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T014244+0800-codex-ibkr-si1m-volatility-expansion-reclaim-7d-gate1-v1/downstream-exact-si-1m-volatility-expansion-reclaim-20260520T020621+0800/`.
- Factor id: `ibkr_si1m_volatility_expansion_reclaim_7d_gate1_v1`.
- Branch path: `TrendExpansion -> VolatilityExpansionReclaim -> ibkr_si1m_volatility_expansion_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Command evidence: `01_auto_quant_results_import`, `02_auto_quant_prior_init`, `04_workflow_seed`, `05_pre_bayes_seed`, `06_export_target_seed`, `07_train_ranker`, `08_apply_ranker`, `09_apply_scores_to_ict`, `10_register_trainer`, and `11_enable_runtime` all exited `0`; `03_analyze_seed.exit=-15`; no completed `12_analyze_after_ranker.exit` or terminal metrics file was materialized at readback.
- Ranker evidence: CatBoost mechanically trained/applied/registered from the exact state, but only from fallback/pseudo-label target rows. `structural_path_ranking_target_summary.json` reports `rows=2`, `candidate_set_size=1`, `mature_rows=0`, `history_mature_rows=0`, `calibrated_rows=0`, `execution_gate_rows=0`, and `training_weight_rows=0`. Runtime enablement reports validation insufficient: `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, `calibration=not_fitted`, while `runtime_selection=enabled_registered_artifact_ready` and `score_model_family=catboost` only prove mechanics.
- Workflow/Pre-Bayes evidence: workflow snapshot still has `latest_execution_candidate=null`, `recent_execution_candidates=[]`, no pending updates, and `actionable_artifacts=0`; Pre-Bayes seed readback has null latest policy/evidence and empty posterior probabilities.
- Terminal decision remains: `gate1_2bps_only_downstream_incomplete_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The latest SI downstream replay improves mechanical coverage of CatBoost registration/runtime enablement, but it does not improve the actual promotion predicates. The branch still has no `5bps/side` Gate 1 survivor, no mature validation rows, no execution candidate, and an analyze timeout/termination. Treat this as observation-only and do not spend more downstream cycles on this exact SI branch until a cost-thicker Gate 1 variant survives `5bps/side` with density.

## Gate 1 Negative Addendum: IBKR MNQ/M2K 1m Relative-Value Z-Score 7D

Append-only terminal row at `2026-05-20 02:19:20 +0800` after observing pre-write hash `fee5ec8f651ab2562a1cdb95feda124bfa2ee48b1da6543956a907af30f33487`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T021750+0800-codex-ibkr-mnq-m2k-1m-relative-value-zscore-gate1-v1/`.
- Factor id: `ibkr_mnq_m2k_1m_relative_value_zscore_gate1_v1`.
- Branch path: `RangeReversion -> RelativeValueZscore -> ibkr_mnq_m2k_1m_relative_value_zscore_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index_spread -> MNQ_M2K_RELVAL -> 1m`.
- Diversity note: this is the second pair/relative-value z-score attempt, with the leg ordering and spread construction reversed from the earlier `M2K_MNQ_SPREAD` zero-trade packet. It is a materially useful boundary sample because it produced trades but still failed practical cost stress.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` and `M2K 202606` `1m 7 D` legs were aligned into a derived `MNQ_M2K_RELVAL` spread with `9269` common rows. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=23`; `local_cache_replay=retained_real_ibkr_legs_plus_derived_spread`.
- Cost/density evidence: `MNQ_M2K_RELVAL/dense/1m` had `14` trades but raw `-0.00%`, `-0.56%` after `2bps/side`, and `-1.40%` after `5bps/side`. `MNQ_M2K_RELVAL/balanced/1m` had `5` trades, raw `+0.05%`, but `-0.05%` after `1bps/side`; `MNQ_M2K_RELVAL/quality/1m` had `4` trades, raw `+0.05%`, but `-0.03%` after `1bps/side`; `flush_reclaim` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, derived spread CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq_m2k_1m_relative_value_zscore_gate1_v1.py`.

Interpretation:

- Reversing the relative-value construction improved density versus the earlier zero-trade spread, but the edge is entirely inside the friction envelope. This should not go downstream and should not be tuned by lowering cost gates. The practical next step remains either a different dense public family or same-root repair of an already `5bps/side` surviving branch, especially `M2K/1m LiquiditySweepRejectShort`.

## Gate 1 Negative: IBKR MNQ 1m TRIX Signal Continuation 7D

Append-only terminal row at `2026-05-20 02:21:10 +0800` after observing pre-write hash `fee5ec8f651ab2562a1cdb95feda124bfa2ee48b1da6543956a907af30f33487`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T020803+0800-codex-ibkr-mnq1m-trix-signal-continuation-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_trix_signal_continuation_7d_gate1_v1`.
- Branch path: `TrendExpansion -> TrixSignalContinuation -> ibkr_mnq1m_trix_signal_continuation_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity note: this gives TRIX triple-EMA signal continuation its own exact-root `MNQ/1m` public-family branch, separate from opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend/ADX, NR7/Crabel, Darvas, Ichimoku, CMF/OBV, Fisher/Stoch, pair z-score, gap fade/go, and VWAP/RSI/RVOL variants.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9269` normalized rows from same-session fresh IBKR source `/tmp/ict-mnq-1m-7d.csv`, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=8`.
- Cost/density evidence: the best raw rows were positive but too sparse for the required density. `MNQ/dense/1m` had `3` trades, `66.6667%` win rate, raw `+0.26%`, `+0.14%` after `2bps/side`, and `-0.04%` after `5bps/side`; `MNQ/balanced/1m` had `3` trades, raw `+0.18%`, `+0.06%` after `2bps/side`, and `-0.12%` after `5bps/side`; `MNQ/zero_line/1m` had `2` trades and failed `2bps/side`; `MNQ/quality/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]` because density was below threshold.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner referenced by claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-ibkr-mnq1m-trix-signal-continuation-7d-gate1-v1.claim`.

Interpretation:

- TRIX continuation is a useful public-family observation because the raw entries were directionally plausible, but it does not meet the user's live-profit gate: there are too few trades and no `5bps/side` survivor. Do not downstream this branch. The failure pattern again points away from thin intraday continuation/reclaim variants and toward either wider-excursion factor families or same-root execution repair on the existing `M2K/1m LiquiditySweepRejectShort` `5bps/side` survivor.

## Downstream Addendum: IBKR MNQ 1m Compression-Breakout VWAP-Persistence Transition-Guard Simulated-Trade Admission

Append-only terminal row at `2026-05-20 02:43:44 +0800` after observing completed ranker/runtime/analyze readbacks for the simulated-trade admission repair.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T012135+0800-codex-ibkr-mnq1m-compression-breakout-vwap-persistence-transition-guard-7d-gate1-v1/simulated-trade-admission-mnq-1m-compression-breakout-vwap-persistence-transition-guard-20260520T022917+0800/`.
- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T012135+0800-codex-ibkr-mnq1m-compression-breakout-vwap-persistence-transition-guard-7d-gate1-v1/`.
- Branch path remains: `TrendExpansion -> CompressionBreakout -> ibkr_mnq1m_compression_breakout_7d_gate1_v1 -> ibkr_mnq1m_compression_breakout_vwap_persistence_transition_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Simulated feedback evidence: `6` same-Auto-Quant-workspace simulated trade rows were ingested (`5` wins / `1` loss / `0` breakevens); exact branch survived; `ranker_validation_ready=true`; `mature_rows=2`; `history_mature_rows=7`.
- Command evidence: `01_auto_quant_results_import` through `19_policy_after_ranker` exited `0`. Manual `20_manual_export_target.exit=2` is not a promotion blocker by itself because the decisive runtime artifacts were present and readable.
- Runtime/execution evidence: `execution_candidate.json` reports `candidate_status=no_trade`; `execution_tree_trace.json` reports `execution_readiness=0.23133329744621622`, `transition_hazard=0.911034227510214`, `gate_status=observe`, and closed-loop admission `status=fail_closed`, `execution_gate_status=execution_observe_only`, reason `exact_structural_branch_visible_but_not_ready_or_actionable`, decision hint `execution_guarded_due_to_pda_hybrid_disagreement`.
- Terminal decision: `simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This repair proves the MNQ compression rooted branch can survive exact identity, simulated feedback ingestion, ranker validation, runtime enablement, and final analyze readback, but it still fails the user's live-profit predicates. The blocker is no longer source diversity or pipeline mechanics; it is execution state quality: current maturity is too low, transition hazard is far above `<0.60`, PDA/hybrid agreement is not satisfied, readiness is far below `>=0.65`, and the execution candidate remains `no_trade`. Preserve this as the strongest MNQ observation branch, but do not promote it or keep stacking near-equivalent VWAP/RSI/RVOL overlays.

## Gate 1 Negative: IBKR MGC 1m Camarilla Pivot Reversion 7D

Append-only terminal row at `2026-05-20 02:45:19 +0800` after a real Auto-Quant Gate 1 slice.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024519+0800-codex-ibkr-mgc1m-camarilla-pivot-reversion-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_camarilla_pivot_reversion_7d_gate1_v1`.
- Branch path: `RangeReversion -> CamarillaPivotReversion -> ibkr_mgc1m_camarilla_pivot_reversion_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity note: this gives the Camarilla/floor-pivot intraday reversion family a real exact-root micro-gold `1m` futures attempt, distinct from VWAP/RSI/RVOL, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend/ATR, Connors RSI2, Williams/MFI, Aroon/CCI, Elder/MACD, PSAR, and initial-balance families.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows reused from the source provider root, `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=4`.
- Cost/density evidence: `MGC/s3_reclaim_balanced/1m` had `2` trades, raw `+0.52%`, `+0.44%` after `2bps/side`, and `+0.32%` after `5bps/side`; `MGC/s3_reclaim_dense/1m` had `2` trades, raw `+0.36%`, `+0.28%` after `2bps/side`, and `+0.16%` after `5bps/side`; `MGC/s4_flush_reclaim/1m` and `MGC/pivot_breakout_retest/1m` had `0` trades. Because density is only `4` total trades across the branch and below the required daily target, `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_camarilla_pivot_reversion_7d_gate1_v1.py`.

Interpretation:

- Camarilla pivot reversion is a useful diversity sample because it produced positive per-trade distance after costs, but the density is far below the live-profit requirement. Do not downstream this exact branch or loosen the density gate to make it pass. The result reinforces the current selection rule: public families deserve real Gate 1 chances, but only real-cost density survivors move to Pre-Bayes/BBN/CatBoost/execution-tree; otherwise the repair budget should return to existing `5bps/side` survivors such as `M2K/1m LiquiditySweepRejectShort`.

## Downstream Addendum: IBKR M2K 1m Liquidity-Sweep Reject-Short RVol/PDA Guard Simulated-Trade Admission

Append-only terminal row at `2026-05-20 02:54:00 +0800` after the same-root RVol/PDA guard and simulated-trade admission repair completed.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Simulated-admission root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T024616+0800/`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Gate 1 authority: real retained IBKR `M2K 202606` `1m 7 D`; all four RVol/PDA guard variants survived both `2bps/side` and `5bps/side`; `rank_total_trade_count=98`; `downstream_allowed=true`. Survivors: `M2K/rvol100_ema55/1m_short`, `M2K/rvol075_ema55/1m_short`, `M2K/rvol075_slope/1m_short`, and `M2K/rvol075_ema200/1m_short`.
- Simulated feedback evidence: `19` same-Auto-Quant-workspace simulated trades were ingested (`12` wins / `7` losses / `0` breakevens); exact branch survived; `ranker_validation_ready=true`; `mature_rows=2`; `history_mature_rows=20`.
- Command evidence: Gate 1 checks `00_provider_status_ibkr`, `04_strategy_py_compile`, `05_auto_quant_agent_material_batch`, `06_auto_quant_agent_material_dispatch`, and `07_auto_quant_agent_material_rank` exited `0`. Downstream checks `01_auto_quant_results_import` through `19_policy_after_ranker` all exited `0`.
- Runtime/execution evidence: `execution_candidate.json` reports `candidate_status=no_trade`; `execution_tree_trace.json` reports `execution_readiness=0.3180972047048167`, `transition_hazard=0.9184975817511946`, `gate_status=observe`, and closed-loop admission `status=fail_closed`, `execution_gate_status=execution_observe_only`, reason `exact_structural_branch_visible_but_not_ready_or_actionable`, decision hint `execution_guarded_due_to_pda_hybrid_disagreement`.
- Terminal decision: `rvol_pda_guard_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is the strongest current exact-root futures repair packet because it has real-cost density at `5bps/side`, a rooted branch through AQ/ranker/runtime, and successful simulated feedback ingestion. It still cannot be called trade-usable: the execution tree remains observe/fail-closed, PDA/hybrid agreement is false, transition hazard is far above `<0.60`, execution readiness remains far below `>=0.65`, and the execution candidate is `no_trade`. Keep this branch as the nearest repair target, but do not lower gates or promote it. The next useful work should attack the execution-state blocker directly: reduce transition hazard and force PDA/hybrid agreement/mature current validation on the same rooted branch, rather than adding another light overlay or starting a weaker cost-negative family.

## Gate 1 Negative: IBKR MGC 1m Floor Pivot CPR Reclaim 7D

Append-only terminal row at `2026-05-20 03:01:56 +0800` after observing pre-write hash `7892d1d7a0416b5a582b3e3525c7ee3fb0c9e5268bd432a37f585f7eae7858af`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024036+0800-codex-ibkr-mgc1m-floor-pivot-cpr-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_floor_pivot_cpr_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> FloorPivotCprReclaim -> ibkr_mgc1m_floor_pivot_cpr_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity note: this gives the classic floor-trader pivot / central-pivot-range intraday family a real exact-root micro-gold `1m` futures attempt. It is adjacent to but distinct from the Camarilla pivot reversion packet, and separate from VWAP/RSI/RVOL, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend/ATR, Connors RSI2, Williams/MFI, Aroon/CCI, Elder/MACD, PSAR, and initial-balance families.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows reused from the source provider root, `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=14`.
- Cost/density evidence: the only raw-positive row, `MGC/r1_reject_short/1m`, had `2` trades, `50.0%` win rate, raw `+0.16%`, `+0.08%` after `2bps/side`, but `-0.04%` after `5bps/side`, and it fails density. `MGC/pivot_reclaim_dense/1m` had `5` trades and raw `-0.44%`; `MGC/pivot_reclaim_balanced/1m` had `4` trades and raw `-0.74%`; `MGC/cpr_breakout_long/1m` had `3` trades and raw `-0.13%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_floor_pivot_cpr_reclaim_7d_gate1_v1.py`.

Interpretation:

- Floor pivot / CPR reclaim has now been given a clean public-family Gate 1 chance on real IBKR MGC `1m` data. The provider and AQ mechanics worked, but the branch is sparse and mostly raw-negative; the only raw-positive short row does not survive `5bps/side` and is too thin. Do not downstream or tune this exact pivot shape by lowering costs. The strongest practical repair target remains the M2K liquidity-sweep reject-short RVol/PDA branch, which already has `5bps/side` density but still needs direct execution-state repair.

## Gate 1 Negative: IBKR M2K 1m Williams/MFI Reclaim 7D

Append-only terminal row at `2026-05-20 03:07:00 +0800` after observing pre-write hash `237c2de11faab620c5badd8385f431e3069833b6e0df273113cc6f619dfd0cff`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T030152+0800-codex-ibkr-m2k1m-williams-mfi-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_williams_mfi_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> WilliamsMfiReclaim -> ibkr_m2k1m_williams_mfi_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Diversity note: this gives the source-backed Williams %R / MFI oscillator-reclaim family its own exact-root `M2K/1m` futures attempt, distinct from the M2K liquidity-sweep reject-short survivor and from MNQ trend/OR/pair/z-score families.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows reused from same-session real provider evidence (`source_provider_root=20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`, `local_cache_replay=false_retained_real_ibkr_same_session`). Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=95`.
- Cost/density evidence: density existed, but all rows were inside the friction envelope. `M2K/willmfi_balanced/1m` had `26` trades, `73.0769%` win rate, raw `+0.43%`, but `-0.09%` after `1bps/side`, `-0.61%` after `2bps/side`, and `-2.17%` after `5bps/side`; `M2K/willmfi_dense/1m` had `36` trades, raw `+0.18%`, but `-1.26%` after `2bps/side`; `M2K/willmfi_flush/1m` had `16` trades and `M2K/willmfi_quality/1m` had `17` trades, both also failed `1bps/side`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_williams_mfi_reclaim_7d_gate1_v1.py`.

Interpretation:

- Williams/MFI reclaim on `M2K/1m` is not a provider or pipeline failure; it is a practical-cost failure. The family produced many trades and attractive raw win rates, but the gross edge was too small for even `1bps/side`, so it must not go downstream or be rescued by lowering costs. This reinforces that the current futures repair budget should stay with the existing M2K liquidity-sweep reject-short RVol/PDA branch, where the blocker is execution-state quality rather than Gate 1 economics.

## Gate 1 Negative: IBKR SI 5m Volatility Expansion Reclaim 1M

Append-only terminal row at `2026-05-20 03:12:00 +0800` after observing pre-write hash `d4f0cb9f84abe175e1b694ef54b73efce9096917b4e55ca17d02e338097fe67d`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T030447+0800-codex-ibkr-si5m-volatility-expansion-reclaim-1m-gate1-v1/`.
- Factor id: `ibkr_si5m_volatility_expansion_reclaim_1m_gate1_v1`.
- Branch path: `TrendExpansion -> VolatilityExpansionReclaim -> ibkr_si5m_volatility_expansion_reclaim_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 5m`.
- Provider/Gate 1 evidence: real IBKR `SI 202607` `5m 1 M`, `6047` normalized rows, `local_cache_replay=false`. Provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=182`.
- Cost/density evidence: the branch had enough trades but no profitable row even before cost stress. `SI/dense/5m` had `62` trades, `46.7742%` win rate, raw `-1.06%`, `-3.54%` after `2bps/side`, and `-7.26%` after `5bps/side`; `SI/balanced/5m` had `43` trades raw `-5.57%`; `SI/wide_hold/5m` had `44` trades raw `-4.09%`; `SI/quality/5m` had `33` trades raw `-4.59%`. `exact_5m_survivors_2bps=[]`; `exact_5m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si5m_volatility_expansion_reclaim_1m_gate1_v1.py`.

Interpretation:

- This is a clean precious-metals timeframe-ladder failure, not a data or pipeline blocker. A wider `5m 1M` SI window produced high density, but all variants were raw-negative before costs, so the exact branch should stop at Gate 1. Do not move it to Pre-Bayes/BBN/CatBoost/execution-tree, and do not use the earlier SI `1m` observation packet as promotion support for this independent `5m` root.

## Execution Predicate Diagnostic: IBKR M2K 1m Liquidity-Sweep Reject-Short RVol/PDA Guard

Append-only diagnostic row at `2026-05-20 03:18:00 +0800` after observing pre-write hash `13ded92d34ac5918c25911295d1abb8c8c68b4bd00718aa991c882b34647baeb`.

- Target Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Target downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T024616+0800/`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- What is already solved: Gate 1 cost/density is real (`98` total ranked trades, all four variants survived `5bps/side`), exact branch survived downstream, `19` same-Auto-Quant-workspace simulated trades were ingested, CatBoost trained/applied/registered/enabled, ranker validation was ready, and downstream commands `01` through `19` all exited `0`.
- Current execution blocker: `execution_tree_trace.json` reports `gate_status=observe`, `branch=transition_guardrail`, `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`, `execution_readiness=0.3180972047048167`, `hybrid_transition_hazard=0.9184975817511946`, `pda_hybrid_alignment=false`, `path_ranker_score_visible_to_execution_tree=true`, and `path_ranker_score_used_by_execution_tree=false`.
- Regime/PDA readback: `workflow_snapshot.json` reports current market state `TrendExpansion/BullTrendAcceleration` with overall confidence `0.620`, while the exact profit branch root is `RangeReversion`. The same readback reports `multi_timeframe_direction_bias=bearish`, `multi_timeframe_alignment_score=1.000`, `multi_timeframe_entry_alignment_score=0.965`, but also says that bearish MTF bias conflicts with the regime/alignment, so factor alignment is neutralized.
- Pre-Bayes/BBN readback: `pre_bayes_quality_score=0.379`, `pre_bayes_gate_status=observe_only`, evidence confidence was downweighted, BBN input was downgraded to neutralized defaults, and the read-only regime BBN label stayed `primary::RangeReversion` only as non-promoting branch trace.
- PDA readback: PDA cluster is `cluster_0`, family=`trend`, confidence=`1.000`, ensemble mean confidence=`0.998`, but DTW/HMM consistency is only `0.453`, below the stability floor; this is the proximate reason the execution tree treats the branch as PDA/hybrid disagreement instead of a tradable RangeReversion short setup.
- Terminal decision remains: `rvol_pda_guard_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The next aligned repair is not another light RVol/VWAP/PDA overlay on this same `RangeReversion` branch. The blocker is current-state agreement: the execution snapshot is reading `TrendExpansion/BullTrendAcceleration` and low PDA sequence consistency, so the RangeReversion short factor is correctly kept observe-only even though AQ economics are strong. A same-root repair must explicitly restrict or replay this factor into true `RangeReversion` windows with PDA consistency above the stability floor and Pre-Bayes no longer neutralized. If the live/current regime remains TrendExpansion, the next candidate should be a new rooted TrendExpansion profit factor, not a RangeReversion short branch forced through the tree.

## Downstream Diagnostic Addendum: IBKR M2K 1m RVol/PDA Guard Full-Ladder Replay Attempt

Append-only terminal row at `2026-05-20 03:34:00 +0800` after observing pre-write hash `8ff9912379836047ea2a86b6a280bffc45d6e48daeea1612559979a1f2ad339f`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T032305+0800/`.
- Source Gate 1 root remains: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Branch path remains: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Wrapper repair evidence: `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_simulated_trade_admission_v1.py` now writes cleaned sibling files for `1m/5m/15m/30m/1h/4h/1d` under the run root from the retained real IBKR `1m` feed. `checks/prompt_to_artifact_checklist.csv` marks `full MTF ladder generated=pass`.
- Simulated feedback evidence: the same Auto-Quant workspace again exported `19` simulated trades (`12` wins / `7` losses / `0` breakevens), and `08_ingest_simulated_trades.exit=0`.
- Command evidence: `01_auto_quant_results_import`, `02_auto_quant_prior_init`, `04_workflow_seed`, `05_pre_bayes_seed`, `06_export_target_seed`, `07_policy_seed`, `08_ingest_simulated_trades`, `09_export_target_after_simulated_trades`, `10_policy_after_simulated_trades`, `11_train_ranker`, `12_apply_ranker`, `13_apply_scores_to_ict`, `14_register_trainer`, `15_enable_runtime`, `17_workflow_after_ranker`, `18_pre_bayes_after_ranker`, and `19_policy_after_ranker` exited `0`; both analyze stages exited `-15` (`03_analyze_seed.exit=-15`, `16_analyze_after_ranker.exit=-15`).
- Runtime/execution evidence: no `execution_candidate.json` or `execution_tree_trace.json` materialized in the new full-ladder run. Terminal metrics therefore report `all_command_exits_zero=false`, `exact_branch_survived=false`, `ranker_validation_ready=false`, `mature_rows=2`, `history_mature_rows=20`, `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`.
- Terminal decision: `rvol_pda_guard_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The full-ladder repair improved artifact coverage but did not repair execution admission. The failure is now explicitly a wrapper/runtime shape problem for full `1m` JSON analyze on this packet: the generated ladder exists, but analyze termination prevented execution candidate/tree materialization. Do not promote the branch and do not call the full-ladder replay a factor verdict. The nearest useful next action is either a lighter full-ladder analyze path that preserves `5m/15m/30m/1h/4h/1d` context without `-15`, or a fresh rooted `TrendExpansion` factor if the live/current M2K regime remains TrendExpansion rather than RangeReversion.

## Downstream Addendum: IBKR M2K 1m Liquidity-Sweep Reject-Short RVol/PDA Guard Full-Ladder Admission Repair

Append-only terminal row at `2026-05-20 03:39:00 +0800` after observing pre-write hash `8ff9912379836047ea2a86b6a280bffc45d6e48daeea1612559979a1f2ad339f`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Full-ladder admission root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T032305+0800/`.
- Branch path remains: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Full-ladder context evidence: the repair generated cleaned JSON siblings under `data/cleaned-mtf/` for `1m`, `5m`, `15m`, `30m`, `1h`, `4h`, and `1d` from the retained real IBKR `M2K 202606 1m 7 D` source rows. This fixed the missing-file side of the prior context hypothesis, but the analyze commands still used `--data-ltf 1m`, `--data-mtf 1h`, and `--data-htf 1d` and did not complete a successful execution-tree refresh.
- Command evidence: `01_auto_quant_results_import`, `02_auto_quant_prior_init`, `04_workflow_seed`, `05_pre_bayes_seed`, `06_export_target_seed`, `07_policy_seed`, `08_ingest_simulated_trades`, `09_export_target_after_simulated_trades`, `10_policy_after_simulated_trades`, `11_train_ranker`, `12_apply_ranker`, `13_apply_scores_to_ict`, `14_register_trainer`, `15_enable_runtime`, `17_workflow_after_ranker`, `18_pre_bayes_after_ranker`, and `19_policy_after_ranker` exited `0`; both `03_analyze_seed` and `16_analyze_after_ranker` exited `-15`.
- Simulated feedback evidence: `19` same-Auto-Quant-workspace simulated trades were ingested (`12` wins / `7` losses / `0` breakevens); `mature_rows=2`; `history_mature_rows=20`.
- Runtime/readback evidence: `checks/simulated_trade_admission_metrics.json` reports `all_command_exits_zero=false`, `exact_branch_survived=false`, `ranker_validation_ready=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, `execution_candidate_status=null`, and `multi_timeframe_summary=null`. `workflow_snapshot.json` has `latest_execution_candidate=null`, `recent_execution_candidates=[]`, `actionable_artifacts=[]`, `pre_bayes_gate_status=blocked`, and `pre_bayes_multi_timeframe_direction_bias=direction_bias_unavailable` after the interrupted analyze path.
- Terminal decision: `full_ladder_admission_repair_fail_closed_analyze_interrupted`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The full-ladder file repair alone did not convert the M2K RangeReversion short branch into a live-practical factor. It proves the context files can be materialized, but without a completed analyze refresh there is no execution candidate, no execution tree parity, no ranker validation readiness, no PDA/hybrid alignment, and no readiness improvement. This branch remains a strong observation/repair seed because Gate 1 economics survived real `5bps/side`, but the next aligned work must either repair the analyze/full-ladder command path and replay exactly once, or pivot to a fresh `TrendExpansion` rooted factor that matches the current execution/PDA state. Do not lower gates or call this branch trade-usable.

## Downstream Addendum: IBKR M2K 1m Liquidity-Sweep Reject-Short RVol/PDA Guard Re-Run Incomplete

Append-only readback row at `2026-05-20 03:48:00 +0800` after observing pre-write hash `8ff9912379836047ea2a86b6a280bffc45d6e48daeea1612559979a1f2ad339f`.

- Re-run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T032305+0800/`.
- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Re-run evidence: simulated feedback again exported `19` same-Auto-Quant-workspace rows (`12` wins / `7` losses / `0` breakevens). Import, prior init, workflow/pre-Bayes/policy readbacks, simulated-trade ingest, target export, CatBoost train/apply/register, score application, runtime enablement, and final workflow/pre-Bayes/policy readbacks all exited `0`.
- Incomplete readback: both analyze calls were terminated (`03_analyze_seed.exit=-15`, `16_analyze_after_ranker.exit=-15`), so this re-run did not materialize `execution_tree_trace.json` or `execution_candidate.json`. Terminal metrics therefore report `all_command_exits_zero=false`, `exact_branch_survived=false`, `ranker_validation_ready=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, `promotion_allowed=false`, and `trade_usable=false`.
- Terminal decision: `rvol_pda_guard_simulated_trade_admission_fail_closed`; `update_goal=false`.

Interpretation:

- This re-run adds no promotion evidence beyond the earlier complete `024616` downstream packet. It confirms that repeating the same simulated-admission wrapper is not the useful next step: the complete packet already showed the real blocker (`transition_guardrail`, high transition hazard, PDA/hybrid disagreement), while this later packet lost execution-tree materialization entirely. Do not replace the prior complete fail-closed verdict with this weaker re-run, and do not keep relaunching the same wrapper unless the analyze timeout or current-state/PDA blocker is directly changed first.

## Gate 1 Negative: IBKR MNQ 1m KST/Coppock Momentum 7D

Append-only terminal row at `2026-05-20 04:05:00 +0800` after observing pre-write hash `5cbf0745de6ba9163f0a0f3fb9ab9a910d7327d12174636717009658c0f2682c`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T034832+0800-codex-ibkr-mnq1m-kst-coppock-momentum-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_kst_coppock_momentum_7d_gate1_v1`.
- Branch path: `TrendExpansion -> KstCoppockMomentum -> ibkr_mnq1m_kst_coppock_momentum_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public KST/Coppock momentum-continuation family from `github.com/cinar/indicator` its own exact-root `MNQ/1m` futures attempt, distinct from oscillator reclaim, VWAP/RVOL, liquidity sweep, Donchian/Turtle, TRIX, PSAR, pivot, and initial-balance lanes.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows, reused from same-session real provider evidence (`source_provider_root=20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`, `local_cache_replay=false_retained_real_ibkr_same_session`). Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=124`.
- Cost/density evidence: density existed, but no exact `1m` row survived practical friction. `MNQ/kst_quality/1m` had `18` trades, `38.8889%` win rate, raw `+0.14%`, but `-0.22%` after `1bps/side`, `-0.58%` after `2bps/side`, and `-1.66%` after `5bps/side`; `MNQ/coppock_reaccel/1m` had `36` trades, raw `-0.23%`, and `-3.83%` after `5bps/side`; `MNQ/kst_dense/1m` had `39` trades, raw `-0.19%`, and `-4.09%` after `5bps/side`; `MNQ/kst_balanced/1m` had `31` trades, raw `-0.37%`, and `-3.47%` after `5bps/side`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_kst_coppock_momentum_7d_gate1_v1.py`.

Interpretation:

- Public KST/Coppock momentum on `MNQ/1m` produced enough trades to be a fair test, but the edge was too small or negative after even `1bps/side`. Do not send this exact root downstream. The stronger research direction remains either repairing execution-state predicates for an already cost-surviving branch, or selecting a materially larger-excursion `1m` family that can survive `5bps/side` before Pre-Bayes/BBN/CatBoost/execution-tree admission.

## Gate 1 Negative: IBKR M2K 1m Chande/CMO Reclaim 7D

Append-only terminal row at `2026-05-20 04:00:00 +0800` after a duplicate-search/pre-write check; post-write readback showed a concurrent KST/Coppock row landed ahead of this Chande/CMO row.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T032527+0800-codex-ibkr-m2k1m-chande-cmo-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_chande_cmo_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> ChandeCmoReclaim -> ibkr_m2k1m_chande_cmo_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Diversity note: this gives the Chande Momentum Oscillator / CMO reclaim family its own exact-root `M2K/1m` attempt, separate from the stronger M2K liquidity-sweep reject-short survivor and from Williams/MFI, VWAP/RVOL, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend, Ichimoku, TRIX, and pivot families.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows reused from same-session real provider evidence (`source_provider_root=20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`, `local_cache_replay=false_retained_real_ibkr_same_session`). Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=110`.
- Cost/density evidence: density existed, but the edge was fully inside the practical-cost envelope. `M2K/cmo_flush/1m` had `29` trades, raw `+0.49%`, `-0.09%` after `1bps/side`, `-0.67%` after `2bps/side`, and `-2.41%` after `5bps/side`; `M2K/cmo_balanced/1m` had `28` trades, raw `+0.38%`, `-0.18%` after `1bps/side`, `-0.74%` after `2bps/side`, and `-2.42%` after `5bps/side`; `M2K/cmo_quality/1m` had `17` trades and failed `1bps/side`; `M2K/cmo_dense/1m` had `36` trades and was raw-negative. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_chande_cmo_reclaim_7d_gate1_v1.py`.

Interpretation:

- Chande/CMO reclaim is a clean public-family negative on real IBKR `M2K/1m` data. The provider and AQ path worked and trade count was not the main blocker, but every profitable-looking row flipped negative by `1bps/side`, so this branch must stop at Gate 1. Do not downstream it, do not lower costs to rescue it, and do not treat it as a repair path for the existing M2K RVol/PDA execution-state blocker.

## Gate 1 Handoff: IBKR SI 5m ATR Exhaustion Short 1M

Append-only terminal row at `2026-05-20 04:28:00 +0800` after observing pre-write hash `921944d0e37bd725e5c466ff6598abd607776713e8d6119f4843e71842903150` and duplicate-searching the Board.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/`.
- Factor id: `ibkr_si5m_atr_exhaustion_short_1m_gate1_v1`.
- Branch path: `TrendExpansion -> AtrExhaustionShort -> ibkr_si5m_atr_exhaustion_short_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 5m`.
- Provider/Gate 1 evidence: retained real IBKR `SI 202607` `5m 1 M`, `6047` normalized rows, `local_cache_replay=false_retained_real_ibkr_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank mechanics completed; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=82`. The fetch exit is recorded as `1` because this packet reused same-session retained real IBKR rows rather than fresh fetch completion.
- Cost/density evidence: two exact `5m` short variants survived `5bps/side`: `SI/atr050_v100_hold30/5m_short` had `16` trades, raw `+1.89%`, `2bps=+1.25%`, `5bps=+0.29%`; `SI/atr030_v125_hold30/5m_short` had `21` trades, raw `+2.53%`, `2bps=+1.69%`, `5bps=+0.43%`. Two other variants survived `2bps/side` but failed `5bps/side`.
- Gate decision: `gate1_ibkr_si5m_volatility_expansion_reclaim_downstream_allowed`; `downstream_allowed=true`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, and provider normalized CSV.

Interpretation:

- This is a meaningful precious-metals Gate 1 survivor: unlike the earlier SI `5m` volatility-expansion reclaim, the ATR exhaustion short variant has real-cost density and two `5bps/side` survivors. It is still not trade-usable: no exact downstream replay has proven Pre-Bayes/BBN/CatBoost/execution-tree direction consistency, no execution candidate/tree predicates exist yet, and the hard live gates (`transition_hazard < 0.60`, `pda_hybrid_alignment=true`, `execution_readiness >= 0.65`) remain untested. The next aligned action is an exact-root downstream replay for this `SI/5m AtrExhaustionShort` branch, not promotion.

## Gate 1 Negative: IBKR MNQ 1m Heikin-Ashi ATR Trend 7D

Append-only terminal row at `2026-05-20 04:29:00 +0800` after observing pre-write hash `921944d0e37bd725e5c466ff6598abd607776713e8d6119f4843e71842903150` and duplicate-searching the Board.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042109+0800-codex-ibkr-mnq1m-heikin-ashi-atr-trend-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_heikin_ashi_atr_trend_7d_gate1_v1`.
- Branch path: `TrendExpansion -> HeikinAshiAtrTrend -> ibkr_mnq1m_heikin_ashi_atr_trend_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives Heikin-Ashi plus ATR trend continuation its own exact-root `MNQ/1m` public-family branch, separate from KST/Coppock, TRIX, SuperTrend/ADX, Ichimoku, Donchian/Turtle, Keltner/ATR, Darvas, NR7/Crabel, gap fade/go, CMF/OBV, and VWAP/RVOL variants.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9269` normalized rows from same-session fresh IBKR source `/tmp/ict-mnq-1m-7d.csv`, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=77`.
- Cost/density evidence: all exact `1m` variants failed practical friction. `MNQ/quality_run4/1m` had `10` trades and raw `-0.01%`; `MNQ/balanced_run3/1m` had `14` trades, raw `+0.01%`, but `1bps=-0.27%`; `MNQ/dense_run2/1m` had `25` trades, raw `-0.08%`; `MNQ/pullback_run3/1m` had `28` trades, raw `-0.19%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; no Pre-Bayes/BBN/CatBoost/execution-tree run; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_heikin_ashi_atr_trend_7d_gate1_v1.py`.

Interpretation:

- Heikin-Ashi ATR trend continuation received a fair real IBKR `MNQ/1m` Gate 1 attempt and failed on economics, not metadata or provider mechanics. The entries were dense enough to evaluate, but the edge was raw-flat to negative and turned worse under `1bps/side`. Do not downstream this branch or rescue it with lower costs; keep it as a diversity negative and shift effort to larger-excursion families or the new SI `5m` ATR exhaustion short survivor.

## Analyze-Path Diagnostic: IBKR M2K 1m Liquidity-Sweep Reject-Short RVol/PDA Guard Full-Ladder Probe

Append-only diagnostic row at `2026-05-20 04:20:00 +0800` after observing pre-write hash `921944d0e37bd725e5c466ff6598abd607776713e8d6119f4843e71842903150` and confirming no active matching run processes.

- Probe root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T032305+0800/`.
- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Branch path remains: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- MTF ladder counts from the retained real IBKR `M2K 202606 1m 7 D` rows: `1m=9392`, `5m=1879`, `15m=627`, `30m=314`, `1h=157`, `4h=43`, `1d=9`.
- Manual analyze probes written under the same run root:
  - `manual_data_root_analyze_probe`: `analyze --data-root data/cleaned-mtf`, exit `1`, error `need at least 29 candles to build features, got 9`.
  - `manual_15m_1h_4h_analyze_probe`: explicit `15m/1h/4h`, exit `1`, same candle-count floor error.
  - `manual_5m_15m_1h_analyze_probe`: explicit `5m/15m/1h`, exit `1`, same candle-count floor error.
- Runtime/execution evidence: after these probes, `execution_candidate.json` and `execution_tree_trace.json` still did not materialize in the full-ladder run state. `workflow_snapshot.json` remained present, but no execution-tree parity or actionable candidate was restored.
- Terminal decision remains: `full_ladder_admission_repair_fail_closed_analyze_feature_floor`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This diagnostic narrows the full-ladder blocker. The later `032305` M2K full-ladder replay is not merely missing sibling files; the 7-day retained source has only `9` daily candles, and the current analyze feature builder refuses the high-timeframe sequence before execution artifacts can be refreshed. Shorter explicit frame combinations did not repair materialization. Do not keep relaunching this same wrapper and do not promote the branch from mechanical ranker success. The practical next path is either a longer real IBKR provider window that can satisfy HTF feature floors for the same exact branch, or a fresh `TrendExpansion` rooted `1m` public-family factor matching the current execution/PDA state.

## Current Direction Correction: Public-Family Diversity Queue

Append-only direction correction at `2026-05-20 04:36:00 +0800` after observing pre-write hash `602bcd0e4c1a5428b102d927b45f2afe57b86c556e640db06c658d644e492aa8`.

- User correction: the factor search must stop over-concentrating on the same small indicator/strategy neighborhood. Time-tested public strategies, scripts, entry patterns, and risk-control families deserve real Gate 1 chances before local favorites are revisited.
- Immediate rule: maintain a rotation queue of materially distinct families. Do not let VWAP/RSI/RVOL/microtrend/liquidity-overlay variants occupy most consecutive profitability slots unless they are explicit overlays on an exact root that already survived cost stress.
- Required public-family pool for upcoming attempts: opening-range breakout/fade/reclaim, Donchian/Turtle, Darvas/box breakout, ADX/ATR, Keltner/squeeze, Connors RSI2, SuperTrend, Elder/MACD impulse, Heikin-Ashi/ATR, Ichimoku, NR7/Crabel, market-profile/initial-balance, floor/Camarilla/CPR pivots, gap fade/go, pair/relative-value z-score, volatility/noise-band breakout, PSAR, TRIX/KST/Coppock, Fisher/Stoch, Williams/MFI, Aroon/CCI, Chande/CMO, CMF/OBV accumulation, and ATR exhaustion/volatility exhaustion.
- Gate discipline remains unchanged: popularity is only candidate selection evidence. Each family still needs a specific rooted branch (`market -> product -> symbol -> timeframe -> main_regime -> sub_regime -> profit_factor`), real provider rows or explicitly retained same-session rows, Auto-Quant Gate 1 density, and `0/1/2/5bps` per-side cost stress. Gate 1 failure is terminal for that exact root; Gate 1 success still needs exact downstream readback before any promotion.
- Near-term action: finish classifying the active `SI/5m AtrExhaustionShort` downstream packet without promotion inflation, then pick the next fresh attempt from the diversity queue rather than micro-tuning the same VWAP/RSI/RVOL cluster.

## Downstream Fragment: IBKR SI 5m ATR Exhaustion Short Same-Frame Exact Wrapper

Append-only fragment row at `2026-05-20 04:44:00 +0800` after observing pre-write hash `a70ece978abd182529ab6611f869feb3cd3639944ea339ad15c4f7e0c9a8a1cc` and duplicate-searching the Board for `SI 5m ATR Exhaustion`, `AtrExhaustionShort`, `042734`, `042919`, `043220`, and `043313`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/`.
- Downstream fragment root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/downstream-exact-si-5m-atr-exhaustion-short-20260520T042734+0800/`.
- Branch path remains: `TrendExpansion -> AtrExhaustionShort -> ibkr_si5m_atr_exhaustion_short_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 5m`.
- Gate 1 context remains meaningful but non-promoting: retained real IBKR `SI 202607` `5m 1 M`, `6047` normalized rows, `82` ranked trades, and two exact `5m` short variants survived `5bps/side`.
- Downstream command evidence for this fragment: `01_auto_quant_results_import.exit=0`, `02_auto_quant_prior_init.exit=0`, `04_workflow_seed.exit=0`, `05_pre_bayes_seed.exit=0`, `06_export_target_seed.exit=0`, `07_train_ranker.exit=0`, `08_apply_ranker.exit=0`, `09_apply_scores_to_ict.exit=0`, `10_register_trainer.exit=0`, and `11_enable_runtime.exit=0`, but `03_analyze_seed.exit=124`.
- Runtime/readback evidence: no `execution_candidate.json`, no `execution_tree_trace.json`, and `state/IBKR_SI5M_ATR_EXHAUSTION_SHORT_EXACT_DOWNSTREAM_V1/workflow_snapshot.json` reports `latest_execution_candidate=null`, `recent_execution_candidates=[]`, `actionable_artifacts=[]`, `current_regime=null`, `overall_confidence=null`, `pre_bayes_gate_status=null`, `pre_bayes_quality_score=null`, `pre_bayes_multi_timeframe_direction_bias=null`, and `branch_path=null`.
- Terminal decision for this fragment: `si5m_atr_exhaustion_short_same_frame_exact_wrapper_fail_closed_analyze_timeout`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This fragment does not invalidate the Gate 1 survivor, but it also does not satisfy the downstream gates. The same-frame wrapper completed several mechanical import/ranker/runtime-enable steps after an analyze timeout, leaving no execution tree, no candidate, no regime readback, no Pre-Bayes quality, and no path parity evidence. Do not promote this packet, do not call it CatBoost/execution-tree aligned, and do not count it toward the hard conditions (`transition_hazard < 0.60`, `pda_hybrid_alignment=true`, `execution_readiness >= 0.65`). The currently in-flight ladder roots (`042919`, `043220`, `043313`) remain separate and must be judged from their own artifacts once they stop.

## Downstream Final Verdict: IBKR SI 5m ATR Exhaustion Short Exact Replay

Append-only terminal row at `2026-05-20 04:53:00 +0800` after observing pre-write hash `4bec4d266b07c5b842b8bbf568d7cd6ae0008ec2f754dfc13b1dc2c60679429c` and re-checking duplicates for `SI 5m ATR Exhaustion`, `042734`, `043313`, and `exact_si_5m_atr_exhaustion_short_downstream_fail_closed`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/`.
- Completed downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/downstream-exact-si-5m-atr-exhaustion-short-20260520T042734+0800/`.
- Retry/downstream hygiene roots: `.../downstream-exact-si-5m-atr-exhaustion-short-20260520T043313+0800/` reached the same analyze-timeout/readback-null shape and has no terminal metrics file; `042919` and `043220` remained partial with only import/prior-init exits. They do not override the completed `042734` verdict.
- Branch path remains: `TrendExpansion -> AtrExhaustionShort -> ibkr_si5m_atr_exhaustion_short_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 5m`.
- Gate 1 survivor carried into downstream: selected row `SI/atr030_v125_hold30/5m_short`, `21` trades, `33.3333%` win rate, raw `+2.53%`, `1bps/side=+2.11%`, `2bps/side=+1.69%`, `5bps/side=+0.43%`; `survives_2bps_per_side=true`; `survives_5bps_per_side=true`.
- Command evidence: import/prior/workflow/pre-Bayes/export/ranker/apply/register/runtime/policy/export steps exited `0`, but both `03_analyze_seed.exit=124` and `12_analyze_after_ranker.exit=124`; stderr says `TIMEOUT after 300s`. The `043313` retry recorded `03_analyze_seed.exit=124`, `12_analyze_after_ranker.exit=-15`, and `TIMEOUT after 360s` on seed analyze.
- Runtime/readback evidence: no exact execution tree materialized; workflow readbacks still report no workflow snapshot, no execution candidates, no actionable artifacts, no current regime, and Pre-Bayes readbacks remain null (`active_regime=null`, `confidence=null`, `gate_status=null`, empty probabilities). Closed-loop admission is `fail_closed` with `ready=false`, `review_status=observe`, and reason `exact_structural_branch_visible_but_not_ready_or_actionable`.
- Path-ranker evidence: CatBoost/runtime mechanics were exercised, but validation is not usable: `structural_path_ranking_target rows=2`, `history_rows=2`, `mature_rows=0`, `history_mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, `calibration=not_fitted`, `trainer_status=present_validation_insufficient`, and runtime selection is only `enabled_registered_artifact_ready`.
- Terminal metrics: `exact_branch_survived=false`, `all_commands_ok=false`, `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`, `execution_tree_allowed=false`, `promotion_allowed=false`, `trade_usable=false`, `update_goal=false`.
- Terminal decision: `exact_si_5m_atr_exhaustion_short_downstream_fail_closed`.

Interpretation:

- `SI/5m AtrExhaustionShort` is a useful public-family precious-metals Gate 1 survivor, but the exact downstream replay fails the live-practical chain. It has real-cost density, yet it does not preserve enough state through analyze/Pre-Bayes/execution tree to prove direction consistency or readiness. Do not promote it, do not keep relaunching the same downstream wrapper, and do not lower requirements. The next training slot should rotate to a materially different public family with a denser execution-facing path, while keeping this SI packet as observation evidence and a future analyze-timeout repair target only if the runtime path itself is being fixed.

## Gate 1 Negative: IBKR MNQ 1m Mass Index Reversal 7D

Append-only terminal row at `2026-05-20 04:56:00 +0800` after observing pre-write hash `5dfe07167b3a16cfda363ebf52e528d4f56c53e46eb2209aa4742a164d3191d2` and confirming no existing Board row for `Mass Index`, `mass_index_reversal`, or `042506`. A separate Choppiness row was already present above, so this row only records Mass Index.

- Mass Index run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042506+0800-codex-ibkr-mnq1m-mass-index-reversal-7d-gate1-v1/`.
- Provider evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows reused from same-session real provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Mass Index branch path: `FUTURES -> equity_index -> MNQ -> 1m -> RangeExpansion -> MassIndexReversal -> ibkr_mnq1m_mass_index_reversal_7d_gate1_v1`.
- Mass Index cost/density: `rank_rows=4`, `rank_total_trade_count=14`; `MNQ/mi_quality/1m` had `5` trades, raw `-0.10%`, `2bps=-0.30%`, `5bps=-0.60%`; `MNQ/mi_balanced/1m` had `9` trades, raw `-0.34%`, `2bps=-0.70%`, `5bps=-1.24%`; dense/exhaustion-reclaim variants had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision for this exact root: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is a useful diversity negative, not a provider or metadata failure. Public Mass Index reversal received a fair real IBKR `MNQ/1m` Gate 1 attempt but was sparse and raw-negative before friction. Do not rescue this root with overlays or downstream mechanics. The next public-family rotation should favor denser, larger-excursion entries rather than another sparse compression/reversal shape on the same `MNQ/1m` window.

## Gate 1 Negative: IBKR MNQ 1m Klinger Volume Flow 7D

Append-only terminal row at `2026-05-20 05:02:00 +0800` after observing pre-write hash `dbf317a0ccc78e028ba48070a5d381fdabacdb7ba86fad01339137d35aed89e4` and duplicate-searching the Board for `Klinger`, `klinger`, `Volume Flow`, `volume_flow`, and `MNQ 1m Klinger`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T044544+0800-codex-ibkr-mnq1m-klinger-volume-flow-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_klinger_volume_flow_7d_gate1_v1`.
- Branch path: `VolumeFlowExpansion -> KlingerVolumeFlow -> ibkr_mnq1m_klinger_volume_flow_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public Klinger Volume Oscillator / volume-flow continuation family a real exact-root `MNQ/1m` futures attempt, separate from VWAP/RSI/RVOL, opening-range, Donchian/Turtle, Darvas, Keltner/ATR, SuperTrend, Ichimoku, NR7/Crabel, Mass Index, Choppiness, gap fade/go, pivots, PSAR, TRIX/KST/Coppock, Fisher/Stoch, CMO, and initial-balance controls.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows, reused from same-session provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1` with `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=157`.
- Cost/density evidence: density existed, but the direction was wrong before friction. `MNQ/kvo_reclaim/1m` had `44` trades, raw `-0.91%`, `2bps=-2.67%`, `5bps=-5.31%`; `MNQ/kvo_balanced/1m` had `45` trades, raw `-0.68%`, `2bps=-2.48%`, `5bps=-5.18%`; `MNQ/kvo_dense/1m` had `60` trades, raw `-1.62%`, `2bps=-4.02%`, `5bps=-7.62%`; `MNQ/kvo_quality/1m` had `8` trades, raw `-0.61%`, `2bps=-0.93%`, `5bps=-1.41%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, source manifest, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_klinger_volume_flow_7d_gate1_v1.py`.

Interpretation:

- Klinger volume-flow is a good diversity probe because it generated enough trades to evaluate, unlike several sparse compression/reversal attempts. The failure is economic and directional: every exact `1m` variant was raw-negative before costs and worse after friction. Do not send this branch downstream or rescue it with overlays. The useful lesson for the next slot is to preserve density but switch to a family with larger excursion capture or inverse/short logic only if that becomes its own exact rooted branch and earns Gate 1 evidence.

## Artifact Hygiene Blocker: IBKR MNQ 1m Alligator/Fractal Breakout 7D

Append-only diagnostic row at `2026-05-20 05:06:00 +0800` after observing pre-write hash `8ef7fa9249d4a71e122b3e3eb38187a1d7fb4bfed68fc44f9f62c7050b274e3f` and duplicate-searching the Board for `Alligator`, `Fractal`, `alligator_fractal`, `044918`, and `Vortex/VI`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T044918+0800-codex-ibkr-mnq1m-alligator-fractal-breakout-7d-gate1-v1/`.
- Claimed factor id: `ibkr_mnq1m_alligator_fractal_breakout_7d_gate1_v1`.
- Claimed branch path: `FUTURES -> equity_index -> MNQ -> 1m -> TrendExpansion -> BillWilliamsAlligatorFractal -> ibkr_mnq1m_alligator_fractal_breakout_7d_gate1_v1`.
- Hygiene mismatch: the run directory and material JSON claim `MNQ/equity_index/BillWilliamsAlligatorFractal`, but `summaries/provider_provenance_matrix.csv` reports `IBKR,FUT,MGC,precious_metals,COMEX,202606,1m,7 D` and all material JSON files point their `data_path` to `data/provider/normalized/ibkr_mgc_202606_1m_7d.csv`. The copied runner under this run is named `run_ibkr_mgc1m_vortex_trend_continuation_7d_gate1_v1.py`, and the terminal summary header says `Exact MGC 1m Vortex/VI trend-continuation rows`.
- Mechanical result: strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; rank rows completed with only `2` total trades and no cost survivor (`MNQ/fractal_dense/1m` raw `+0.01%`, `1bps=-0.03%`, `2bps=-0.07%`, `5bps=-0.19%`; other variants `0` trades). However, because provenance and branch identity disagree, this is not a clean Alligator/Fractal factor verdict.
- Terminal decision: `artifact_hygiene_blocked_no_clean_factor_verdict`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- Do not count this packet as a valid MNQ Alligator/Fractal negative or a valid MGC Vortex/VI negative. It is useful only as a provenance/branch-identity blocker: the next attempt must rerun the family with coherent `symbol/product/provider/data_path/branch_path` before Gate 1 economics can be trusted. This reinforces the regime-root rule: branch identity is part of the factor, not metadata decoration.

## Gate 1 Negative: IBKR MNQ 1m Alligator/Fractal Breakout 7D Clean Rerun

Append-only terminal row at `2026-05-20 05:20:00 +0800` after observing pre-write hash `8b3304306b95f6ccecd6debec72623b85973cfe36b418a0a9afdfb300970fa96` and confirming the earlier `044918` packet was an artifact-hygiene blocker rather than a clean verdict.

- Clean rerun roots: `support/docs/experiments/actionable-regime-confidence/runs/20260520T045240+0800-codex-ibkr-mnq1m-alligator-fractal-breakout-7d-gate1-v1/` and `support/docs/experiments/actionable-regime-confidence/runs/20260520T045405+0800-codex-ibkr-mnq1m-alligator-fractal-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_alligator_fractal_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> BillWilliamsAlligatorFractal -> ibkr_mnq1m_alligator_fractal_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from same-session provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`, `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=2`.
- Cost/density evidence: `MNQ/fractal_dense/1m` had only `2` trades, raw `+0.01%`, `1bps=-0.03%`, `2bps=-0.07%`, `5bps=-0.19%`; `MNQ/fractal_balanced/1m`, `MNQ/fractal_quality/1m`, and `MNQ/fractal_retest/1m` each had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The clean Alligator/Fractal rerun fixes the earlier branch/data mismatch, but the exact-root economics still fail at Gate 1. It is too sparse and loses money once even `1bps/side` is applied. Do not downstream it or rescue it with overlays. Preserve it as a valid public-family negative and keep rotating toward denser, larger-excursion `1m` families or exact short/inverse variants that earn their own cost-stressed evidence.

## Gate 1 Negative: IBKR MNQ 1m Klinger Volume Flow Short 7D

Append-only terminal row at `2026-05-20 05:18:00 +0800` after observing pre-write hash `8b3304306b95f6ccecd6debec72623b85973cfe36b418a0a9afdfb300970fa96` and duplicate-searching the Board for `Klinger Volume Flow Short`, `klinger_volume_flow_short`, `045216`, and `kvo_short`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T045216+0800-codex-ibkr-mnq1m-klinger-volume-flow-short-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_klinger_volume_flow_short_7d_gate1_v1`.
- Branch path: `VolumeFlowExpansion -> KlingerVolumeFlowShort -> ibkr_mnq1m_klinger_volume_flow_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows, reused from same-session provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1` with `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=109`.
- Cost/density evidence: the inverse/short Klinger branch improved raw direction for only one row, but still failed practical friction. `MNQ/kvo_short_quality/1m_short` had `32` trades, raw `+1.04%`, `1bps=+0.40%`, `2bps=-0.24%`, `5bps=-2.16%`; `MNQ/kvo_short_reject/1m_short` had `28` trades, raw `-0.63%`, `2bps=-1.75%`, `5bps=-3.43%`; `MNQ/kvo_short_dense/1m_short` had `13` trades, raw `-0.81%`, `2bps=-1.33%`, `5bps=-2.11%`; `MNQ/kvo_short_balanced/1m_short` had `36` trades, raw `-0.46%`, `2bps=-1.90%`, `5bps=-4.06%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, source manifest, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_klinger_volume_flow_short_7d_gate1_v1.py`.

Interpretation:

- This clean exact-root short-side Klinger attempt confirms the long-side Klinger failure was not just direction selection. It reached enough density to evaluate, but the best row flipped negative by `2bps/side` and all rows failed `5bps/side`. Do not downstream this branch or relax the cost gate. Preserve it as a public-family diversity negative and rotate the next clean attempt toward a larger-excursion family with coherent provenance.

## Gate 1 Negative: IBKR MNQ 1m Ease of Movement Displacement 7D

Append-only terminal row at `2026-05-20 05:28:00 +0800` after observing pre-write hash `4121024ca5a1f3723cb89194f71fdbf86de5bfc39a5763ff63bc82b7e67ee4d6` and duplicate-searching the Board for `EOM Displacement`, `EaseOfMovementDisplacement`, `eom_displacement`, `050238`, `050444`, and `eom_balanced`.

- Run roots: `support/docs/experiments/actionable-regime-confidence/runs/20260520T050238+0800-codex-ibkr-mnq1m-eom-displacement-7d-gate1-v1/` and `support/docs/experiments/actionable-regime-confidence/runs/20260520T050444+0800-codex-ibkr-mnq1m-eom-displacement-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_eom_displacement_7d_gate1_v1`.
- Branch path: `DisplacementExpansion -> EaseOfMovementDisplacement -> ibkr_mnq1m_eom_displacement_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public Ease of Movement displacement family a clean exact-root `MNQ/1m` futures attempt, separate from Klinger, Alligator/Fractal, Mass Index, Choppiness, KST/Coppock, Heikin-Ashi/ATR, VWAP/RVOL, Donchian/Turtle, Ichimoku, SuperTrend, pivots, and initial-balance families.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows, reused from same-session provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1` with `local_cache_replay=false_retained_real_ibkr_same_session` / `false`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=203`.
- Cost/density evidence: density was excellent, but every exact `1m` row was raw-negative before costs. `MNQ/eom_balanced/1m` had `49` trades, raw `-0.63%`, `1bps=-1.61%`, `2bps=-2.59%`, `5bps=-5.53%`; `MNQ/eom_dense/1m` had `59` trades, raw `-0.71%`, `2bps=-3.07%`, `5bps=-6.61%`; `MNQ/eom_pullback/1m` had `53` trades, raw `-0.25%`, `2bps=-2.37%`, `5bps=-5.55%`; `MNQ/eom_quality/1m` had `42` trades, raw `-0.88%`, `2bps=-2.56%`, `5bps=-5.08%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, source manifest, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_eom_displacement_7d_gate1_v1.py`.

Interpretation:

- Ease of Movement displacement got a fair, dense, public-family Gate 1 attempt on real retained IBKR `MNQ/1m` rows. This is not a provider or sparsity failure; it is a directional/economic failure because all rows were negative before friction and degraded further under costs. Do not downstream this root. Preserve it as evidence that high-density public families are being tried, but rotate the next slot toward a materially different larger-excursion or opposite-direction family rather than another volume/displacement continuation clone.

## Provider Blocker: IBKR MCL 1m Donchian/ATR Breakout 7D

Append-only blocker row at `2026-05-20 05:23:00 +0800` after observing pre-write hash `4121024ca5a1f3723cb89194f71fdbf86de5bfc39a5763ff63bc82b7e67ee4d6` and duplicate-searching the Board for `MCL 1m Donchian`, `mcl1m_donchian`, `043259`, and `provider_window_blocked_no_factor_verdict`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T043259+0800-codex-ibkr-mcl1m-donchian-atr-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mcl1m_donchian_atr_breakout_7d_gate1_v1`.
- Intended branch path: `FUTURES -> energy -> MCL -> 1m -> TrendExpansion -> DonchianAtrBreakout -> ibkr_mcl1m_donchian_atr_breakout_7d_gate1_v1`.
- Provider evidence: intended real IBKR `MCL 202606` `1m 7 D`, but normalized provider rows were `0`; `00_fetch_ibkr_mcl_1m_7d.exit=1`.
- Fetch blocker: stderr reports repeated `positions request timed out`, then `Cannot reach IBKR Gateway at 127.0.0.1:4002 (clientId=947)` with all candidate clientIds already in use: `947, 1047, 1048, 1049, 1050, 1051`.
- Gate status: no Auto-Quant rank rows were produced; `rank_rows=0`; `rank_total_trade_count=0`; `branch_fields_preserved=false`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `provider_window_blocked_no_factor_verdict`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/00_fetch_ibkr_mcl_1m_7d.exit`, and `command-output/00_fetch_ibkr_mcl_1m_7d.err`.

Interpretation:

- This is not a clean Donchian/ATR factor verdict. The MCL energy branch is still untested because the IBKR fetch never produced rows. Do not mark this as strategy-negative or downstream it. A future MCL attempt needs either a free IBKR clientId/Gateway session or retained real MCL rows before Auto-Quant Gate 1 economics can be judged.

## Gate 1 Negative: IBKR MYM 1m Liquidity-Sweep Reject-Short 7D

Append-only terminal row at `2026-05-20 05:31:00 +0800` after observing pre-write hash `8e04101694a72b537ad79eada0348d68551e70ac757781d3c52c58c943fee185` and duplicate-searching the Board for `MYM 1m Liquidity`, `mym1m_liquidity`, `023551`, and `LiquiditySweepRejectShort -> ibkr_mym`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T023551+0800-codex-ibkr-mym1m-liquidity-sweep-reject-short-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_liquidity_sweep_reject_short_7d_gate1_v1`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_mym1m_liquidity_sweep_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Provider/Gate 1 evidence: real IBKR `MYM 202606` `1m 7 D`, `9506` normalized rows, `local_cache_replay=false`. Provider status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=3`; `rank_total_trade_count=115`.
- Cost/density evidence: density was usable, but all exact `1m` short rows were negative before and after costs. `MYM/quality/1m_short` had `27` trades, raw `-0.21%`, `1bps=-0.75%`, `2bps=-1.29%`, `5bps=-2.91%`; `MYM/dense/1m_short` had `48` trades, raw `-0.88%`, `2bps=-2.80%`, `5bps=-5.68%`; `MYM/balanced/1m_short` had `40` trades, raw `-0.75%`, `2bps=-2.35%`, `5bps=-4.75%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_liquidity_sweep_reject_short_7d_gate1_v1.py`.

Interpretation:

- The Micro Dow liquidity-sweep reject-short branch received a fair exact-root IBKR Gate 1 attempt and failed on economics, not provider mechanics or sparsity. This reinforces that the stronger M2K liquidity-sweep survivor did not transfer directly to MYM. Do not downstream this MYM branch or rescue it with overlays; keep it as cross-instrument negative evidence and rotate to a different root family or back to exact execution repair only when the root has real-cost survivors.

## Gate 1 Negative: IBKR MNQ 1m Ultimate/Williams Reclaim 7D

Append-only terminal row at `2026-05-20 05:33:00 +0800` after observing pre-write hash `8e04101694a72b537ad79eada0348d68551e70ac757781d3c52c58c943fee185` and duplicate-searching the Board for `Ultimate/Williams`, `UltimateWilliams`, `ultimate_williams`, `050957`, and `uo_balanced`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T050957+0800-codex-ibkr-mnq1m-ultimate-williams-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_ultimate_williams_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> UltimateWilliamsReclaim -> ibkr_mnq1m_ultimate_williams_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public Ultimate Oscillator / Williams %R reclaim family a real exact-root `MNQ/1m` futures attempt, separate from VWAP/RSI/RVOL, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend, Ichimoku, NR7/Crabel, Darvas, CMF/OBV, KST/Coppock, Heikin-Ashi/ATR, Klinger, EOM, and Alligator/Fractal controls.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows, reused from same-session provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1` with `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=155`.
- Cost/density evidence: density was usable, but the edge sat inside the cost envelope. `MNQ/uo_balanced/1m` had `43` trades, raw `+0.17%`, `1bps=-0.69%`, `2bps=-1.55%`, `5bps=-4.13%`; `MNQ/uo_dense/1m` had `57` trades, raw `-0.16%`, `2bps=-2.44%`, `5bps=-5.86%`; `MNQ/willr_flush_reclaim/1m` had `43` trades, raw `-0.03%`, `2bps=-1.75%`, `5bps=-4.33%`; `MNQ/uo_quality/1m` had `12` trades, raw `-0.20%`, `2bps=-0.68%`, `5bps=-1.40%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, source manifest, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_ultimate_williams_reclaim_7d_gate1_v1.py`.

Interpretation:

- Ultimate/Williams reclaim got a fair public-family Gate 1 attempt with real IBKR `MNQ/1m` rows and enough trades to judge. It is not a provider or metadata failure, but it fails practical costs because the only raw-positive row turns negative at `1bps/side` and every row fails `2bps/side` and `5bps/side`. Do not downstream or rescue this exact branch with overlays. Keep rotating the public-family queue toward materially different, larger-excursion setups rather than returning to the familiar VWAP/RSI/RVOL cluster.

## Gate 1 Negative: IBKR MNQ 1m Qstick Body Momentum 7D

Append-only terminal row at `2026-05-20 05:38:00 +0800` after observing pre-write hash `9529444b10bed77ab578216960636df1e3060695741d1e77125530a1eb12a62e` and duplicate-searching the Board for `Qstick`, `qstick`, `051339`, and `CandleBodyMomentum`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T051339+0800-codex-ibkr-mnq1m-qstick-body-momentum-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_qstick_body_momentum_7d_gate1_v1`.
- Branch path: `CandleBodyMomentum -> QstickBodyMomentum -> ibkr_mnq1m_qstick_body_momentum_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public Qstick candle-body momentum family a real exact-root `MNQ/1m` futures attempt, separate from oscillator reclaim, volume-flow/displacement, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend, Ichimoku, NR7/Crabel, Darvas, CMF/OBV, KST/Coppock, Heikin-Ashi/ATR, Klinger, EOM, and Alligator/Fractal controls.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows, reused from same-session provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1` with `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=177`.
- Cost/density evidence: density was usable, but the gross edge was too thin for costs. `MNQ/qstick_quality/1m` had `41` trades, raw `+0.40%`, `1bps=-0.42%`, `2bps=-1.24%`, `5bps=-3.70%`; `MNQ/qstick_dense/1m` had `65` trades, raw `-0.19%`, `2bps=-2.79%`, `5bps=-6.69%`; `MNQ/qstick_balanced/1m` had `54` trades, raw `+0.05%`, `2bps=-2.11%`, `5bps=-5.35%`; `MNQ/qstick_reversal/1m` had `17` trades, raw `-0.03%`, `2bps=-0.71%`, `5bps=-1.73%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, source manifest, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_qstick_body_momentum_7d_gate1_v1.py`.

Interpretation:

- Qstick body momentum got a fair public-family Gate 1 attempt with real IBKR `MNQ/1m` rows and enough trades to judge. It is not a provider, branch metadata, or sparsity failure; the best row was only `+0.40%` raw and flipped negative by `1bps/side`, with no `2bps/side` or `5bps/side` survivor. Do not downstream this root or lower costs to pass it. Preserve it as diversity evidence and keep rotating toward larger-excursion or opposite-direction families that can survive real costs before entering Pre-Bayes/BBN/CatBoost/execution-tree.

## Gate 1 Negative: IBKR MNQ 1m Initial Balance Breakout/Fade 7D

Append-only terminal row at `2026-05-20 05:40:00 +0800` after observing pre-write hash `a885c27c3666b7a35d6ecd8ec30300b0ce43c94311287f6d2b2ee8cb74805a4c` and duplicate-searching the Board for `Initial Balance Breakout`, `initial_balance_breakout`, `005529`, `AuctionMarketStructure`, and `ib45_failed_break_fade`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T005529+0800-codex-ibkr-mnq1m-initial-balance-breakout-fade-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_initial_balance_breakout_fade_7d_gate1_v1`.
- Branch path: `AuctionMarketStructure -> InitialBalanceBreakoutFade -> ibkr_mnq1m_initial_balance_breakout_fade_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: real IBKR `MNQ 202606` `1m 7 D`, `9269` normalized rows from same-session source `/tmp/ict-mnq-1m-7d.csv`, `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=0`.
- Cost/density evidence: all exact `1m` auction-market rows produced zero trades: `MNQ/ib30_breakout_dense/1m`, `MNQ/ib45_breakout_balanced/1m`, `MNQ/ib60_breakout_quality/1m`, and `MNQ/ib45_failed_break_fade/1m` each had `0` trades and no cost-stressed survivor. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_initial_balance_breakout_fade_7d_gate1_v1.py`.

Interpretation:

- Initial Balance breakout/fade got a clean public-family exact-root attempt, but this implementation is too restrictive for the retained `MNQ/1m` window and fails the density requirement before costs can matter. Do not downstream this root and do not count it as trade-usable. If revisiting auction-market structure, use a materially denser opening-drive or acceptance/rejection variant with broader session windows rather than retuning this zero-trade branch.

## Gate 1 Negative: IBKR MNQ 1m TD Sequential Exhaustion 7D

Append-only terminal row at `2026-05-20 05:43:00 +0800` after observing pre-write hash `d0dc34792046807a72a617159fd134b9aa4aa6a084a0004dfadc6bb1c53b9962` and duplicate-searching the Board for `TD Sequential`, `td_sequential`, `052455`, and `SequentialExhaustion`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T052455+0800-codex-ibkr-mnq1m-td-sequential-exhaustion-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_td_sequential_exhaustion_7d_gate1_v1`.
- Branch path: `ExhaustionReversal -> TdSequentialExhaustion -> ibkr_mnq1m_td_sequential_exhaustion_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public DeMark TD Sequential exhaustion family a real exact-root `MNQ/1m` futures attempt, separate from candle-body momentum, oscillator reclaim, volume-flow/displacement, opening-range/initial-balance, Donchian/Turtle, Keltner/ATR, SuperTrend, Ichimoku, NR7/Crabel, Darvas, CMF/OBV, KST/Coppock, and Heikin-Ashi/ATR controls.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows, reused from same-session provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1` with `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=4`.
- Cost/density evidence: the family failed density before any downstream gate. `MNQ/td13_reversal_quality/1m` and `MNQ/td_combo_wide/1m` had `0` trades; `MNQ/td9_reclaim_dense/1m` and `MNQ/td9_reclaim_balanced/1m` each had `2` trades, raw `-0.39%`, `1bps=-0.43%`, `2bps=-0.47%`, `5bps=-0.59%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, source manifest, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_td_sequential_exhaustion_7d_gate1_v1.py`.

Interpretation:

- TD Sequential exhaustion got a clean public-family Gate 1 attempt, but it is too sparse on this exact `MNQ/1m` branch and the only nonzero rows are raw-negative. This is a valid diversity negative, not a provider or branch-identity failure. Do not downstream or rescue this exact root with overlays; the next public-family slot should favor denser, larger-excursion entries or move to an existing `5bps/side` survivor for same-root execution-readiness repair.

## Repeat Provider Blocker: IBKR MCL 1m Donchian/ATR Breakout 7D

Append-only blocker row at `2026-05-20 05:37:12 +0800` after observing pre-write hash `0b42b7d69725fe428d13714d5a7489f6105eccaf03951cf3af6ca0305e99a01d` and duplicate-searching the Board for `052641`, `Repeat Provider Blocker: IBKR MCL`, and `MCL 052641`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T052641+0800-codex-ibkr-mcl1m-donchian-atr-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mcl1m_donchian_atr_breakout_7d_gate1_v1`.
- Intended branch path: `FUTURES -> energy -> MCL -> 1m -> TrendExpansion -> DonchianAtrBreakout -> ibkr_mcl1m_donchian_atr_breakout_7d_gate1_v1`.
- Provider evidence: intended real IBKR `MCL 202606` `1m 7 D`, but normalized provider rows were again `0`; `00_fetch_ibkr_mcl_1m_7d.exit=1`; `local_cache_replay=false`.
- Fetch blocker: stderr again reports repeated `positions request timed out`, then `Cannot reach IBKR Gateway at 127.0.0.1:4002 (clientId=947)` because all candidate clientIds were already in use: `947, 1047, 1048, 1049, 1050, 1051`.
- Gate status: no Auto-Quant rank rows were produced; `rank_rows=0`; `rank_total_trade_count=0`; `branch_fields_preserved=false`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `provider_window_blocked_no_factor_verdict`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/00_fetch_ibkr_mcl_1m_7d.exit`, and `command-output/00_fetch_ibkr_mcl_1m_7d.err`.

Interpretation:

- This repeat run confirms the MCL Donchian/ATR branch remains provider-window blocked, not strategy-negative. Do not count the energy/MCL root as tested until a Gateway window or retained real MCL rows produce nonzero provider rows; rotate live training toward available retained futures/metals rows or a free IBKR clientId rather than fabricating a factor verdict from this blocked window.

## Gate 1 Negative: IBKR MGC 1m Vortex/VI Trend Continuation 7D Clean Rerun

Append-only terminal row at `2026-05-20 05:48:00 +0800` after observing pre-write hash `20a6f868eb21994372c7007f0f3dc349aac667c8b030524761249f71b55a751d` and duplicate-searching the Board for `VortexTrendContinuation`, `ibkr_mgc1m_vortex_trend_continuation_7d_gate1_v1`, `054538`, `MGC 1m Vortex`, and `Vortex/VI trend-continuation`. The earlier `044918` Alligator/Fractal packet remains a provenance-hygiene blocker and is not counted as a clean MGC Vortex verdict.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T054538+0800-codex-ibkr-mgc1m-vortex-trend-continuation-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_vortex_trend_continuation_7d_gate1_v1`.
- Branch path: `TrendExpansion -> VortexTrendContinuation -> ibkr_mgc1m_vortex_trend_continuation_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives the public Vortex Indicator / VI trend-continuation family a clean exact-root micro-gold `1m` futures attempt, separate from VWAP/RSI/RVOL, Donchian/Turtle, Keltner/ATR, SuperTrend/ADX, PSAR, Connors RSI2, Williams/MFI, Aroon/CCI, Elder/MACD, pivots/CPR, and initial-balance families.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows, sourced from same-session provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=131`.
- Cost/density evidence: density was sufficient to judge, but the edge was inside practical costs. `MGC/vi_reclaim/1m` had `35` trades, raw `+0.11%`, `1bps=-0.59%`, `2bps=-1.29%`, `5bps=-3.39%`; `MGC/vi_quality/1m` had `16` trades, raw `+0.51%`, `1bps=+0.19%`, `2bps=-0.13%`, `5bps=-1.09%`; `MGC/vi_dense/1m` had `47` trades, raw `+0.20%`, `2bps=-1.68%`, `5bps=-4.50%`; `MGC/vi_balanced/1m` had `33` trades, raw `-0.69%`, `2bps=-2.01%`, `5bps=-3.99%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_vortex_trend_continuation_7d_gate1_v1.py`.

Interpretation:

- Vortex/VI now has a clean public-family Gate 1 verdict on coherent `MGC/precious_metals/1m` branch metadata. The provider and Auto-Quant mechanics worked and trade density was not the blocker, but no row survived `2bps/side` or `5bps/side`, so this branch must stop as observation evidence. Do not send it to Pre-Bayes/BBN/CatBoost/execution-tree and do not lower the cost gate. Continue rotating public families or repair an existing exact-root `5bps/side` survivor's execution predicates instead of stacking overlays on this Vortex root.

## Gate 1 Negative: IBKR SI 1m Wide-Excursion Reclaim 7D

Append-only terminal row at `2026-05-20 05:48:11 +0800` after observing pre-write hash `9bd3dbdc6897cafc99beaecbbdd5293a0e5c2202fff735c227b5303f2814a2a8` and duplicate-searching the Board for `053730`, `SI 1m wide-excursion`, and `WideExcursionReclaim`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T053730+0800-codex-ibkr-si1m-wide-excursion-reclaim-retained-7d-gate1-v1/`.
- Factor id: `ibkr_si1m_wide_excursion_reclaim_retained_7d_gate1_v1`.
- Branch path: `TrendExpansion -> WideExcursionReclaim -> ibkr_si1m_wide_excursion_reclaim_retained_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `SI 202607` `1m 7 D`, `9453` normalized rows, `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=66`.
- Cost/density evidence: density was enough to judge, but no exact `1m` row survived practical costs. `SI/late_hold/1m` had `15` trades, raw `+0.30%`, `1bps=0.00%`, `2bps=-0.30%`, `5bps=-1.20%`; `SI/wide_quality/1m` had `15` trades, raw `+0.17%`, `1bps=-0.13%`, `2bps=-0.43%`, `5bps=-1.33%`; `SI/wide_balanced/1m` had `20` trades, raw `-0.32%`, `2bps=-1.12%`, `5bps=-2.32%`; `SI/session_impulse/1m` had `16` trades, raw `-2.32%`, `2bps=-2.96%`, `5bps=-3.92%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si1m_wide_excursion_reclaim_retained_7d_gate1_v1.py`.

Interpretation:

- The silver `1m` wide-excursion reclaim branch used real retained IBKR rows and preserved the full regime-rooted branch path, but the raw edge was too small for costs. This is a clean Gate 1 observation negative, not a provider failure. Do not downstream it or combine overlays on this root until a same-root row survives real-cost gates with stronger per-trade excursion.

## Gate 1 Observation: IBKR MNQ 5m Linear-Regression Channel Breakout 10D

Append-only observation row at `2026-05-20 05:48:11 +0800` after observing pre-write hash `9bd3dbdc6897cafc99beaecbbdd5293a0e5c2202fff735c227b5303f2814a2a8` and duplicate-searching the Board for `054532`, `053733`, `MNQ 5m Linear`, and `LinearRegressionChannelBreakout`.

- Run roots: `support/docs/experiments/actionable-regime-confidence/runs/20260520T053733+0800-codex-ibkr-mnq5m-linreg-channel-breakout-10d-gate1-v1/` and matching rerun `support/docs/experiments/actionable-regime-confidence/runs/20260520T054532+0800-codex-ibkr-mnq5m-linreg-channel-breakout-10d-gate1-v1/`.
- Factor id: `ibkr_mnq5m_linreg_channel_breakout_10d_gate1_v1`.
- Branch path: `TrendChannelExpansion -> LinearRegressionChannelBreakout -> ibkr_mnq5m_linreg_channel_breakout_10d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 5m`.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `5m 10 D`, `2679` normalized rows from source provider root `20260519T222102+0800-codex-ibkr-futures-micro-trend-pullback-reclaim-1m-mtf-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=19`.
- Cost/density evidence: one exact `5m` row survived only the `2bps/side` screen, not the hard `5bps/side` promotion screen. `MNQ/linreg_retest/5m` had `3` trades, raw `+0.45%`, `1bps=+0.39%`, `2bps=+0.33%`, `5bps=+0.15%`, but was too sparse to count as survivor; `MNQ/linreg_dense/5m` had `7` trades, raw `+0.36%`, `1bps=+0.22%`, `2bps=+0.08%`, `5bps=-0.34%`, and is `exact_5m_survivors_2bps=['MNQ/linreg_dense/5m']`; `MNQ/linreg_balanced/5m` had `5` trades, raw `-0.09%`; `MNQ/linreg_quality/5m` had `4` trades, raw `-0.75%`. `exact_5m_survivors_5bps=[]`.
- Gate flags: `downstream_allowed=true`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=true`; `execution_tree_allowed=true`; but `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq5m_linreg_channel_breakout_10d_gate1_v1.py`.

Interpretation:

- Linear-regression channel breakout is the first fresh MNQ `5m` branch in this slice that clears a lower `2bps/side` observation gate and preserves exact branch identity, so it is worth downstream inspection as a diagnostic candidate. It still fails the user's hard retention standard because no dense row survives `5bps/side`, total trade density is only `19` trades over `10D`, and no final execution-tree evidence yet proves `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, or stable `execution_readiness >= 0.65`. Treat it as an observation/downstream-debug candidate, not a trade-usable profitable factor.

## Downstream Addendum: IBKR SI 5m ATR Exhaustion Short Alt Simulated-Trade Admission

Append-only terminal row at `2026-05-20 06:02:00 +0800` after observing pre-write hash `c6551b7d575f6fa1791f90c6ce76971006febb2510b42328efb72349eff41124` and duplicate-searching the Board for `053245`, `SI5M_ATR_EXHAUSTION_SHORT_ALT`, and `simulated-trade-admission-si-5m-atr-exhaustion-short-alt-survivor`.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/`.
- Simulated-admission root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/simulated-trade-admission-si-5m-atr-exhaustion-short-alt-survivor-20260520T053245+0800/`.
- Branch path: `TrendExpansion -> AtrExhaustionShort -> ibkr_si5m_atr_exhaustion_short_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 5m`.
- Selected Gate 1 row: `IBKR SI ATR exhaustion short atr050_v100_hold30 5m 1M`, `16` trades, raw `+1.89%`, win rate `43.75%`; same Auto-Quant workspace exported `16` simulated trades (`7` wins / `9` losses / `0` breakevens).
- Command evidence: `01_auto_quant_results_import` through `19_policy_after_ranker` all exited `0`; `all_command_exits_zero=true`; `exact_branch_survived=true`; `timed_out_commands=[]`.
- Ladder/readiness evidence: readback covered `5m`, `15m`, and `1h` (`6047`, `2016`, and `504` bars) but reported missing `1m`, `30m`, `4h`, and `1d`; `mature_rows=2`; `history_mature_rows=17`; `ranker_validation_ready=true`; `path_ranker_score_visible_to_execution_tree=true`; `path_ranker_score_used_by_execution_tree=false`.
- Execution evidence: `execution_candidate.json` reports `candidate_status=no_trade`, `actionable=false`, selected/trade direction `Bear`. Terminal metrics report `execution_gate_status=observe`, `execution_readiness=0.2344186944501619`, `transition_hazard=0.9679827616849933`, and `pda_hybrid_alignment=false`; execution tree closed-loop admission remains `status=fail_closed`, reason `exact_structural_branch_visible_but_not_ready_or_actionable`, hint `execution_guarded_due_to_pda_hybrid_disagreement`.
- Terminal decision: `si5m_atr_exhaustion_short_simulated_trade_admission_fail_closed`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=true`; `execution_tree_allowed=true`; but `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This SI `5m` ATR exhaustion short alt survivor successfully exercised the rooted downstream chain and repaired the prior incomplete readback, but it still fails the practical execution predicates by a wide margin. The branch is not blocked by command failure or branch metadata; it is blocked by current execution quality: high transition hazard, PDA/hybrid disagreement, low execution readiness, no executable candidate, and incomplete full-ladder context. Preserve it as observation/repair evidence only. Do not lower the hard gates or add another simulated-feedback loop unless the next hypothesis directly reduces `transition_hazard`, aligns PDA, and raises current execution readiness on the same rooted path.

## Downstream Addendum: IBKR MNQ 5m Linear-Regression Channel Breakout Exact Downstream

Append-only terminal row at `2026-05-20 06:02:00 +0800` after observing pre-write hash `c6551b7d575f6fa1791f90c6ce76971006febb2510b42328efb72349eff41124` and duplicate-searching the Board for `downstream-exact-mnq-5m-linreg-channel-breakout-20260520T055304`, `MNQ5M_LINREG_CHANNEL_BREAKOUT_DOWNSTREAM`, and `Downstream Addendum: IBKR MNQ 5m Linear`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T054532+0800-codex-ibkr-mnq5m-linreg-channel-breakout-10d-gate1-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T054532+0800-codex-ibkr-mnq5m-linreg-channel-breakout-10d-gate1-v1/downstream-exact-mnq-5m-linreg-channel-breakout-20260520T055304+0800/`.
- Branch path: `TrendChannelExpansion -> LinearRegressionChannelBreakout -> ibkr_mnq5m_linreg_channel_breakout_10d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 5m`.
- Gate 1 reminder: retained real IBKR `MNQ 202606` `5m 10 D`, `2679` rows; exact branch identity preserved; only `MNQ/linreg_dense/5m` survived `2bps/side`; no `5bps/side` survivor, so this was downstream diagnostic evidence rather than promotion evidence from the start.
- Command evidence: downstream checks `01_auto_quant_results_import` through `16_export_target_final` all exited `0`; execution candidate/tree artifacts and structural target summary materialized.
- Structural/ranker evidence: `mature_rows=0`; `history_mature_rows=0`; `rows=4`; `history_rows=6`; no calibrated rows or execution-gate rows were available for mature training.
- Execution evidence: `execution_candidate.json` reports `candidate_status=no_trade`, `actionable=false`, selected/trade direction `Bear`. `execution_tree_trace.json` reports closed-loop admission `status=fail_closed`, `candidate_status=execution_blocked`, `execution_gate_status=execution_blocked`, `execution_tree_gate_status=blocked`, branch `block_crowded`, decision hint `execution_blocked_regardless_of_prediction`, and reason `exact_structural_branch_visible_but_not_ready_or_actionable`. The hard predicate fields were not promotable in this trace: no positive `execution_readiness`, `transition_hazard`, or `pda_hybrid_alignment` pass was produced.
- Terminal decision: `mnq5m_linreg_channel_breakout_exact_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The MNQ `5m` linear-regression channel branch earned a downstream diagnostic because it preserved branch identity and had a small `2bps/side` survivor, but the exact downstream confirms it is not practical. It has no `5bps/side` cost survivor, no mature rows, no trade candidate, and the execution tree blocks it as crowded/observe-only. Keep it as public-family diversity evidence and do not promote or keep tuning this exact branch unless a future variant first survives the real-cost density gate and then produces mature same-root execution feedback.

## Gate 1 Negative: IBKR MNQ 30m/1h Short Failed Expansion Fade

Append-only terminal row at `2026-05-20 05:56:05 +0800` after observing pre-write hash `c6551b7d575f6fa1791f90c6ce76971006febb2510b42328efb72349eff41124` and duplicate-searching the Board for `055605`, `Short Failed Expansion Fade`, `short_failed_expansion_fade`, and `FailedBreakoutFade`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T055605+0800-hermes-ibkr-mnq-short-failed-expansion-fade-30m-1h-gate1-v1/`.
- Factor id: `ibkr_mnq_short_failed_expansion_fade_gate1_v1`.
- Branch paths: `TrendExpansion -> FailedBreakoutFade -> ibkr_mnq_short_failed_expansion_fade_gate1_v1` and `TrendExpansion -> FailedBreakoutFade -> ibkr_mnq_short_failed_expansion_fade_gate1_v1`. Labels/provenance prefixes: `FUTURES -> equity_index -> MNQ -> 30m`; `FUTURES -> equity_index -> MNQ -> 1h`.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` rows from source provider root `20260519T220712+0800-codex-ibkr-futures-index-precious-metals-opening-vwap-rvol-reclaim-1m-mtf-gate1-v1`; `30m` had `998` rows and `1h` had `499` rows; `local_cache_replay=false_source_provider_root_reused`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `material_count=6`; `rank_rows=6`; `rank_total_trade_count=2`.
- Cost/density evidence: the branch failed density and direction before downstream gates. Four rows had `0` trades. The only nonzero rows were `30m-atr_impulse_continuation` and `30m-quality_expansion`, each with `1` trade, raw `-0.15%`, `1bps=-0.17%`, `2bps=-0.19%`, and `5bps=-0.25%`. `survivors_2bps=[]`; `survivors_5bps=[]`.
- Terminal decision: `drop_gate1_no_cost_density`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, provider normalized CSVs, copied runner under `scripts/`, and command outputs. This runner does not emit `summaries/rank_rows.csv`; the terminal JSON summary is the authoritative row-level artifact for this packet.

Interpretation:

- This was a deliberately different, lower-turnover MNQ failed-expansion fade attempt using real retained `30m/1h` futures rows to seek larger per-trade distance. It did not solve the profitability gate: the exact rooted paths were preserved and AQ completed, but trade density collapsed to two losing trades total. Do not downstream it or stack overlays; it is a clean observation negative that pushes the next search away from restrictive failed-breakout fades unless the entry is made substantially denser without lowering cost gates.

## Downstream Addendum: IBKR M2K 1m Liquidity-Sweep Reject-Short RVol/PDA Guard 05:45 Admission

Append-only terminal row at `2026-05-20 06:08:00 +0800` after observing pre-write hash `31b2306ea736ed5f6e74d0ea47d5febd6abaa4433da1b7a822bb20b1956219ef` and duplicate-searching the Board for `054532+0800.*m2k`, `simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T054532`, and `M2K1M_LIQUIDITY_SWEEP_REJECT_SHORT_RVOL_PDA_GUARD_SIM_TRADE_ADMISSION`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Simulated-admission root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T054532+0800/`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Gate 1 authority: retained real IBKR `M2K 202606` `1m 7 D`; the RVol/PDA guard packet had four exact `5bps/side` survivors and remains the strongest same-root futures repair lead, not a trade-ready factor.
- Simulated feedback evidence: `19` same-Auto-Quant-workspace simulated trades were ingested (`12` wins / `7` losses / `0` breakevens); `mature_rows=2`; `history_mature_rows=20`; `ranker_validation_ready=true`.
- Command evidence: `01_auto_quant_results_import` through `19_policy_after_ranker` all exited `0`; `all_command_exits_zero=true`; `exact_branch_survived=true`; `timed_out_commands=[]`.
- MTF/readiness evidence: analyze readback covered only `1m`, `15m`, and `1h` (`9392`, `627`, and `157` bars) and still reported missing `5m`, `30m`, `4h`, and `1d`; `higher_timeframe_direction_bias=bearish`; `lower_timeframe_entry_alignment_score=0.9649`.
- Execution evidence: `execution_candidate.json` reports `candidate_status=no_trade`, `actionable=false`, selected/trade direction `Bear`. Terminal metrics report `execution_gate_status=observe`, `execution_readiness=0.3180972047048167`, `transition_hazard=0.9184975817511946`, `pda_hybrid_alignment=false`, `path_ranker_score_visible_to_execution_tree=true`, and `path_ranker_score_used_by_execution_tree=false`; execution tree closed-loop admission remains `status=fail_closed`, reason `exact_structural_branch_visible_but_not_ready_or_actionable`, hint `execution_guarded_due_to_pda_hybrid_disagreement`.
- Terminal decision: `rvol_pda_guard_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This 05:45 rerun is now a clean completed downstream packet rather than an interrupted wrapper artifact: every command exited `0`, the exact branch survived, and the same-root simulated feedback was visible. It still fails the user's practical retention standard. The blocker is not Auto-Quant economics but execution admission: transition hazard is far above `<0.60`, PDA/hybrid alignment is false, execution readiness is far below `>=0.65`, and the ranker score is visible but not used by the execution tree. Do not launch another light RVol/VWAP/liquidity overlay for this root; the next aligned repair must directly change current-state/PDA agreement or materialize a true RangeReversion execution window, otherwise pivot to a fresh rooted TrendExpansion factor that matches the live execution state.

## Gate 1 Negative: IBKR MNQ 30m/1h Low-Turnover Volatility Expansion

Append-only terminal row at `2026-05-20 06:23:00 +0800` after observing pre-write hash `10f894997110ea8f517d017f9533403d4c1c483e196c8b64ff20dba6633d3000` and duplicate-searching the Board for `061638`, `low-turnover volatility expansion`, `low_turnover_vol_expansion`, and `VolatilityCompressionExpansion`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T061638+0800-hermes-ibkr-mnq-low-turnover-vol-expansion-30m-1h-gate1-v1/`.
- Factor id: `ibkr_mnq_low_turnover_vol_expansion_gate1_v1`.
- Branch paths: `TrendExpansion -> VolatilityCompressionExpansion -> ibkr_mnq_low_turnover_vol_expansion_gate1_v1` and `TrendExpansion -> VolatilityCompressionExpansion -> ibkr_mnq_low_turnover_vol_expansion_gate1_v1`. Labels/provenance prefixes: `FUTURES -> equity_index -> MNQ -> 30m`; `FUTURES -> equity_index -> MNQ -> 1h`.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` rows from source provider root `20260519T220712+0800-codex-ibkr-futures-index-precious-metals-opening-vwap-rvol-reclaim-1m-mtf-gate1-v1`; `30m` had `998` rows and `1h` had `499` rows; `local_cache_replay=false_source_provider_root_reused`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `material_count=6`; `rank_rows=6`; `rank_total_trade_count=42`.
- Cost/density evidence: this attempt intentionally used lower turnover and larger targets, but direction failed before practical-cost gates. The `30m` rows had `8`, `10`, and `11` trades with raw returns `-2.20%`, `-1.90%`, and `-2.89%`; after `5bps/side`, they were `-3.00%`, `-2.90%`, and `-3.99%`. The `1h` rows had `4`, `4`, and `5` trades with raw returns `-1.42%`, `-1.42%`, and `-1.20%`; after `5bps/side`, they were `-1.82%`, `-1.82%`, and `-1.70%`. `survivors_2bps=[]`; `survivors_5bps=[]`.
- Terminal decision: `drop_gate1_no_cost_density`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSVs, copied runner under `scripts/`, and command outputs.

Interpretation:

- This MNQ low-turnover volatility-expansion branch used real retained IBKR futures rows and preserved the regime-rooted branch paths, but every ranked variant was negative even before costs. It is a clean Gate 1 negative, not a provider or Auto-Quant blocker. Do not downstream it, and do not keep retuning restrictive `30m/1h` volatility-expansion variants unless a new hypothesis first changes the direction/entry edge rather than only cost handling.

## Gate 1 Negative: IBKR M2K 1m Heikin-Ashi/ATR TrendExpansion 7D

Append-only terminal row at `2026-05-20 06:26:00 +0800` after observing pre-write hash `ac5f7857141d4577b748d4ba738a3d53835f06e0733b43ff024e5ee0bfedf093` and duplicate-searching the Board for `M2K.*Heikin`, `heikin_ashi_atr_trend`, `062123`, `HeikinAshiAtrTrend`, and `ibkr_m2k1m_heikin_ashi_atr_trend_7d_gate1_v1`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T062123+0800-codex-ibkr-m2k1m-heikin-ashi-atr-trend-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_heikin_ashi_atr_trend_7d_gate1_v1`.
- Branch path: `TrendExpansion -> HeikinAshiAtrTrend -> ibkr_m2k1m_heikin_ashi_atr_trend_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Diversity/root note: this was opened because the strongest M2K RangeReversion short survivor still failed execution on current-state/PDA disagreement while M2K readback leaned `TrendExpansion/BullTrendAcceleration`; the factor family is public Heikin-Ashi/ATR trend continuation, separate from the repeated liquidity/VWAP/RVOL/oscillator-reclaim cluster.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from source provider root `20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=67`.
- Cost/density evidence: the branch had enough density to judge, but direction/economics failed before costs. `M2K/dense_run2/1m` had `22` trades, raw `-0.36%`, `1bps=-0.80%`, `2bps=-1.24%`, `5bps=-2.56%`; `M2K/pullback_run3/1m` had `26` trades, raw `-0.13%`, `2bps=-1.17%`, `5bps=-2.73%`; `M2K/quality_run4/1m` had `7` trades, raw `-0.34%`, `2bps=-0.62%`, `5bps=-1.04%`; `M2K/balanced_run3/1m` had `12` trades, raw `-0.69%`, `2bps=-1.17%`, `5bps=-1.89%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_heikin_ashi_atr_trend_7d_gate1_v1.py`.

Interpretation:

- The M2K TrendExpansion/Heikin-Ashi ATR branch was a clean regime-rooted diversity attempt on real retained futures rows, but it does not solve the practical-profit gate: all rows were raw-negative and there are no `2bps/side` or `5bps/side` survivors. Do not downstream this branch or tune it by lowering cost gates. The useful conclusion is narrower: matching the current TrendExpansion root is necessary but not sufficient; the next M2K attempt needs a different trend-entry edge with larger per-trade excursion, or the existing RangeReversion short survivor needs a genuine current-state/PDA alignment repair rather than another light overlay.

## Gate 1 Negative: IBKR MGC 5m Pretty Good Oscillator Reversion 10D

Append-only terminal row at `2026-05-20 06:40:00 +0800` after observing pre-write hash `32e1efba3a1ed5295ae24590fe55f295d6f0fa92ee4ce83a02a430de3e171290` and duplicate-searching the Board for `063229`, `pgo_reversion`, `Pretty Good Oscillator`, and `MGC 5m PGO`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T063229+0800-codex-ibkr-mgc5m-pgo-reversion-10d-gate1-v1/`.
- Factor id: `ibkr_mgc5m_pgo_reversion_10d_gate1_v1`.
- Branch path: `MeanReversionPressure -> PrettyGoodOscillatorReversion -> ibkr_mgc5m_pgo_reversion_10d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 5m`.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `5m 10 D`, `2679` normalized rows from source provider root `20260519T222102+0800-codex-ibkr-futures-micro-trend-pullback-reclaim-1m-mtf-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=0`.
- Cost/density evidence: all four public Pretty Good Oscillator reversion variants produced zero trades: `MGC/pgo_dense/5m`, `MGC/pgo_balanced/5m`, `MGC/pgo_quality/5m`, and `MGC/pgo_capitulation/5m` each had `0` trades, raw `0.00%`, `1bps=0.00%`, `2bps=0.00%`, and `5bps=0.00%`. `exact_5m_survivors_2bps=[]`; `exact_5m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `materials/source_manifest.json`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc5m_pgo_reversion_10d_gate1_v1.py`.

Interpretation:

- This gives the public Pretty Good Oscillator / PGO reversion family a clean exact-root `MGC/5m` precious-metals attempt using real retained IBKR rows. The factor did not fail because of provider, metadata, or Auto-Quant mechanics; it failed because the entry conditions produced no trades at all. Do not downstream or loosen this PGO branch. The next precious-metals attempt should either use a denser 1m family or a different 5m mean-reversion trigger with proven trade generation before cost stress.

## Gate 1 Negative: IBKR MGC 5m Linear-Regression Channel Breakout 10D

Append-only terminal row at `2026-05-20 06:48:00 +0800` after observing pre-write hash `27825670bf85bbae43a318670f659ac525ca8d58a22fcb953e2211b2db38679c` and duplicate-searching the Board for `061655`, `mgc5m_linreg_channel_breakout_10d_gate1_v1`, and `MGC 5m Linear`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T061655+0800-codex-ibkr-mgc5m-linreg-channel-breakout-10d-gate1-v1/`.
- Factor id: `ibkr_mgc5m_linreg_channel_breakout_10d_gate1_v1`.
- Branch path: `TrendChannelExpansion -> LinearRegressionChannelBreakout -> ibkr_mgc5m_linreg_channel_breakout_10d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 5m`.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `5m 10 D`, `2679` normalized rows from source provider root `20260519T222102+0800-codex-ibkr-futures-micro-trend-pullback-reclaim-1m-mtf-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=17`.
- Cost/density evidence: this public linear-regression channel branch generated enough trades to judge, but every row was raw-negative and failed cost stress. `MGC/linreg_retest/5m` had `11` trades, raw `-0.36%`, `1bps=-0.58%`, `2bps=-0.80%`, `5bps=-1.46%`; `MGC/linreg_dense/5m` had `4` trades, raw `-0.14%`, `2bps=-0.30%`, `5bps=-0.54%`; `MGC/linreg_balanced/5m` had `1` trade, raw `-0.01%`; `MGC/linreg_quality/5m` had `1` trade, raw `-0.02%`. `exact_5m_survivors_2bps=[]`; `exact_5m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner under `scripts/`.

Interpretation:

- The MGC `5m` linear-regression channel branch is a clean real-IBKR Gate 1 negative, not a provider or branch-metadata blocker. It adds public-family diversity evidence, but it should not move downstream: no variant survives even `2bps/side`, and the direction is negative before practical execution costs.

## Provider Blocker: IBKR SI 1m Aroon/CCI Reclaim 7D

Append-only terminal row at `2026-05-20 06:48:00 +0800` after observing pre-write hash `27825670bf85bbae43a318670f659ac525ca8d58a22fcb953e2211b2db38679c` and duplicate-searching the Board for `061746`, `si1m_aroon_cci_reclaim_7d_gate1_v1`, and `AroonCciReclaim`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T061746+0800-codex-ibkr-si1m-aroon-cci-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_si1m_aroon_cci_reclaim_7d_gate1_v1`.
- Intended branch path: `FUTURES -> precious_metals -> SI -> 1m -> TrendExpansion -> AroonCciReclaim -> ibkr_si1m_aroon_cci_reclaim_7d_gate1_v1`.
- Provider evidence: `provider-status` exited `0`, but the IBKR historical fetch for `SI 202607` `1m 7 D` exited `1` with `0` rows and `local_cache_replay=false`. Fetch stderr repeated `positions request timed out` and ended with `Cannot reach IBKR Gateway at 127.0.0.1:4002 (clientId=951)... all candidate clientIds are already in use: 951, 1051, 1052, 1053, 1054, 1055`.
- Gate 1 evidence: `rank_rows=0`; `rank_total_trade_count=0`; `exact_1m_cost_stress=[]`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`; `branch_fields_preserved=false`. The strategy compile check was recorded as `1` only because no provider material existed after the fetch blocker.
- Terminal classification: provider/clientId blocker, not a factor verdict. `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, empty `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, `command-output/01_ibkr_fetch_si_1m_7d.err`, and runner under `scripts/`.

Interpretation:

- This SI `1m` Aroon/CCI reclaim attempt did not reach a real Auto-Quant factor gate because no IBKR rows were fetched and no cache replay was used. Keep it as an infrastructure/provider blocker. It should be retried only after the IBKR client-id/Gateway contention is cleared; do not count it as evidence for or against the Aroon/CCI factor family on SI.

## Gate 1 Negative: IBKR MGC 1m Ulcer Index Risk-Pressure Rebound 7D

Append-only terminal row at `2026-05-20 06:58:00 +0800` after observing pre-write hash `2074e0df8dfb2c45b9696a6a19e343f3b259be59f4318a60248f1942f81bb26d` and duplicate-searching the Board for `055547`, `ulcer_risk_pressure_rebound`, `UlcerIndexRebound`, and `MGC 1m Ulcer`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T055547+0800-codex-ibkr-mgc1m-ulcer-risk-pressure-rebound-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_ulcer_risk_pressure_rebound_7d_gate1_v1`.
- Branch path: `RiskPressureReversal -> UlcerIndexRebound -> ibkr_mgc1m_ulcer_risk_pressure_rebound_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives the public Ulcer Index / drawdown-pressure rebound family a real exact-root micro-gold `1m` futures attempt, separate from RSI/VWAP, Connors RSI2, Williams/MFI, Aroon/CCI, Vortex, PSAR, floor/CPR/Camarilla pivots, PGO, and linear-regression channel controls.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows from source provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=0`.
- Cost/density evidence: all four Ulcer Index risk-pressure rebound variants produced zero trades: `MGC/ulcer_dense/1m`, `MGC/ulcer_balanced/1m`, `MGC/ulcer_quality/1m`, and `MGC/ulcer_capitulation/1m` each had `0` trades, raw `0.00%`, `1bps=0.00%`, `2bps=0.00%`, and `5bps=0.00%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_ulcer_risk_pressure_rebound_7d_gate1_v1.py`.

Interpretation:

- The MGC `1m` Ulcer Index branch preserves the user's regime-rooted path grammar and uses real retained IBKR rows, but it fails before profitability evaluation because no variant generated trades. Treat it as a zero-density public-family negative. Do not downstream or stack overlays on this root; the next micro-gold attempt needs a denser trigger with larger per-trade excursion rather than another restrictive reclaim shape.

## Gate 1 Negative: IBKR MNQ 1m Detrended Price Oscillator Reversion 7D

Append-only terminal row at `2026-05-20 07:03:00 +0800` after observing pre-write hash `143dc1095f324fda3cdcc60cddc5cb89733160c7f2f16441b4326d069f9221a8` and duplicate-searching the Board/claims for `065821`, `dpo_reversion`, `DetrendedPriceOscillatorReversion`, and `IBKR_MNQ1M_DPO_REVERSION`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T065821+0800-codex-ibkr-mnq1m-dpo-reversion-7d-gate1-v1/`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T065703+0800-codex-ibkr-mnq1m-dpo-reversion-7d-gate1-v1.claim`.
- Factor id: `ibkr_mnq1m_dpo_reversion_7d_gate1_v1`.
- Branch path: `MeanReversionPressure -> DetrendedPriceOscillatorReversion -> ibkr_mnq1m_dpo_reversion_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public Detrended Price Oscillator / DPO reversion family a real exact-root `MNQ/1m` futures attempt, separate from VWAP/RSI/RVOL, opening-range, Donchian/Turtle, Keltner/ATR, SuperTrend, Ichimoku, NR7/Crabel, Darvas, CMF/OBV, KST/Coppock, Heikin-Ashi/ATR, Klinger, EOM, Alligator/Fractal, Qstick, TD Sequential, linear-regression channel, Aroon/CCI, and PGO controls.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from source provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=36`.
- Cost/density evidence: `MNQ/dpo_quality/1m` had `4` trades, raw `+0.13%`, `1bps=+0.05%`, `2bps=-0.03%`, `5bps=-0.27%`; `MNQ/dpo_balanced/1m` had `13` trades, raw `-0.08%`, `2bps=-0.60%`, `5bps=-1.38%`; `MNQ/dpo_dense/1m` had `11` trades, raw `-0.26%`, `2bps=-0.70%`, `5bps=-1.36%`; `MNQ/dpo_capitulation/1m` had `8` trades, raw `-0.22%`, `2bps=-0.54%`, `5bps=-1.02%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `materials/source_manifest.json`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_dpo_reversion_7d_gate1_v1.py`.

Interpretation:

- DPO reversion got a clean public-family Gate 1 attempt on real retained MNQ `1m` futures data. The best row was gross-positive but too small to survive `2bps/side`; the denser rows were raw-negative. Do not downstream or tune this exact DPO root with overlays. Preserve it as a useful negative control and move the next search toward a larger-excursion family or a direct execution-predicate repair for an existing `5bps/side` survivor.

## Gate 1 Negative: IBKR M2K 1m Donchian/Turtle Breakout 7D

Append-only terminal row at `2026-05-20 07:06:00 +0800` after observing pre-write hash `143dc1095f324fda3cdcc60cddc5cb89733160c7f2f16441b4326d069f9221a8` and duplicate-searching the Board for `064312`, `m2k1m_donchian_turtle_breakout`, `Donchian.*M2K`, and `DonchianTurtleBreakout`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T064312+0800-codex-ibkr-m2k1m-donchian-turtle-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_donchian_turtle_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> DonchianTurtleBreakout -> ibkr_m2k1m_donchian_turtle_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from source provider root `20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=99`.
- Cost/density evidence: the branch generated enough trades to judge, but the edge stayed inside the friction envelope. `M2K/turtle20_balanced/1m` had `24` trades, raw `+0.76%`, `1bps=+0.28%`, `2bps=-0.20%`, and `5bps=-1.64%`; `M2K/turtle55_trend/1m` had `18` trades, raw `+0.39%`, `2bps=-0.33%`, and `5bps=-1.41%`; `M2K/turtle20_dense/1m` had `28` trades, raw `+0.57%`, `2bps=-0.55%`, and `5bps=-2.23%`; `M2K/turtle20_pullback_break/1m` had `29` trades, raw `+0.22%`, `1bps=-0.36%`, `2bps=-0.94%`, and `5bps=-2.68%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_donchian_turtle_breakout_7d_gate1_v1.py`.

Interpretation:

- This gives the M2K `1m` TrendExpansion root a real public Donchian/Turtle breakout attempt on retained IBKR futures rows. It is not provider-blocked and not branch-metadata-blocked, but it fails practical cost stress before downstream. Do not promote, downstream, or rescue this branch by lowering cost gates; rotate away from channel-breakout variants unless a new exact-root hypothesis increases per-trade excursion enough to survive `2bps/side` and `5bps/side`.

## Gate 1 Negative: IBKR M2K 1m Keltner/ATR Channel Breakout 7D

Append-only terminal row at `2026-05-20 07:06:00 +0800` after observing pre-write hash `143dc1095f324fda3cdcc60cddc5cb89733160c7f2f16441b4326d069f9221a8` and duplicate-searching the Board for `064331`, `m2k1m_keltner_atr_channel_breakout`, `Keltner.*M2K`, and `KeltnerAtrChannelBreakout`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T064331+0800-codex-ibkr-m2k1m-keltner-atr-channel-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_keltner_atr_channel_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> KeltnerAtrChannelBreakout -> ibkr_m2k1m_keltner_atr_channel_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from source provider root `20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=85`.
- Cost/density evidence: the branch produced density but did not survive realistic costs. `M2K/balanced_upper20/1m` had `13` trades, raw `+0.48%`, `1bps=+0.22%`, `2bps=-0.04%`, and `5bps=-0.82%`; `M2K/dense_upper20/1m` had `18` trades, raw `+0.02%`, `1bps=-0.34%`, `2bps=-0.70%`, and `5bps=-1.78%`; `M2K/pullback_reclaim_mid/1m` had `42` trades, raw `+0.24%`, `1bps=-0.60%`, `2bps=-1.44%`, and `5bps=-3.96%`; `M2K/trend_upper20/1m` had `12` trades, raw `-0.06%`, `2bps=-0.54%`, and `5bps=-1.26%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_keltner_atr_channel_breakout_7d_gate1_v1.py`.

Interpretation:

- This Keltner/ATR packet confirms the same practical blocker on the current M2K `1m` TrendExpansion channel-breakout cell: AQ mechanics and rooted metadata are healthy, but the edge disappears by `2bps/side` and is decisively negative by `5bps/side`. Do not run Pre-Bayes/BBN/CatBoost/execution-tree for this root. The nearest viable work remains either direct execution-predicate repair for the stronger M2K RangeReversion short survivor, or a fresh `1m`/`5m` futures family that is not another light channel breakout.

## Gate 1 Negative Addendum: IBKR SI 1m Aroon/CCI Reclaim 7D Retained-Real Replay

Append-only terminal row at `2026-05-20 07:12:00 +0800` after observing pre-write hash `de45316712c9191bf90a2b42d101f60d8e573536d618af0b1001dca314e7274a` and duplicate-searching the Board for `065652`, `si1m_aroon_cci_reclaim_7d_gate1_v1`, and `IBKR SI 1m Aroon/CCI Reclaim 7D`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T065652+0800-codex-ibkr-si1m-aroon-cci-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_si1m_aroon_cci_reclaim_7d_gate1_v1`.
- Branch path: `TrendExpansion -> AroonCciReclaim -> ibkr_si1m_aroon_cci_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Provider/Gate 1 evidence: fresh IBKR fetch for this rerun still exited `1` because of client-id contention, so `fresh_provider_fetch=false`; the run explicitly replayed retained real IBKR `SI 202607` `1m 7 D` rows rather than synthetic rows. Provider status exited `0`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=54`.
- Cost/density evidence: the retained-real branch generated enough trades to judge, but it failed practical costs. `SI/aroon_cci_quality/1m` had `13` trades, raw `-0.24%`, `1bps=-0.50%`, `2bps=-0.76%`, and `5bps=-1.54%`; `SI/aroon_cci_breakout/1m` had `9` trades, raw `+0.20%`, `1bps=+0.02%`, `2bps=-0.16%`, and `5bps=-0.70%`; `SI/aroon_cci_balanced/1m` had `18` trades, raw `-1.61%`, `2bps=-2.33%`, and `5bps=-3.41%`; `SI/aroon_cci_dense/1m` had `14` trades, raw `-1.47%`, `2bps=-2.03%`, and `5bps=-2.87%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si1m_aroon_cci_reclaim_7d_gate1_v1.py`.

Interpretation:

- This addendum upgrades the earlier SI Aroon/CCI attempt from a pure provider/client-id blocker into a retained-real factor verdict, while preserving the provider caveat. The public Aroon/CCI reclaim family does not survive `2bps/side` or `5bps/side` on the exact SI `1m` TrendExpansion root, so it must stay observation-only and must not be downstreamed.

## Gate 1 Negative: IBKR M2K 1m TD Sequential Exhaustion 7D

Append-only terminal row at `2026-05-20 07:15:00 +0800` after observing pre-write hash `80d7502ae30fdfdf278e0f4a005233ef442e538d839f56e680aced253bd597e4` and duplicate-searching the Board for `063902`, `m2k1m_td_sequential`, `M2K 1m TD`, and `TdSequentialExhaustion`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T063902+0800-codex-ibkr-m2k1m-td-sequential-exhaustion-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_td_sequential_exhaustion_7d_gate1_v1`.
- Branch path: `ExhaustionReversal -> TdSequentialExhaustion -> ibkr_m2k1m_td_sequential_exhaustion_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from source provider root `20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=4`.
- Cost/density evidence: this public DeMark TD Sequential exhaustion family was too sparse and raw-negative on M2K. `M2K/td9_reclaim_dense/1m` and `M2K/td9_reclaim_balanced/1m` each had `2` trades, `50.0%` win rate, raw `-0.01%`, `1bps=-0.05%`, `2bps=-0.09%`, and `5bps=-0.21%`; `M2K/td13_reversal_quality/1m` and `M2K/td_combo_wide/1m` each had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_td_sequential_exhaustion_7d_gate1_v1.py`.

Interpretation:

- M2K TD Sequential exhaustion is now classified separately from the older MNQ TD packet. It preserves the regime-rooted path and uses retained real IBKR futures rows, but it fails Gate 1 on both density and cost-stressed direction. Do not downstream or rescue this exact root; it is a useful exhaustion-family negative that further supports moving either to a materially larger-excursion 1m/5m family or to direct execution-predicate repair for the existing M2K `5bps/side` RangeReversion short survivor.

## Gate 1 Negative: IBKR MNQ 1m TTM Squeeze Momentum 7D

Append-only terminal row at `2026-05-20 07:22:00 +0800` after observing pre-write hash `9587c5e60690fdd4d5d5495b8a75264a13d3067a6e13ea2cacf82ba9c848b0b9` and duplicate-searching the Board for `071845`, `TTM Squeeze`, `ttm_squeeze`, and `TtmSqueezeMomentum`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T071845+0800-codex-ibkr-mnq1m-ttm-squeeze-momentum-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_ttm_squeeze_momentum_7d_gate1_v1`.
- Branch path: `VolatilityCompression -> TtmSqueezeMomentum -> ibkr_mnq1m_ttm_squeeze_momentum_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from source provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=134`.
- Cost/density evidence: the TTM/Bollinger-Keltner squeeze family produced enough trades, but every row failed practical cost stress. `MNQ/squeeze_dense/1m` had `26` trades, raw `+0.49%`, `1bps=-0.03%`, `2bps=-0.55%`, and `5bps=-2.11%`; `MNQ/squeeze_quality/1m` had `36` trades, raw `+0.10%`, `1bps=-0.62%`, `2bps=-1.34%`, and `5bps=-3.50%`; `MNQ/squeeze_retest/1m` had `37` trades, raw `+0.60%`, `1bps=-0.14%`, `2bps=-0.88%`, and `5bps=-3.10%`; `MNQ/squeeze_balanced/1m` had `35` trades, raw `+0.10%`, `1bps=-0.60%`, `2bps=-1.30%`, and `5bps=-3.40%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_ttm_squeeze_momentum_7d_gate1_v1.py`.

Interpretation:

- TTM squeeze momentum is a clean public-family density sample on real retained MNQ `1m` futures rows. It fails because the edge is smaller than realistic costs: all rows flip negative by `1bps/side` or `2bps/side`, and there are no `5bps/side` survivors. Do not downstream or lower cost gates for this exact VolatilityCompression branch; use it as another negative that points the next search toward larger-excursion entries or toward direct execution-state repair of an existing `5bps/side` survivor.

## Downstream Addendum: IBKR AAP 15m Failed-Breakdown Reclaim Exact Branch

Append-only terminal row at `2026-05-20 07:20:00 +0800` after observing pre-write hash `9202123c07ff669994858781b74286f025bf05b7f38bba744530779418fddf7f` and duplicate-searching the Board for `AAP15M`, `AAP 15m`, `aap_auto_parts_retail_failed_breakdown`, `downstream-exact-aap-15m`, `071050`, and `AutoPartsRetailOpeningRangeFailureReclaim`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T012529+0800-codex-ibkr-aap-auto-parts-retail-failed-breakdown-reclaim-gate1-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T012529+0800-codex-ibkr-aap-auto-parts-retail-failed-breakdown-reclaim-gate1-v1/downstream-exact-aap-15m-failed-breakdown-reclaim-20260520T071050+0800/`.
- Branch path: `RangeReversion -> AutoPartsRetailOpeningRangeFailureReclaim -> failed_breakdown_reclaim -> ibkr_aap_auto_parts_retail_failed_breakdown_reclaim_gate1_v1`. Labels/provenance prefix: `IBKR -> US_EQUITY -> AUTO_PARTS_RETAIL -> AAP -> 1m`.
- Gate 1 authority: real IBKR AAP provider ladder fetched successfully for `1m`, `5m`, `15m`, `30m`, `1h`, plus provider-only `4h`/`1d`; the exact selected row was `AAP/15m/balanced` with `6` trades, `66.6667%` win rate, raw `+1.83%`, `1bps=+1.71%`, `2bps=+1.59%`, and `5bps=+1.23%`.
- Downstream command evidence: `01_auto_quant_results_import` through `16_export_target_final` all exited `0`; `all_commands_ok=true`; exact branch survived; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=true`; `execution_tree_allowed=true`.
- Structural/ranker evidence: CatBoost trainer artifact registered and runtime enabled, but validation did not mature: `raw_scored_mature=0`, `production_validation=0`, `observation_validation=0`, `mature_rows=0`, and `history_mature_rows=0`.
- Execution evidence: closed-loop branch admission stayed `status=fail_closed`, `candidate_status=execution_blocked`, reason `exact_structural_branch_visible_but_not_ready_or_actionable`; hard execution predicates failed with `execution_readiness=0.3984828752770156`, `transition_hazard=0.9687928829066927`, and `pda_hybrid_alignment=false`.
- Terminal decision: `exact_aap_15m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/downstream_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, `command-output/*`, `path_ranker_scores.csv`, `path_ranker_model/trainer_artifact.json`, and copied runner under `scripts/`.

Interpretation:

- This is materially better evidence than another Gate 1 negative: the AAP failed-breakdown reclaim branch had a real `5bps/side` survivor and passed the downstream mechanics without flattening the rooted path. It still does not meet the user's retention standard. Direction/gate flow reached Pre-Bayes/BBN/CatBoost/execution tree, but execution admission failed on high transition hazard, PDA/hybrid disagreement, sub-`0.65` readiness, and zero mature validation rows. Preserve it as a strong observation and execution-repair lead, not trade-usable alpha.

## Gate 1 Survivor: IBKR SI 5m Tight-Range Band Expansion Fade 1M

Append-only terminal row at `2026-05-20 07:20:20 +0800` after observing pre-write hash `9587c5e60690fdd4d5d5495b8a75264a13d3067a6e13ea2cacf82ba9c848b0b9` and duplicate-searching the Board/claims for `070456`, `tight_range_band_expansion_fade`, `TightRangeBandExpansionFade`, and `SI 5m Tight`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/`.
- Factor id: `ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Branch path: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Diversity/source note: this is a range-consolidation band expansion/fade family on silver futures, materially different from the repeated VWAP/RSI/RVOL/channel-breakout cluster.
- Provider/Gate 1 evidence: retained real IBKR `SI 202607` `5m 1 M`, `6047` normalized rows; fresh fetch exited `1`, but the run explicitly used retained same-session real IBKR rows with `local_cache_replay=false_retained_real_ibkr_same_session`. Provider status, strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=36`.
- Cost/density evidence: two exact-root rows survived both `2bps/side` and `5bps/side`. `SI/dense_fade/5m_range_fade` had `9` trades, `77.7778%` win rate, raw `+2.23%`, `1bps=+2.05%`, `2bps=+1.87%`, and `5bps=+1.33%`; `SI/quality_fade/5m_range_fade` had `7` trades, `57.1429%` win rate, raw `+1.16%`, `1bps=+1.02%`, `2bps=+0.88%`, and `5bps=+0.46%`. The other two rows were raw-negative. `exact_5m_survivors_2bps=['SI/dense_fade/5m_range_fade','SI/quality_fade/5m_range_fade']`; `exact_5m_survivors_5bps=['SI/dense_fade/5m_range_fade','SI/quality_fade/5m_range_fade']`.
- Gate decision: `downstream_allowed=true`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, copied runner, and Auto-Quant material files under the run root.

Interpretation:

- This is the best new public/diverse futures packet in this slice: it has real retained IBKR rows, preserved regime-rooted branch identity, enough density to judge, and two `5bps/side` survivors. It is not yet practical or promotable because CatBoost/execution-tree have not been run and the hard execution predicates are still unknown. Next step should be exact-root downstream for the SI `5m` survivor path, preserving `RangeConsolidation -> TightRangeBandExpansionFade` through Pre-Bayes/BBN/CatBoost/execution tree and checking `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and `execution_readiness >= 0.65`.

## Downstream Partial Addendum: IBKR SI 5m Tight-Range Band Expansion Fade Exact Branch

Append-only terminal row at `2026-05-20 07:35:00 +0800` after observing pre-write hash `2551cd61a7e803031700241b7b338686c1c3b73050dc8b85746dd4e313ace045` and duplicate-searching the Board for `072137`, `072345`, `exact_si5m_tight_range`, `downstream-exact-si-5m-tight-range-band-expansion-fade`, and `TightRangeBandExpansionFade.*downstream`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/`.
- Exact downstream root used for classification: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/downstream-exact-si-5m-tight-range-band-expansion-fade-20260520T072137+0800/`.
- Duplicate downstream hygiene note: `...072345+0800/` was also started against the same Gate 1 root and only had import/prior-init evidence at inspection time; it does not override the fuller `072137` classification.
- Branch path: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Gate 1 authority: retained real IBKR `SI 202607` `5m 1 M`, `6047` normalized rows; exact selected survivors remained `SI/dense_fade/5m_range_fade` (`9` trades, raw `+2.23%`, `2bps=+1.87%`, `5bps=+1.33%`) and `SI/quality_fade/5m_range_fade` (`7` trades, raw `+1.16%`, `2bps=+0.88%`, `5bps=+0.46%`).
- Downstream command evidence on `072137`: `01_auto_quant_results_import=0`, `02_auto_quant_prior_init=0`, `03_analyze_seed=124` (`TIMEOUT after 360s`), `04_workflow_seed=0`, `05_pre_bayes_seed=0`, `06_export_target_seed=0`, `07_train_ranker=0`, `08_apply_ranker=0`, `09_apply_scores_to_ict=0`, `10_register_trainer=0`, and `11_enable_runtime=0`. `12_analyze_after_ranker` had a command file but no completed exit/metrics at inspection time.
- Structural/ranker evidence: CatBoost trainer artifact was generated and registered as `model_family=catboost`; `path_ranker_scores.csv` contained the exact rooted branch with score `0.7506586567765241`, so exact branch score visibility was repaired.
- Validation/execution blocker evidence: runtime enable reported `trainer_artifact_status=present_validation_insufficient`, `mature_rows=0`, `history_mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, `calibration=not_fitted`, and `runtime_selection=enabled_registered_artifact_ready`. Seed workflow remained `status=fail_closed`, `candidate_status=execution_candidate_observed`, reason `exact_structural_branch_visible_but_not_ready_or_actionable`, with bootstrap path rather than exact execution admission.
- Terminal decision: `exact_si5m_tight_range_band_expansion_fade_downstream_partial_fail_closed`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=true`; `execution_tree_allowed=false_or_unproven_after_analyze_timeout`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/*.exit`, `command-output/*`, `path_ranker_scores.csv`, `path_ranker_model/trainer_artifact.json`, `state/.../policy_training/structural_path_ranking_target_summary.json`, copied runner under `scripts/`, and Gate 1 `checks/terminal_metrics.json`.

Interpretation:

- This is the strongest current SI precious-metals lead, but it is still not a practical factor. It passed real-cost Gate 1 and reached exact-root CatBoost/runtime enable, yet execution admission did not mature: analyze timed out, validation rows were `0/30`, and the workflow stayed fail-closed. Next useful work is not another duplicate downstream wrapper; it is either same-root feedback/maturity repair for this RangeConsolidation fade branch or a different SI/MGC factor family with enough real `5bps/side` density and direct execution predicate improvement.

## Downstream Terminal Addendum: IBKR SI 5m Tight-Range Band Expansion Fade 072345 Manual Readback

Append-only terminal addendum at `2026-05-20 08:22:00 +0800` after observing pre-write hash `a9d25780465e0bc848e603572e5aee837eb697ebccdd2ca59419cb4d50fc9e35` and duplicate-searching the Board for `072345`, `fail_closed_analyze_timeout_no_execution_candidate`, and `exact_si5m_tight_range_band_expansion_fade_downstream_fail_closed`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/`.
- Exact downstream root used for this terminal readback: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/downstream-exact-si-5m-tight-range-band-expansion-fade-20260520T072345+0800/`.
- Branch path: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Gate 1 authority remains intact: exact retained-real `5bps/side` survivors were `SI/dense_fade/5m_range_fade` (`9` trades, raw `+2.23%`, `2bps=+1.87%`, `5bps=+1.33%`) and `SI/quality_fade/5m_range_fade` (`7` trades, raw `+1.16%`, `2bps=+0.88%`, `5bps=+0.46%`).
- Command/readback evidence: `01_auto_quant_results_import=0`, `02_auto_quant_prior_init=0`, `03_analyze_seed=124`, `04_workflow_seed=0`, `05_pre_bayes_seed=0`, `06_export_target_seed=0`, `07_train_ranker=0`, `08_apply_ranker=0`, `09_apply_scores_to_ict=0`, `10_register_trainer=0`, `11_enable_runtime=0`, and manual readbacks `13_manual_workflow_after_ranker=0`, `14_manual_pre_bayes_after_ranker=0`, `15_manual_policy_after_ranker=0`, `16_manual_export_target_final=0`. `seed_analyze_timed_out=true`; `final_analyze_missing=true`; `all_commands_ok=false`.
- Structural/ranker evidence: exact branch survived and ranker score was visible; CatBoost trainer artifact was generated/registered with `model_family=catboost`, runtime selection was `enabled_registered_artifact_ready`, and target/history rows were `2/2`.
- Validation/execution blocker evidence: no `execution_candidate.json` or `execution_tree_trace.json` materialized after the analyze timeout; `execution_candidate_status=missing_execution_candidate`, `execution_tree_trace_status=missing_execution_tree_trace`, `mature_rows=0`, `history_mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, and `observation_validation=0/30`.
- Hard predicate readback: `execution_readiness=0.0`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=true`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Terminal decision: `exact_si5m_tight_range_band_expansion_fade_downstream_fail_closed_analyze_timeout_no_execution_candidate`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, `manual-readback/*.json`, `manual-readback/*.exit`, `path_ranker_scores.csv`, `path_ranker_model/catboost_model.cbm`, and `state/.../policy_training/structural_path_ranking_target_summary.json`.

Interpretation:

- This supersedes the earlier partial note that treated `072345` as only import/prior-init evidence. The SI `5m` range-fade root remains a strong observation because it has real `5bps/side` density and exact-root CatBoost visibility, but it is still not practical: no execution candidate/tree materialized, validation is `0/30`, and the hard predicate trio is fully failed. The next aligned work is not another downstream wrapper; it is same-root maturity plus execution-candidate materialization repair for this exact `RangeConsolidation -> TightRangeBandExpansionFade` path, or a different `5bps/side` survivor whose downstream can actually reduce `transition_hazard`, align PDA, and raise `execution_readiness`.

## Gate 1 Negative: IBKR M2K 1m McGinley Dynamic Pullback 7D

Append-only terminal row at `2026-05-20 07:47:04 +0800` after observing pre-write hash `22403c79cc7cea98a8938681034a3f86b4c61405016f5a4dbdd87c574c2450d5` and duplicate-searching the Board/claims for `072625`, `072341`, `mcginley_dynamic_pullback`, `McGinleyDynamicPullback`, and `M2K 1m McGinley`.

- Run root used for terminal classification: `support/docs/experiments/actionable-regime-confidence/runs/20260520T072625+0800-codex-ibkr-m2k1m-mcginley-dynamic-pullback-7d-gate1-v1/`.
- Earlier duplicate root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T072341+0800-codex-ibkr-m2k1m-mcginley-dynamic-pullback-7d-gate1-v1/`; both reached stable Gate 1 exits, and the later `072625` packet is the retained terminal readback.
- Factor id: `ibkr_m2k1m_mcginley_dynamic_pullback_7d_gate1_v1`.
- Branch path: `TrendPullback -> McGinleyDynamicPullback -> ibkr_m2k1m_mcginley_dynamic_pullback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Diversity/source note: this gives the McGinley Dynamic adaptive moving-average pullback family a real `M2K/1m` futures attempt, separate from fixed SMA/EMA, VWAP, Donchian/Turtle, Keltner/ATR, TD Sequential, and liquidity-sweep variants.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from source provider root `20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=23`.
- Cost/density evidence: `M2K/quality_md34/1m` had one isolated winning trade, raw `+0.42%`, `2bps=+0.38%`, and `5bps=+0.32%`, but it fails the user's density gate and the packet's own survivor lists because one trade over seven days is not enough. The denser rows were negative or too small: `M2K/dense_md18/1m` had `9` trades, raw `-0.20%`, `2bps=-0.56%`, `5bps=-1.10%`; `M2K/deep_reclaim_md24/1m` had `11` trades, raw `+0.01%`, `2bps=-0.43%`, `5bps=-1.09%`; `M2K/balanced_md24/1m` had `2` trades, raw `-0.05%`, `2bps=-0.13%`, `5bps=-0.25%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_mcginley_dynamic_pullback_7d_gate1_v1.py`.

Interpretation:

- McGinley Dynamic deserved a slot in the public/time-tested diversity queue, but this exact `M2K/1m` TrendPullback root is not usable. The only cost-positive row is too sparse, while the denser rows fail raw or cost stress. Do not downstream this root or rescue it with overlays; keep it as a diversity negative and continue toward either larger-excursion public families or execution-maturity repair for the SI `5m` range fade and other existing `5bps/side` survivors.

## Gate 1 Negative: IBKR M2K 1m Guppy/GMMA Trend Continuation 7D

Append-only terminal row at `2026-05-20 07:39:00 +0800` after observing pre-write hash `28c8a9504e7e63845ca322ab436649736a59efb7d3bac07ccc0546d727298b27` and duplicate-searching the Board for `073430`, `m2k1m_guppy_gmma`, `GuppyGmmaTrendContinuation`, and `GMMA`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T073430+0800-codex-ibkr-m2k1m-guppy-gmma-trend-continuation-7d-gate1-v1/`.
- Factor id: `ibkr_m2k1m_guppy_gmma_trend_continuation_7d_gate1_v1`.
- Branch path: `TrendExpansion -> GuppyGmmaTrendContinuation -> ibkr_m2k1m_guppy_gmma_trend_continuation_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from source provider root `20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=6`.
- Cost/density evidence: GMMA was too sparse and did not survive realistic costs. `M2K/gmma_dense/1m` had `1` trade, raw `+0.04%`, `1bps=+0.02%`, `2bps=0.00%`, and `5bps=-0.06%`; `M2K/gmma_retest/1m` had `5` trades, `40.0%` win rate, raw `-0.22%`, `1bps=-0.32%`, `2bps=-0.42%`, and `5bps=-0.72%`; `M2K/gmma_balanced/1m` and `M2K/gmma_quality/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_guppy_gmma_trend_continuation_7d_gate1_v1.py`.

Interpretation:

- This gives the public Guppy/GMMA trend-continuation family a clean M2K `1m` retained-real futures attempt, but it fails before downstream because it has neither trade density nor cost-stressed edge. Do not run Pre-Bayes/BBN/CatBoost/execution-tree for this root. Rotate away from light M2K `1m` trend-continuation variants unless the next hypothesis directly increases excursion and density enough to survive `2bps/side` and `5bps/side`.

## Gate 1 Observation at 2bps Only: IBKR MNQ 1m PPO Histogram Pullback 7D

Append-only terminal row at `2026-05-20 07:41:00 +0800` after observing pre-write hash `59cd2d20330838b437c983e1728ea5029e824601f18b4cb1d420e3bb6411cc98` and duplicate-searching the Board for `073249`, `073542`, `ppo_histogram`, `PpoHistogramPullback`, and `PPO histogram`.

- Run root used for classification: `support/docs/experiments/actionable-regime-confidence/runs/20260520T073542+0800-codex-ibkr-mnq1m-ppo-histogram-pullback-7d-gate1-v1/`.
- Duplicate hygiene note: `20260520T073249+0800-codex-ibkr-mnq1m-ppo-histogram-pullback-7d-gate1-v1/` produced the same metrics and does not need a separate Board row.
- Factor id: `ibkr_mnq1m_ppo_histogram_pullback_7d_gate1_v1`.
- Branch path: `MomentumPullback -> PpoHistogramPullback -> ibkr_mnq1m_ppo_histogram_pullback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from source provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=28`.
- Cost/density evidence: one exact row survived only `2bps/side`, not the hard `5bps/side` screen. `MNQ/ppo_dense/1m` had `21` trades, `42.8571%` win rate, raw `+0.99%`, `1bps=+0.57%`, `2bps=+0.15%`, and `5bps=-1.11%`; `MNQ/ppo_reclaim/1m` had `7` trades and was raw-negative; `MNQ/ppo_balanced/1m` and `MNQ/ppo_quality/1m` had `0` trades. `exact_1m_survivors_2bps=['MNQ/ppo_dense/1m']`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `gate1_ibkr_mnq1m_ppo_histogram_pullback_2bps_only_observation`; `downstream_allowed=false_under_current_5bps_hard_gate`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_ppo_histogram_pullback_7d_gate1_v1.py`.

Interpretation:

- PPO histogram pullback is better than many raw-negative MNQ `1m` attempts, but it is still too thin for practical execution because the edge vanishes by `5bps/side`. Keep it as source-backed public-family diversity evidence only. Do not promote or downstream under the current hard gate unless a later variant materially increases per-trade excursion while preserving density.

## Downstream Addendum: IBKR MNQ 1m PPO Histogram Pullback Exact Branch

Append-only terminal row at `2026-05-20 07:52:00 +0800` after observing pre-write hash `6e50b0fbbe81ee1df11e9bdbac35c8e00e7a8cd1dc10646f409342a136d415b5` and duplicate-searching the Board for `074334`, `PPO.*downstream`, `ppo_histogram_pullback_downstream`, `exact_mnq1m_ppo`, and `MNQ1M_PPO_HISTOGRAM_PULLBACK_DOWNSTREAM`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T073249+0800-codex-ibkr-mnq1m-ppo-histogram-pullback-7d-gate1-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T073249+0800-codex-ibkr-mnq1m-ppo-histogram-pullback-7d-gate1-v1/downstream-exact-mnq-1m-ppo-histogram-pullback-20260520T074334+0800/`.
- Branch path: `MomentumPullback -> PpoHistogramPullback -> ibkr_mnq1m_ppo_histogram_pullback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Gate 1 authority: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows. Gate 1 had only a `2bps/side` survivor (`MNQ/ppo_dense/1m`: `21` trades, raw `+0.99%`, `2bps=+0.15%`, `5bps=-1.11%`) and no `5bps/side` survivor.
- Downstream command evidence: `01_auto_quant_results_import=0`, `02_auto_quant_prior_init=0`, `03_analyze_seed=124`, `04_workflow_seed=0`, `05_pre_bayes_seed=0`, `06_export_target_seed=0`, `07_train_ranker=0`, `08_apply_ranker=0`, `09_apply_scores_to_ict=0`, `10_register_trainer=1`, `11_enable_runtime=0`, `12_analyze_after_ranker=124`, `13_workflow_after_ranker=0`, `14_pre_bayes_after_ranker=0`, `15_policy_after_ranker=0`, and `16_export_target_final=0`; `all_commands_ok=false`.
- Structural/ranker evidence: `path_ranker_scores.csv` contained the exact rooted branch score (`0.7506586567765241`), but trainer registration failed and runtime fell back to `runtime_selection=enabled_candidate_set_ready` with `trainer_artifact=missing`.
- Validation/execution blocker evidence: `raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`, `calibration=not_fitted`, `exact_branch_survived=false`, closed-loop admission remained `status=fail_closed` with bootstrap path and reason `exact_structural_branch_visible_but_not_ready_or_actionable`; hard predicates were `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`.
- Terminal decision: `exact_mnq_1m_ppo_histogram_pullback_downstream_fail_closed`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=true`; `execution_tree_allowed=true_but_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, `command-output/*`, `path_ranker_scores.csv`, `state/.../policy_training/structural_path_ranking_target_summary.json`, and copied downstream runner artifacts.

Interpretation:

- This downstream replay confirms the earlier Gate 1 concern: PPO is not a practical factor. It was only `2bps`-positive before downstream, then failed exact execution admission with analyze timeouts, missing trainer registration, no mature validation rows, and the hard execution predicates at their worst values. Preserve it as observation/diversity evidence; do not tune PPO further unless the next variant first clears the `5bps/side` Gate 1 screen with materially larger excursion.

## Gate 1 Negative: IBKR MGC 1m Percent-B/MFI Reclaim 7D

Append-only terminal row at `2026-05-20 07:58:00 +0800` after observing pre-write hash `13a9bf44a910ed44c4b14523984a7cc87181db3187bd950843b4418a74d1740a` and duplicate-searching the Board for `074708`, `percentband_mfi_reclaim`, `PercentBandMfiReclaim`, and `MGC 1m Percent`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T074708+0800-codex-ibkr-mgc1m-percentband-mfi-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_percentband_mfi_reclaim_7d_gate1_v1`.
- Branch path: `RangeReversion -> PercentBandMfiReclaim -> ibkr_mgc1m_percentband_mfi_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives the public Percent-B plus Money Flow Index reclaim family a real exact-root micro-gold `1m` futures attempt, separate from Williams/MFI, Relative Vigor, Connors RSI2, Aroon/CCI, Vortex, PSAR, floor/CPR/Camarilla pivots, PGO, linear-regression channel, VWAP/RVOL, and opening-range variants.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows from source provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=96`.
- Cost/density evidence: the branch produced density, but no row survived realistic costs. `MGC/pbmfi_flush/1m` had `3` trades, raw `+0.02%`, `1bps=-0.04%`, `2bps=-0.10%`, and `5bps=-0.28%`; `MGC/pbmfi_dense/1m` had `49` trades, raw `-1.47%`, `2bps=-3.43%`, and `5bps=-6.37%`; `MGC/pbmfi_balanced/1m` had `32` trades, raw `-0.41%`, `2bps=-1.69%`, and `5bps=-3.61%`; `MGC/pbmfi_quality/1m` had `12` trades, raw `-0.35%`, `2bps=-0.83%`, and `5bps=-1.55%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_percentband_mfi_reclaim_7d_gate1_v1.py`.

Interpretation:

- Percent-B/MFI got a fair public-family Gate 1 attempt on coherent real retained IBKR MGC `1m` futures rows. The failure is practical economics, not provider or branch metadata: dense variants were raw-negative, and the only raw-positive flush row flipped negative at `1bps/side`. Do not downstream this exact root or lower cost gates to rescue it. Keep it as another diversity negative, and continue the rotation only with materially different or larger-excursion families; otherwise spend repair budget on existing `5bps/side` survivors such as SI `5m` tight-range fade or M2K liquidity-sweep reject-short.

## Gate 1 Negative: IBKR SI 1m Chaikin A/D Reclaim 7D

Append-only terminal row at `2026-05-20 08:09:00 +0800` after observing pre-write hash `3bc74b70fb56b0db3c60900b05275a4755d775ee8156244b878ab89b67cef4c3` and duplicate-searching the Board for `075139`, `chaikin_ad_reclaim`, `ChaikinAdOscillatorReclaim`, and `SI 1m Chaikin`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T075139+0800-codex-ibkr-si1m-chaikin-ad-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_si1m_chaikin_ad_reclaim_7d_gate1_v1`.
- Branch path: `VolumeAccumulation -> ChaikinAdOscillatorReclaim -> ibkr_si1m_chaikin_ad_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Diversity/source note: this gives the public Chaikin Accumulation/Distribution oscillator family a real exact-root silver `1m` futures attempt, separate from VWAP/RSI/RVOL, Aroon/CCI, PPO, Williams/MFI, Percent-B/MFI, Vortex, Klinger, OBV/CMF, and opening-range variants.
- Provider/Gate 1 evidence: retained real IBKR `SI 202607` `1m 7 D`, `9453` normalized rows from source provider root `20260520T065652+0800-codex-ibkr-si1m-aroon-cci-reclaim-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=179`.
- Cost/density evidence: the branch was dense enough to judge, but the gross edge stayed inside the friction envelope. `SI/chaikin_dense/1m` had `61` trades, raw `+0.83%`, `1bps=-0.39%`, `2bps=-1.61%`, and `5bps=-5.27%`; `SI/chaikin_quality/1m` had `19` trades, raw `-0.11%`, `2bps=-0.87%`, and `5bps=-2.01%`; `SI/chaikin_washout/1m` had `67` trades, raw `-0.34%`, `2bps=-3.02%`, and `5bps=-7.04%`; `SI/chaikin_balanced/1m` had `32` trades, raw `-1.23%`, `2bps=-2.51%`, and `5bps=-4.43%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si1m_chaikin_ad_reclaim_7d_gate1_v1.py`.

Interpretation:

- Chaikin A/D received a fair source-backed Gate 1 chance on coherent retained-real SI `1m` futures rows. This is a cost failure, not a data or rooted-path failure: the dense variant was positive before friction but already negative at `1bps/side`, and every row failed `2bps/side` and `5bps/side`. Do not run Pre-Bayes/BBN/CatBoost/execution-tree for this root, and do not lower costs to make it pass. Keep it as a useful volume-accumulation negative sample; the next productive work should either repair execution predicates on an existing `5bps/side` survivor, or choose a materially larger-excursion family that can survive the same real-cost gate.

## Gate 1 Negative: IBKR MNQ 1m ROC Momentum Breakout 7D

Append-only terminal row at `2026-05-20 08:13:00 +0800` after observing pre-write hash `ed3453cd003cdbcb1d15894155a051e85b6435148c0c088f995e889307085281` and duplicate-searching the Board/claims for `080009`, `roc_momentum_breakout`, `RocMomentumBreakout`, `ROC momentum`, and `Rate-of-Change`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T080009+0800-codex-ibkr-mnq1m-roc-momentum-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_roc_momentum_breakout_7d_gate1_v1`.
- Branch path: `MomentumExpansion -> RocMomentumBreakout -> ibkr_mnq1m_roc_momentum_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public Rate-of-Change / ROC momentum-breakout family a real exact-root `MNQ/1m` futures attempt, separate from VWAP/RSI/RVOL, oscillator reclaim, KST/Coppock, PPO, Qstick, volume-flow, pivots, and the claimed Hilbert cycle lane.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from source provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=4`.
- Cost/density evidence: ROC did not produce enough trades to judge as live-practical and did not survive the hard cost screen. `MNQ/roc_retest/1m` had `2` trades, raw `+0.19%`, `1bps=+0.15%`, `2bps=+0.11%`, and `5bps=-0.01%`, but fails the density gate; `MNQ/roc_dense/1m` had `2` trades, raw `+0.08%`, `1bps=+0.04%`, `2bps=0.00%`, and `5bps=-0.12%`; `MNQ/roc_balanced/1m` and `MNQ/roc_quality/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_roc_momentum_breakout_7d_gate1_v1.py`.

Interpretation:

- ROC momentum breakout got a fair public-family slot on real retained IBKR `MNQ/1m` futures rows, answering the diversity requirement with an actual Gate 1 run rather than another familiar local indicator tweak. The result is still negative: the branch preserved metadata, but trade density was too low and no row survived `5bps/side`. Do not downstream this root or lower density/cost gates. The practical search should keep rotating materially different public families only when they offer larger excursion, while repair budget stays focused on existing `5bps/side` survivors such as SI `5m` tight-range fade and M2K liquidity-sweep reject-short.

## Gate 1 Negative: IBKR MNQ 1m Hilbert/SineWave Cycle Turn 7D

Append-only terminal row at `2026-05-20 08:17:00 +0800` after observing pre-write hash `96d30f995c1378fe465b5d8942a670d243cabaebc31bd81680a3b5500e9fb194` and duplicate-searching the Board for `075831`, `hilbert_sinewave_cycle_turn`, `HilbertSineWaveCycleTurn`, `cycle_balanced`, and `Gate 1 Negative: IBKR MNQ 1m Hilbert`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T075831+0800-codex-ibkr-mnq1m-hilbert-sinewave-cycle-turn-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_hilbert_sinewave_cycle_turn_7d_gate1_v1`.
- Branch path: `CycleReversal -> HilbertSineWaveCycleTurn -> ibkr_mnq1m_hilbert_sinewave_cycle_turn_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public Hilbert transform / SineWave dominant-cycle turn family a real exact-root `MNQ/1m` futures attempt, separate from ROC, PPO, KST/Coppock, Qstick, Klinger, VWAP/RVOL, opening-range, and other momentum/volume-flow variants.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from source provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=56`.
- Cost/density evidence: the branch had enough trades to judge but no practical edge after friction. `MNQ/cycle_balanced/1m` had `12` trades, raw `+0.06%`, `1bps=-0.18%`, `2bps=-0.42%`, and `5bps=-1.14%`; `MNQ/cycle_dense/1m` had `20` trades, raw `+0.03%`, `1bps=-0.37%`, `2bps=-0.77%`, and `5bps=-1.97%`; `MNQ/cycle_quality/1m` had `5` trades, raw `+0.11%`, `1bps=+0.01%`, `2bps=-0.09%`, and `5bps=-0.39%`; `MNQ/cycle_reclaim/1m` had `19` trades, raw `-0.19%`, `2bps=-0.95%`, and `5bps=-2.09%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_hilbert_sinewave_cycle_turn_7d_gate1_v1.py`.

Interpretation:

- Hilbert/SineWave adds useful family diversity because it is a cycle-turn formulation rather than another linear momentum or VWAP overlay. The exact `MNQ/1m` result is still a clean Gate 1 cost failure: raw edge is tiny, the best rows flip negative by `1bps/side` or `2bps/side`, and there is no `5bps/side` survivor. Do not downstream this root or keep retuning the same cycle trigger unless a future hypothesis materially increases per-trade excursion before cost stress.

## Gate 1 Negative: IBKR MNQ 1m DeMarker Reversal 7D

Append-only terminal row at `2026-05-20 08:15:12 +0800` after observing pre-write hash `1c69a23713f95e5c999de38ef4f54fb3cb5fc06d35e2f3f5a49cd479ffc25713` and duplicate-searching the Board/claims for `DeMarker`, `demarker_reversal`, `070936`, `ibkr_mnq1m_demarker_reversal_7d_gate1_v1`, and `MNQ 1m DeMarker`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070936+0800-codex-ibkr-mnq1m-demarker-reversal-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_demarker_reversal_7d_gate1_v1`.
- Branch path: `RangeReversion -> DeMarkerReversal -> ibkr_mnq1m_demarker_reversal_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives the public DeMarker oscillator reversal family a real exact-root `MNQ/1m` futures attempt, separate from VWAP/RSI/RVOL, DPO, Fisher/Stoch, TRIX, KST, Klinger, Mass Index, Donchian, TTM squeeze, and Hilbert cycle lanes.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from source provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=0`.
- Cost/density evidence: DeMarker reversal produced no trades across all four variants, so there is no practical density or cost edge to test. `MNQ/dem_dense/1m`, `MNQ/dem_balanced/1m`, `MNQ/dem_quality/1m`, and `MNQ/dem_capitulation/1m` all had `0` trades and `0.00%` raw/`1bps`/`2bps`/`5bps` results. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_demarker_reversal_7d_gate1_v1.py`.

Interpretation:

- DeMarker earned a public-family diversity slot, but this exact `MNQ/1m` RangeReversion root is a zero-trade Gate 1 failure. Do not downstream or add overlays to rescue it. Preserve it as coverage evidence and rotate toward a larger-excursion family or toward same-root execution repair on an existing `5bps/side` survivor.

## Gate 1 Negative: IBKR MES 1m KAMA Efficiency Pullback 7D

Append-only terminal row at `2026-05-20 08:15:12 +0800` after observing pre-write hash `1c69a23713f95e5c999de38ef4f54fb3cb5fc06d35e2f3f5a49cd479ffc25713` and duplicate-searching the Board/claims for `KAMA`, `kama_efficiency_pullback`, `073250`, `073205`, and `ibkr_mes1m_kama_efficiency_pullback_7d_gate1_v1`.

- Run root used for terminal classification: `support/docs/experiments/actionable-regime-confidence/runs/20260520T073250+0800-codex-ibkr-mes1m-kama-efficiency-pullback-7d-gate1-v1/`.
- Earlier partial root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T073205+0800-codex-ibkr-mes1m-kama-efficiency-pullback-7d-gate1-v1/`; the later `073250` root contains the complete checks and terminal metrics.
- Factor id: `ibkr_mes1m_kama_efficiency_pullback_7d_gate1_v1`.
- Branch path: `TrendEfficiency -> KaufmanAdaptivePullback -> ibkr_mes1m_kama_efficiency_pullback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Diversity/source note: this gives Kaufman's Adaptive Moving Average / efficiency-ratio pullback a real `MES/1m` exact-root futures attempt, separate from fixed EMA/SMA, McGinley, GMMA, ROC, VWAP/RVOL, Aroon/CCI, TTM squeeze, and channel-breakout lanes.
- Provider/Gate 1 evidence: retained real IBKR `MES 202606` `1m 7 D`, `9432` normalized rows from source provider root `20260520T012135+0800-codex-ibkr-mes1m-opening-drive-vwap-rvol-continuation-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=64`.
- Cost/density evidence: KAMA generated dense entries, but the edge was fully inside the friction envelope. `MES/kama_reclaim/1m` had `64` trades, `53.1250%` win rate, raw `+0.56%`, `1bps=-0.72%`, `2bps=-2.00%`, and `5bps=-5.84%`; `MES/kama_dense/1m`, `MES/kama_balanced/1m`, and `MES/kama_quality/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mes1m_kama_efficiency_pullback_7d_gate1_v1.py`.

Interpretation:

- KAMA/Efficiency Ratio answered the factor-diversity requirement with a real public-family attempt on coherent `MES/1m` futures rows, but it failed on practical economics. Dense raw profit flipped negative at `1bps/side`, so this branch must stop at Gate 1. Do not downstream, lower costs, or add a second overlay until a base KAMA-family variant can preserve real-cost density.

## Gate 1 Negative: IBKR MES 1m ROC Momentum Breakout 7D

Append-only terminal row at `2026-05-20 08:15:12 +0800` after observing pre-write hash `1c69a23713f95e5c999de38ef4f54fb3cb5fc06d35e2f3f5a49cd479ffc25713` and duplicate-searching the Board/claims for `MES 1m ROC`, `mes1m_roc`, `RocMomentumBreakout`, `081210`, and `081052`.

- Run root used for terminal classification: `support/docs/experiments/actionable-regime-confidence/runs/20260520T081210+0800-codex-ibkr-mes1m-roc-momentum-breakout-7d-gate1-v1/`.
- Earlier duplicate root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T081052+0800-codex-ibkr-mes1m-roc-momentum-breakout-7d-gate1-v1/`; the later `081210` root contains the complete terminal metrics.
- Factor id: `ibkr_mes1m_roc_momentum_breakout_7d_gate1_v1`.
- Branch path: `MomentumExpansion -> RocMomentumBreakout -> ibkr_mes1m_roc_momentum_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Diversity/source note: this gives the public Rate-of-Change / ROC momentum-breakout family a second exact-root equity-index futures cell on `MES/1m`, distinct from the earlier `MNQ/1m` ROC attempt and from KAMA/GMMA/TTM/Chaikin/PPO/VWAP/RVOL variants.
- Provider/Gate 1 evidence: retained real IBKR `MES 202606` `1m 7 D`, `9432` normalized rows from source provider root `20260520T073250+0800-codex-ibkr-mes1m-kama-efficiency-pullback-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=2`.
- Cost/density evidence: ROC on `MES/1m` was too sparse and did not clear the hard cost/density screen. `MES/roc_dense/1m` and `MES/roc_retest/1m` each had only `1` trade, raw `+0.10%`, `1bps=+0.08%`, `2bps=+0.06%`, and `5bps=0.00%`; `MES/roc_balanced/1m` and `MES/roc_quality/1m` had `0` trades. The one-trade rows fail density and do not survive `5bps/side`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mes1m_roc_momentum_breakout_7d_gate1_v1.py`.

Interpretation:

- This MES ROC rerun confirms the ROC family still does not meet the user's retention gate on another equity-index futures root. The branch preserved metadata and ran cleanly, but trade count and cost survival are insufficient. Do not downstream or retune ROC immediately; continue with materially different public families or spend repair time on existing `5bps/side` survivors whose blockers are execution candidate materialization, transition hazard, PDA alignment, and readiness.

## Gate 1 Negative: IBKR SI 1m Force Index Displacement 7D

Append-only terminal row at `2026-05-20 08:31:00 +0800` after observing pre-write hash `8a9c3fffc8ed1e7343a885e70f81625888639ccabeba8fd313cab6932d5a1711` and duplicate-searching the Board/claims for `Force Index`, `force_index_displacement`, `SI 1m Force`, `081449`, and `IBKR_SI1M_FORCE`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T081449+0800-codex-ibkr-si1m-force-index-displacement-7d-gate1-v1/`.
- Factor id: `ibkr_si1m_force_index_displacement_7d_gate1_v1`.
- Branch path: `DisplacementExpansion -> ForceIndexDisplacement -> ibkr_si1m_force_index_displacement_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 1m`.
- Diversity/source note: this gives the public Force Index displacement/reclaim family a real exact-root `SI/1m` precious-metals futures attempt, distinct from Chaikin A/D, Aroon/CCI, volatility-expansion reclaim, tight-range `5m` fade, ATR exhaustion, VWAP/RVOL, Percent-B/MFI, and ROC/Hilbert equity-index cells.
- Provider/Gate 1 evidence: retained real IBKR `SI 202607` `1m 7 D`, `9453` normalized rows from source provider root `20260520T065652+0800-codex-ibkr-si1m-aroon-cci-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=2`.
- Cost/density evidence: this root failed both trade density and cost stress. `SI/force_reclaim/1m` had `2` trades, `0.0%` win rate, raw `-0.22%`, `1bps=-0.26%`, `2bps=-0.30%`, and `5bps=-0.42%`; `SI/force_dense/1m`, `SI/force_balanced/1m`, and `SI/force_quality/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si1m_force_index_displacement_7d_gate1_v1.py`.

Interpretation:

- Force Index displacement did not rescue the `SI/1m` precious-metals lane. The exact rooted branch was preserved and the AQ run completed, but the only nonzero row was sparse and raw-negative before costs. Do not downstream or add overlays to this root; keep it as another negative public-family sample and spend downstream attention on existing `5bps/side` survivors such as SI `5m` tight-range fade only after terminal execution-readiness evidence is available.

## Diagnostic Addendum: IBKR SI 5m Tight-Range Band Expansion Fade Tail-800 Analyze Materialization

Append-only diagnostic row at `2026-05-20 08:25:40 +0800` after observing pre-write hash `9cbd3867e9165cad95b3cc908d6951fed138947ec3219391bd3fc0c7cac2df59` and duplicate-searching the Board for `sample-diagnostic-tail800`, `SI5m Tight-Range Sample`, and `Tight-Range.*Diagnostic`.

- Parent Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/`.
- Parent exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/downstream-exact-si-5m-tight-range-band-expansion-fade-20260520T072345+0800/`.
- Diagnostic packet: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/downstream-exact-si-5m-tight-range-band-expansion-fade-20260520T072345+0800/sample-diagnostic-tail800-20260520T082338+0800/`.
- Branch under test remains: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Diagnostic setup: copied the retained real IBKR `SI 202607` `5m` file's latest `800` bars into `si5m_tail800.csv` and ran a bounded `analyze` with that file as explicit LTF/MTF/HTF input against an isolated sample state. This does not replace the full-window downstream root; it tests whether execution materialization is impossible or whether the full `6047`-row analyze path is the blocker.
- Command evidence: `command-output/analyze_sample.exit=0`; sample state materialized `workflow_snapshot.json`, `analyze_runs.json`, `execution_candidate.json`, and `execution_tree_trace.json`. The parent full-window downstream still has `03_analyze_seed.exit=124`, no completed post-ranker analyze, and no parent `execution_candidate.json`/`execution_tree_trace.json`.
- Sample execution readback: `execution_candidate.json` reports `candidate_status=no_trade`, `actionable=false`, selected `trade_direction=Bull`, `pre_bayes_gating_status=observe_only`, `pre_bayes_quality_score=0.336`, `pda_sequence=regime_disagreement:cluster_0:trend:1.000`, `hybrid_regime_label=range_choppy`, and `mtf_direction=bearish` with `mtf_alignment=1.0000` / `mtf_entry_alignment=0.8861`.
- Sample workflow readback: `latest_analyze.phase_summary` reports `execution_readiness=0.171`, `execution_gate=execution_blocked`, `execution_edge=0.284`; risk flags include `pre_bayes:multi_timeframe_direction_conflict`, `pre_bayes:hostile_liquidity_requires_high_uncertainty`, and `pre_bayes:pda_regime_family_disagreement`. The sample MTF readback covered synthetic `15m/1h/1d` from the explicit frames and reported missing `1m/5m/30m/4h` in the analyzer's interval taxonomy.
- Hard predicate status: sample materialization proves the SI branch can produce execution artifacts on a smaller window, but it still fails the practical gates: `execution_readiness=0.171 < 0.65`, `pda_hybrid_alignment` is not true and is represented as PDA/regime-family disagreement, selected direction `Bull` conflicts with bearish multi-timeframe bias, and promotion/trade remain false. The parent full-window exact downstream remains fail-closed with `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false` because its full-window analyze did not complete.
- Terminal decision: `si5m_tight_range_tail800_diagnostic_materialized_but_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This narrows the SI `5m` tight-range blocker. It is no longer accurate to say execution materialization is categorically impossible: a bounded tail sample does produce candidate/tree artifacts. But the sample is still not practical and does not meet the user's hard gates. The next aligned repair must either make the full-window analyze finish with real `1m/5m/15m/30m/1h/4h/1d` ladder coverage or change the same-root execution-facing filter so PDA family, selected direction, and multi-timeframe bias agree while preserving the original `5bps/side` cost survivor. Do not promote this branch from the tail sample.

## Gate 1 Negative: IBKR MES 1m Liquidity-Sweep Reject Short 7D

Append-only terminal row at `2026-05-20 08:35:00 +0800` after observing pre-write hash `777399906e789ab18d53fd170b436dd5b1d36193523e7c7be95a12798036ef93` and duplicate-searching the Board/claims for `082124`, `mes1m_liquidity_sweep_reject_short`, `MES 1m Liquidity`, and `LiquiditySweepRejectShort.*MES`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T082124+0800-codex-ibkr-mes1m-liquidity-sweep-reject-short-7d-gate1-v1/`.
- Factor id: `ibkr_mes1m_liquidity_sweep_reject_short_7d_gate1_v1`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_mes1m_liquidity_sweep_reject_short_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Transfer note: this is an exact MES sibling transfer of the stronger M2K `LiquiditySweepRejectShort` idea, used to test whether the profitable branch generalizes to another equity-index micro future without changing the rooted factor grammar.
- Provider/Gate 1 evidence: retained real IBKR `MES 202606` `1m 7 D`, `9432` normalized rows from source provider root `20260520T012135+0800-codex-ibkr-mes1m-opening-drive-vwap-rvol-continuation-7d-gate1-v1`; provider fetch exited `0`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=3`; `rank_total_trade_count=0`.
- Cost/density evidence: all three exact-root short variants produced zero trades: `MES/dense/1m_short`, `MES/balanced/1m_short`, and `MES/quality/1m_short` each had `0` trades and `0.00%` at `0/1/2/5bps/side`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner under the run root.

Interpretation:

- The M2K liquidity-sweep short edge does not transfer mechanically to `MES/1m` with this exact branch. The failure is not provider, compile, or rooted metadata; the exact factor simply generated zero AQ trades on real retained MES rows. Do not downstream or tune this sibling by lowering density gates. Keep practical repair attention on the original M2K survivor or SI `5m` tight-range fade, and require any future cross-symbol clone to prove real trade density before entering Pre-Bayes/BBN/CatBoost/execution-tree.

## Simulated-Trade Admission Observation: IBKR SI 5m Tight-Range Band Expansion Fade

Append-only terminal row at `2026-05-20 08:22:00 +0800` after observing pre-write
hash `777399906e789ab18d53fd170b436dd5b1d36193523e7c7be95a12798036ef93` and
duplicate-searching the Board for `080358`,
`simulated-trade-admission-si-5m-tight-range-band-expansion-fade-20260520T080358`,
and `si5m_tight_range_band_expansion_fade_simulated_trade`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/simulated-trade-admission-si-5m-tight-range-band-expansion-fade-20260520T080358+0800/`.
- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/`.
- Branch path: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Repair evidence: ingested `9` same-Auto-Quant-workspace simulated trades (`7` wins / `2` losses), trained CatBoost on `10` history rows, and applied exact-branch ranker scores. Exact branch scored `raw_path_score=0.850627707257148`; command exits `01` through `15` were `0`.
- Final readback: closed-loop admission stayed `status=fail_closed`, `ready=false`, `candidate_status=execution_observe_only`, and `decision_hint=execution_guarded_due_to_pda_hybrid_disagreement`. Execution candidate stayed `actionable=false` / `candidate_status=no_trade`. Execution tree reported `execution_readiness=0.2344186944501619`, `hybrid_transition_hazard=0.9679827616849933`, `pda_hybrid_alignment=false`, and path-ranker score not visible/used by final execution tree. Validation remained short: `mature_rows=2`, `history_mature_rows=10`, `raw_scored_mature=10/30`, `production_validation=9/30`, `observation_validation=9/30`. Post-ranker analyze command output exists, but no stable `16_analyze_after_ranker.exit` was present at terminal readback.
- Decision: observe-only repair evidence; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This preserves the useful signal without overstating it: SI `5m` tight-range fade remains a strong observation/repair lead because it had real `5bps/side` Gate 1 survivors and simulated feedback improved ranker visibility, but it is still not practical. The remaining blocker is execution admission: exact execution candidate materialization, lower transition hazard, PDA alignment, and mature validation rows.

## Clean Gate 1 Negative: IBKR MNQ 1m Alligator/Fractal Breakout 7D

Append-only terminal row at `2026-05-20 08:42:00 +0800` after observing current Board tail, preserving the earlier hygiene-blocked Alligator/Fractal row, and duplicate-searching the Board for `083848`, `Clean Gate 1 Negative: IBKR MNQ 1m Alligator`, `Alligator/Fractal.*083848`, and `Bill Williams Alligator`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T083848+0800-codex-ibkr-mnq1m-alligator-fractal-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_alligator_fractal_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> BillWilliamsAlligatorFractal -> ibkr_mnq1m_alligator_fractal_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Hygiene correction: this rerun replaces the earlier `044918/045240/045405` Alligator/Fractal packets for verdict purposes because those packets had MNQ branch labels but MGC provider/material data paths. The new packet's `provider_provenance_matrix.csv`, material JSON `data_path`, rank rows, and `terminal_metrics.json` all point to retained real IBKR `MNQ 202606` `1m 7 D` rows.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1m 7 D`, `9481` normalized rows from source provider root `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=2`.
- Cost/density evidence: Alligator/Fractal stayed too sparse and too small after friction. `MNQ/fractal_dense/1m` had `2` trades, `50.0%` win rate, raw `+0.01%`, `1bps=-0.03%`, `2bps=-0.07%`, and `5bps=-0.19%`; `MNQ/fractal_balanced/1m`, `MNQ/fractal_quality/1m`, and `MNQ/fractal_retest/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `checks/00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`; provenance/material scans show MNQ paths and no MGC data-path residue in the clean run.

Interpretation:

- This converts the previous Alligator/Fractal artifact-hygiene blocker into a valid exact-root Gate 1 verdict. The verdict is still negative: the public Bill Williams family did not produce enough `MNQ/1m` trade density and failed by `1bps/side`, with no `2bps/side` or `5bps/side` survivor. Do not downstream this root or count the older hygiene-blocked packets as factor evidence. The practical work remains focused on existing `5bps/side` survivors such as M2K `1m` liquidity-sweep reject-short and SI `5m` tight-range fade, where the blocker is execution admission rather than Gate 1 economics.

## Gate 1 Negative: IBKR MES 1m Elder/MACD Impulse 7D

Append-only terminal row at `2026-05-20 08:32:57 +0800` after observing pre-write
hash `e001780f8e7c332d5f36e32e9d9b0ff7ab90ead565af9b57a68df7502ff62e08`
and duplicate-searching the Board/claims for `MES/1m Elder`,
`mes1m_elder_macd_impulse`, `082718`, and `ElderMacdImpulse`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T082718+0800-codex-ibkr-mes1m-elder-macd-impulse-7d-gate1-v1/`.
- Factor id: `ibkr_mes1m_elder_macd_impulse_7d_gate1_v1`.
- Branch path: `TrendExpansion -> ElderMacdImpulse -> ibkr_mes1m_elder_macd_impulse_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Diversity/source note: this gives Elder impulse / MACD histogram continuation a fresh `MES/1m` public-family exact-root attempt, distinct from the prior MGC `5m` Elder packet and from MES KAMA/ROC/liquidity-sweep, Force/Chaikin, and VWAP/RVOL variants.
- Provider/Gate 1 evidence: retained real IBKR `MES 202606` `1m 7 D`, `9432` normalized rows from source provider root `20260520T073250+0800-codex-ibkr-mes1m-kama-efficiency-pullback-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Provider-status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=28`.
- Cost/density evidence: `MES/elder_quality/1m` had `4` trades, raw `+0.12%`, `1bps=+0.04%`, `2bps=-0.04%`, and `5bps=-0.28%`; `MES/elder_balanced/1m` had `6` trades, raw `+0.06%`, `1bps=-0.06%`, `2bps=-0.18%`, and `5bps=-0.54%`; `MES/elder_pullback/1m` had `10` trades, raw `+0.02%`, `1bps=-0.18%`, `2bps=-0.38%`, and `5bps=-0.98%`; `MES/elder_dense/1m` had `8` trades, raw `-0.05%`, `1bps=-0.21%`, `2bps=-0.37%`, and `5bps=-0.85%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mes1m_elder_macd_impulse_7d_gate1_v1.py`.

Interpretation:

- Elder/MACD got a fair public-family slot on a new `MES/1m` exact root and had enough trades to judge, but the positive raw edge vanished by `2bps/side`. Do not downstream this branch or lower the cost gate. It is useful diversity evidence; the next work should either repair an existing `5bps/side` survivor's execution predicates or try a larger-excursion family/market cell.

## Gate 1 Negative: IBKR MNQ 1h Keltner Channel Trend Long

Append-only terminal row at `2026-05-20 08:44:00 +0800` after observing pre-write hash `a776111bcde3738ba3082c95b92d6680153b352220e6233aca89f94034ad040a` and duplicate-searching the Board/claims for `keltner_trend_channel_long_v1`, `mnq1h-keltner-channel-trend-long-exact`, and `PublicStrategyDiversity -> KeltnerChannel`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T235624+0800-codex-ibkr-mnq1h-keltner-channel-trend-long-exact-gate1-v1/`.
- Factor id: `keltner_trend_channel_long_v1`.
- Branch path: `PublicStrategyDiversity -> KeltnerChannel -> keltner_trend_channel_long_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1h`.
- Diversity/source note: this is a public-strategy Keltner channel trend-breakout exact-root attempt on `MNQ/1h`, separate from 1m Keltner/ATR, VWAP/RVOL, compression, ROC, PPO, Donchian, Darvas, and oscillator lanes.
- Provider/Gate 1 evidence: retained real IBKR `MNQ 202606` `1h 1 M`, `500` normalized rows from source root `20260519T230734+0800-codex-ibkr-futures-mnq-microtrend-1h-pda-transition-stability-light-gate1-v1`; `local_cache_replay=false_reused_self_owned_retained_ibkr_packet`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=1`; `rank_total_trade_count=9`.
- Cost/density evidence: `MNQ/keltner_trend_channel_long/1h` had `9` trades, `33.3333%` win rate, raw `-0.95%`, `1bps=-1.13%`, `2bps=-1.31%`, and `5bps=-1.85%`. `exact_1h_survivors_2bps=[]`; `exact_1h_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material file, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1h_keltner_channel_trend_long_exact_gate1_v1.py`.

Interpretation:

- Keltner channel trend-long was a clean wider-timeframe public-family attempt, but it failed the practical economics before downstream. Do not promote, downstream, or use the negative `1h` row to rescue weaker 1m siblings. Keep this as futures public-family coverage and continue either with a fresh real-cost Gate 1 lane when the AQ backend is free or with execution-predicate repair on an existing `5bps/side` survivor.

## Diagnostic Observation: IBKR SI 5m Tight-Range Cleaned MTF Ladder

Append-only diagnostic row at `2026-05-20 08:57:36 +0800` after observing pre-write hash `c54bb198a662ab9d7f1bf3ac804881d09848c45147887dfdec080891dd6d7ca7` and duplicate-searching the Board/claims for `ladder-diagnostic-explicit-cleaned-20260520T084947`, `SI 5m Cleaned-Ladder`, `si5m_cleaned_ladder_diagnostic`, and `084947`.

- Diagnostic root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/simulated-trade-admission-si-5m-tight-range-band-expansion-fade-20260520T080358+0800/ladder-diagnostic-explicit-cleaned-20260520T084947+0800/`.
- Parent simulated-admission root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/simulated-trade-admission-si-5m-tight-range-band-expansion-fade-20260520T080358+0800/`.
- Branch remains: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Diagnostic purpose: test whether the prior missing `4h` / `30m` / `1d` MTF coverage was because the SI wrapper generated CSV frames but passed only `--data-ltf 5m`, `--data-mtf 15m`, and `--data-htf 1h`, leaving analyzer auto-fill unable to discover sibling intervals. This is a command-shape diagnostic only, not a new factor or promotion attempt.
- Ladder materialized: convention-shaped cleaned JSON siblings were created for `1m=9453`, `5m=6047`, `15m=2016`, `30m=1008`, `1h=504`, `4h=136`, and `1d=27` rows. The `1m` file came from retained real IBKR `SI 202607` `1m 7D`; `5m` came from the parent retained real IBKR `SI 202607` `5m 1M`; higher frames were resampled from the parent `5m` file.
- Command evidence: the first shell harness hit a macOS portability issue (`timeout: command not found`) and was replaced by a Python `subprocess.run(..., timeout=240)` wrapper in the same diagnostic root. Final command exits were `analyze_cleaned_ladder.exit=124` and `workflow_cleaned_ladder.exit=0`.
- Readback: `workflow_snapshot.json` exists, but `execution_candidate.json` and `execution_tree_trace.json` did not materialize before the 240s analyze cap. `checks/terminal_metrics.json` records `decision=si5m_cleaned_ladder_diagnostic_analyze_timeout_partial_workflow`, empty `multi_timeframe_summary`, no execution readiness, no transition hazard, and no PDA alignment verdict.
- Terminal decision: diagnostic-only observation; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The SI tight-range root still has real `5bps/side` Gate 1 survivors, but the cleaned full-ladder attempt did not repair execution admission. The immediate blocker is no longer public-factor diversity; the Board already has many clean public-family negatives plus this cost survivor. The practical repair path is to make full-window analyze finish and materialize exact execution candidate/tree artifacts with PDA/regime/direction alignment, or to move to the other existing cost survivor (`M2K/1m` liquidity-sweep reject-short) for the same execution-predicate repair. Do not lower the gates or call this trade-usable from ladder coverage alone.

## Gate 1 Negative: IBKR MES 1m Schaff Trend Cycle 7D

Append-only terminal row at `2026-05-20 08:58:36 +0800` after observing pre-write hash `fc94cf40df853d6c0deea46b4f355a1b11f3a1100a358b822c13a17cfa2aacce` and duplicate-searching the Board/claims for `085335`, `schaff_trend_cycle`, `SchaffTrendCycle`, and `Schaff Trend`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T085335+0800-codex-ibkr-mes1m-schaff-trend-cycle-7d-gate1-v1/`.
- Factor id: `ibkr_mes1m_schaff_trend_cycle_7d_gate1_v1`.
- Branch path: `TrendExpansion -> SchaffTrendCycle -> ibkr_mes1m_schaff_trend_cycle_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Diversity/source note: this gives Schaff Trend Cycle / STC trend-cycle continuation a real exact-root `MES/1m` futures Gate 1 slot, separate from VWAP/RSI/RVOL, Elder/MACD, KAMA, ROC, Gann HiLo, Keltner, Alligator/Fractal, and oscillator-reclaim variants.
- Provider/Gate 1 evidence: retained real IBKR `MES 202606` `1m 7 D`, `9432` normalized rows from source provider root `20260520T082718+0800-codex-ibkr-mes1m-elder-macd-impulse-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=143`.
- Cost/density evidence: STC generated enough trades to judge, but no row survived the hard cost screen. `MES/stc_balanced/1m` had `33` trades, raw `+1.02%`, `1bps=+0.36%`, `2bps=-0.30%`, and `5bps=-2.28%`; `MES/stc_reclaim/1m` had `45` trades, raw `+0.86%`, `1bps=-0.04%`, `2bps=-0.94%`, and `5bps=-3.64%`; `MES/stc_dense/1m` had `49` trades, raw `+0.63%`, `1bps=-0.35%`, `2bps=-1.33%`, and `5bps=-4.27%`; `MES/stc_quality/1m` had `16` trades, raw `-0.01%`, `2bps=-0.65%`, and `5bps=-1.61%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mes1m_schaff_trend_cycle_7d_gate1_v1.py`.

Interpretation:

- Schaff Trend Cycle answers the factor-diversity complaint with a time-tested trend-cycle family and a dense real IBKR futures run. The economics are still not practical: the best raw edge is consumed by `2bps/side`, and `5bps/side` is decisively negative. Do not downstream this root or keep retuning STC immediately; preserve it as useful negative coverage and continue rotating to materially different high-excursion families or repairing existing `5bps/side` survivors.

## Gate 1 Negative: IBKR MES 1m Gann HiLo Activator 7D

Append-only terminal row at `2026-05-20 08:58:36 +0800` after observing pre-write hash `fc94cf40df853d6c0deea46b4f355a1b11f3a1100a358b822c13a17cfa2aacce` and duplicate-searching the Board/claims for `085457`, `gann_hilo`, `GannHiLoActivator`, and `Gann HiLo`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T085457+0800-codex-ibkr-mes1m-gann-hilo-activator-7d-gate1-v1/`.
- Factor id: `ibkr_mes1m_gann_hilo_activator_7d_gate1_v1`.
- Branch path: `TrendExpansion -> GannHiLoActivator -> ibkr_mes1m_gann_hilo_activator_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Diversity/source note: this gives the classic Gann HiLo Activator trend-following family a real exact-root `MES/1m` futures Gate 1 attempt, separate from STC, Elder/MACD, KAMA, ROC, Keltner, Alligator/Fractal, VWAP/RVOL, and oscillator/volume-flow lanes.
- Provider/Gate 1 evidence: retained real IBKR `MES 202606` `1m 7 D`, `9432` normalized rows from source provider root `20260520T082718+0800-codex-ibkr-mes1m-elder-macd-impulse-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=162`.
- Cost/density evidence: Gann HiLo produced dense raw-positive rows but failed real-cost survival. `MES/hilo_balanced/1m` had `27` trades, raw `+0.95%`, `1bps=+0.41%`, `2bps=-0.13%`, and `5bps=-1.75%`; `MES/hilo_dense/1m` had `59` trades, raw `+1.17%`, `1bps=-0.01%`, `2bps=-1.19%`, and `5bps=-4.73%`; `MES/hilo_quality/1m` had `5` trades, raw `-0.04%`, `2bps=-0.24%`, and `5bps=-0.54%`; `MES/hilo_retest/1m` had `71` trades, raw `+0.77%`, `1bps=-0.65%`, `2bps=-2.07%`, and `5bps=-6.33%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mes1m_gann_hilo_activator_7d_gate1_v1.py`.

Interpretation:

- Gann HiLo also received a fair public-family chance with real retained IBKR data and dense trades, but the branch fails the same practical economics: no `2bps/side` or `5bps/side` survivor despite positive raw rows. This should count as diversity evidence, not as a tradable factor. The next productive step is either a larger-excursion public family on a fresh symbol/timeframe cell, or same-root execution repair on an already cost-surviving branch such as `M2K/1m` liquidity-sweep reject-short or `SI/5m` tight-range fade.

## Impact-Radius Fail-Closed: NET 5m Entry Window Trim

Append-only terminal row at `2026-05-20 08:58:30 +0800` after observing pre-write hash `fc94cf40df853d6c0deea46b4f355a1b11f3a1100a358b822c13a17cfa2aacce` and duplicate-searching the Board for `entry_window_trim`, `Entry Window Trim`, `232936`, `233743`, and `exact_net_5m_entry_window_trim`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T232936+0800-codex-yf-ai-security-net5m-entry-window-trim-gate1-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T232936+0800-codex-yf-ai-security-net5m-entry-window-trim-gate1-v1/downstream-exact-net-5m-entry-window-trim-20260519T233743+0800/`.
- Source near-pass: the earlier `NET/5m` PDA-sequence consistency light clone had `32` trades, survived `5bps/side`, reached `execution_readiness=0.67`, and kept `pda_hybrid_alignment=true`, but failed the hard gate at `transition_hazard=0.6304902417013154`.
- Branch path: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_net_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1 -> entry_window_trim_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> NET -> 5m`.
- Repair intent: one-predicate same-root probe that ended entries at `19:05` instead of `19:20`, preserving the original PDA-sequence strategy shape and cost-density floor while testing whether late-session entries were driving the transition-hazard miss.
- Gate 1 evidence: fresh YF/yfinance fetches for `NET 5m/15m/30m/1h/1d` all exited `0`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `rank_rows=5`; `rank_total_trade_count=41`; exact `NET/5m` had `31` trades, `61.2903%` win rate, Sharpe `2.9947`, raw `+3.52%`, `+2.90%` at `1bps/side`, `+2.28%` at `2bps/side`, and `+0.42%` at `5bps/side`; downstream was allowed.
- Downstream evidence: commands `00` through `20` exited `0`, `exact_branch_survived=true`, and `31` real trades were ingested (`19` wins / `12` losses). Path-ranker score was visible/used/validation-ready=`true/true/true`, and `mature_rows/history_mature_rows=3/34`.
- Terminal blocker: closed-loop admission remained `fail_closed`; live structural execution candidate stayed `actionable=false`, `ready=false`, and `status=execution_blocked`; execution tree stayed `gate_status=observe`, `branch=transition_guardrail`, and `execution_readiness=0.67`, but `transition_hazard=0.979444675748211` and `pda_hybrid_alignment=false` failed the hard predicates.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `real-trades/all_real_trades.jsonl`, downstream `state/YF_AI_SECURITY_NET5M_ENTRY_WINDOW_TRIM_DOWNSTREAM/workflow_snapshot.json`, downstream `state/YF_AI_SECURITY_NET5M_ENTRY_WINDOW_TRIM_DOWNSTREAM/execution_candidate.json`, and downstream `state/YF_AI_SECURITY_NET5M_ENTRY_WINDOW_TRIM_DOWNSTREAM/execution_tree_trace.json`.
- Verification evidence: `python3 -m json.tool` passed for both metrics JSON files, and `python3 -m py_compile` passed for `run_yf_ai_security_net5m_entry_window_trim_gate1_v1.py` and `run_yf_ai_security_net5m_entry_window_trim_exact_downstream_v1.py`.
- Terminal decision: `exact_net_5m_entry_window_trim_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The CRWD/YF `5m` mother-template still transfers useful real-cost density into `NET/5m`, but this one-predicate entry-window trim is not the right repair. It preserved the `5bps/side` survivor and execution readiness, yet made the downstream predicate mix worse by flipping `pda_hybrid_alignment=false` and lifting `transition_hazard` far above the original near-pass. Do not lower the hazard gate, call this trade-usable, or stack another late-session trim. The next NET same-root repair should isolate PDA/hybrid family agreement directly, or pause NET after repeated repairs and move the mother-template impact-radius search to another adjacent clone with the same exact-root gates.

## Gate 1 Negative: IBKR M2K 5m Darvas Box Breakout Retained

Append-only terminal row at `2026-05-20 09:08:00 +0800` after observing pre-write hash `44aad6f5b5c376a6c7669732ddc86dbfb3c17ba7f4af894a67344e01a3fe6f28` and duplicate-searching the Board/claims for `085444`, `m2k5m_darvas_box_breakout`, `M2K 5m Darvas`, and `DarvasBoxBreakout -> ibkr_m2k5m`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T085444+0800-codex-ibkr-m2k5m-darvas-box-breakout-retained-gate1-v1/`.
- Factor id: `ibkr_m2k5m_darvas_box_breakout_retained_gate1_v1`.
- Branch path: `VolatilityExpansion -> DarvasBoxBreakout -> ibkr_m2k5m_darvas_box_breakout_retained_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 5m`.
- Diversity/source note: this gives Darvas box breakout / volume expansion a lower-turnover `M2K/5m` exact-root attempt, separate from the stronger M2K `1m` liquidity-sweep reject-short survivor and from M2K `1m` Chande/CMO, Donchian/Turtle, Keltner, McGinley, GMMA, and TD Sequential lanes.
- Provider/Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D` rows from source provider root `20260520T064312+0800-codex-ibkr-m2k1m-donchian-turtle-breakout-7d-gate1-v1`, resampled to `1879` `5m` rows; `local_cache_replay=false_retained_real_ibkr_same_session_resampled_1m_to_5m`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=8`.
- Cost/density evidence: only `M2K/box_dense/5m` traded, with `8` trades, `12.5%` win rate, raw `-0.07%`, `1bps=-0.23%`, `2bps=-0.39%`, and `5bps=-0.87%`. `M2K/box_balanced/5m`, `M2K/box_quality/5m`, and `M2K/box_retest/5m` each had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k5m_darvas_box_breakout_retained_gate1_v1.py`.

Interpretation:

- The lower-turnover Darvas/box breakout hypothesis did not improve practical M2K economics. Provider provenance, AQ mechanics, and rooted metadata were clean, but the exact `5m` branch was sparse and raw-negative before costs. Do not downstream or overlay this Darvas branch. Keep M2K practical repair attention on the existing `1m` liquidity-sweep reject-short/RVOL-PDA survivor, where the blocker remains exact execution candidate materialization, transition hazard, PDA alignment, and mature validation rather than Gate 1 cost density.

## Gate 1 Negative: IBKR MES 1m RWI Trend Breakout 7D

Append-only terminal row at `2026-05-20 09:17:00 +0800` after observing pre-write hash `a3382ade1598ef18f9d38c07a72daf30126b205a8aaa764a2f497d0c80bd8687` and duplicate-searching the Board/claims for `084256`, `rwi_trend_breakout`, `RWI trend breakout`, and `RandomWalkIndexTrendBreakout`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T084256+0800-codex-ibkr-mes1m-rwi-trend-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mes1m_rwi_trend_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> RandomWalkIndexTrendBreakout -> ibkr_mes1m_rwi_trend_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MES -> 1m`.
- Diversity/source note: this gives Random Walk Index / RWI trend breakout a real exact-root `MES/1m` public-family attempt, distinct from MES Elder/MACD, Gann HiLo, Schaff Trend Cycle, ROC, KAMA, VWAP/RVOL, and liquidity-sweep branches.
- Provider/Gate 1 evidence: retained real IBKR `MES 202606` `1m 7 D`, `9432` normalized rows from source provider root `20260520T082718+0800-codex-ibkr-mes1m-elder-macd-impulse-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=2`.
- Cost/density evidence: RWI was too sparse for the user's practical gate. `MES/rwi_retest/1m` had `2` trades, `100.0%` win rate, raw `+0.09%`, `1bps=+0.05%`, `2bps=+0.01%`, and `5bps=-0.11%`; `MES/rwi_dense/1m`, `MES/rwi_balanced/1m`, and `MES/rwi_quality/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mes1m_rwi_trend_breakout_7d_gate1_v1.py`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- RWI trend breakout was a clean provider/AQ/rooted-metadata run, but it fails before downstream because there is no practical trade density and no `5bps/side` survivor. Do not downstream or overlay this branch. Treat it as useful public-family negative coverage and keep practical repair focus on existing true cost-density survivors such as M2K `1m` liquidity-sweep reject-short/RVOL-PDA and SI `5m` tight-range fade, where the remaining blocker is execution admission rather than Gate 1 economics.

## Gate 1 Negative: IBKR MYM 15m VWMA Pullback Breakout Retained

Append-only terminal row at `2026-05-20 09:24:00 +0800` after observing pre-write hash `cfcc1c8579e56f7fae306772e8bac7b3b74d3b0a40418dcb43aefc0b9d600162` and duplicate-searching the Board/claims for `MYM 15m VWMA`, `mym15m_vwma`, `VwmaPullbackBreakout`, and `091852`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T091852+0800-codex-ibkr-mym15m-vwma-pullback-breakout-retained-gate1-v1/`.
- Factor id: `ibkr_mym15m_vwma_pullback_breakout_retained_gate1_v1`.
- Branch path: `TrendExpansion -> VwmaPullbackBreakout -> ibkr_mym15m_vwma_pullback_breakout_retained_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 15m`.
- Diversity/source note: this gives VWMA pullback breakout / volume-weighted trend a fresh `MYM/15m` public-family slot, separate from MYM `1m` TOD/session momentum, MYM opening-range failure, M2K Darvas, and MES/MNQ trend-cycle indicators.
- Provider/Gate 1 evidence: retained real IBKR `MYM 202606` `1m 7 D` rows from source provider root `20260520T014537+0800-codex-ibkr-mym1m-tod-session-momentum-7d-gate1-v1`, resampled to `631` `15m` rows; `local_cache_replay=false_retained_real_ibkr_same_session_resampled_1m_to_15m`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=7`.
- Cost/density evidence: `MYM/vwma_dense/15m` had `3` trades, `66.6667%` win rate, raw `+0.47%`, `1bps=+0.41%`, `2bps=+0.35%`, and `5bps=+0.17%`, but it failed the density requirement. `MYM/vwma_retest/15m` had `3` trades, raw `-0.04%`, `2bps=-0.16%`, and `5bps=-0.34%`; `MYM/vwma_balanced/15m` had `0` trades; `MYM/vwma_quality/15m` had `1` trade, raw `-0.05%`, `2bps=-0.09%`, and `5bps=-0.15%`. `exact_15m_survivors_2bps=[]`; `exact_15m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T091852+0800-codex-ibkr-mym15m-vwma-pullback-breakout-retained-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- VWMA pullback breakout added a genuinely different volume-weighted trend family on a fresh `MYM/15m` futures cell. The best row stayed positive after `5bps/side`, but `3` trades over the retained window is too sparse for the user's 1-3 trades/day practical density target and downstream admission. Do not promote or downstream this branch; keep it as diversity evidence and continue toward either larger-excursion public families or exact-root execution-predicate repair on existing real `5bps/side` survivors.

## AQ Workspace Parity Repair: IBKR M2K 1m Liquidity-Sweep Reject-Short RVOL/PDA

Append-only diagnostic row at `2026-05-20 09:22:54 +0800` after observing pre-write hash `afae1f36b30ae1dec9f7a51a4f2ae4e8ae181b859de86c9c2de52502a7a9ae3e` and duplicate-searching the Board/claims for `aq_workspace_exact_futures_parity`, `20260520T092254`, `054532.*futures`, and `exact_futures_parity`.

- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/`.
- Current same-root downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T054532+0800/`.
- Branch path remains exact-rooted: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Repair purpose: make the latest Auto-Quant workspace execute the exact futures short survivor, not the generic `TomacNQ_KillzoneBreakout` seed and not a spot-mode proxy. This directly addresses the user's requirement to keep training through Auto-Quant while preserving exact branch identity.
- Repair evidence: `auto-quant-prepare` returned `status=prepared`, `dependency_status_after=dependency_ready_data_ready`, and the workspace contains `M2K_USD` `1m/5m/15m/30m/1h/4h/1d` spot and futures feathers. The exact strategy file `IbkrFutM2KLiquiditySweepRejectShortRvolPdaGuardRvol100Ema551MinV1.py` was restored from the source Gate 1 `agent-material` into the current workspace `user_data/strategies_external/` and passed `python3 -m py_compile`. The current `config.tomac.json` was restored to `trading_mode=futures`, `margin_mode=isolated`, and `exchange._ft_has_params.uses_leverage_tiers=false` from the original exact AQ workspace.
- Parity run: `cd state/.deps/auto-quant && ./.venv/bin/python run_tomac.py` exited `0` after the repair. Exact strategy result: `19` shorts on `M2K/USD`, `63.1579%` win rate, raw `+2.60%`, profit factor `5.3879`, max drawdown `-0.2535%`, Sharpe `35.7718`, Sortino `194.2923`, and Calmar `2794.7759`. This matches the prior Gate 1 survivor packet; the generic `TomacNQ_KillzoneBreakout` still had `0` trades and remains non-exact-branch noise.
- Remaining downstream blocker: this repair does not change the hard execution gates. The latest same-root downstream readback still has `exact_branch_survived=true`, `execution_candidate_status=no_trade`, `execution_readiness=0.3180972047048167`, `transition_hazard=0.9184975817511946`, `pda_hybrid_alignment=false`, `promotion_allowed=false`, and `trade_usable=false`. It also shows `mature_rows=2`, `history_mature_rows=20`, and MTF analyze coverage only `1m/15m/1h` with `5m/30m/4h/1d` missing from the execution readback.
- Evidence file: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/simulated-trade-admission-m2k-1m-liquidity-sweep-reject-short-rvol-pda-guard-20260520T054532+0800/checks/aq_workspace_exact_futures_parity_20260520T092254+0800.json`.
- Terminal decision: `aq_workspace_exact_futures_parity_repaired_but_downstream_execution_predicates_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is real progress on the strongest M2K survivor, but not a promotion. The AQ workspace now preserves the exact futures short branch and reproduces the survivor instead of silently degrading to spot/generic zero-trade behavior. The next same-root step must target the execution blockers directly: full `1m/5m/15m/30m/1h/4h/1d` analyze coverage, PDA sequence consistency above the floor, regime/root alignment between `RangeReversion` branch and current `TrendExpansion/BullTrendAcceleration` market state, `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and stable `execution_readiness >= 0.65`.

## Gate 1 Negative: IBKR MYM 1m TTM Squeeze Momentum 7D

Append-only terminal row at `2026-05-20 09:31:00 +0800` after observing pre-write hash `4dbd173e846f2ce19041d6d772534e7737be8cc6c841b4d10c502311a9e6c017` and duplicate-searching the Board/claims for `MYM 1m TTM`, `mym1m_ttm_squeeze`, `TTMSqueezeMomentum -> ibkr_mym1m`, and `092213`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T092213+0800-codex-ibkr-mym1m-ttm-squeeze-momentum-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_ttm_squeeze_momentum_7d_gate1_v1`.
- Branch path: `VolatilityCompression -> TtmSqueezeMomentum -> ibkr_mym1m_ttm_squeeze_momentum_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Diversity/source note: this gives TTM / Bollinger-Keltner squeeze momentum a real exact-root `MYM/1m` futures Gate 1 slot, separate from MYM TOD/session momentum, MYM 15m VWMA pullback breakout, MYM opening-range failure, and the MES/MNQ trend-cycle public-family packets.
- Provider/Gate 1 evidence: retained real IBKR `MYM 202606` `1m 7 D`, `9506` normalized rows from source provider root `20260520T023551+0800-codex-ibkr-mym1m-liquidity-sweep-reject-short-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=95`.
- Cost/density evidence: TTM squeeze generated enough trades to judge, but no exact row survived the user's real-cost screen. `MYM/squeeze_quality/1m` had `26` trades, raw `+0.31%`, `1bps=-0.21%`, `2bps=-0.73%`, and `5bps=-2.29%`; `MYM/squeeze_dense/1m` had `15` trades, raw `+0.36%`, `1bps=+0.06%`, `2bps=-0.24%`, and `5bps=-1.14%`; `MYM/squeeze_retest/1m` had `25` trades, raw `+0.16%`, `2bps=-0.84%`, and `5bps=-2.34%`; `MYM/squeeze_balanced/1m` had `29` trades, raw `+0.09%`, `2bps=-1.07%`, and `5bps=-2.81%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_ttm_squeeze_momentum_7d_gate1_v1.py`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- This was a clean `MYM/1m` 1m-origin public-family trial with real retained IBKR data and adequate trade density, but the edge was too small for friction: every row failed by `2bps/side` and none survived `5bps/side`. Do not downstream or overlay this TTM squeeze branch. Keep it as useful negative diversity evidence and continue either with a larger-excursion fresh family or with direct execution-predicate repair on the existing real `5bps/side` survivors such as M2K `1m` liquidity-sweep reject-short/RVOL-PDA and SI `5m` tight-range fade.

## Gate 1 Negative: IBKR MNQ 1m Initial Balance Acceptance 7D

Append-only terminal row at `2026-05-20 09:37:44 +0800` after observing pre-write hash `33f06d44163256a5fe4d9b26cd2bb5a96aec42ecc66679b45b41b1021d4a18d4` and duplicate-searching the Board/claims for `20260520T093744`, `mnq1m_initial_balance_acceptance`, `InitialBalanceAcceptance -> ibkr_mnq1m_initial`, and `IBKR_MNQ1M_INITIAL_BALANCE_ACCEPTANCE`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093744+0800-codex-ibkr-mnq1m-initial-balance-acceptance-7d-gate1-v1/`.
- Factor id: `ibkr_mnq1m_initial_balance_acceptance_7d_gate1_v1`.
- Branch path: `MarketProfile -> InitialBalanceAcceptance -> ibkr_mnq1m_initial_balance_acceptance_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
- Diversity/source note: this gives market-profile / initial-balance acceptance and failed-break reclaim its own exact-root `MNQ/1m` futures Gate 1 attempt, separate from the earlier MNQ initial-balance breakout/fade packet and from VWAP/RSI/RVOL, Donchian/Turtle, Keltner, Darvas, TTM squeeze, Alligator/Fractal, and trend-cycle families.
- Provider/Gate 1 evidence: fresh real IBKR `MNQ 202606` `1m 7 D`, `9269` normalized rows; `local_cache_replay=false_fresh_ibkr_fetch_from_same_session`. Provider-status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=0`.
- Cost/density evidence: all four exact `1m` variants produced zero trades: `MNQ/ib30_acceptance_dense/1m`, `MNQ/ib60_acceptance_balanced/1m`, `MNQ/ib30_failed_break_reclaim/1m`, and `MNQ/ib60_midpoint_drive/1m` each had `0` trades and `0.00%` raw / `1bps` / `2bps` / `5bps` PnL. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, fresh provider normalized CSV, Auto-Quant material files, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_initial_balance_acceptance_7d_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-ibkr-mnq1m-initial-balance-acceptance-7d-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_provider_status_ibkr.exit=0`, `04_strategy_py_compile.exit=0`, `05_auto_quant_agent_material_batch.exit=0`, `06_auto_quant_agent_material_dispatch.exit=0`, and `07_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- Initial-balance acceptance got a clean public-family exact-root chance on fresh IBKR `MNQ/1m` rows, but it failed before costs because no variant entered a trade. This is useful diversity evidence, not a provider or branch-metadata failure. Do not downstream this root or tune it by lowering the density gate; move practical effort back to larger-excursion fresh families or to direct execution-predicate repair on real `5bps/side` survivors such as M2K `1m` liquidity-sweep reject-short/RVOL-PDA and SI `5m` tight-range fade.

## Exact Downstream Fail-Closed: IBKR SI 5m LinReg Channel Breakout

Append-only terminal row at `2026-05-20 09:52:00 +0800` after observing pre-write hash `03d8e84e68a7b13856cf78c5e663294a3181d7bb9874d4e001b73cf18bbd98f9` and duplicate-searching the Board/claims for `SI 5m LinReg`, `si5m_linreg`, `LinearRegressionChannelBreakout -> ibkr_si5m`, `092555`, `093800`, and `exact_si_5m_linreg_channel_breakout`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T092555+0800-codex-ibkr-si5m-linreg-channel-breakout-1m-gate1-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T092555+0800-codex-ibkr-si5m-linreg-channel-breakout-1m-gate1-v1/downstream-exact-si-5m-linreg-channel-breakout-20260520T093800+0800/`.
- Factor id: `ibkr_si5m_linreg_channel_breakout_1m_gate1_v1`.
- Branch path: `TrendChannelExpansion -> LinearRegressionChannelBreakout -> ibkr_si5m_linreg_channel_breakout_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 5m`.
- Gate 1 evidence: retained real IBKR `SI 202607` `5m 10 D`, `6047` normalized rows from the same-session retained source root `20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=29`.
- Cost/density evidence: `SI/linreg_retest/5m` survived the real-cost screen with `9` trades, `55.5556%` win rate, raw `+1.28%`, `1bps=+1.10%`, `2bps=+0.92%`, and `5bps=+0.38%`. Sibling rows did not qualify: `SI/linreg_quality/5m` had only `2` trades despite positive cost stress, while `linreg_dense` and `linreg_balanced` were negative. `exact_5m_survivors_2bps=['SI/linreg_retest/5m']`; `exact_5m_survivors_5bps=['SI/linreg_retest/5m']`; downstream was earned.
- Downstream evidence: import/prior, workflow, Pre-Bayes readback, target export, CatBoost train/apply, external-score apply, trainer registration, runtime enable, policy readback, and final target export all exited `0`; `pre_bayes_allowed=true`; `bbn_allowed=true`; `catboost_allowed=true`; `execution_tree_allowed=true`. Both `03_analyze_seed` and `12_analyze_after_ranker` timed out with exit `124`, so `all_commands_ok=false`.
- Terminal blocker: exact branch did not survive execution readback (`exact_branch_survived=false`), no actionable execution candidate materialized, `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`. Ranker runtime was enabled/ready with a CatBoost registered artifact and one active match, but validation stayed immature: `mature_rows=0`, `history_mature_rows=0`, `raw_scored_mature=0/30`, `production_validation=0/30`, and `observation_validation=0/30`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/rank_rows.csv`, downstream `checks/terminal_metrics.json`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `path_ranker_scores.csv`, downstream `state/IBKR_SI5M_LINREG_CHANNEL_BREAKOUT_EXACT_DOWNSTREAM_V1/policy_training/structural_path_ranking_target_summary.json`, and downstream command exits under `checks/*.exit`.
- Terminal decision: `exact_si_5m_linreg_channel_breakout_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- Linear-regression channel breakout is a real precious-metals `5m` cost survivor and should stay in the repair set, but it is not trade-usable. The downstream chain preserved the exact rooted path through Pre-Bayes/CatBoost/runtime registration, yet execution admission failed because both analyze legs timed out, the exact execution candidate did not materialize, mature validation is `0/30`, and the hard predicates remain at `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`. Next same-root work should repair analyze/execution materialization and validation density; do not call this promoted or lower the gates.

## Gate 1 Negative: IBKR MYM 1m True Strength Index Reclaim 7D

Append-only terminal row at `2026-05-20 09:55:00 +0800` after observing pre-write hash `9b9585be6ba04c23d917ab27d4c31488941650d353b391bb6d8e239e4737841b` and duplicate-searching the Board/claims for `MYM 1m True Strength`, `mym1m_true_strength`, `TrueStrengthIndexReclaim`, `092757`, and `tsi_quality`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T092757+0800-codex-ibkr-mym1m-true-strength-index-reclaim-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_true_strength_index_reclaim_7d_gate1_v1`.
- Branch path: `MomentumReversal -> TrueStrengthIndexReclaim -> ibkr_mym1m_true_strength_index_reclaim_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Diversity/source note: this gives True Strength Index / TSI momentum-reclaim a fresh `MYM/1m` public-family attempt, separate from MYM TTM squeeze, VWMA, TOD/session momentum, opening-range failure, and liquidity-sweep branches.
- Provider/Gate 1 evidence: retained real IBKR `MYM 202606` `1m 7 D`, `9438` normalized rows from source provider root `20260520T012742+0800-codex-ibkr-mym1m-tod-session-momentum-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=46`.
- Cost/density evidence: TSI generated enough trades to judge, but no exact row survived real costs. `MYM/tsi_quality/1m` had `3` trades, raw `+0.19%`, `1bps=+0.13%`, `2bps=+0.07%`, and `5bps=-0.11%`, but it failed density and `5bps`; `MYM/tsi_dense/1m` had `31` trades, raw `0.00%`, `1bps=-0.62%`, `2bps=-1.24%`, and `5bps=-3.10%`; `MYM/tsi_balanced/1m` had `12` trades, raw `-0.16%`, `2bps=-0.64%`, and `5bps=-1.36%`; `MYM/tsi_capitulation/1m` had `0` trades. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_true_strength_index_reclaim_7d_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T092524+0800-codex-ibkr-mym1m-true-strength-index-reclaim-7d-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- True Strength Index earned a fair public-family `MYM/1m` Auto-Quant slot and produced dense enough total activity to evaluate, but the practical edge was not there: the dense row was flat before costs and negative after `1bps/side`, while the only small positive quality row was sparse and negative by `5bps/side`. Do not downstream or rescue this branch with overlays; keep it as another diversity negative and focus repair effort on actual `5bps/side` survivors such as SI `5m` linear-regression channel breakout, SI `5m` tight-range fade, and M2K `1m` liquidity-sweep reject-short/RVOL-PDA.

## Gate 1 Survivor + Downstream Fail-Closed: IBKR M2K 1m RVOL/PDA Consistency Floor

Append-only terminal row at `2026-05-20 10:02:00 +0800` after observing pre-write hash `49488b2c8910791320c23a985578f56b2b46eaae702580626b5ceaab2bd70d84` and duplicate-searching the Board/claims for `093302`, `093640`, `pda_consistency_floor`, and `Rvol/PDA Consistency`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093302+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-7d-gate1-v1/`.
- Same-root simulated-admission root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093302+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-7d-gate1-v1/simulated-trade-admission-m2k-1m-rvol-pda-consistency-floor-20260520T093640+0800/`.
- Factor id: `ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Gate 1 evidence: retained real IBKR `M2K 202606` `1m 7 D`, `9392` normalized rows from the same-contract retained source root `20260520T005518+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-7d-gate1-v1`; provider-status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=1`; `rank_total_trade_count=17`.
- Cost/density evidence: `M2K/pda_floor/1m_short` had `17` shorts, `64.7059%` win rate, raw `+2.79%`, `1bps=+2.45%`, `2bps=+2.11%`, and `5bps=+1.09%`. `exact_1m_survivors_2bps=['M2K/pda_floor/1m_short']`; `exact_1m_survivors_5bps=['M2K/pda_floor/1m_short']`; downstream was earned.
- Downstream evidence: same-workspace simulated admission ingested `17` simulated trades (`11` wins / `6` losses / `0` breakevens). Commands `01_auto_quant_results_import` through `19_policy_after_ranker` all exited `0`; `exact_branch_survived=true`; `ranker_validation_ready=true`; `mature_rows=2`; `history_mature_rows=18`.
- Terminal blocker: execution stayed fail-closed with `execution_candidate_status=no_trade`, `closed_loop_branch_admission.status=fail_closed`, `execution_readiness=0.3180972047048167`, `transition_hazard=0.9184975817511946`, and `pda_hybrid_alignment=false`. `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, same-root admission `summaries/terminal_decision_summary.md`, `checks/*.exit`, `state/IBKR_M2K1M_RVOL_PDA_CONSISTENCY_FLOOR_SIM_TRADE_ADMISSION_V1/execution_candidate.json`, and `state/IBKR_M2K1M_RVOL_PDA_CONSISTENCY_FLOOR_SIM_TRADE_ADMISSION_V1/execution_tree_trace.json`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; all recorded Gate 1 and same-root admission command exits above were `0`.

Interpretation:

- The PDA consistency-floor overlay improved the M2K survivor's Gate 1 economics and remains one of the strongest nearby exact-root repair leads. It still is not trade-usable: simulated feedback and CatBoost/ranker visibility do not overcome the live execution predicates. The next useful M2K work must directly reduce transition hazard, align PDA/hybrid regime, and materialize an actionable execution candidate, not add another light RVOL/PDA/liquidity overlay or lower gates.

## Gate 1 Survivor + Downstream Fail-Closed: IBKR SI 15m Turtle Soup False-Breakout Cleaned

Append-only terminal row at `2026-05-20 10:02:00 +0800` after observing pre-write hash `49488b2c8910791320c23a985578f56b2b46eaae702580626b5ceaab2bd70d84` and duplicate-searching the Board/claims for `SI 15m Turtle Soup`, `si15m_turtle_soup_false_breakout_cleaned`, `093432`, `094810`, and `exact_si15m_turtle_soup`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093432+0800-codex-ibkr-si15m-turtle-soup-false-breakout-cleaned-gate1-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093432+0800-codex-ibkr-si15m-turtle-soup-false-breakout-cleaned-gate1-v1/downstream-exact-si-15m-turtle-soup-false-breakout-cleaned-20260520T094810+0800/`.
- Factor id: `ibkr_si15m_turtle_soup_false_breakout_cleaned_gate1_v1`.
- Branch path: `FalseBreakoutReversal -> TurtleSoupReversal -> ibkr_si15m_turtle_soup_false_breakout_cleaned_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 15m`.
- Gate 1 evidence: retained cleaned IBKR SI `15m` ladder replay, not fresh-provider parity; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=37`.
- Cost/density evidence: `SI/soup_balanced/15m` had `7` trades, `85.7143%` win rate, raw `+2.39%`, `2bps=+2.11%`, and `5bps=+1.69%`; `SI/soup_dense/15m` had `16` trades, `62.5%` win rate, raw `+1.91%`, `2bps=+1.27%`, and `5bps=+0.31%`; `SI/soup_capitulation/15m` survived `2bps` but failed `5bps`; `SI/soup_quality/15m` was positive but only `3` trades. `exact_15m_survivors_2bps=['SI/soup_balanced/15m','SI/soup_capitulation/15m','SI/soup_dense/15m']`; `exact_15m_survivors_5bps=['SI/soup_balanced/15m','SI/soup_dense/15m']`; downstream was earned.
- Downstream evidence: commands `01_auto_quant_results_import` through `16_export_target_final` all exited `0`; `all_commands_ok=true`; `exact_branch_survived=true`; Pre-Bayes/CatBoost/ranker/runtime mechanics completed on the exact rooted branch.
- Terminal blocker: downstream remained `exact_si15m_turtle_soup_downstream_fail_closed` with `execution_candidate_status=fail_closed`, `execution_readiness=0.4226170943776133`, `transition_hazard=0.9658840155768782`, `pda_hybrid_alignment=false`, `mature_rows=0`, `history_mature_rows=0`, and `raw_scored_mature=0`. `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: Gate 1 `checks/terminal_metrics.json`, Gate 1 `summaries/terminal_decision_summary.md`, downstream `checks/downstream_metrics.json`, downstream `summaries/terminal_decision_summary.md`, downstream `state/IBKR_SI15M_TURTLE_SOUP_FALSE_BREAKOUT_CLEANED_EXACT_DOWNSTREAM_V1/workflow_snapshot.json`, `execution_candidate.json`, `execution_tree_trace.json`, and command exits under `checks/*.exit`.
- Verification evidence: `python3 -m json.tool` passed for Gate 1 and downstream metrics JSON files; all recorded downstream command exits were `0`.

Interpretation:

- SI `15m` Turtle Soup cleaned is a real precious-metals cost survivor and improves factor diversity with a public false-breakout family, but it is not practical yet. The failure is no longer Gate 1 economics; it is execution admission and validation: high transition hazard, PDA/hybrid disagreement, and zero mature rows. Keep it as a repair candidate, not a promoted factor, and do not treat retained cleaned-ladder replay as fresh-provider full parity.

## Gate 1 Negative: IBKR MGC 1m MAMA/FAMA Trend Turn 7D

Append-only terminal row at `2026-05-20 10:08:00 +0800` after duplicate-searching the Board/claims for `MAMA`, `FAMA`, `mama_fama`, and `trend turn`; only the active claim existed, with no prior Board row.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T100427+0800-codex-ibkr-mgc1m-mama-fama-trend-turn-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_mama_fama_trend_turn_7d_gate1_v1`.
- Branch path: `CycleTrendTurn -> MamaFamaTrendTurn -> ibkr_mgc1m_mama_fama_trend_turn_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives Ehlers MESA Adaptive Moving Average / MAMA-FAMA trend-turn its own exact-root micro-gold `1m` futures attempt, distinct from VWAP/RVOL, Donchian/Turtle, Keltner/ATR, SuperTrend, PSAR, Vortex, Aroon/CCI, Williams/MFI, Ulcer/PGO, pivots/CPR/Camarilla, and linear-regression channel controls.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows from source provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=227`.
- Cost/density evidence: MAMA/FAMA produced enough trades to judge but was raw-negative before costs and worse after costs. `MGC/mama_dense/1m` had `73` trades, raw `-0.52%`, `2bps=-3.44%`, and `5bps=-7.82%`; `MGC/mama_quality/1m` had `37` trades, raw `-0.46%`, `2bps=-1.94%`, and `5bps=-4.16%`; `MGC/mama_reclaim/1m` had `63` trades, raw `-0.95%`, `2bps=-3.47%`, and `5bps=-7.25%`; `MGC/mama_balanced/1m` had `54` trades, raw `-0.65%`, `2bps=-2.81%`, and `5bps=-6.05%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T100213+0800-codex-ibkr-mgc1m-mama-fama-trend-turn-7d-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- MAMA/FAMA got a fair public-family exact-root chance and produced high trade density, but the branch direction was wrong before friction and therefore fails even before downstream. Do not promote, downstream, or rescue this exact MGC `1m` trend-turn shape with overlays. Preserve it as useful diversity negative evidence and keep practical repair focus on true `5bps/side` survivors where the remaining blocker is execution admission rather than Gate 1 economics.

## Gate 1 Negative: IBKR MGC 1m HMA Pullback 7D

Append-only terminal row at `2026-05-20 10:28:25 +0800` after observing pre-write hash `18d4a8d3c3d990b46f6abe682051d5f2b0a62b4e1d77fc2e1d5f808e1a854165` and duplicate-searching the Board for `HMA`, `Hull`, `hma_pullback`, `100940`, and `HullMaPullback`; only the external claim existed.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T100940+0800-codex-ibkr-mgc1m-hma-pullback-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_hma_pullback_7d_gate1_v1`.
- Branch path: `TrendPullback -> HullMaPullback -> ibkr_mgc1m_hma_pullback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives Hull Moving Average / HMA pullback continuation its own exact-root micro-gold `1m` futures attempt, separate from MAMA/FAMA, VWAP/RVOL, Donchian/Turtle, Keltner/ATR, SuperTrend, PSAR, Vortex, Aroon/CCI, Williams/MFI, Ulcer/PGO, pivots/CPR/Camarilla, and linear-regression channel controls.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows from source provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=277`.
- Cost/density evidence: HMA produced high density, but no exact row survived real costs. `MGC/hma_balanced/1m` had `72` trades, raw `+0.14%`, `1bps=-1.30%`, `2bps=-2.74%`, and `5bps=-7.06%`; `MGC/hma_dense/1m` had `85` trades, raw `-0.31%`, `2bps=-3.71%`, and `5bps=-8.81%`; `MGC/hma_quality/1m` had `44` trades, raw `-0.64%`, `2bps=-2.40%`, and `5bps=-5.04%`; `MGC/hma_reclaim/1m` had `76` trades, raw `-0.20%`, `2bps=-3.24%`, and `5bps=-7.80%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_hma_pullback_7d_gate1_v1.py`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `python3 -m py_compile` passed for the runner; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- HMA pullback answered another source-backed MGC `1m` public-family slot with real retained IBKR rows and high trade density, but the gross edge is too small or negative before realistic friction. Do not downstream or overlay this branch. Treat it as useful diversity negative evidence and keep practical repair effort on branches that already survived `5bps/side`, especially M2K `1m` liquidity-sweep/RVOL-PDA and SI `5m`/`15m` precious-metals survivors whose blocker is execution admission.

## Direction Correction: Factor Diversity Is Now A Gate

Append-only direction row at `2026-05-20 10:25:17 +0800` after observing pre-write hash `6d301316312fa6878299cc19381ddd48fb1702385593ca1b0c8ab5eb14ac6dec` and duplicate-searching the Board/claims for `diversity is an execution gate`, `公共家族轮换`, `factor diversity correction`, `coverage matrix`, and `多样性.*执行 gate`.

- User correction: the factor search must not keep circling a few familiar indicators or local strategy shapes. There are many time-tested public strategies, scripts, entries, risk controls, and execution logics; they deserve fair Gate 1 chances before the lane falls back into VWAP/RSI/RVOL/opening-drive/microtrend variants.
- Runtime skill update: `/Users/thrill3r/.hermes/skills/software-development/ict-engi-fact-rese-muta/SKILL.md` now treats diversity as an execution gate. Before a fresh profitability Gate 1, build/consult a compact coverage matrix with `family`, `market`, `product`, `symbol`, `timeframe`, `status`, `best_cost_bps`, `run_root`, and `next_action`.
- Required behavior for next fresh Gate 1: choose a public/time-tested family that has not already been terminalized for the same `market/product/symbol/timeframe/root`; if a family has a clean Gate 1 cost failure, preserve it as evidence and cool down that exact cell instead of rescuing it with overlays.
- Familiar-indicator cooldown: VWAP/RSI/RVOL/opening-drive/liquidity micro-variants are allowed only as downstream overlays on an existing exact-root survivor, or when the hypothesis directly repairs `transition_hazard`, `pda_hybrid_alignment`, `execution_readiness`, mature feedback, or execution-candidate materialization.
- Current audit implication: many public families already have fair negative or survivor rows, including ORB, Donchian/Turtle, Keltner, Darvas, NR7/Crabel, SuperTrend, Ichimoku, CMF/OBV, KST/Coppock, Mass Index, Klinger, Choppiness, Alligator/Fractal, pivots/CPR/Camarilla, Vortex, Aroon/CCI, PSAR, TTM squeeze, MAMA/FAMA, TSI, Chaikin A/D, Force Index, VPT, RWI, LinReg channel, Turtle Soup, and others. The next new candidate must therefore either use a still-fresh family/cell or shift market/product/symbol/timeframe materially.

Terminal decision: `factor_diversity_rotation_gate_required`; no promotion claim; no factor marked trade-usable; `update_goal=false`.

## Gate 1 Negative: IBKR MYM 1m DEMA/TEMA Pullback 7D

Append-only terminal row at `2026-05-20 10:43:34 +0800` after observing pre-write hash `06f2c36517a4856092a6c18b294efef19679e3b4e31f2528888a32734718a4d3` and duplicate-searching the Board for `DEMA`, `TEMA`, `dema_tema`, `103446`, and `DemaTemaPullback`; only the external claim existed.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T103446+0800-codex-ibkr-mym1m-dema-tema-pullback-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_dema_tema_pullback_7d_gate1_v1`.
- Branch path: `TrendPullback -> DemaTemaZeroLagPullback -> ibkr_mym1m_dema_tema_pullback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Diversity/source note: this gives DEMA/TEMA zero-lag trend pullback its own exact-root `MYM/1m` futures attempt, separate from MYM TTM squeeze, TSI, VWMA, TOD/session momentum, opening-range failure, liquidity-sweep, and MGC HMA/MAMA-FAMA controls.
- Provider/Gate 1 evidence: retained real IBKR `MYM 202606` `1m 7 D`, `9438` normalized rows from source provider root `20260520T092757+0800-codex-ibkr-mym1m-true-strength-index-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=304`.
- Cost/density evidence: DEMA/TEMA produced high density, but no exact row survived realistic costs. `MYM/zero_lag_dense/1m` had `89` trades, raw `+0.65%`, `1bps=-1.13%`, `2bps=-2.91%`, and `5bps=-8.25%`; `MYM/tema_reclaim/1m` had `85` trades, raw `+0.01%`, `2bps=-3.39%`, and `5bps=-8.49%`; `MYM/zero_lag_balanced/1m` had `68` trades, raw `-0.44%`, `2bps=-3.16%`, and `5bps=-7.24%`; `MYM/zero_lag_quality/1m` had `62` trades, raw `-1.10%`, `2bps=-3.58%`, and `5bps=-7.30%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_dema_tema_pullback_7d_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-ibkr-mym1m-dema-tema-pullback-7d-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `python3 -m py_compile` passed for the runner; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- DEMA/TEMA answered a fresh public-family `MYM/1m` slot with very high trade density, but the gross edge is far inside the friction envelope: even the best raw-positive dense row flips negative by `1bps/side`. Do not downstream or rescue this branch with overlays. Preserve it as diversity negative evidence and continue practical repair on existing true cost survivors whose blocker is execution admission, not Gate 1 economics.

## Gate 1 Negative: IBKR MGC 1m VIDYA/CMO Adaptive Pullback 7D

Append-only terminal row at `2026-05-20 11:03:00 +0800` after duplicate-searching the Board/claims for `VIDYA`, `vidya_cmo`, `MGC 1m VIDYA`, `110005`, and `110027`; only the current external claim existed before this write. Duplicate root `20260520T110027+0800-codex-ibkr-mgc1m-vidya-cmo-pullback-7d-gate1-v1` produced the same terminal verdict/exits and is suppressed; use `110005` as the authoritative row.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T110005+0800-codex-ibkr-mgc1m-vidya-cmo-pullback-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_vidya_cmo_pullback_7d_gate1_v1`.
- Branch path: `TrendEfficiency -> VidyaCmoAdaptivePullback -> ibkr_mgc1m_vidya_cmo_pullback_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives Variable Index Dynamic Average / VIDYA with CMO adaptive pullback its own exact-root micro-gold `1m` futures attempt, distinct from MAMA/FAMA, HMA, DEMA/TEMA, VWAP/RVOL, Donchian/Turtle, Keltner/ATR, SuperTrend, PSAR, Vortex, Aroon/CCI, Williams/MFI, Ulcer/PGO, and pivot controls.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows from source provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=235`.
- Cost/density evidence: VIDYA/CMO produced enough trades to judge, but no exact row survived realistic costs. `MGC/vidya_quality/1m` had `29` trades, raw `+0.31%`, `1bps=-0.27%`, `2bps=-0.85%`, and `5bps=-2.59%`; `MGC/vidya_balanced/1m` had `50` trades, raw `+0.42%`, `1bps=-0.58%`, `2bps=-1.58%`, and `5bps=-4.58%`; `MGC/vidya_reclaim/1m` had `78` trades, raw `-0.10%`, `2bps=-3.22%`, and `5bps=-7.90%`; `MGC/vidya_dense/1m` had `78` trades, raw `-0.21%`, `2bps=-3.33%`, and `5bps=-8.01%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_vidya_cmo_pullback_7d_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-ibkr-mgc1m-vidya-cmo-pullback-7d-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed for both `110005` and duplicate `110027`; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0` on both roots.

Interpretation:

- VIDYA/CMO got a fair public-family exact-root chance and added real diversity beyond the familiar indicator cluster, but the raw edge is too small for micro-gold intraday friction: the two raw-positive rows already fail at `1bps/side`, and nothing survives `2bps/side` or `5bps/side`. Do not downstream, promote, or rescue this exact root with overlays. Preserve it as a clean diversity negative and keep practical work focused on existing true `5bps/side` survivors whose blocker is execution admission, especially M2K `1m` liquidity-sweep/RVOL-PDA and SI `5m`/`15m` precious-metals branches.

## Gate 1 Negative: IBKR MYM 1m CMF/OBV Accumulation Breakout 7D

Append-only terminal row at `2026-05-20 11:15:31 +0800` after observing pre-write hash `f574e8cd2d5a8834812df51d25aba5749faf5285b83b343a9d5977fd4ff48ae5` and duplicate-searching the Board/claims for `MYM CMF/OBV`, `mym1m_cmf_obv`, `CmfObvAccumulationBreakout`, `024117`, and `MYM/obv_quality`; only the older `MNQ/1m` CMF/OBV Board row and the external MYM claim existed.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024117+0800-codex-ibkr-mym1m-cmf-obv-accumulation-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_cmf_obv_accumulation_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> CmfObvAccumulationBreakout -> ibkr_mym1m_cmf_obv_accumulation_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Diversity/source note: this gives Chaikin Money Flow / On-Balance Volume accumulation breakout its own exact-root micro-Dow `1m` futures attempt, distinct from the prior `MNQ/1m` CMF/OBV row and separate from MYM DEMA/TEMA, TSI, VWMA, TTM squeeze, TOD/session momentum, opening-range failure, and liquidity-sweep controls.
- Provider/Gate 1 evidence: fresh real IBKR `MYM 202606` `1m 7 D`, `9511` normalized rows; `local_cache_replay=false`. Provider-status, IBKR fetch, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=123`.
- Cost/density evidence: CMF/OBV produced enough trades to judge, but every exact row was raw-negative and no row survived realistic costs. `MYM/obv_quality_n60/1m` had `19` trades, raw `-0.13%`, `1bps=-0.51%`, `2bps=-0.89%`, and `5bps=-2.03%`; `MYM/accum_dense_n36/1m` had `38` trades, raw `-0.19%`, `2bps=-1.71%`, and `5bps=-3.99%`; `MYM/accum_balanced_n48/1m` had `32` trades, raw `-0.20%`, `2bps=-1.48%`, and `5bps=-3.40%`; `MYM/vwap_accum_n42/1m` had `34` trades, raw `-0.37%`, `2bps=-1.73%`, and `5bps=-3.77%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, fresh provider normalized CSV, Auto-Quant material files, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-ibkr-mym1m-cmf-obv-accumulation-breakout-7d-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_provider_status_ibkr.exit=0`, `01_ibkr_fetch_mym_1m_7d.exit=0`, `04_strategy_py_compile.exit=0`, `05_auto_quant_agent_material_batch.exit=0`, `06_auto_quant_agent_material_dispatch.exit=0`, and `07_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- MYM CMF/OBV answered a fresh equity-index `1m` public accumulation-breakout slot with fresh IBKR rows, exact rooted metadata, and adequate trade count, but the branch direction was negative before cost stress. Do not downstream, promote, or rescue this exact root with overlays. Preserve it as a clean diversity negative and keep practical repair effort on true `5bps/side` survivors whose remaining blocker is execution admission, especially M2K `1m` liquidity-sweep/RVOL-PDA and SI `5m`/`15m` precious-metals branches.

## Gate 1 Negative: IBKR MGC/SI 1m Relative-Value Z-Score 7D

Append-only terminal row at `2026-05-20 11:18:12 +0800` after observing pre-write hash `c473d5ff753596a09bb2d111c761eea37b48476daf603a71dbbc89e70d42c172` and duplicate-searching the Board/claims for `MGC/SI`, `MGC SI`, `relative_value_zscore`, `111253`, `GoldSilverZScoreReversion`, and `MGC_SI/dense/1m_spread`; no exact Board row existed. Duplicate partial roots `20260520T111433+0800-codex-ibkr-mgc-si-relative-value-zscore-1m-gate1-v1` and `20260520T111440+0800-codex-ibkr-mgc-si-relative-value-zscore-1m-gate1-v1` only reached early checks and are suppressed; use `111253` as the authoritative completed packet.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T111253+0800-codex-ibkr-mgc-si-relative-value-zscore-1m-gate1-v1/`.
- Factor id: `ibkr_mgc_si_relative_value_zscore_1m_gate1_v1`.
- Branch path: `RelativeValueMeanReversion -> GoldSilverZScoreReversion -> ibkr_mgc_si_relative_value_zscore_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals_relative_value -> MGC_SI_SPREAD -> 1m`.
- Diversity/source note: this gives the precious-metals pair/relative-value z-score family a `1m` micro-gold/silver spread attempt, distinct from the earlier equity-index relative-value attempts and from single-leg MGC/SI trend, pivot, oscillator, and breakout controls.
- Provider/Gate 1 evidence: retained real IBKR same-session legs were available for both sides: `MGC 202606` `1m 7 D` with `9320` rows from `20260520T100940+0800-codex-ibkr-mgc1m-hma-pullback-7d-gate1-v1`, and `SI 202607` `1m 7 D` with `9320` rows from `20260520T090527+0800-codex-ibkr-si1m-vpt-divergence-reclaim-7d-gate1-v1`; synthetic spread rows `9320`; `local_cache_replay=retained_real_ibkr_same_session`. Provider-status, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`.
- Cost/density evidence: the spread materials produced zero trades, so there is no practical cost-stressed density to hand downstream. `MGC_SI/dense/1m_spread`, `MGC_SI/balanced/1m_spread`, `MGC_SI/quality/1m_spread`, and `MGC_SI/wide/1m_spread` each had `0` trades, raw `0.00%`, `1bps=0.00%`, `2bps=0.00%`, and `5bps=0.00%`. `rank_total_trade_count=0`; `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, spread material artifacts, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc_si_relative_value_zscore_1m_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T111253+0800-codex-ibkr-mgc-si-relative-value-zscore-1m-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_provider_status_ibkr.exit=0`, `01_strategy_py_compile.exit=0`, `02_auto_quant_agent_material_batch.exit=0`, `03_auto_quant_agent_material_dispatch.exit=0`, and `04_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- MGC/SI relative-value answered a fresh precious-metals pair/spread slot with retained real IBKR legs and exact rooted metadata, but the first profit-factor branch generated no trades. This is a density failure rather than a provider or metadata failure. Do not downstream, promote, or tune this exact z-score shape until the entry rule is materially denser; practical repair budget should stay with true `5bps/side` survivors whose blocker is execution admission rather than Gate 1 economics.

## Gate 1 Negative: IBKR MGC 5m Choppiness Breakout 10D

Append-only terminal row at `2026-05-20 11:21:04 +0800` after observing pre-write hash `23c94243baf530a568f653740bb8a19e3e4154658e26206a2d5fa52e9243bc41` and duplicate-searching the Board/claims for `MGC 5m Choppiness`, `mgc5m_choppiness`, `111854`, `ChoppinessBreakout`, and `MGC/chop_dense/5m`; only the current external claim and a different `MNQ/1m` choppiness claim existed.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T111854+0800-codex-ibkr-mgc5m-choppiness-breakout-10d-gate1-v1/`.
- Factor id: `ibkr_mgc5m_choppiness_breakout_10d_gate1_v1`.
- Branch path: `RangeCompression -> ChoppinessBreakout -> ibkr_mgc5m_choppiness_breakout_10d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 5m`.
- Diversity/source note: this gives Choppiness Index compression-breakout a real `MGC/5m` precious-metals Gate 1 attempt, separate from MGC `1m` MAMA/FAMA, HMA, VIDYA/CMO, pivots, Turtle Soup, and the MGC/SI relative-value spread attempt.
- Provider/Gate 1 evidence: retained real same-session IBKR `MGC 202606` `5m 10 D`, `2679` rows from source provider root `20260519T222102+0800-codex-ibkr-futures-micro-trend-pullback-reclaim-1m-mtf-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=10`.
- Cost/density evidence: choppiness breakout was sparse and raw-negative when it traded. `MGC/chop_balanced/5m` and `MGC/chop_quality/5m` each had `0` trades. `MGC/chop_dense/5m` and `MGC/chop_retest/5m` each had `5` trades, `0.0%` win rate, raw `-0.36%`, `1bps=-0.46%`, `2bps=-0.56%`, and `5bps=-0.86%`. `exact_5m_survivors_2bps=[]`; `exact_5m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc5m_choppiness_breakout_10d_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-ibkr-mgc5m-choppiness-breakout-10d-gate1-v1.claim`.

## Gate 1 Negative: IBKR MYM 1m Elder Ray Bull/Bear Power

Append-only terminal row at `2026-05-20 11:24:00 +0800` after observing pre-write hash `3cb7c55d9e76821b86ede8075d6e9dc6b2760a7d0396133229b84ce3fc54895d` and duplicate-searching the Board/claims for `112003`, `elder_ray`, `bull_bear`, `Elder Ray`, and `mym1m`; no exact Board row existed.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T112003+0800-codex-ibkr-mym1m-elder-ray-bull-bear-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_elder_ray_bull_bear_7d_gate1_v1`.
- Branch path: `PowerMomentum -> ElderRayBullBearPower -> ibkr_mym1m_elder_ray_bull_bear_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Evidence scope: retained-real IBKR `MYM 202606` `1m` `7 D` rows (`9438`); strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields preserved.
- Cost/density evidence: `4` exact rows / `242` total trades, but all failed real-cost stress. `MYM/ray_reclaim/1m`: `90` trades, raw `+0.47%`, `1bps=-1.33%`, `2bps=-3.13%`, `5bps=-8.53%`; `MYM/ray_dense/1m`: `111` trades, raw `+0.12%`, `1bps=-2.10%`, `2bps=-4.32%`, `5bps=-10.98%`; `MYM/ray_balanced/1m`: `33` trades, raw `-0.17%`, `2bps=-1.49%`; `MYM/ray_quality/1m`: `8` trades, raw `-0.12%`, `2bps=-0.44%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Gate verdict: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_elder_ray_bull_bear_7d_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T111939+0800-codex-ibkr-mym1m-elder-ray-bull-bear-7d-gate1-v1.claim`.
- Reverse signal: Elder Ray generated plenty of raw activity on `MYM/1m`, but the edge was fully consumed by `1bps/side` friction. Cool this exact cell and rotate family/cell or work on true `5bps/side` survivors instead of downstreaming this packet.

## Supersession: IBKR MGC/SI 1m Relative-Value Z-Score Later Readback

Append-only supersession note at `2026-05-20 11:24:00 +0800` after later duplicate `112009` completed with fuller economic evidence than the earlier `111253` zero-trade packet. The practical verdict did not improve.

- Superseding run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T112009+0800-codex-ibkr-mgc-si-relative-value-zscore-1m-gate1-v1/`.
- Same branch path: `FUTURES -> precious_metals_relative_value -> MGC_SI_SPREAD -> 1m -> RelativeValueMeanReversion -> GoldSilverZScoreReversion -> ibkr_mgc_si_relative_value_zscore_1m_gate1_v1`, with AQ probe suffix `aq_spot_compatible_long_spread_only_probe_v1` in row-level branch fields.
- Later cost/density evidence: `MGC_SI/dense/1m_spread`: `135` trades, raw `+0.99%`, `1bps=-1.71%`, `2bps=-4.41%`, `5bps=-12.51%`; `MGC_SI/quality/1m_spread`: `36` trades, raw `+0.72%`, `1bps=0.00%`, `2bps=-0.72%`, `5bps=-2.88%`; `MGC_SI/wide/1m_spread`: `23` trades, raw `+0.94%`, `1bps=+0.48%`, `2bps=+0.02%`, `5bps=-1.36%`; `MGC_SI/balanced/1m_spread`: `76` trades, raw `+0.34%`, `1bps=-1.18%`, `2bps=-2.70%`, `5bps=-7.26%`.
- Gate verdict remains: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[MGC_SI/wide/1m_spread]`; `exact_1m_survivors_5bps=[]`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc_si_relative_value_zscore_1m_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T112009+0800-codex-ibkr-mgc-si-relative-value-zscore-1m-gate1-v1.claim`.
- Reverse signal: this is not a density-zero family anymore, but the only `2bps` survivor is near flat and all variants fail `5bps`; do not downstream or promote. A future revisit needs materially better spread-entry geometry or execution-leg modeling.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- MGC `5m` Choppiness Breakout added a fair public range-compression family sample, but it failed Gate 1 on both density and direction: only two rows traded, both were raw-negative before costs, and no exact row survived `2bps/side` or `5bps/side`. Do not downstream, promote, or rescue this exact root with overlays. Preserve it as diversity negative evidence and keep practical work on true cost survivors whose remaining blocker is downstream execution admission.

## Auto-Quant Same-Root Repair Attempt: IBKR M2K 1m RVOL/PDA Consistency Floor

Append-only terminal row at `2026-05-20 11:26:00 +0800` after observing pre-write hashes `2b0a9f1aa84d97895d599971589b1be32811c9ac0dbd7367cc6ae9592f5bac06` for this Board and `bbc222afd691447f2c254631c40e68570625cf732df4444cfb0d25cb4205fb21` for the diversity matrix, and duplicate-searching for `M2K autoresearch`, `rvol_pda_autoresearch`, `TomacNQ_KillzoneBreakout`, and `111809`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T111809+0800-codex-m2k1m-rvol-pda-autoresearch-repair-v1/`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T111809+0800-codex-m2k1m-rvol-pda-autoresearch-repair-v1.claim`.
- Purpose: bounded Auto-Quant repair attempt for the existing exact-root `M2K/1m RVOL/PDA consistency floor` survivor, targeting the prior blockers `multi_timeframe_direction_conflict`, weak PDA sequence consistency, high transition hazard, `pda_hybrid_alignment=false`, and low execution readiness.
- Input ladder: retained real IBKR `M2K 202606` `1m 7 D` rows from `20260520T093302+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-7d-gate1-v1` were materialized as candle JSON. `1m` used `9392` retained rows directly; `5m/15m/30m/1h/4h/1d` were mechanically derived from that same retained `1m` source and explicitly marked as derived, not separate provider fetches.
- Auto-Quant control-plane evidence: initial `factor-autoresearch` exited `0` but reported `data_ready=false` / `auto_quant_prepare_required_before_run`; `auto-quant-prepare` exited `0` and changed readiness to `data_ready=true`; the prepared `factor-autoresearch` rerun exited `0`, produced a healthy handoff, and reported `auto_quant_active_strategy_count=1`.
- Measured AQ output: direct `run_tomac.py` produced a successful AQ backtest run (`Done: 1 succeeded, 0 failed`) for generated strategy `TomacNQ_KillzoneBreakout` on `M2K/USD`, but it made `0` trades, `0.00%` total profit, win rate `0.0%`, and profit factor `0.0` over the loaded window. A shell telemetry write used a relative path after `cd` into the AQ repo, so `04_run_tomac.wrapper_exit=1`; this is wrapper hygiene only. The measured AQ success marker is `04_run_tomac.aq_measured_success=0`.
- Branch identity verdict: this attempt did not repair the exact M2K short survivor. AQ generated a generic NQ-style long killzone breakout seed, not an exact `M2K/1m RVOL/PDA consistency floor` execution candidate or PDA/transition repair. Because it produced zero trades, there is no cost-stressed density, no same-root downstream admission, and no updated execution-tree evidence.
- Terminal decision: `autoresearch_repair_no_candidate_zero_trades`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `data/input_manifest.json`, `data/m2k_202606_*.candles.json`, `checks/00_materialize_candles.exit`, `checks/01_factor_autoresearch.exit`, `checks/02_auto_quant_prepare.exit`, `checks/03_factor_autoresearch_after_prepare.exit`, `checks/04_run_tomac.wrapper_exit`, `checks/04_run_tomac.aq_measured_success`, `command-output/01_factor_autoresearch.*`, `command-output/02_auto_quant_prepare.*`, `command-output/03_factor_autoresearch_after_prepare.*`, `command-output/04_run_tomac.*`, and AQ handoff artifacts under `/tmp/ict-engine-m2k-rvol-pda-autoresearch-repair-v1/state/IBKR_M2K1M_RVOL_PDA_CONSISTENCY_FLOOR_AUTORESEARCH_REPAIR_V1/`.
- Verification evidence: `python3 -m json.tool` passed for all generated candle JSON files and the input manifest; `auto-quant-prepare` reported `status=prepared`; prepared `factor-autoresearch` reported `data_ready=true`; measured AQ output reported `trade_count=0` and `Done: 1 succeeded, 0 failed`.

Interpretation:

- The diversity correction was honored by not launching another familiar indicator clone, and the M2K repair path was tested through Auto-Quant. The result is a clean non-promotion: AQ currently hands this root to a generic killzone seed that has no trades and does not address the PDA/transition execution blockers. Do not promote or rerun this same handoff unchanged. The next useful work is either fixing AQ seed generation so it preserves the exact M2K short/PDA branch, or pivoting practical repair budget to the SI `5m` tight-range fade survivor where the remaining blocker is execution materialization rather than zero-trade autoresearch output.

## Gate 1 Negative: IBKR MYM 1m Chandelier Exit Breakout 7D

Append-only terminal row at `2026-05-20 11:39:00 +0800` after duplicate-searching the Board/claims/scripts/runs for `Chandelier`, `volatility-stop`, `chandelier_exit`, and `20260520T113310`; no exact terminal row existed.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T113310+0800-codex-ibkr-mym1m-chandelier-exit-breakout-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_chandelier_exit_breakout_7d_gate1_v1`.
- Branch path: `TrendExpansion -> ChandelierExitBreakout -> ibkr_mym1m_chandelier_exit_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Diversity/source note: this gives Chandelier Exit / ATR trailing-stop breakout a real `MYM/1m` futures Gate 1 attempt, outside the crowded VWAP/RSI/RVOL/opening-drive/liquidity cluster and distinct from Elder Ray, TSI, AO/AC, DEMA/TEMA, TTM, CMF/OBV, and moving-average pullback rows.
- Provider/Gate 1 evidence: retained real IBKR `MYM 202606` `1m 7 D`, `9438` rows from source provider root `20260520T092757+0800-codex-ibkr-mym1m-true-strength-index-reclaim-7d-gate1-v1`; `local_cache_replay=false_retained_real_ibkr_same_session`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=267`.
- Cost/density evidence: trade density was high, but direction was negative before costs. `MYM/chandelier_dense/1m`: `86` trades, raw `-0.54%`, `1bps=-2.26%`, `2bps=-3.98%`, `5bps=-9.14%`; `MYM/chandelier_balanced/1m`: `60` trades, raw `-0.99%`, `2bps=-3.39%`; `MYM/chandelier_reclaim/1m`: `76` trades, raw `-0.88%`, `2bps=-3.92%`; `MYM/chandelier_quality/1m`: `45` trades, raw `-0.56%`, `2bps=-2.36%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `checks/*.exit`, retained provider normalized CSV, Auto-Quant material files, runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mym1m_chandelier_exit_breakout_7d_gate1_v1.py`, and claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-ibkr-mym1m-chandelier-exit-breakout-7d-gate1-v1.claim`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- Chandelier Exit was given a fair, high-density public-family attempt on real retained IBKR `MYM/1m` data. The exact rooted metadata and AQ mechanics worked, but all variants were raw-negative before realistic costs. Do not downstream, promote, or rescue this exact root with light filters. Preserve it as diversity negative evidence and continue toward true `5bps/side` survivors, especially same-root execution repair for M2K `1m` RVOL/PDA or SI precious-metals survivor branches.

## Gate 1 Negative: IBKR MGC 1m Stochastic Momentum Index 7D

Append-only terminal row at `2026-05-20 11:44:07 +0800` after observing pre-write hash `0b8d3e3c1429153bca35450102a4814890b72d3aff845faaf67e00a782aba3a8` and duplicate-searching the Board/matrix for `Stochastic Momentum`, `stochastic_momentum`, `113744`, and `ibkr_mgc1m_stochastic_momentum_index_7d_gate1_v1`; only the matrix row existed, no Board terminal row.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T113744+0800-codex-ibkr-mgc1m-stochastic-momentum-index-7d-gate1-v1/`.
- Factor id: `ibkr_mgc1m_stochastic_momentum_index_7d_gate1_v1`.
- Branch path: `MomentumReclaim -> StochasticMomentumIndex -> ibkr_mgc1m_stochastic_momentum_index_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives Stochastic Momentum Index oscillator reclaim a fair public-family `MGC/1m` precious-metals attempt, distinct from MGC VWAP/RVOL, HMA, VIDYA/CMO, pivots, Choppiness, Turtle Soup, and MGC/SI relative-value cells.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows from source provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=40`.
- Cost/density evidence: SMI failed before realistic costs. `MGC/smi_quality/1m` had `3` trades, raw `-0.03%`, `1bps=-0.09%`, `2bps=-0.15%`, `5bps=-0.33%`; `MGC/smi_zero_reclaim/1m` had `14` trades, raw `-0.01%`, `2bps=-0.57%`, `5bps=-1.41%`; `MGC/smi_dense/1m` had `14` trades, raw `-0.05%`, `2bps=-0.61%`, `5bps=-1.45%`; `MGC/smi_balanced/1m` had `9` trades, raw `-0.08%`, `2bps=-0.44%`, `5bps=-0.98%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_stochastic_momentum_index_7d_gate1_v1.py`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- SMI is a clean Gate 1 negative, not a provider or metadata problem. It produced enough rows to judge but all variants were raw-flat/negative and none survived `2bps/side` or `5bps/side`. Cool down the exact `MGC/1m/StochasticMomentumIndex` cell and do not downstream or rescue with light filters.

## Gate 1 Negative: IBKR MGC 1m FRAMA Adaptive Breakout 7D

Append-only terminal row at `2026-05-20 11:44:00 +0800` after observing pre-write hashes `0b8d3e3c1429153bca35450102a4814890b72d3aff845faaf67e00a782aba3a8` for this Board and `0ebebf01ee2529971f7f7204eda699caaddfe91462e2a8cb3ebb11c1c5b20188` for the diversity matrix, and duplicate-searching the Board/matrix/claims for `FRAMA`, `FramaAdaptiveBreakout`, `frama_adaptive`, `113952`, and `ibkr_mgc1m_frama`. Only the current self-owned `/tmp` claim and an older VIDYA note that FRAMA was absent were found.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T113952+0800-codex-ibkr-mgc1m-frama-adaptive-breakout-7d-gate1-v1/`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T113622+0800-codex-ibkr-mgc1m-frama-adaptive-breakout-7d-gate1-v1.claim`.
- Factor id: `ibkr_mgc1m_frama_adaptive_breakout_7d_gate1_v1`.
- Branch path: `AdaptiveTrendExpansion -> FramaAdaptiveBreakout -> ibkr_mgc1m_frama_adaptive_breakout_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> MGC -> 1m`.
- Diversity/source note: this gives Ehlers Fractal Adaptive Moving Average / FRAMA adaptive breakout a fair public-family micro-gold `1m` futures attempt, distinct from VWAP/RSI/RVOL, MAMA/FAMA, HMA, VIDYA/CMO, Vortex, pivots, PSAR, Donchian, Keltner, ADX, and SuperTrend roots.
- Provider/Gate 1 evidence: retained real IBKR `MGC 202606` `1m 7 D`, `9320` normalized rows from source provider root `20260519T232924+0800-codex-ibkr-mgc1m-opening-vwap-rvol-reclaim-7d-gate1-v1`; `local_cache_replay=false_source_provider_root_reused`. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=125`.
- Cost/density evidence: FRAMA generated enough trades to judge, but every exact row failed real-cost stress. `MGC/frama_balanced/1m` had `23` trades, raw `0.00%`, `1bps=-0.46%`, `2bps=-0.92%`, and `5bps=-2.30%`; `MGC/frama_dense/1m` had `48` trades, raw `-0.19%`, `2bps=-2.11%`, and `5bps=-4.99%`; `MGC/frama_retest/1m` had `41` trades, raw `-0.41%`, `2bps=-2.05%`, and `5bps=-4.51%`; `MGC/frama_quality/1m` had `13` trades, raw `-0.10%`, `2bps=-0.62%`, and `5bps=-1.40%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `summaries/rank_rows.csv`, `summaries/provider_provenance_matrix.csv`, `checks/*.exit`, and runner `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mgc1m_frama_adaptive_breakout_7d_gate1_v1.py`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed; `00_strategy_py_compile.exit=0`, `01_auto_quant_agent_material_batch.exit=0`, `02_auto_quant_agent_material_dispatch.exit=0`, and `03_auto_quant_agent_material_rank.exit=0`.

Interpretation:

- FRAMA got a real diversity slot and did not pass the economic gate. This is a clean public-family negative, not a provider or branch-metadata failure. Cool exact `MGC/1m/FramaAdaptiveBreakout`; do not downstream or add light overlays. Keep fresh Gate 1 rotation broad, and keep practical repair budget focused on true `5bps/side` survivors such as M2K RVOL/PDA and SI tight-range/Turtle/LinReg branches.

## Simulated-Admission Fail-Closed: IBKR SI 15m Turtle Soup False-Breakout Cleaned

Append-only terminal row at `2026-05-20 11:47:50 +0800` after observing pre-write hashes `9b9bb636f71ea1dff8289624036b295dd843b7c2ea557799ce8c4c55bb11b12e` for this Board and `ebf37d52524325ee59f78ac4c20e5bb8786b1a79e41909bdcda63293af591324` for the small-cycle board, and duplicate-searching both boards for `114420`, `simulated-trade-admission-si-15m-turtle-soup-false-breakout-cleaned-20260520T114420`, `SI15m.*Turtle Soup.*114420`, and `turtle_soup_false_breakout_cleaned.*simulated`; no exact terminal row existed.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093432+0800-codex-ibkr-si15m-turtle-soup-false-breakout-cleaned-gate1-v1/simulated-trade-admission-si-15m-turtle-soup-false-breakout-cleaned-20260520T114420+0800/`.
- Source Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093432+0800-codex-ibkr-si15m-turtle-soup-false-breakout-cleaned-gate1-v1/`.
- Branch path: `FalseBreakoutReversal -> TurtleSoupReversal -> ibkr_si15m_turtle_soup_false_breakout_cleaned_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 15m`.
- Gate 1 survivors used: `SI/soup_balanced/15m` and `SI/soup_dense/15m` as the primary `5bps/side` survivors; `SI/soup_capitulation/15m` stayed 2bps-only and was not used for primary admission.
- Feedback evidence: same-Auto-Quant-workspace simulated backtest feedback exported `23` rows (`16` wins, `7` losses, `0` breakevens): `soup_balanced` contributed `7` rows (`6` wins, `1` loss), and `soup_dense` contributed `16` rows (`10` wins, `6` losses). This run explicitly remains `local_cache_replay=true` and `fresh_provider_parity=false` on retained cleaned IBKR SI continuous data.
- Command evidence: `03_analyze_seed`, `04_workflow_seed`, `05_pre_bayes_seed`, `06_export_target_seed`, `07_policy_seed`, `08_ingest_simulated_trades`, `09_export_target_after_simulated_trades`, `10_policy_after_simulated_trades`, `11_train_ranker`, `12_apply_ranker`, `13_apply_scores_to_ict`, `14_register_trainer`, `15_enable_runtime`, `16_analyze_after_ranker`, `17_workflow_after_ranker`, `18_pre_bayes_after_ranker`, and `19_policy_after_ranker` exited `0`; `01_auto_quant_results_import=1` and `02_auto_quant_prior_init=1`, so `all_command_exits_zero=false` even though later readbacks completed.
- Wrapper/schema readback: the two failed import/prior commands failed before admission because `materials/ibkr_si15m_turtle_soup_false_breakout_cleaned_strategy_library.json` emitted `profit_factor` as the rooted factor id string at line `70`, while the loader expected numeric `f64` profit factor. Treat this as a wrapper/library-generation defect to fix before rerun, not as evidence that the factor passed. Later stages still completed from existing state/readback material and remained fail-closed.
- Admission evidence: `exact_branch_survived=true`; `ranker_validation_ready=true`; path-ranker score was visible to and used by the execution tree; `mature_rows=1`, `history_mature_rows=23`, `raw_scored_mature=1`, `production_validation=0`, `observation_validation=0`.
- Execution blocker: closed-loop admission stayed fail-closed with `execution_candidate_status=no_trade`, `execution_candidate_actionable=false`, `execution_gate_status=observe`, `execution_readiness=0.4226170943776133`, `transition_hazard=0.9658840155768782`, and `pda_hybrid_alignment=false`. The execution tree reported guarded/transition-guardrail behavior, not an actionable trade.
- Terminal decision: `si15m_turtle_soup_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Evidence files: `checks/simulated_trade_admission_metrics.json`, `checks/terminal_metrics.json`, `summaries/terminal_decision_summary.md`, `checks/*.exit`, `simulated-trades/all_simulated_trades.jsonl`, `state/IBKR_SI15M_TURTLE_SOUP_FALSE_BREAKOUT_CLEANED_SIM_TRADE_ADMISSION_V1/execution_candidate.json`, and `state/IBKR_SI15M_TURTLE_SOUP_FALSE_BREAKOUT_CLEANED_SIM_TRADE_ADMISSION_V1/execution_tree_trace.json`.

Interpretation:

- This SI Turtle Soup branch remains a useful precious-metals repair lead, not a live-practical factor. Simulated feedback repaired branch/ranker visibility and preserved the exact rooted path, but it did not produce an actionable execution candidate and it still fails the hard predicate trio by a wide margin. The next same-root work should target transition/PDA alignment and current execution-candidate materialization with fresh-provider parity, not more generic simulated-feedback replay or lowered gates.

## Supersession: IBKR SI 15m Turtle Soup Simulated-Admission Clean Rerun

Append-only supersession note at `2026-05-20 12:10:26 +0800` after observing pre-write hashes `4a2a3b2681bbcd3d4ce1398a2afd83500f57d4133258ae9c0ae41424356ff406` for this Board and `f66bb5664d36b374051eee370edf0ef0dfde9b007c6c58e1833e3bf28404eb8f` for the diversity matrix, and duplicate-searching for `115328`, `si15m_turtle_soup_simulated_trade_admission`, and `regime_root_survivor_blocker_report`.

- Superseding run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093432+0800-codex-ibkr-si15m-turtle-soup-false-breakout-cleaned-gate1-v1/simulated-trade-admission-si-15m-turtle-soup-false-breakout-cleaned-20260520T115328+0800/`.
- Superseded repair row: `simulated-trade-admission-si-15m-turtle-soup-false-breakout-cleaned-20260520T114420+0800/`; keep it as wrapper-defect evidence, but use `115328` for current SI15 simulated-admission classification.
- Branch path: `FalseBreakoutReversal -> TurtleSoupReversal -> ibkr_si15m_turtle_soup_false_breakout_cleaned_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 15m`.
- Command evidence: all `01-19` simulated-admission commands exited `0`; the earlier import/prior string-vs-f64 wrapper defect is not present in the clean rerun.
- Feedback evidence: same-Auto-Quant-workspace simulated feedback again used `23` trades (`16` wins / `7` losses / `0` breakevens) from primary `5bps/side` survivors `SI/soup_balanced/15m` and `SI/soup_dense/15m`; `local_cache_replay=true`; `fresh_provider_parity=false`.
- Admission evidence: `exact_branch_survived=true`; `ranker_validation_ready=true`; `path_ranker_score_visible_to_execution_tree=true`; `path_ranker_score_used_by_execution_tree=false`; `mature_rows=2`; `history_mature_rows=24`; `raw_scored_mature=2`; `production_validation=0`; `observation_validation=0`.
- Execution blocker: closed-loop admission stayed fail-closed with `execution_candidate_status=no_trade`, `execution_candidate_actionable=false`, `execution_readiness=0.4226170943776133`, `transition_hazard=0.9658840155768782`, and `pda_hybrid_alignment=false`.
- Blocker report: `checks/regime_root_survivor_blocker_report.json` and `summaries/regime_root_survivor_blocker_report.md` classify the branch as `repair_same_root_pda_sequence_alignment`; blockers include `pre_bayes_conflict:pda_regime_family_disagreement`, `pre_bayes_conflict:pda_sequence_cluster_weak`, `pre_bayes_conflict:pda_sequence_low_consistency`, `execution_candidate_no_trade`, `execution_readiness_below_0_65`, `transition_hazard_ge_0_60`, `pda_hybrid_alignment_not_true`, and `path_ranker_visible_but_not_used`.
- Terminal decision remains: `si15m_turtle_soup_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Verification evidence: `python3 -m json.tool checks/simulated_trade_admission_metrics.json` passed; `python3 -m json.tool checks/regime_root_survivor_blocker_report.json` passed; `checks/*.exit` were all `0`.

Interpretation:

- The clean rerun proves the prior wrapper defect was not the real blocker. Same-root simulated feedback and CatBoost visibility are now mechanically clean, but the branch still fails because PDA regime-family/sequence evidence disagrees with the rooted Turtle Soup reversal context. Do not run another generic simulated-feedback replay or add a light indicator overlay. The next useful SI15 action is to supply stronger same-root PDA sequence evidence, choose a regime root whose PDA family agrees with the survivor, or move to another 5bps survivor whose PDA/transition blockers are more directly repairable.

## Read-Only Blocker Map: Regime-Root Survivor Repair Queue

Append-only handoff row at `2026-05-20 12:22:15 +0800` after duplicate-searching for `survivor blocker map`, `regime-root survivor blocker`, `20260520T122215`, and `codex-regime-root-survivor-blocker-map`; no prior row existed. This was read-only: no AQ dispatch, no `analyze` rerun, and no state mutation.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T122215+0800-codex-regime-root-survivor-blocker-map-v1/`.
- Evidence files: `checks/survivor_blocker_map.json` and `summaries/terminal_decision_summary.md`.
- Scope: compact current blocker map for true or near `5bps/side` survivors under the user's hard gates: real-cost density, same-root direction consistency, `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and stable `execution_readiness >= 0.65`.
- Result: no inspected survivor currently satisfies all hard gates. All entries remain `observe_or_repair_only`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Current repair queue: M2K `1m` RVOL/PDA consistency floor (`readiness=0.3181`, `hazard=0.9185`, `pda=false`), M2K `1m` liquidity-sweep reject-short quality (`readiness=0.3181`, `hazard=0.9185`, `pda=false`), SI `5m` tight-range band expansion fade (`readiness=0.2344`, `hazard=0.9680`, `pda=false`), SI `5m` LinReg (`readiness=0.0`, `hazard=1.0`, `pda=false`), SI `15m` Turtle Soup (`readiness=0.4226`, `hazard=0.9659`, `pda=false`), SI `5m` ATR exhaustion (`readiness=0.0`, `hazard=1.0`, `pda=false`), and NET `5m` PDA-sequence near-pass (`readiness=0.67`, `hazard=0.63049`, `pda=true`).
- Collision note: active owners were visible on M2K post-runtime analyze, SI15 Turtle simulated admission/ranker, and MYM ROC Gate 1 AQ dispatch during this readback. Do not take over their artifacts or append their results as this lane's output.
- Next executable work when capacity clears: same-root execution materialization/PDA-transition repair on SI `5m` tight-range fade or M2K `1m` RVOL/PDA; otherwise fix a code-level materialization/schema blocker before rerun. Fresh Gate 1 should only proceed on an unclaimed public-family cell.

Interpretation:

- The blocker map turns the current objective into concrete missing predicates instead of another indicator sweep. It confirms the goal is not complete and makes the next repair target explicit: execution candidate materialization, PDA/transition alignment, and maturity/readiness, not lowered gates or cosmetic overlays.

## Supersession: IBKR SI 15m Turtle Soup Simulated-Admission Latest Clean Rerun

Append-only supersession note at `2026-05-20 12:12:00 +0800` after duplicate-searching for `115739` and validating `checks/simulated_trade_admission_metrics.json`.

- Latest run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093432+0800-codex-ibkr-si15m-turtle-soup-false-breakout-cleaned-gate1-v1/simulated-trade-admission-si-15m-turtle-soup-false-breakout-cleaned-20260520T115739+0800/`.
- Same rooted branch: `FUTURES -> precious_metals -> SI -> 15m -> FalseBreakoutReversal -> TurtleSoupReversal -> ibkr_si15m_turtle_soup_false_breakout_cleaned_gate1_v1`.
- Command evidence: all `01-19` commands exited `0`; the earlier import/prior schema defect remains fixed.
- Admission evidence: `exact_branch_survived=true`; `ranker_validation_ready=true`; `path_ranker_score_visible_to_execution_tree=true`; `path_ranker_score_used_by_execution_tree=false`; `mature_rows=2`; `history_mature_rows=24`; `raw_scored_mature=2`; `production_validation=0`; `observation_validation=0`.
- Execution blocker remains unchanged: `execution_candidate_status=no_trade`, `execution_candidate_actionable=false`, `execution_readiness=0.4226170943776133`, `transition_hazard=0.9658840155768782`, `pda_hybrid_alignment=false`.
- Terminal decision remains: `si15m_turtle_soup_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- `115739` confirms the clean `115328` result rather than improving it. SI15 Turtle Soup is still a useful `5bps/side` survivor repair lead, but the blocker is substantive PDA/transition/live admission quality, not wrapper mechanics or lack of another simulated feedback replay.

## Gate 1 Negative: IBKR MYM 1m ROC Momentum Continuation 7D

Append-only terminal row at `2026-05-20 12:12:00 +0800` after duplicate-searching for `115738`, `mym1m_roc_momentum_continuation`, and `RateOfChangeMomentum`; the small-cycle Board already had terminal evidence, but this compact current Board did not.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T115738+0800-codex-ibkr-mym1m-roc-momentum-continuation-7d-gate1-v1/`.
- Factor id: `ibkr_mym1m_roc_momentum_continuation_7d_gate1_v1`.
- Branch path: `MomentumContinuation -> RateOfChangeMomentum -> ibkr_mym1m_roc_momentum_continuation_7d_gate1_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> MYM -> 1m`.
- Evidence: retained-real IBKR `MYM 202606` `1m 7 D`; strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; `branch_fields_preserved=true`; `rank_rows=4`; `rank_total_trade_count=137`.
- Cost/density evidence: `MYM/roc_dense/1m` had `50` trades, raw `+0.73%`, but `1bps=-0.27%`, `2bps=-1.27%`, `5bps=-4.27%`; `MYM/roc_reclaim/1m` had `49` trades, raw `+0.21%`, `2bps=-1.75%`; `MYM/roc_quality/1m` had `10` trades, raw `+0.03%`, `5bps=-0.97%`; `MYM/roc_balanced/1m` had `28` trades, raw `-0.18%`. `exact_1m_survivors_2bps=[]`; `exact_1m_survivors_5bps=[]`.
- Terminal decision: `drop_or_block_gate1_practical`; `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- MYM ROC gave another public momentum family a real `1m` Gate 1 chance, but the edge was too thin for even `1bps/side`. Preserve as diversity negative evidence; no downstream and no light retune.

## Readback Fail-Closed: M2K 1m RVOL/PDA Guard Ranker Visibility Rerun

Append-only terminal row at `2026-05-20 12:12:00 +0800` after duplicate-searching for `115632` and validating `checks/ranker_visibility_rerun_metrics.json`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T024014+0800-codex-ibkr-m2k1m-liquidity-sweep-reject-short-rvol-pda-guard-7d-gate1-v1/ranker-visibility-rerun-m2k-rvol-pda-20260520T115632+0800/`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Command evidence: `01_analyze_after_runtime=124`; `02_policy_after=0`; `03_workflow_refresh=0`; `04_pre_bayes_refresh=0`; `05_export_target=0`.
- Runtime evidence: policy readback shows candidate-set CatBoost material exists with `runtime_matches=3`, `raw_scored_mature=20/30`, `production_validation=19/30`, `observation_validation=19/30`, but `ready=false`.
- Execution blocker: `path_ranker_score_visible_to_execution_tree=false`; `path_ranker_score_used_by_execution_tree=false`; `ranker_validation_ready=false`; `execution_readiness=0.3180972047048167`; `transition_hazard=0.9184975817511946`; `pda_hybrid_alignment=false`.
- Terminal decision: `ranker_visibility_still_missing_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- M2K remains a real `5bps/side` survivor, but the current repair still fails on exact execution materialization, validation readiness, transition hazard, and PDA alignment. Do not stack another light liquidity/RVOL/VWAP overlay; next work must make ranker scores actually reach the execution tree and mature validation rows before promotion can be considered.

## Code Repair: Exact Synthetic AQ Handoff No Longer Seeds Generic NQ

Append-only implementation evidence at `2026-05-20 13:09:32 +0800` after observing pre-write hashes `c0fa297a7eb56b4016476a6b048cc229338c54780244236220c9604b1d2e22d9` for this Board and `af18b0b34e45b3987ed62c59efb61222eca510b47c9b1dbcab695955eaacdf6b` for the diversity matrix, and duplicate-searching for `generic NQ`, `TomacNQ`, `autoresearch repair`, and `seed-required`.

- Purpose: repair the blocker identified in the `M2K/1m RVOL/PDA consistency floor` Auto-Quant autoresearch repair attempt, where the exact rooted branch was handed to a generic `TomacNQ_KillzoneBreakout` long seed and produced `0` trades instead of an exact M2K short/PDA repair candidate.
- Code touched: `src/application/auto_quant/workspace_profile.rs` and `src/application/auto_quant/mod.rs`.
- Behavior change: decorated exact runtime symbols such as `IBKR_M2K1M_RVOL_PDA_CONSISTENCY_FLOOR_AUTORESEARCH_REPAIR_V1` now require exact-compatible seed strategies. The synthetic profile no longer counts/copies generic upstream seed strategies for those exact roots; future handoffs should surface `auto_quant_seed_strategies_required` and `auto_quant_active_strategy_count=0` instead of silently running a non-exact generic NQ strategy.
- Verification: `CARGO_TARGET_DIR=.local-artifacts/cargo-target cargo test --lib synthetic_profile_does_not_seed_nq_fallback_for_other_symbols --quiet` passed; `CARGO_TARGET_DIR=.local-artifacts/cargo-target cargo test --lib exact_runtime_synthetic_profile_ignores_generic_upstream_seed_strategy --quiet` passed; `CARGO_TARGET_DIR=.local-artifacts/cargo-target cargo test --lib auto_quant:: --quiet` passed `167` tests.
- Promotion verdict: this is a control-plane repair only, not a factor promotion. M2K remains `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false` until a new exact seed is authored/run and downstream hard gates pass: real-cost density, same-root direction, `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and stable `execution_readiness >= 0.65`.

Interpretation:

- The previous zero-trade generic AQ autoresearch result is now pinned as a tooling defect instead of factor evidence. The next aligned M2K action is to author or generate an exact M2K short/PDA seed under the rooted branch, then rerun AQ and downstream gates without lowering thresholds.

## Code Repair: Exact Freqtrade Source Material Preserved as Seed Copy

Append-only implementation evidence at `2026-05-20 13:22:24 +0800` after observing pre-write hashes `9e54b62a7c42905803cbf7c1752dba5b78b164933b55c458a940b6476c6f05da` for this Board and `5f75244020f5afbc418af315692eeba245a5900e658bbd42d6d72619b71fba68` for the diversity matrix.

- Purpose: repair the next blocker from the `M2K/1m RVOL/PDA consistency floor` Auto-Quant exact-seed retry, where `strategy-material-root` exposed the exact short `1m` RVOL/PDA Freqtrade strategy but seed materialization converted it into the generic long-only EMA/RSI scaffold.
- Code touched: `src/application/auto_quant/seed_evidence.rs`.
- Behavior change: when source material is a standalone safe Freqtrade `IStrategy` with `populate_indicators`, `populate_entry_trend`, and `populate_exit_trend`, seed materialization now writes an exact copy and adapts only the class name to the generated seed strategy name. This preserves source `timeframe`, `can_short`, `enter_short`, and `exit_short`. Sources importing `tomac` still fall back to the non-importing derived scaffold.
- Verification: `CARGO_TARGET_DIR=.local-artifacts/cargo-target cargo test --lib materializes_exact_freqtrade_source_without_long_scaffold_substitution --quiet` passed; `CARGO_TARGET_DIR=.local-artifacts/cargo-target cargo test --lib seed_evidence --quiet` passed `5` tests; `rustfmt --check src/application/auto_quant/seed_evidence.rs` passed; `CARGO_TARGET_DIR=.local-artifacts/cargo-target cargo test --lib auto_quant:: --quiet` passed `169` tests.
- Promotion verdict: this is a seed-materialization repair only, not a factor promotion. The prior generated M2K long scaffold remains invalid evidence; M2K still requires a rerun with exact seed semantics and downstream hard gates before any promotion: real-cost density, same-root direction, `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, and stable `execution_readiness >= 0.65`.

Interpretation:

- The bridge can now preserve exact standalone Freqtrade logic instead of silently substituting a generic long scaffold. The next M2K retry should verify the newly materialized seed body first, then rerun AQ/downstream; do not treat this code repair itself as a passed factor gate.

## Exact-Seed Futures Rerun: M2K RVOL/PDA Consistency Floor Still Fail-Closed

Append-only terminal evidence at `2026-05-20 13:32:29 +0800` after duplicate-searching for `20260520T132108`, `exact_seed_after_patch`, `TomacIbkrfutm2kliquiditysweeprejectshortrvolpdaguardpdafloor1minv1Seed`, and `20260520T132502`.

- Exact-seed repair run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T132108+0800-codex-m2k-exact-seed-class-parity-rerun-v1/`.
- AQ/Tomac exact-seed metrics: `checks/exact_seed_after_patch_metrics.json`; command output: `command-output/04_run_tomac_exact_seed_after_patch.out`; exit file: `checks/04_run_tomac_exact_seed_after_patch.exit`.
- Downstream same-root readback: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093302+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-7d-gate1-v1/simulated-trade-admission-m2k-1m-rvol-pda-consistency-floor-20260520T132502+0800/`.
- Branch path: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Seed parity: `source_semantics_preserved=true`; `exact_freqtrade_strategy_preserved=true`; `timeframe_1m_preserved=true`; `can_short_preserved=true`; `enter_short_preserved=true`; `exit_short_preserved=true`; `generic_long_scaffold_absent=true`.
- Runtime fix notes: the exact seed had to run through the Auto-Quant `strategies_external` lookup path with futures/isolated mode and futures data aliases, not the prior spot long-scaffold path.
- AQ/Tomac result: `17` shorts, raw `+2.79%`, win `64.7059%`, PF `8.4044`, Sharpe `40.1455`; cost stress stayed positive at `1bps=+2.45%`, `2bps=+2.11%`, and `5bps=+1.09%`; `downstream_allowed=true`.
- Downstream readback: all `01-19` commands exited `0`; `exact_branch_survived=true`; simulated same-workspace feedback ingested `17` trades (`11` wins / `6` losses); `mature_rows=2`; `history_mature_rows=18`; `ranker_validation_ready=true`; MTF coverage included `1m/5m/15m/30m/1h/4h`, while `1d` had only `9` bars and was below the `29` bar minimum.
- Execution blocker remains hard: `execution_candidate_status=no_trade`; `execution_readiness=0.3211044072278747`; `transition_hazard=0.9184975817511946`; `pda_hybrid_alignment=false`; `path_ranker_score_visible_to_execution_tree=true`; `path_ranker_score_used_by_execution_tree=false`.
- Terminal decision: `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The exact-seed bridge and futures/Tomac path are now mechanically repaired, and the same M2K root remains a real cost survivor. The objective is still not complete because execution admission fails on the hard predicate trio and the execution tree does not consume the visible ranker score. Next useful work is same-root execution-materialization/PDA-transition repair, not another light RVOL/VWAP/liquidity overlay and not lowered gates.

## Full-MTF Readback: SI 5m Tight-Range Band Expansion Fade Still Fail-Closed

Append-only terminal row at `2026-05-20 13:46:39 +0800` after duplicate-searching the Board, matrix, Hermes references, and `/tmp/ict-engine-agent-claims/board-b-factor-refinement` for `134019`, `tight-range-band-expansion-fade-full-mtf`, `TightRangeBandExpansionFade`, and `20260520T133827`.

- Full-MTF readback root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/simulated-trade-admission-si-5m-tight-range-band-expansion-fade-full-mtf-20260520T134019+0800/`.
- Metrics: `checks/simulated_trade_admission_metrics.json`.
- Valid MTF subset manifest: `data/cleaned-mtf-analyze-valid/analyze-valid-mtf-subset-manifest.json`.
- Branch path: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Selected Gate 1 row remained `dense_fade`: `9` trades, win rate `77.7778%`, raw `+2.23%`; same-Auto-Quant-workspace simulated feedback ingested the same `9` trades (`7` wins / `2` losses).
- MTF repair evidence: runtime consumed `multi_timeframe_source=analyze_explicit_with_auto_fill covered_intervals=5m,15m,30m,1h,4h`; valid subset rows were `5m=6047`, `15m=2016`, `30m=1008`, `1h=504`, and `4h=136`. `1d` existed with `27` bars but was below the `29` bar analyze minimum, and true `1m` context was not fabricated from retained `5m` rows.
- Command evidence: all downstream commands `01_auto_quant_results_import` through `19_policy_after_ranker` exited `0`; no timeouts.
- Admission evidence: `exact_branch_survived=true`; `ranker_validation_ready=true`; `path_ranker_score_visible_to_execution_tree=true`; `path_ranker_score_used_by_execution_tree=false`; `mature_rows=2`; `history_mature_rows=10`.
- Execution blocker remains hard: `execution_candidate_status=no_trade`; `execution_candidate_actionable=false`; `execution_gate_status=observe`; `execution_readiness=0.2344186944501619`; `transition_hazard=0.9679827616849933`; `pda_hybrid_alignment=false`.
- Terminal decision: `si5m_tight_range_band_expansion_fade_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- Adding valid `30m` and `4h` context repaired the previous MTF readback gap and made ranker validation ready, but it did not repair execution admission. The SI `5m` branch stays a useful `5bps/side` observation/repair lead only. The next aligned action is fresh true lower-timeframe `1m` context for this SI root or direct execution-tree score consumption/PDA-transition repair; do not repeat simulated feedback, add another light overlay, or lower the hard gates.

## True-1m Context Readback: SI 5m Tight-Range Still Fail-Closed

Append-only terminal row at `2026-05-20 14:07:00 +0800` after duplicate-searching the Board, diversity matrix, Hermes references, and `/tmp/ict-engine-agent-claims/board-b-factor-refinement` for `135707`, `true-1m-context`, `TightRangeBandExpansionFade`, and the exact branch path.

- True-1m-context run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/simulated-trade-admission-si-5m-tight-range-band-expansion-fade-true-1m-context-20260520T135707+0800/`.
- Metrics: `checks/simulated_trade_admission_metrics.json`.
- Valid subset manifest: `data/cleaned-mtf-analyze-valid/analyze-valid-mtf-subset-manifest.json`.
- Branch path: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Selected Gate 1 row remained `dense_fade`: `9` trades, win rate `77.7778%`, raw `+2.23%`; same-Auto-Quant-workspace simulated feedback ingested `9` trades (`7` wins / `2` losses).
- True lower-timeframe context: retained real IBKR `SI 202607` `1m 7D` rows from `support/docs/experiments/actionable-regime-confidence/runs/20260520T075139+0800-codex-ibkr-si1m-chaikin-ad-reclaim-7d-gate1-v1/data/provider/normalized/ibkr_si_202607_1m_7d.csv`; the `1m` context was not fabricated from `5m`.
- Runtime consumed valid MTF subset `1m=9453`, `5m=6047`, `15m=2016`, `30m=1008`, `1h=504`, and `4h=136`; `1d=27/29` remained below the analyze minimum and was excluded.
- Command evidence: all downstream commands `01_auto_quant_results_import` through `19_policy_after_ranker` exited `0`; no timeouts.
- Admission evidence: `exact_branch_survived=true`; `path_ranker_score_visible_to_execution_tree=true`; `path_ranker_score_used_by_execution_tree=false`; `mature_rows=2`; `history_mature_rows=10`; `ranker_validation_ready=false` with `raw_scored_mature=10/30`, `production_validation=9/30`, and `observation_validation=9/30`.
- Execution blocker remains hard: `execution_candidate_status=no_trade`; `execution_candidate_actionable=false`; `execution_gate_status=observe`; `execution_readiness=0.3719991181993223`; `transition_hazard=0.9657999879763439`; `pda_hybrid_alignment=false`.
- Terminal decision: `si5m_tight_range_band_expansion_fade_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- True retained `1m` context improved execution readiness from the full-MTF readback (`0.2344`) to `0.3720`, but it did not satisfy validation readiness, PDA alignment, transition hazard, or execution admission. Keep SI `5m` TightRangeBandExpansionFade in the repair queue only. The next useful same-root work must directly improve real/current validation rows, PDA/range-family agreement, transition hazard, or execution readiness; do not repeat simulated-feedback replay, add light RVOL/VWAP/liquidity overlays, or lower hard gates.

## Current-Code Readback: SI 5m Tight-Range History-Gate Metadata Repair Still Fail-Closed

Append-only terminal row at `2026-05-20 14:19:22 +0800` after duplicate-searching current Board/matrix/claims for `history-gate`, `141922`, `TightRangeBandExpansionFade`, and the exact branch path. This replay used an isolated state copy; it did not mutate the original `135707` run state.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/post-history-gate-metadata-readback-si5m-tight-range-20260520T141922+0800/`.
- Source run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/simulated-trade-admission-si-5m-tight-range-band-expansion-fade-true-1m-context-20260520T135707+0800/`.
- Metrics: `checks/post_history_gate_readback_metrics.json`.
- Summary: `summaries/terminal_decision_summary.md`.
- Branch path: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Command evidence: current binary rebuilt successfully; isolated readback commands `01_analyze_after_history_gate_repair`, `02_workflow_after_history_gate_repair`, `03_pre_bayes_after_history_gate_repair`, and `04_policy_after_history_gate_repair` all exited `0`.
- Metadata repair evidence: execution-tree lineage now sees the same-root history gate metadata, with score line `path_ranker_score_input=0.5164 visible=true used=false ranker_validation_ready=false execution_gate_status=pass effective_prediction_vote_score=0.4076`.
- Remaining validation blocker: `ranker_validation_ready=false`; policy readback remains `raw_scored_mature=10/30`, `production_validation=9/30`, and `observation_validation=9/30`.
- Execution blocker remains hard: branch admission `fail_closed`, ready/actionable `false/false`; execution candidate `no_trade`; `execution_gate_status=execution_observe_only`; `execution_tree_branch=transition_guardrail`; `execution_readiness=0.3719991181993223`; `transition_hazard=0.9657999879763439`; `pda_hybrid_alignment=false`.
- Terminal decision: `si5m_tight_range_post_history_gate_metadata_readback_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The code-level history-gate metadata repair worked for this branch: the execution tree no longer sees `execution_gate_status=none` for the same-root ranker score. That narrows the remaining blocker to insufficient validation rows plus PDA/range-family disagreement, high transition hazard, and sub-`0.65` readiness. Do not rerun the same simulated-feedback packet again; the next aligned SI `5m` action must add real/current validation density or directly repair PDA/range-family and transition-readiness gates.

## Corrected Exact Downstream Rerun: SI 5m ATR Exhaustion Short Still Fail-Closed

Append-only terminal row at `2026-05-20 14:22:36 +0800` after observing pre-write hash `818932608d1095bb7552fa6562cb106d1c6f05cd90ab54427f88f153e18953db` for this Board and duplicate-searching Board/matrix/claims for `140919`, `downstream-exact-si-5m-atr-exhaustion-short-20260520T140919`, `exact_si5m_atr_exhaustion_short_downstream_fail_closed`, and direction-metadata markers.

- Corrected exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T042108+0800-codex-ibkr-si5m-atr-exhaustion-short-1m-gate1-v1/downstream-exact-si-5m-atr-exhaustion-short-20260520T140919+0800/`.
- Metrics: `checks/downstream_metrics.json`.
- Summary: `summaries/terminal_decision_summary.md`.
- Wrapper hygiene fix: `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_si5m_atr_exhaustion_short_exact_downstream_v1.py` now writes exact short-side strategy-library metadata with `direction=short` and `trade_direction=Bear`; regression coverage is `support/docs/experiments/actionable-regime-confidence/scripts/test_ibkr_si5m_atr_exhaustion_short_exact_downstream_metadata.py`.
- Branch path remains: `TrendExpansion -> AtrExhaustionShort -> ibkr_si5m_atr_exhaustion_short_1m_gate1_v1`. Labels/provenance prefix: `FUTURES -> precious_metals -> SI -> 5m`.
- Gate 1 survivors remained real-cost positive at `5bps/side`: `SI/atr050_v100_hold30/5m_short` and `SI/atr030_v125_hold30/5m_short`.
- Command evidence: `01_auto_quant_results_import=0`, `02_auto_quant_prior_init=0`, `04_workflow_seed=0`, `05_pre_bayes_seed=0`, `06_export_target_seed=0`, `07_train_ranker=0`, `08_apply_ranker=0`, `09_apply_scores_to_ict=0`, `10_register_trainer=0`, `11_enable_runtime=0`, `13_workflow_after_ranker=0`, `14_pre_bayes_after_ranker=0`, `15_policy_after_ranker=0`, and `16_export_target_final=0`; both analyze calls timed out: `03_analyze_seed=124` and `12_analyze_after_ranker=124`.
- Downstream verdict: `all_commands_ok=false`, `exact_ranker_score_visible=true`, but `exact_branch_survived=false`, `mature_rows=0`, `execution_candidate_status=null`, `execution_readiness=0.0`, `transition_hazard=1.0`, and `pda_hybrid_alignment=false`.
- Terminal decision: `exact_si5m_atr_exhaustion_short_downstream_fail_closed`; `pre_bayes_allowed=false`; `bbn_allowed=false`; `catboost_allowed=false`; `execution_tree_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The metadata repair prevents the earlier short survivor from being handed downstream as a generic/Bull direction, but it does not repair live admission. The corrected exact wrapper still times out in both analyze passes and never preserves the exact branch into execution admission. Keep SI `5m` ATR exhaustion short as observation/repair evidence only; the stronger current ATR readback remains the valid-MTF probe at `20260520T135401+0800`, and the next useful work must directly address PDA/regime alignment, transition hazard, execution candidate materialization, or real/current validation rows.

## Repair Priority Diagnostic: Existing 5bps Survivors Before Next Launch

Append-only terminal row at `2026-05-20 14:36:37 +0800` after duplicate-searching the Board, diversity matrix, scripts/runs, and `/tmp/ict-engine-agent-claims/board-b-factor-refinement` for `143637`, `repair_priority_diagnostic`, and `Survivor Repair Priority`.

- Diagnostic root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T143637+0800-codex-board-b-survivor-repair-priority-diagnostic-v1/`.
- Summary: `summaries/repair_priority_diagnostic.md`.
- Metrics: `checks/repair_priority_diagnostic.json`.
- Inputs: M2K `1m` RVOL/PDA exact-seed downstream blocker report, SI `5m` TightRangeBandExpansionFade post-history-gate blocker report, and SI `5m` ATR valid-MTF blocker report.
- Ranked repair queue: `si5m_tight_range` score `6.924346`; `si5m_atr` score `1.114127`; `m2k_rvol_pda` score `0.94004`.
- Selected next repair target: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Selection reason: SI tight-range is the only compared survivor without `multi_timeframe_direction_conflict`, has `pre_bayes_gating_status=pass_neutralized`, best evidence quality (`0.5803470685111064`), best current execution readiness (`0.3719991181993223`), true retained `1m` context, and current history-gate metadata visibility.
- Remaining blockers: execution candidate `no_trade`, `transition_hazard=0.9657999879763439`, `pda_hybrid_alignment=false`, `ranker_validation_ready=false`, `path_ranker_score_used_by_execution_tree=false`, and short validation rows (`raw_scored_mature=10/30`, `production_validation=9/30`, `observation_validation=9/30`).
- Terminal decision: `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The next useful Board B action is not a fresh M2K/SI indicator clone and not another simulation-only feedback replay. Use the next practical slot on SI `5m` TightRangeBandExpansionFade real/current validation density plus PDA/range-family and transition-readiness repair. If that path is blocked, only then rotate to a materially different unclaimed public-family cell.

## Exact AQ Seed Probe Addendum: SI 5m Leverage-Tier Blocker Repaired But Economics Still Drop

Append-only addendum at `2026-05-20 14:53:01 +0800` after observing pre-write hashes `5f4ba5158840ab38a238a1ecdbe0eff3905fedf1edc9135b4d5c979e8b9053a7` for this Board and `d67bd2e5bf524727555eb43b0b80e5ffeaf79535cdd3ac916dc9b773cc365d98` for the diversity matrix, and duplicate-searching Board/matrix/claims for `exact_si5m_aq_seed_repair`, `gate1_drop_exact_aq_seed`, `08_run_tomac_exact_si5m_seeds_after_leverage_flag`, and `Si5mHazardGuardedFadeSeed`.

- Run root remains: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/post-history-gate-metadata-readback-si5m-tight-range-20260520T141922+0800/`.
- Repair summary: `summaries/exact_si5m_aq_seed_repair_summary.md`.
- Repair metrics: `checks/exact_si5m_aq_seed_repair_metrics.json`.
- Latest exact-seed summary: `summaries/exact_aq_seed_terminal_decision_summary.md`.
- Repaired blocker: previous `OperationalException: Pairs SI5M/USD got no leverage tiers available`; isolated `config.tomac.json` now sets `exchange._ft_has_params.uses_leverage_tiers=false`.
- Command evidence: workspace exit `checks/08_run_tomac_exact_si5m_seeds_after_leverage_flag.exit=0`; latest summary records `07_run_tomac_exact_si5m_seeds=0` and `08_run_tomac_exact_si5m_seeds_after_leverage_flag=0`.
- Economic verdict is unchanged: `Si5mHazardGuardedFadeSeed` `10` shorts, raw `-0.50%`, `2bps=-0.90%`, `5bps=-1.50%`; `Si5mPdaRangeConsistencyShortSeed` `0` trades; `Si5mRangeBoxBreakdownFadeSeed` `24` shorts, raw `-2.23%`, `2bps=-3.19%`, `5bps=-4.63%`.
- Terminal decision: `gate1_drop_exact_aq_seed_economics_raw_negative_or_zero_trade`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This addendum narrows the outcome: the isolated futures/Tomac infrastructure blocker is repaired, but the tested exact same-root seed shapes are still raw-negative or zero-trade. Do not downstream this seed batch. Keep the earlier SI `5m` TightRangeBandExpansionFade branch as repair-only, and move any next SI attempt toward real/current validation density or a materially different same-root PDA/range-family hypothesis.

## Validation Gap Diagnostic: SI 5m Tight-Range Still Needs Real/Current Rows And PDA/Transition Repair

Append-only terminal row at `2026-05-20 14:59:00 +0800` after observing pre-write hashes `b41a2c4de41ac2015253d49d520a6e9848e0cce5a725470bc793b29d3b745b45` for this Board and `511d5da23b3e7b564c9e77413c1bb9213577422dda9d70adbc4ee0390d8ec77f` for the diversity matrix, and duplicate-searching Board/matrix/claims for `145600`, `validation_gap_diagnostic`, `Validation Gap Diagnostic`, and `repair_blocked_on_validation_density`.

- Diagnostic root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T145600+0800-codex-si5m-tight-range-validation-gap-diagnostic-v1/`.
- Summary: `summaries/validation_gap_diagnostic.md`.
- Metrics: `checks/validation_gap_diagnostic.json`.
- Source run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T070456+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/post-history-gate-metadata-readback-si5m-tight-range-20260520T141922+0800/`.
- Branch path remains: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Diagnostic scope: read-only artifact analysis only; no provider fetch, no AQ rerun, no Pre-Bayes/BBN/CatBoost mutation, and no execution-tree state mutation.
- Validation gap: current structural target rows `5`, current mature rows `2`; history rows `19`, history mature rows `10`; gate readback remains `raw_scored_mature=10/30`, `production_validation=9/30`, `observation_validation=9/30`.
- Exact-branch row gap: current exact-branch target rows `3` with `2` mature; history exact-branch target rows `15` with `10` mature; exact feedback rows in `auto_quant_real_trade_feedback_catboost_training.csv` are `0`.
- Execution gap: candidate `no_trade`, actionable `false`; execution tree `fail_closed`; execution readiness `0.3719991181993223`; transition hazard `0.9657999879763439`; `pda_hybrid_alignment=false`; path-ranker visible `true`, used `false`, validation ready `false`, source `history_path`, family `catboost`.
- Terminal decision: `repair_blocked_on_validation_density_pda_transition_and_execution_candidate_materialization`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The next blocker is not Tomac mechanics or seed execution. The retained SI `5m` tight-range branch still needs same-root real/current validation density plus a PDA/range-family or transition-readiness repair that can materialize an execution candidate. Do not rerun these exact seed shapes, repeat simulated-feedback replay, lower gates, or call the branch trade-usable.

## Corrected Datafeed Rerun: TVR CRWD 5m Provider-Parity Branch Still Fail-Closed

Append-only terminal row at `2026-05-20 15:20:48 +0800` after observing pre-write hash `219c62a5c65314c7764ef213e0949d9717f2a1c5ee95666dc390bf629dab1402` for this Board and duplicate-searching Board/claims for `152048`, `corrected-data`, and `exact_tvr_crwd_5m_downstream_fail_closed`.

- Corrected downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260519T143455+0800-hermes-tvr-crwd-pda-mtf-soft-confirmation-1m-full-ladder-v1/downstream-exact-crwd-5m-corrected-data-20260520T152048+0800/`.
- Metrics: `checks/downstream_metrics.json`.
- Summary: `summaries/terminal_decision_summary.md`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T152048+0800-codex-tvr-crwd-corrected-data-exact-downstream-v1.claim`.
- Wrapper/datafeed repair: `support/docs/experiments/actionable-regime-confidence/scripts/run_tvr_crwd_pda_mtf_soft_confirmation_5m_exact_downstream_corrected_v1.py` avoids the inherited ARKK aliases and runs analyze/post-ranker analyze on `tvr_crwd_5m.csv`, `tvr_crwd_15m.csv`, and `tvr_crwd_30m.csv`.
- Branch path remains: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> tvr_crwd_pda_mtf_soft_confirmation_5m_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Gate 1 row is unchanged and remains cost-surviving: `28` trades, win `67.8571%`, raw `+4.73%`, `1bps=+4.17%`, `2bps=+3.61%`, and `5bps=+1.93%`.
- Command evidence: all corrected downstream commands `01_auto_quant_results_import` through `16_policy_after_ranker` exited `0`; exact branch survived downstream.
- Terminal blocker remains hard: closed-loop admission stayed `status=fail_closed`, `ready=false`, `actionable=false`, `candidate_status=execution_observe_only`; execution candidate stayed `no_trade`; execution tree stayed `gate_status=observe`, `branch=transition_guardrail`, `execution_readiness=0.3205531634565365`, `pda_hybrid_alignment=false`, `transition_hazard=null`; path-ranker visible/used/validation-ready=`true/false/false`.
- Terminal decision: `exact_tvr_crwd_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The earlier TVR CRWD downstream had a real datafeed contamination bug from inherited `tvr_arkk_*` analyze inputs. The corrected rerun fixes the datafeed readback and improves readiness from `0.1500` to `0.3206`, but it still fails the promotion gates because PDA alignment is false, validation readiness is false, execution candidate is `no_trade`, and the visible path-ranker score is not used by the execution tree. Keep TVR CRWD as provider-parity observation only; do not promote it or lower the CRWD hard gates.

## Correction Addendum: SI 5m Tight-Range Autoresearch Revalidated Cost Survivors But Did Not Repair Admission

Append-only correction at `2026-05-20 15:27:00 +0800` after observing pre-write hash `457b6232aef404db4d0e6de2874e028747b1a63c280d5d24870539c0edb194a3` for this Board and duplicate-searching Board/matrix for `151200`, `autoresearch_continuation_revalidated`, `DenseFade`, and `LateMean`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T151200+0800-codex-si5m-tight-range-autoresearch-continuation-v1/`.
- Metrics: `checks/autoresearch_continuation_metrics.json`.
- Summary: `summaries/autoresearch_continuation_terminal_summary.md`.
- Workspace-owner repair: the copied state initially had a stale handoff that still recommended executing the old `070456` Auto-Quant workspace. After refreshing the handoff and re-preparing, `auto-quant-status` pointed `workspace.repo_root` and `recommended_next_command` at the local `151200/state/.deps/auto-quant` workspace.
- Exact local Tomac run: `checks/06_run_tomac_exact_si5m_materials.exit=0`; generic `TomacNQ` seed was disabled; `config.tomac.json` ran `SI5M/USD` in isolated futures mode with leverage-tier lookup disabled.
- Corrected Gate 1 replay: `IbkrSi5mTightRangeBandExpansionFadeDenseFadeV1` survived `5bps/side` with `12` shorts, raw `+1.69%`, `1bps=+1.45%`, `2bps=+1.21%`, and `5bps=+0.49%`; `IbkrSi5mTightRangeBandExpansionFadeLateMeanV1` survived `5bps/side` with `4` shorts, raw `+1.10%`, `1bps=+1.02%`, `2bps=+0.94%`, and `5bps=+0.70%`.
- Dropped rows: `BalancedFade` had `4` shorts, raw `-0.03%`, `5bps=-0.43%`; `QualityFade` had `2` shorts, raw `-0.48%`, `5bps=-0.68%`.
- Correction to earlier claim/matrix wording: this continuation is not a no-cost-survivor Gate 1 drop. It is a cost-survivor revalidation that still does not add real/current validation density or repair PDA/range-family disagreement, transition hazard, path-ranker use, or execution candidate materialization.
- Terminal decision: `autoresearch_continuation_revalidated_cost_survivors_but_no_new_downstream_repair`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The local Auto-Quant/Tomac mechanics are now isolated and valid for this replay, and the original same-root cost survivor evidence is still real. But the prior validation-gap diagnostic remains authoritative for admission: current branch still needs acceptable same-root real/current validation rows plus direct PDA/transition/execution-candidate repair. Do not downstream this correction as a fresh promotion attempt, do not lower gates, and do not repeat the stale copied-state handoff pattern.

## Fresh IBKR Gate 1 + Exact Downstream: SI 5m Tight-Range Still Fail-Closed

Append-only terminal row at `2026-05-20 15:55:00 +0800` after observing pre-write hashes `c65268fbc9809f44c82fcb00655f346c4610f2116cb3ea6b0dafcdec2a892190` for this Board and `96d64cdb3f139410cb52aed7e7f04565545bdbe45bafa98f21d0a158607c5e87` for the diversity matrix, and duplicate-searching Board/matrix/claims for `154206`, `154505`, `5911`, and `exact_si5m_tight_range_band_expansion_fade_downstream_fail_closed`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T154206+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; summary: `summaries/terminal_decision_summary.md`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T154206+0800-codex-ibkr-si5m-tight-range-band-expansion-fade-1m-gate1-v1/downstream-exact-si-5m-tight-range-band-expansion-fade-20260520T154505+0800/`.
- Downstream metrics: `checks/downstream_metrics.json`; summary: `summaries/terminal_decision_summary.md`.
- Branch path remained: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Fresh-provider improvement: IBKR fetch exited `0` with `5911` normalized `SI 202607` `5m` rows for `1 M`; `local_cache_replay=false`. This is not the prior retained-only replay.
- Gate 1 cost evidence: `SI/dense_fade/5m_range_fade` had `9` shorts, raw `+2.23%`, `1bps=+2.05%`, `2bps=+1.87%`, `5bps=+1.33%`; `SI/quality_fade/5m_range_fade` had `7` shorts, raw `+1.16%`, `1bps=+1.02%`, `2bps=+0.88%`, `5bps=+0.46%`. `BalancedFade` and `LateMean` were negative after costs.
- Downstream evidence: all commands `01_auto_quant_results_import` through `16_export_target_final` exited `0`; exact branch survived; CatBoost/path-ranker score was visible; execution tree gate was attempted.
- Terminal blocker: closed-loop admission stayed `fail_closed`; execution candidate stayed `no_trade`; `mature_rows=0`, `execution_readiness=0.4444998121450926`, `transition_hazard=0.95189338967589`, and `pda_hybrid_alignment=false`.
- Terminal decision: `exact_si5m_tight_range_band_expansion_fade_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This run repairs the earlier missing current/fresh-provider evidence for the exact SI `5m` tight-range root and reconfirms real `5bps/side` cost survivors on fresh IBKR rows. It still does not clear practical admission: validation maturity is zero and the execution predicate trio remains hard-failed. Keep this as a stronger repair lead, not a promoted factor. The next useful SI work must directly reduce transition hazard, align PDA/hybrid regime, and materialize an actionable execution candidate without lowering gates.

## TVR CRWD 5m TrendReclaim Full-Ladder Downstream: Cost Survivor Still Fail-Closed

Append-only terminal row at `2026-05-20 16:00:30 +0800` after observing pre-write hashes `448e9d1c66b5db0212f31e337983b9a88bcb5be35fae18889ee7ce37ed81ba88` for this Board and `d5362a4d222b6ccd464ede4fbce269dab335a999355e17c549c9d082de318a17` for the diversity matrix, and duplicate-searching Board/matrix/claims for `160030`, `tvr_crwd5m_trend_reclaim_full_ladder`, and `downstream-exact-tvr-crwd5m-trend-reclaim`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T155414+0800-codex-tvr-crwd5m-trend-reclaim-full-ladder-gate1-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T155414+0800-codex-tvr-crwd5m-trend-reclaim-full-ladder-gate1-v1/downstream-exact-tvr-crwd5m-trend-reclaim-20260520T160030+0800/`.
- Downstream metrics: `checks/downstream_metrics.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_tvr_crwd5m_trend_reclaim_full_ladder_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T1550+0800-codex-tvr-crwd5m-trend-reclaim-full-ladder-gate1-v1.claim`.
- Branch path remained exact: `TrendExpansion -> TrendReclaim -> tvr_crwd5m_trend_reclaim_full_ladder_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 5m`.
- Gate 1 exact `5m` cost survivor: `23` trades, `78.2609%` win rate, raw `+5.88%`, `1bps=+5.42%`, `2bps=+4.96%`, and `5bps=+3.58%`.
- Downstream command evidence: all commands from real-trade extraction through AQ import/prior, analyze, Pre-Bayes, policy export, real-trade ingest, CatBoost train/apply/register, runtime enable, post-ranker analyze/readbacks, and execution-candidate workflow exited `0`.
- Exact branch survived downstream and the path-ranker score was visible to execution, but admission failed closed: persisted execution candidate `no_trade`; live execution candidate `execution_blocked`; `mature_rows=2`, `history_mature_rows=24`; `execution_readiness=0.3205531634565365`; `transition_hazard=0.9498010120308746`; `pda_hybrid_alignment=false`; path-ranker visible/used/validation-ready=`true/false/false`.
- Terminal decision: `exact_crwd_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This 5m branch is independent from the failed `1m` sibling and did earn a real TVR cost-survivor downstream attempt. It still cannot be promoted because the same-root execution candidate is blocked/no-trade, PDA/hybrid alignment is false, transition hazard is far above the hard gate, and ranker validation/use is incomplete. Preserve it as provider-parity observation/repair evidence only.

## TVR CRWD 1m TrendReclaim Full-Ladder Gate 1: Exact 1m Root Failed Cost Density

Append-only terminal row at `2026-05-20 16:18 +0800` after observing pre-write hashes `07992d3b571aca9d1b253a562129d43a2a8aa6bd83a2e12afd60efe8cc9f4bb5` for this Board and `0d03d1a4df01393c046d9c0bbb8530e7499f439d67ad2362a12a2bdb19604a81` for the diversity matrix, and duplicate-searching Board/matrix/claims for `154648`, `tvr_crwd1m_trend_reclaim`, and `tvr_crwd1m_trend_reclaim_gate1_failed_cost_density_or_branch_fields`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T154648+0800-codex-tvr-crwd1m-trend-reclaim-full-ladder-gate1-v1/`.
- Metrics: `checks/terminal_metrics.json`; summary: `summaries/terminal_decision_summary.md`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T1542+0800-codex-tvr-crwd1m-trend-reclaim-full-ladder-gate1-v1.claim`.
- Branch path remained exact: `TrendExpansion -> TrendReclaim -> tvr_crwd1m_trend_reclaim_full_ladder_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> CRWD -> 1m`.
- Provider provenance: retained/corrected TradingViewRemix CRWD rows only; `1m` root used `2731` retained rows with `local_cache_replay=true`, so this does not claim fresh-provider parity.
- Gate 1 command evidence: `00_strategy_py_compile`, `01_auto_quant_agent_material_batch`, `02_auto_quant_agent_material_dispatch`, and `03_auto_quant_agent_material_rank` all exited `0`; rank output contained `7` rows.
- Exact `1m` root verdict: `9` trades, raw `-0.29%`, `2bps/side=-0.65%`, `5bps/side=-1.19%`, Sharpe `-3.1131`; failed cost density and stopped before downstream.
- Sibling context only: `5m` (`23` trades, raw `+5.88%`, `5bps=+3.58%`), `30m` (`5` trades, raw `+1.91%`, `5bps=+1.41%`), `15m` (`6` trades, raw `+1.08%`, `5bps=+0.48%`), and `1h` (`1` trade, raw `+0.59%`, `5bps=+0.49%`) were positive, but they do not promote or rescue the failed exact `1m` lane.
- Terminal decision: `tvr_crwd1m_trend_reclaim_gate1_failed_cost_density_or_branch_fields`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This terminalizes the independent `1m` CRWD TrendReclaim root as a clean Gate 1 negative. Positive `5m/15m/30m/1h` siblings must remain separate per-timeframe lanes; only the already-terminalized `5m` sibling earned downstream replay, and the `1m` root should not be patched with overlays or downstream replay until a fresh exact `1m` hypothesis survives real cost density.

## Read-Only Blocker Report: CRWD 5m TrendReclaim Remains Repair-Only

Append-only terminal readback at `2026-05-20 16:25:28 +0800` after observing pre-write hashes `0496c9ea50c5cb4b5c5b156728af8998fcc39d57be9f0a96c9178f351481d57f` for this Board and `0d03d1a4df01393c046d9c0bbb8530e7499f439d67ad2362a12a2bdb19604a81` for the diversity matrix, and duplicate-searching Board/matrix for `20260520T162321`, `crwd-trendreclaim-blocker-report`, and `crwd_trendreclaim_remains_repair_only_no_promotion`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T162321+0800-codex-crwd-trendreclaim-blocker-report-v1/`.
- Corrected metrics: `checks/combined_crwd_trendreclaim_blocker_report.corrected.json`.
- Summary: `summaries/terminal_decision_summary.md`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T162321+0800-codex-crwd-trendreclaim-blocker-report-v1.claim`.
- Scope: read-only comparison of the existing IBKR and TVR exact `CRWD/5m -> TrendExpansion -> TrendReclaim` downstream artifacts; no provider fetch, Auto-Quant dispatch, analyze rerun, or runtime-state mutation.
- Correction note: the first helper pass expected an `exact_*_survivors_5bps` schema and misread these CRWD metrics. The corrected report uses authoritative `cost_stress_row` values from each downstream `checks/downstream_metrics.json`.
- IBKR branch: `US_EQ -> single_stock -> CRWD -> 5m -> TrendExpansion -> TrendReclaim -> ibkr_crwd5m_retained_trend_reclaim_gate1_v1`; cost row `85` trades, raw `+7.42%`, `2bps=+4.02%`, `5bps=-1.08%`; ranker visible/used/ready=`true/true/true`; execution candidate `execution_blocked`; `execution_readiness=0.67`, `transition_hazard=0.9621317282445029`, `pda_hybrid_alignment=false`.
- TVR branch: `US_EQ -> single_stock -> CRWD -> 5m -> TrendExpansion -> TrendReclaim -> tvr_crwd5m_trend_reclaim_full_ladder_v1`; cost row `23` trades, raw `+5.88%`, `2bps=+4.96%`, `5bps=+3.58%`; ranker visible/used/ready=`true/false/false`; execution candidate `execution_blocked`; `execution_readiness=0.3205531634565365`, `transition_hazard=0.9498010120308746`, `pda_hybrid_alignment=false`.
- Terminal decision: `crwd_trendreclaim_remains_repair_only_no_promotion`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- IBKR repaired ranker use but still fails 5bps economics plus PDA/transition/actionable execution. TVR is the stronger repair lead because it has real `5bps/side` margin, but it remains short of validation/ranker-use and still fails the hard PDA/transition/actionable-execution gates. Do not promote either lane, do not rerun the failed `1m` sibling, and do not add another generic overlay. The next useful TVR work must add same-root mature validation and make ranker scores used before another PDA repair is meaningful.

## Read-Only Reverse-Gate Diagnostic: M2K RVOL/PDA Cost Survivor Still Observation-Only

Append-only terminal readback at `2026-05-20 16:18:29 +0800` after observing pre-write hashes `380ab23f597745f673d820c1ea8acead7b8e6d1a31ca2d4ad1ccf1aa7117db3f` for this Board and `7b23784535da76e08922cd8571aa890883b533330b7f6cef8395f47f750ea666` for the diversity matrix, and duplicate-searching Board/matrix/claims for `20260520T161829`, `m2k_rvol_pda_cost_survivor_remains_observation_reverse_gates_incomplete`, and `M2K Execution Admission Reverse-Gate Diagnostic`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T161829+0800-codex-m2k-execution-admission-reverse-gate-diagnostic-v1/`.
- Metrics: `checks/reverse_gate_metrics.json`.
- Summary: `summaries/terminal_decision_summary.md`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T161829+0800-codex-m2k-execution-admission-reverse-gate-diagnostic-v1.claim`.
- Scope: read-only artifact analysis only; no provider fetch, Auto-Quant dispatch, analyze rerun, runtime-state mutation, or downstream promotion.
- Source downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093302+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-7d-gate1-v1/exact-seed-futures-fee-ladder-downstream-m2k-1m-rvol-pda-consistency-floor-20260520T133948+0800/`.
- Branch path remained exact: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Cost survivor context remains real but insufficient: exact short-side futures seed parity was repaired; `17` shorts, raw `+2.79%`, `5bps/side=+1.09%`.
- Reverse-gate blockers: ranker validation still short (`raw_scored_mature=17/30`, `production_validation=16/30`, `observation_validation=16/30`); execution candidate remains `no_trade`; `execution_readiness=0.3211044072278747`; `transition_hazard=0.9184975817511946`; `pda_hybrid_alignment=false`; path-ranker visible/used=`true/false`.
- Terminal decision: `m2k_rvol_pda_cost_survivor_remains_observation_reverse_gates_incomplete`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- M2K `1m` RVOL/PDA remains a same-root repair lead, not a practical factor. The next useful work must add real/current same-root mature validation and directly repair PDA/hybrid alignment, transition hazard, and execution-candidate materialization. Do not rerun generic AQ seeds, repeat simulation-only feedback, or add light RVOL/VWAP/liquidity overlays.

## Read-Only Same-Root Readback: SI 5m TightRange Still Validation/Execution Blocked

Append-only terminal readback at `2026-05-20 16:29:32 +0800` after observing pre-write hashes `40b3304aac9658a84c7d225d969c8bbf55fb94db3724e8b03e711aaf7e2948cd` for this Board and `66a5cbada1ce2237df347c17723cfb1dd8f3d3ccfdf827566c646388247eb19b` for the diversity matrix, and duplicate-searching Board/matrix/claims for `162932`, `same-root readback`, `si5m_tight_range_same_root`, `154505`, and `TightRangeBandExpansionFade`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T162932+0800-codex-si5m-tight-range-same-root-readback-v1/`.
- Metrics: `checks/readback_metrics.json`.
- Summary: `summaries/terminal_decision_summary.md`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T162932+0800-codex-si5m-tight-range-same-root-readback-v1.claim`.
- Scope: read-only same-root workflow, Pre-Bayes, policy-training, and structural target export readback against the existing `154505` SI downstream state; no provider fetch, Auto-Quant dispatch, or new analyze rerun.
- Command exits: `01_workflow_refresh=0`, `02_pre_bayes_refresh=0`, `03_policy_status=0`, `04_export_target=0`.
- Branch path remained exact: `RangeConsolidation -> TightRangeBandExpansionFade -> ibkr_si5m_tight_range_band_expansion_fade_1m_gate1_v1`.
- Cost-survivor context remains useful but insufficient: fresh IBKR `5911` rows; `dense_fade` had `9` shorts with `5bps=+1.33%`; `quality_fade` had `7` shorts with `5bps=+0.46%`.
- Fresh readback blocker state: Pre-Bayes stayed `pass_neutralized` with confidence `0.6645952013340393`, but policy/ranker validation stayed empty (`raw_scored_mature=0/30`, `production_validation=0/30`, `observation_validation=0/30`), Auto-Quant real-trade feedback matched rows stayed `0`, export target had `4` rows and `0` mature rows, execution candidate stayed `no_trade`, `execution_readiness=0.4444998121450926`, `transition_hazard=0.95189338967589`, `pda_hybrid_alignment=false`, and path-ranker visible/used/ready=`true/false/false`.
- Terminal decision: `si5m_tight_range_same_root_remains_observation_validation_and_execution_blocked`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This readback does not change the factor verdict: SI `5m` TightRange remains a strong observation/repair lead, not a practical factor. Do not rerun Gate 1 or repeat metadata/readback-only work. The next useful action is to add acceptable same-root real/current feedback rows and repair PDA/hybrid transition agreement so an exact execution candidate can materialize and the ranker score can be used by the execution tree.

## Same-Root Hazard-Micro Trim Replay: NET 5m Preserves Cost But Regresses Admission Predicates

Append-only terminal row at `2026-05-20 17:44:33 +0800` after observing pre-write hashes `cb7042adebbacc81d72924e00b75d40e946824eb020308628dfb2407043568df` for this Board and `9f650ad9111a03cc62ad6f36c2c13f5c125998641b90885f8a2d0fd40d9ff81c` for the diversity matrix, and duplicate-searching Board/matrix/claims for `171116`, `171258`, `hazard_micro_trim`, and `exact_net_5m_downstream_fail_closed`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T171116+0800-codex-yf-ai-security-net5m-pda-sequence-consistency-light-hazard-micro-trim-v1/`.
- Exact downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T171116+0800-codex-yf-ai-security-net5m-pda-sequence-consistency-light-hazard-micro-trim-v1/downstream-exact-net-5m-pda-sequence-consistency-light-20260520T171258+0800/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; downstream metrics: `checks/downstream_metrics.json`; summaries: `summaries/terminal_decision_summary.md`.
- Branch path remained exact: `RangeReversion -> AiSecuritySoftwareOversoldReclaim -> rsi_vwap_reclaim_dense -> yf_ai_security_software_rsi_vwap_reclaim_net_5m_v1 -> session_liquidity_transition_stability_v1 -> pda_mtf_soft_confirmation_v1 -> pda_sequence_consistency_light_v1 -> hazard_micro_trim_v1`. Labels/provenance prefix: `US_EQ -> single_stock -> NET -> 5m`.
- Provider evidence: fresh yfinance/YF `NET` rows with `local_cache_replay=false` for `5m/15m/30m/1h/1d`; exact `5m` row count `2965`.
- Gate 1 cost evidence: exact `NET/5m` had `32` trades, `62.5%` win rate, raw `+4.05%`, `1bps=+3.41%`, `2bps=+2.77%`, and `5bps=+0.85%`; downstream was allowed.
- Downstream evidence: all command exits were `0`; exact branch survived; `32` real trades were ingested (`20` wins / `12` losses); path-ranker visible/used/validation-ready=`true/true/true`; `mature_rows=3`, `history_mature_rows=35`.
- Terminal blocker: persisted execution candidate stayed `no_trade`; live structural candidate stayed `execution_observe_only` / `fail_closed`; execution tree stayed `gate_status=observe`, branch `transition_guardrail`; `execution_readiness=0.67`, but `transition_hazard=0.9497840248880068` and `pda_hybrid_alignment=false` failed the hard predicates.
- Verification evidence: `python3 -m json.tool` passed for both metrics JSON files, and `python3 -m py_compile` passed for `run_yf_ai_security_net5m_pda_sequence_consistency_light_gate1_v1.py` and `run_yf_ai_security_net5m_pda_sequence_consistency_light_exact_downstream_v1.py`.
- Terminal decision: `exact_net_5m_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The NET `5m` mother-template still has real cost density and clean ranker use, but the hazard-micro trim does not move the branch toward the required `transition_hazard < 0.60` and `pda_hybrid_alignment=true` state. It is worse than the older near-pass that held `pda_hybrid_alignment=true` with `transition_hazard=0.63049`. Do not repeat late/hazard micro trims or lower gates. The next Board B action should either isolate the PDA/hybrid family-agreement predicate directly with a new hypothesis, or cool down NET and move the same template to a fresh adjacent clone/provider cell.

## Bybit OP/ARB 1m Full-Ladder Volatility Pullback Gate 1: Origin Blocked, HTF Context Only

Append-only terminal row at `2026-05-20 17:48 +0800` after checking the current Board/matrix tails, `/tmp/ict-engine-agent-claims/board-b-factor-refinement`, and active factor/AQ processes before taking a fresh non-SI/non-CRWD/non-M2K lane.

- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T-current-codex-bybit-op-arb-keltner-atr-pullback-1m-full-ladder-v1.claim`.
- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T174614+0800-codex-bybit-op-arb-keltner-atr-pullback-1m-full-ladder-v1/`.
- Metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper reuse: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_crypto_volatility_pullback_reclaim_gate1_v1.py` now accepts env-configured `RUN_ID`, `AQ_SYMBOL`, `FACTOR_ID`, `BRANCH_PATH`, `BYBIT_CATEGORY`, `SYMBOLS`, and `TIMEFRAMES`, preserving existing default behavior while allowing full-ladder Bybit linear probes.
- Branch path remained exact: `TrendExpansion -> BybitLayer2KeltnerAtrPullback -> keltner_atr_pullback_reclaim_1m_full_ladder -> bybit_op_arb_keltner_atr_pullback_reclaim_1m_full_ladder_v1`.
- Provider evidence: real Bybit public `linear` rows were acquired for `OPUSDT` and `ARBUSDT` across `1m/5m/15m/30m/1h/4h/1d` (`provider_count=14`, `local_cache_replay=false`); intraday validation exited `0`; both `1d` validation probes exited `1`.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and rank all exited `0`; rank rows `14`; branch fields were preserved.
- Cost verdict: no exact `1m` origin survivor met the density/cost gate. `OPUSDT/1m` had only `2` trades despite `2bps=+0.65%`; `ARBUSDT/1m` had `4` trades and `2bps=-0.55%`. Context-only survivors were `OPUSDT/30m` (`8` trades, `2bps=+2.97%`, `5bps=+2.49%`) and `OPUSDT/4h` (`6` trades, `2bps=+2.94%`, `5bps=+2.58%`).
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This was a clean real-provider/AQ Gate 1 attempt on a materially different crypto family from the latest WLD/TAO CMF/OBV negative. It produced useful higher-timeframe context, but it does not satisfy the user's `1m`-origin density requirement and must not be pushed downstream or promoted. The next crypto attempt should pivot to a denser `1m` entry family with wider expected per-trade excursion, not retune this exact OP/ARB pullback shape.

## IBKR Futures 1m Liquidity-Sweep VWAP Reclaim 7D Rerun: Dense But Fully Inside Cost Envelope

Append-only terminal row at `2026-05-20 17:53:06 +0800` after observing pre-write hashes `f7f5dc192b09c69c690501654c6128aba1c8b814b176b5fb3c81da4b2cc8c916` for this Board and `837e29d483c46f26e276a2fd28738d37fc5bc1b6fb5aff6f7e8dc031e77198dd` for the diversity matrix, checking active processes/claims, and duplicate-searching Board/matrix for `20260520T174821`, `liquidity-sweep-vwap-reclaim-1m-7d`, `exact_1m_survivors_2bps`, and `drop_or_block_gate1_practical`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T174821+0800-codex-ibkr-futures-liquidity-sweep-vwap-reclaim-1m-7d-gate1-v1/`.
- Metrics: `checks/terminal_metrics.json`; summary: `summaries/terminal_decision_summary.md`; rank rows: `summaries/rank_rows.csv`; provider matrix: `summaries/provider_provenance_matrix.csv`.
- Scope: Gate 1 only. No Pre-Bayes/BBN/CatBoost/execution-tree downstream was run because the practical cost gate failed.
- Command exits: `provider_status_ibkr=0`, `ibkr_fetch_mes_1m_7d=0`, `ibkr_fetch_mnq_1m_7d=0`, `ibkr_fetch_mgc_1m_7d=0`, `strategy_py_compile=0`, `auto_quant_agent_material_batch=0`, `auto_quant_agent_material_dispatch=0`, `auto_quant_agent_material_rank=0`.
- Provider/AQ evidence: real IBKR rows for `MES`, `MNQ`, and `MGC` `1m` over the 7D lane; Auto-Quant material batch, dispatch, and rank all completed with rooted branch fields preserved.
- Cost/density evidence: dense enough to judge, but no exact `1m` row survived even `1bps/side` to `2bps/side` friction. `MES/dense/1m` had `61` trades, raw `+0.97%`, `1bps=-0.25%`, `2bps=-1.47%`, and `5bps=-5.13%`; `MES/balanced/1m` had `54` trades, raw `+0.55%`, `1bps=-0.53%`, `2bps=-1.61%`, and `5bps=-4.85%`; `MES/quality/1m` had `39` trades, raw `+0.45%`, `1bps=-0.33%`, `2bps=-1.11%`, and `5bps=-3.45%`. `MNQ/dense/1m` had `76` trades, raw `+0.62%`, `1bps=-0.90%`, `2bps=-2.42%`, and `5bps=-6.98%`; `MNQ/balanced/1m` had `62` trades, raw `+0.83%`, `1bps=-0.41%`, `2bps=-1.65%`, and `5bps=-5.37%`; `MNQ/quality/1m` had `43` trades, raw `+0.50%`, `1bps=-0.36%`, `2bps=-1.22%`, and `5bps=-3.80%`. `MGC` rows were raw-negative before costs.
- Terminal decision: `drop_or_block_gate1_practical`; `exact_1m_survivors_2bps=[]`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Verification evidence: `python3 -m json.tool` passed for `checks/terminal_metrics.json`; `python3 -m py_compile` passed for `run_ibkr_futures_liquidity_sweep_vwap_reclaim_1m_gate1_v1.py`.

Interpretation:

- This rerun closes the fresh active IBKR futures liquidity-sweep VWAP reclaim `1m` Gate 1 lane as a clean negative, not a provider or AQ failure. The branch has trade density, but the excursion is completely inside realistic execution costs. Do not downstream this root and do not retune nearby VWAP/liquidity-sweep overlays for `MES/MNQ/MGC 1m`; rotate to a materially wider-excursion public family or a different market cell.

## Bybit DOGE/PEPE 1m Stop-Run Reclaim Full-Ladder Gate 1: Origin Blocked

Append-only terminal row at `2026-05-20 18:03:16 +0800` after observing pre-write hashes `f2c2d4cb6ac22c097303dfc56fa2534a945cdd2614f0ecd7344f5190848b0235` for this Board and `74b04c2d1c655f5de0ae7e13e567b827279f39b5fd0d5acd51e9bac62002a904` for the diversity matrix, and duplicate-searching Board/matrix/claims for `175707`, `doge_pepe_stoprun`, `DOGEUSDT/1000PEPEUSDT`, `stoprun-reclaim`, and `stop-run reclaim`.

- Primary run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T175707+0800-codex-bybit-doge-pepe-stoprun-reclaim-1m-full-ladder-v1/`.
- Primary metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_doge_pepe_stoprun_reclaim_1m_full_ladder_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T175646+0800-codex-bybit-doge-pepe-stoprun-reclaim-1m-full-ladder-v1.claim`.
- Branch path remained exact: `RangeReversion -> BybitMemecoinLiquiditySweep -> one_minute_stoprun_reclaim_full_ladder -> bybit_doge_pepe_stoprun_reclaim_1m_full_ladder_v1`.
- Provider evidence: real Bybit public `linear` rows were acquired for `DOGEUSDT` and `1000PEPEUSDT` across `1m/5m/15m/30m/1h/4h/1d` (`provider_count=14`, `local_cache_replay=false` by construction); intraday validation exited `0`; both `1d` validation probes exited `1`.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and rank all exited `0`; rank rows `14`; branch fields were preserved.
- Cost verdict: no exact `1m` origin survivor met density/cost gates. `DOGEUSDT/1m` had `1` trade, raw `-0.10%`, `2bps=-0.14%`; `1000PEPEUSDT/1m` had `2` trades, raw `-0.39%`, `2bps=-0.47%`. Context-only non-origin row: `DOGEUSDT/15m` had `7` trades, raw `+1.94%`, `2bps=+1.66%`, `5bps=+1.24%`.
- Collision audit: an earlier same-lane claim `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T175628+0800-codex-bybit-doge-pepe-stoprun-reclaim-1m-full-ladder-v1.claim` produced a parallel run `support/docs/experiments/actionable-regime-confidence/runs/20260520T175917+0800-codex-bybit-doge-pepe-stoprun-reclaim-1m-full-ladder-v1/`; that run used `PEPEUSDT` rather than `1000PEPEUSDT`, so the PEPE fetches exited `1`, retained only `DOGEUSDT` rows (`provider_count=7`), produced `7` rank rows and only `3` total trades, and also had no exact `1m` or any cost survivor. It does not override the primary corrected `1000PEPEUSDT` run.
- Verification evidence: `python3 -m json.tool` passed for primary `checks/terminal_metrics.json` and `checks/cost_stress.json`; `python3 -m py_compile` passed for the primary wrapper.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is a real-provider/AQ Gate 1 attempt on a materially different Bybit memecoin stop-run family. It produced one small `15m` DOGE context survivor, but the exact `1m` origin was sparse and negative after costs. Do not downstream either same-lane packet, and do not flatten the `15m` context row into `1m` proof. A future continuation would need a separate exact `15m` branch or a materially denser `1m` hypothesis, not another same stop-run parameter retune on this cell.

## Bybit BNB/LINK 1m NR7 Range-Expansion Full-Ladder Gate 1: Cost/Density Drop

Append-only terminal row at `2026-05-20 18:09:36 +0800` after observing pre-write hashes `595a83992d91b9587ea14d2fbeeb0bfe1e3d59ad458ccac833bbd98553a6be7e` for this Board and `a0f720f805804129ef12fb5d7c3aeb4741a20eb9a47585e32aee67c45ac9209d` for the diversity matrix, and duplicate-searching Board/matrix/claims for `180537`, `180446`, `BNBUSDT/LINKUSDT`, `bybit_bnb_link_nr7`, `NR7 range-expansion`, and `drop_gate1_cost_or_density_failed`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T180537+0800-codex-bybit-bnb-link-nr7-range-expansion-1m-full-ladder-v1/`.
- Metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_bnb_link_nr7_range_expansion_1m_full_ladder_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T180446+0800-codex-bybit-bnb-link-nr7-range-expansion-1m-full-ladder-v1.claim`.
- Branch path remained exact: `VolatilityCompression -> NR7RangeExpansion -> bybit_bnb_link_nr7_range_expansion_1m_full_ladder_v1`. Labels/provenance prefix: `CryptoLinearPerp`.
- Provider evidence: real Bybit public `linear` rows were acquired for `BNBUSDT` and `LINKUSDT` across `1m/5m/15m/30m/1h/4h/1d` (`provider_count=14`); intraday and `4h` validation exited `0`; both `1d` validation probes exited `1`.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and rank all exited `0`; rank rows `14`; total trades `30`; branch fields were preserved.
- Cost verdict: no exact `1m` origin row survived density/cost. `BNBUSDT/1m` had `1` trade, raw `-0.09%`, `2bps=-0.13%`; `LINKUSDT/1m` had `1` trade, raw `-0.05%`, `2bps=-0.09%`. Positive higher-timeframe rows were sparse context only: `BNBUSDT/5m` `3` trades with `2bps=+0.45%`, `BNBUSDT/30m` `5` trades with `2bps=+0.81%`, `BNBUSDT/1h` `4` trades with `2bps=+0.90%`, and `LINKUSDT/30m` `3` trades with `2bps=+0.91%`.
- Verification evidence: `python3 -m json.tool` passed for `checks/terminal_metrics.json` and `checks/cost_stress.json`; `python3 -m py_compile` passed for the wrapper.
- Terminal decision: `drop_gate1_cost_or_density_failed`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is a clean real-provider/AQ negative for the exact Bybit BNB/LINK `1m` NR7 range-expansion root. Do not downstream or retune this sparse `1m` branch. If the higher-timeframe context is reused, it must become a separate exact `30m` or `1h` lane with its own provider/AQ/cost/downstream decision; otherwise rotate to a materially denser `1m` family.

## Bybit DOGEUSDT 15m Stop-Run Reclaim Exact Lane: Cost Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 18:15:34 +0800` after observing pre-write hashes `12e21eef05a230ff79341ac2d8b5a7f96c8e147c089056ce7666641c886dfbcf` for the compact Board, `a0f720f805804129ef12fb5d7c3aeb4741a20eb9a47585e32aee67c45ac9209d` for the diversity matrix, and `6c8e43a011affcc9d006a8c2ef4e776a0511c64adac1dad16a4b7488b2745f13` for this Board. Duplicate search found no existing `181041` / `181257` / `bybit_dogeusdt_stoprun_reclaim_15m_exact` terminal row.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T181041+0800-codex-bybit-dogeusdt-15m-stoprun-reclaim-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T181041+0800-codex-bybit-dogeusdt-15m-stoprun-reclaim-exact-gate1-v1/downstream-exact-dogeusdt-15m-stoprun-reclaim-20260520T181257+0800/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; downstream metrics: `checks/downstream_metrics.json`; summaries: `summaries/terminal_decision_summary.md`.
- Branch path remained exact: `RangeReversion -> BybitMemecoinLiquiditySweep -> stoprun_reclaim_15m_exact -> bybit_dogeusdt_stoprun_reclaim_15m_exact_v1`.
- Provider/AQ evidence: real Bybit public `linear` fetch acquired `1000` `DOGEUSDT/15m` rows from `2026-02-17` to `2026-05-17`; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Cost verdict: exact `15m` lane survived Gate 1 cost stress with `7` trades, raw `+1.94%`, `1bps/side=+1.80%`, `2bps/side=+1.66%`, and `5bps/side=+1.24%`.
- Downstream verdict: Auto-Quant import/prior, analyze, workflow/pre-bayes/policy readbacks, CatBoost train/apply/register/runtime, and final analyze/readbacks all exited `0`, but admission failed closed: execution candidate `no_trade`, execution tree `observe` on `transition_guardrail`, `execution_readiness=0.54213634699414`, `transition_hazard=1.0`, `pda_hybrid_alignment=false`, path-ranker score visible but not used by execution tree, `ranker_validation_ready=false`, and policy mature/history mature rows `0/0`.
- Terminal decision: `exact_15m_gate1_survivor_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This exact `DOGEUSDT/15m` restart validates the prior higher-timeframe context as a real cost-surviving lane, but it is still not trade-usable. Treat it as repair-only evidence: next work must reduce transition hazard, align PDA/hybrid evidence, and mature same-root validation/execution-candidate materialization. Do not flatten this into the failed DOGE/1000PEPE `1m` root and do not promote from cost survival alone.

## Bybit AVAX/AAVE/UNI/SAND 1m Connors RSI2 Rebound Full-Ladder Gate 1: Origin Blocked

Append-only terminal row at `2026-05-20 18:22:28 +0800` after observing pre-write hashes `fc5cd676dff45674961c43995533c5e98a66d2eb2e0ce45a867edcbbe4478ad6` for the compact Board, `25da578bb0a67e7aa8f934f7b72ff583a2cb65f49c9d4ce6182ee95192ccf8fe` for the diversity matrix, and `154f444ed362a732357b3c31acc1d9a29e107e3a4f6f2a02a537658cb7fcbb94` for this Board. Duplicate search showed the diversity matrix already had a Connors addendum, but this Board and the compact current Board lacked the terminal row.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T181527+0800-codex-bybit-avax-aave-uni-sand-connors-rsi2-rebound-1m-full-ladder-v1/`.
- Metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_avax_aave_uni_sand_connors_rsi2_rebound_1m_full_ladder_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T181408+0800-codex-bybit-avax-aave-uni-sand-connors-rsi2-rebound-1m-full-ladder-v1.claim`.
- Branch path remained exact: `RangeReversion -> ConnorsRsi2Rebound -> bybit_avax_aave_uni_sand_connors_rsi2_rebound_1m_full_ladder_v1`.
- Provider evidence: real Bybit public `linear` rows were acquired for `AVAXUSDT`, `AAVEUSDT`, `UNIUSDT`, and `SANDUSDT` across `1m/5m/15m/30m/1h/4h/1d` (`provider_count=28`); intraday and `4h` validation exited `0`; `1d` validation probes exited `1` on short daily slices.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and rank all exited `0`; rank rows `28`; total trades `128`; branch fields were preserved.
- Cost verdict: no exact `1m` origin survivor met density/cost gates. `SANDUSDT/1m` had `2` trades, raw `+0.08%`, `2bps=+0.00%`; `AAVEUSDT/1m` had `3` trades, raw `+0.17%`, `2bps=+0.05%`; `AVAXUSDT/1m` had `4` trades, raw `+0.13%`, `2bps=-0.03%`; `UNIUSDT/1m` had `3` trades, raw `+0.20%`, `2bps=+0.08%`. Stronger higher-timeframe rows were context only: `SANDUSDT/30m` had `10` trades and `5bps=+4.84%`; `AAVEUSDT/30m` had `7` trades and `5bps=+3.05%`; `AVAXUSDT/30m` had `6` trades and `5bps=+1.85%`; `UNIUSDT/30m` had `6` trades and `5bps=+1.10%`.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is a real-provider/AQ Gate 1 attempt on a public Connors RSI2 crypto rebound family. It produced useful `30m` context, but the exact `1m` origin is too sparse/thin for downstream. If the `30m` edge is reused, it must become a separate exact rooted timeframe lane with its own provider/AQ/cost/downstream decision.

## Bybit LTC/BCH 1m PSAR Trend-Flip Full-Ladder Gate 1: Origin Blocked

Append-only terminal row at `2026-05-20 18:18 +0800` after observing pre-write hashes `b08e3b71e1aa349df1732ea25c44338939c13f5006d2537722ae75644572d3fc` for this Board and `4f1f8c82208ad65d928a4ec87240622c045144987d3d8478154c92a657be1346` for the diversity matrix, and duplicate-searching Board/matrix/claims for `181611`, `ltc-bch-psar`, `LTCUSDT/BCHUSDT`, and `PsarTrendFlip`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T181611+0800-codex-bybit-ltc-bch-psar-trend-flip-1m-full-ladder-v1/`.
- Metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_ltc_bch_psar_trend_flip_1m_full_ladder_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T181426+0800-codex-bybit-ltc-bch-psar-trend-flip-1m-full-ladder-v1.claim`.
- Branch path remained exact: `TrendExpansion -> PsarTrendFlip -> bybit_ltc_bch_psar_trend_flip_1m_full_ladder_v1`. Labels/provenance prefix: `CryptoLinearPerp`.
- Provider evidence: real Bybit public `linear` rows were acquired for `LTCUSDT` and `BCHUSDT` across `1m/5m/15m/30m/1h/4h/1d` (`provider_count=14`); intraday and `4h` validation exited `0`; both `1d` validation probes exited `1`.
- Auto-Quant evidence: strategy compile, material batch, dispatch, and rank all exited `0`; rank rows `14`; total trades `138`; branch fields were preserved.
- Cost verdict: no exact `1m` origin survivor met the density/cost gate. `LTCUSDT/1m` had `17` trades, raw `-0.18%`, `2bps=-0.86%`, `5bps=-1.88%`; `BCHUSDT/1m` had `10` trades, raw `-0.07%`, `2bps=-0.47%`, `5bps=-1.07%`. Context-only non-origin row: `BCHUSDT/1h` had `11` trades, raw `+2.35%`, `2bps=+1.91%`, `5bps=+1.25%`.
- Verification evidence: `python3 -m json.tool` passed for `checks/terminal_metrics.json` and `checks/cost_stress.json`; `python3 -m py_compile` passed for the wrapper.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is a clean real-provider/AQ Gate 1 attempt on a new Bybit large-cap PSAR trend-flip cell. It produced a useful `BCHUSDT/1h` context survivor, but the exact `1m` origin was raw-negative after costs and must not be sent downstream. If the `1h` context is reused, it needs a separate exact `BCHUSDT/1h` lane with its own provider/AQ/cost/downstream decision; otherwise rotate to a denser `1m` family with wider per-trade excursion.

## M2K 1m RVOL/PDA Consistency-Floor Simulated Admission Rerun: Mechanics Clean, Gates Still Fail

Append-only terminal row at `2026-05-20 18:34:36 +0800` after observing pre-write hashes `e37e5826c67c6f04e360bd55596a616dbf5acf4e1598af430352ffdd1a329e3b` for this Board and `5e1ae88417d0a8dbebfebfd4db4c6e7b29df45aea58422be8d51ea276f86cd8c` for the diversity matrix, checking active processes, and duplicate-searching Board/matrix for `182204`, `rvol_pda_guard_simulated_trade_admission_fail_closed`, and `simulated-trade-admission-m2k-1m-rvol-pda-consistency-floor`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T093302+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-7d-gate1-v1/simulated-trade-admission-m2k-1m-rvol-pda-consistency-floor-20260520T182204+0800/`.
- Metrics: `checks/simulated_trade_admission_metrics.json`; summary: `summaries/terminal_decision_summary.md`.
- Branch path remained exact: `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`. Labels/provenance prefix: `FUTURES -> equity_index -> M2K -> 1m`.
- Command evidence: all `19/19` commands exited `0`, from AQ import/prior through seed readbacks, simulated-trade ingest, CatBoost train/apply/register, runtime enable, post-ranker analyze, workflow, Pre-Bayes, and policy readbacks.
- Feedback evidence: same-Auto-Quant-workspace simulated trades ingested `17` rows (`11` wins / `6` losses / `0` breakevens); exact branch survived.
- Admission blocker state: `mature_rows=2`, `history_mature_rows=18`, `ranker_validation_ready=false`, execution candidate `no_trade`, `execution_readiness=0.3211044072278747`, `transition_hazard=0.9184975817511946`, `pda_hybrid_alignment=false`, path-ranker visible/used=`true/false`.
- Verification evidence: `python3 -m json.tool` passed for `checks/simulated_trade_admission_metrics.json`; `python3 -m py_compile` passed for `run_ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_consistency_floor_simulated_trade_admission_v1.py`.
- Terminal decision: `rvol_pda_guard_simulated_trade_admission_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This rerun proves the same-root mechanics are clean, but it does not move the branch toward the user's hard execution gates. It is still observation/repair evidence only. Do not repeat simulation-only admission reruns for this M2K root; the next useful work needs real/current matched feedback density plus direct PDA/hybrid alignment and transition-hazard repair, or a different exact root.

## Bybit BCHUSDT 1h PSAR Trend-Flip Exact Lane: Cost Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 18:34 +0800` after observing pre-write hashes `5da8b86f83e3b3cfd0e4e7e7e6e7cf35b0a4782d29dc5bff3368b58641fbab35` for this Board and `25da578bb0a67e7aa8f934f7b72ff583a2cb65f49c9d4ce6182ee95192ccf8fe` for the diversity matrix, and duplicate-searching Board/matrix/claims for `182906`, `183032`, `BCHUSDT 1h PSAR`, and `bybit_bchusdt_psar_trend_flip_1h_exact`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T182906+0800-codex-bybit-bchusdt-1h-psar-trend-flip-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T182906+0800-codex-bybit-bchusdt-1h-psar-trend-flip-exact-gate1-v1/downstream-exact-bchusdt-1h-psar-trend-flip-20260520T183032+0800/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress_table.json`; downstream metrics: `checks/downstream_metrics.json`; summaries: `summaries/terminal_decision_summary.md`.
- Wrappers: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_bchusdt_1h_psar_trend_flip_exact_gate1_v1.py` and `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_bchusdt_1h_psar_trend_flip_exact_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T182439+0800-codex-bybit-bchusdt-1h-psar-trend-flip-exact-v1.claim`.
- Branch path remained exact: `TrendExpansion -> PsarTrendFlip -> psar_trend_flip_1h_exact -> bybit_bchusdt_psar_trend_flip_1h_exact_v1`. Labels/provenance prefix: `CryptoLinearPerp`.
- Gate 1 evidence: real Bybit public `linear` fetch acquired `1000` `BCHUSDT/1h` rows from `2026-02-17` to `2026-05-17`; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Cost verdict: exact `1h` lane survived Gate 1 cost stress with `11` trades, raw `+2.35%`, `1bps/side=+2.13%`, `2bps/side=+1.91%`, and `5bps/side=+1.25%`.
- Downstream verdict: Auto-Quant import/prior, analyze, workflow/pre-bayes/policy readbacks, CatBoost train/apply/register/runtime, and final analyze/readbacks all exited `0`. Admission still failed closed: execution candidate `no_trade`, execution tree `observe` on `transition_guardrail`, `execution_readiness=0.33869022220381295`, `transition_hazard=0.975842157596383`, `pda_hybrid_alignment=false`, path-ranker score visible but not used by execution tree, and `ranker_validation_ready=false`.
- Verification evidence: `python3 -m json.tool` passed for Gate 1 metrics, cost stress, and downstream metrics JSON; `python3 -m py_compile` passed for both wrappers.
- Terminal decision: `exact_1h_gate1_survivor_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This independent `BCHUSDT/1h` exact restart confirms the higher-timeframe PSAR context row was a real cost-surviving lane, but it still fails the hard execution predicates by a wide margin. Treat it as observation/repair-only evidence. The next useful work must directly improve PDA/hybrid alignment, transition hazard, ranker validation/use, and exact execution-candidate materialization; do not promote this branch from cost survival alone and do not use it as proof for the failed `1m` root.

## Bybit AAVEUSDT 30m Connors RSI2 Exact Lane: Cost Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 18:42 +0800` after observing pre-write hashes `be392dd20b6a533136613961c2328c5f9fab8af9e5438c1e020b83d93d3ed60f` for this Board, `f399ae00cf5703e3362db3c18e2c3b013bfbed0878c8fad57afed02681def3e6` for the compact Board, and `9978c02b66028153155407991bd860c4bcdf145d9ddd2c25360f023a983c1dc4` for the diversity matrix. A prior prefixed-path scratch root `20260520T183648` was superseded because the current contract requires regime-rooted factor paths without market labels.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T184018+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T184018+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1/downstream-exact-aaveusdt-30m-connors-rsi2-rebound-20260520T184127+0800/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress_table.json`; downstream metrics: `checks/downstream_metrics.json`; summaries: `summaries/terminal_decision_summary.md`.
- Wrappers: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_aaveusdt_30m_connors_rsi2_rebound_exact_gate1_v1.py` and `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_aaveusdt_30m_connors_rsi2_rebound_exact_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T184000+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1.claim`.
- Canonical branch path remained exact: `RangeReversion -> ConnorsRsi2Rebound30mExact -> bybit_aaveusdt_connors_rsi2_rebound_30m_exact_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbol=AAVEUSDT`, `timeframe=30m`.
- Gate 1 evidence: real Bybit public `linear` fetch acquired `1000` `AAVEUSDT/30m` rows from `2026-02-17` to `2026-05-17`; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Cost verdict: exact `30m` lane survived Gate 1 cost stress with `7` trades, raw `+3.75%`, `1bps/side=+3.61%`, `2bps/side=+3.47%`, and `5bps/side=+3.05%`.
- Downstream verdict: AQ import/prior, analyze, workflow/pre-bayes/policy readbacks, CatBoost train/apply/register/runtime, and final analyze/readbacks all exited `0`. Admission still failed closed: execution candidate `no_trade`, execution tree `observe` on `transition_guardrail`, `execution_readiness=0.3122334871360051`, `transition_hazard=0.6351980946948512`, `pda_hybrid_alignment=true`, path-ranker score visible but not used by execution tree, `ranker_validation_ready=false`, and policy mature/history mature rows `0/0`.
- Verification evidence: `python3 -m json.tool` passed for Gate 1 metrics, cost stress, and downstream metrics JSON; `python3 -m py_compile` passed for both wrappers.
- Terminal decision: `exact_30m_gate1_survivor_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This independent `AAVEUSDT/30m` exact restart confirms the higher-timeframe Connors context was real cost-surviving evidence, but it is still observation/repair only. The next useful work must reduce transition hazard below `0.60`, raise execution readiness to `>=0.65`, mature validation rows, and make the visible path-ranker score actionable in the execution tree. Do not promote from cost survival alone and do not use this as proof for the failed `1m` origin.

## Superseded Scratch: Bybit AAVEUSDT 30m Connors RSI2 Prefixed-Path Replay

Superseded by canonical terminal row `20260520T184018/20260520T184127` above. This earlier replay used the older market-prefixed branch path and is provenance only, not the authoritative terminal row.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T183648+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T183648+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1/downstream-exact-aaveusdt-30m-connors-rsi2-rebound-20260520T183758+0800/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress_table.json`; downstream metrics: `checks/downstream_metrics.json`; summaries: `summaries/terminal_decision_summary.md`.
- Wrappers: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_aaveusdt_30m_connors_rsi2_rebound_exact_gate1_v1.py` and `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_aaveusdt_30m_connors_rsi2_rebound_exact_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T184000+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1.claim`.
- Branch path remained exact: `RangeReversion -> ConnorsRsi2Rebound30mExact -> bybit_aaveusdt_connors_rsi2_rebound_30m_exact_v1`.
- Gate 1 evidence: real Bybit public `linear` fetch acquired `1000` `AAVEUSDT/30m` rows from `2026-02-17` to `2026-05-17`; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Cost verdict: exact `30m` lane survived Gate 1 cost stress with `7` trades, raw `+3.75%`, `1bps/side=+3.61%`, `2bps/side=+3.47%`, and `5bps/side=+3.05%`.
- Downstream verdict: Auto-Quant import/prior, analyze, workflow/pre-bayes/policy readbacks, CatBoost train/apply/register/runtime, and final analyze/readbacks all exited `0`. Admission still failed closed: execution candidate `no_trade`, execution tree `observe` on `transition_guardrail`, `execution_readiness=0.3122334871360051`, `transition_hazard=0.6351980946948512`, `pda_hybrid_alignment=true`, path-ranker score visible but not used by execution tree, and `ranker_validation_ready=false`.
- Verification evidence: `python3 -m json.tool` passed for Gate 1 metrics, cost stress, and downstream metrics JSON; `python3 -m py_compile` passed for both wrappers.
- Superseded decision: provenance-only; authoritative decision is the canonical `exact_30m_gate1_survivor_downstream_fail_closed` row above.

Interpretation:

- This independent `AAVEUSDT/30m` exact restart confirms another Connors RSI2 higher-timeframe context row was a real cost-surviving lane, but it still misses the hard execution predicates. PDA/hybrid alignment is true, unlike SAND/BCH, but transition hazard remains above `0.60`, readiness is far below `0.65`, validation is not mature, and the visible path-ranker score is not consumed by the execution tree. Treat it as repair-only evidence, not a practical factor or proof for the failed `1m` origin.

## Bybit AVAXUSDT 30m Connors RSI2 Exact Lane: Cost Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 18:48:05 +0800` after observing pre-write hashes `a230c693da2ea4f848d257344077c42f8b1e9aa2340d1e28d70162ee79759e1e` for this Board and `9689489cf2caa46d52328ef23240849e97dc628271219ac057618b70f5363e21` for the diversity matrix, and duplicate-searching Board/matrix for `AVAXUSDT 30m Connors`, `avaxusdt_30m_connors`, `20260520T184515`, and `20260520T184552`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T184515+0800-codex-bybit-avaxusdt-30m-connors-rsi2-rebound-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T184515+0800-codex-bybit-avaxusdt-30m-connors-rsi2-rebound-exact-gate1-v1/downstream-exact-avaxusdt-30m-connors-rsi2-rebound-20260520T184552+0800/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress_table.json`; downstream metrics: `checks/downstream_metrics.json`; summaries: `summaries/terminal_decision_summary.md`.
- Wrappers: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_avaxusdt_30m_connors_rsi2_rebound_exact_gate1_v1.py` and `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_avaxusdt_30m_connors_rsi2_rebound_exact_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T184311+0800-codex-bybit-avaxusdt-30m-connors-rsi2-rebound-exact-gate1-v1.claim`.
- Canonical branch path remained exact: `RangeReversion -> ConnorsRsi2Rebound30mExact -> bybit_avaxusdt_connors_rsi2_rebound_30m_exact_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbol=AVAXUSDT`, `timeframe=30m`.
- Gate 1 evidence: real Bybit public `linear` fetch acquired `1000` `AVAXUSDT/30m` rows; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Cost verdict: exact `30m` lane survived Gate 1 cost stress with `6` trades, raw `+2.45%`, `1bps/side=+2.33%`, `2bps/side=+2.21%`, and `5bps/side=+1.85%`.
- Downstream verdict: AQ import/prior, analyze, workflow/pre-bayes/policy readbacks, CatBoost train/apply/register/runtime, and final analyze/readbacks all exited `0`. Admission still failed closed: execution candidate `no_trade`, execution tree `observe` on `transition_guardrail`, `execution_readiness=0.44486982675234005`, `transition_hazard=0.9823474605286929`, `pda_hybrid_alignment=false`, path-ranker score visible but not used by execution tree, `ranker_validation_ready=false`, and policy mature/history mature rows `0/0`.
- Verification evidence: `python3 -m json.tool` passed for Gate 1 metrics, cost stress, and downstream metrics JSON; `python3 -m py_compile` passed for both wrappers; every Gate 1 and downstream `.exit` file was `0`.
- Terminal decision: `exact_30m_gate1_survivor_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This independent `AVAXUSDT/30m` exact restart confirms another Connors RSI2 higher-timeframe context row as a real cost-surviving lane, but it is weaker than AAVE/SAND for admission repair: PDA alignment is false and transition hazard is far above `0.60`. Treat it as observation/repair-only evidence. Do not promote from cost survival alone, and do not use it as proof for the failed `1m` origin.

## Bybit NEAR/APT 1m-Origin BBWidth Pullback Reclaim Full Ladder: Gate 1 Negative

Append-only terminal row at `2026-05-20 current continuation` after observing pre-write hashes `3f67007efd5418d238671fdd285860a445e614e3650978ce8e75da1ce0410069` for this Board and `2fc376cf701ce4efb35d919cb0811405cff2dbb28a017639de1b386f59b45121` for the diversity matrix, and duplicate-searching Board/matrix/claims for `185031`, `NEAR`, `APT`, `BBWidth`, `bbwidth`, and `bybit_near_apt_bbwidth`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T185031+0800-codex-bybit-near-apt-bbwidth-pullback-reclaim-1m-full-ladder-v1/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_crypto_volatility_pullback_reclaim_gate1_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T184937+0800-codex-bybit-near-apt-bbwidth-pullback-reclaim-1m-full-ladder-v1.claim`.
- Branch path remained regime-rooted: `Transition -> CryptoVolatilityCompression -> bbwidth_pullback_reclaim_1m_full_ladder -> bybit_near_apt_bbwidth_pullback_reclaim_1m_full_ladder_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbols=NEARUSDT/APTUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetch acquired real rows for both symbols across the requested ladder from `2026-02-17` to `2026-05-17`; fetch exits were `0`; validate-market-state passed for `1m/5m/15m/30m/1h/4h`; `1d` validation failed only on insufficient candles (`90 < 100` for both symbols); strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`.
- Exact `1m` origin cost verdict: failed Gate 1 real-cost density. `NEARUSDT/1m` had `5` trades, raw `-0.09%`, `2bps/side=-0.29%`, `5bps/side=-0.59%`; `APTUSDT/1m` had `6` trades, raw `+0.21%`, `2bps/side=-0.03%`, `5bps/side=-0.39%`.
- Higher-timeframe context only: `NEARUSDT/30m` had `3` trades and `5bps/side=+2.70%`; `APTUSDT/30m` had `4` trades and `5bps/side=+2.24%`; `APTUSDT/1h` had `2` trades and `5bps/side=+1.79%`. These are not proof for the failed `1m` origin and were not downstreamed.
- Verification evidence: `python3 -m json.tool` passed for Gate 1 metrics and cost stress JSON; `python3 -m py_compile` passed for the wrapper; `.exit` readback confirmed provider/AQ mechanics above.
- Terminal decision: `drop_gate1_negative_or_zero_density`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This Bybit NEAR/APT BBWidth pullback-reclaim lane is a clean real-provider/AQ Gate 1 negative for the exact `1m` origin. Keep the sparse `30m/1h` positives as context only; do not promote, downstream, or repeat this exact branch unless a new hypothesis directly changes origin density and cost survival.

## AAVEUSDT 30m Connors Canonical Root Blocker Report

Append-only terminal row at `2026-05-20 18:54:10 +0800` after verifying the blocker report JSON and summary, confirming the diversity matrix already has the matching addendum, and duplicate-searching this Board/matrix for `AAVEUSDT 30m Connors Canonical Root Blocker`, `regime_root_survivor_blocker_report`, `repair_execution_gate_status_before_ranker_consumption`, and `pre_bayes_conflict`.

- Canonical Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T184018+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1/`.
- Blocker report JSON: `checks/regime_root_survivor_blocker_report.json`.
- Blocker report summary: `summaries/regime_root_survivor_blocker_report.md`.
- Canonical branch path remained exact: `RangeReversion -> ConnorsRsi2Rebound30mExact -> bybit_aaveusdt_connors_rsi2_rebound_30m_exact_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbol=AAVEUSDT`, `timeframe=30m`.
- Verified blocker classification: `repair_execution_gate_status_before_ranker_consumption`.
- Gate 1 remains a real cost survivor (`7` trades, `5bps/side=+3.05%`) and downstream remains fail-closed: execution candidate `no_trade`, `execution_readiness=0.3122334871360051`, `transition_hazard=0.6351980946948512`, `pda_hybrid_alignment=true`, `ranker_validation_ready=false`, and path-ranker visible but not used by the execution tree.
- Blockers: `execution_candidate_no_trade`, `execution_readiness_below_0_65`, `transition_hazard_ge_0_60`, `ranker_validation_not_ready`, `path_ranker_visible_but_not_used`, `pre_bayes_conflict:hostile_liquidity_requires_high_uncertainty`, and `pre_bayes_conflict:low_directional_separation`.
- Verification evidence: `python3 -m json.tool checks/regime_root_survivor_blocker_report.json` passed, and `python3 -m unittest support/scripts/research/tests/test_regime_factor_tree_normalizer.py` passed `5/5`.
- Terminal status stays unchanged: `promotion_allowed=false`, `trade_usable=false`, `update_goal=false`.

Interpretation:

- Keep `AAVEUSDT/30m` Connors as a repair candidate only. It is the nearest current Connors repair lead because PDA alignment is already true and transition hazard is closest to the hard `0.60` gate, but it is not practical until exact execution-candidate materialization, validation maturity, transition hazard, ranker consumption, and readiness gates pass together. Do not repeat market-prefixed reruns or promote from cost survival alone.

## Bybit Major Crypto 1m-Origin NR7 Range Expansion Full Ladder: Gate 1 Negative

Append-only terminal row at `2026-05-20 current continuation` after observing pre-write hashes `4c6ba6345e9a9d69f304c665be12141fa69395be709bdbde608a480e07efd95d` for this Board and `b1b4e96725c94bb822177433eb8fc0fcd034e521b4c028a2465068a8ae79791a` for the diversity matrix, and duplicate-searching Board/matrix/claims for `185719`, `185802`, `bybit_major_nr7`, `BTCUSDT/ETHUSDT/SOLUSDT/XRPUSDT`, `Major NR7`, and `major-nr7`.

- Canonical Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T185802+0800-codex-bybit-major-nr7-range-expansion-1m-full-ladder-v1/`.
- Superseded duplicate/provenance root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T185719+0800-codex-bybit-major-nr7-range-expansion-1m-full-ladder-v1/`; it reached the same `drop_gate1_cost_or_density_failed` decision and is not the authoritative row.
- Gate 1 metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_major_nr7_range_expansion_1m_full_ladder_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T185559+0800-codex-bybit-major-nr7-range-expansion-1m-full-ladder-v1.claim`.
- Branch path remained regime-rooted: `VolatilityCompression -> NR7RangeExpansion -> nr7_range_expansion_1m_full_ladder -> bybit_major_nr7_range_expansion_1m_full_ladder_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbols=BTCUSDT/ETHUSDT/SOLUSDT/XRPUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for `28` symbol/timeframe cells from `2026-02-17` to `2026-05-17`; intraday and `4h` validations exited `0`; all four `1d` validations exited `1` only on insufficient daily candles (`90 < 100`); strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; rank produced `28` rows, `36` total trades, and preserved rooted branch fields.
- Exact `1m` origin cost verdict: failed Gate 1 density. `BTCUSDT/1m`, `ETHUSDT/1m`, `SOLUSDT/1m`, and `XRPUSDT/1m` each produced `0` trades, so there were no `2bps/side` or `5bps/side` cost survivors.
- Higher-timeframe context only: best sparse positives included `SOLUSDT/30m` `4` trades with `5bps/side=+2.29%`, `XRPUSDT/1h` `2` trades with `5bps/side=+1.14%`, `BTCUSDT/30m` `4` trades with `5bps/side=+0.57%`, and `ETHUSDT/1h` `1` trade with `5bps/side=+0.54%`. These are not proof for the failed `1m` origin and were not downstreamed.
- Verification evidence: `python3 -m json.tool` passed for both canonical and superseded metrics/cost JSON; `python3 -m py_compile` passed for the wrapper; `.exit` readback confirmed provider/AQ mechanics above; no active major-NR7/AQ process remained after completion.
- Terminal decision: `drop_gate1_cost_or_density_failed`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This claimed Bybit major-crypto NR7 range-expansion lane is a clean real-provider/AQ Gate 1 negative for the exact `1m` origin. Do not run Pre-Bayes/BBN/CatBoost/execution-tree for this packet. If the sparse `30m/1h` positives are reused, they must be opened as separate exact timeframe lanes with their own claims, cost-density tests, and downstream decisions.

## AAVEUSDT 30m Connors Rvol/Washout Guard: Cost Survivor, Same Downstream Blocker

Append-only terminal row at `2026-05-20 18:59:29 +0800` after repairing the read-only AAVE signal diagnostic schema (`timestamp`, not `date`), adding a same-root rvol/washout guard wrapper, and running full Gate 1 plus downstream replay.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T185656+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-rvol-washout-guard-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T185656+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-rvol-washout-guard-gate1-v1/downstream-rvol-washout-guard-aaveusdt-30m-connors-rsi2-rebound-20260520T185748+0800/`.
- Wrappers: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_aaveusdt_30m_connors_rsi2_rebound_rvol_washout_guard_gate1_v1.py` and `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_aaveusdt_30m_connors_rsi2_rebound_rvol_washout_guard_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T185656+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-rvol-washout-guard-v1.claim`.
- Canonical branch path remained regime-rooted: `RangeReversion -> ConnorsRsi2Rebound30mExact -> bybit_aaveusdt_connors_rsi2_rebound_30m_exact_v1 -> bybit_aaveusdt_connors_rsi2_rebound_rvol_washout_guard_30m_v1`. Labels remain `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbol=AAVEUSDT`, `timeframe=30m`.
- Guard tested: raise participation floor to `rvol96 >= 0.79` and washout floor to `washout_depth_atr >= -0.09`, preserving `6` of the original `7` diagnostic signals.
- Gate 1 evidence: real Bybit public `linear` fetch acquired `1000` `AAVEUSDT/30m` rows; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; branch fields were preserved.
- Cost verdict: the overlay survived Gate 1 cost stress with `6` trades, raw `+4.08%`, `1bps/side=+3.96%`, `2bps/side=+3.84%`, and `5bps/side=+3.48%`.
- Downstream verdict: all `15` command exits were `0`, but admission stayed fail-closed with the same blocker trio as the unguarded root: execution candidate `no_trade`, execution tree `observe` on `transition_guardrail`, `execution_readiness=0.3122334871360051`, `transition_hazard=0.6351980946948512`, `pda_hybrid_alignment=true`, path-ranker visible/used=`true/false`, `ranker_validation_ready=false`, and policy mature/history mature rows `0/0`.
- Verification evidence: `python3 -m py_compile` passed for both new wrappers; every Gate 1 and downstream `.exit` file was `0`; `git diff --check` passed for the new wrappers.
- Terminal decision: `same_root_overlay_cost_survivor_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- The rvol/washout guard improved Gate 1 economics but did not repair transition hazard, readiness, ranker consumption, or execution-candidate materialization. Stop stacking lightweight same-timeframe Connors filters on this AAVE `30m` root unless the next hypothesis directly changes execution admission or mature validation; otherwise rotate to a different root/family.

## AAVEUSDT 30m Connors Auto-Quant Autoresearch Seed: Synthetic Incubation Only

Append-only terminal row at `2026-05-20 19:04:32 +0800` after observing pre-write hashes `c8ad423bc28cba5edb540ab8ba5e93a780d416aebb3d8fc26ff59666718b7bff` for this Board and `56bc24f670212da3e3cf7dac64211f06fb52aba17def7b8ff4067f886dd3d824` for the diversity matrix, and duplicate-searching Board/matrix for `190034`, `AAVEUSDT 30m Connors Auto-Quant Autoresearch Seed`, `seed_positive_synthetic_incubate_real_replay_required`, and `TomacBybitaaveusdt30mconnorsrsi2reboundexactv1Seed`.

- Seed run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T190034+0800-codex-bybit-aaveusdt-30m-connors-autoresearch-seed-v1/`.
- Metrics: `checks/terminal_metrics.json`.
- Summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_aaveusdt_30m_connors_autoresearch_seed_v1.py`.
- Source exact root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T184018+0800-codex-bybit-aaveusdt-30m-connors-rsi2-rebound-exact-gate1-v1/`.
- Branch path remained regime-rooted but is a seed child only: `RangeReversion -> ConnorsRsi2Rebound30mExact -> bybit_aaveusdt_connors_rsi2_rebound_30m_exact_v1 -> autoquant_autoresearch_seed_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbol=AAVEUSDT`, `timeframe=30m`.
- Normalization evidence: reused the exact AAVE provider CSV and produced `1000` clean rows from `2026-04-26 04:30:00+00:00` to `2026-05-17 00:00:00+00:00`.
- Control-plane evidence: `factor-autoresearch`, `auto-quant-prepare`, prepared `factor-autoresearch`, `factor-autoresearch-status`, and `run_tomac.py` all exited `0`; the status surface still reported `no_autoresearch_state`, so the result is seed/AQ execution evidence rather than a persisted autoresearch session.
- Auto-Quant seed metric: `TomacBybitaaveusdt30mconnorsrsi2reboundexactv1Seed` on synthetic-profile AQ data produced `7` trades, `+3.75%` total profit, `85.7143%` win rate, Sharpe `9.9435`, profit factor `13.0697`, and max drawdown `-0.3097%`.
- Verification evidence: `python3 -m json.tool checks/terminal_metrics.json` passed and all `.exit` files read back `0`.
- Terminal decision: `seed_positive_synthetic_incubate_real_replay_required`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This seed largely reproduces the known AAVE `30m` Connors economics under the synthetic Auto-Quant profile. It does not repair the canonical branch blockers: execution candidate still needs materialization, validation is not mature, transition hazard remains above `0.60`, and readiness remains below `0.65`. Do not promote this seed or downstream it as-is; only a fresh exact real-provider AQ material that changes those blockers is worth another downstream replay.

## Bybit TON/ONDO 1m-Origin Vortex Trend Continuation Full Ladder: HTF Context Only

Append-only terminal row at `2026-05-20 current continuation` after observing pre-write hashes `d35b1fae8e57e227cfcc97eae9cd8bd903e7252c059b6ba7209bdc28f9c20d3f` for this Board and `dc6710bb6bdb5dd6b0a6f8dfda2484afe141259a502ae1d1296d0fc30114e5d0` for the diversity matrix, and duplicate-searching Board/matrix for `185909`, `TONUSDT`, `ONDOUSDT`, `VortexTrendContinuation`, `bybit_ton_ondo_vortex`, and `TON/ONDO Vortex`.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T185909+0800-codex-bybit-ton-ondo-vortex-trend-continuation-1m-full-ladder-v1/`.
- Gate 1 metrics: `checks/terminal_metrics.json`; cost stress: `checks/cost_stress.json`; summary: `summaries/terminal_decision_summary.md`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_ton_ondo_vortex_trend_continuation_1m_full_ladder_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T185742+0800-codex-bybit-ton-ondo-vortex-trend-continuation-1m-full-ladder-v1.claim`.
- Branch path remained regime-rooted: `TrendExpansion -> CryptoVortexTrendContinuation -> vortex_trend_continuation_1m_full_ladder -> bybit_ton_ondo_vortex_trend_continuation_1m_full_ladder_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbols=TONUSDT/ONDOUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for `14` symbol/timeframe cells; intraday and `4h` validations exited `0`; both `1d` validations exited `1` only on insufficient daily candles (`90 < 100`); strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`; rank produced `14` rows, `154` total trades, and preserved rooted branch fields.
- Exact `1m` origin cost verdict: failed real-cost gate. `TONUSDT/1m` had `4` trades, raw `+0.13%`, `2bps/side=-0.03%`, and `5bps/side=-0.27%`; `ONDOUSDT/1m` had `13` trades, raw `+0.51%`, `1bps/side=+0.25%`, `2bps/side=-0.01%`, and `5bps/side=-0.79%`.
- Higher-timeframe context only: `TONUSDT/30m` had `24` trades, raw `+7.39%`, `2bps/side=+6.43%`, and `5bps/side=+4.99%`; `TONUSDT/15m` had `6` trades, raw `+0.73%`, `2bps/side=+0.49%`, and `5bps/side=+0.13%`. These are independent context candidates only, not proof for the failed `1m` origin.
- Verification evidence: `python3 -m json.tool` passed for `checks/terminal_metrics.json` and `checks/cost_stress.json`; `python3 -m py_compile` passed for the wrapper; `.exit` readback confirmed provider/AQ mechanics above.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This Vortex/VI lane found a plausible TONUSDT higher-timeframe context, but the requested `1m` origin did not survive costs. Do not run Pre-Bayes/BBN/CatBoost/execution-tree for this full-ladder packet. If carrying the idea forward, open an independent exact `TONUSDT/30m` lane with its own claim, provider/AQ/cost gate, and downstream admission proof.

## Bybit TONUSDT 30m Vortex Exact Lane: Cost Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 19:08:53 +0800` after duplicate-searching the Board/matrix/claims for `TONUSDT/30m`, `tonusdt_30m_vortex`, and `vortex_trend_continuation_30m`, then opening the higher-timeframe context row as an independent exact timeframe lane.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T190559+0800-codex-bybit-tonusdt-30m-vortex-trend-continuation-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T190559+0800-codex-bybit-tonusdt-30m-vortex-trend-continuation-exact-gate1-v1/downstream-exact-tonusdt-30m-vortex-trend-continuation-20260520T190743+0800/`.
- Wrappers: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_tonusdt_30m_vortex_trend_continuation_exact_gate1_v1.py` and `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_tonusdt_30m_vortex_trend_continuation_exact_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T190559+0800-codex-bybit-tonusdt-30m-vortex-trend-continuation-exact-v1.claim`.
- Canonical branch path remained regime-rooted: `TrendExpansion -> CryptoVortexTrendContinuation30mExact -> bybit_tonusdt_vortex_trend_continuation_30m_exact_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbol=TONUSDT`, `timeframe=30m`.
- Gate 1 evidence: real Bybit public `linear` fetch acquired `1000` `TONUSDT/30m` rows; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Cost verdict: exact `30m` lane survived Gate 1 cost stress with `24` trades, raw `+7.39%`, `1bps/side=+6.91%`, `2bps/side=+6.43%`, and `5bps/side=+4.99%`.
- Downstream verdict: Auto-Quant import/prior, analyze, workflow/pre-bayes/policy readbacks, CatBoost train/apply/register/runtime, and final analyze/readbacks all exited `0`. Admission still failed closed: execution candidate `no_trade`, execution tree `observe` on `transition_guardrail`, `execution_readiness=0.30321999798713817`, `transition_hazard=0.9715836816009797`, `pda_hybrid_alignment=false`, path-ranker score visible but not used by execution tree, `ranker_validation_ready=false`, and policy mature/history mature rows `0/0`.
- Verification evidence: `python3 -m py_compile` passed for both new wrappers; every Gate 1 and downstream `.exit` file was `0`.
- Terminal decision: `exact_30m_gate1_survivor_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This exact TONUSDT `30m` Vortex lane is a strong real-cost observation sample but not a practical factor. Cost density alone did not create an execution candidate, and the hard execution predicates are worse than the AAVE repair lead because PDA is false and transition hazard is far above `0.60`. Do not promote or repeat the same Vortex exact lane unless the next hypothesis directly repairs PDA/transition/readiness or mature validation.

## Canonical Correction: Bybit TONUSDT 30m Vortex Exact Root

Correction at `2026-05-20 19:19:38 +0800`: the earlier TONUSDT `30m` exact
rows (`190559/190743`, `191428/191501`, `191449`, and `191639`) preserve the
same cost/downstream verdict but used the older branch node
`CryptoVortexTrendContinuation`. Treat those rows as provenance only for the
canonical-tree contract.

- Authoritative/current canonical TONUSDT evidence is:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191808+0800-codex-bybit-tonusdt-30m-vortex-trend-continuation-exact-gate1-v1/`
  and downstream
  `downstream-exact-tonusdt-30m-vortex-trend-continuation-20260520T191828+0800/`.
- Its claim is
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T190550+0800-codex-bybit-tonusdt-30m-vortex-trend-continuation-exact-v1.claim`.
- Canonical branch path:
  `TrendExpansion -> CryptoVortexTrendContinuation30mExact -> bybit_tonusdt_vortex_trend_continuation_30m_exact_v1`. Labels remain separate:
  `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbol=TONUSDT`, `timeframe=30m`.
- Current terminal decision remains
  `canonical_exact_30m_gate1_survivor_downstream_fail_closed`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.
- Fresh canonical rerun evidence: Bybit public `linear` fetched `1000` `TONUSDT/30m`
  rows; Gate 1 stayed positive with `24` trades, raw `+7.39%`,
  `1bps/side=+6.91%`, `2bps/side=+6.43%`, and `5bps/side=+4.99%`.
  Downstream again completed all `15` commands with exit `0` but failed
  admission with execution candidate `no_trade`, execution tree `observe` on
  `transition_guardrail`, `execution_readiness=0.30321999798713817`,
  `transition_hazard=0.9715836816009797`, `pda_hybrid_alignment=false`,
  `ranker_validation_ready=false`, and path-ranker score not used by execution
  tree.
- Blocker report decision:
  `repair_same_root_pda_sequence_alignment`; key blockers include
  `pre_bayes_observe_only`, `pda_sequence_cluster_weak`,
  `pda_sequence_low_consistency`, `pda_hybrid_alignment_not_true`,
  `transition_hazard_ge_0_60`, and `execution_readiness_below_0_65`.
- Verification evidence for the current rerun: `python3 -m json.tool` passed for
  `checks/terminal_metrics.json`, `checks/downstream_metrics.json`, and
  `checks/regime_root_survivor_blocker_report.json`; every Gate 1 and downstream
  `.exit` file under the `191808/191828` roots is present for command readback.

Interpretation:

- This correction is documentation hygiene only. It does not promote TONUSDT
  `30m` Vortex and does not change the hard-gate verdict: the branch is
  observation/repair evidence until same-root PDA, transition hazard, execution
  candidate materialization, validation maturity, and readiness gates all pass.

## IBKR M2K 1m RVOL/PDA Consistency-Floor Canonical Repair: Cost Survivor, Simulated Admission Fail-Closed

Append-only terminal row at `2026-05-20 19:21:52 +0800` after polling the
previously running downstream wrapper and confirming no related
M2K/AQ/CatBoost process remained live.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191436+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-canonical-gate1-v1/`.
- Downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191436+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-canonical-gate1-v1/simulated-trade-admission-m2k-1m-rvol-pda-consistency-floor-canonical-20260520T191545+0800/`.
- Wrappers:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_rvol_pda_consistency_floor_canonical_gate1_v1.py` and
  `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_m2k1m_rvol_pda_consistency_floor_canonical_simulated_trade_admission_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T191436+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-canonical-v1.claim`.
- Canonical branch path stayed regime-rooted:
  `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`.
  Labels/provenance: `provider=IBKR`, `sec_type=FUT`, `symbol=M2K`,
  `contract=202606`, `timeframe=1m`, `duration=7 D`,
  `local_cache_replay=retained_real_ibkr_same_contract_replay`.
- Gate 1 evidence: retained real same-contract replay preserved branch fields
  and produced `17` shorts, `64.7059%` win rate, raw `+2.79%`,
  `1bps/side=+2.45%`, `2bps/side=+2.11%`, and `5bps/side=+1.09%`;
  `downstream_allowed=true`.
- Downstream evidence: Auto-Quant import/prior, seed analyze, workflow,
  Pre-Bayes, policy/export readbacks, simulated-trade ingest, CatBoost train,
  ranker apply/register/runtime, post-ranker analyze/workflow/Pre-Bayes/policy
  all exited `0` (`19` commands total). Same-workspace simulated feedback
  ingested `17` rows (`11` wins / `6` losses).
- Admission verdict: exact branch survived and path-ranker score was visible to
  the execution tree, but the execution candidate stayed `no_trade`;
  `execution_readiness=0.3211044072278747`,
  `transition_hazard=0.9184975817511946`,
  `pda_hybrid_alignment=false`, `ranker_validation_ready=false`,
  `mature_rows=2`, `history_mature_rows=18`, and path-ranker score was not used
  by the execution tree.
- Terminal decision:
  `rvol_pda_guard_simulated_trade_admission_fail_closed`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This canonical repair fixes the old label-prefixed branch-path issue and keeps
  the strongest M2K same-root survivor as evidence, but it is still not
  practical alpha. Simulated feedback improved readback/maturity only to a
  fail-closed state; the next same-root slice must directly materialize an exact
  execution candidate and repair PDA/transition/readiness instead of lowering
  gates or repeating the same simulated-admission probe.

## Bybit DOT/ATOM SuperTrend-ADX Full Ladder And DOTUSDT 30m Exact: Cost Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 19:22 +0800` after duplicate-searching
current Board/matrix/claims for `DOTUSDT`, `ATOMUSDT`, `SuperTrend-ADX`,
`dotusdt_30m_supertrend`, and `bybit_dotusdt_supertrend`.

- Full-ladder Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191450+0800-codex-bybit-dot-atom-supertrend-adx-1m-full-ladder-v1/`.
- Exact Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191831+0800-codex-bybit-dotusdt-30m-supertrend-adx-exact-gate1-v1/`.
- Exact downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191831+0800-codex-bybit-dotusdt-30m-supertrend-adx-exact-gate1-v1/downstream-exact-dotusdt-30m-supertrend-adx-20260520T191955+0800/`.
- Wrappers:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_dot_atom_supertrend_adx_1m_full_ladder_v1.py`,
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_dotusdt_30m_supertrend_adx_exact_gate1_v1.py`, and
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_dotusdt_30m_supertrend_adx_exact_downstream_v1.py`.
- Claims:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T191255+0800-codex-bybit-dot-atom-supertrend-adx-1m-full-ladder-v1.claim` and
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T191700+0800-codex-bybit-dotusdt-30m-supertrend-adx-exact-v1.claim`.
- Full-ladder branch path:
  `TrendExpansion -> CryptoSuperTrendAdxContinuation -> supertrend_adx_continuation_1m_full_ladder -> bybit_dot_atom_supertrend_adx_1m_full_ladder_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbols=DOTUSDT/ATOMUSDT`,
  `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Exact branch path:
  `TrendExpansion -> CryptoSuperTrendAdxContinuation -> bybit_dotusdt_supertrend_adx_30m_exact_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbol=DOTUSDT`, `timeframe=30m`.
- Full-ladder provider/AQ evidence: Bybit public `linear` fetched real rows for
  all `14` symbol/timeframe cells; intraday and `4h` validations exited `0`;
  both `1d` validations exited `1` only on short `90`-row daily slices;
  strategy compile, Auto-Quant material batch, dispatch, and rank exited `0`;
  rank produced `14` rows, `205` total trades, and rooted branch fields were
  preserved.
- Full-ladder exact `1m` origin verdict: failed real-cost gate. `DOTUSDT/1m`
  had `20` trades, raw `-0.32%`, `2bps/side=-1.12%`, and
  `5bps/side=-2.32%`; `ATOMUSDT/1m` had `19` trades, raw `-1.38%`,
  `2bps/side=-2.14%`, and `5bps/side=-3.28%`.
- Higher-timeframe context from the full ladder: `DOTUSDT/30m` had `19` trades,
  raw `+5.55%`, `2bps/side=+4.79%`, and `5bps/side=+3.65%`; `DOTUSDT/5m` had
  `7` trades, raw `+2.66%`, `2bps/side=+2.38%`, and
  `5bps/side=+1.96%`; `ATOMUSDT/15m` had `17` trades, raw `+3.96%`,
  `2bps/side=+3.28%`, and `5bps/side=+2.26%`.
- Exact `DOTUSDT/30m` Gate 1 evidence: real Bybit public `linear` fetched
  `1000` `DOTUSDT/30m` rows; validate-market-state, strategy compile,
  Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields
  were preserved.
- Exact `DOTUSDT/30m` cost verdict: cost survivor with `19` trades, raw
  `+5.55%`, `1bps/side=+5.17%`, `2bps/side=+4.79%`, and
  `5bps/side=+3.65%`.
- Downstream verdict: Auto-Quant import/prior, analyze,
  workflow/pre-bayes/policy readbacks, CatBoost train/apply/register/runtime,
  and final analyze/readbacks all exited `0`. Admission still failed closed:
  execution candidate `no_trade`, `execution_readiness=0.46954126403519203`,
  `transition_hazard=0.983045845831087`, `pda_hybrid_alignment=false`,
  `ranker_validation_ready=false`, and path-ranker score not used by the
  execution tree.
- Verification evidence: `python3 -m py_compile` passed for all three wrappers;
  `python3 -m json.tool` passed for exact Gate 1 `checks/terminal_metrics.json`,
  `checks/cost_stress_table.json`, and downstream `checks/downstream_metrics.json`;
  every exact Gate 1 and downstream `.exit` file was `0`; `git diff --check`
  passed for the three wrappers and current Board/matrix docs.
- Terminal decision: `exact_30m_gate1_survivor_downstream_fail_closed`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- SuperTrend-ADX improved diversity and found a real Bybit `DOTUSDT/30m`
  cost-surviving observation, but it still did not materialize an actionable
  same-root execution candidate and failed the hard PDA/transition/readiness
  predicates. Do not promote this branch or repeat the same DOTUSDT `30m`
  SuperTrend-ADX lane unless the next slice directly repairs PDA alignment,
  transition hazard, execution candidate materialization, or validation
  maturity.

## Bybit ENA/PENDLE BBWidth Pullback-Reclaim Full Ladder: 1m Origin Blocked

Append-only terminal row at `2026-05-20 19:27 +0800` after duplicate-searching
Board/matrix/claims for `ENAUSDT`, `PENDLEUSDT`, `ENA/PENDLE`, and exact Bybit
ENA/PENDLE BBWidth/pullback branches.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T192730+0800-codex-bybit-ena-pendle-bbwidth-pullback-reclaim-1m-full-ladder-v1/`.
- Wrapper:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_crypto_volatility_pullback_reclaim_gate1_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T192654+0800-codex-bybit-ena-pendle-bbwidth-pullback-reclaim-1m-full-ladder-v1.claim`.
- Canonical branch path:
  `Transition -> CryptoVolatilityCompression -> bbwidth_pullback_reclaim_1m_full_ladder -> bybit_ena_pendle_bbwidth_pullback_reclaim_1m_full_ladder_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbols=ENAUSDT/PENDLEUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for all `14`
  symbol/timeframe cells. Intraday and `4h` validations exited `0`; both `1d`
  validations exited `1` on short `90`-row daily slices. Strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; rank produced `14`
  rows, `44` total trades, and preserved rooted branch fields.
- Exact `1m` origin verdict: failed real-cost gate. `ENAUSDT/1m` had `4`
  trades, raw `-0.17%`, `2bps/side=-0.33%`, and `5bps/side=-0.57%`;
  `PENDLEUSDT/1m` had `4` trades, raw `-0.42%`, `2bps/side=-0.58%`, and
  `5bps/side=-0.82%`.
- Higher-timeframe context only: `ENAUSDT/1h` had `8` trades, raw `+2.35%`,
  `2bps/side=+2.03%`, and `5bps/side=+1.55%`. This is not proof for the failed
  `1m` origin and was not downstreamed.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`;
  `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.

Interpretation:

- ENA/PENDLE gives a fresh Bybit linear volatility-compression sample, but the
  exact `1m` origin is negative after costs. Preserve the `ENAUSDT/1h` row as
  independent higher-timeframe context only; do not run Pre-Bayes/BBN/CatBoost
  or execution-tree admission for this failed `1m` branch.

## Bybit ICP/ETC Aroon-CCI Reclaim Full Ladder: Gate 1 Cost/Density Negative

Append-only terminal row at `2026-05-20 19:36 +0800` after duplicate-searching
Board/matrix/claims/scripts for `ICPUSDT`, `ETCUSDT`, `Aroon`, `CCI`, and
exact Bybit ICP/ETC Aroon-CCI branches.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T193251+0800-codex-bybit-icp-etc-aroon-cci-reclaim-1m-full-ladder-v1/`.
- Wrapper:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_icp_etc_aroon_cci_reclaim_1m_full_ladder_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T193111+0800-codex-bybit-icp-etc-aroon-cci-reclaim-1m-full-ladder-v1.claim`.
- Canonical branch path:
  `TrendExpansion -> CryptoAroonCciReclaim -> aroon_cci_reclaim_1m_full_ladder -> bybit_icp_etc_aroon_cci_reclaim_1m_full_ladder_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbols=ICPUSDT/ETCUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for all `14`
  symbol/timeframe cells. Intraday and `4h` validations exited `0`; both `1d`
  validations exited `1` on short `90`-row daily slices. Strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; rank produced `14`
  rows, `24` total trades, and preserved rooted branch fields.
- Exact `1m` origin verdict: failed practical cost/density. `ETCUSDT/1m` had
  only `2` trades, raw `+0.19%`, `2bps/side=+0.11%`, and
  `5bps/side=-0.01%`; `ICPUSDT/1m` had `3` trades, raw `-0.53%`,
  `2bps/side=-0.65%`, and `5bps/side=-0.83%`.
- Higher-timeframe context only: `ICPUSDT/1h` had `1` trade, raw `+1.19%`,
  `2bps/side=+1.15%`, and `5bps/side=+1.09%`; `ETCUSDT/15m` had `4` trades,
  raw `+0.40%`, `2bps/side=+0.24%`, and `5bps/side=0.00%`. These are too
  sparse or friction-bound and were not downstreamed.
- Verification evidence: wrapper `python3 -m py_compile` exited `0`;
  `python3 -m json.tool` passed for `checks/terminal_metrics.json` and
  `checks/cost_stress.json`; `.exit` files show fetch/compile/AQ exits `0`
  except expected short-slice `1d` validations at exit `1`; `git diff --check`
  passed for the new wrapper before launch.
- Terminal decision: `drop_gate1_cost_or_density_failed`;
  `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.
- Duplicate run
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T200939+0800-codex-bybit-doge-pepe-stoprun-range-reclaim-1m-full-ladder-v1/`
  completed after the active wrapper process cleared and confirmed the same
  negative verdict with `14` rank rows: exact `1m` rows had `0` trades, only
  `DOGEUSDT/15m` traded (`3` trades), and it stayed negative after cost stress
  (`2bps/side=-0.77%`, `5bps/side=-0.95%`). No downstream was earned.

Interpretation:

- ICP/ETC Aroon-CCI improves public-family coverage but does not provide a
  usable cost-stressed `1m` origin or a dense higher-timeframe sibling. Cool down
  this exact Bybit linear Aroon-CCI cell and move to a materially different
  family or market cell.

## Bybit KAS/PYTH KAMA Efficiency-Pullback Full Ladder: 1m Origin Blocked

Append-only terminal row at `2026-05-20 19:45 +0800` after duplicate-searching
Board/matrix/claims/scripts for `KASUSDT`, `PYTHUSDT`, and exact Bybit
KAMA efficiency-pullback branches.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T194018+0800-codex-bybit-kas-pyth-kama-efficiency-pullback-1m-full-ladder-v1/`.
- Wrapper:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_kas_pyth_kama_efficiency_pullback_1m_full_ladder_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T1941+0800-codex-bybit-kas-pyth-kama-efficiency-pullback-1m-full-ladder-v1.claim`.
- Canonical branch path:
  `TrendPullback -> CryptoKamaEfficiencyPullback -> kama_efficiency_pullback_1m_full_ladder -> bybit_kas_pyth_kama_efficiency_pullback_1m_full_ladder_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbols=KASUSDT/PYTHUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for all `14`
  symbol/timeframe cells. Intraday and `4h` validations exited `0`; both `1d`
  validations exited `1` on short `90`-row daily slices. Strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; rank produced `14`
  rows, `68` total trades, and preserved rooted branch fields.
- Exact `1m` origin verdict: failed practical cost/density. `KASUSDT/1m` had
  only `3` trades, raw `-0.23%`, `2bps/side=-0.35%`, and
  `5bps/side=-0.53%`; `PYTHUSDT/1m` had `3` trades, raw `-0.37%`,
  `2bps/side=-0.49%`, and `5bps/side=-0.67%`.
- Higher-timeframe context only: `KASUSDT/15m` had `6` trades, raw `+2.01%`,
  `2bps/side=+1.77%`, and `5bps/side=+1.41%`; `KASUSDT/1h` had `8` trades,
  raw `+1.90%`, `2bps/side=+1.58%`, and `5bps/side=+1.10%`;
  `KASUSDT/30m` had `12` trades but failed `5bps/side` at `-0.33%`.
  These do not rescue the failed `1m` origin and were not downstreamed.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`;
  `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.

Interpretation:

- KAS/PYTH KAMA efficiency-pullback gives useful Bybit higher-timeframe context
  and public-family coverage, but the required exact `1m` origin is sparse and
  negative after costs. Preserve the `KASUSDT/15m` and `KASUSDT/1h` rows as
  context only; do not run Pre-Bayes/BBN/CatBoost/execution-tree for this failed
  `1m` branch.

## Bybit FIL/INJ KST-Coppock Momentum Full Ladder: HTF Survivors, 1m Origin Blocked

Append-only terminal row at `2026-05-20 19:43 +0800` after duplicate-searching
Board/matrix/claims for `FILUSDT`, `INJUSDT`, `KST`, `Coppock`, and exact
Bybit FIL/INJ KST-Coppock branches.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T193817+0800-codex-bybit-fil-inj-kst-coppock-momentum-1m-full-ladder-v1/`.
- Wrapper:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_fil_inj_kst_coppock_momentum_1m_full_ladder_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T1935+0800-codex-bybit-fil-inj-kst-coppock-momentum-1m-full-ladder-v1.claim`.
- Canonical branch path:
  `TrendExpansion -> CryptoKstCoppockMomentum -> kst_coppock_momentum_1m_full_ladder -> bybit_fil_inj_kst_coppock_momentum_1m_full_ladder_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbols=FILUSDT/INJUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for all `14`
  symbol/timeframe cells. Intraday and `4h` validations exited `0`; both `1d`
  validations exited `1` on short `90`-row daily slices. Strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; rank produced `14`
  rows, `189` total trades, and preserved rooted branch fields.
- Exact `1m` origin verdict: failed real-cost gate. `FILUSDT/1m` had `8`
  trades, raw `+0.29%`, `1bps/side=+0.13%`, `2bps/side=-0.03%`, and
  `5bps/side=-0.51%`; `INJUSDT/1m` had `9` trades, raw `-0.63%`,
  `2bps/side=-0.99%`, and `5bps/side=-1.53%`.
- Higher-timeframe survivors: `INJUSDT/30m` had `23` trades, raw `+14.46%`,
  `2bps/side=+13.54%`, and `5bps/side=+12.16%`; `INJUSDT/1h` had `27`
  trades, raw `+12.13%`, `2bps/side=+11.05%`, and `5bps/side=+9.43%`;
  `INJUSDT/15m` had `18` trades, raw `+4.04%`, `2bps/side=+3.32%`, and
  `5bps/side=+2.24%`; `INJUSDT/5m` had `8` trades, raw `+1.57%`,
  `2bps/side=+1.25%`, and `5bps/side=+0.77%`.
- Verification evidence: wrapper `python3 -m py_compile` exited `0`; fresh
  `python3 -m json.tool` passed for `checks/terminal_metrics.json` and
  `checks/cost_stress.json`; `.exit` files show fetch/compile/AQ exits `0`
  except expected short-slice `1d` validations at exit `1`.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`;
  `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.

Interpretation:

- FIL/INJ KST-Coppock produced the strongest recent Bybit HTF context, especially
  `INJUSDT/30m` and `INJUSDT/1h`, but the exact `1m` origin still failed real
  costs. Do not downstream the failed `1m` branch. If carrying the INJUSDT `30m`
  or `1h` survivor, reopen it as a separate exact-timeframe lane with a fresh
  claim, cost-density gate, and full downstream admission proof.

## Gate 1 Negative: Bybit XLM/TRX Ichimoku Cloud Continuation 1m Full Ladder

Append-only terminal row at `2026-05-20 19:42 +0800` for the fresh Bybit public-family lane claimed under `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T193847+0800-codex-bybit-xlm-trx-ichimoku-cloud-continuation-1m-full-ladder-v1.claim`.

- Run root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T194013+0800-codex-bybit-xlm-trx-ichimoku-cloud-continuation-1m-full-ladder-v1/`.
- Runner: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_xlm_trx_ichimoku_cloud_continuation_1m_full_ladder_v1.py`.
- Branch path: `TrendExpansion -> CryptoIchimokuCloudContinuation -> ichimoku_cloud_continuation_1m_full_ladder -> bybit_xlm_trx_ichimoku_cloud_continuation_1m_full_ladder_v1`. Market/product/provider/symbol/timeframe stayed as labels/provenance only.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for `XLMUSDT/TRXUSDT` across `1m/5m/15m/30m/1h/4h/1d` (`14` provider cells). Intraday and `4h` validations exited `0`; daily validations exited `1` on short `90`-row slices. Strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; rank produced `14` rows, `148` total trades, and preserved rooted branch fields.
- Exact `1m` origin failed the practical cost/density gate: `TRXUSDT/1m` had `6` trades, raw `+0.06%`, `2bps=-0.18%`, `5bps=-0.54%`; `XLMUSDT/1m` had `8` trades, raw `-0.53%`, `2bps=-0.85%`, `5bps=-1.33%`.
- Higher-timeframe positives are context only and do not rescue the failed origin: `TRXUSDT/4h` had `10` trades, raw `+5.81%`, `5bps=+4.81%`; `TRXUSDT/1h` had `20` trades, raw `+3.53%`, `5bps=+1.53%`; `XLMUSDT/1h` had `16` trades, raw `+2.83%`, `5bps=+1.23%`; `XLMUSDT/15m` had `7` trades, raw `+1.28%`, `5bps=+0.58%`.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`; `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Next useful work: cool down the exact Bybit XLM/TRX Ichimoku `1m` origin. Open any `1h/4h` Ichimoku lane only as a separately claimed exact-timeframe branch with its own cost/density and downstream gates; do not flatten HTF positives into this failed `1m` objective.

## Bybit TRXUSDT 4h Ichimoku Exact Root: Gate 1 Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 19:54 +0800` after restarting the prior XLM/TRX full-ladder `TRXUSDT/4h` HTF context as an independent exact timeframe branch.

- Gate 1 root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T195252+0800-codex-bybit-trxusdt-4h-ichimoku-cloud-continuation-exact-gate1-v1/`.
- Downstream root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T195252+0800-codex-bybit-trxusdt-4h-ichimoku-cloud-continuation-exact-gate1-v1/downstream-exact-trxusdt-4h-ichimoku-cloud-continuation-20260520T195331+0800/`.
- Wrappers: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_trxusdt_4h_ichimoku_cloud_continuation_exact_gate1_v1.py` and `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_trxusdt_4h_ichimoku_cloud_continuation_exact_downstream_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T194919+0800-codex-bybit-trxusdt-4h-ichimoku-cloud-continuation-exact-v1.claim`.
- Canonical branch path: `TrendExpansion -> CryptoIchimokuCloudContinuation -> bybit_trxusdt_ichimoku_cloud_continuation_4h_exact_v1`. Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`, `symbol=TRXUSDT`, `timeframe=4h`.
- Gate 1 evidence: Bybit public `linear` fetched `535` real `TRXUSDT/4h` rows; validate-market-state, strategy compile, Auto-Quant material batch, dispatch, and rank all exited `0`; branch fields were preserved.
- Gate 1 economics passed: `10` trades, `70.0%` win rate, raw `+5.81%`, `2bps/side=+5.41%`, and `5bps/side=+4.81%`.
- Downstream completed all `15` command exits `0` through Auto-Quant import/prior, analyze, workflow, Pre-Bayes/BBN readbacks, structural target export, CatBoost train/apply/register, runtime enable, final analyze, workflow, and policy readback.
- Admission still failed closed: execution candidate `no_trade`; execution tree `observe` on `fill_viable`; `execution_readiness=0.6092951190113165`, below the required `0.65`; `transition_hazard=0.5832500334194493`, under the hard `0.60` ceiling; `pda_hybrid_alignment=true`; path-ranker visible/used=`true/false`; `ranker_validation_ready=false`; policy mature/history mature rows `0/0`.
- Terminal decision: `exact_4h_gate1_survivor_downstream_fail_closed`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is the nearest recent Bybit exact-root branch to the hard execution gates because PDA alignment is true and transition hazard is below `0.60`, but readiness is still short of `0.65`, no execution candidate materialized, the ranker score is not used by the execution tree, and mature validation is absent. Preserve as observation/repair evidence only. Do not promote or call it trade-usable.

## Bybit TRXUSDT 4h Ichimoku Simulated Feedback: Mechanics Repaired, Still Fail-Closed

Append-only terminal row at `2026-05-20 20:08 +0800` for the same-root simulated-feedback repair probe on the exact `TRXUSDT/4h` Ichimoku branch.

- Source root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T195252+0800-codex-bybit-trxusdt-4h-ichimoku-cloud-continuation-exact-gate1-v1/`.
- Repair root: `support/docs/experiments/actionable-regime-confidence/runs/20260520T195252+0800-codex-bybit-trxusdt-4h-ichimoku-cloud-continuation-exact-gate1-v1/simulated-trade-admission-trxusdt-4h-ichimoku-cloud-continuation-20260520T200726+0800/`.
- Wrapper: `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_trxusdt_4h_ichimoku_cloud_continuation_exact_simulated_trade_admission_v1.py`.
- Claim: `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T200438+0800-codex-bybit-trxusdt-4h-ichimoku-simulated-feedback-admission-v1.claim`.
- Feedback evidence: exported and ingested `10` same-Auto-Quant-workspace simulated trades (`7` wins, `3` losses, `0` breakevens). This is repair evidence only, not live promotion proof.
- Runtime chain: Auto-Quant import/prior, seed analyze, workflow, Pre-Bayes, target export, policy readback, simulated-trade ingest, post-feedback target/policy readbacks, CatBoost/fallback trainer, external score apply, trainer register, runtime enable, final analyze/workflow/Pre-Bayes/policy all exited `0`.
- Result: exact branch survived and the two earlier near-gates remained good (`transition_hazard=0.5832500334194493`, `pda_hybrid_alignment=true`), but admission still failed: `mature_rows=1`, `history_mature_rows=10`, `raw_scored_mature=1`, `ranker_validation_ready=false`, execution candidate `no_trade`, `execution_readiness=0.6092951190113165`, path-ranker visible/used=`true/false`.
- Terminal decision: `trx4h_ichimoku_simulated_feedback_fail_closed`; `hard_gates_clear_under_simulated_feedback=false`; `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Next useful work: do not repeat simulated feedback on this exact branch. A further TRXUSDT/4h continuation must add real or acceptable mature feedback and directly materialize an exact execution candidate while preserving `transition_hazard < 0.60` and `pda_hybrid_alignment=true`.

## Bybit SEI/TIA Donchian-Turtle Full Ladder: 1m Origin Blocked, HTF Context Only

Append-only terminal row at `2026-05-20 19:53 +0800` after checking active
processes, latest claims, Board/matrix tails, and collisions for `SEI`, `TIA`,
`Donchian`, `Turtle`, and `bybit_sei_tia`.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T195111+0800-codex-bybit-sei-tia-donchian-turtle-breakout-1m-full-ladder-v1/`.
- Runner:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_sei_tia_donchian_turtle_breakout_1m_full_ladder_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T1950+0800-codex-bybit-sei-tia-donchian-turtle-breakout-1m-full-ladder-v1.claim`.
- Canonical branch path:
  `TrendExpansion -> CryptoDonchianTurtleBreakout -> donchian_turtle_breakout_1m_full_ladder -> bybit_sei_tia_donchian_turtle_breakout_1m_full_ladder_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbols=SEIUSDT/TIAUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: Bybit public `linear` fetched real rows for all `14`
  symbol/timeframe cells. Intraday and `4h` validations exited `0`; both `1d`
  validations exited `1` on short `90`-row daily slices. Strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; rank produced `14`
  rows, `118` total trades, and preserved rooted branch fields.
- Exact `1m` origin failed the practical cost gate. `SEIUSDT/1m` had `7`
  trades, raw `-0.25%`, `2bps/side=-0.53%`, and `5bps/side=-0.95%`;
  `TIAUSDT/1m` had `4` trades, raw `-0.42%`, `2bps/side=-0.58%`, and
  `5bps/side=-0.82%`.
- Higher-timeframe context only: `SEIUSDT/4h` had `5` trades, raw `+4.56%`,
  `2bps/side=+4.36%`, and `5bps/side=+4.06%`; `SEIUSDT/15m` had `6` trades,
  raw `+3.35%`, `2bps/side=+3.11%`, and `5bps/side=+2.75%`; `TIAUSDT/15m`
  had `7` trades, raw `+3.05%`, `2bps/side=+2.77%`, and `5bps/side=+2.35%`;
  `SEIUSDT/1h` had `12` trades, raw `+2.49%`, `2bps/side=+2.01%`, and
  `5bps/side=+1.29%`.
- Verification evidence: `python3 -m py_compile` exited `0` for the wrapper;
  fresh `python3 -m json.tool` passed for `checks/terminal_metrics.json` and
  `checks/cost_stress.json`; `.exit` files show fetch/compile/AQ exits `0`
  except expected short-slice `1d` validations at exit `1`.
- Terminal decision: `higher_timeframe_subclass_only_origin_blocked`;
  `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.

Next useful work: preserve the SEI/TIA Donchian-Turtle packet as public-family
Bybit context only. Do not downstream the failed `1m` origin; any `SEIUSDT/4h`,
`SEIUSDT/15m`, `TIAUSDT/15m`, or `SEIUSDT/1h` continuation must be opened as a
separate exact-timeframe branch with its own claim, cost/density proof, and full
downstream admission gates.

## Bybit TRXUSDT 1h Ichimoku Exact Root: Gate 1 Survivor, Downstream Fail-Closed

Append-only terminal row at `2026-05-20 20:02 +0800` after restarting the prior
XLM/TRX full-ladder `TRXUSDT/1h` HTF context as an independent exact timeframe
branch. Collision search found the `TRXUSDT/4h` exact branch already
terminalized, but no `TRXUSDT/1h` exact branch claim or row.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T200052+0800-codex-bybit-trxusdt-1h-ichimoku-cloud-continuation-exact-gate1-v1/`.
- Downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T200052+0800-codex-bybit-trxusdt-1h-ichimoku-cloud-continuation-exact-gate1-v1/downstream-exact-trxusdt-1h-ichimoku-cloud-continuation-20260520T200145+0800/`.
- Wrappers:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_trxusdt_1h_ichimoku_cloud_continuation_exact_gate1_v1.py` and
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_trxusdt_1h_ichimoku_cloud_continuation_exact_downstream_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T2000+0800-codex-bybit-trxusdt-1h-ichimoku-cloud-continuation-exact-v1.claim`.
- Canonical branch path:
  `TrendExpansion -> CryptoIchimokuCloudContinuation -> bybit_trxusdt_ichimoku_cloud_continuation_1h_exact_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbol=TRXUSDT`, `timeframe=1h`.
- Gate 1 evidence: Bybit public `linear` fetched `1000` real `TRXUSDT/1h`
  rows; validate-market-state, strategy compile, Auto-Quant material batch,
  dispatch, and rank all exited `0`; branch fields were preserved.
- Gate 1 economics passed: `20` trades, `50.0%` win rate, raw `+3.53%`,
  `2bps/side=+2.73%`, and `5bps/side=+1.53%`.
- Downstream completed all `15` command exits `0` through Auto-Quant
  import/prior, analyze, workflow, Pre-Bayes/BBN readbacks, structural target
  export, CatBoost train/apply/register, runtime enable, final analyze,
  workflow, and policy readback.
- Admission still failed closed: execution candidate `no_trade`; execution tree
  `observe` on `transition_guardrail`; `execution_readiness=0.5077235236322891`,
  below the required `0.65`; `transition_hazard=0.6082572807622642`, just above
  the hard `0.60` ceiling; `pda_hybrid_alignment=true`; path-ranker
  visible/used=`true/false`; `ranker_validation_ready=false`; policy
  mature/history mature rows `0/0`.
- Terminal decision: `exact_1h_gate1_survivor_downstream_fail_closed`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- TRXUSDT `1h` Ichimoku is a real exact-root cost survivor and keeps PDA
  alignment, but it is weaker than the `4h` sibling on execution readiness and
  hazard. Preserve it as observation/repair evidence only. Do not promote or
  call it trade-usable until an exact branch both materializes an actionable
  execution candidate and satisfies `transition_hazard < 0.60`,
  `pda_hybrid_alignment=true`, and `execution_readiness >= 0.65` with mature
  validation.

## Bybit DOGE/PEPE Stop-Run Range-Reclaim Full Ladder: Gate 1 Negative

Append-only terminal row at `2026-05-20 20:10 +0800` after routing through
`sd/ict-engi-fact-rese-muta`, checking active claims/processes, repairing the
wrapper identity so the `range_reclaim` branch did not duplicate the older
`stoprun_reclaim` packet, and running the fresh Gate 1 wrapper.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T200735+0800-codex-bybit-doge-pepe-stoprun-range-reclaim-1m-full-ladder-v1/`.
- Duplicate collision readback root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T200939+0800-codex-bybit-doge-pepe-stoprun-range-reclaim-1m-full-ladder-v1/`.
- Wrapper:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_bybit_doge_pepe_stoprun_range_reclaim_1m_full_ladder_v1.py`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260520T200501+0800-codex-bybit-doge-pepe-stoprun-range-reclaim-1m-full-ladder-v1.claim`.
- Canonical branch path:
  `RangeReversion -> BybitMemecoinLiquiditySweep -> range_reclaim_1m_full_ladder -> bybit_doge_pepe_stoprun_range_reclaim_1m_full_ladder_v1`.
  Labels: `market=CryptoLinearPerp`, `provider=Bybit public linear`,
  `symbols=DOGEUSDT/PEPEUSDT`, `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ evidence: the first run fetched real DOGEUSDT rows for all
  requested timeframes while PEPEUSDT fetches exited `1`. A same-lane collision
  rerun then completed with Bybit-mapped `1000PEPEUSDT` rows across all requested
  timeframes, confirming this as a provider-covered negative rather than a PEPE
  availability blocker. Intraday/4h validations exited `0`; both `1d`
  validations exited `1` on short daily slices. Strategy compile, Auto-Quant
  material batch, dispatch, and rank exited `0`.
- Gate 1 result: the collision readback rank produced `14` rows and only `3`
  total trades. Exact `DOGEUSDT/1m` and `1000PEPEUSDT/1m` both had `0` trades.
  The only nonzero row was `DOGEUSDT/15m` with `3` trades, raw `-0.65%`,
  `1bps/side=-0.71%`, `2bps/side=-0.77%`, and `5bps/side=-0.95%`.
- Terminal decision: `drop_gate1_cost_or_density_failed`;
  `downstream_allowed=false`; `promotion_allowed=false`; `trade_usable=false`;
  `update_goal=false`.

Interpretation:

- This is a clean negative for the exact DOGE/PEPE `1m` stop-run range-reclaim
  root. Do not downstream this packet, and do not retune the adjacent DOGE/PEPE
  stop-run reclaim family in the next slot. The next useful work should rotate
  to a materially different market/symbol/family cell or repair an existing true
  cost-density survivor's execution-admission blockers.

## Tomac ES/YM/NQ Local Futures Screens: No Practical Admission Yet, One Low-Frequency Observation Lead

Append-only terminal row at `2026-05-21 04:04:19 +0800` after routing through
`sd/ict-engi-fact-rese-muta`, reading current repo instructions, checking active
Tomac/AQ processes, and using the user's local Databento/Tomac folders for
`ES`, `YM`, and `NQ` futures. Pre-write Board hash:
`3a8a1ef0597d291d15a9ae633d21eaaa8b5534ec3c2b3ee3387e7bf163bdef8d`.

- Source data requested by operator:
  `/Users/thrill3r/Downloads/Tomac/es future 2021-2025`,
  `/Users/thrill3r/Downloads/Tomac/ym future 2021-2025`, and
  `/Users/thrill3r/Downloads/Tomac/nq future 2021-2025`.
- Valid normalized dominant-outright screen:
  `/tmp/ict-tomac-futures-factor/run-20260521-es-ym-nq/`.
  Decision: `no_cross_symbol_5bps_survivor_observation_only`. Best dense
  1m family, `tomac_futures_1m_wide_range_failed_breakout_short_v1`, had
  `6952` trades and `1.796` trades/session but failed costs
  (`2bps/side=-279.166%`, `5bps/side=-696.286%` aggregate).
- Broader outright scan:
  `/tmp/ict-engine-tomac-futures-outright-scan-20260521/`.
  Decision: `drop_no_gate1_survivor`, `gate1_survivor_count=0`.
- Density expansion:
  `/tmp/tomac-density-expansion-scan-20260521-v2/`.
  Decision: `drop_no_density_expansion_gate1_candidate`; top rows were
  raw-positive observations but failed cost/walk-forward gates.
- Bidirectional and two-stage local searches:
  `/tmp/ict-engine-tomac-futures-bidir-search-20260521/terminal_metrics.json`
  and `/tmp/ict-engine-tomac-two-stage-discovery-20260521/terminal_metrics.json`.
  Decisions: `drop_no_bidirectional_gate1_survivor` and
  `drop_no_exact_two_stage_survivor`; both have `gate1_survivor_count=0` and
  `strict_5bps_survivor_count=0`.
- ES parameter search:
  `/tmp/ict-tomac-futures-factor/run-20260521-param-search-es-v2/`.
  Decision: `no_walk_forward_5bps_survivor_observation_only`.
- Event-lab screen:
  `/tmp/ict-tomac-futures-factor/run-20260521-event-lab-es-ym-nq/`.
  ES/YM event rows had hundreds of trades and 63%-67% headline win rates, but
  every inspected row failed `2bps/side` and `5bps/side`; do not downstream.
- Daily/overnight regime screen:
  `/tmp/ict-engine-tomac-daily-overnight-regime-screen-20260521Tlocal/`.
  Terminal verdict: `drop_no_gate1_screen_survivor`,
  `gate1_screen_survivor_count=0`; all downstream booleans remain false.

Strongest observation lead from the daily/overnight screen:

- Factor:
  `tomac_idxfut_prior_day_exhaustion_reversal_prior_trend_down_thr0100_am_v1`.
- Canonical branch:
  `RangeReversion -> PriorDayExhaustion -> NextSessionExhaustionReversal -> tomac_idxfut_prior_day_exhaustion_reversal_prior_trend_down_thr0100_am_v1`.
- Rule shape: if prior RTH day closed down at least `1.0%`, enter opposite
  direction at next session `09:35` open and exit `11:30` close.
- Aggregate economics on ES/YM/NQ: `403` trades, `59.55335%` win rate, raw
  `+62.491136%`, `2bps/side=+46.371136%`, `5bps/side=+22.191136%`.
- Year split after `5bps/side`: `2021=+4.300401%`, `2022=+1.69638%`,
  `2023=+3.542696%`, `2024=+2.946814%`, `2025=+9.704846%`.
- Symbol split after `5bps/side`: `ES=+6.215807%`, `YM=+4.990061%`,
  `NQ=+10.985269%`.
- Price sanity passed on audited trades: ES entries `3629.5..6842.25`, YM
  entries `29069..46973`, NQ entries `10828.25..25574`.
- Blocker: strict Board B density still fails (`0.312645` trades/day, below the
  `1-3/day` target) and no exact AQ/downstream admission has proven
  `transition_hazard < 0.60`, `pda_hybrid_alignment=true`, or
  `execution_readiness >= 0.65`. Therefore this is a low-frequency observation
  lead only, not a practical/trade-usable factor.

Next useful work:

- Do not lower the density or execution gates. Use the prior-day exhaustion
  branch only as a regime/context filter or as a low-frequency control lead.
- Continue searching for a denser `1m`-origin or low-timeframe intraday factor
  under a regime-rooted branch that keeps real-cost density near `1-3/day`.
- If a new exact-root survivor appears, replay it through Auto-Quant first and
  only then through Pre-Bayes/BBN/CatBoost/execution-tree. Promotion remains
  blocked unless the same rooted branch has consistent direction, sufficient
  density after costs, `transition_hazard < 0.60`,
  `pda_hybrid_alignment=true`, `execution_readiness >= 0.65`, and
  `trade_usable=true`.

## Tomac NQ First30 Momentum: Exact AQ Cost Survivor, Downstream Fail-Closed

Append-only terminal addendum at `2026-05-21 04:11 +0800` after checking Board
collisions for `first30`, `tomac_nq_first30`, and `prior_day_down_reversal`.

- Run root:
  `/private/tmp/tomac-nq-first30-momentum-downstream-20260521/`.
- Source Gate 1 root:
  `/private/tmp/tomac-nq-first30-momentum-exact-aq-20260521/`.
- Terminal metrics:
  `/private/tmp/tomac-nq-first30-momentum-downstream-20260521/checks/terminal_metrics.json`.
- Canonical branch path:
  `TrendExpansion -> RthOpeningDriveMomentum -> First30Momentum -> tomac_nq_first30_momentum_thr0008_exact_aq_v1`.
  Labels: `market=futures`, `product=equity_index`, `provider=local_csv_tomac`,
  `symbol=NQ`, `timeframe=1m`.
- Exact AQ Gate 1 passed cost stress: strategy compiled and `run_tomac.py`
  exited `0`; `123` trades; long/short split `53 / 70`; gross profit
  `+39.1%`; win rate `59.3496%`; profit factor `1.7091`; raw Sharpe
  `0.2315`; max drawdown `-10.3451%`; `1bps/side=+36.64%`,
  `2bps/side=+34.18%`, and `5bps/side=+26.8%`.
- Downstream was attempted on the same rooted branch: Auto-Quant import,
  prior-init, workflow/pre-Bayes/export, ranker train/apply/apply-scores,
  trainer register, runtime enable, final workflow/pre-Bayes/policy/export all
  exited `0`; both analyze calls timed out (`124`).
- Admission failed closed: no Pre-Bayes posterior, no execution candidate,
  `execution_readiness=null`, `transition_hazard=null`,
  `pda_hybrid_alignment=null`, raw scored mature rows `1/30`, production
  validation rows `0/30`, observation validation rows `0/30`, and trainer
  status `present_validation_insufficient`.
- Terminal decision: `observation_exact_aq_cost_survivor_downstream_fail_closed`;
  `downstream_allowed=false`; `pre_bayes_allowed=false`; `bbn_allowed=false`;
  `catboost_allowed=false`; `execution_tree_allowed=false`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is the strongest Tomac index-futures exact AQ survivor found so far, but
  it is not practical under the Board B objective because execution admission
  did not materialize and all hard downstream gates remain false. Do not repeat
  the same first30 root as another light threshold tweak. The next useful work
  is either an execution-materialization/mature-feedback repair for this exact
  root or a denser `1m`-origin factor with the same AQ cost discipline.

### Same-Root Simulated-Trade Admission Repair: Still Fail-Closed

Append-only terminal addendum at `2026-05-21 05:31 +0800` after replaying the
same exact Auto-Quant workspace trades as branch-preserving feedback for the
existing NQ First30 survivor. No cost gate was lowered.

- Repair root:
  `/tmp/tomac-nq-first30-simulated-admission-20260521/run-20260521T052125+0800/`.
- Terminal metrics:
  `/tmp/tomac-nq-first30-simulated-admission-20260521/run-20260521T052125+0800/checks/simulated_admission_metrics.json`.
- Same branch path:
  `TrendExpansion -> RthOpeningDriveMomentum -> First30Momentum -> tomac_nq_first30_momentum_thr0008_exact_aq_v1`.
- Commands: exact-trade extraction, Auto-Quant import/prior, `auto-quant-ingest-real-trades`,
  target export, policy readback, CatBoost train/apply, score application,
  trainer registration, runtime enable, workflow, Pre-Bayes, and final policy
  all exited `0`; the bounded post-ranker `analyze` leg timed out with exit
  `124`.
- Same-workspace feedback evidence: `123` simulated trades were ingested as
  `73` wins / `49` losses / `1` breakeven; `123/123` feedback rows carried
  structural feedback for the exact rooted branch.
- What improved: history validation repaired materially
  (`history_mature_rows=125`, `raw_scored_mature_rows=126`).
- What still blocks: current `mature_rows=3`, `ranker_validation_ready=false`,
  no execution candidate, `execution_readiness=0.0`, `transition_hazard=1.0`,
  and `pda_hybrid_alignment=false`.
- Terminal decision: `tomac_nq_first30_simulated_admission_fail_closed`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- Same-root trade feedback repairs the historical maturity side but not current
  execution admission. This branch remains an observation/repair lead only. The
  next useful work is not another First30 threshold tweak; it must either
  materialize a current exact execution candidate with PDA/transition/readiness
  repair, or pivot to a denser `1m` regime-rooted factor that survives
  Auto-Quant `5bps/side` before downstream.

## Tomac Prior-Day-Down Exit Ladder and Public Trend Matrix: Gate 1 Negatives

Append-only terminal addendum at `2026-05-21 04:11 +0800` after auditing the
active density-repair and clean trend screens.

- Prior-day-down exit ladder root:
  `/tmp/ict-engine-tomac-prior-day-down-exit-ladder-20260521Tlocal/`.
  Objective: density repair for
  `RangeReversion -> PriorDayExhaustion -> NextSessionExhaustionReversal`.
  Result: `candidate_count=210`, `gate1_screen_survivor_count=0`, verdict
  `drop_no_gate1_screen_survivor`.
- Best exit-ladder row remained the already documented low-frequency variant:
  `tomac_idxfut_prior_day_down_reversal_thr0100_0935_1130_v1`, `403` trades,
  `0.312645` trades/day, raw `+62.491136%`, `2bps/side=+46.371136%`,
  `5bps/side=+22.191136%`, all three symbols positive at `5bps/side`, but
  density below the required `1-3/day`.
- Clean public trend matrix root:
  `/tmp/tomac-clean-trend-breakout-agent-20260521/`.
  Result: `393` rows; every row had `gate1_candidate_2bps=false` and
  `strict_5bps_survivor=false`.
- Representative trend near-misses were sparse or cost/validation failures:
  `NQ/1d supertrend_10_3` had `157` trades and positive 5bps full return but
  only `0.08598` trades/day; `YM/4h supertrend_10_3` had `1102` trades and
  `0.6035` trades/day but `5bps/full=-0.5945`; multiple NQ/4h trend rows were
  positive at `2bps` but negative at `5bps`.

Interpretation:

- The prior-day exhaustion family remains a valid low-frequency observation
  lead, not a practical Board B factor. The public trend matrix adds diversity
  coverage but no survivor. Continue with a new denser `1m` origin or repair the
  exact NQ first30 downstream blocker; do not lower density or `5bps` cost gates.

## Tomac ES/YM/NQ Greedy No-Lookahead Composite: Gate 1 Survivor, Downstream Fail-Closed

Append-only terminal addendum at `2026-05-21 07:28 +0800` after routing through
`sd/ict-engi-fact-rese-muta`, re-reading the current Board, duplicate-searching
for `tomac_es_ym_nq_nolookahead_greedy_core_slots_ex_may_sep_nov_v1`, and
preserving the pre-write Board hash
`a42a2a0e93a1667df6d0885f20f242726bf78fd0bbcb8d362dceb712098d6eea`.

- Downstream run root:
  `/private/tmp/tomac-es-ym-nq-greedy-filtered-downstream-20260521/`.
- Source Gate 1 root:
  `/private/tmp/tomac-greedy-filtered-exact-aq-20260521/`.
- Terminal metrics:
  `/private/tmp/tomac-es-ym-nq-greedy-filtered-downstream-20260521/checks/terminal_metrics.json`.
- Canonical branch path:
  `TrendExpansion -> SessionDirectionalBias -> NoLookaheadGreedyComposite -> tomac_es_ym_nq_nolookahead_greedy_core_slots_ex_may_sep_nov_v1`.
  Labels: `market=futures`, `product=equity_index_portfolio`,
  `provider=local_csv_tomac`, `symbols=ES/YM/NQ`, `timeframe=1m`.
- Exact AQ Gate 1 passed cost stress: `902` trades, gross `+118.33%`,
  win rate `62.0843%`, profit factor `2.2144`, Sharpe `2.3143`,
  `1bps/side=+100.29%`, `2bps/side=+82.25%`,
  and `5bps/side=+28.13%`. Pair splits were positive:
  `NQ=473 trades/+56.23%`, `ES=230/+37.47%`, and `YM=199/+24.63%`.
- Same-workspace replay feedback was exported as `902` rows
  (`557` wins / `343` losses / `2` breakevens). The interrupted
  `auto-quant-ingest-real-trades` process left no `08` exit file and truncated
  temp-state `update_runs.json`; the truncated file was backed up as
  `update_runs.json.truncated-20260521T072124+0800`, then the temp-state JSON
  was replaced with a valid empty list so native readbacks could continue from
  the preserved CSV target/history artifacts.
- Downstream readbacks after repair: export, policy, CatBoost train/apply,
  score import, trainer register, runtime enable, workflow, Pre-Bayes, and final
  policy readbacks exited `0`; both analyze passes timed out (`124`).
- What improved: exact branch survived, CatBoost ranker became runtime eligible,
  ranker validation is ready, `history_mature_rows=905`,
  `raw_scored_mature=905/30`, `production_validation=905/30`, and
  `observation_validation=902/30`.
- What still blocks promotion: closed-loop admission stayed `fail_closed` with
  `candidate_status=observe`, `actionable=false`,
  `reason=exact_structural_branch_visible_but_not_ready_or_actionable`,
  Pre-Bayes posterior remained empty, current `mature_rows=3`,
  no execution candidate materialized, and there is no passing evidence for
  `execution_readiness >= 0.65`, `transition_hazard < 0.60`, or
  `pda_hybrid_alignment=true`.
- Terminal decision: `exact_es_ym_nq_downstream_fail_closed`;
  `promotion_allowed=false`; `trade_usable=false`; `update_goal=false`.

Interpretation:

- This is the strongest Tomac ES/YM/NQ exact AQ cost survivor in this local-data
  round, but it is still not a practical/live factor. Treat it as an
  observation and repair lead only. The next useful work is execution-candidate
  materialization plus PDA/transition/readiness repair for this exact root, or a
  fresh denser no-lookahead `1m` root that again survives exact AQ `5bps/side`.

## Tomac XAU/GC Hilbert Cycle-Turn 1m: Gate 1 Negative

Append-only terminal row at `2026-05-21 10:19:50 +0800` after routing through
`sd/ict-engi-fact-rese-muta`, re-reading current XAU claims, duplicate-searching
for `tomac_xau_hilbert_cycle_turn_1m_v1` and `20260521T085033`, and observing
the current Board hash
`d14bbb8db00944fd80ca09577b5f1c5cc1bef0cf2edb31bbac89c24b977b8aa9` before
this append. The diversity matrix had concurrent terminal-row activity during
this turn, so this row is append-only and does not claim a matrix lock.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T085033+0800-codex-tomac-xau-hilbert-cycle-turn-1m-gate1-v1/`.
- Scratch root:
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T085033+0800`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T0830+0800-codex-tomac-xau-hilbert-cycle-turn-1m-gate1-v1.claim`.
- Terminal metrics:
  `checks/terminal_metrics.json`; rank rows:
  `summaries/rank_rows.csv`; command exit: `checks/00_run_tomac.exit=0`.
- Source staging selected highest-volume positive outright `GC*` rows from the
  supplied XAU/GC local CSV, rejected `1,976,135` spread/non-outright rows, and
  retained `1,769,524` true `1m` rows from `2021-01-06` through `2026-01-05`.
  The `5m/15m/30m/1h/4h/1d` ladder was derived context from the same `1m` source,
  not independent provider proof.
- Canonical branch paths stayed regime-rooted under
  `CycleReversal -> HilbertPhaseTurn -> HilbertCycleTurn -> <factor_id>`.
  Labels: `market=futures`, `product=precious_metals`,
  `provider=local_tomac_glbx_mdp3`, `symbol=GC/XAU`, `timeframe=1m`.
- Gate 1 result: Auto-Quant exited `0`, ranked `3` exact rows with `15,556`
  total trades, and found no `2bps/side` or `5bps/side` survivor:
  `exact_1m_survivors_2bps=[]`, `exact_1m_survivors_5bps=[]`.
- Rows were dense but raw/cost negative: balanced `5,359` trades,
  `4.180/day`, raw `-26.74%`, `2bps=-241.10%`, `5bps=-562.64%`;
  dense `5,966` trades, `4.654/day`, raw `-57.95%`, `2bps=-296.59%`,
  `5bps=-654.55%`; quality `4,231` trades, `3.300/day`, raw `-23.18%`,
  `2bps=-192.42%`, `5bps=-446.28%`.
- Terminal decision: `observation_gate1_no_practical_5bps_density_survivor`;
  `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- Hilbert cycle-turn is useful public-family diversity for XAU/GC, but this
  exact local `1m` cell is overactive and negative before costs. Do not run
  Pre-Bayes/BBN/CatBoost/execution-tree on this root, and do not threshold-retune
  the same Hilbert trigger unless a new hypothesis first proves materially wider
  per-trade excursion at Gate 1.

## Repeat Provider Blocker: IBKR MCL 1m Donchian/ATR Breakout 7D, 20260521

Append-only blocker row at `2026-05-21 10:28:10 +0800` after re-reading active
claims/processes, inspecting the completed run artifacts, duplicate-searching
for prior `IBKR MCL 1m Donchian/ATR` blocker rows, and observing Board hash
`42aded7835df985bc60fdbe08dcc5ab6d63d03315c19e84b67d645e01d2f732d` before
this append.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T102100+0800-codex-ibkr-mcl1m-donchian-atr-breakout-7d-gate1-v1/`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T102100+0800-codex-ibkr-mcl1m-donchian-atr-breakout-7d-gate1-v1.claim`.
- Intended branch path:
  `TrendExpansion -> DonchianAtrBreakout -> ibkr_mcl1m_donchian_atr_breakout_7d_gate1_v1`.
  Labels: `market=futures`, `product=energy`, `provider=IBKR`, `symbol=MCL`,
  `contract=202606`, `timeframe=1m`, `window=7 D`.
- Provider evidence: intended real IBKR `MCL 202606` `1m 7 D`, but normalized
  provider rows were `0`; `checks/00_fetch_ibkr_mcl_1m_7d.exit=1`.
- Gate 1 evidence: `rank_rows=0`, `rank_total_trade_count=0`, no branch fields
  materialized, and `exact_1m_survivors_2bps=[]`,
  `exact_1m_survivors_5bps=[]`.
- Terminal decision: `provider_window_blocked_no_factor_verdict`;
  `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is still a provider/window failure, not a Donchian/ATR strategy negative.
  Do not relaunch the MCL `1m 7 D` branch until a free IBKR session or retained
  real MCL rows can produce nonzero provider rows; rotate training to a market
  cell with actual data or a same-root repair lane with completed Gate 1
  economics.

## Gate 1 Negative: IBKR MNQ 1m Ease of Movement Displacement Current Rerun

Append-only terminal row at `2026-05-21 10:28 +0800` after routing through
`sd/ict-engi-fact-rese-muta`, confirming `provider-status` reports
`ibkr.status=ready`, and applying the operator's priority for IBKR historical
data or simulated trading.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T102148+0800-codex-ibkr-mnq1m-eom-displacement-7d-gate1-v1/`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T101842+0800-codex-ibkr-mnq1m-eom-displacement-7d-gate1-v1.claim`.
- Runner:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_ibkr_mnq1m_eom_displacement_7d_gate1_v1.py`.
- Terminal metrics:
  `checks/terminal_metrics.json`; rank rows:
  `summaries/rank_rows.csv`; command exits:
  `00_strategy_py_compile=0`, `01_auto_quant_agent_material_batch=0`,
  `02_auto_quant_agent_material_dispatch=0`, and
  `03_auto_quant_agent_material_rank=0`.
- Provider evidence: retained real IBKR `MNQ 202606` `1m 7 D` rows, copied from
  the prior same-session historical provider root
  `20260520T021030+0800-codex-ibkr-mnq1m-gap-fade-go-7d-gate1-v1`;
  `rows=9481`; `local_cache_replay=false_retained_real_ibkr_same_session`.
- Canonical branch path:
  `DisplacementExpansion -> EaseOfMovementDisplacement -> ibkr_mnq1m_eom_displacement_7d_gate1_v1`.
  Labels/provenance prefix: `FUTURES -> equity_index -> MNQ -> 1m`.
  Branch fields were preserved through AQ rank.
- Gate 1 result: Auto-Quant ranked `4` rows with `203` total trades, but every
  row was raw-negative and failed `2bps/side` and `5bps/side`:
  `exact_1m_survivors_2bps=[]`, `exact_1m_survivors_5bps=[]`.
- Row readback: `eom_balanced` `49` trades, raw `-0.63%`, `2bps=-2.59%`,
  `5bps=-5.53%`; `eom_dense` `59` trades, raw `-0.71%`, `2bps=-3.07%`,
  `5bps=-6.61%`; `eom_pullback` `53` trades, raw `-0.25%`,
  `2bps=-2.37%`, `5bps=-5.55%`; `eom_quality` `42` trades, raw `-0.88%`,
  `2bps=-2.56%`, `5bps=-5.08%`.
- Terminal decision: `drop_or_block_gate1_practical`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- EOM displacement got a fresh IBKR-first AQ pass in the current turn, but it
  fails before downstream because the edge is negative before costs. Do not run
  simulated-trade admission or Pre-Bayes/BBN/CatBoost/execution-tree on this
  root. The next IBKR-first work should repair a true `5bps/side` survivor such
  as M2K `1m` RVOL/PDA consistency floor or SI `5m` tight-range fade, or open a
  materially different fresh historical-data cell.

## Gate 1 Negative: Tomac XAU WideRange Breakout-Retest `1m`

Append-only terminal row at `2026-05-21 10:50 +0800` after routing through
`sd/ict-engi-fact-rese-muta`, rechecking the active claim/process state, and
confirming the run had completed with `checks/00_run_tomac.exit=0`.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T102100+0800-codex-tomac-xau-local-regime-rooted-mtf-gate1-v1/`.
- Scratch root:
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T102100+0800/`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T102020+0800-codex-tomac-xau-wide-range-breakout-retest-1m-gate1-v1.claim`.
- Runner:
  `support/docs/experiments/actionable-regime-confidence/scripts/run_tomac_xau_local_regime_rooted_mtf_gate1_v1.py`.
- Source data: local Tomac/GLBX XAU/GC `1m` CSV selected positive outright
  `GC*` rows, rejected `1,976,135` spread/non-outright rows, and staged
  `1,769,524` `1m` rows from `2021-01-06` through `2026-01-05`.
- Ladder evidence: per the operator clarification, `5m/15m/30m/1h/4h/1d`
  were synthesized from the exact staged `1m` source and recorded in
  `checks/mtf_context_coverage.json`; they are context/provenance for this
  `1m` lane, not independent provider proof.
- Canonical branch path:
  `WideRange -> WeeklyRangeExpansion -> WideRangeBreakoutRetest -> tomac_xau_wide_range_breakout_retest_1m_v1_balanced`.
  Labels/provenance: `market=futures`, `product=precious_metals`,
  `provider=Tomac local GLBX/COMEX`, `symbol=XAU/GC`, `timeframe=1m`,
  `window=20210106-20260105`.
- Gate 1 result: `880` trades, `0.686427/day`, win rate `33.4091%`,
  raw `-1.67%`, `1bps=-19.27%`, `2bps=-36.87%`, and `5bps=-89.67%`;
  `exact_1m_survivors_2bps=[]`, `exact_1m_survivors_5bps=[]`.
- Terminal decision: `observation_gate1_no_practical_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a true Gate 1 negative, not a data-ladder blocker: the higher
  timeframe ladder exists synthetically, but the exact `1m` branch is raw
  negative, below the target `1` to `3` trades/day density, and deeply negative
  after real-cost stress. Do not downstream, threshold-retune, or stack overlays
  on this exact branch. The next XAU work should run synthesized higher
  timeframes as their own timeframe-specific AQ lanes with separate terminal
  decisions, or rotate to a materially different public family.

## Gate 1 Observation: IBKR NVDA VWAP/RVOL Single-Stock Ladder

Append-only terminal row at `2026-05-21 10:56 +0800` after duplicate-scanning
the Board/matrix and reading the terminal claim plus
`checks/post_repair_terminal_metrics.json`.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T102507+0800-codex-ibkr-nvda-single-stock-vwap-rvol-gate1-v1/`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T102507+0800-codex-ibkr-nvda-single-stock-vwap-rvol-gate1-v1.claim`.
- Runner/material scope:
  `ibkr_nvda_vwap_reclaim_rvol_v1` under canonical branch
  `TrendExpansion -> SingleStockMomentum -> vwap_reclaim_rvol -> ibkr_nvda_vwap_reclaim_rvol_v1`.
  Labels/provenance: `market=equities`, `product=single_stock`,
  `provider=IBKR historical`, `symbol=NVDA`, ladder `1m/5m/15m/30m/1h`.
- Provider evidence: real IBKR rows acquired for `1m_10d=9600`,
  `5m_3m=12096`, `15m_3m=4032`, `30m_3m=2016`, and `1h_3m=1008`.
- AQ evidence: original managed batch hit a non-factor bootstrap path, then the
  local Auto-Quant repair completed with `05b_auto_quant_agent_material_batch_local=0`,
  `06b_auto_quant_agent_material_dispatch_local=0`, and
  `07b_auto_quant_agent_material_rank_local=0`; rank rows `5`.
- Gate 1 cost stress: `30m` had `27` trades, raw `+1.19%`,
  `2bps=+0.11%`, but `5bps=-1.51%` and density only `0.43/day`; `5m` had
  `67` trades, raw `+1.90%`, but `2bps=-0.78%`; `1m` had `9` trades,
  raw `+0.26%`, `2bps=-0.10%`, and `5bps=-0.64%`. No timeframe survived
  the hard `5bps/side` requirement.
- Terminal decision: `observation_only_no_practical_density_or_5bps_survivor`;
  `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- NVDA VWAP/RVOL is not a provider failure and not a downstream candidate.
  The best sibling only survives `2bps/side` with sub-target density, while the
  `1m` origin fails cost stress. Preserve it as single-stock momentum coverage
  evidence and rotate to a different factor leaf or a stronger same-root repair
  that can survive `5bps/side` before Pre-Bayes/BBN/CatBoost/execution-tree.

## Gate 1 Observation: IBKR QQQ Intraday Micro-Trend Reclaim Density Ladder

Append-only terminal row at `2026-05-21 11:22 +0800` after duplicate-scanning
the Board/matrix and reading the terminal claim plus
`checks/terminal_metrics.json`.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T102035+0800-hermes-ibkr-qqq-intraday-micro-trend-reclaim-density-1m-mtf-v1/`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1000+0800-codex-ibkr-qqq-intraday-micro-trend-reclaim-density-1m-mtf-v1.claim`.
- Factor id:
  `ibkr_qqq_intraday_micro_trend_reclaim_density_1m_mtf_v1`.
- Original artifact branch:
  `US -> equity_etf -> QQQ -> 1m -> Trend -> SessionLiquidity -> intraday_micro_trend_reclaim_density -> ibkr_qqq_intraday_micro_trend_reclaim_density_1m_mtf_v1`.
  Canonical regime-root reinterpretation for future work should drop the
  market/provider/symbol/timeframe prefix and keep
  `Trend -> SessionLiquidity -> intraday_micro_trend_reclaim_density -> ibkr_qqq_intraday_micro_trend_reclaim_density_1m_mtf_v1`;
  `US/equity_etf/QQQ/1m` are labels/provenance.
- Provider evidence: fresh real IBKR rows for `1m=6720`, `5m=4224`,
  `15m=1408`, `30m=704`, `1h=352`, `4h=88`, and `1d=251`; all fetch exits
  `0`, with `local_cache_replay=false`.
- AQ evidence: material batch, dispatch, and rank all exited `0`;
  `material_count=21`, `rank_rows=21`, branch fields were preserved.
- Gate 1 readback: the `1m` origin had `origin_survivors_2bps=[]`; best
  `1m` evidence did not earn downstream. The strongest sibling was
  `quality/QQQ/1h` with `8` trades, raw `+0.99%`, `2bps=+0.67%`,
  `5bps=+0.19%`, and `survives_5bps_per_side=true`. Other denser siblings
  failed `5bps/side`, including `dense/QQQ/5m` with `67` trades,
  raw `+3.31%`, `2bps=+0.63%`, but `5bps=-3.39%`.
- Terminal decision: `drop_gate1_no_ibkr_cost_density`;
  `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is not a provider failure; it is a timeframe-lane failure for the
  requested `1m` origin. The `1h` sibling is a possible future exact lane, but
  it cannot rescue the failed `1m` branch or justify downstream on this packet.
  If continued, promote `quality/QQQ/1h` only as its own
  timeframe-specific rooted Gate 1 lane with fresh cost/density and downstream
  admission evidence.

## Simulated-Trade Admission Observation: IBKR M2K RVOL/PDA Consistency-Floor Canonical Rerun

Append-only terminal row at `2026-05-21 11:02 +0800` after prioritizing the
active IBKR simulated-trade lane and letting the wrapper complete its own
timeout/readback sequence.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191436+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-canonical-gate1-v1/simulated-trade-admission-m2k-1m-rvol-pda-consistency-floor-canonical-20260521T102703+0800/`.
- Source run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260520T191436+0800-codex-ibkr-m2k1m-rvol-pda-consistency-floor-canonical-gate1-v1/`.
- Terminal metrics:
  `checks/simulated_trade_admission_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T-current-codex-ibkr-m2k1m-rvol-pda-consistency-floor-simulated-admission-v1.claim`.
- Canonical branch path:
  `RangeReversion -> LiquiditySweepRejectShort -> ibkr_m2k1m_liquidity_sweep_reject_short_7d_gate1_v1 -> ibkr_m2k1m_liquidity_sweep_reject_short_rvol_pda_guard_pda_consistency_floor_v1`.
- Same-workspace simulated feedback exported and ingested `17` trades:
  `11` wins, `6` losses, `0` breakevens.
- Command exits: import/prior/workflow/Pre-Bayes/export/policy, simulated-trade
  ingest, CatBoost train/apply, score apply, trainer registration, runtime
  enable, and final workflow/Pre-Bayes/policy readbacks exited `0`; both analyze
  calls timed out: `03_analyze_seed=124` and
  `16_analyze_after_ranker=124`.
- MTF evidence: analyze-valid ladder kept `1m=9392`, `5m=1879`,
  `15m=627`, `30m=314`, `1h=157`, and `4h=43` bars; `1d=9` was recorded as
  insufficient for the `29`-bar analyze feature minimum.
- Gate readback: `exact_branch_survived=false`, `mature_rows=2`,
  `history_mature_rows=18`, `ranker_validation_ready=false`,
  `execution_readiness=0.0`, `transition_hazard=1.0`,
  `pda_hybrid_alignment=false`, and no actionable execution candidate was
  materialized.
- Terminal decision: `rvol_pda_guard_simulated_trade_admission_fail_closed`;
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is useful same-root M2K repair evidence, but it is still observation-only.
  Simulated feedback exercised import, Pre-Bayes, policy export, CatBoost,
  score application, trainer registration, and runtime enablement, but it did
  not clear branch survival, validation, execution materialization,
  transition hazard, PDA alignment, or readiness gates. Do not paper-trade or
  promote this branch; the next same-root work must add acceptable mature/current
  feedback and materialize an exact execution candidate, or rotate to another
  real IBKR historical-data cell with a fresh `5bps/side` survivor.

## Gate 1 Observation: IBKR XOP Energy ETF Washout Reclaim Retained History

Append-only terminal row at `2026-05-21 11:40 +0800` after prioritizing the
IBKR historical-data lane over more simulated-feedback replay.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T112522+0800-codex-ibkr-xop-energy-etf-washout-reclaim-retained-gate1-v1/`.
- Source provider preflight:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T111022+0800-codex-ibkr-xop-energy-etf-provider-ladder-preflight-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; rank rows:
  `summaries/rank_rows.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T112522+0800-codex-ibkr-xop-energy-etf-washout-reclaim-retained-gate1-v1.claim`.
- Canonical branch path:
  `RangeReversion -> EnergyEtfWashoutReclaim -> xop_energy_etf_washout_reclaim_v1`.
  Labels remain separate: `market=US_EQUITY_ETF`, `product=energy_etf`,
  `provider=IBKR`, `symbol=XOP`, `sec_type=STK`, `primary_exchange=ARCA`.
- Retained real IBKR rows used: `1m=6720`, `5m=4222`, `15m=1408`,
  `30m=1992`, `1h=998`, `4h=527`, and `1d=251`;
  `local_cache_replay=false` and `retained_provider_history=true`.
- Command exits: wrapper compile, strategy compile, Auto-Quant material batch,
  dispatch, and rank all exited `0`.
- Gate 1 readback: `material_count=21`, `rank_rows=21`,
  `rank_total_trade_count=1609`, and `branch_fields_preserved=true`.
- Cost stress survivors: `2bps/side` survivors were `XOP/balanced/4h`,
  `XOP/quality/4h`, `XOP/balanced/30m`, `XOP/balanced/15m`,
  `XOP/quality/15m`, and `XOP/quality/30m`; `5bps/side` survivors were only
  `XOP/balanced/4h` and `XOP/quality/4h`.
- Terminal decision:
  `gate1_xop_energy_etf_washout_reclaim_cost_survivor_observation`;
  `pre_bayes_allowed=true` and `bbn_allowed=true` for the exact `4h`
  survivor lane only, while `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a real retained-IBKR historical Gate 1 survivor on the `4h` lane, not
  a provider-only row and not trade readiness. The `1m` origin and denser
  low-timeframe siblings did not survive `5bps/side`, so they must not be rescued
  by the `4h` result. If continued, downstream handoff should be scoped to the
  exact `XOP/balanced/4h` or `XOP/quality/4h` survivor with fresh admission
  readbacks; no simulated-trade admission should run until exact downstream first
  materializes the same-root execution candidate.

## Gate 1 Observation: Tomac ES/YM/NQ Synthetic MTF Shifted Counter Replay

Append-only terminal row at `2026-05-21 11:45 +0800` after exact Auto-Quant
replay completed for the previously strong local screen.

- Run root:
  `/private/tmp/tomac-synth-mtf-shifted-exact-aq-20260521`.
- Terminal metrics: `checks/terminal_metrics.json`; command exit:
  `checks/run_tomac.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1058+0800-codex-tomac-synth-mtf-shifted-n120-counter-exact-aq-v1.claim`.
- Canonical branch path:
  `TrendExpansion -> SyntheticMtfRange -> tomac_synth_mtf_shifted_mom_n120_counter_th2_side1_v1`.
  Labels remain separate: local Tomac clean index futures, synthetic `1m`
  replay, symbols `ES/YM/NQ`, window `2021-01-03` through `2025-12-31`.
- Screen validation before exact AQ was strong: `338` signals, gross
  `+83.292%`, net `5bps/side +66.392%`, train `2021-2023 +33.255%`, test
  `2024-2025 +33.137%`, and `4/5` positive net years.
- Exact AQ replay readback: `338` trades, raw `+30.36%`, win rate `62.72%`,
  profit factor `1.9806`, max drawdown `-3.9643%`, and per-pair profits
  `NQ +13.23%`, `ES +8.63%`, `YM +8.49%`.
- Cost/density stress: `1bps/side +23.60%`, `2bps/side +16.84%`,
  `5bps/side -3.44%`; AQ calendar-day density was only `0.1855` trades/day
  versus the `1-3` target.
- Terminal decision:
  `exact_aq_replay_gate1_fail_cost_density`; `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a useful screen-to-AQ negative sample. The screen survived train/test
  and yearly checks, but exact AQ slippage/cost and practical density gates
  failed. Do not downstream this branch. The next same-root attempt needs a
  denser or higher-excursion entry that can survive `5bps/side` under exact AQ,
  or the search should rotate to a materially different family.

## Exact Downstream Observation: IBKR XOP 4h Energy ETF Washout Reclaim

Append-only terminal row at `2026-05-21 11:45 +0800` after reading the exact
`4h` downstream packet for the `XOP/balanced/4h` Gate 1 survivor.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T112522+0800-codex-ibkr-xop-energy-etf-washout-reclaim-retained-gate1-v1/downstream-exact-xop-4h-washout-reclaim-20260521T114154+0800/`.
- Source Gate 1:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T112522+0800-codex-ibkr-xop-energy-etf-washout-reclaim-retained-gate1-v1/`.
- Terminal metrics: `checks/downstream_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; execution candidate:
  `state/IBKR_XOP_4H_ENERGY_ETF_WASHOUT_RECLAIM_DOWNSTREAM_V1/execution_candidate.json`;
  execution tree trace:
  `state/IBKR_XOP_4H_ENERGY_ETF_WASHOUT_RECLAIM_DOWNSTREAM_V1/execution_tree_trace.json`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T114154+0800-codex-ibkr-xop-4h-washout-reclaim-exact-downstream-v1.claim`.
- Canonical branch path:
  `RangeReversion -> EnergyEtfWashoutReclaim -> xop_energy_etf_washout_reclaim_v1`.
- Selected Gate 1 row: `XOP/balanced/4h`, `39` trades, win rate `69.2308%`,
  raw `+4.16%`, `2bps=+2.60%`, and `5bps=+0.26%`.
- Downstream command exits: Auto-Quant import/prior, seed analyze,
  workflow/pre-Bayes/export, CatBoost train/apply, score apply, trainer
  registration, runtime enable, after-ranker analyze, workflow/pre-Bayes/policy,
  and final export all exited `0`.
- Gate readback: `pre_bayes_allowed=true`, `bbn_allowed=true`,
  `catboost_allowed=true`, and `execution_tree_allowed=true`, but
  `exact_branch_survived=false`, execution candidate `candidate_status=no_trade`,
  `actionable=false`, `selected_direction=Bear`, while Gate 1 material was a
  long washout-reclaim survivor.
- Execution predicates: `execution_readiness=0.3137560308788672`,
  `transition_hazard=0.9036259813111243`, and
  `pda_hybrid_alignment=false`.
- Terminal decision: `exact_xop_4h_downstream_fail_closed`;
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- The exact `4h` branch earned a full downstream mechanical replay, but it fails
  the practical gate. The main blocker is not AQ mechanics; it is downstream
  admission: direction drift into a bearish no-trade candidate, high transition
  hazard, PDA disagreement, and readiness below `0.65`. Treat this as
  observation/repair evidence only. Do not run simulated-trade admission or
  promote this branch until a same-root variant preserves direction through AQ
  and execution, reduces `transition_hazard` below `0.60`, sets
  `pda_hybrid_alignment=true`, and lifts stable `execution_readiness >= 0.65`.

## Provider Preflight: IBKR UUP Dollar ETF Trend Pullback Ladder

Append-only terminal row at `2026-05-21 11:53 +0800` for the next fresh
IBKR-historical cell, started after the XOP downstream fail-closed result and
while AQ slots were busy.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T115037+0800-codex-ibkr-uup-dollar-etf-provider-ladder-preflight-v1/`.
- Metrics: `checks/provider_ladder_preflight_metrics.json`; row summary:
  `summaries/provider_rows.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T114859+0800-codex-ibkr-uup-dollar-etf-provider-ladder-preflight-v1.claim`.
- Candidate branch:
  `TrendExpansion -> DollarEtfTrendPullback -> uup_dollar_etf_keltner_reclaim_provider_ladder_preflight_v1`.
  Labels remain separate: `market=US_EQUITY_ETF`, `product=dollar_etf`,
  `provider=IBKR`, `symbol=UUP`, `sec_type=STK`, `primary_exchange=ARCA`.
- Real IBKR historical fetches all exited `0` and produced retained rows:
  `1m=5679` over `7 D`, `5m=3531` over `1 M`, `15m=1181` over `1 M`,
  `30m=1774` over `3 M`, `1h=891` over `3 M`, `4h=523` over `6 M`, and
  `1d=251` over `1 Y`.
- A preflight row-counter bug was repaired in the generated metrics:
  the raw IBKR CSV header is `ts`, not `timestamp`; `row_count_repair=count_ts_header_v1`.
- Decision: `provider_ladder_ready_for_aq_gate1`;
  `aq_gate1_ran=false`, `promotion_allowed=false`, `trade_usable=false`, and
  `update_goal=false`.

Interpretation:

- This is provider-ready only. It prepares a fresh, less-crowded IBKR
  historical-data cell for a later Auto-Quant Gate 1 cost-density run. It is not
  profitability evidence and does not justify Pre-Bayes/BBN/CatBoost/execution
  replay until AQ rank rows and cost stress exist.

## Gate 1 Negative: Tomac XAU WideRange Breakout-Retest Synthesized `5m`

Append-only terminal row at `2026-05-21 11:49 +0800` after rerunning the
Tomac local XAU/GC WideRange breakout-retest lane with the fixed
timeframe-unique material package id.

- Corrected run root:
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T113854+0800/`.
- Compact run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T113854+0800-codex-tomac-xau-local-regime-rooted-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; rank rows:
  `summaries/rank_rows.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exit:
  `checks/00_run_tomac.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1050+0800-codex-tomac-xau-wide-range-synthetic-ladder-gate1-v1.claim`.
- Source: local Tomac/GLBX XAU/GC `1m` CSV, normalized to the highest-volume
  positive outright `GC*` front row per timestamp; `1,769,524` staged `1m`
  rows. The `5m/15m/30m/1h/4h/1d` context ladder was synthesized from that
  staged `1m` source and marked as derived context, not independent provider
  proof.
- Corrected direct AQ lane: `5m`, material
  `tomac-xau-wide-range-breakout-retest-1m-v1-balanced-5m`, strategy
  `TomacXauWideRangeBreakoutRetestBalanced5MinV1`.
- Canonical branch path:
  `WideRange -> WeeklyRangeExpansion -> WideRangeBreakoutRetest -> tomac_xau_wide_range_breakout_retest_1m_v1_balanced`.
  Labels remain separate: provider `local_csv_tomac_databento_glbx`, market
  `futures`, product `precious_metals`, symbol `XAU/GC`, timeframe `5m`, window
  `20210106-20260105`.
- Gate 1 readback: `880` trades, `0.686427` trades/day, win rate `33.4091%`,
  profit factor `0.9103`, raw `-1.67%`, `1bps/side -19.27%`,
  `2bps/side -36.87%`, and `5bps/side -89.67%`.
- Terminal decision:
  `observation_gate1_no_practical_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.
- The earlier active six-timeframe root
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T104826+0800/` is not
  used for a factor verdict: its copied wrapper used the old package-id logic
  that collided all selected timeframe materials into one file, and its first
  AQ attempt exited without terminal metrics or rank rows.

Interpretation:

- The user-approved synthetic `5m` lane is now mechanically verified under the
  fixed material identity path and remains a clean Gate 1 negative. Do not run
  Pre-Bayes, BBN, CatBoost/path-ranker, execution tree, simulated admission, or
  promotion for this branch. Remaining synthesized siblings
  `15m/30m/1h/4h/1d` still need corrected per-timeframe runs before they can be
  classified.

## Gate 1 Negative: Tomac XAU WideRange Breakout-Retest Corrected Synthesized Full Ladder

Append-only terminal row at `2026-05-21 12:02 +0800` after the fixed
timeframe-unique wrapper completed the full user-approved synthesized ladder
for `5m/15m/30m/1h/4h/1d`.

- Corrected full-ladder run root:
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T115005+0800/`.
- Terminal metrics: `checks/terminal_metrics.json`; rank rows:
  `summaries/rank_rows.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Command exits: all six direct AQ lanes exited `0`:
  `5m`, `15m`, `30m`, `1h`, `4h`, and `1d`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1150+0800-codex-tomac-xau-wide-range-synth-15m-gate1-v1.claim`.
- Source: local Tomac/GLBX XAU/GC `1m` CSV, normalized to highest-volume
  positive outright `GC*` front rows; `1,769,524` staged `1m` rows. The
  `5m/15m/30m/1h/4h/1d` ladder is synthesized from that exact staged `1m`
  source and remains derived context, not independent provider proof.
- Canonical branch path:
  `WideRange -> WeeklyRangeExpansion -> WideRangeBreakoutRetest -> tomac_xau_wide_range_breakout_retest_1m_v1_balanced`.
  Labels remain separate: provider `local_csv_tomac_databento_glbx`, market
  `futures`, product `precious_metals`, symbol `XAU/GC`, timeframe, and window
  `20210106-20260105`.
- Per-timeframe Gate 1 readback:
  - `5m`: `192` trades, `0.149766` trades/day, raw `-0.23%`,
    `1bps=-4.07%`, `2bps=-7.91%`, `5bps=-19.43%`.
  - `15m`: `67` trades, `0.052262` trades/day, raw `+0.05%`,
    `1bps=-1.29%`, `2bps=-2.63%`, `5bps=-6.65%`.
  - `30m`: `25` trades, `0.019501` trades/day, raw `+1.69%`,
    `1bps=+1.19%`, `2bps=+0.69%`, `5bps=-0.81%`.
  - `1h`: `19` trades, `0.014821` trades/day, raw `+2.87%`,
    `1bps=+2.49%`, `2bps=+2.11%`, `5bps=+0.97%`.
  - `4h`: `13` trades, `0.010140` trades/day, raw `+1.32%`,
    `1bps=+1.06%`, `2bps=+0.80%`, `5bps=+0.02%`.
  - `1d`: `0` trades.
- No lane satisfied the combined cost-density gate. The `1h` and `4h` siblings
  survived `5bps/side` arithmetically, but their densities are far below the
  requested `1-3` trades/day target, so `gate1_5bps_density_survivor=false`.
- Terminal decision:
  `observation_gate1_no_practical_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This completes the corrected synthesized XAU WideRange ladder classification.
  Higher-timeframe positive cost rows are too sparse to rescue the branch, and
  failed lower-timeframe siblings remain failed. Do not downstream this branch or
  tune gates downward; preserve it as observation evidence and rotate to a
  denser 1m/5m family or a different fresh market cell.
- Duplicate single-lane confirmation: root
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T115857+0800/` reran
  the same `30m` lane with `checks/00_run_tomac_30m_TomacXauWideRangeBreakoutRetestBalanced30MinV1.exit=0`;
  readback matched the full-ladder row (`25` trades, `0.019501` trades/day,
  raw `+1.69%`, `2bps=+0.69%`, `5bps=-0.81%`, no combined survivor). Treat it
  as duplicate confirmation, not a new branch or downstream candidate.

## Gate 1 Observation: Tomac ES/YM/NQ Synthetic Multiday Shifted H5 Replay

Append-only terminal row at `2026-05-21 12:05 +0800` after exact Auto-Quant
replay completed for the stronger multiday shifted screen candidate.

- Run root:
  `/private/tmp/tomac-synth-mtf-multiday-shifted-exact-aq-20260521`.
- Terminal metrics: `checks/terminal_metrics.json`; corrected compact metrics:
  `checks/corrected_terminal_metrics.json`; command exit:
  `checks/run_tomac.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1145+0800-codex-tomac-synth-mtf-multiday-h5-exact-aq-v1.claim`.
- Canonical branch path:
  `TrendExpansion -> SyntheticMtfRange -> tomac_synth_mtf_multiday_shifted_mom_n120_h5_none_th5_side1_v1`.
  Labels remain separate: local Tomac clean index futures, synthetic `1m`
  replay, symbols `ES/YM/NQ`, window `2021-01-03` through `2025-12-31`,
  entry on next `1m` open after the first 120 closed `1m` bars, exit at the
  fifth RTH session close.
- Screen validation before exact AQ was very strong: `1637` signal rows,
  gross `+316.732%`, net `5bps/side +234.882%`, train `2021-2023 +72.057%`,
  test `2024-2025 +162.825%`, and `4/5` positive net years.
- Exact AQ replay readback: `1058` trades, raw `+59.03%`, win rate `57.2779%`,
  profit factor `1.3187`, max drawdown `-13.8002%`, and per-pair profits
  `NQ +28.78%`, `ES +19.58%`, `YM +10.67%`.
- Cost/density stress: `1bps/side +37.87%`, `2bps/side +16.71%`,
  `5bps/side -46.77%`; AQ calendar-day density was `0.5807` trades/day,
  below the requested `1-3` target.
- Terminal decision:
  `exact_aq_replay_gate1_fail_cost_density`; `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is useful because it shows a high-screen, multi-day shifted branch can
  preserve direction through exact AQ at raw/2bps, but it still fails the hard
  practical gate. Do not downstream this branch. The next Tomac synthetic
  candidate needs materially higher exact AQ profit per trade, lower turnover
  while keeping `1-3/day`, or a different rooted family that survives
  `5bps/side` before Pre-Bayes/BBN/CatBoost/execution-tree replay.

## Exact Downstream Observation: Tomac NQ OR15 Breakout Continuation

Append-only terminal row at `2026-05-21 12:20 +0800` after both OR15 exact
downstream wrappers produced terminal summaries.

- Exact downstream root:
  `/private/tmp/tomac-nq-or15-breakout-exact-aq-20260521/downstream-exact-nq-or15-breakout-20260521T114106+0800/`.
- Real-trade feedback downstream root:
  `/private/tmp/tomac-nq-or15-breakout-downstream-20260521/`.
- Source Gate 1 root:
  `/private/tmp/tomac-nq-or15-breakout-exact-aq-20260521`.
- Terminal metrics:
  `downstream-exact-nq-or15-breakout-20260521T114106+0800/checks/downstream_metrics.json`
  and `/private/tmp/tomac-nq-or15-breakout-downstream-20260521/checks/downstream_metrics.json`;
  terminal summaries:
  `downstream-exact-nq-or15-breakout-20260521T114106+0800/summaries/terminal_decision_summary.md`
  and `/private/tmp/tomac-nq-or15-breakout-downstream-20260521/summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1100+0800-codex-tomac-nq-or15-breakout-exact-aq-replay.claim`.
- Canonical branch path:
  `TrendExpansion -> OpeningRangeExpansion -> IntradayBreakoutContinuation -> tomac_nq_or15_breakout_continuation_h90_b0p0_max_extension_any_targeted_causal_v1`.
  Labels remain separate: local Tomac clean NQ futures, synthetic `1m` replay,
  window `2021-01-03` through `2025-12-31`.
- Source Gate 1 was strong: `1283` trades, gross `+345.9`, and
  `5bps/side +217.60`, so it earned downstream inspection.
- Exact downstream command readback: import/prior, workflow, Pre-Bayes,
  export-target, train/apply ranker, score apply, trainer registration, runtime
  enable, and manual readbacks exited `0`; seed analyze exited `-15` and the
  terminal summary classified from manual readbacks.
- Exact downstream decision:
  `exact_nq_or15_downstream_fail_closed_manual_readback`; exact branch did not
  survive, validation stayed `raw_scored_mature=1/30`, production `0/30`,
  observation `0/30`, execution actionable `false`, `execution_readiness=0.0`,
  `transition_hazard=1.0`, and `pda_hybrid_alignment=false`.
- Real-trade feedback downstream ingested/exported feedback evidence but did not
  rescue admission: exact replay feedback rows/wins/losses/breakevens were
  `1284 / 758 / 523 / 3`; timed-out commands were `03_analyze_seed`,
  `08_ingest_real_trades`, and `16_analyze_after_ranker`; final readback showed
  `exact_branch_survived=false`, `mature_rows=3`, `history_mature_rows=1287`,
  `ranker_validation_ready=false`, no actionable execution candidate,
  `execution_readiness=0.0`, `transition_hazard=1.0`, and
  `pda_hybrid_alignment=false`.
- Terminal decision for both packets: `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- OR15 is a strong Gate 1/screen lead, but it is not a practical branch yet.
  The current blocker is downstream same-root execution admission and validation,
  not initial AQ profitability. Do not promote, simulate-admit again, or lower
  gates. The next same-root repair would need exact execution candidate
  materialization, stable PDA alignment, transition hazard below `0.60`,
  readiness at least `0.65`, and enough mature validation rows to make the
  ranker/readiness gates meaningful.

## Exact Downstream Fail-Closed: Tomac NQ OR15 Breakout Continuation

Append-only terminal row at `2026-05-21 12:19 +0800` after both the exact
downstream replay and the real-trade feedback replay reached terminal metrics.

- Exact downstream root:
  `/private/tmp/tomac-nq-or15-breakout-exact-aq-20260521/downstream-exact-nq-or15-breakout-20260521T114106+0800/`.
- Real-trade feedback downstream root:
  `/private/tmp/tomac-nq-or15-breakout-downstream-20260521/`.
- Source Gate 1 root:
  `/private/tmp/tomac-nq-or15-breakout-exact-aq-20260521/`.
- Terminal metrics and summaries:
  `downstream-exact-nq-or15-breakout-20260521T114106+0800/checks/downstream_metrics.json`,
  `downstream-exact-nq-or15-breakout-20260521T114106+0800/summaries/terminal_decision_summary.md`,
  `/private/tmp/tomac-nq-or15-breakout-downstream-20260521/checks/downstream_metrics.json`,
  and `/private/tmp/tomac-nq-or15-breakout-downstream-20260521/summaries/terminal_decision_summary.md`.
- Canonical branch path:
  `TrendExpansion -> OpeningRangeExpansion -> IntradayBreakoutContinuation -> tomac_nq_or15_breakout_continuation_h90_b0p0_max_extension_any_targeted_causal_v1`.
  Labels remain separate: local Tomac clean NQ futures, synthetic `1m` replay,
  and the NQ material windows.
- Source Gate 1 was strong enough to justify downstream replay: `1283` trades,
  gross `+345.9`, `5bps/side +217.6`, and `survives_5bps=true`.
- Exact downstream command status: Auto-Quant import/prior, workflow,
  Pre-Bayes, export target, ranker train/apply, score apply, trainer
  registration, runtime enable, and manual readbacks exited `0`; the seed
  analyze was interrupted with `-15`.
- Exact downstream manual readback decision:
  `exact_nq_or15_downstream_fail_closed_manual_readback`. The path-ranker model
  became visible through a registered model artifact, but the exact branch did
  not survive into a real execution state: `exact_branch_survived=false`,
  `raw_scored_mature=1/30`, `production_validation=0/30`,
  `observation_validation=0/30`, execution `actionable=false`,
  `execution_readiness=0.0`, `transition_hazard=1.0`, and
  `pda_hybrid_alignment=false`.
- Real-trade feedback replay ingested/exported exact replay feedback rows
  (`1284` rows: `758` wins, `523` losses, `3` breakevens), trained/applied and
  registered a ranker, and enabled runtime; timed-out commands were
  `03_analyze_seed`, `08_ingest_real_trades`, and `16_analyze_after_ranker`.
- Real-trade feedback terminal decision:
  `tomac_nq_or15_downstream_fail_closed`; `exact_branch_survived=false`,
  `mature_rows=3`, `history_mature_rows=1287`, `ranker_validation_ready=false`,
  execution candidate absent, `execution_readiness=0.0`,
  `transition_hazard=1.0`, `pda_hybrid_alignment=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a valuable high-density/cost-surviving Gate 1 sample, but it still
  fails the user's hard downstream predicates. Do not promote it and do not
  lower gates. The next same-root repair would need exact execution-candidate
  materialization plus mature same-root validation and a direct fix to
  transition hazard/PDA/readiness; otherwise rotate to a different dense
  1m/5m branch.

## Corrected Gate 1 Readback: IBKR UUP Dollar ETF Keltner Reclaim

Append-only correction at `2026-05-21 12:13 +0800` after rerunning the UUP
Gate 1 terminal summarizer with fixed timeframe/window parsing. Earlier UUP
rows that treated `5m-1m` as a `1m` package label are superseded by this
readback: package ids encode `{timeframe}-{provider_window}`, so
`ibkr-uup-dollar-etf-keltner-reclaim-5m-1m-v1` means `5m` timeframe and
`1 M` provider window.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T121133+0800-codex-ibkr-uup-dollar-etf-keltner-reclaim-1m-mtf-gate1-v1/`.
- Source provider preflight:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T115037+0800-codex-ibkr-uup-dollar-etf-provider-ladder-preflight-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exits all `0` for strategy
  compile, Auto-Quant material batch, dispatch, and rank.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T121133+0800-codex-ibkr-uup-dollar-etf-keltner-reclaim-1m-mtf-gate1-v1.claim`.
- Canonical branch:
  `TrendExpansion -> DollarEtfTrendPullback -> dollar_etf_keltner_reclaim_v1`.
  Labels remain separate: `market=US_EQUITY_ETF`, `product=dollar_etf`,
  `provider=IBKR`, `symbol=UUP`, and timeframe/window provenance.
- Corrected cost rows: `1m` `19` trades raw `-0.07%`, `2bps=-0.83%`,
  `5bps=-1.97%`; `5m` `15` trades raw `+0.22%`, `2bps=-0.38%`,
  `5bps=-1.28%`; `15m` `5` trades raw `+0.36%`, `2bps=+0.16%`,
  `5bps=-0.14%`; `30m` `18` trades raw `+1.61%`, `2bps=+0.89%`,
  `5bps=-0.19%`; `1h` `9` trades raw `+1.33%`, `2bps=+0.97%`,
  `5bps=+0.43%`; `4h` and `1d` were negative.
- Decision: `gate1_higher_timeframe_cost_survivor_observation`;
  `origin_1m_survivors_2bps=[]`, `downstream_allowed=false`,
  `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- IBKR historical data path is proven for this UUP ladder, but the `1m` origin
  failed cost density. The sparse `1h` `5bps` survivor is observation-only and
  must not be used for simulated admission or downstream promotion. Cool this
  UUP/Keltner cell and rotate to a materially different family/cell unless a
  future exact same-root `1m`/dense lane first survives costs.

## Gate 1 Observation: IBKR AEP Regulated Utility Opening-Drive RVOL

Append-only terminal row at `2026-05-21 12:27 +0800` after prioritizing the
IBKR API historical-data path for a regulated-utility single-stock lane.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T121824+0800-codex-ibkr-aep-regulated-utility-opening-drive-rvol-gate1-v1/`.
- Terminal summary:
  `summaries/terminal_decision_summary.md`; provider matrix:
  `summaries/provider_provenance_matrix.csv`; rank artifact:
  `state/auto-quant/IBKR_AEP_REGULATED_UTILITY_OPENING_DRIVE_RVOL_GATE1/auto_quant_agent_material_rank.20260521T042151.617Z.json`.
  Structured summary JSON:
  `summaries/ibkr_aep_regulated_utility_opening_drive_rvol_gate1_v1.json`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T121754+0800-codex-ibkr-aep-regulated-utility-opening-drive-rvol-gate1-v1.claim`.
- Canonical branch:
  `TrendExpansion -> RegulatedUtilityOpeningDrive -> rvol_breakout_pullback -> ibkr_aep_regulated_utility_opening_drive_rvol_gate1_v1`.
  Labels remain separate: `market=US_EQUITY`, `provider=IBKR`, `symbol=AEP`,
  and timeframe/window provenance.
- Provider/API readback: `provider-status --provider ibkr --agent` exited `0`;
  IBKR `1m 30 D` fetch exited `3` and was downgraded to real `1m 10 D`
  (`8951` rows, exit `0`); `5m 3 M=10246` rows, `15m 3 M=3431` rows,
  `30m 3 M=1726` rows, and `1h 3 M=873` rows all fetched from IBKR with
  exits `0`.
- AQ mechanics completed: strategy compile, material batch, dispatch, and rank
  all exited `0`; `material_count=5`, `rank_rows=5`,
  `rank_total_trade_count=82`, and rooted branch fields were preserved.
- Gate 1 rank readback: `1m` `20` trades, win rate `30.0000%`, raw `-0.40%`,
  Sharpe `-3.5659`; `5m` `29` trades, win rate `37.9310%`, raw `+0.70%`,
  Sharpe `0.3994`; `15m` `18` trades, raw `-2.34%`; `30m` `10` trades,
  raw `-2.97%`; `1h` `5` trades, raw `-0.97%`.
- Decision from structured JSON: `drop_or_incubate_gate1_cost_failure`;
  the markdown terminal summary label says `keep_small_only`, but both artifacts
  agree on no downstream/promotion. Use the stricter structured decision for
  Board B classification. `downstream_allowed=false`,
  `simulated_admission_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This proves the IBKR historical-data path for AEP and gives a clean
  observation packet, but it does not earn simulated trade admission or
  downstream replay. Only the `5m` sibling was raw-positive, while the `1m`
  origin and higher-timeframe siblings were negative or too thin. Cool this
  exact AEP opening-drive RVOL cell; the next IBKR-first attempt should use a
  materially different symbol/family or a wider-excursion same-family hypothesis.

## Gate 1 Observation: Tomac ES/YM/NQ Portfolio Rolling Breakout Exact AQ

Append-only terminal row at `2026-05-21 12:31 +0800` after the inherited
portfolio rolling-breakout exact AQ replay finished on the local Tomac clean
index-futures 1m datasets.

- Run root:
  `/private/tmp/tomac-portfolio-rolling-breakout-exact-aq-20260521/`.
- Terminal metrics: `checks/terminal_metrics.json`; exact AQ stdout:
  `command-output/run_tomac.out`; command exit: `checks/run_tomac.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1215+0800-codex-tomac-portfolio-rolling-breakout-exact-aq-v1.claim`.
- Canonical branch:
  `TrendExpansion -> RollingBreakout -> tomac_portfolio_rolling_event_break_long_n15_holdd3_none_es_ym_nq_v1`.
  Labels remain separate: local Tomac clean index futures, symbols `ES/YM/NQ`,
  synthetic futures `1m`, window `2021-01-03 23:01:00` through
  `2025-12-31 00:00:00`, first rolling `15m` breakout per symbol/day, next
  `1m` open entry, and third RTH close exit.
- Screen validation before exact AQ was strong but noisy: `3726` signal rows,
  gross `+603.9604%`, net `5bps/side +231.3604%`, train `2021-2023 +60.5679%`,
  test `2024-2025 +170.7925%`, and `4/5` positive net years.
- Exact AQ replay readback: `3717` trades, `2.0401` trades/day, raw `+29.26%`,
  win rate `55.1251%`, profit factor `1.08`, Sharpe `0.9713`, and max drawdown
  `-13.2252%`; per-pair profits were `NQ +11.35%`, `YM +9.89%`, and
  `ES +8.02%`.
- Cost stress failed immediately: `1bps/side -45.08%`, `2bps/side -119.42%`,
  and `5bps/side -342.44%`. Trade density was inside the requested `1-3/day`
  band, but per-trade excursion was too small for realistic friction.
- Decision: `exact_aq_replay_gate1_fail_cost_density`; `downstream_allowed=false`,
  `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`, `trade_usable=false`,
  and `update_goal=false`.

Interpretation:

- This rolling-breakout portfolio branch preserves direction from screen into
  exact AQ only before costs. Because it fails even `1bps/side`, do not run
  Pre-Bayes/BBN/CatBoost/execution-tree or simulated admission. Future Tomac
  portfolio work needs materially fewer trades or much wider per-trade edge
  before downstream replay is justified.

## Gate 1 Observation: Tomac XAU EMA Pullback Synthetic 5m

Append-only terminal row at `2026-05-21 12:40 +0800` after classifying the
local Tomac XAU synthetic `5m` EMA pullback/reclaim lane requested from the
1m-origin GC/XAU CSV ladder.

- Run root:
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T122402+0800/`.
- Compact run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T122402+0800-codex-tomac-xau-local-regime-rooted-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; rank rows:
  `summaries/rank_rows.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exit
  `checks/00_run_tomac_5m_TomacXauMomentumContinuationEmaPullbackReclaimBalanced5MinV1.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1230+0800-codex-tomac-xau-ema-pullback-synth-5m-gate1-v1.claim`.
- Canonical branch:
  `MomentumContinuation -> ShallowPullback -> EmaPullbackReclaim -> tomac_xau_ema_pullback_reclaim_1m_v1_balanced`.
  Labels remain separate: local Tomac GC/XAU 1m CSV, highest-volume positive
  outright `GC*` selection, synthetic `5m` derived from `1m`, and window
  `2021-01-06` through `2026-01-05`.
- Source ladder readback: normalized `1m` rows `1,769,524`; synthetic coverage
  from the same `1m` origin was `5m=354,516`, `15m=118,173`, `30m=59,090`,
  `1h=29,559`, `4h=7,995`, and `1d=1,555`.
- Gate 1 readback for the selected `5m` lane: `4,776` trades,
  `3.725429/day`, win rate `42.7345%`, profit factor `0.8963`,
  Sharpe `-2.1785`, raw `-45.80%`, `1bps/side=-141.32%`,
  `2bps/side=-236.84%`, and `5bps/side=-523.40%`.
- Decision: `observation_gate1_no_practical_5bps_density_survivor`;
  `exact_1m_survivors_2bps=[]`, `exact_1m_survivors_5bps=[]`,
  `exact_mtf_survivors_2bps=[]`, `exact_mtf_survivors_5bps=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This branch is dense, but the edge is negative before costs and deteriorates
  sharply under friction. Do not downstream or repair this exact EMA pullback
  shape; cool the XAU EMA-pullback cell and rotate to a materially different
  family if more XAU work is needed.

## Gate 1 Observation: Bybit BTCUSDT Volatility Expansion Full Ladder

Append-only terminal row at `2026-05-21 12:42 +0800` after classifying the
Bybit public REST BTCUSDT volatility-expansion `1m`-origin full-ladder packet.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T122451+0800-codex-bybit-btcusdt-volatility-expansion-1m-full-ladder-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; cost stress:
  `checks/cost_stress.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; AQ rank artifact:
  `state/auto-quant/BYBIT_BTCUSDT_VOLATILITY_EXPANSION_1M_FULL_LADDER_V1/auto_quant_agent_material_rank.20260521T043224.580Z.json`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1230+0800-codex-bybit-btcusdt-volatility-expansion-1m-full-ladder-v1.claim`.
- Canonical branch:
  `TrendExpansion -> CryptoVolatilityExpansion -> volatility_expansion_1m_full_ladder -> bybit_btcusdt_volatility_expansion_1m_full_ladder_v1`.
  Labels remain separate: `market=crypto_linear_perp`, `provider=Bybit public REST`,
  `category=linear`, `symbol=BTCUSDT`, and ladder timeframes
  `1m/5m/15m/30m/1h/4h/1d`.
- Provider/AQ readback: fetch exits were `0` for all seven timeframes; validation
  exits were `0` except `1d` validation exit `1`; strategy compile, material
  batch, AQ dispatch, and AQ rank all exited `0`; rooted branch fields were
  preserved.
- Gate 1 rows by timeframe: `1m` `1` trade raw `-0.21%`; `5m` `3` trades raw
  `+0.97%`, `5bps=+0.67%`; `15m` `4` trades raw `-0.66%`; `30m` `7` trades
  raw `-4.74%`; `1h` `6` trades raw `-0.16%`; `4h` and `1d` had `0` trades.
- Decision: `drop_gate1_cost_or_density_failed`;
  `origin_1m_cost_survivors_2bps_side=[]`, `any_cost_survivors_2bps_side=[]`,
  `downstream_allowed=false`, `promotion_allowed=false`, `trade_usable=false`,
  and `update_goal=false`.

Interpretation:

- The `5m` sibling was positive after cost stress but had only `3` trades over
  the requested window, while the `1m` origin was negative and sparse. This is
  useful provider/AQ evidence only. Do not use the sparse positive sibling for
  simulated admission or downstream promotion.

## Gate 1 Observation: IBKR LQD Credit ETF Duration-Flight Reclaim

Append-only terminal row at `2026-05-21 12:47 +0800` after the active LQD
duration-flight reclaim Gate 1 run completed AQ dispatch and rank.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T123321+0800-codex-ibkr-lqd-credit-etf-duration-flight-reclaim-1m-mtf-gate1-v1/`.
- Source provider preflight:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T122512+0800-codex-ibkr-lqd-credit-etf-provider-ladder-preflight-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; AQ rank artifact:
  `state/auto-quant/IBKR_LQD_CREDIT_ETF_DURATION_FLIGHT_RECLAIM_1M_MTF_GATE1_V1/auto_quant_agent_material_rank.20260521T043629.787Z.json`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T123321+0800-codex-ibkr-lqd-credit-etf-duration-flight-reclaim-1m-mtf-gate1-v1.claim`.
- Canonical branch:
  `RangeReversion -> CreditEtfDurationFlightReclaim -> lqd_credit_etf_duration_flight_reclaim_v1`.
  Labels remain separate: `market=US_EQUITY_ETF`, `provider=IBKR`,
  `symbol=LQD`, and timeframe/window provenance.
- Provider rows reused from same-turn IBKR preflight with
  `local_cache_replay=false_reused_same_turn_ibkr_provider_preflight`: `1m 7 D`
  `5,986` rows, `5m 1 M` `3,485` rows, `15m 1 M` `1,165` rows, `30m 3 M`
  `1,679` rows, `1h 3 M` `852` rows, `4h 6 M` `492` rows, and `1d 1 Y`
  `251` rows.
- AQ mechanics completed: strategy compile, material batch, dispatch, and rank
  all exited `0`; `backend_busy=false`, `material_count=7`, and `rank_rows=7`.
- Gate 1 rank/cost readback: `1m` `15` trades raw `-0.16%`, `2bps=-0.76%`,
  `5bps=-1.66%`; `5m` `31` trades raw `-1.04%`, `2bps=-2.28%`,
  `5bps=-4.14%`; `15m` `17` trades raw `-1.52%`, `2bps=-2.20%`,
  `5bps=-3.22%`; `30m` `25` trades raw `-0.43%`, `2bps=-1.43%`,
  `5bps=-2.93%`; `1h` `13` trades raw `-0.55%`, `2bps=-1.07%`,
  `5bps=-1.85%`; `4h` `15` trades raw `+0.05%`, `2bps=-0.55%`,
  `5bps=-1.45%`; `1d` `4` trades raw `-1.63%`, `2bps=-1.79%`,
  `5bps=-2.03%`.
- Decision: `drop_gate1_no_cost_survivor`; `origin_1m_survivors_2bps=[]`,
  `survivors_2bps=[]`, `survivors_5bps=[]`, `downstream_allowed=false`,
  `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This proves the LQD IBKR provider/AQ ladder path, but every lane is negative
  after realistic cost stress. Do not downstream, simulate-admit, or overlay this
  exact duration-flight reclaim branch; rotate to a different credit ETF family
  or a different market cell.

## Gate 1 Observation: Tomac ES OR30 Tue/Thu 5m Exact AQ

Append-only terminal row at `2026-05-21 12:56 +0800` after classifying the
inherited Tomac ES opening-range `5m` exact AQ packet.

- Run root:
  `/private/tmp/tomac-es-or30-breakout-tue-thu-5m-exact-aq-20260521T1235/`.
- Compact root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T1235+0800-codex-tomac-es-or30-breakout-tue-thu-5m-exact-aq-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; exact AQ stdout:
  `command-output/run_tomac_es_or30_5m.out`; command exit:
  `checks/run_tomac_es_or30_5m.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1235+0800-codex-tomac-es-or30-breakout-tue-thu-5m-exact-aq-v1.claim`.
- Canonical branch:
  `TrendExpansion -> OpeningRangeExpansion -> OpeningRangeBreakout -> tomac_es_or30_breakout_tue_thu_5m_v1`.
  Labels remain separate: `market=futures`, `product=equity_index`,
  `provider=local_tomac_databento_clean`, `symbol=ES`, and `timeframe=5m`.
- Data fill readback: input rows `353,743`; filled rows `171,269`;
  output rows `525,012`; fill method
  `past_close_forward_fill_ohlc_volume_zero`; `future_lookahead=false`.
- Exact AQ stdout readback: `1,662` trades, `0.91/day`, win rate `61.9735%`,
  profit factor `1.0586`, Sharpe `0.4241`, Sortino `0.6442`,
  max drawdown `-9.8110%`, raw `+17.28%`, long/short split
  `793/869`, and command exit `0`.
- Cost stress from the same per-side rule used in Board B Gate 1 rows:
  `1bps/side=-15.96%`, `2bps/side=-49.20%`, and `5bps/side=-148.92%`.
  The `terminal_metrics.json` `rows=[]`, so the verdict is derived from the
  completed `run_tomac` stdout plus explicit cost stress arithmetic.
- Decision: `drop_or_observation_gate1`; `downstream_allowed=false`,
  `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This branch is directionally positive before costs and close to the requested
  daily density, but the per-trade excursion is too small. Do not downstream or
  simulate-admit this ES OR30 shape; any future opening-range ES work needs a
  much wider per-trade edge or materially fewer churn trades before replay.

## Gate 1 Observation: Tomac ES/YM/NQ Direct Auto-Quant Loop

Append-only terminal row at `2026-05-21 13:08 +0800` after classifying the
Tomac ES/YM/NQ staged local futures Auto-Quant loop packet.

- Run root:
  `/private/tmp/ict-engine-tomac-index-futures-aq-loop-20260521T123709+0800/`.
- Compact root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T123709+0800-codex-tomac-index-futures-aq-loop-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exit:
  `checks/round_00_run_tomac.exit=124`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T123617+0800-codex-tomac-es-ym-nq-autoquant-loop-fresh-v1.claim`.
- Scope labels: `market=futures`, `product=equity_index`, `symbols=ES/YM/NQ`,
  `provider=local_tomac_databento_clean`, timerange `20210101-20251231`.
  All context timeframes were staged from clean `1m` origin as
  `5m/15m/30m/1h/4h/1d`; those synthetic frames are labels/context, not branch
  roots.
- Source/staging readback: ES, YM, and NQ each had `1m` staged rows
  `2,625,060`; `5m=525,012`, `15m=175,004`, `30m=87,502`,
  `1h=43,751`, `4h=10,939`, and `1d=1,824`, with
  `future_lookahead=false`.
- AQ loop readback: round `0` exited `124`, rank rows `0`, `survivors_5bps=[]`,
  and no same-root cost-density survivor was produced.
- Decision: `autoquant_oracle_failed`; `downstream_allowed=false`,
  `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a mechanics/oracle timeout packet, not a positive or negative factor
  verdict. Do not promote, downstream, or duplicate the same loop until the
  specific `run_tomac.py` timeout path is repaired or narrowed to a smaller exact
  candidate.

## Gate 1 Observation: IBKR TLT Treasury ETF Duration-Flight Reclaim

Append-only terminal row at `2026-05-21 13:09 +0800` after the TLT
duration-flight reclaim ladder produced completed AQ ranking artifacts.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T124744+0800-codex-ibkr-tlt-treasury-duration-flight-reclaim-1m-mtf-gate1-v1/`.
- Source provider root:
  `/tmp/ict-engine-ibkr-credit-duration-historical-preflight-v1/`.
- Terminal metrics: `checks/terminal_metrics.json` was stale at
  `aq_backend_busy_material_ready`; completed AQ rank artifact:
  `state/auto-quant/IBKR_TLT_TREASURY_DURATION_FLIGHT_RECLAIM_1M_MTF_GATE1_V1/auto_quant_agent_material_rank.20260521T045828.075Z.json`;
  rank command exit: `checks/03_auto_quant_agent_material_rank.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T124744+0800-codex-ibkr-tlt-treasury-duration-flight-reclaim-1m-mtf-gate1-v1.claim`.
- Canonical branch:
  `RangeReversion -> TreasuryDurationFlightReclaim -> tlt_treasury_duration_flight_reclaim_v1`.
  Labels remain separate: `market=US_EQUITY_ETF`, `provider=IBKR`,
  `symbol=TLT`, and timeframe/window provenance.
- Provider rows reused from same-turn IBKR preflight with
  `local_cache_replay=false_reused_same_turn_ibkr_provider_preflight`: `1m 7 D`
  `2,730` rows, `5m 1 M` `1,716` rows, `15m 3 M` `1,638` rows,
  `30m 3 M` `819` rows, `1h 6 M` `848` rows, `4h 1 Y` `585` rows,
  and `1d 1 Y` `251` rows.
- AQ rank readback by timeframe: `1m` `4` trades raw `-0.08%`;
  `5m` `4` trades raw `-0.39%`; `15m` `5` trades raw `+0.06%`;
  `30m` `4` trades raw `+0.69%`, estimated `2bps/side=+0.53%`,
  `5bps/side=+0.29%`; `1h` `5` trades raw `+1.32%`,
  estimated `2bps/side=+1.12%`, `5bps/side=+0.82%`; `4h` `6` trades
  raw `-1.75%`; `1d` `1` trade raw `-0.83%`.
- Decision: `observation_gate1_sparse_cost_survivors_density_failed`;
  `origin_1m_survivors_2bps=[]`, `survivors_5bps=[30m,1h]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- The `30m` and `1h` siblings are cost-positive after estimated per-side stress,
  but only `4` and `5` trades over `3 M`/`6 M`; they do not meet the requested
  `1-3/day` density. The `1m` origin is negative. Preserve this as sparse
  observation evidence only; do not simulate-admit or downstream the TLT branch
  without a denser same-root survivor.

## Gate 1 Observation: Tomac XAU EMA Pullback Synthetic `15m`-`1d`

Append-only terminal row at `2026-05-21 13:03 +0800` after classifying the
local Tomac XAU synthetic EMA pullback/reclaim ladder for remaining frames
`15m/30m/1h/4h/1d`.

- Run root:
  `/private/tmp/ict-engine-tomac-xau-local-gate1-20260521T124125+0800/`.
- Compact root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T124125+0800-codex-tomac-xau-local-regime-rooted-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T124112+0800-codex-tomac-xau-ema-pullback-synth-15m-1d-gate1-v1.claim`.
- Canonical branch family:
  `MomentumContinuation -> ShallowPullback -> EmaPullbackReclaim -> tomac_xau_ema_pullback_reclaim_1m_v1_*`.
  Labels remain separate: local Tomac GC/XAU `1m` CSV, highest-volume positive
  outright `GC*` rows, synthetic timeframe, and window provenance.
- Source/staging readback: `1,769,524` staged `1m` rows from
  `2021-01-06` through `2026-01-05`; spread/non-outright rows rejected:
  `1,976,135`; synthetic context rows were `15m=118,173`, `30m=59,090`,
  `1h=29,559`, `4h=7,995`, and `1d=1,555`.
- Command readback: all `15` direct `run_tomac.py` commands exited `0`;
  rank rows `15`; rank total trade count `14,784`.
- Best raw positives still failed cost stress: `30m quality` raw `+8.81%`,
  `1bps=-13.05%`, `2bps=-34.91%`, `5bps=-100.49%`; `1h quality` raw
  `+9.68%`, `1bps=-6.76%`, `2bps=-23.20%`, `5bps=-72.52%`; `1h balanced`
  raw `+0.45%`, `1bps=-18.01%`.
- Decision: `observation_gate1_no_practical_5bps_density_survivor`;
  `exact_mtf_survivors_2bps=[]`, `exact_mtf_survivors_5bps=[]`,
  `exact_1m_survivors_2bps=[]`, `exact_1m_survivors_5bps=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- The EMA pullback family produced dense enough raw activity in lower frames,
  but all positive rows lived inside the cost envelope. This completes the
  remaining synthesized XAU EMA-pullback ladder as observation only. Do not
  downstream, simulate-admit, or keep mutating this exact XAU EMA-pullback cell
  without a materially wider per-trade excursion hypothesis.

## Gate 1 Observation: Tomac NQ Prevday Continuation Exact AQ

Append-only terminal row at `2026-05-21 13:16 +0800` after the NQ causal
previous-day continuation exact AQ replay finished.

- Run root:
  `/private/tmp/tomac-nq-prevday-continuation-exact-aq-20260521/`.
- Terminal metrics: `checks/terminal_metrics.json`; command exit:
  `checks/run_tomac.exit=1`; stdout/stderr:
  `command-output/run_tomac.out` and `command-output/run_tomac.err`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1234+0800-codex-tomac-nq-prevday-continuation-exact-aq-v1.claim`.
- Canonical branch:
  `TrendExpansion -> SessionDirectionalBias -> CausalRthContinuation -> tomac_nq_causal_prevday_continuation_th0p0_all_v3`.
  Labels remain separate: `market=US_INDEX_FUTURES`, `product=CME_E_MINI_INDEX_FUTURES`,
  `symbol=NQ`, `base_timeframe=1m`, and synthetic context
  `5m/15m/30m/1h/4h/1d`.
- Signal validation readback before exact AQ: `1,243` signals,
  `0.9983935742971888` trades/RTH session day, gross `+699.4159%`,
  `2bps=+649.6959%`, `5bps=+575.1159%`, and all `2021-2025` yearly
  `5bps` buckets positive. This is diagnostic signal evidence only.
- Exact AQ readback: `run_tomac_exit=1`, `run_tomac_timed_out=false`,
  `trade_count=0`, and `gate1_survives_5bps=false`; stdout ended with
  `InvalidOrderException` because `Stake amount 50078523.895110786 too high for NQ/USD`.
- Decision: `exact_aq_replay_failed`; `downstream_allowed=false`,
  `pre_bayes_allowed=false`, `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- The standalone signal reconstruction is strong but cannot be promoted because
  the exact Auto-Quant replay failed before producing executable trades. Treat
  this as a config/mechanics repair lead, not a factor survivor; next work must
  fix the NQ futures stake/leverage sizing path and rerun exact AQ before any
  downstream or admission step.

## Gate 1 Observation: Tomac NQ Synthetic-MTF Vector Screen

Append-only terminal row at `2026-05-21 13:20 +0800` after classifying the
local NQ synthetic-MTF vector candidate screen.

- Run root:
  `/private/tmp/ict-engine-tomac-nq-synth-mtf-vector-gate1-20260521T130505+0800/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; candidate rank:
  `summaries/candidate_rank.csv`; survivor rank:
  `summaries/survivor_rank.csv`.
- Source CSV:
  `/Users/thrill3r/Downloads/Tomac/nq future 2021-2025/NQ_1min_Continuous_Shifted_2836.csv`.
- Scope labels: `market=US_INDEX_FUTURES`, `product=CME_E_MINI_INDEX_FUTURES`,
  `symbol=NQ`, `base_timeframe=1m`, analysis window `2021-01-01` through
  `2025-12-31`, and synthetic context `5m/15m/30m/1h/4h/1d` derived from `1m`.
  Synthetic frames are same-source context, not independent provider parity.
- Candidate readback: `candidate_count=7`, `rth_session_count=1290`,
  `gate1_survivor_count=0`. Best raw rows still failed cost/OOS gates:
  `RangeReversion -> LiquiditySweepReclaim -> tomac_nq_vwap_sweep_reclaim_synth_mtf_v1`
  had `1,716` trades, `1.330/session`, raw `+39.97%`, `2bps=-28.67%`,
  `5bps=-131.63%`, OOS `5bps=-61.78%`; `MomentumContinuation -> EmaPullbackReclaim -> tomac_nq_ema_pullback_reclaim_synth_mtf_v1`
  had `1,529` trades, `1.185/session`, raw `+25.07%`, `2bps=-36.09%`,
  `5bps=-127.83%`.
- Decision: `observation_no_cost_density_oos_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This was candidate discovery only, not exact Auto-Quant/Freqtrade proof. The
  density is acceptable for some rows, but every candidate flips negative after
  realistic costs and OOS stress. Do not downstream; use it only as evidence
  that these NQ synthetic-MTF shapes need materially wider per-trade excursion.

## Gate 1 Observation: Tomac ES/YM/NQ Bounded Direct Auto-Quant Loop

Append-only terminal row at `2026-05-21 13:21 +0800` after classifying the
bounded local Tomac ES/YM/NQ direct Auto-Quant loop.

- Run root:
  `/private/tmp/ict-engine-tomac-index-futures-aq-loop-20260521T125839+0800/`.
- Compact root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T125839+0800-codex-tomac-index-futures-aq-loop-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exit:
  `checks/round_00_run_tomac.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T125837+0800-codex-tomac-es-ym-nq-autoquant-loop-bounded-v1.claim`.
- Scope labels: `market=futures`, `product=equity_index`, `symbols=ES/YM/NQ`,
  `provider=local_tomac_csv`, timerange `20210101-20251231`, with
  `5m/15m/30m/1h/4h/1d` context derived from staged `1m`.
- AQ readback: round `0` exited `0`, `rank_rows=12`, `survivors_5bps=[]`,
  and best `5bps` row was still negative at `-7.75%`.
- Best raw rows: aggregate
  `RangeReversion -> LiquiditySweepReclaim -> VwapSweepReclaim -> tomac_idxfut_aq_range_vwap_sweep_reclaim_long_1m_r0`
  had `1,896` trades, `1.505/day`, raw `+3.33%`, `2bps=-72.51%`,
  `5bps=-186.27%`; per-pair NQ had `612` trades, raw `+3.95%`,
  `2bps=-20.53%`, `5bps=-57.25%`. ES opening-drive RVOL was raw `+5.26%`
  but `2bps=-9.74%`, `5bps=-32.24%`.
- Decision: `observation_no_autoquant_survivor_yet`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This bounded loop proved direct Auto-Quant mechanics after the earlier timeout,
  but every row is inside the cost envelope. The root families are useful
  negative evidence only; do not downstream or simulate-admit them without a new
  exact-root candidate that survives real-cost density first.

## Gate 1 Carry-Forward: Tomac YM Prevday Continuation Exact AQ

Append-only Gate 1 carry-forward row at `2026-05-21 13:25 +0800` after reading
the corrected YM previous-day continuation exact AQ parity artifact. This is not
a promotion row; downstream readbacks are still required.

- Run root:
  `/private/tmp/tomac-ym-prevday-continuation-exact-aq-20260521T1305/`.
- Corrected Gate 1 artifact:
  `checks/gate1_parity_corrected.json`; original terminal metrics:
  `checks/terminal_metrics.json`; command exit: `checks/run_tomac.exit=0`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T1305+0800-codex-tomac-ym-prevday-continuation-exact-aq-v1.claim`.
- Canonical branch:
  `TrendExpansion -> SessionDirectionalBias -> CausalRthContinuation -> tomac_ym_causal_prevday_continuation_th0p0_all_v1`.
  Labels remain separate: `market=US_INDEX_FUTURES`, `product=CME_MINI_INDEX_FUTURES`,
  `symbol=YM`, and `base_timeframe=1m`.
- Exact AQ readback: `1,239` trades, gross `+11519.09%`,
  `1bps/side=+11494.31%`, `2bps/side=+11469.53%`,
  `5bps/side=+11395.19%`, win rate `76.594%`, profit factor `7.9525`,
  Sharpe `4.5194`, and run exit `0`.
- Parity correction: wrapper text residue reported `symbol_scope=NQ` and
  calendar density false, but corrected artifact identifies per-pair/signals as
  YM with `rth_session_density=0.9951807228915662`,
  `signal_density=0.9959839357429718`, and
  `density_gate_roughly_1_to_3_daily=true`.
- Decision: `exact_aq_gate1_cost_density_survivor_downstream_required`;
  `downstream_allowed=true`, `promotion_allowed=false`, `trade_usable=false`,
  and `update_goal=false`. Required next gates remain same-root direction
  consistency, mature validation rows, `transition_hazard < 0.60`,
  `pda_hybrid_alignment=true`, and stable `execution_readiness >= 0.65`.

Interpretation:

- This is a legitimate same-root Gate 1 survivor and should continue into the
  filter/BBN/CatBoost/execution-tree chain. It is still not trade-usable until
  the downstream run proves direction consistency and clears the hard execution
  predicates without lowering thresholds.

## Gate 1 Observation: IBKR MYM5M Opening-Range Expansion Failure

Append-only terminal row at `2026-05-21 13:25 +0800` after reading the completed
MYM 5m opening-range expansion-failure Gate 1 packet.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260521T130936+0800-codex-ibkr-mym5m-opening-range-expansion-failure-1m-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260521T130936+0800-codex-ibkr-mym5m-opening-range-expansion-failure-1m-gate1-v1.claim`.
- Canonical branch:
  `AuctionMarketStructure -> OpeningRangeExpansionFailure -> ibkr_mym5m_opening_range_expansion_failure_1m_gate1_v1`.
  Labels remain separate: `provider=IBKR`, `product=equity_index`,
  `symbol=MYM`, `contract=202606`, `timeframe=5m`, and provider duration `1 M`.
- Command exits: provider status, IBKR fetch, strategy compile, material batch,
  dispatch, and rank all exited `0`.
- Provider readback: `5,880` retained real IBKR rows from
  `data/provider/normalized/ibkr_mym_202606_5m_1m.csv`;
  `local_cache_replay=false`.
- AQ/cost readback: `rank_rows=4`, total trades across ranked exact rows `2`.
  Best rows were `MYM/failed_breakdown_reclaim_dense/5m` and
  `MYM/failed_breakdown_reclaim_quality/5m`, each with `1` trade, raw `+0.13%`,
  `1bps/side=+0.11%`, `2bps/side=+0.09%`, and `5bps/side=+0.03%`.
  The two breakout-fade rows had `0` trades.
- Decision: `drop_or_block_gate1_practical`;
  `exact_5m_survivors_2bps=[]`, `exact_5m_survivors_5bps=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`, `bbn_allowed=false`,
  `catboost_allowed=false`, `execution_tree_allowed=false`,
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a clean Gate 1 terminal negative, not a provider or mechanics failure.
  Branch identity was preserved, but trade density is far below the practical
  gate and no downstream step is allowed.

## Gate 1 Observation: Bybit OP/APT Stop-Run Reclaim Full-Ladder Drop

Append-only terminal row at `2026-05-22 02:06 +0800` after rechecking the
post-cleanup OP/APT run root, claim, and hard `5bps/side + density` audit.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T015311+0800-codex-bybit-op-apt-stoprun-reclaim-1m-full-ladder-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; hard audit:
  `checks/hard_5bps_density_audit.json`; cost stress:
  `checks/cost_stress.json`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T015136+0800-codex-bybit-op-apt-stoprun-reclaim-gate1.claim`.
- Canonical branch:
  `RangeReversion -> CryptoStopRunReclaim -> one_minute_stoprun_reclaim_full_ladder -> bybit_op_apt_stoprun_reclaim_1m_full_ladder_v1`.
  Labels remain separate: `provider=Bybit public linear`,
  `symbols=OPUSDT/APTUSDT`, and `timeframes=1m/5m/15m/30m/1h/4h/1d`.
- Command readback: `14` real Bybit public linear ladder legs were acquired;
  strategy compile, material batch, dispatch, and rank exited `0`;
  `rank_rows=14`, `rank_total_trade_count=25`, and
  `branch_fields_preserved=true`.
- Exact `1m` origin cost verdict: failed Gate 1. `APTUSDT/1m` had `3` trades,
  raw `-0.20%`, `2bps/side=-0.32%`, and `5bps/side=-0.50%`;
  `OPUSDT/1m` had `1` trade, raw `-0.16%`, `2bps/side=-0.20%`, and
  `5bps/side=-0.26%`.
- Higher-timeframe context only: sparse positive `5bps/side` rows were
  `APTUSDT/30m` with `2` trades and `+2.19%`, `OPUSDT/30m` with `1` trade and
  `+1.09%`, `OPUSDT/1h` with `1` trade and `+1.09%`, and `OPUSDT/15m` with
  `5` trades and `+0.07%`. The hard audit reports
  `origin_1m_cost_survivors_5bps_density=[]` and
  `any_cost_survivors_5bps_density=[]`.
- Decision: `drop_gate1_no_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a terminal Gate 1 negative for the OP/APT `1m` origin root. Do not
  downstream, simulate-admit, or use the sparse higher-timeframe positives to
  rescue the failed exact root; rotate to a materially different claimed cell.

## Gate 1 Observation: IBKR MSTR 1m Opening-Drive Trend-Pullback Drop

Append-only terminal row at `2026-05-22 02:22 +0800` after rechecking the
MSTR run root, small-cycle terminal row, diversity matrix row, and hard
`5bps/side + density` readback.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T020917+0800-codex-ibkr-mstr1m-opening-drive-trend-pullback-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; cost stress:
  `summaries/rank_rows_cost_stress.csv`; provider provenance:
  `summaries/provider_provenance_matrix.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T020505+0800-codex-ibkr-mstr1m-opening-drive-trend-pullback-gate1.claim`.
- Canonical branch:
  `TrendExpansion -> HighBetaEquityOpeningDrive -> opening_drive_trend_pullback -> ibkr_mstr1m_opening_drive_trend_pullback_gate1_v1`.
  Labels remain separate: `provider=IBKR`, `market=US_EQUITY`,
  `product=bitcoin_treasury_high_beta`, `symbol=MSTR`, and `timeframe=1m`.
- Provider/AQ readback: real IBKR rows were acquired for the full context
  ladder `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11574/4889/1630/815/853/585/501`; provider status, fetches, strategy
  compile, Auto-Quant material batch, dispatch, and rank exited `0`;
  `rank_rows=4`, `rank_total_trade_count=4`, `local_cache_replay=false`, and
  `branch_fields_preserved=true`.
- Exact `1m` Gate 1 readback: each of `opening_drive_dense`,
  `opening_drive_balanced`, `trend_pullback_reclaim`, and
  `late_morning_continuation` had `1` trade over `30` observation days
  (`0.033333/day`), raw `-1.14%`, `1bps/side=-1.16%`,
  `2bps/side=-1.18%`, and `5bps/side=-1.24%`;
  `exact_1m_survivors_2bps_density=[]` and
  `exact_1m_survivors_5bps_density=[]`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a clean real-provider/AQ Gate 1 negative, not a provider or
  mechanics failure. Cool down the exact MSTR high-beta opening-drive
  trend-pullback cell; do not downstream, simulate-admit, promote, or lightly
  retune this packet.

## Gate 1 Carry-Forward: Bybit ATOMUSDT 15m SuperTrend-ADX Exact

Append-only Gate 1 carry-forward row at `2026-05-22 02:23 +0800` after reading
the completed independent Bybit `ATOMUSDT/15m` SuperTrend-ADX exact-timeframe
packet.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T021812+0800-codex-bybit-atomusdt-15m-supertrend-adx-exact-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; cost stress:
  `checks/cost_stress_table.json`; provider provenance:
  `summaries/provider_provenance_matrix.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T021404+0800-codex-bybit-atomusdt15m-supertrend-adx-exact-gate1.claim`.
- Canonical branch:
  `TrendExpansion -> CryptoSuperTrendAdxContinuation -> bybit_atomusdt_supertrend_adx_15m_exact_v1`.
  Labels remain separate: `provider=bybit_public`, `category=linear`,
  `symbol=ATOMUSDT`, and `timeframe=15m`.
- Provider/AQ readback: Bybit public REST retained `1000` `ATOMUSDT/15m`
  rows for `2026-05-06 14:15:00+00:00` through
  `2026-05-17 00:00:00+00:00` after retrying transient SSL errors; market-state
  validation, strategy compile, Auto-Quant material batch, dispatch, and rank
  exited `0`; `rank_rows=1`; `branch_fields_preserved=true`.
- Exact `15m` Gate 1 readback: `17` trades, win rate `52.9412%`, raw
  `+3.96%`, `1bps/side=+3.62%`, `2bps/side=+3.28%`, and
  `5bps/side=+2.26%`; `exact_15m_survives_5bps=true`.
- Decision: `keep_exact_15m_gate1_cost_survivor`;
  `downstream_allowed=true`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a real same-root Gate 1 survivor and should continue into the
  Auto-Quant import/prior, analyze, Pre-Bayes/BBN, CatBoost/path-ranker, and
  execution-tree readback chain. It is not trade-usable until downstream proves
  same-root direction consistency, mature validation, `transition_hazard < 0.60`,
  `pda_hybrid_alignment=true`, and stable `execution_readiness >= 0.65`.

## Downstream Decision: Bybit ATOMUSDT 15m SuperTrend-ADX Exact Fail-Closed

Append-only downstream row at `2026-05-22 02:36 +0800` after classifying the
completed same-root Bybit `ATOMUSDT/15m` SuperTrend-ADX downstream chain.

- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T021812+0800-codex-bybit-atomusdt-15m-supertrend-adx-exact-gate1-v1/`.
- Downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T021812+0800-codex-bybit-atomusdt-15m-supertrend-adx-exact-gate1-v1/downstream-exact-atomusdt-15m-supertrend-adx-20260522T022844+0800/`.
- Downstream metrics: `checks/downstream_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T0229+0800-codex-bybit-atomusdt15m-supertrend-adx-exact-downstream.claim`.
- Canonical branch:
  `TrendExpansion -> CryptoSuperTrendAdxContinuation -> bybit_atomusdt_supertrend_adx_15m_exact_v1`.
  Labels remain separate: `provider=bybit_public`, `category=linear`,
  `symbol=ATOMUSDT`, and `timeframe=15m`.
- Downstream command readback: Auto-Quant import/prior, analyze,
  workflow-status, Pre-Bayes, structural path target export, policy status,
  CatBoost train/apply, score import, trainer registration, runtime enable,
  ranker refresh analyze, workflow refresh, and final policy readback all
  exited `0`; `all_command_exits_zero=true`.
- Same-root readback: `rank_rows=1`, `rank_total_trades=17`,
  `path_ranker_score_visible_to_execution_tree=true`, and the rooted branch
  stayed `TrendExpansion -> CryptoSuperTrendAdxContinuation ->
  bybit_atomusdt_supertrend_adx_15m_exact_v1`.
- Admission readback: `execution_candidate_actionable=false`,
  `execution_candidate_status=no_trade`, `execution_tree_gate_status=observe`,
  `execution_tree_branch=transition_guardrail`,
  `execution_readiness=0.5468224457465702`,
  `transition_hazard=0.6549902291111538`,
  `pda_hybrid_alignment=true`, `ranker_validation_ready=false`,
  `mature_rows=0`, `history_mature_rows=0`, and
  `path_ranker_score_used_by_execution_tree=false`.
- Decision: `observation_downstream_fail_closed_transition_guardrail`;
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- ATOMUSDT/15m SuperTrend-ADX remains a useful same-root observation packet:
  Gate 1 survived hard `5bps/side`, and the downstream chain ran cleanly, but
  it failed the practical execution gates. Do not promote, simulate-admit, or
  lower thresholds from this packet; any future repair must reduce
  `transition_hazard` below `0.60`, lift `execution_readiness` to at least
  `0.65`, keep PDA aligned, and add mature same-root validation.

## Gate 1 Observation: IBKR SMCI 1m BB-Keltner Squeeze Breakout Drop

Append-only terminal row at `2026-05-22 02:25 +0800` after reading the completed
IBKR `SMCI/1m` BB-Keltner squeeze breakout Gate 1 packet.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T022232+0800-codex-ibkr-smci1m-bb-keltner-squeeze-breakout-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; cost stress:
  `summaries/rank_rows_cost_stress.csv`; provider provenance:
  `summaries/provider_provenance_matrix.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T021959+0800-codex-ibkr-smci1m-bb-keltner-squeeze-breakout-gate1.claim`.
- Canonical branch:
  `VolatilityCompression -> HighBetaEquitySqueezeBreakout -> bb_keltner_squeeze_breakout -> ibkr_smci1m_bb_keltner_squeeze_breakout_gate1_v1`.
  Labels remain separate: `provider=IBKR`, `market=US_EQUITY`,
  `product=ai_server_high_beta`, `symbol=SMCI`, and `timeframe=1m`.
- Provider/AQ readback: real IBKR rows were acquired for the full context
  ladder `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11588/4892/1631/816/854/585/502`; provider status, fetches, strategy
  compile, Auto-Quant material batch, dispatch, and rank exited `0`;
  `rank_rows=4`, `rank_total_trade_count=35`, `local_cache_replay=false`, and
  `branch_fields_preserved=true`.
- Exact `1m` Gate 1 readback: `squeeze_retest_reclaim` had `9` trades,
  raw `-0.24%`, `2bps/side=-0.60%`, and `5bps/side=-1.14%`;
  `squeeze_dense` had `10` trades, raw `-0.66%`, `2bps/side=-1.06%`, and
  `5bps/side=-1.66%`; `squeeze_expansion_quality` had `8` trades,
  raw `-0.13%`, `2bps/side=-0.45%`, and `5bps/side=-0.93%`;
  `squeeze_balanced` had `8` trades, raw `-0.53%`, `2bps/side=-0.85%`, and
  `5bps/side=-1.33%`. `exact_1m_survivors_2bps_density=[]` and
  `exact_1m_survivors_5bps_density=[]`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a clean real-provider/AQ Gate 1 negative. The exact `1m` branch has
  enough sample to judge, but all rows are raw-negative and worsen under cost
  stress. Do not downstream, simulate-admit, promote, or lightly retune this
  SMCI squeeze packet.

## Gate 1 Observation: IBKR KO/PEP 1m Relative-Value Z-Score Drop

Append-only terminal row at `2026-05-22 02:39 +0800` after rechecking the
completed IBKR `KO/PEP` consumer-staples pair relative-value z-score Gate 1
packet, its small-cycle terminal row, and diversity matrix row.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T022417+0800-codex-ibkr-ko-pep-relative-value-zscore-1m-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; cost stress:
  `summaries/rank_rows_cost_stress.csv`; provider provenance:
  `summaries/provider_provenance_matrix.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T021922+0800-codex-ibkr-ko-pep-relval-zscore-1m-gate1.claim`.
- Canonical branch:
  `RangeReversion -> ConsumerStaplesRelativeValue -> ko_pep_relative_value_zscore_1m -> ibkr_ko_pep_relative_value_zscore_1m_gate1_v1`.
  Labels remain separate: `provider=IBKR`, `market=US_EQUITY`,
  `product=consumer_staples_pair`, `root_symbol=KO`, `paired_symbol=PEP`,
  and `timeframe=1m`.
- Provider/AQ readback: real IBKR rows were acquired for both `KO` and `PEP`
  across `1m/5m/15m/30m/1h/4h/1d`; common spread rows were
  `1m=11589`, `5m=4892`, `15m=1631`, `30m=816`, `1h=854`, `4h=585`, and
  `1d=501`; provider status, fetches, strategy compile, Auto-Quant material
  batch, dispatch, and rank exited `0`; `rank_rows=4`;
  `local_cache_replay=false`; `branch_fields_preserved=True`.
- Exact `1m` Gate 1 readback: `dense_reclaim` had `68` trades
  (`2.266667/day`) but raw only `+0.07%`, `2bps/side=-2.65%`, and
  `5bps/side=-6.73%`; `quality_reclaim` had `29` trades, raw `-0.02%`,
  `2bps/side=-1.18%`, and `5bps/side=-2.92%`; `balanced_reclaim` had `50`
  trades, raw `-0.22%`, `2bps/side=-2.22%`, and `5bps/side=-5.22%`;
  `flush_reclaim` had `4` trades, raw `-0.15%`, `2bps/side=-0.31%`, and
  `5bps/side=-0.55%`. `exact_1m_survivors_2bps_density=[]` and
  `exact_1m_survivors_5bps_density=[]`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a clean real-provider/AQ Gate 1 negative for a public pair
  relative-value diversity slot. The dense row has practical trade density but
  lives inside the cost envelope; do not downstream, simulate-admit, promote, or
  lightly retune this exact KO/PEP pair-z-score root.

## Gate 1 Observation: TOMAC NQ 1m Session Seasonality Exact AQ Drop

Append-only terminal row updated at `2026-05-22 02:53 +0800` after repairing and
reading the scanner-equivalent exact TOMAC `NQ/1m` session-seasonality AQ
replay. The earlier r4 readback is stale and invalidated by the r5
signal-parity repair.

- Run root:
  `/tmp/ict-engine-tomac-session-seasonality-aq-20260522-r5`.
- Terminal metrics: `checks/terminal_metrics.json`; rank rows:
  `summaries/rank_rows.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-local-codex-tomac-session-seasonality-scan.claim`.
- Canonical branch:
  `SessionRhythm -> TimeOfDaySeasonality -> AdaptiveSlotContrarian`.
  `TOMAC`, local `NQ`, source CSV, futures mode, and `1m` are provenance labels,
  not branch roots.
- Replay hygiene:
  r2 was wrapper-blocked by a 12-column OHLCV+signal feather; r4 repaired the
  loader schema but used `122` pre-parity sidecar signals and is not the final
  factor verdict. r5 used a 6-column OHLCV feather plus scanner-equivalent RTH
  slot-return `tod_signals.feather`; parity probe matched vector trades
  `151/151`, and compile/run exits were `0/0`.
- Exact `1m` Gate 1 readback:
  `1768519` cleaned source rows, no synthetic fill, `151` exact signals/trades,
  `trades_per_session=0.097044`, win rate `53.6424%`, profit factor `1.8951`,
  raw `+21.26%`, `1bps/side=+18.24%`, `2bps/side=+15.22%`, and
  `5bps/side=+6.16%`. The row survives hard cost but fails practical density.
- Decision: `observation_positive_5bps_but_sparse_exact_aq_replay`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is a valid parity-repaired TOMAC AQ observation. The candidate survives
  hard `5bps/side`, but it is far below the practical density gate, so it stops
  at Gate 1 and does not open downstream.

## Downstream Observation: IBKR MARA 5m Miner Relstrength Fail-Closed

Append-only terminal row at `2026-05-22 03:52 +0800` after rechecking the latest
corrected exact downstream replay for the prior IBKR `MARA/5m` bitcoin-miner
relative-strength range-expansion survivor.

- Source Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T032444+0800-codex-ibkr-mara-riot-bitcoin-miner-relstrength-range-expansion-1m-mtf-gate1-v1/`.
- Latest downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T032444+0800-codex-ibkr-mara-riot-bitcoin-miner-relstrength-range-expansion-1m-mtf-gate1-v1/downstream-exact-mara-5m-relstrength-range-expansion-20260522T034454+0800/`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T0339+0800-codex-ibkr-mara5m-relstrength-exact-downstream.claim`.
- Canonical branch:
  `TrendExpansion -> CryptoEquityRelativeStrengthRangeExpansion -> bitcoin_miner_relstrength_range_expansion -> miner_relstrength_range_expansion_exact_v1`.
  `IBKR`, `MARA`, `RIOT`, NASDAQ, bitcoin-miner equity, and `5m` are
  provenance labels, not branch roots.
- Command/readback parity: all downstream command exits were `0`
  (`01_auto_quant_results_import` through `15_policy_after_ranker`); metrics JSON
  parsed; `rank_rows=1`; `rank_total_trades=116`; path-ranker score was visible
  to the execution tree but not used by it.
- Execution verdict: execution candidate stayed actionable=`false`,
  status=`no_trade`; execution tree gate status was `observe` on
  `transition_guardrail`; `execution_readiness=0.25413775641984`,
  `transition_hazard=0.9798223989102177`, `pda_hybrid_alignment=false`,
  `ranker_validation_ready=false`, `mature_rows=1`, and
  `history_mature_rows=1`.
- Decision: `observation_downstream_fail_closed_transition_guardrail`;
  `promotion_allowed=false`, `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This exact `MARA/5m` sibling is a clean downstream mechanics replay of a hard
  cost survivor, but it is not practical/live-ready. It does not rescue the
  failed exact `1m` origin and should not be promoted, simulate-admitted, or
  used to lower transition/PDA/readiness gates.

## Gate 1 Observation: IBKR XPH 1m Schaff Trend Cycle Drop

Append-only terminal row at `2026-05-22 03:55 +0800` after reading the completed
IBKR `XPH/1m` Schaff Trend Cycle MTF Gate 1 packet and its aborted duplicate
runner.

- Authoritative run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T034447+0800-codex-ibkr-xph-schaff-trend-cycle-1m-mtf-gate1-v1/`.
- Aborted duplicate root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T034448+0800-codex-ibkr-xph-schaff-trend-cycle-1m-mtf-gate1-v1/`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-ibkr-xph-schaff-trend-cycle-1m-mtf-gate1.claim`.
- Canonical branch:
  `TrendExpansion -> PharmaceuticalEtfSchaffTrendCycle -> schaff_trend_cycle_continuation -> ibkr_xph_schaff_trend_cycle_1m_mtf_gate1_v1`.
  `IBKR`, `XPH`, ARCA, pharmaceutical ETF, and timeframes are provenance
  labels, not branch roots.
- Provider/AQ readback: provider status, all fetches, strategy compile,
  material batch, dispatch, and rank exited `0`; fresh IBKR rows covered
  `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11632/4907/1636/818/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=55`; branch
  fields were preserved.
- Exact `1m` Gate 1 readback: `stc_dense`, `stc_balanced`, `stc_quality`, and
  `stc_reclaim` each had `1` trade over `30` days, raw `+0.07%`,
  `2bps/side=+0.03%`, `5bps/side=-0.03%`, and failed practical density.
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`, and
  `higher_timeframe_survivors_5bps_density=[]`.
- Duplicate handling: root `20260522T034448+0800` is explicitly marked
  `aborted_duplicate_runner=true`; the authoritative root remains
  `20260522T034447+0800`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is clean real-provider/AQ coverage for a public Schaff Trend Cycle
  family, but it is not downstream-eligible or trade-usable. Cool down this exact
  `XPH/1m/SchaffTrendCycle` cell; do not promote, simulate-admit, lower gates,
  or lightly retune it from this packet.

## Gate 1 Observation: IBKR XLB 1m Initial-Balance Range Expansion/Fade Drop

Append-only terminal row at `2026-05-22 04:04 +0800` after reading the completed
IBKR `XLB/1m` initial-balance range expansion/fade Gate 1 packet. The diversity
matrix already contains XLB terminal addenda, so this row only brings the compact
Board B current doc up to the same terminal state.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T035054+0800-codex-ibkr-xlb-initial-balance-range-expansion-fade-1m-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; cost stress:
  `summaries/rank_rows_cost_stress.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T034541+0800-codex-ibkr-xlb-initial-balance-range-expansion-fade-1m-gate1.claim`.
- Canonical branch:
  `AuctionMarketStructure -> InitialBalanceRangeExpansionFade -> initial_balance_range_expansion_fade -> ibkr_xlb_initial_balance_range_expansion_fade_1m_gate1_v1`.
  `IBKR`, `XLB`, ARCA, materials ETF, and timeframes are provenance labels, not
  branch roots.
- Provider/AQ readback: provider status, all fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; fresh IBKR rows
  covered `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11676/4910/1637/819/855/585/501`; `local_cache_replay=false`;
  `rank_rows=4`; `rank_total_trade_count=0`; `branch_fields_preserved=true`.
- Exact `1m` Gate 1 readback: `ib30_expansion_dense`,
  `ib60_expansion_balanced`, `ib30_failed_break_fade`, and
  `ib60_midpoint_reclaim` all had zero trades across `30` observation days, raw
  `0.00%`, `1bps/side=0.00%`, `2bps/side=0.00%`, `5bps/side=0.00%`, and
  `0.000/day`. `exact_1m_survivors_2bps_density=[]` and
  `exact_1m_survivors_5bps_density=[]`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is clean real-provider/AQ coverage for a public auction-market/initial
  balance family, but it failed before costs could matter because every exact
  `1m` row was zero-trade. Cool down this exact
  `XLB/1m/InitialBalanceRangeExpansionFade` cell; do not downstream, simulate
  admit, promote, lower gates, or lightly retune this packet.

## Gate 1 Observation: Bybit XRP/ADA 1m DeMarker Reclaim Drop

Append-only terminal row at `2026-05-22 04:10 +0800` after reading the completed
Bybit public `XRPUSDT/ADAUSDT` DeMarker oscillator reclaim `1m` full-ladder
Gate 1 packet.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T035624+0800-codex-bybit-xrp-ada-demarker-reclaim-1m-full-ladder-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; cost stress:
  `summaries/rank_rows_cost_stress.csv`; terminal summary:
  `summaries/terminal_decision_summary.md`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-bybit-xrp-ada-demarker-reclaim-1m-full-ladder.claim`.
- Canonical branch:
  `RangeReversion -> CryptoDeMarkerOscillatorReclaim -> demarker_oscillator_reclaim -> bybit_xrp_ada_demarker_reclaim_1m_full_ladder_v1`.
  `Bybit`, linear perp category, `XRPUSDT/ADAUSDT`, and timeframes are
  provenance labels, not branch roots.
- Provider/AQ readback: provider status, all fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; `provider_count=14`,
  `material_count=14`, `rank_rows=14`, `rank_total_trade_count=0`,
  `branch_fields_preserved=true`, and `local_cache_replay=false`.
  Validation exited `1` only for the two `1d` context lanes
  (`XRPUSDT/1d`, `ADAUSDT/1d`); lower-timeframe origin and context lanes
  validated with exit `0`.
- Exact `1m` Gate 1 readback: both `XRPUSDT/1m/demarker_reclaim` and
  `ADAUSDT/1m/demarker_reclaim` had zero trades, raw `0.00%`,
  `1bps/side=0.00%`, `2bps/side=0.00%`, `5bps/side=0.00%`, and `0.000/day`.
  Every higher-timeframe DeMarker row was also zero-trade.
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`, and
  `higher_timeframe_survivors_5bps_density=[]`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- This is clean public-crypto DeMarker diversity coverage, but the exact `1m`
  origin produced no trades, so there is no cost-stressed density to hand
  downstream. Do not use sparse/higher-timeframe context to rescue it; cool down
  this exact `Bybit XRP/ADA 1m DeMarker` cell.

## Gate 1 Observation: IBKR XLV 1m PSAR Trend-Flip Drop

Append-only terminal row at `2026-05-22 04:05 +0800` after reading the completed
IBKR `XLV` healthcare ETF Parabolic SAR / PSAR trend-flip `1m`-origin MTF
Gate 1 packet. This row mirrors the already-terminalized small-cycle Board B
and diversity-matrix rows; no downstream, simulated admission, promotion, trade
action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T035905+0800-codex-ibkr-xlv-psar-trend-flip-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-ibkr-xlv-psar-trend-flip-1m-mtf-gate1.claim`.
- Canonical branch:
  `TrendExpansion -> HealthcareEtfPsarTrendFlip -> psar_trend_flip ->
  ibkr_xlv_psar_trend_flip_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `XLV`, healthcare ETF, ARCA, and timeframes are
  provenance labels, not branch roots.
- Provider/AQ readback: provider status, all IBKR fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; fresh IBKR rows
  covered `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11684/4911/1637/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=1803`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` PSAR variants were dense but friction-killed.
  `XLV/1m/psar_dense` had `270` trades, raw `+0.04%`, `1bps=-5.36%`,
  `2bps=-10.76%`, and `5bps=-26.96%`; `XLV/1m/psar_reflip` had `256`
  trades, raw `+0.27%`, `1bps=-4.85%`, `2bps=-9.97%`, and `5bps=-25.33%`;
  `XLV/1m/psar_balanced` and `XLV/1m/psar_quality` were already raw-negative.
  Higher-timeframe rows did not produce a `5bps/side` density survivor.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- XLV PSAR trend-flip reached clean real-provider/AQ Gate 1, but the exact
  `1m` root was overactive, cost-negative after `1bps/side`, and failed hard
  `5bps/side` plus practical density. Preserve it as healthcare ETF PSAR
  public-family coverage only; do not downstream, simulate-admit, promote,
  threshold-lower, or lightly retune this exact `XLV/1m/PSAR` cell.

## Gate 1 Observation: IBKR XLP 1m Elder Ray Power Drop

Append-only terminal row at `2026-05-22 04:05 +0800` after reading the completed
IBKR `XLP` consumer-staples ETF Elder Ray bull/bear power `1m`-origin MTF
Gate 1 packet. This row mirrors the already-terminalized small-cycle Board B
and diversity-matrix rows; no downstream, simulated admission, promotion, trade
action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T040210+0800-codex-ibkr-xlp-elder-ray-power-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-ibkr-xlp-elder-ray-power-1m-mtf-gate1.claim`.
- Canonical branch:
  `TrendExpansion -> ConsumerStaplesEtfElderRayPower ->
  elder_ray_bull_bear_power -> ibkr_xlp_elder_ray_power_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `XLP`, consumer-staples ETF, ARCA, and timeframes
  are provenance labels, not branch roots.
- Provider/AQ readback: provider status, all IBKR fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; fresh IBKR rows
  covered `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11687/4912/1638/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=788`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` rows were overactive or raw-negative after costs.
  `XLP/1m/ray_dense` had `149` trades, raw `-0.35%`, `1bps=-3.33%`,
  `2bps=-6.31%`, and `5bps=-15.25%`; `XLP/1m/bear_reclaim` had `119`
  trades, raw `-0.27%`, `1bps=-2.65%`, `2bps=-5.03%`, and `5bps=-12.17%`;
  `XLP/1m/ray_balanced` had `99` trades, raw `-0.28%`, `1bps=-2.26%`,
  `2bps=-4.24%`, and `5bps=-10.18%`; `XLP/1m/ray_quality` had `46` trades,
  raw `-0.11%`, `1bps=-1.03%`, `2bps=-1.95%`, and `5bps=-4.71%`.
  Positive higher-timeframe/context rows did not qualify as separate hard
  survivors: `XLP/1h/ray_quality` had only `2` trades despite `5bps=+0.09%`,
  and `XLP/1d/ray_quality` had only `3` trades despite `5bps=+0.28%`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- XLP Elder Ray power reached clean real-provider/AQ Gate 1, but the exact
  `1m` root was overactive, cost-negative, and failed hard `5bps/side` plus
  practical density. Preserve it as consumer-staples ETF Elder Ray coverage
  only; do not downstream, simulate-admit, promote, threshold-lower, or lightly
  retune this exact `XLP/1m/ElderRayPower` cell.

## Gate 1 Observation: IBKR EFA 1m Alligator/Fractal Breakout Drop

Append-only terminal row at `2026-05-22 04:57 +0800` after running and reading
the completed IBKR `EFA` developed-markets ETF Bill Williams Alligator/Fractal
breakout `1m`-origin MTF Gate 1 packet. This row mirrors the terminal claim and
diversity-matrix evidence; no downstream, simulated admission, promotion, trade
action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T045340+0800-codex-ibkr-efa-alligator-fractal-breakout-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; cost rows:
  `summaries/rank_rows_cost_stress.csv`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-ibkr-efa-alligator-fractal-breakout-1m-mtf-gate1.claim`.
- Canonical branch:
  `TrendExpansion -> DevelopedMarketsEtfAlligatorFractal ->
  alligator_fractal_breakout ->
  ibkr_efa_alligator_fractal_breakout_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `EFA`, developed-markets ETF, ARCA, and timeframes
  are provenance labels, not branch roots.
- Provider/AQ readback: identity test, runner/test `py_compile`, provider
  status, all IBKR fetches, strategy compile, Auto-Quant material batch,
  dispatch, and rank exited `0`; fresh IBKR rows covered
  `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11700/4914/1638/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=13`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` rows were too sparse and failed after even
  `1bps/side`. `EFA/1m/fractal_dense` had `4` trades (`0.133333/day`),
  raw `+0.01%`, `1bps=-0.07%`, `2bps=-0.15%`, and `5bps=-0.39%`;
  `EFA/1m/fractal_balanced`, `EFA/1m/fractal_quality`, and
  `EFA/1m/fractal_retest` had `0` trades. Context rows did not rescue the
  exact root: `EFA/5m/fractal_dense` had `5` trades, raw `-0.41%`,
  `5bps=-0.91%`; `EFA/15m/fractal_dense` had `2` trades, raw `-0.30%`,
  `5bps=-0.50%`; `EFA/4h/fractal_dense` and `EFA/4h/fractal_balanced` each
  had `1` losing trade.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- EFA Alligator/Fractal reached clean real-provider/AQ Gate 1, but the exact
  `1m` root produced only a sparse raw-positive row that failed immediately
  under cost stress, with no practical `5bps/side` density survivor anywhere in
  the ladder. Preserve it as developed-markets ETF Alligator/Fractal public
  family coverage only; do not downstream, simulate-admit, promote, lower gates,
  or lightly retune this exact `EFA/1m/AlligatorFractal` cell.

## Gate 1 Observation: IBKR IJH 1m Force Index Displacement Drop

Append-only terminal row at `2026-05-22 05:03 +0800` after running and reading
the completed IBKR `IJH` mid-cap ETF Force Index displacement/reclaim
`1m`-origin MTF Gate 1 packet. This row mirrors the terminal claim and
diversity-matrix evidence; no downstream, simulated admission, promotion, trade
action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T045905+0800-codex-ibkr-ijh-force-index-displacement-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; cost rows:
  `summaries/rank_rows_cost_stress.csv`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-ibkr-ijh-force-index-displacement-1m-mtf-gate1.claim`.
- Canonical branch:
  `DisplacementExpansion -> MidcapEtfForceIndexDisplacement ->
  force_index_displacement_reclaim ->
  ibkr_ijh_force_index_displacement_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `IJH`, mid-cap ETF, ARCA, and timeframes are
  provenance labels, not branch roots.
- Provider/AQ readback: identity test and runner/test `py_compile` exited `0`;
  provider status, all IBKR fetches, strategy compile, Auto-Quant material
  batch, dispatch, and rank exited `0`; fresh IBKR rows covered
  `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11700/4914/1638/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=1`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` rows had zero trades across all four variants:
  `force_reclaim_dense`, `force_reclaim_balanced`, `force_impulse_quality`, and
  `force_displacement_retest`. The only nonzero row was context-only
  `IJH/1h/force_reclaim_dense` with `1` trade (`0.00813/day`), raw `+0.12%`,
  `1bps=+0.10%`, `2bps=+0.08%`, and `5bps=+0.02%`, but
  `density_1_to_3_per_day=false`; it cannot rescue the failed exact `1m`
  origin.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- IJH Force Index displacement reached clean real-provider/AQ Gate 1, but the
  exact `1m` root produced no trades and the lone `1h` positive row was far
  below practical density. Preserve it as mid-cap ETF Force Index public-family
  coverage only; do not downstream, simulate-admit, promote, lower gates, or
  lightly retune this exact `IJH/1m/ForceIndexDisplacement` cell.

## Gate 1 Observation: IBKR IWM 1m BB-Keltner Squeeze Breakout Drop

Append-only terminal row at `2026-05-22 05:08 +0800` after running and reading
the canonical IBKR `IWM` small-cap ETF Bollinger-Keltner squeeze breakout
`1m`-origin MTF Gate 1 packet. A later duplicate run root was marked aborted in
the claim; use only the canonical run root below for classification. No
downstream, simulated admission, promotion, trade action, or goal update was
launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T050415+0800-codex-ibkr-iwm-bb-keltner-squeeze-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; cost rows:
  `summaries/rank_rows_cost_stress.csv`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-ibkr-iwm-bb-keltner-squeeze-1m-mtf-gate1.claim`.
- Canonical branch:
  `VolatilityCompression -> SmallCapEtfBbKeltnerSqueezeBreakout ->
  bb_keltner_squeeze_breakout ->
  ibkr_iwm_bb_keltner_squeeze_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `IWM`, small-cap ETF, ARCA, and timeframes are
  provenance labels, not branch roots.
- Provider/AQ readback: identity test and runner/test `py_compile` exited `0`;
  provider status, all IBKR fetches, strategy compile, Auto-Quant material
  batch, dispatch, and rank exited `0`; fresh IBKR rows covered
  `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11700/4914/1638/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=114`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` rows had activity but failed immediately after cost:
  `IWM/1m/squeeze_dense` had `29` trades (`0.966667/day`), raw `+0.20%`,
  `1bps=-0.38%`, `2bps=-0.96%`, and `5bps=-2.70%`;
  `IWM/1m/squeeze_retest_reclaim` had `20` trades, raw `+0.01%`,
  `5bps=-1.99%`; `IWM/1m/squeeze_balanced` had `19` trades, raw `+0.05%`,
  `5bps=-1.85%`; `IWM/1m/squeeze_release_quality` had `9` trades,
  raw `+0.06%`, `5bps=-0.84%`. Sparse context positives did not qualify:
  `IWM/5m/squeeze_retest_reclaim` had `6` trades (`0.095238/day`),
  `5bps=+0.02%`; `IWM/5m/squeeze_balanced` had `3` trades
  (`0.047619/day`), `5bps=+0.01%`; `IWM/1d/squeeze_release_quality` had
  `4` trades (`0.007984/day`), `5bps=+0.16%`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- IWM BB-Keltner squeeze reached clean real-provider/AQ Gate 1, but the exact
  `1m` root was inside the friction envelope and sparse positive `5m/1d`
  context rows failed practical density. Preserve it as small-cap ETF
  Bollinger-Keltner squeeze public-family coverage only; do not downstream,
  simulate-admit, promote, lower gates, or lightly retune this exact
  `IWM/1m/BBKeltnerSqueezeBreakout` cell.

## Gate 1 Observation: IBKR EEM 1m Ultimate Oscillator Reclaim Drop

Append-only terminal row at `2026-05-22 05:15 +0800` after running and reading
the completed IBKR `EEM` emerging-markets ETF Ultimate Oscillator reclaim
`1m`-origin MTF Gate 1 packet. This row mirrors the terminal claim and
diversity-matrix evidence; no downstream, simulated admission, promotion, trade
action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T050814+0800-codex-ibkr-eem-ultimate-oscillator-reclaim-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; terminal summary:
  `summaries/terminal_decision_summary.md`; cost rows:
  `summaries/rank_rows_cost_stress.csv`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T044808+0800-codex-ibkr-eem-ultimate-oscillator-reclaim-1m-mtf-gate1.claim`.
- Canonical branch:
  `RangeReversion -> EmergingMarketsEtfUltimateOscillatorReclaim ->
  ultimate_oscillator_reclaim ->
  ibkr_eem_ultimate_oscillator_reclaim_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `EEM`, emerging-markets ETF, ARCA, and timeframes
  are provenance labels, not branch roots.
- Provider/AQ readback: identity test and runner/test `py_compile` exited `0`;
  provider status, all IBKR fetches, strategy compile, Auto-Quant material
  batch, dispatch, and rank exited `0`; fresh IBKR rows covered
  `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11700/4914/1638/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=765`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` rows had activity but failed hard cost stress:
  `EEM/1m/uo_reclaim_dense` had `206` trades (`6.866667/day`),
  raw `+2.22%`, `1bps=-1.90%`, `2bps=-6.02%`, and `5bps=-18.38%`;
  `EEM/1m/uo_reclaim_balanced` had `129` trades (`4.300000/day`),
  raw `+2.18%`, `5bps=-10.72%`; `EEM/1m/uo_signal_reversal` had
  `79` trades (`2.633333/day`), raw `+1.24%`, `5bps=-6.66%`;
  `EEM/1m/uo_quality_reclaim` had `38` trades (`1.266667/day`),
  raw `+0.49%`, `5bps=-3.31%`. Sparse higher-timeframe positives did not
  rescue the exact root: `EEM/1d/uo_quality_reclaim` had `2` trades
  (`0.003992/day`), raw `+1.20%`, `5bps=+1.00%`; `EEM/4h/uo_reclaim_dense`
  had `8` trades (`0.031873/day`), raw `+1.05%`, `5bps=+0.25%`.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- EEM Ultimate Oscillator reclaim reached clean real-provider/AQ Gate 1, but
  every exact `1m` row failed hard `5bps/side` and sparse `4h/1d` positives
  failed practical density. Preserve it as emerging-markets ETF Ultimate
  Oscillator public-family coverage only; do not downstream, simulate-admit,
  promote, lower gates, or lightly retune this exact
  `EEM/1m/UltimateOscillatorReclaim` cell.

## Downstream Observation: TOMAC TOD Balanced Portfolio Fail-Closed

Append-only terminal row at `2026-05-22 05:55 +0800` after reading the
full-window exact downstream replay for the corrected TOMAC TOD balanced
portfolio Gate 1 survivor. This row is scoped to the full-window downstream
root below and does not preempt the separate reduced-window materialization
repair claim.

- Source Gate 1 root:
  `/private/tmp/ict-engine-tomac-tod-balanced-portfolio-aq-20260522-aug-fee5-shiftm1-suppressed-r1`.
- Corrected Gate 1 metrics:
  `checks/terminal_metrics_corrected_cost.json`; summary:
  `summaries/terminal_decision_summary_corrected_cost.md`.
- Downstream root:
  `/tmp/ict-engine-tomac-tod-balanced-portfolio-downstream-20260522T053034+0800`.
- Downstream metrics:
  `checks/downstream_metrics.json`; summary:
  `summaries/terminal_decision_summary.md`; command exits:
  `checks/*.exit`.
- Claims/readbacks:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T052741-codex-tomac-tod-portfolio-downstream.claim`
  and
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T054821+0800-codex-tomac-tod-extension-gate-readback.md`.
- Canonical branch:
  `SessionRhythm -> TimeOfDaySeasonality -> BalancedAdaptiveSlotPortfolio ->
  tomac_tod_balanced_adaptive_slot_portfolio_exact_v1`.
  `TOMAC`, local futures CSV, `NQ/XAU/YM`, `1m`, and synthetic
  `5m/15m/30m/1h/4h/1d` context are provenance labels, not branch roots.

Gate 1 readback:
- Corrected full-window AQ replay survived the hard cost/density gate:
  `1625` trades, `1.044344` trades/session, corrected
  `5bps/side` total profit `+37.62%`, win rate `48.9231%`,
  profit factor `1.2803`, signal parity `true`, density target
  `1-3/session=true`, and `gate1_survivor=true`.
- The original `terminal_metrics.json` double-charged cost columns are stale
  for the hard gate; use `terminal_metrics_corrected_cost.json`.

Downstream readback:
- `auto-quant-results-import`, `auto-quant-prior-init`, `workflow-status`,
  `pre-bayes-status`, `export-structural-path-ranking-target`, path-ranker
  train/apply, score import, trainer register, runtime enable, post-ranker
  workflow/pre-Bayes/policy readbacks, and final target export completed.
- Command exits were not clean end-to-end: `03_analyze_seed.exit=124` and
  `12_analyze_after_ranker.exit=-15`.
- Final downstream metrics:
  `decision=gate1_survivor_downstream_fail_closed`,
  `exact_branch_survived=false`,
  `closed_loop_branch_admission=null`,
  `execution_candidate_actionable=null`,
  `execution_readiness=0.0`,
  `transition_hazard=1.0`,
  `pda_hybrid_alignment=false`, and no execution tree/candidate materialized.
- Policy training remained immature:
  `mature_rows=1`, `history_mature_rows=1`, `rows=2`, and
  `rows_with_execution_gate_status=0`.

Reduced-window materialization repair:
- Separate repair root:
  `/tmp/ict-engine-tomac-tod-balanced-portfolio-downstream-reduced-20260522T0552`;
  metrics `checks/downstream_metrics.json`; summary
  `summaries/terminal_decision_summary.md`; claim
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T0552-codex-tomac-tod-reduced-window-downstream-repair.claim`.
- Bounded NQ representative window completed cleanly from import through final
  export: `01-16` exits were all `0`; data rows were `1m=178599`,
  `15m=12040`, and `1h=3111`; `representative_stream_only=true` and
  `portfolio_extension_complete=false`.
- The repair only fixed mechanics, not admission:
  `decision=reduced_window_downstream_fail_closed`,
  `exact_branch_survived=false`, `closed_loop_branch_admission=null`,
  `execution_candidate_actionable=false`, `execution_candidate_status=no_trade`,
  `execution_tree_gate_status=observe`, branch `transition_guardrail`,
  `execution_readiness=0.3717323243829561`,
  `transition_hazard=0.6390215249399245`, `pda_hybrid_alignment=true`,
  `path_ranker_score_visible_to_execution_tree=true`,
  `path_ranker_score_used_by_execution_tree=false`, and
  `ranker_validation_ready=false`.
- Reduced-window policy training also stayed immature:
  `mature_rows=1`, `history_mature_rows=2`, `rows=4`,
  `history_rows=10`, and `rows_with_execution_gate_status=0`.

Decision:
- `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=true` mechanically,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- TOMAC TOD balanced portfolio is a genuine corrected Gate 1 survivor, but the
  exact full-window downstream replay is fail-closed. Preserve it as a
  same-root materialization repair lead only; do not call it practical,
  extension-complete, live, or trade-usable until exact branch survival,
  mature validation, execution candidate/tree materialization, and the hard
  predicate trio all pass together.

Post-ingest continuation readback at `2026-05-22 06:57 +0800`:
- Follow-up root:
  `/tmp/ict-engine-tomac-tod-balanced-feedback-post-ingest-continuation-20260522T065105+0800`;
  metrics `checks/downstream_metrics.json`; summary
  `summaries/terminal_decision_summary.md`; claim
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T065105+0800-codex-tomac-tod-feedback-post-ingest-continuation.claim`.
- Scope: same-root continuation after the source feedback-admission run's
  `08_ingest_real_trades.exit=124`; no Gate 1 rerun, no lowered gates, and no
  repo code changes.
- Command layer repaired: all `10/10` post-ingest commands exited `0`
  (`11_train_ranker` through `20_export_target_final`), including
  CatBoost/path-ranker train/apply/register/runtime enable, analyze, workflow,
  Pre-Bayes, policy, and final target export readbacks.
- Admission still failed closed:
  `decision=post_ingest_continuation_fail_closed`,
  `exact_branch_survived=false`, execution candidate `no_trade`,
  `execution_candidate_actionable=false`, execution tree `observe` on
  `transition_guardrail`, and hint
  `execution_guarded_due_to_high_transition_hazard`.
- Hard predicates remain below gate:
  `execution_readiness=0.3717323243829561`,
  `transition_hazard=0.6390215249399245`, and
  `pda_hybrid_alignment=true`.
- Ranker improved but did not enter execution:
  `path_ranker_score_visible_to_execution_tree=true`,
  `path_ranker_score_used_by_execution_tree=false`, and
  `ranker_validation_ready=true`.
- Validation/maturity is not current-production-ready:
  `mature_rows=3`, `history_mature_rows=3255`, `raw_scored_mature=3`,
  `production_validation=0`, and `observation_validation=0`.
- Decision remains `promotion_allowed=false`, `trade_usable=false`, and
  `update_goal=false`. This supersedes the reduced-window next-action note:
  do not repeat backtest-trade feedback or broaden the extension from this
  packet; the next TOMAC work must repair exact branch survival, actionable
  execution materialization, current validation maturity, hazard `<0.60`, and
  readiness `>=0.65` using acceptable evidence.

## Gate 1 Observation: IBKR IYR 1m DPO Reversion Drop

Append-only terminal row at `2026-05-22 06:10 +0800` after running and reading
the completed IBKR `IYR` real-estate ETF Detrended Price Oscillator reversion
`1m`-origin MTF Gate 1 packet. No downstream, simulated admission, promotion,
trade action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T060656+0800-codex-ibkr-iyr-dpo-reversion-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; summary:
  `summaries/terminal_decision_summary.md`; cost rows:
  `summaries/rank_rows_cost_stress.csv`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T060656+0800-codex-ibkr-iyr-dpo-reversion-1m-mtf-gate1.claim`.
- Canonical branch:
  `RangeReversion -> RealEstateEtfDpoReversion ->
  detrended_price_oscillator_reversion ->
  ibkr_iyr_dpo_reversion_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `IYR`, real-estate ETF, ARCA, and timeframes are
  provenance labels, not branch roots.
- Provider/AQ readback: provider status, all IBKR fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; fresh IBKR rows
  covered `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11700/4914/1638/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=610`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` rows were dense but failed hard cost:
  `IYR/1m/dpo_dense` had `105` trades (`3.500000/day`), raw `+1.51%`,
  `1bps=-0.59%`, `2bps=-2.69%`, and `5bps=-8.99%`;
  `IYR/1m/dpo_reclaim` had `87` trades (`2.900000/day`), raw `+0.14%`,
  `2bps=-3.34%`, and `5bps=-8.56%`; `IYR/1m/dpo_balanced` had `66`
  trades (`2.200000/day`), raw `-0.27%`, and `5bps=-6.87%`;
  `IYR/1m/dpo_quality` had `40` trades (`1.333333/day`), raw `-1.39%`,
  and `5bps=-5.39%`. Sparse positive daily rows failed practical density and
  cannot rescue the exact `1m` origin.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- IYR DPO reversion reached clean real-provider/AQ Gate 1, but exact `1m` rows
  stayed inside the friction envelope and the sparse daily positives are not
  practical density. Preserve it as real-estate ETF DPO coverage only; do not
  downstream, simulate-admit, promote, lower gates, or lightly retune this exact
  `IYR/1m/DPOReversion` cell.

## Gate 1 Observation: IBKR EWC 1m CMO Rebound/Reclaim Drop

Append-only terminal row at `2026-05-22 06:24 +0800` after running and reading
the completed IBKR `EWC` Canada ETF Chande Momentum Oscillator rebound/reclaim
`1m`-origin MTF Gate 1 packet. No downstream, simulated admission, promotion,
trade action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T062357+0800-codex-ibkr-ewc-cmo-rebound-reclaim-1m-mtf-gate1-v1/`.
- Terminal metrics: `checks/terminal_metrics.json`; summary:
  `summaries/terminal_decision_summary.md`; cost rows:
  `summaries/rank_rows_cost_stress.csv`; command exits: `checks/*.exit`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T062357+0800-codex-ibkr-ewc-cmo-rebound-reclaim-1m-mtf-gate1.claim`.
- Canonical branch:
  `RangeReversion -> CanadaEtfCmoReboundReclaim ->
  chande_momentum_rebound_reclaim ->
  ibkr_ewc_cmo_rebound_reclaim_1m_mtf_gate1_v1`.
  `IBKR`, `US_EQUITY_ETF`, `EWC`, Canada ETF, ARCA, and timeframes are
  provenance labels, not branch roots.
- Provider/AQ readback: provider status, all IBKR fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`; fresh IBKR rows
  covered `1m/5m/15m/30m/1h/4h/1d` with row counts
  `11700/4914/1638/819/855/585/501`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=736`;
  `branch_fields_preserved=true`.
- Cost stress: exact `1m` rows failed hard cost/density:
  `EWC/1m/cmo_balanced` had `65` trades (`2.166667/day`), raw `+0.65%`,
  `1bps=-0.65%`, `2bps=-1.95%`, and `5bps=-5.85%`;
  `EWC/1m/cmo_quality` had `20` trades (`0.666667/day`), raw `+0.48%`,
  `1bps=+0.08%`, `2bps=-0.32%`, and `5bps=-1.52%`;
  `EWC/1m/cmo_dense` had `105` trades (`3.500000/day`), raw `-0.50%`,
  `2bps=-4.70%`, and `5bps=-11.00%`; `EWC/1m/cmo_reclaim` had `90`
  trades (`3.000000/day`), raw `-0.87%`, and `5bps=-9.87%`.
  Higher-timeframe positives such as `EWC/1h/cmo_reclaim` (`22` trades,
  `0.178862/day`, `5bps=+0.57%`) and `EWC/1h/cmo_dense` (`24` trades,
  `0.195122/day`, `5bps=+0.38%`) failed practical density and cannot rescue
  the exact `1m` origin.
- Decision: `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`,
  `origin_1m_survivors_5bps_density=[]`,
  `higher_timeframe_survivors_5bps_density=[]`,
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- EWC CMO rebound/reclaim reached clean real-provider/AQ Gate 1, but exact `1m`
  rows stayed inside the friction envelope and higher-timeframe positives were
  sparse observations only. Preserve it as Canada ETF CMO coverage; do not
  downstream, simulate-admit, promote, lower gates, or lightly retune this exact
  `EWC/1m/CMOReboundReclaim` cell.

## Gate 1 Observation: IBKR PAVE 1m Chaikin A/D Accumulation Breakout Drop

Append-only terminal row at `2026-05-22 07:12 +0800` for the IBKR `PAVE`
infrastructure ETF Chaikin A/D accumulation breakout `1m`-origin MTF Gate 1
lane. No downstream, simulated admission, promotion, trade action, or goal
update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T070629+0800-codex-ibkr-pave-chaikin-ad-accumulation-breakout-1m-mtf-gate1-v1`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T070113+0800-codex-ibkr-pave-chaikin-ad-accumulation-breakout-1m-mtf-gate1.claim`.
- Branch:
  `TrendExpansion -> InfrastructureEtfChaikinAccumulationBreakout ->
  range_breakout_cmf_confirm ->
  ibkr_pave_chaikin_ad_accumulation_breakout_1m_mtf_gate1_v1`.
  `IBKR`, `PAVE`, ETF/exchange, and timeframe labels are provenance only.
- Verification:
  runner compile, provider status, all seven IBKR fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`;
  `local_cache_replay=false`; `material_count=28`; `rank_rows=28`;
  `rank_total_trade_count=1618`; `branch_fields_preserved=true`.
- Cost stress:
  exact `1m` rows had no hard-cost density survivor:
  `dense` `182` trades / raw `+1.98%` / `5bps=-16.22%`;
  `retest` `163` trades / raw `+1.31%` / `5bps=-14.99%`;
  `balanced` `146` trades / raw `+1.21%` / `5bps=-13.39%`;
  `quality` `110` trades / raw `+0.15%` / `5bps=-10.85%`.
  Higher-timeframe `30m` and `1h` positives survived `5bps/side`
  arithmetically but failed practical density.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- PAVE gives the infrastructure ETF Chaikin A/D volume-accumulation family a
  clean real-provider/AQ attempt, but it is not downstream-eligible or
  trade-usable. Cool down exact `PAVE/1m/ChaikinADAccumulationBreakout`; no
  simulated admission, promotion, threshold lowering, light retune, or
  practical extension work from this packet.

## Gate 1 Observation: IBKR CVNA 1m Donchian/Keltner Compression Breakout Drop

Append-only terminal row at `2026-05-22 07:31 +0800` for a fresh IBKR `CVNA`
high-beta auto-retail Donchian/Keltner compression breakout `1m`-origin MTF
Gate 1 lane. This is provider/AQ/cost evidence only; no downstream, simulated
admission, promotion, trade action, or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T072742+0800-codex-ibkr-cvna-donchian-keltner-compression-breakout-1m-mtf-gate1-v1`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T072742+0800-codex-ibkr-cvna-donchian-keltner-compression-breakout-1m-mtf-gate1.claim`.
- Branch:
  `TrendExpansion -> AutoRetailerDonchianKeltnerCompressionBreakout ->
  donchian_keltner_compression_breakout ->
  ibkr_cvna_donchian_keltner_compression_breakout_1m_mtf_gate1_v1`.
  `IBKR`, `CVNA`, NYSE, high-beta auto-retail, and timeframe labels are
  provenance only.
- Verification:
  RED identity test failed on missing runner; final identity test and
  `py_compile` exited `0`; provider status, all seven IBKR fetches, strategy
  compile, Auto-Quant material batch, dispatch, and rank exited `0`;
  `local_cache_replay=false`; `material_count=28`; `rank_rows=28`;
  `rank_total_trade_count=160`; `branch_fields_preserved=true`.
- Cost stress:
  exact `1m` rows failed hard `5bps/side` despite two practical-density rows:
  `compression_dense` `57` trades / `1.900000/day` / raw `+0.62%` /
  `5bps=-5.08%`; `compression_balanced` `38` trades / `1.266667/day` /
  raw `+0.13%` / `5bps=-3.67%`; `retest_breakout` `38` trades /
  raw `-0.49%` / `5bps=-4.29%`; `compression_quality` `19` trades /
  raw `-0.43%` / `5bps=-2.33%`. Higher timeframes produced no hard-cost
  density survivor.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- CVNA adds a clean high-excursion single-stock compression-breakout coverage
  slot, but it is not downstream-eligible or trade-usable. Cool down exact
  `CVNA/1m/DonchianKeltnerCompressionBreakout`; no simulated admission,
  promotion, threshold lowering, light retune, or practical extension work from
  this packet.

## Gate 1 Observation: IBKR DDOG 1m Chandelier/ATR Trend Drop

Append-only terminal row at `2026-05-22 07:43 +0800` for the IBKR `DDOG`
cloud-observability single-stock Chandelier/ATR trend-continuation
`1m`-origin MTF Gate 1 lane. This is provider/AQ/cost evidence only; no
downstream, simulated admission, promotion, trade action, or goal update was
launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T074032+0800-codex-ibkr-ddog-chandelier-atr-trend-1m-mtf-gate1-v1`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T072617+0800-codex-ibkr-ddog-chandelier-atr-trend-1m-mtf-gate1.claim`.
- Branch:
  `TrendExpansion -> CloudObservabilityChandelierAtrTrend ->
  chandelier_atr_trend_continuation ->
  ibkr_ddog_chandelier_atr_trend_1m_mtf_gate1_v1`. `IBKR`, `DDOG`,
  NASDAQ, cloud-observability, and timeframe labels are provenance only.
- Verification:
  identity test and runner `py_compile` exited `0`; provider status, all seven
  IBKR fetches, strategy compile, Auto-Quant material batch, dispatch, and rank
  exited `0`; `local_cache_replay=false`; `material_count=28`; `rank_rows=28`;
  `rank_total_trade_count=442`; `branch_fields_preserved=true`.
- Provider rows:
  fresh IBKR ladder rows were `11700/4914/1638/819/441/585/501` for
  `1m/5m/15m/30m/1h/4h/1d`.
- Cost stress:
  exact `1m` rows failed hard cost/density: `balanced` had `16` trades
  (`0.533333/day`), raw `-2.75%`, `2bps=-3.39%`, `5bps=-4.35%`;
  `pullback_reclaim` had `24` trades (`0.800000/day`), raw `-5.98%`,
  `5bps=-8.38%`; `quality` had `6` trades, raw `-3.56%`,
  `5bps=-4.16%`; `dense` had zero trades. Sparse higher-timeframe positives
  such as `1h/pullback_reclaim` (`24` trades, `0.380952/day`,
  `5bps=+4.93%`) and `30m/pullback_reclaim` (`28` trades,
  `0.444444/day`, `5bps=+0.92%`) failed practical density and cannot rescue
  the exact `1m` origin.
- Decision:
  `drop_gate1_cost_or_density_failed`; `survivors_5bps_per_side=[]`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- DDOG Chandelier/ATR trend continuation reached clean real-provider/AQ Gate 1
  with rooted metadata preserved, but no row survived both hard `5bps/side`
  cost stress and practical density. Preserve it as cloud-observability trend
  coverage only; do not downstream, simulate-admit, promote, lower gates, or
  lightly retune this exact `DDOG/1m/ChandelierAtrTrendContinuation` cell.

## Gate 1 Observation: IBKR RKLB 1m NR7/Crabel Opening-Range Expansion Drop

Append-only terminal row at `2026-05-22 07:57 +0800` for the completed IBKR
`RKLB` Rocket Lab high-beta space/aerospace NR7/Crabel opening-range expansion
`1m`-origin MTF Gate 1 lane. This row records provider/AQ/cost-gate evidence
only; no downstream, simulated admission, promotion, trade action, or goal
update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T075442+0800-codex-ibkr-rklb-nr7-crabel-opening-range-expansion-1m-mtf-gate1-v1`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T-current-codex-ibkr-rklb-nr7-crabel-opening-range-expansion-1m-mtf-gate1.claim`.
- Branch:
  `VolatilityExpansion -> RocketLabNr7CrabelOpeningRangeExpansion ->
  nr7_crabel_opening_range_expansion ->
  ibkr_rklb_nr7_crabel_opening_range_expansion_1m_mtf_gate1_v1`. `IBKR`,
  `RKLB`, NASDAQ, Rocket Lab/space-aerospace, and timeframe labels are
  provenance only.
- Verification:
  identity test and runner `py_compile` exited `0`; provider status, all seven
  IBKR fetches, strategy compile, Auto-Quant material batch, dispatch, and rank
  exited `0`; `local_cache_replay=false`; `material_count=28`; `rank_rows=28`;
  `rank_total_trade_count=70`; `branch_fields_preserved=true`.
- Provider rows:
  fresh IBKR ladder rows were `11700/4914/1638/819/855/585/502` for
  `1m/5m/15m/30m/1h/4h/1d`.
- Cost stress:
  exact `1m` variants all had zero trades and no survivor. The only
  raw-positive nonzero sibling, `RKLB/5m/nr7_or_dense`, had `48` trades over
  `63` days (`0.761905/day`), raw `+0.83%`, `1bps=-0.13%`, `2bps=-1.09%`,
  and `5bps=-3.97%`; this failed both hard cost and practical density.
  `RKLB/30m/nr7_or_dense` had `8` trades, raw `-0.63%`, `5bps=-1.43%`;
  `RKLB/15m/nr7_or_dense` had `14` trades, raw `-4.44%`, `5bps=-5.84%`.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, and `update_goal=false`.

Interpretation:

- RKLB NR7/Crabel opening-range expansion reached clean real-provider/AQ Gate 1
  with rooted metadata preserved, but exact `1m` produced zero trades and no
  sibling met hard cost plus practical density. Preserve it as Rocket Lab
  NR7/Crabel public-family coverage only; do not downstream, simulate-admit,
  promote, lower gates, or lightly retune this exact
  `RKLB/1m/Nr7CrabelOpeningRangeExpansion` cell.

## Gate 1 Observation: IBKR SHOP 1m KST-Coppock Momentum Drop

Append-only terminal row at `2026-05-22 08:02 +0800` for the completed IBKR
`SHOP` ecommerce-platform single-stock KST-Coppock momentum `1m`-origin MTF
Gate 1 lane. This row records provider/AQ/cost-gate evidence only; no
downstream, simulated admission, promotion, trade action, extension completion,
or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T075829+0800-codex-ibkr-shop-kst-coppock-1m-mtf-gate1-v1`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T075311+0800-codex-ibkr-shop-kst-coppock-1m-mtf-gate1.claim`.
- Branch:
  `TrendExpansion -> EcommercePlatformKstCoppockMomentum ->
  kst_coppock_momentum -> ibkr_shop_kst_coppock_1m_mtf_gate1_v1`. `IBKR`,
  `SHOP`, NYSE, ecommerce-platform, and timeframe labels are provenance only.
- Verification:
  identity test and runner `py_compile` exited `0`; provider status, all seven
  IBKR fetches, strategy compile, Auto-Quant material batch, dispatch, and rank
  exited `0`; `local_cache_replay=false`; `material_count=28`; `rank_rows=28`;
  `rank_total_trade_count=1035`; `branch_fields_preserved=true`.
- Provider rows:
  fresh IBKR ladder rows were `11700/4914/1638/819/855/585/502` for
  `1m/5m/15m/30m/1h/4h/1d`.
- Cost stress:
  exact `1m/kst_impulse_quality` had `72` trades over `30` days
  (`2.400000/day`) and survived only through `2bps/side`: raw `+3.24%`,
  `1bps=+1.80%`, `2bps=+0.36%`, but `5bps=-3.96%`. Other active exact `1m`
  variants failed sooner: `1m/kst_reclaim_dense` had `187` trades
  (`6.233333/day`), raw `+3.36%`, `1bps=-0.38%`, `5bps=-15.34%`;
  `1m/kst_reclaim_balanced` had `119` trades, raw `+1.83%`,
  `5bps=-10.07%`; `1m/coppock_accel` had `41` trades (`1.366667/day`),
  raw `+1.41%`, `1bps=+0.59%`, `2bps=-0.23%`, `5bps=-2.69%`.
  Higher-timeframe positives such as `30m/kst_reclaim_dense` (`38` trades,
  `5bps=+0.11%`) and `1h/kst_reclaim_balanced` (`15` trades,
  `5bps=+1.44%`) failed practical density and cannot rescue the exact
  `1m` origin.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=['SHOP/1m/kst_impulse_quality']`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- SHOP KST-Coppock reached clean real-provider/AQ Gate 1 with rooted metadata
  preserved, but the exact `1m` edge is `2bps`-only and fails the hard
  `5bps/side` gate. Preserve it as ecommerce-platform KST-Coppock
  public-family coverage only; do not downstream, simulate-admit, promote,
  lower gates, or lightly retune this exact `SHOP/1m/KST-Coppock` cell.

## Gate 1 Observation: IBKR CELH 1m Gap Fade-Go Drop

Append-only terminal row at `2026-05-22 08:18 +0800` for the completed IBKR
`CELH` consumer-beverage single-stock opening gap fade/go reclaim `1m`-origin
MTF Gate 1 lane. This row records provider/AQ/cost-gate evidence only; no
downstream, simulated admission, promotion, trade action, extension completion,
or goal update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T080802+0800-codex-ibkr-celh-gap-fade-go-1m-mtf-gate1-v1`.
- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T0800+0800-codex-ibkr-celh-gap-fade-go-1m-mtf-gate1.claim`.
- Branch:
  `OpeningGapRegime -> ConsumerBeverageGapFadeGo ->
  gap_fade_go_opening_reclaim -> ibkr_celh_gap_fade_go_1m_mtf_gate1_v1`.
  `IBKR`, `CELH`, NASDAQ, consumer-beverage, and timeframe labels are
  provenance only.
- Verification:
  RED identity test failed on missing runner; final identity test and
  `py_compile` exited `0`; provider status, all seven IBKR fetches, strategy
  compile, Auto-Quant material batch, dispatch, and rank exited `0`;
  `local_cache_replay=false`; `material_count=28`; `rank_rows=28`;
  `rank_total_trade_count=113`; `branch_fields_preserved=true`.
- Provider rows:
  fresh IBKR ladder rows were `11700/4914/1638/819/855/585/501` for
  `1m/5m/15m/30m/1h/4h/1d`.
- Cost stress:
  exact `1m` rows survived hard cost arithmetically but failed practical
  density: `gap_go_up_dense` had `16` trades over `30` days
  (`0.533333/day`), raw `+4.35%`, `1bps=+4.03%`, `2bps=+3.71%`, and
  `5bps=+2.75%`; `gap_go_up_quality` had `10` trades (`0.333333/day`),
  raw `+3.44%`, `1bps=+3.24%`, `2bps=+3.04%`, and `5bps=+2.44%`;
  `gap_fade_down_dense` had only `2` trades (`0.066667/day`), raw `+0.49%`,
  and `5bps=+0.29%`. The strongest non-1m sibling, `5m/gap_go_up_dense`,
  had `17` trades over `63` days (`0.269841/day`), raw `+3.12%`,
  `5bps=+1.42%`, and still failed density. No row met hard `5bps/side`
  plus `1` to `3` trades/day practical density.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- CELH gap fade/go reached clean real-provider/AQ Gate 1 with rooted metadata
  preserved, but all positive exact `1m` and sibling rows are below practical
  density. Preserve it as consumer-beverage gap fade/go public-family coverage
  only; do not downstream, simulate-admit, promote, lower gates, or lightly
  retune this exact `CELH/1m/GapFadeGo` cell.

## Gate 1 Observation: IBKR CRDO 1m New-High Pullback Breakout Drop

Append-only terminal row at `2026-05-22 08:25 +0800` for the IBKR `CRDO`
AI-infrastructure single-stock 52-week/new-high pullback breakout `1m`-origin
MTF Gate 1 lane. This records provider/AQ/cost-gate evidence only; no
downstream, simulated admission, promotion, trade action, extension completion,
or goal update was launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T0820+0800-codex-ibkr-crdo-new-high-pullback-breakout-1m-mtf-gate1.claim`.
- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T082248+0800-codex-ibkr-crdo-new-high-pullback-breakout-1m-mtf-gate1-v1`.
- Branch:
  `TrendExpansion -> AiInfrastructureNewHighPullbackBreakout ->
  new_high_pullback_breakout ->
  ibkr_crdo_new_high_pullback_breakout_1m_mtf_gate1_v1`. `IBKR`,
  `US_EQUITY`, `CRDO`, `NASDAQ`, AI-infrastructure labels, and timeframes are
  provenance only.
- Verification:
  identity test printed all four checks as `ok`; runner/test `py_compile`
  exited `0`; provider status, all seven IBKR fetches, strategy compile,
  Auto-Quant material batch, dispatch, and rank exited `0`;
  `local_cache_replay=false`; `material_count=28`; `rank_rows=28`;
  `rank_total_trade_count=0`; `branch_fields_preserved=true`.
- Provider rows:
  fresh IBKR ladder rows were `11700/4914/1638/819/855/585/501` for
  `1m/5m/15m/30m/1h/4h/1d`.
- Cost stress:
  all ranked rows across `1m/5m/15m/30m/1h/4h/1d` produced zero trades and
  zero raw or cost-stressed profit. Exact `1m` variants
  `breakout_pullback_dense`, `breakout_pullback_quality`, `vcp_reclaim_dense`,
  and `vcp_reclaim_quality` each had `0` trades and no cost survivor; higher
  timeframe siblings also had `0` trades.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- CRDO new-high pullback breakout reached clean real-provider/AQ Gate 1 with
  rooted metadata preserved, but produced zero trades across the full ladder.
  Preserve it as AI-infrastructure new-high pullback coverage only; do not
  downstream, simulate-admit, promote, lower gates, or lightly retune this exact
  `CRDO/1m/NewHighPullbackBreakout` cell.

## Gate 1 Observation: IBKR NVO 1m Darvas/Box Breakout Drop

Append-only terminal row at `2026-05-22 08:31 +0800` for the IBKR `NVO`
GLP-1 leader ADR Darvas/box breakout retest `1m`-origin MTF Gate 1 lane. This
records provider/AQ/cost-gate evidence only; no downstream, simulated
admission, promotion, trade action, extension completion, or goal update was
launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T082017+0800-codex-ibkr-nvo-darvas-box-breakout-1m-mtf-gate1.claim`.
- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T082740+0800-codex-ibkr-nvo-darvas-box-breakout-1m-mtf-gate1-v1`.
- Branch:
  `VolatilityExpansion -> Glp1LeaderDarvasBoxBreakout ->
  darvas_box_breakout_retest ->
  ibkr_nvo_darvas_box_breakout_1m_mtf_gate1_v1`. `IBKR`,
  `US_EQUITY`, `NVO`, `NYSE`, GLP-1 leader ADR labels, and timeframes are
  provenance only.
- Verification:
  provider status, all seven IBKR fetches, strategy compile, Auto-Quant
  material batch, dispatch, and rank exited `0`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=353`;
  `branch_fields_preserved=true`.
- Provider rows:
  fresh IBKR ladder rows were `11700/4914/1638/819/855/585/501` for
  `1m/5m/15m/30m/1h/4h/1d`.
- Cost stress:
  exact `1m/retest_dense` had `72` trades over `30` days (`2.400000/day`),
  raw `+0.40%`, `1bps=-1.04%`, `2bps=-2.48%`, and `5bps=-6.80%`;
  exact `1m/box_breakout_dense` had `81` trades (`2.700000/day`),
  raw `+0.07%`, `1bps=-1.55%`, `2bps=-3.17%`, and `5bps=-8.03%`;
  exact `1m/retest_quality` had `20` trades (`0.666667/day`), raw `+0.49%`,
  `1bps=+0.09%`, `2bps=-0.31%`, and `5bps=-1.51%`; exact
  `1m/box_breakout_quality` had `27` trades (`0.900000/day`), raw `-0.01%`
  and `5bps=-2.71%`. Sparse non-1m positives such as
  `30m/retest_dense` (`8` trades, `0.126984/day`, `5bps=+0.81%`) and
  `1d/retest_dense` (`1` trade, `5bps=+0.78%`) failed practical density and
  cannot rescue the exact `1m` origin.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- NVO Darvas/box breakout reached clean real-provider/AQ Gate 1 with rooted
  metadata preserved, but the exact `1m` rows are inside the friction envelope
  and the sparse positive siblings fail practical density. Preserve it as
  GLP-1 leader Darvas/box public-family coverage only; do not downstream,
  simulate-admit, promote, lower gates, or lightly retune this exact
  `NVO/1m/DarvasBoxBreakout` cell.

## Gate 1 Observation: IBKR HIMS 1m VCP High-Tight-Flag Breakout Drop

Append-only terminal row at `2026-05-22 08:36 +0800` for the IBKR `HIMS`
digital-health single-stock volatility-contraction/high-tight-flag breakout
`1m`-origin MTF Gate 1 lane. This records provider/AQ/cost-gate evidence only;
no downstream, simulated admission, promotion, trade action, extension
completion, or goal update was launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T082819+0800-codex-ibkr-hims-vcp-high-tight-flag-breakout-1m-mtf-gate1.claim`.
- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T083308+0800-codex-ibkr-hims-vcp-high-tight-flag-breakout-1m-mtf-gate1-v1`.
- Branch:
  `TrendExpansion -> DigitalHealthVcpHighTightFlagBreakout ->
  vcp_high_tight_flag_breakout ->
  ibkr_hims_vcp_high_tight_flag_breakout_1m_mtf_gate1_v1`. `IBKR`,
  `US_EQUITY`, `HIMS`, `NYSE`, digital-health labels, and timeframes are
  provenance only.
- Verification:
  RED identity test failed on the missing runner before implementation; final
  identity test passed `4/4`; runner/test `py_compile` exited `0`; provider
  status, all seven IBKR fetches, strategy compile, Auto-Quant material batch,
  dispatch, and rank exited `0`; `local_cache_replay=false`;
  `material_count=28`; `rank_rows=28`; `rank_total_trade_count=0`;
  `branch_fields_preserved=true`.
- Provider rows:
  fresh IBKR ladder rows were `11700/4914/1638/819/855/585/501` for
  `1m/5m/15m/30m/1h/4h/1d`.
- Cost stress:
  all ranked rows across `1m/5m/15m/30m/1h/4h/1d` produced zero trades and
  zero raw or cost-stressed profit. Exact `1m` variants
  `vcp_breakout_dense`, `vcp_breakout_quality`, `flag_retest_dense`, and
  `flag_retest_quality` each had `0` trades and no cost survivor; higher
  timeframe siblings also had `0` trades.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `origin_1m_survivors_2bps_density=[]`;
  `origin_1m_survivors_5bps_density=[]`;
  `higher_timeframe_survivors_5bps_density=[]`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- HIMS VCP/high-tight-flag breakout reached clean real-provider/AQ Gate 1 with
  rooted metadata preserved, but produced zero trades across the full ladder.
  Preserve it as digital-health VCP/high-tight-flag coverage only; do not
  downstream, simulate-admit, promote, lower gates, or lightly retune this exact
  `HIMS/1m/VcpHighTightFlagBreakout` cell.

## Gate 1 Observation: TOMAC XAU/YM Lead-Lag Exact AQ Drop

Append-only terminal row at `2026-05-22 08:40 +0800` for the exact
Auto-Quant replay of the strongest local TOMAC cross-instrument lead-lag
discovery row, `XAU:YM`:
`tomac_xau_ym_leadlag_divergence_reclaim_w1560_z1.5_x0.25_lm4_tr1_rv0.4_h120_cd60`.
This records exact AQ/cost-gate evidence only; no Pre-Bayes, BBN, CatBoost,
execution-tree, simulated admission, promotion, trade action, extension
completion, or goal update was launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260522T0812+0800-codex-tomac-cross-instrument-leadlag-reclaim-fullwindow.claim`.
- Discovery root:
  `/tmp/ict-engine-tomac-cross-instrument-leadlag-reclaim-20260522Tdirect`.
- Exact AQ root:
  `/tmp/ict-engine-tomac-cross-instrument-leadlag-reclaim-20260522Tdirect/exact_aq_xau_ym_top`.
- Branch:
  `RangeTransition -> CrossInstrumentLeadLagDivergenceReclaim ->
  leadlag_divergence_reclaim ->
  tomac_xau_ym_leadlag_divergence_reclaim_w1560_z1.5_x0.25_lm4_tr1_rv0.4_h120_cd60`.
  `TOMAC`, local `XAU/YM/NQ` CSVs, futures mode, and timeframe labels are
  provenance only.
- Verification:
  exact strategy `py_compile` exited `0`; exact `run_tomac.py` exited `0`;
  terminal metrics are in
  `/tmp/ict-engine-tomac-cross-instrument-leadlag-reclaim-20260522Tdirect/exact_aq_xau_ym_top/checks/terminal_metrics.json`;
  exact rank row is in
  `/tmp/ict-engine-tomac-cross-instrument-leadlag-reclaim-20260522Tdirect/exact_aq_xau_ym_top/summaries/rank_rows.csv`.
- Exact AQ result:
  the vector screen had `3115` trades, but exact AQ produced `2908` trades
  across `1552` sessions (`1.873711` trades/session), so
  `signal_count_parity=false`. Exact raw profit was `+114.73%` and
  `1bps/side=+56.57%`, but hard friction failed at `2bps/side=-1.59%` and
  `5bps/side=-176.07%`; `survives_5bps_per_side=false`;
  `gate1_survivor=false`; `survivors_5bps=[]`.
- Decision:
  `exact_autoquant_replay_no_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- TOMAC XAU/YM lead-lag was a useful full-window discovery lead, but the exact
  AQ replay did not preserve hard `5bps/side` economics. Preserve the exact
  replay as a cost-stress negative and do not downstream, simulate-admit,
  promote, lower gates, or call this lead-lag packet practical. Any next TOMAC
  lead-lag attempt must start from a different exact-AQ-replayable candidate or
  repair the exact AQ cost/parity gap before tree handoff.

## Downstream Observation: IBKR CEG 5m Late-Reclaim Fail-Closed

Append-only terminal row at `2026-05-23 03:34 +0800` for the exact downstream
readback of the completed IBKR `CEG` power-infrastructure VWAP/NR7 reclaim
`5m/late_reclaim` higher-timeframe Gate 1 survivor. This is a scoped
higher-timeframe lane only; it does not rescue the failed exact `1m` origin.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T033405+0800-codex-ibkr-ceg5m-late-reclaim-exact-downstream.claim`.
- Source Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T032228+0800-codex-ibkr-ceg-power-infra-vwap-nr7-reclaim-1m-mtf-gate1-v1`.
- Downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T032228+0800-codex-ibkr-ceg-power-infra-vwap-nr7-reclaim-1m-mtf-gate1-v1/downstream-exact-ceg-5m-late-reclaim-20260523T033405+0800`.
- Branch:
  `RangeConsolidation -> VolatilityCompressionExpansion ->
  vwap_nr7_reclaim ->
  ibkr_ceg_power_infra_vwap_nr7_reclaim_1m_mtf_gate1_v1`. `IBKR`,
  `US_EQUITY`, `single_stock`, `CEG`, `5m`, and `3 M` are provenance labels,
  not branch roots.
- Gate 1 survivor:
  `ibkr-ceg-power-infra-vwap-nr7-reclaim-5m-late_reclaim-3m-v1` had `75`
  trades over `64` trading days (`1.171875/day`), raw `+7.89%`,
  `1bps/side=+6.39%`, `2bps/side=+4.89%`, and `5bps/side=+0.39%`.
- Downstream verification:
  Auto-Quant import, prior init, seed analyze, workflow status, Pre-Bayes,
  structural target export, policy status, path-ranker train/apply, score
  import, trainer registration, runtime enable, post-ranker analyze, workflow,
  Pre-Bayes, policy, and final export all exited `0`; terminal metrics are in
  `checks/downstream_metrics.json`.
- Final readback:
  `exact_branch_survived=true`, but `execution_candidate_status=no_trade`,
  `execution_candidate_actionable=false`, `execution_readiness=0.20922802139752503`,
  `transition_hazard=1.0`, `pda_hybrid_alignment=false`,
  `path_ranker_score_visible_to_execution_tree=true`,
  `path_ranker_score_used_by_execution_tree=false`,
  `ranker_validation_ready=false`, `mature_rows=1`, and
  `history_mature_rows=2`.
- Decision:
  `exact_ceg_5m_late_reclaim_downstream_fail_closed`;
  `promotion_allowed=false`, `trade_usable=false`, `extension_complete=false`,
  and `update_goal=false`.

Interpretation:

- CEG 5m late-reclaim is a real IBKR hard-cost density observation with exact
  branch survival, but it fails the hard practical gates on execution
  materialization, mature rows, transition hazard, PDA alignment, and execution
  readiness. Preserve it as observation/repair evidence only. Do not promote,
  simulate-admit, lower gates, or treat it as a practical factor.

## Gate 1 Observation: IBKR VRT 1m Bayesian-Markov Trend Detector Drop

Append-only terminal row at `2026-05-23 03:52 +0800` for the IBKR `VRT`
data-center infrastructure Bayesian-Markov trend detector `1m`-origin full
ladder Gate 1 lane. This records provider/AQ/cost-gate evidence only; no
downstream, simulated admission, promotion, trade action, extension completion,
or goal update was launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T-current-codex-ibkr-vrt-bayesian-markov-trend-detector-1m-mtf-gate1.claim`.
- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T033648+0800-codex-ibkr-vrt-bayesian-markov-trend-detector-1m-mtf-gate1-v1`.
- Branch:
  `TrendExpansion -> DataCenterInfrastructureBayesianMarkovTrendContinuation ->
  bayesian_markov_trend_detector ->
  ibkr_vrt_bayesian_markov_trend_detector_1m_mtf_gate1_v1`. `IBKR`,
  `US_EQUITY`, `VRT`, data-center infrastructure label, and timeframes are
  provenance only.
- Verification:
  provider status, all seven IBKR fetches, strategy compile, Auto-Quant material
  batch, dispatch, and rank exited `0`; `branch_fields_preserved=true`;
  `rank_rows=7`; `rank_total_trade_count=0`.
- Gate 1 result:
  all ranked rows across `1m/5m/15m/30m/1h/4h/1d` produced `0` trades,
  `0.000` trades/day, `0.00%` raw, and no `1bps/2bps/5bps` survivor.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- VRT answered a fresh data-center infrastructure Markov/trend-continuation
  slot with real IBKR provider rows and clean AQ mechanics, but the exact branch
  generated no trades across the ladder. Preserve it as a density negative and
  do not downstream, promote, lower gates, or lightly retune this exact
  `VRT/1m/BayesianMarkovTrendDetector` cell.

## Gate 1 Observation: IBKR NTNX 1m Bayesian-Markov Trend Detector Provider Block

Append-only terminal row at `2026-05-23 07:43 +0800` for the IBKR `NTNX`
enterprise-cloud infrastructure Bayesian-Markov trend detector `1m`-origin full
ladder Gate 1 lane. This records provider/material blockage only; no Auto-Quant
rank evidence, downstream, simulated admission, promotion, trade action,
extension completion, or goal update was launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T071433+0800-codex-ibkr-ntnx-bayesian-markov-trend-detector-1m-mtf-gate1.claim`.
- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T072352+0800-codex-ibkr-ntnx-bayesian-markov-trend-detector-1m-mtf-gate1-v1`.
- Branch:
  `TrendExpansion -> EnterpriseCloudInfrastructureBayesianMarkovTrendContinuation ->
  bayesian_markov_trend_detector ->
  ibkr_ntnx_bayesian_markov_trend_detector_1m_mtf_gate1_v1`. `IBKR`,
  `US_EQUITY`, `NTNX`, enterprise-cloud infrastructure label, and timeframes
  are provenance only.
- Verification:
  provider status exited `0`, but every IBKR NTNX fetch across the retained
  ladder and retry windows exited `3` with `reqHistoricalData: Timeout`; the
  terminal metrics are in `checks/terminal_metrics.json`, provider readback is
  in `summaries/provider_provenance_matrix.csv`, and the terminal summary is in
  `summaries/terminal_decision_summary.md`.
- Gate 1 result:
  `provider_data_acquired_count=0`, `material_count=0`, `rank_rows=0`,
  `rank_total_trade_count=0`, `branch_fields_preserved=false`, and no
  `1bps/2bps/5bps` survivor exists.
- Decision:
  `provider_or_aq_blocked_no_gate1_verdict`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- NTNX is an infrastructure/provider blockage, not a Bayesian-Markov factor
  verdict. Do not call the factor negative, do not downstream, and retry only
  after the failed IBKR historical-data leg is repaired or a separately claimed
  provider lane supplies real retained material.

## Downstream Observation: Bybit APEUSDT 5m Elder Ray Power Fail-Closed

Append-only terminal row at `2026-05-23 07:44 +0800` for the reopened exact
Bybit `APEUSDT` Elder Ray bull/bear power `5m` lane. This is a scoped
higher-timeframe exact lane from prior subclass evidence; it does not rescue the
failed `1m` origin.

- Claims:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T-current-codex-bybit-apeusdt-5m-elder-ray-power-exact-gate1.claim`
  and
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T073943+0800-codex-bybit-apeusdt-5m-elder-ray-power-exact-downstream.claim`.
- Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T074059+0800-codex-bybit-apeusdt-5m-elder-ray-power-exact-gate1-v1`.
- Downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T074059+0800-codex-bybit-apeusdt-5m-elder-ray-power-exact-gate1-v1/downstream-exact-apeusdt-5m-elder-ray-power-20260523T074256+0800`.
- Branch:
  `MomentumContinuation -> CryptoElderRayPower ->
  elder_ray_bull_bear_power_5m_exact ->
  bybit_apeusdt_elder_ray_power_5m_exact_v1`. `Bybit`, `APEUSDT`,
  `linear_perp`, and `5m` are provenance labels, not branch roots.
- Gate 1 survivor:
  `bybit-apeusdt-5m-elder-ray-power-exact-v1` had `6` trades at
  `1.73/day`, raw `+0.72%`, `1bps/side=+0.60%`, `2bps/side=+0.48%`, and
  `5bps/side=+0.12%`; `branch_fields_preserved=true`.
- Downstream verification:
  Auto-Quant import/prior, analyze, workflow, Pre-Bayes, structural target
  export, policy, CatBoost/path-ranker train/apply/register/enable, post-ranker
  analyze/workflow/policy, and final metrics completed with
  `all_command_exits_zero=true`; terminal metrics are in
  `checks/downstream_metrics.json`.
- Final readback:
  `execution_candidate_actionable=false`, `execution_candidate_status=no_trade`,
  `execution_tree_gate_status=observe`, `execution_tree_branch=transition_guardrail`,
  `execution_readiness=0.487050812943753`,
  `transition_hazard=0.9875370421259952`, `pda_hybrid_alignment=false`,
  `path_ranker_score_visible_to_execution_tree=true`,
  `path_ranker_score_used_by_execution_tree=false`, and
  `ranker_validation_ready=false`.
- Decision:
  `exact_apeusdt_5m_elder_ray_downstream_fail_closed`;
  `promotion_allowed=false`, `trade_usable=false`, `extension_complete=false`,
  and `update_goal=false`.

Interpretation:

- APEUSDT 5m Elder Ray is a real higher-timeframe hard-cost survivor, but the
  exact downstream fails the practical gates on actionability, transition
  hazard, PDA alignment, readiness, and ranker validation. Preserve it as
  observation/repair evidence only. Do not promote, lower gates, or treat it as
  practical alpha.

## Gate 1 Observation: IBKR USDCHF 1m Bollinger Squeeze Mean-Reclaim Drop

Append-only terminal row at `2026-05-23 07:48 +0800` for the IBKR `USDCHF`
CASH/IDEALPRO MIDPOINT Bollinger squeeze mean-reclaim `1m`-origin full-ladder
Gate 1 lane. This records provider/AQ/cost-gate evidence only; no downstream,
simulated admission, promotion, trade action, extension completion, or goal
update was launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T071806+0800-codex-ibkr-usdchf-fx-bollinger-squeeze-mean-reclaim-1m-mtf-gate1.claim`.
- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T074020+0800-codex-ibkr-usdchf-fx-bollinger-squeeze-mean-reclaim-1m-mtf-gate1-v1`.
- Branch:
  `RangeReversion -> FxBollingerSqueezeMeanReclaim ->
  midpoint_bollinger_squeeze_mean_reclaim ->
  ibkr_usdchf_fx_bollinger_squeeze_mean_reclaim_1m_mtf_gate1_v1`. `IBKR`,
  `FX`, `USDCHF`, `CASH/IDEALPRO`, `MIDPOINT`, and timeframes are provenance
  only.
- Verification:
  provider status, retained IBKR ladder fetches, strategy compile, Auto-Quant
  material batch, dispatch, and rank produced terminal artifacts; terminal
  metrics are in `checks/terminal_metrics.json`, provider readback is in
  `summaries/provider_provenance_matrix.csv`, and rank rows are in
  `summaries/rank_rows.csv`.
- Gate 1 result:
  `provider_data_acquired_count=7`, `material_count=14`, `rank_rows=14`,
  `rank_total_trade_count=58`, `branch_fields_preserved=true`, but no
  `1bps/2bps/5bps` survivor exists. The two positive raw rows were consumed by
  costs: exact `1m/quality` had `5` trades (`0.416667/day`), raw `+0.07%`,
  `1bps=-0.03%`, `2bps=-0.13%`, and `5bps=-0.43%`; `5m/quality` had `4`
  trades (`0.051948/day`), raw `+0.01%`, and `5bps=-0.39%`.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- USDCHF reached real provider/AQ Gate 1, but the exact `1m` FX root did not
  survive hard cost stress or practical density. Preserve it as an FX
  Bollinger-squeeze negative sample and do not downstream, promote, lower gates,
  or clone this exact low-excursion shape without a materially wider excursion
  hypothesis.

## Terminal Diagnostic: Bybit LINKUSDT Volatility Pullback Full-Ladder Origin Block

Append-only terminal row at `2026-05-23 10:58 +0800` for the Bybit public
`LINKUSDT` volatility-compression pullback/reclaim full-ladder repair
diagnostic. This diagnostic tested whether the prior exact `1h` downstream
blocker could be repaired by replacing the single-file MTF replay with distinct
real ladder frames. It did not run downstream, simulated admission, promotion,
trade action, extension completion, or goal update.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T105627+0800-codex-bybit-linkusdt-vol-pullback-full-ladder-repair-diagnostic.claim`.
- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T105638+0800-codex-bybit-linkusdt-vol-pullback-full-ladder-repair-diagnostic-v1`.
- Branch:
  `Transition -> CryptoVolatilityCompression -> bbwidth_pullback_reclaim_full_ladder_repair_diag -> bybit_linkusdt_volatility_pullback_reclaim_full_ladder_repair_diag_v1`. `Bybit`, `LINKUSDT`, linear perp, and timeframes are provenance labels only.
- Verification:
  Bybit linear fetches succeeded for `1m/5m/15m/30m/1h/4h/1d` with retained rows
  `1000/1000/1000/1000/1000/535/90`; strategy compile, material batch,
  dispatch, and rank exited `0`; branch fields were preserved. The `1d`
  market-state validation exited `1`, so `all_commands_ok=false` and the row is
  diagnostic-only.
- Gate result:
  exact `1m` had `1` trade, raw `-0.10%`, `1bps=-0.12%`, `2bps=-0.14%`,
  `5bps=-0.20%`; `5m` had `3` trades, raw `-0.49%`, `5bps=-0.79%`. Higher
  timeframe subclass rows survived hard cost stress, led by `1h` with `8`
  trades, raw `+3.07%`, `5bps=+2.27%`, and `30m` with `6` trades, raw
  `+1.87%`, `5bps=+1.27%`, but those do not repair the failed `1m` origin.
- Decision:
  `higher_timeframe_subclass_only_origin_blocked`;
  `downstream_allowed=false`, `promotion_allowed=false`, `trade_usable=false`,
  `extension_complete=false`, and `update_goal=false`.

Interpretation:

- The prior LINKUSDT exact `1h` branch remains useful subclass/repair evidence,
  but the full-ladder diagnostic confirms it cannot be treated as an
  extension-ready practical factor. A full-market/full-product/full-symbol/full-cycle
  extension cannot start from this root because the `1m` origin and `5m` sibling
  fail hard economics before downstream gates.

## Downstream Observation: IBKR AMD 30m Opening-Drive RVOL Exact Fail-Closed

Append-only terminal row at `2026-05-23 13:18 +0800` for existing IBKR `AMD`
single-stock opening-drive RVOL breakout exact `30m` evidence. This row consumes
already completed Gate 1 and same-root downstream artifacts that were present in
the run tree but not exposed by the compact Board B tail; no new provider fetch,
Auto-Quant rerun, simulated admission, promotion, trade action, extension
completion, or goal update was launched.

- Claim:
  `/tmp/ict-engine-agent-claims/board-b-factor-refinement/20260523T-current-codex-ibkr-amd30m-existing-downstream-terminalization.claim`.
- Latest Gate 1 root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T192735+0800-codex-ibkr-amd-30m-opening-drive-rvol-breakout-exact-gate1-v1`.
- Latest downstream root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260522T192735+0800-codex-ibkr-amd-30m-opening-drive-rvol-breakout-exact-gate1-v1/downstream-exact-amd-30m-20260522T201417+0800`.
- Branch:
  `TrendExpansion -> SingleStockOpeningDrive -> rvol_breakout_pullback -> ibkr_amd_opening_drive_rvol_breakout_30m_v1`. `IBKR`, `US_EQUITY`,
  `single_stock`, `AMD`, and `30m` are provenance labels, not branch roots.
- Gate 1 survivor:
  `ibkr-amd-opening-drive-rvol-breakout-exact-30m-3m-v1` had `17` trades,
  `76.4706%` win rate, raw `+4.43%`, `2bps/side=+3.75%`, and
  `5bps/side=+2.73%`.
- Downstream verification:
  Auto-Quant import, prior init, analyze, workflow status, Pre-Bayes,
  structural target export, policy status, CatBoost/path-ranker train/apply,
  score import, trainer registration, runtime enable, post-ranker analyze,
  workflow, and policy all exited `0`; terminal metrics are in
  `checks/downstream_metrics.json`.
- Final readback:
  Pre-Bayes gate stayed `observe_only`; canonical posterior stayed `range` with
  confidence `0.33786182622465244` and probabilities
  `range=0.34660661700033757`, `transition=0.23107107800022503`,
  `trend=0.23107107800022503`, `stress=0.1912512269992124`; execution candidate
  stayed `actionable=false` / `status=no_trade`; execution tree stayed
  `gate=observe`, branch `transition_guardrail`, hint
  `execution_guarded_due_to_pda_hybrid_disagreement`; path-ranker was visible
  but not used by the execution tree; `mature_rows=0` and
  `history_mature_rows=0`.
- Decision:
  `exact_amd_30m_opening_drive_rvol_downstream_fail_closed`;
  `promotion_allowed=false`, `trade_usable=false`, `extension_complete=false`,
  and `update_goal=false`.

Interpretation:

- AMD 30m is a real IBKR exact hard-cost survivor and the full same-root
  downstream chain ran cleanly, but it is not practical/live-ready. It fails on
  execution actionability, PDA disagreement, path-ranker use, and mature
  validation. Preserve it as a TrendExpansion / single-stock opening-drive
  subclass repair lead only; do not duplicate Gate 1, simulate-admit, lower
  gates, or promote before current mature validation and actionable execution
  materialize on the exact branch.

## Gate 1 Observation: IBKR EWM 1m Bayesian-Markov Trend Detector Drop

Append-only terminal row at `2026-05-23 19:27 +0800` for the IBKR `EWM`
Malaysia ETF Bayesian-Markov trend detector `1m`-origin full-ladder Gate 1
lane. This records provider/AQ/cost-gate evidence only; no downstream,
simulated admission, promotion, trade action, extension completion, or goal
update was launched.

- Run root:
  `support/docs/experiments/actionable-regime-confidence/runs/20260523T190542+0800-codex-ibkr-ewm-bayesian-markov-trend-detector-1m-mtf-gate1-v1`.
- Branch:
  `TrendExpansion -> MalaysiaEtfBayesianMarkovTrendContinuation ->
  bayesian_markov_trend_detector ->
  ibkr_ewm_bayesian_markov_trend_detector_1m_mtf_gate1_v1`. `IBKR`,
  `US_EQUITY`, `ETF`, `EWM`, Malaysia ETF label, and timeframes are provenance
  labels, not branch roots.
- Verification:
  seven IBKR ladder fetches completed with retained rows across
  `1m/5m/15m/30m/1h/4h/1d`; strategy compile, Auto-Quant material batch,
  dispatch, and rank completed; `commands_all_zero=true`,
  `branch_fields_preserved=true`, `provider_rows=7`, `material_count=28`,
  `rank_rows=28`, and `rank_total_trade_count=918`.
- Gate 1 result:
  the exact `1m` origin had no hard-cost density survivor. Representative rows:
  `1m/balanced` had `46` trades over `30` trading days (`1.533/day`), raw
  `+1.06%`, `1bps=+0.14%`, `2bps=-0.78%`, and `5bps=-3.54%`; `1m/wide` had
  `25` trades (`0.833/day`), raw `-0.60%`, `1bps=-1.10%`, `2bps=-1.60%`, and
  `5bps=-3.10%`. Higher timeframe positives such as `5m/wide` at `60` trades
  and `5bps=+2.30%` are subclass observations only and do not rescue the failed
  exact `1m` origin.
- Decision:
  `drop_gate1_no_exact_1m_5bps_density_survivor`;
  `downstream_allowed=false`, `pre_bayes_allowed=false`,
  `bbn_allowed=false`, `catboost_allowed=false`,
  `execution_tree_allowed=false`, `promotion_allowed=false`,
  `trade_usable=false`, `extension_complete=false`, and `update_goal=false`.

Interpretation:

- EWM produced a clean real-provider/AQ Bayesian-Markov trend detector packet,
  but the exact `1m` origin failed hard `5bps/side` economics. Preserve it as
  negative trend-continuation evidence for this rooted branch. If the `5m`
  positive sibling is reused, it must become a separate exact timeframe lane
  with its own claim, cost-density test, positive/negative feedback replay, and
  downstream decision; do not lower promotion gates or call this `1m` root
  practical.
