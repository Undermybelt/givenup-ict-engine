# Source-Backed MainRegimeV2 Root Materialization

This run materializes the 2026-05-11 source-backed root ontology and reruns unchanged 95% gates. It also probes public Kraken trades/depth for direct manipulation inputs, but keeps Manipulation fail-closed because the public probe is not an aligned historical order-lifecycle/L2 calibration set.
