# Source Label Macro Context Rule Miner v1

Run id: `20260512T052301-codex-source-label-macro-context-rule-miner-v1`

Gate result: `source_label_macro_context_rule_miner_v1=aborted_nonterminal_superseded_no_evidence`

## Decision

This root is closed as non-counting. The initial macro/context rule miner was stopped by its owning agent after it remained nonterminal for several minutes and a newer numeric-tree confidence probe was already running in parallel.

No rows were scored, no confidence labels were accepted, no source/control evidence was acquired, no target roots were mutated, no canonical merge or downstream promotion rerun occurred, and `update_goal=false`.

## Boundary

Treat this root only as cleanup for an in-flight marker. It is not accepted regime-confidence evidence, not source/control evidence, not canonical merge input, not downstream promotion evidence, not trade evidence, and not completion authorization.
